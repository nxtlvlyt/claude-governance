MISSION-ID: M-MUDDYTIRES.MT-CUTOVER-FUEL-CHIP-FIX-2026-06-23.S1
PARENT: M-MUDDYTIRES.MT-CUTOVER-FUEL-CHIP-FIX-2026-06-23
TARTIB-INDEX: 1 of 2
REQUIRES: none (first in tartib order)
STEPS: 8

PARENT MAQSAD: (see parent mission)

Maqsad: sub-mission 1 of 2 — AUTH GATE (FIRST, fail-closed): confirm wrangler is authenticated as markabass@gmail.com before touching anything; also ensures _logs/ exists for later tees. If the email is absent (wrong/expired token) the witness exits non-zero -> HALT WITH RECEIPT. whoami does not expose token scopes; pages(write) is proven implicitly by a later non-403 deploy. through RESOLVE the project's production branch from substrate (do NOT guess 'main'; do NOT omit --branch — omission infers the current git branch and lands in PREVIEW). Writes the production branch name to a file the deploy step reads. Empty result -> exit non-zero. NOTE: the --json field path deployment_trigger.metadata.branch is UNVERIFIED; fail-closed if the schema differs.

Steps:
  1. AUTH GATE (FIRST, fail-closed): confirm wrangler is authenticated as markabass@gmail.com before touching anything; also ensures _logs/ exists for later tees. If the email is absent (wrong/expired token) the witness exits non-zero -> HALT WITH RECEIPT. whoami does not expose token scopes; pages(write) is proven implicitly by a later non-403 deploy. [verify] _logs/mt-cutover-fuel-chip-fix.whoami.txt
  2. PRECHECK: checkout the proven fix branch and assert HEAD is the proven artifact sha 340b98e. Mismatch (wrong branch / local behind remote) -> exit non-zero -> HALT WITH RECEIPT. [command] _logs/mt-cutover-fuel-chip-fix.head.txt
  3. PROOF-TOKEN GATE: confirm the live-verify witness token 'hasVehicleProfile' actually exists in the committed source at 340b98e, so the final live gate is falsifiable. If absent, the mission's witness is mis-specified -> HALT WITH RECEIPT before any deploy. NOTE: served path js/plan-day.js is UNVERIFIED (file_read unavailable) — must match the live /js/plan-day.js path. [verify] js/plan-day.js
  4. DIFF-SCOPE GUARD (blast-radius / REVERSIBLE constraint): prove the change vs integration/apex-2026-06-22 is EXACTLY one file (plan-day.js) and contains the showFuel/hasVehicleProfile change. Any other touched file (backend Workers/accounts/other features must be UNTOUCHED) -> exit non-zero -> HALT WITH RECEIPT. [verify] _logs/mt-cutover-fuel-chip-fix.diffscope.txt
  5. Obtain a Pages preview URL for the fix branch via scripts/qc-deploy-preview.mjs, writing it to a file later steps read (no cross-step shell vars). Silent-failure guard: assert a *.pages.dev URL was captured, else exit non-zero. NOTE: script path/output shape UNVERIFIED. [command] _logs/mt-cutover-fuel-chip-fix.preview-url.txt
  6. SOTA 8-gate QC (Endpoint, Leak, Console, Panel, Sentry, VisReg, Lighthouse, Bundle) against the preview URL — runs FIRST and BLOCKS staging/deploy. Two assertions: harness actually produced gate output (guards silent failure by requiring the last gate name 'Bundle'), AND no gate emitted 'BLOCK'. Any BLOCK -> exit non-zero -> HALT WITH RECEIPT (operator decides override). NOTE: harness path/flags UNVERIFIED. [verify] _logs/mt-cutover-fuel-chip-fix.qc.txt
  7. MERGE the fix branch into integration/apex-2026-06-22 with --no-ff. Witness asserts a merge commit now exists at HEAD; a conflict leaves no merge commit -> exit non-zero -> HALT WITH RECEIPT (do not resolve conflicts without operator direction). [command] _logs/mt-cutover-fuel-chip-fix.merge.txt
  8. RESOLVE the project's production branch from substrate (do NOT guess 'main'; do NOT omit --branch — omission infers the current git branch and lands in PREVIEW). Writes the production branch name to a file the deploy step reads. Empty result -> exit non-zero. NOTE: the --json field path deployment_trigger.metadata.branch is UNVERIFIED; fail-closed if the schema differs. [command] _logs/mt-cutover-fuel-chip-fix.prodbranch.txt

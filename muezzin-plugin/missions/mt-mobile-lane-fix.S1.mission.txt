MISSION-ID: mt-mobile-lane-fix.S1
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - map.html
VISUAL-QC-REQUIRED
REQUIRES: mt-mobile-qc-hardening.S1.S1 (tartib — the overlap detector lands first and then VERIFIES this fix)
STEPS: 6

Maqsad: The D1 half of the operator's 2026-07-03 mobile audit ("will the zoom buttons always
block the words? the magnifying glass always block the zoom buttons?"), SPLIT OUT of
mt-mobile-qc-hardening.S1.S1 after x2 receipts showed dense in-place edits to the 393KB
map.html dying in the known windowed-edit engine gap (INBOX HIGH 2026-06-18). Deliverable:
the top-left control lane (site-search FAB, Leaflet zoom column, control buttons) stacks
WITHOUT intersection at 375px-wide viewports and above.
ADDITIVE-ONLY CONSTRAINT (the receipted cure for map.html — reviews/add-spot/confidence-badge
all landed this way while in-place reworks died): the edit APPENDS one new, self-contained
<style id="mt-mobile-lane-fix"> block immediately before </head> containing a single
@media (max-width: 480px) rule-set that repositions the lane elements by their EXISTING
ids/classes (absolute offsets + a declared z-index order). Nothing already present in
map.html is modified or deleted — the new block wins by cascade order and specificity. The verify step
anchors on the literal id string "mt-mobile-lane-fix" so edit and verify cannot disagree
(the x2's attempt-1 killer was a plan asserting a breakpoint string its own edit never wrote).

niyyah: One appended style block a 4-line diff can review, verified by the overlap detector
that lands before it (tartib) — the robots check the fix on the phone the operator no longer
has to be. Scope every assertion to map.html only.

Steps:
  1. Idempotent baseline: record branch + HEAD; if map.html already contains id="mt-mobile-lane-fix" output BLOCK_PRESENT (NOT verify-only — amended 19:1x: the existing block is live and INSUFFICIENT, receipted by the operator's screenshot pair #mt-search-icon over .mt-locate; step 2 on a BLOCK_PRESENT pass EDITS the existing block's rules in place, keeping the same id, correcting offsets/z-order for the named selectors); else CLEAN_BASELINE (step 2 appends the block fresh). Never exit 1 on existence. [command]
  2. LITERAL BLOCK CONTENTS PINNED 2026-07-15T07:0xZ (conductor dry-run receipts, attempt-4 diagnosis: the committed block was FABRICATED CSS — its #site-search-fab and .leaflet-control-buttons .leaflet-control selectors match NOTHING in map.html; its .leaflet-top.leaflet-left top:5px !important LOSES to ui-chrome.js line 421's runtime-injected top:auto!important;bottom:60px!important (later same-specificity !important wins the cascade, so the appended block can never move the lane without HIGHER specificity); and its .leaflet-control-zoom top:60px absolute rule CREATED the filtbtn/zoom collision. The block below was validated LIVE on the preview deploy /map at 375x812 isMobile via injected style: PAIR_SEARCH_LOCATE=clear PAIR_FILT_ZOOM=clear PAIR_SEARCH_ZOOM=clear PAIR_LOCATE_ZOOM=clear LOCATE_SIZE=90x44 SEARCH_SIZE=44x44. The body #map selector prefixes are LOAD-BEARING specificity — do not simplify them.) Replace the ENTIRE contents of the existing <style id="mt-mobile-lane-fix"> block (same id, same position) with exactly:
     @media (max-width: 480px) {
       /* Lane stays bottom-anchored (agrees with ui-chrome's injected rule; body #map out-specifies it) */
       body #map .leaflet-top.leaflet-left { top: auto !important; bottom: 60px !important; z-index: 900 !important; }
       /* Zoom column IN FLOW inside the lane, stacking with .mt-locate instead of absolute-escaping into #filtbtn */
       body #map .leaflet-top.leaflet-left .leaflet-control-zoom { position: relative !important; top: auto !important; left: auto !important; margin: 8px 0 0 10px !important; z-index: 800 !important; }
       /* Search FAB lifted above the lane's grown stack; 44px tap floor (operator receipt 2026-07-03 ~19:2x) */
       body #mt-search-icon { left: 10px !important; bottom: 270px !important; z-index: 1101 !important; min-width: 44px !important; min-height: 44px !important; padding: 11px !important; box-sizing: border-box !important; }
       /* Tap-target floor on My location */
       body .mt-locate { min-width: 44px !important; min-height: 44px !important; padding: 11px 10px !important; box-sizing: border-box !important; z-index: 1002 !important; }
     }
     Nothing outside the block changes (additive-only constraint stands). [edit] map.html
  3. Verify map.html contains the literal string mt-mobile-lane-fix AND that the block sits inside <head> (Select-String receipts for both), and confirm via git diff --stat that map.html shows ONLY additions (no deleted lines). [verify]
  4. Scoped cleanliness (LITERAL COMMAND PINNED 2026-07-15T04:4xZ — attempt 3 receipt: despite the twice-re-pinned prose above stating the correct invariant, the seat improvised the OLD expected-dirty polarity a THIRD time ("if (-not (git status --porcelain -- map.html)) { exit 1 }"), the receipted mt-addspot class; prose invariants do not survive seat improvisation, only literal commands do. The invariant, unchanged: NOTHING ELSE changed — only foreign (non-map.html) dirt fails; map.html dirty or clean are BOTH correct). Run exactly: $foreign = @(git status --porcelain -uno -- ':!map.html'); if ($foreign.Count -gt 0) { Write-Output "FOREIGN-TRACKED-DIRT: $($foreign -join '; ')"; exit 1 }; Write-Output SCOPED_CLEAN [verify]
     (PATH-SCOPED 2026-07-15T06:1xZ per the plan gate's SECOND identical refusal — its rule is structural: no whole-tree porcelain boolean in a shared repo, scope by pathspec. -uno suppresses ambient untracked strays; the ':!map.html' exclude pathspec lists only foreign TRACKED modifications — the nothing-else-changed invariant in the gate's own prescribed shape. Prior 05:4x row-filter amendment half-read the prescription.)
  5. git commit map.html by explicit name (git add map.html; git commit -m "<message naming this mission, the parent audit receipt, and the additive-only pattern>"); confirm the block is committed at HEAD. IDEMPOTENT on a CLEAN_ALREADY pass: if git status --porcelain -- map.html is empty AND git show HEAD:map.html contains mt-mobile-lane-fix, output ALREADY_COMMITTED and exit 0 WITHOUT running git commit (a no-change commit exits 1 and is not a failure of this mission). [command]
  6. OUTCOME CHECK (added 2026-07-03T13:2x — RULE 11 caught this mission as grep-only before first fire; panels correctly reject visual missions with zero execution) (PAIR ACCEPTANCE PINNED 19:1x — operator screenshot receipt: the search magnifier sits ON TOP of the "My location" control on his phone, the EXACT pair the detector preflight flagged: "OVERLAP: #mt-search-icon intersects .mt-locate in mobile viewport"; the current live block does NOT fix this pair, so block-presence alone is a hollow pass): in ONE self-contained command (the proven trip-cost shape): confirm git status --porcelain -- map.html is empty (step 5 committed), run wrangler pages deploy . --project-name=muddytires --branch=preview, capture the emitted https://<subdomain>.muddytires.pages.dev URL into a variable, poll "$url/map" until HTTP 200 (up to 10x5s), write scratch-lanefix-qc.mjs via here-string (playwright, viewport 375x812 isMobile: page.goto($env:MT_PREVIEW_URL + '/map', {waitUntil:'domcontentloaded', timeout:45000}), then page.waitForSelector('#map, .leaflet-container', {state:'visible', timeout:60000}) — TIMEOUT PINNED 2026-07-15T07:0xZ: attempt 4's three step-retries each died at a 15000ms waitForSelector on the cold fresh-hash deploy while the conductor's warm-alias runs passed in under 15s; 60s absorbs first-hit cold start (secondary defect; the primary was the fabricated CSS, fixed in step 2), then wait ~2s for control layout to settle, read the boundingBoxes of #mt-search-icon, .mt-locate, #filtbtn and .leaflet-control-zoom, then print evidence lines STYLE_BLOCK=<present|absent>, PAIR_SEARCH_LOCATE=<clear|OVERLAP>, PAIR_FILT_ZOOM=<clear|OVERLAP>, LOCATE_SIZE=<WxH>, SEARCH_SIZE=<WxH>, PAGEERRORS=<count>, then LANEFIX_QC_PASS only when the block is present AND both pairs are clear AND both sizes are >=44x44 AND pageerrors is 0 — if a pair overlaps, the CSS in step 2 must be CORRECTED for those selectors and the mission re-runs its edit, never passes on presence alone), run it with $env:MT_PREVIEW_URL = $url; node scratch-lanefix-qc.mjs, then Remove-Item the scratch. The printed evidence lines ARE the render receipt. [command]

Done means: map.html committed at HEAD carrying the appended mt-mobile-lane-fix style block
(additions-only diff), AND the engine's headless-browser render of /map (visual witness
against the auto-provisioned preview) at the mobile viewport shows the top-left control lane
collision-free — with the sibling detector mission's overlap check (which lands first by
tartib) as the standing regression guard. Executable + headless-browser render verification
(live-outcome bar, 2026-07-03).

MISSION-CLASS: ops-deploy (PRODUCTION CUTOVER — wrangler pages deploy to the live muddytires Pages project; touches muddytires.ca; REVERSIBLE via Pages deployment history)
MISSION-ID: M-MUDDYTIRES.MT-CUTOVER-1-MAPLANDING-LIVE
REPO-ROOT: E:\AI_Storage\muddytires-frontend-wt
DEPLOY-PROJECT: muddytires   (Pages; domains muddytires.pages.dev + muddytires.ca; direct-upload, Git Provider = No)
PROVEN-ARTIFACT: branch oracle-frontend-swap @ commit 2ad8764 (== preview deployment d5de7dc6, verified live on oracle-landing-preview.muddytires.pages.dev)

# WHY: Operator authorized 2026-06-18 (decision: "map+landing -> live"). The map+landing on oracle-frontend-swap are proven on
# the preview; production muddytires.ca still serves the OLD existing map. This cuts the proven map+landing over to the live apex.
# Backend Workers + accounts stay on TEST (operator chose map+landing ONLY). Command-class: deterministic wrangler steps. The
# live-verify gate (step 5) is what protects production: a wrong-branch deploy lands as a PREVIEW and leaves muddytires.ca
# unchanged, so the gate fails CLOSED with no harm.

Niyyah: cut the PROVEN oracle-frontend-swap map+landing over to live muddytires.ca, reversibly. Capture the rollback ref BEFORE
deploying; deploy ONLY the proven artifact (commit 2ad8764, the source of preview d5de7dc6); prove the cutover by fetching the
live apex AFTER. Never declare success without the live apex serving the new content. Backend/accounts UNTOUCHED (map+landing only).

GROUND IN (read before acting — REQUIRED):
- D:\Desktop\MuddyTires-Status.md (the operator-facing inventory: "Live vs Preview" section — confirms muddytires.ca UNCHANGED and
  the oracle-frontend-swap preview is the proven frontend).
- `wrangler whoami` must show markabass@gmail.com with pages(write). If not, HALT WITH RECEIPT (wrong/expired token).

Maqsad: ONE deliverable — muddytires.ca serves the proven oracle-frontend-swap map (/map) + landing (/) ; nothing else changed. Steps (run from REPO-ROOT, all deterministic):
1. PRECHECK: `cd E:\AI_Storage\muddytires-frontend-wt && git rev-parse --abbrev-ref HEAD` MUST print `oracle-frontend-swap` and
   `git rev-parse HEAD` MUST start `2ad8764`. If not, checkout oracle-frontend-swap and re-verify. HALT WITH RECEIPT on mismatch.
2. ROLLBACK REF (capture BEFORE any deploy): `wrangler pages deployment list --project-name muddytires > _logs/mt-cutover-1.predeploy.txt`.
   Extract the most recent Environment=Production deployment id into ROLLBACK_ID (if none exists, record "ROLLBACK_ID=none (first
   production deploy — apex currently old-map)"; that is a SAFE state, not an error). Echo ROLLBACK_ID into the receipt.
3. STAGE the deploy dir = the worktree's servable static set (index.html [landing-at-root], map.html, landing.html, about.html,
   partners.html, guides.html, quirky.html, pois.json, img/, robots.txt, og-cover.png if present). Do NOT include workers/, d1/,
   .git, node_modules. Confirm index.html is the LANDING (contains the hero band / "free" + "sourced legality" copy), not the map.
4. DEPLOY TO PRODUCTION: `wrangler pages deploy <staged-dir> --project-name muddytires --branch <PRODUCTION_BRANCH> --commit-dirty=true`
   where PRODUCTION_BRANCH is this project's production branch (resolve from `wrangler pages project list` / project config; it is
   the branch whose deployments show Environment=Production). Capture full stdout (the new deployment id + url) to the receipt.
5. VERIFY LIVE (positive-assertion, EXIT NON-ZERO ON ABSENCE — this gate protects production):
   - `curl -sS -o /dev/null -w "%{http_code}" https://muddytires.ca/` == 200 AND the body contains the NEW landing markers
     (hero band copy: sourced legality / free / no-login) — NOT the old existing-map index.
   - `curl -sS https://muddytires.ca/map` (or /map.html) returns 200 and contains the Oracle control / Crown-land markers.
   - Confirm the new deployment's Environment == Production (`wrangler pages deployment list` top row). If it is Preview, the apex
     did NOT change -> FAIL with receipt (wrong branch); production is intact, requeue with the corrected PRODUCTION_BRANCH.
6. RECORD rollback instructions into _logs/mt-cutover-1.receipt.md: "To revert: redeploy ROLLBACK_ID via
   `wrangler pages deployment ...` or promote it in the dashboard pages/view/muddytires/<ROLLBACK_ID>."

Done means: https://muddytires.ca/ serves the new landing and /map serves the new Oracle map (both 200, new-content-verified),
the new deployment is Environment=Production, ROLLBACK_ID + revert steps are recorded. Backend Workers, D1, and accounts UNTOUCHED.

Constraints: REVERSIBLE-ONLY (rollback ref captured in step 2 BEFORE deploy — no deploy may run before the ref is recorded);
map+landing ONLY (no worker/D1/accounts changes); live-verify gate is FAIL-CLOSED (no success claim without the live apex serving
new content); deterministic command-class (no model-emit authoring); if the deploy lands as Preview, that is a clean no-harm FAIL.

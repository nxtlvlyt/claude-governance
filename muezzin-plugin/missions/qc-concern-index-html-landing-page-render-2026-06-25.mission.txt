MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.QC-CONCERN-INDEX-HTML-LANDING-PAGE-RENDER-2026-06-25
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: feat/qc-concern-index-html-landing-page-render-2026-06-25
ALLOW-FILES:
  - js/*.js
  - workers/**/*.js

Niyyah: address the QC CONCERN named "index.html landing page render" from the 2026-06-25 walk.

Maqsad: GATE: Page returns 200, parses without console errors — FAIL: Console reports a 401 error (Unauthorized) during resource load. Stat hydration and Hidden cards remain hidden PASS, so the page renders correctly but emits an unauthorized fetch that pollutes the console for unauthenticated visitors on the public landing. Identify the resource and either remove the unauthenticated call, swap to a public endpoint, or guard it behind a logged-in check so the landing console is clean. Restore behavior to that specified in docs/FEATURE-CATALOG-2026-06-23.md.

Done means: e2e-runner walk returns {clean}; the concern is resolved; no other features regress.

Amanah: this slice is part of the muddytires.ca quality-uplift backlog.

MODEL-ROUTING: TIER_CLOUD_CODE — kimi-k2.7-code or deepseek-v4-pro for fix authoring.

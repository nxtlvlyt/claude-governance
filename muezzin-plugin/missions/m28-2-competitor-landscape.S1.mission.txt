MISSION-ID: M-MUDDYTIRES.COMPETITOR-LANDSCAPE.2.S2.S1
MISSION-CLASS: research (search-grounded; produces ONE markdown card; no code, no live-site touch)
PARENT: M-MUDDYTIRES.COMPETITOR-LANDSCAPE.2.S2  (sub-mission 1 of 2, auto-split by the size ceiling)
REQUIRES: none (first sub-mission of 2 in tartib order)

Niyyah: arm the muddytires positioning to beat the WHOLE competitive field, not just iOverlander — by knowing, for each

Maqsad: this is part 1 of 2 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): this is part 2 of 2 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - Chain-integrity guard: assert the predecessor (S1) deliverable competitor-landscape-2026.md exists in cwd and is substantive (>=30 lines), so the gate never green-lights against a missing file or a stub. S2 owns ONLY the completeness gate; it does NOT author this file (S1 does) — here it is a read-only predecessor input. Path is cwd-relative and UNVERIFIED (file_read unavailable); fails closed if absent.
  - Assert all SIX competitor sections are present (>=6 markdown '##' section headers). Witnesses one-per-competitor coverage at the structural level; fails non-zero if fewer than 6 sections exist.
  - COMPLETENESS GATE field 1 (Pricing): assert the pricing field label appears at least 6 times (one per competitor). Exact label string is UNVERIFIED (file_read unavailable, spec truncated) — tolerant regex used to reduce false-fail; still fails closed when under-covered.
  - COMPLETENESS GATE field 2 (Free / Boondocking coverage): assert the free/dispersed-camping field label appears at least 6 times (one per competitor). Label UNVERIFIED; tolerant regex; fails closed when under-covered.
  - COMPLETENESS GATE field 3 (Canadian coverage): assert the Canadian-coverage field label appears at least 6 times (one per competitor). Label UNVERIFIED; tolerant regex; fails closed when under-covered.
  - COMPLETENESS GATE field 4 (Key Weakness): assert the per-competitor weakness field label appears at least 6 times (one per competitor). Label UNVERIFIED; tolerant regex; fails closed when under-covered. Distinct from the synthesis-level honest 'where they beat us' note checked in step 8.
  - COMPLETENESS GATE field 5 (MuddyTires counter / positioning): assert the muddytires-counter field label appears at least 6 times (one per competitor). Label UNVERIFIED; tolerant regex; fails closed when under-covered.
  - COMPLETENESS GATE honest-weakness note: assert the intellectually honest 'where they beat us' note is present at least once. Fails non-zero if the note is absent — the core mission requirement that the card admits real competitor advantages.

Done means: steps M-MUDDYTIRES.COMPETITOR-LANDSCAPE.2.S2.S1.1..M-MUDDYTIRES.COMPETITOR-LANDSCAPE.2.S2.S1.8 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — step M-MUDDYTIRES.COMPETITOR-LANDSCAPE.2.S2.1 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — competitor-landscape-2026.md exists, covers all 6 competitors with the fields above, every factual claim is
either source-URL-cited or marked "(inference)", includes the synthesis + the honest "where they beat us" note. If a
competitor genuinely can't be researched (no results), say so explicitly rather than inventing facts.

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
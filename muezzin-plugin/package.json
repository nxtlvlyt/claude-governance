{
  "name": "muezzin-plugin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Muezzin mission engine. NOTE: this package.json is NEW (the live plugin had none) — it exists to (1) declare type:module so the engine's .mjs/.js modules resolve consistently, and (2) install jsdom so runtime_verify's HTML load-check is ACTIVE, not dormant (defect 1b). Run `npm i` in the plugin dir after the conductor applies this file.",
  "devDependencies": {
    "jsdom": "^25.0.1"
  }
}

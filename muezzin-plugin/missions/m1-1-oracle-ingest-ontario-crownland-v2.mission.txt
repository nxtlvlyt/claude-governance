MISSION-CLASS: code-repo (data ingest into D1 via wrangler + positive-assertion verify; ADDITIVE — new table + one status doc; no live-site/index.html/pois touch)
MISSION-ID: M-MUDDYTIRES.M1-1.ORACLE-INGEST-ONTARIO-CROWNLAND.V2
REPO-ROOT: E:\AI_Storage\muddytires-d1-wt
ALLOW-FILES: d1/crownland-ingest.md

# SUPERSEDES M1.1 v1 (FAILED x2). DIAGNOSIS from v1 receipts: v1 got 4/5 steps RIGHT — it fetched REAL Ontario CLUPA
# crown-land data from the live LIO ArcGIS service and CREATED the crown_land_policy table on the remote D1. It FAILED
# only at step 5 (LOAD): the INSERT was built by string-concatenating feature NAME_ENG values WITHOUT escaping single
# quotes, so Ontario area names containing apostrophes broke the SQL. v1 attempt-2 then churned because SearXNG went
# BLIND mid-replan (now HEALED — searxng restarted, 39 results on control query). This v2 keeps v1's working steps and
# fixes ONLY the LOAD-escaping, which is the one real defect.

Niyyah: land real Ontario Crown-land policy data in our own D1 — the first brick of the Oracle no competitor has — and
prove it queryable, with the INSERT properly escaped so apostrophe area names can't break it.

Maqsad: ONE deliverable — a populated D1 table `crown_land_policy` holding a small real subset of Ontario CLUPA crown-land
policy areas + d1/crownland-ingest.md documenting the verified row count + source. Steps (each positive-assertion, EXIT
NON-ZERO on absence):
1. PREFLIGHT: wrangler authed + muddytires-pois D1 reachable on --remote (SELECT 1).
2. FETCH a small real CLUPA subset from the live Ontario LIO ArcGIS REST service (the "Crown land use policy area
   (provincial)" layer — v1 successfully used ws.lioservices.lrc.gov.on.ca/arcgis2/rest/services/LIO_OPEN_DATA layer;
   use a central-Ontario bounding box + a 25-record cap, f=json) into d1/clupa-raw.json. Assert features.length > 0.
3. CREATE TABLE IF NOT EXISTS crown_land_policy (id INTEGER PRIMARY KEY, area_name TEXT, designation TEXT, policy_summary
   TEXT, geometry_json TEXT) on muddytires-pois --remote. Idempotent.
4. LOAD — build INSERT statements from d1/clupa-raw.json, BUT **ESCAPE every text value for SQL: replace each single
   quote ' with two single quotes '' before embedding** (PowerShell: ($s -replace "'","''")). Apply to area_name,
   designation, policy_summary, and geometry_json. Execute the INSERTs on --remote. (THIS is the v1 fix — unescaped
   apostrophes were the only failure.)
5. VERIFY (positive-assertion, --remote): SELECT COUNT(*) AS n FROM crown_land_policy, assert n > 0 (EXIT 1 if 0).
6. WRITE d1/crownland-ingest.md: verified row count, the source service URL + bbox, timestamp — each number confirmed by
   the fail-on-absence query.

Done means: crown_land_policy on the remote muddytires-pois D1 with n > 0 real Ontario CLUPA rows, d1/crownland-ingest.md
documents it, live site + pois untouched. If the LIO service is unreachable or returns 0, FAIL HONESTLY at step 2 with the
exact URL tried.

Constraints: ADDITIVE; positive-assertion verifies (fail on absence); ESCAPE single quotes in ALL text inserted (the v1
defect); SMALL bbox subset only (no province-wide pull); wrangler authed (HALT with exact scope if missing); commit
--no-verify; NAS untouched.

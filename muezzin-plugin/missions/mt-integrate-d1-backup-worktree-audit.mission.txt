MISSION MT-INTEGRATE-D1-BACKUP-WORKTREE-AUDIT
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - workers/d1-backup/index.js
  - wrangler.d1-backup.toml
  - docs/DISASTER-RECOVERY.md
REQUIRES: none

Niyyah: Land the d1-backup feature's one commit into the integration repo's real history so
its scheduled-backup worker stops living only on a side branch, without silently dropping
either side of any overlap the integration repo has grown since that branch was cut. Authoring
and firing this mission is NOT a verification that the d1-backup feature code is correct —
this mission only proves the commit integrates cleanly and is syntactically valid JS. Whether
the worker actually backs up DB_POIS/DB_ACCOUNTS correctly, prunes on the right schedule,
authenticates /run properly, and restores per its own docs is exactly what this mission's own
witness and verdict-panel run is for, not something this authoring or this integration step
can certify.

Maqsad: Bring commit 634abd59ce5c7aa42061f1d4a641d5820ecfcab4 ("feat(d1-backup): scheduled D1
to R2 backups with 7d retention + restore docs") onto the current HEAD of the integration repo
at REPO-ROOT, producing exactly the three files below with their content intact, a passing
syntax check on the one JS file, a new commit carrying the feature's own message, and a
working tree clean with respect to this mission's own ALLOW-FILES.

Context (verified facts, absolute paths, no prescribed resolution beyond "keep both sides"):
- REPO-ROOT's current HEAD is on a branch other than main (integration branches move); this
  mission targets whatever HEAD is checked out at fire time, not a fixed branch name.
- The commit's source is the linked worktree C:\Users\marka\code\mt-feat-d1-backup-2026-06-23
  on branch feat/d1-backup-2026-06-23 (git worktree list run from REPO-ROOT confirms this is a
  LINKED worktree of the SAME repository as REPO-ROOT, so the commit object already lives in
  REPO-ROOT's shared object database today — no fetch, remote add, or file copy step is needed
  before the cherry-pick; a prior brief that named a different source path is stale).
- The commit is a leaf: 3 files changed, 865 insertions, 0 deletions (workers/d1-backup/index.js
  416 lines, wrangler.d1-backup.toml 74 lines, docs/DISASTER-RECOVERY.md 375 lines). None of the
  three paths exists at REPO-ROOT's current HEAD, and the commit's own parent is already an
  ancestor of REPO-ROOT's current HEAD, so a plain cherry-pick is expected to apply with zero
  conflicts — this mission still carries conflict-handling steps because "expected" from a
  point-in-time check is not a guarantee at fire time.
- REPO-ROOT's working tree currently carries pre-existing untracked files unrelated to this
  feature (for example an api-index-response.json, an e2e report under docs/, an e2e-shots
  directory, a feature-catalog-entry.txt, an e2e-runner script under scripts/). Those are OUT
  OF SCOPE for this mission: this mission's own ALLOW-FILES covers only the three paths above,
  and "clean working tree" in this mission's Done means refers only to those three paths plus
  the absence of an in-progress cherry-pick, never to unrelated pre-existing dirt this mission
  did not create and has no ALLOW-FILES basis to touch.

Steps:
  1. [command] preflight: from REPO-ROOT, run `git status --short` and `git rev-parse HEAD`.
     Confirm none of workers/d1-backup/index.js, wrangler.d1-backup.toml, or
     docs/DISASTER-RECOVERY.md already exist and none of the three are listed as dirty. Record
     the HEAD sha as the rollback baseline.
  2. [command] integrate: from REPO-ROOT, run
     `git cherry-pick 634abd59ce5c7aa42061f1d4a641d5820ecfcab4`. The commit object is already
     reachable in REPO-ROOT's object database via the linked worktree named above.
  3. [command] resolve-conflicts-if-any: run ONLY if step 2 reports a conflict. A conflict here
     means REPO-ROOT's own history independently created a file at one of the three paths since
     the branch was cut. For every conflicted path, open the file and keep BOTH REPO-ROOT's
     existing content and the feature's added content — merge them into one file; never resolve
     with a one-sided `git checkout --ours` or `--theirs` if that would discard either side's
     real content. Then `git add` each resolved path and run `git cherry-pick --continue`. Skip
     this step entirely when step 2 completes with no conflict.
  4. [command] syntax-check: from REPO-ROOT, run `node --check workers/d1-backup/index.js` — the
     only .js/.mjs file among the three changed paths (wrangler.d1-backup.toml and
     docs/DISASTER-RECOVERY.md are not JS and are not node-checked). Must exit 0; if it does not,
     fix the file's syntax (preserving both-sides content from step 3 if a merge happened there)
     and re-run until it exits 0.
  5. [command] verify: from REPO-ROOT, run `git status --short` (must show nothing for the three
     ALLOW-FILES paths and no other new dirt), confirm `.git/CHERRY_PICK_HEAD` does not exist,
     and run `git log -1 --format=%s` and confirm it reports
     "feat(d1-backup): scheduled D1 to R2 backups with 7d retention + restore docs".

Done means:
  - workers/d1-backup/index.js exists at REPO-ROOT, is 416 lines, and its content is the one
    committed by 634abd59ce5c7aa42061f1d4a641d5820ecfcab4 (or that content plus REPO-ROOT's own
    prior content at that path if a merge in step 3 was needed — never a one-sided overwrite).
  - wrangler.d1-backup.toml exists at REPO-ROOT (74 lines) and docs/DISASTER-RECOVERY.md exists
    at REPO-ROOT (375 lines), under the same both-sides-preserved rule if either path collided.
  - `node --check workers/d1-backup/index.js` run from REPO-ROOT exits 0.
  - `git log -1 --format=%s` at REPO-ROOT's HEAD reports
    "feat(d1-backup): scheduled D1 to R2 backups with 7d retention + restore docs".
  - `git status --short` at REPO-ROOT shows no uncommitted changes on any of the three
    ALLOW-FILES paths, and `.git/CHERRY_PICK_HEAD` does not exist (no integration left
    in-progress). Pre-existing unrelated untracked files elsewhere in the working tree are out
    of this mission's scope and are not evidence against this clause either way.
  - If step 3 ever fired for real, the resulting file(s) demonstrably carry both sides' content
    (named in the mission's own receipt/diff), not a silent drop of either.
  - None of the above is a claim that the d1-backup feature's own logic is correct — that
    verdict belongs to this mission's witness and verdict-panel run, not to this integration
    step.

Context: REPO-ROOT is a real project repo, not a sandbox; containment is enforced by
ALLOW-FILES against the kernel in mission_class.mjs, not by filesystem isolation. Every command
above runs at REPO-ROOT.
Amanah: entrusted; discharge with ihsan.

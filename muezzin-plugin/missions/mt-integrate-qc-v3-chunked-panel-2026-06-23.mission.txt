MISSION MT-INTEGRATE-QC-V3-CHUNKED-PANEL-2026-06-23
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - workflow-qc-gated-promote-v3-2026-06-23.mjs
  - docs/QC-PANEL-CHUNKED-REVIEW-2026-06-23.md
  - docs/QC-PIPELINE-SOTA-2026-06-23.md
REQUIRES: none

Niyyah: Branch mt-feat-qc-v3-chunked-panel-2026-06-23 carries two finished
commits — bc4638b ("feat(qc): v3 panel — chunked per-file review fixes
diff-truncation") and 375e40b ("docs(qc): SOTA-uplift QC pipeline — 8
mandatory gates + escape hatches") — sitting in their own worktree, not yet
both landed on the integration line. bc4638b restructures the QC-gated
promote workflow's Panel phase so the git diff is partitioned per file
(50KB verbatim / 50KB-sample-with-disclosure cutoffs) instead of truncating
the whole diff to ~10KB, the failure a kimi-k2.6 BLOCK verdict caught on
preview waoskc5ov. This mission's whole job is mechanical: get both
commits' content onto the target repo's current HEAD without losing
anything already there, run the syntax checks a code-repo mission owes,
record their exact results honestly even when a result is a failure, and
leave the tree clean for this mission's own scope. Authoring this mission,
and landing whatever is not already landed, is NOT a review of whether the
chunked-review pattern is well designed, whether the 50KB cutoffs are the
right thresholds, whether the adversarial-refuter default is correctly
wired, or whether the workflow file actually runs the way its own
documentation claims it does. None of that is settled by writing or running
these steps. That determination belongs entirely to this mission's own
witness-and-verdict-panel run once it fires, not to this mission's
authorship or mechanical execution — and this mission's authoring pass
already surfaced one concrete substrate finding for that panel to weigh,
recorded plainly in Context below rather than smoothed over.

Maqsad: Bring commits 375e40b0db479ede0e14407e6eb416e85936d305 and
bc4638b960221c135e97b6624c95c8b15c974942 onto the current HEAD of the
target repo C:\Users\marka\code\mt-integration-2026-06-22, with
workflow-qc-gated-promote-v3-2026-06-23.mjs, docs/QC-PANEL-CHUNKED-REVIEW-2026-06-23.md,
and docs/QC-PIPELINE-SOTA-2026-06-23.md present and byte-complete, any
conflict resolved by keeping BOTH sides — the target's pre-existing
unrelated changes AND this feature's added content, never silently dropping
either — the one touched .mjs file run through `node --check` with its
exact real result recorded (pass or the specific error, never assumed), the
result committed with a clear message (or the existing commits confirmed
already present if some or all of the content turns out to already be
integrated), and the target repo's working tree left clean with respect to
this mission's three ALLOW-FILES entries.

Context (verified against live substrate this session, 2026-07-01, absolute
paths, no prescribed resolution beyond "keep both sides"):
- The source worktree C:\Users\marka\code\mt-feat-qc-v3-chunked-panel-2026-06-23
  exists on disk and is a git worktree of the same underlying repository as
  the target (both are worktrees of C:/Users/marka/code/muddytires-pages/.git,
  confirmed via `git worktree list` and `git rev-parse --git-dir` this
  session). Both of this feature's commit SHAs already resolve to type
  "commit" via `git cat-file -t` run inside the TARGET repo itself — the
  objects are already present in the shared object database. No fetch,
  remote-add, or patch file is needed regardless of which worktree path is
  used to name the commits.
- `git merge-base --is-ancestor 375e40b0db479ede0e14407e6eb416e85936d305 HEAD`,
  run in the target repo this session, reports true, and
  docs/QC-PIPELINE-SOTA-2026-06-23.md already exists on disk in the target
  with content byte-identical to that commit (`git diff 375e40b -- docs/QC-PIPELINE-SOTA-2026-06-23.md`
  produced no output). This half of the feature is already integrated —
  confirmed separately by the sibling mission mt-integrate-qc-pipeline-sota-docs
  which landed it. An attempted cherry-pick of an already-applied commit
  reports "The previous cherry-pick is now empty" and must be resolved with
  --skip, never --allow-empty.
- `git merge-base --is-ancestor bc4638b960221c135e97b6624c95c8b15c974942 HEAD`,
  run in the target repo this session, reports false, and neither
  workflow-qc-gated-promote-v3-2026-06-23.mjs nor
  docs/QC-PANEL-CHUNKED-REVIEW-2026-06-23.md exists yet on disk in the
  target. This is the half of the feature this mission still has real work
  to do on. Confirmed diff stat for bc4638b (`git show --stat`, this
  session): 2 files changed, 551 insertions(+), 0 deletions(-), both new,
  both listed in ALLOW-FILES above. No .html file is part of either
  commit's diff (confirmed via `git show --stat` on both SHAs) — no
  visual-QC render-witness gate applies to this mission.
- VERIFIED FINDING for the verdict panel, not this mission's to resolve:
  running `node --check workflow-qc-gated-promote-v3-2026-06-23.mjs`
  against the source worktree's copy this session (node v22.20.0) fails
  with "SyntaxError: Illegal return statement" at line 55, because the file
  is authored as a workflow-DSL script body — top-level `phase(...)`,
  `await agent(...)`, `await parallel(...)`, and top-level `return`
  statements outside any function — meant to be executed inside a wrapping
  function by an external dispatch harness, not run as a standalone ES
  module. The feature's own doc, docs/QC-PANEL-CHUNKED-REVIEW-2026-06-23.md,
  states in its own Self-test section that "the workflow file is a
  contract, not a self-executing script" consumed by
  "mt-audit/workflow-runner.mjs (or equivalent)" — yet the very next line
  of that same doc prescribes `node --check` on this exact file as a "dry
  run" verification step. That specific claim in the feature's own
  documentation does not hold: the command it prescribes does not pass: it
  throws. This mission does not judge whether the DSL design itself is
  sound, only records that the doc's stated self-test command fails as
  documented, honestly, as a receipt.
- The target repo's HEAD moves between missions (these worktrees are reused
  repeatedly) — work against whatever HEAD is actually current at fire time
  and re-run the ancestry checks in step 1 rather than trusting this
  session's snapshot. No TARGET-BRANCH is declared on purpose: land onto
  whichever branch is actually checked out as HEAD when this fires, matching
  this mission's sibling mt-integrate-* missions for this same feature.
- The target repo's working tree carries pre-existing untracked files
  unrelated to this feature (confirmed this session: api-index-response.json,
  docs/e2e-report-2026-07-01.json, e2e-shots/, feature-catalog-entry.txt,
  scripts/e2e-runner.mjs). These predate this mission and are out of scope.
  "Clean working tree" in this mission's Done means refers to this
  mission's three ALLOW-FILES entries, not a demand to clear unrelated
  pre-existing debris.

Steps:
  1. In the target repo, record the current branch and HEAD, then run
     `git merge-base --is-ancestor 375e40b0db479ede0e14407e6eb416e85936d305 HEAD`
     and `git merge-base --is-ancestor bc4638b960221c135e97b6624c95c8b15c974942 HEAD`.
     Whichever reports true is already integrated — skip cherry-picking it
     and do not treat the skip as a failure. Whichever reports false must be
     cherry-picked in step 2. [command] check-ancestry-both-commits
  2. For each commit step 1 found NOT yet an ancestor, run
     `git cherry-pick <sha>`, oldest-authored first (375e40b before bc4638b,
     since bc4638b was authored on top of 375e40b — apply a parent before its
     child if both somehow still need picking). If a cherry-pick comes back
     empty ("previous cherry-pick is now empty"), resolve with --skip, never
     --allow-empty; an empty commit proves nothing landed. If it reports a
     real conflict, resolve every conflicted hunk by keeping BOTH sides — the
     target's pre-existing unrelated content AND this feature's added
     content, merged together, never deleting one side to make a conflict
     disappear. Only the three ALLOW-FILES entries above may be touched to
     complete a resolution. [command] cherry-pick-remaining-commits
  3. If step 2 resolved a real conflict, stage only the touched ALLOW-FILES
     entries by name (never `git add -A` or `git add .`, which would sweep in
     the target repo's pre-existing unrelated untracked files) and run
     `git cherry-pick --continue`. [command] continue-cherry-pick-scoped
  4. Run `node --check workflow-qc-gated-promote-v3-2026-06-23.mjs` from the
     target repo root — the one .js/.mjs file either commit touches — and
     record the exact result: exit code, and if nonzero, the full error
     text. Report this result as it actually comes back; do not report a
     pass unless the command actually exits 0. A SyntaxError result on this
     specific file has already been verified once during this mission's
     authoring pass (see Context) and is an expected, disclosed outcome, not
     a sign the cherry-pick itself went wrong. [command] node-check-workflow-file
  5. Run `git status --short -- workflow-qc-gated-promote-v3-2026-06-23.mjs docs/QC-PANEL-CHUNKED-REVIEW-2026-06-23.md docs/QC-PIPELINE-SOTA-2026-06-23.md`
     and confirm it prints nothing, and run `git log --oneline -20` and
     confirm commits reachable from HEAD carry the messages beginning
     "feat(qc): v3 panel" and "docs(qc): SOTA-uplift QC pipeline" (whether
     landed by this mission's step 2 or already present from prior work).
     [command] confirm-landed-and-clean
  6. Run `git show --stat` on whichever commit(s) this mission's step 2
     actually produced (or on the pre-existing commits, if step 1 found them
     already ancestors) and confirm none lists a .html file. [command] confirm-no-html-touched

Done means:
  - workflow-qc-gated-promote-v3-2026-06-23.mjs,
    docs/QC-PANEL-CHUNKED-REVIEW-2026-06-23.md, and
    docs/QC-PIPELINE-SOTA-2026-06-23.md all exist on disk in
    C:\Users\marka\code\mt-integration-2026-06-22, each containing the
    feature's actual content (the workflow file defines the 8-phase
    `meta.phases` array including a "Panel" phase whose detail mentions
    "CHUNKED per-file"; the chunked-review doc documents the kimi-k2.6 BLOCK
    incident on preview waoskc5ov; the pipeline-sota doc documents 8
    mandatory gates) — verified by reading each file's on-disk content, not
    by trusting a command's exit code alone.
  - `git merge-base --is-ancestor 375e40b0db479ede0e14407e6eb416e85936d305 HEAD`
    and `git merge-base --is-ancestor bc4638b960221c135e97b6624c95c8b15c974942 HEAD`,
    both run in the target repo after this mission's steps, both report
    true — both commits' content is present in the target's history,
    whether because it was already there or because this mission's
    cherry-picks landed it.
  - `git log --oneline -20` on the target repo's current branch shows
    commits whose messages are or begin with "feat(qc): v3 panel" and
    "docs(qc): SOTA-uplift QC pipeline".
  - `node --check workflow-qc-gated-promote-v3-2026-06-23.mjs` was actually
    run against the file as it sits in the target repo after landing, and
    its exact result is recorded as a receipt — exit code and, if nonzero,
    the error text verbatim. This mission's own authoring pass already
    verified this file fails with "SyntaxError: Illegal return statement"
    at line 55 (the file is a workflow-DSL script body with top-level
    `return`, meant to run inside a wrapping harness, not standalone) — a
    repeat of that same specific failure on the landed copy is the VERIFIED,
    EXPECTED result and does NOT fail this Done-means; the receipt is what
    is required, not a manufactured pass. If the landed file instead exits
    0 (e.g. because conflict resolution altered it, which is not expected
    here since no conflict is expected), that changed result must also be
    reported plainly, not silently reconciled with this Context's prior
    finding.
  - `git status --short -- workflow-qc-gated-promote-v3-2026-06-23.mjs docs/QC-PANEL-CHUNKED-REVIEW-2026-06-23.md docs/QC-PIPELINE-SOTA-2026-06-23.md`
    prints nothing (empty output) once this mission's steps are complete.
    Any OTHER pre-existing untracked or modified file already present in the
    target repo before this mission started is outside this mission's
    ALLOW-FILES scope and is not this mission's concern; its continued
    presence does not fail this Done-means.
  - No .html file was added, modified, or deleted by this mission's steps
    (confirmed via `git show --stat` on whichever commit(s) carry the
    feature) — no visual-QC render-witness gate applies to this mission.
  - If a real conflict was resolved in step 2/3, the committed result
    contains BOTH the target's pre-existing unrelated content and the
    complete content of whichever of the three ALLOW-FILES entries was
    conflicted — neither side silently dropped.
  - This Done-means covers integration mechanics and honest receipt-taking
    only: the files intact and committed, a clean or correctly resolved
    history, a syntax check actually run with its true result recorded, and
    a scoped-clean tree. It makes no claim that the chunked-review panel
    design is itself sound, that the 50KB thresholds are correct, that the
    adversarial-refuter wiring behaves as documented, or that the workflow
    file is fit to run in production despite failing its own doc's
    prescribed self-test command — those determinations belong entirely to
    this mission's own witness-and-verdict-panel run downstream, not to
    this mission's authorship or mechanical execution.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

MISSION-CLASS: ops-deploy
MISSION-ID: M-MUDDYTIRES.DEPLOY-PRODUCTION
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22

# THE CHAIN'S OWN DEPLOY PATH (M-DEPLOY-SELF-VERIFY, 2026-07-02). Built so a conductor never
# again hand-deploys muddytires + hand-verifies the render (this session: I did exactly that,
# and hand-caught that the deploy had regressed the live map to the SSR shell — nothing in the
# pipeline verified it). This ops-deploy mission runs VERBATIM (bypasses the architect panel per
# COMMAND-CLASS-VERBATIM), so the commands below are never re-authored. The last line is the
# self-verify: node deploy_gate.mjs renders the LIVE production map and EXITS NON-ZERO if the
# interactive surface didn't render or a required feature script never loaded — so a broken deploy
# FAILS this mission (the chain catches it), instead of shipping silently.
#
# PRECONDITION the conductor ensures BEFORE firing (this mission does NOT resolve divergence):
# local main is synced with github/main (git push here is fast-forward-only; a non-ff push fails
# the mission on purpose — deploy a reconciled tree, never a diverged one). Uses `git add -u`
# (tracked files only) so it never sweeps unrelated untracked files into a production commit.

Niyyah: give the chain a deploy path that publishes muddytires AND proves the publish rendered —
the missing last mile that made every integrated feature invisible and let a routing regression
ship unnoticed. The deploy is real; the verification is not optional; a failed render fails the mission.

Maqsad: ONE deliverable — the current committed muddytires site LIVE on the production Cloudflare
Pages project (muddytires.ca), with the interactive map + its feature scripts PROVEN to render by
deploy_gate.mjs against the live URL. Deploy + self-verify in one receipted mission, no conductor hands.

Done means: `wrangler pages deploy` reports Deployment complete; deploy_gate.mjs exits 0 against
https://muddytires.ca/map (leaflet-container present AND leaflet.js + aurora-overlay.js +
plan-day-gpx-export.js all loaded). If the gate exits non-zero the mission FAILS honestly — the
deploy rendered as a shell/regression and must not be reported as shipped.

Constraints: ADDITIVE (deploys committed state, no content authoring); fast-forward push only
(non-ff fails — resolve divergence before firing); `git add -u` never sweeps untracked files;
the deploy_gate self-verify is MANDATORY and its non-zero exit is the mission's failure signal.

```pwsh
git add -u; git commit -m "deploy: ship committed muddytires site to production (self-verified)" --no-verify --allow-empty
git push github main
wrangler pages deploy . --project-name=muddytires --commit-dirty=true
Start-Sleep -Seconds 20; node C:\Users\marka\.claude\muezzin-plugin\deploy_gate.mjs "https://muddytires.ca/map" --require=leaflet.js,aurora-overlay.js,plan-day-gpx-export.js --selector=.leaflet-container
```

Status: AUTHORED, HELD — NOT fired. A production deploy stays gated on explicit operator/conductor
intent (deploys are never auto-promoted). Fire it deliberately when a deploy is wanted; the
deploy_gate self-verify then makes a broken deploy impossible to ship silently. This mission is the
chain's answer to "why did the conductor hand-deploy + hand-verify" — now it does both itself.

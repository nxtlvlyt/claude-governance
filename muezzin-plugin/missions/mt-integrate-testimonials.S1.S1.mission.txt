MISSION-ID: MT-INTEGRATE-TESTIMONIALS.S1.S1
MISSION-CLASS: code-repo
PARENT: MT-INTEGRATE-TESTIMONIALS.S1
TARTIB-INDEX: 1 of 2
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - landing.html
  - build-testimonials.mjs
REQUIRES: none (first in tartib order)
UNLINKED-OK: landing.html pre-exists at HEAD and this mission only splices a marker-delimited section into it via the 06c5814 cherry-pick; the change-set adds no page and alters no navigation (verified 2026-07-17: git show --stat 06c5814 touches only landing.html +13 lines and the new build-testimonials.mjs), so page reachability is unchanged by this mission.
STEPS: 8

PARENT MAQSAD: sub-mission 1 of 2 - Ensure the target repo has TARGET-BRANCH main actually checked out before any write (repo volatility was observed this session; do not trust a previously recorded HEAD). through If the cherry-pick was left uncommitted by conflict resolution, stage and finalize it with a message naming testimonials/06c5814; confirm the working tree is clean with no unmerged paths.

Maqsad: sub-mission 1 of 2 - Checkout main branch to ensure TARGET-BRANCH is checked out before any writes. through Finalize the cherry-pick, commit scoped to the two allow-files, and confirm the scoped working tree clean.

STEP-SHAPE DISCIPLINE (conductor amendment 2026-07-17, after two MIQAT refusals - 2026-07-12T00:03:40Z and 2026-07-12T00:37:30Z: the split child inherited the parent's visual-QC header tag, and the first same-wake fix moved the tag to S1.S2 but left the tag's literal token in this child's Done-means prose, so the whole-text lint regex re-armed rule 11 on the refire - the same class spot-share-card.S1 hit and cured 2026-07-16 by rewording, applied here. This child is git-mechanical only; the deploy-and-render witness is wholly S1.S2's charter. Commands below are literal and dry-run-informed: git merge-tree --write-tree --merge-base=06c5814^ HEAD 06c5814 exited 0 against HEAD 0449664 on 2026-07-17, so the pick is predicted conflict-free; the tolerance in step 3 is defensive only and fails closed on unresolved markers rather than improvising a resolution):

Steps:
  1. Checkout main + baseline, stdout only, IDEMPOTENT. Run exactly: git checkout main; if ($LASTEXITCODE -ne 0) { exit 1 }; git rev-parse --abbrev-ref HEAD; git rev-parse HEAD; if ((Test-Path build-testimonials.mjs) -or (Select-String -Path landing.html -Pattern 'testimonials:start' -Quiet)) { Write-Output ALREADY_PRESENT } else { Write-Output CLEAN_BASELINE } [command]
  2. Preconditions: no pick in flight, source commit present in the object store, IDEMPOTENT. Run exactly: if (Test-Path (git rev-parse --git-path CHERRY_PICK_HEAD)) { exit 1 }; git cat-file -e 06c58148cf01b13388f08b53b2cd8e8942f7b182; if ($LASTEXITCODE -ne 0) { exit 1 }; Write-Output SOURCE_COMMIT_PRESENT [command]
  3. Land 06c5814; a conflict exit is TOLERATED only when zero markers remain - the assertion is the artifacts, IDEMPOTENT via artifact-presence skip (cherry-pick makes a new sha, so ancestry of 06c5814 is never the skip test). Run exactly: if ((Test-Path build-testimonials.mjs) -and (Select-String -Path landing.html -Pattern 'testimonials:start' -Quiet)) { Write-Output SKIP_ALREADY_INTEGRATED; exit 0 }; git cherry-pick -x 06c5814; if (Test-Path (git rev-parse --git-path CHERRY_PICK_HEAD)) { if (Select-String -Path landing.html -Pattern '^<{7}' -Quiet) { exit 1 }; git add -- landing.html build-testimonials.mjs; git -c core.editor=true cherry-pick --continue; if ($LASTEXITCODE -ne 0) { exit 1 } }; if (-not (Test-Path build-testimonials.mjs)) { exit 1 } [command] build-testimonials.mjs
  4. Splice-anchor witness on landing.html: both markers present, zero conflict markers. Run exactly: if (Select-String -Path landing.html -Pattern '^<{7}|^={7}|^>{7}' -Quiet) { exit 1 }; if (-not (Select-String -Path landing.html -Pattern 'testimonials:start' -Quiet)) { exit 1 }; if (-not (Select-String -Path landing.html -Pattern 'testimonials:end' -Quiet)) { exit 1 }; Write-Output MARKERS_CLEAN [verify] landing.html
  5. Build-script integrity: zero conflict markers and the full ~321-line body. Run exactly: if (Select-String -Path build-testimonials.mjs -Pattern '^<{7}|^={7}|^>{7}' -Quiet) { exit 1 }; $n = (Get-Content build-testimonials.mjs | Measure-Object -Line).Lines; if ($n -lt 300) { exit 1 }; Write-Output "SCRIPT_LINES=$n" [verify] build-testimonials.mjs
  6. Syntax-check the integrated build script with a real Node execution. Run exactly: node --check build-testimonials.mjs; if ($LASTEXITCODE -ne 0) { exit 1 }; Write-Output SYNTAX_OK [verify] build-testimonials.mjs
  7. Scoped commit if pending, IDEMPOTENT (pathspec on the commit itself, never a bare commit). Run exactly: git add -- landing.html build-testimonials.mjs; git diff --cached --quiet -- landing.html build-testimonials.mjs; if ($LASTEXITCODE -ne 0) { git commit -m "feat(landing): daily-rotated 5-star testimonials section (integrate 06c5814)" -- landing.html build-testimonials.mjs; if ($LASTEXITCODE -ne 0) { exit 1 } }; Write-Output COMMITTED_OR_CLEAN [command]
  8. Final repo-state witness: no lingering pick, scoped tree clean, deliverables on disk. Run exactly: if (Test-Path (git rev-parse --git-path CHERRY_PICK_HEAD)) { exit 1 }; if ((git status --porcelain -- landing.html build-testimonials.mjs).Length -gt 0) { exit 1 }; if (-not (Test-Path build-testimonials.mjs)) { exit 1 }; if (-not (Select-String -Path landing.html -Pattern 'testimonials:start' -Quiet)) { exit 1 }; Write-Output SCOPE_CLEAN [verify]

Done means: landing.html and build-testimonials.mjs land via the 06c5814 cherry-pick with zero conflict markers, the ~321-line build script intact and node-check passed, the integration committed with a pathspec scoped to the two allow-files, and the scoped tree clean - git-mechanical verification only. The deploy-and-render witness is S1.S2's charter: that sibling carries the visual-QC header tag and the executable wrangler+playwright step (moved there 2026-07-11; this Done-means was reworded 2026-07-17 so the tag's literal token no longer appears anywhere in this child - the spot-share-card.S1 rule-11 re-arm cure).

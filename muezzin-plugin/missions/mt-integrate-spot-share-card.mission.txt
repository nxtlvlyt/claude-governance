MISSION MT-INTEGRATE-SPOT-SHARE-CARD
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: main
ALLOW-FILES:
  - js/share-card.js
  - map.html
REQUIRES: none
VISUAL-QC-REQUIRED
PREVIEW-BASE-URL: http://localhost:8788

Niyyah: The feature "mt-feat-spot-share-card" (commit 18e7eb9, sitting in its source
worktree C:\Users\marka\code\mt-feat-spot-share-card) has never been integrated into
this repo's HEAD. This mission's entire job is mechanical integration — land the commit
onto the current tree without losing either side's changes, and prove the result is
syntactically sound and actually renders. Nothing this mission does constitutes a
judgment that the share-card feature's code is correct, safe, or behaviorally sound —
not whether the inline byte-mode QR encoder (ECC level L, versions 1..6) produces a
scannable code for every POI id shape, not whether the six-decimal lat/lng formatting is
accurate, not whether HTML-escaping of hostile POI names before they reach the
document.write-authored print window is complete against every injection vector, and not
whether the popupopen hook coexists correctly with the existing Share/Save-as F9 chips it
renders alongside. That determination belongs entirely to this mission's own witness-
and-verdict-panel run, downstream of this mission producing the integrated result — the
node --check receipt, the headless-browser render of map.html, and the seat panel's
judgment on the actual integrated diff. Authoring or executing this mission is not
verification of the feature.

Maqsad: The commit "18e7eb9 feat(share-card): per-spot printable share-card with QR code
(opt-in F9)" is cherry-picked or merged from the source worktree onto the current HEAD of
C:\Users\marka\code\mt-integration-2026-06-22, with any conflicts resolved by keeping
BOTH sides — this repo's existing unrelated changes AND the share-card feature's changes,
never silently dropping either — every changed .js/.mjs file passes `node --check`, the
result is committed with a clear message, and the target repo's working tree is left
clean (git status shows no uncommitted changes to the touched files).

Context (verified against live substrate this session, absolute paths, no prescribed
resolution beyond "keep both sides"):
- Source worktree: C:\Users\marka\code\mt-feat-spot-share-card, branch
  feat/spot-share-card, tip 18e7eb96e661d9f0beaf5b46fb596e8b94aaeba5. Confirmed this
  session via `git log --oneline` and `git status --short` (clean, no uncommitted
  changes). Do not rewrite that worktree, only read/cherry-pick FROM it.
- Confirmed diff stat for 18e7eb9 (`git show --stat`, this session): 2 files changed, 676
  insertions(+), 1 deletion(-) — js/share-card.js (675 new lines, a brand-new
  self-contained module: a Leaflet `popupopen` hook, an inline byte-mode QR encoder
  ECC=L versions 1..6, an `encodeQR`/`qrToSvg` pair rendering the QR as inline SVG, and a
  `document.write`-authored print window opened via `window.print()` showing the POI
  name, six-decimal lat/lng, and a QR code linking to
  `muddytires.ca/map.html#spot=<id>`) and map.html (+1/-1: a single new
  `<script src="js/share-card.js" defer></script>` tag inserted into the existing
  script-tag chain immediately after the `js/share-export.js` tag; map.html's script
  chain lives on one long physical line, so a one-tag insertion shows as that one line
  being replaced with itself plus the new tag, not a pure +1/-0 add). `node --check
  js/share-card.js` was run against the source worktree's copy this session and exits 0.
- Target repo (this session, `git branch --show-current` / `git log --oneline -1`): on
  branch main at HEAD 5e19874307f1548c404bf0a42b90831c00da9539 ("Merge branch
  'feat/qc-concern-vehicle-profile-picker-2026-06-25'"), working tree clean at the moment
  this Context was written.
- REPO VOLATILITY OBSERVED THIS SESSION: consecutive `git status --short` calls against
  this same target repo, seconds apart, showed a transient set of untracked/staged
  litter (api-index-response.json, docs/e2e-report-2026-07-01.json, e2e-shots/,
  feature-catalog-entry.txt, scripts/e2e-runner.mjs, plus a staged build-testimonials.mjs
  / landing.html pair) that was present on one check and gone on the next, with no action
  by this session causing either change — the same shared-repo HEAD/tree-race hazard
  named in sibling integration missions against this repo. Do not assume the branch,
  HEAD, or working-tree cleanliness recorded above is still true by the time this mission
  actually fires; re-confirm live as the first step. If TARGET-BRANCH main is not what is
  actually checked out, check it out explicitly before touching any file. Any unrelated
  litter encountered is out of this mission's scope — neither committed nor deleted.
- Confirmed this session (`git merge-base --is-ancestor 18e7eb9 HEAD` in the target
  repo): commit 18e7eb9 is NOT yet an ancestor of the target repo's HEAD — the feature is
  genuinely outstanding, not already integrated by another path.
- Confirmed this session (`git cat-file -e main:js/share-card.js` fails — "does not
  exist"): js/share-card.js does not exist on the target repo's main yet, so its half of
  the cherry-pick is a clean add. map.html DOES already exist on main (311 lines,
  confirmed via `git cat-file -e main:map.html` and `wc -l`) and is the file receiving
  the single-line script-tag insertion.
- The commit object 18e7eb96e661d9f0beaf5b46fb596e8b94aaeba5 is already present in the
  target repo's own object store (confirmed via `git cat-file -t` returning "commit"
  this session, since worktrees under C:\Users\marka\code share object storage) —
  cherry-pick can reference the SHA directly with no fetch/remote-add step needed.
- A dry-run cherry-pick of 18e7eb9 performed this session (on a throwaway branch off
  main, via `git cherry-pick --no-commit -n`, then fully reverted via `git reset --hard
  HEAD` + `git clean -fd` + `git checkout main` + `git branch -D` before this mission
  file was written, leaving the target repo exactly as found) applied with zero
  conflicts ("Auto-merging map.html", exit 0) against the HEAD observed at that moment —
  `js/share-card.js` landed as a clean new-file add and map.html's script chain gained
  exactly the one new `<script src="js/share-card.js" defer></script>` tag, inserted
  right after the existing `js/share-export.js` tag, with every other tag on that line
  unchanged. This is evidence the integration is expected to be mechanically clean, not a
  guarantee — the volatility observed above means the tree this mission actually meets at
  fire time may differ; if a real conflict does occur, resolve by keeping BOTH sides'
  content, never `--ours` / `--theirs`.
- This diff touches map.html (an HTML file), which is why VISUAL-QC-REQUIRED and
  PREVIEW-BASE-URL are declared above. http://localhost:8788 is a PLACEHOLDER for a local
  `wrangler pages dev` run — the operator must confirm the port a real local preview
  server actually binds to on the machine firing this mission before relying on the
  captured render; if nothing answers at PREVIEW-BASE-URL, that must surface as a named
  NO_PREVIEW_BASE_URL-style receipt, not a silently skipped check.
- Nothing about this mission's authorship constitutes verification that the feature code
  is correct — that is exactly what this mission's own witness and verdict-panel run,
  downstream of the integration, will determine.

Steps:
  1. In the target repo, confirm the actually-checked-out branch and HEAD
     (`git -C C:\Users\marka\code\mt-integration-2026-06-22 branch --show-current` and
     `git log --oneline -1`). If the checked-out branch is not TARGET-BRANCH main, check
     it out explicitly before any further step. Then confirm js/share-card.js does not
     already exist on disk. [command] confirm-target-branch-clean
  2. In the source worktree C:\Users\marka\code\mt-feat-spot-share-card, confirm commit
     18e7eb96e661d9f0beaf5b46fb596e8b94aaeba5 exists (`git show 18e7eb9`) without
     modifying that worktree. [command] capture-patch
  3. In the target repo, at its current HEAD on TARGET-BRANCH main, cherry-pick the
     commit (`git -C C:\Users\marka\code\mt-integration-2026-06-22 cherry-pick
     18e7eb96e661d9f0beaf5b46fb596e8b94aaeba5`) or an equivalent merge of that single
     commit. [command] integrate-commit
  4. If the cherry-pick (or equivalent merge) reports conflicts on js/share-card.js,
     map.html, or any other ALLOW-FILES entry, resolve every conflicted hunk by hand so
     the resolved content contains BOTH the target's pre-existing content AND the
     share-card feature's added content — never `--ours` / `--theirs` and never a
     resolution that deletes either side's content. [edit] resolve-conflicts
  5. Run `node --check js/share-card.js` from the target repo root and confirm it exits
     0; run `node --check` on every other .js/.mjs file the merge touched or
     conflict-resolved. [command] node-check-all
  6. If step 3 left the integration uncommitted (conflict resolution required `git add` +
     `git cherry-pick --continue`, or a manual merge commit), commit explicitly with a
     message that references the source commit's own subject line ("feat(share-card):
     per-spot printable share-card with QR code (opt-in F9)") and/or the SHA 18e7eb9.
     Then confirm `git status --short` is empty (clean working tree, no unmerged paths)
     and `git log --oneline -5` shows the landed commit at or near HEAD. [command]
     commit-and-verify-clean
  7. Because map.html changed and VISUAL-QC-REQUIRED is set, run the visual-witness
     pipeline: serve the target repo's static files locally, load map.html from
     PREVIEW-BASE-URL in a headless browser (puppeteer), capture a render, and record a
     visual verdict on the integrated result in this mission's result object. If
     PREVIEW-BASE-URL is unreachable, record that as a named NO_PREVIEW_BASE_URL receipt
     rather than skipping the check silently. [command] visual-witness-render

Done means:
  - `git log` in C:\Users\marka\code\mt-integration-2026-06-22 shows a commit on
    TARGET-BRANCH main containing the integrated share-card feature, with a commit
    message that names "mt-feat-spot-share-card" and/or "18e7eb9".
  - js/share-card.js exists in the target repo at HEAD with non-trivial content
    (approximately 675 lines implementing the Leaflet popupopen hook, the inline
    byte-mode QR encoder, and the document.write-authored print window showing POI
    name/lat-lng/QR code, confirmed by reading the file, not assumed).
  - map.html in the target repo contains exactly one
    `<script src="js/share-card.js" defer></script>` tag added to its existing script
    chain immediately after the js/share-export.js tag, with no other line content lost
    or altered (confirmed by reading the file, not by trusting the cherry-pick's exit
    code alone).
  - `node --check js/share-card.js` exits 0, and `node --check` exits 0 on every other
    .js/.mjs file this integration touched or conflict-resolved.
  - `git status --short` in the target repo shows a clean working tree (no uncommitted
    changes, no unmerged paths) at the end of the mission.
  - Because this diff touches map.html and VISUAL-QC-REQUIRED is set above, the visual-
    witness pipeline must actually run and produce a receipt: a headless-browser
    (puppeteer) render of map.html served from PREVIEW-BASE-URL is captured and returns a
    visual verdict in this mission's result object — a rendering check, not a read of the
    markup. If PREVIEW-BASE-URL turns out to be the wrong local port, that failure must
    surface as a named NO_PREVIEW_BASE_URL-style receipt, not a silently skipped check.
  - This mission's authorship and execution prove only that the integration is
    mechanically sound (declared files present with real content, feature commit
    reachable in target history, syntax checks pass, a render was actually captured) —
    not that the feature's code is correct, safe, or behaviorally right (e.g. that the QR
    codes actually decode and link to the right spot, that the print layout is legible,
    or that HTML-escaping of hostile POI names is complete). That judgment belongs to
    this mission's own witness plus verdict-panel run, downstream of this mission
    producing the integrated result.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

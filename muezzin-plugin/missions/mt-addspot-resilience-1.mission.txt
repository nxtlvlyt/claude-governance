MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.ADDSPOT-RESILIENCE-1
REPO-ROOT: E:\AI_Storage\muddytires-frontend-wt
ALLOW-FILES:
  - js/add-spot-worker.js
  - js/account.js
  - map.html
REQUIRES: mt-firstparty-auth-routing-1 (same repo + same files; must land first to avoid collision)

Niyyah: a traveller stops at a beautiful boondock with one bar of signal, fills in the spot, hits submit — and the post fails on the
weak connection, wiping everything they typed. Or they fill the whole form only to be told AFTER submitting that they must sign in
first. Both waste the one moment they had to contribute. Make the form forgive a bad connection and tell the truth up front.

Maqsad: make add-spot resilient for real on-the-road use. "Done" is: (1) the add-spot form's inputs persist to localStorage as the user
types and survive a page reload / failed submission / signup interruption, cleared only on a successful post; (2) the login requirement
is shown UP FRONT (when the form opens) as an inline "Sign in to add spots" message, not as a toast after the user already filled +
submitted; (3) a submission error on weak signal keeps its message visible until the user dismisses it (not auto-hidden after 5s), and
geolocation errors show an inline message + allow manual coordinate entry instead of a dead alert(). node --check passes; committed.
NOT deployed (conductor deploys + mobile-throttled-network verifies).

Context (facts only — from the de-risk workflow; the seats implement):
- TODAY (verified): add-spot form data is NOT persisted; a network failure during submission or the signup flow loses all input. The
  login check fires inside the submit handler (reviewSpot()) AFTER submission, surfacing as a post-submission toast. Geolocation/submit
  errors use alert()/auto-hide toasts.
- THE FIX (behavior, not prescribed code): persist the add-spot inputs (name, node_type, lat, lng, note, amenities, any photo refs) to
  localStorage on input; restore on form open; clear on confirmed success. Move the login gate to the form-OPEN path (inline message in
  the form sheet). Replace alert()/5s-auto-hide with inline, dismiss-on-user messages. Keep it XSS-safe (textContent/escapeHtml — the
  existing pattern in these files).
- DEPENDENCY: this edits js/add-spot-worker.js + js/account.js which mt-firstparty-auth-routing-1 ALSO edits (the AUTH_BASE -> same-origin
  change). REQUIRES that mission first so this builds on the post-routing files (no overwrite). Same-repo -> serial.
- COMMIT DISCIPLINE: explicit per-file git add of the ALLOW-FILES — NEVER `git add .`. commit --no-verify.
- Out of scope: the deploy; the routing change (its own mission); the workers.

Done means (acceptance contract): add-spot inputs persist in localStorage (survive reload/failed-submit, cleared on success); login
requirement shown inline at form-open; submission/geo errors are inline + user-dismissed (no data-losing alert/auto-hide); XSS-safe;
node --check passes; committed via explicit per-file git add, --no-verify; NOT deployed. Receipt names the conductor mobile-throttled
verify step (DevTools 3G-slow: fill form, fail a submit, reload, confirm data survives).

Constraints: code-repo in frontend-wt; writes only the ALLOW-FILES; builds on the post-routing files (REQUIRES routing); XSS-safe;
explicit per-file git add (never -A); commit --no-verify; deeds proven by receipts.

MISSION QC-CONCERN-ACCOUNT-AUTH-MODAL-ACCOUNT-JS-2026-06-25
MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.QC-CONCERN-ACCOUNT-AUTH-MODAL-ACCOUNT-JS-2026-06-25
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: feat/qc-concern-account-auth-modal-account-js-2026-06-25
ALLOW-FILES:
  - js/account.js
  - docs/FEATURE-CATALOG-2026-06-23.md
REQUIRES: none

Niyyah: RE-AUTHORED after this exact mission file was deleted following an engine-isolation
failure rooted in every code-repo mission sharing ONE git worktree (REPO-ROOT above) with no
per-mission sandbox isolation. Reconstruction this session (not from memory — read from
result.json, retro, git log, and live disk content) surfaced TWO separate facts that must
both be acted on:
  (1) This mission's OWN original QC-concern fix already landed on a prior attempt/session —
      commit b619a4d ("restore two-pill header slot + centered max-w-500px modal with dimmed
      backdrop", trailer "Mission: M-MUDDYTIRES.QC-CONCERN-ACCOUNT-AUTH-MODAL-ACCOUNT-JS-2026-
      06-25") and commit cf18994 ("guard the unauthenticated fetch behind logged-in check") are
      both already committed and both already present in js/account.js on disk, read in full
      this session. This mission's job is NOT to redo that fix.
  (2) A LATER attempt/retry of this same mission died at the sandbox/branch-checkout gate
      before any architect panel ever ran — confirmed via
      qc-concern-account-auth-modal-account-js-2026-06-25.mission.result.json: {"ok": false,
      "phase": "sandbox", "reason": "...refusing checkout '...poi-affiliate-cards...' ->
      '...account-auth-modal...' — worktree is dirty:\nM js/fire-ban-layer.js\n M
      js/site-search.js..."} and the matching retro (0 events, 0 plan-phases, 0
      steps-committed). An UNRELATED queued mission (touching js/fire-ban-layer.js and
      js/site-search.js, neither in this mission's own scope) left the shared worktree dirty
      and blocked this mission's checkout. That is a queue/worktree-hygiene defect upstream of
      this mission's file scope; nothing in ALLOW-FILES below caused it or can fix it.
Separately, the actual batch-level gap this mission exists to close is STILL open: the Feature
Catalog (docs/FEATURE-CATALOG-2026-06-23.md, read in full this session) still lists
"js/account.js (login, profile, auth UI)" under "TODO — Priority B+ features to catalog" —
unchanged since the 2026-06-23 doc version, zero features cataloged for this module.

Maqsad: On this mission's own TARGET-BRANCH: (a) verify the already-landed auth-modal fix
(commits b619a4d + cf18994) is intact and unregressed, (b) add minimal, additive, stable id
attributes to the 7 currently-unlabeled DOM nodes renderAuthModal() builds in js/account.js
(overlay, panel, sign-up tab, sign-in tab, form, submit button, cancel button) — no class,
text, or event-handler change — so the feature becomes selector-addressable for the
e2e-runner/catalog QC pipeline, and (c) author a real `### Feature: Account auth modal
(login/signup)` section in docs/FEATURE-CATALOG-2026-06-23.md following the catalog's own
template exactly, citing the real ids added in (b) and the real regression history (b619a4d,
cf18994), then remove the now-stale `js/account.js (login, profile, auth UI)` line from the
TODO module list.

Context (verified against live substrate this session, absolute paths, read from disk not
recalled):
- js/account.js (383 lines, read in full) already contains, inside init():
  `let slot = document.getElementById('mt-account'); if (!slot) { // QC concern 2026-06-25:
  #mt-account missing from the DOM ... }` (the b619a4d fallback-creation fix) and
  `const token = ...; if (token) { currentUser = await getMe(); }` (the cf18994 gated-fetch
  fix). Both confirmed present in the CURRENT working-tree file, not just in git history.
- renderAuthModal() in js/account.js builds exactly 7 DOM elements with NO id attribute today
  (confirmed by reading the function body): the overlay `<div class="fixed inset-0
  bg-black/50...">`, the panel `<div class="bg-white rounded-lg shadow-lg...">`, the "Sign up"
  tab `<button>`, the "Sign in" tab `<button>`, the `<form>`, the submit `<button>`, and the
  cancel `<button>`. The only existing automation hooks are `.mt-account-msg` (message div)
  and `#mt-account` (the mount slot).
- docs/FEATURE-CATALOG-2026-06-23.md (259 lines, read in full) has exactly 3 cataloged
  features (Vehicle profile picker, Fuel-cost chip, Carbon-footprint chip) plus the empty
  template, and still lists `- js/account.js (login, profile, auth UI)` at the line matching
  `js/account.js (login, profile, auth UI)` under "## TODO — Priority B+ features to catalog".
- The shared worktree is under active concurrent use by other queued missions (observed this
  session: checked-out branch and untracked-file set both changed between read-only
  inspections minutes apart, and a sibling mission file for this same batch — qc-concern-
  retail-parking-overlay-retail-parking-overlay-js-2026-06-25 — independently names the exact
  same concurrency hazard). If the dirty-checkout guard refuses this mission's checkout again
  for a reason outside ALLOW-FILES, name that as a receipt rather than force-cleaning another
  mission's files.
- Pre-existing untracked debris in the shared worktree unrelated to this mission
  (api-index-response.json, docs/e2e-report-2026-07-01.json, e2e-shots/,
  feature-catalog-entry.txt, scripts/e2e-runner.mjs) predates this mission and is out of
  scope — "clean working tree" in Done means below refers only to this mission's own
  ALLOW-FILES entries.

Steps:
  1. Record current state before touching anything, and confirm both halves of the gap are
     what Context above says they are: `git -C C:\Users\marka\code\mt-integration-2026-06-22
     status --short`; `git -C C:\Users\marka\code\mt-integration-2026-06-22 branch
     --show-current`; `git -C C:\Users\marka\code\mt-integration-2026-06-22 show
     HEAD:js/account.js | Select-String -Pattern "QC concern 2026-06-25: #mt-account missing"`
     (expect a match — the prior fix should already be there; if NO match, stop and report —
     do not attempt to re-author that fix under this mission's narrower scope). `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 show HEAD:docs/FEATURE-CATALOG-2026-06-23.md
     | Select-String -Pattern "js/account.js \(login, profile, auth UI\)"` (expect a match —
     the TODO line should still be there; if NO match, the catalog gap is already closed by a
     sibling mission — stop and report that instead of authoring a duplicate entry).
     [command] verify-target-state
  2. Switch the shared worktree onto this mission's own TARGET-BRANCH by NAME (the exact class
     of failure that orphaned fixes in sibling missions this batch was skipping this step or
     landing on the wrong branch): `git -C C:\Users\marka\code\mt-integration-2026-06-22
     checkout feat/qc-concern-account-auth-modal-account-js-2026-06-25`. If that branch does
     not exist, create it fresh: `git -C C:\Users\marka\code\mt-integration-2026-06-22
     checkout -B feat/qc-concern-account-auth-modal-account-js-2026-06-25 main`. Afterward
     `git -C C:\Users\marka\code\mt-integration-2026-06-22 branch --show-current` MUST print
     exactly "feat/qc-concern-account-auth-modal-account-js-2026-06-25" before proceeding.
     [command] checkout-target-branch
  3. Edit js/account.js: inside renderAuthModal(), add one `id` attribute to each of the 7
     elements identified in Context — additive only, no class/text/handler change:
     `overlay.id = 'mt-auth-overlay';`, `panel.id = 'mt-auth-panel';`,
     `signupBtn.id = 'mt-auth-tab-signup';`, `loginBtn.id = 'mt-auth-tab-login';`,
     `form.id = 'mt-auth-form';`, `submit.id = 'mt-auth-submit';`,
     `cancel.id = 'mt-auth-cancel';`. [edit] add-stable-selectors
  4. Verify the edit parses and is additive-only: `node --check js/account.js` (must exit 0);
     `git -C C:\Users\marka\code\mt-integration-2026-06-22 diff -- js/account.js` must show
     only added `.id = '...'` lines and zero removed lines. [command] node-check-additive
  5. Append a new `### Feature: Account auth modal (login/signup)` section to
     docs/FEATURE-CATALOG-2026-06-23.md under a `## Priority B — cataloged this pass` heading
     (create that heading, placed after the existing "## Priority A" section and before "##
     TODO — Priority B+ features to catalog", if it does not already exist; if a sibling
     qc-concern mission in this same batch has already created that heading, append this
     feature's section under the existing one instead of duplicating the heading). Follow the
     catalog's own template fields exactly (Page/Loaded by/Pre-state required/
     Selectors/Interaction sequence/Expected behavior/Expected visual/Discoverability/Verdict
     gates/Regression history), citing the 7 real selectors added in step 3
     (#mt-auth-overlay, #mt-auth-panel, #mt-auth-tab-signup, #mt-auth-tab-login, #mt-auth-form,
     #mt-auth-submit, #mt-auth-cancel) — no leftover template placeholders like
     `<CSS selector>` — and citing both real prior fixes in Regression history: "2026-06-25:
     #mt-account missing from DOM on some pages — init() now falls back to creating the slot
     (commit b619a4d)" and "2026-06-25: unauthenticated getMe() 401 console noise on landing —
     gated behind session-token check (commit cf18994)". [edit] author-catalog-entry
  6. Remove the now-stale `- js/account.js (login, profile, auth UI)` line from the "## TODO —
     Priority B+ features to catalog" module list in docs/FEATURE-CATALOG-2026-06-23.md, since
     it is now cataloged by step 5. [edit] remove-todo-line
  7. Cross-check the new catalog entry against the real file — no drifted or placeholder
     selectors: read the working-tree js/account.js and confirm each of the 7 ids from step 3
     appears in it exactly once (e.g. `Select-String -Path js/account.js -Pattern
     "mt-auth-overlay|mt-auth-panel|mt-auth-tab-signup|mt-auth-tab-login|mt-auth-form|mt-auth-submit|mt-auth-cancel"`
     returns 7 matches), then confirm each of those same 7 ids also appears in the new section
     of docs/FEATURE-CATALOG-2026-06-23.md written in step 5. [command] cross-check-selectors
  8. Stage and commit both files with a message naming "account-auth-modal" and/or
     "account.js" and a trailer citing this mission's MISSION-ID (matching the house
     convention already used by commits b619a4d and cf18994). Confirm afterward: `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 branch --show-current` still prints the
     TARGET-BRANCH name; `git -C C:\Users\marka\code\mt-integration-2026-06-22 status --short
     -- js/account.js docs/FEATURE-CATALOG-2026-06-23.md` is empty; `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 log --oneline -3` shows the new commit at or
     near HEAD. [command] commit-and-verify-clean

Done means:
  - `git branch --show-current` in C:\Users\marka\code\mt-integration-2026-06-22 prints
    exactly "feat/qc-concern-account-auth-modal-account-js-2026-06-25" (not any other
    mission's branch — the exact defect class that orphaned fixes in sibling missions this
    batch).
  - Reading js/account.js on that branch shows the pre-existing 2026-06-25 fixes UNCHANGED
    (the #mt-account fallback-creation block and the token-gated getMe() call both still
    present, confirmed by reading the surrounding lines, not just the diff) PLUS the 7 new id
    attributes from step 3 — an addition, nothing removed.
  - `node --check js/account.js` exits 0.
  - Reading docs/FEATURE-CATALOG-2026-06-23.md on that branch shows a `### Feature: Account
    auth modal (login/signup)` section with no leftover template placeholders, whose
    Selectors block cites all 7 real ids added to js/account.js and whose Regression history
    cites both commit b619a4d and commit cf18994; AND the `js/account.js (login, profile,
    auth UI)` line is gone from the "TODO — Priority B+ features to catalog" list.
  - `git status --short -- js/account.js docs/FEATURE-CATALOG-2026-06-23.md` in the target
    repo is empty (pre-existing unrelated untracked debris elsewhere in the shared worktree —
    api-index-response.json, docs/e2e-report-2026-07-01.json, e2e-shots/,
    feature-catalog-entry.txt, scripts/e2e-runner.mjs — is not this mission's to clean up).
  - `git log --oneline -3` on that branch shows a commit at or near HEAD naming
    "account-auth-modal" and/or "account.js".
  - No file outside ALLOW-FILES (js/account.js, docs/FEATURE-CATALOG-2026-06-23.md) was
    touched.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

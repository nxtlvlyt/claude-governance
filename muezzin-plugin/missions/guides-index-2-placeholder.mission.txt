MISSION-CLASS: code-repo (author ONE NEW small static .html on a CLEAN worktree + positive-assertion verify + commit; NEW file = NO clobber; NO source dependency = NO fabrication surface; NO deploy; live muddytires.ca / prod Pages / DNS UNTOUCHED)
MISSION-ID: M-MUDDYTIRES.GUIDES-INDEX.2
REPO-ROOT: E:\AI_Storage\muddytires-frontend-wt
ALLOW-FILES: guides.html

# WHY: guides.html does not exist -> the "Guides" nav link 404s (operator: "the guides not working"). guides-index-1 FAILED x2
# because it scoped a topic-list extracted from partners.html (sparse source) -> the seat PADDED with fabricated guide slugs ->
# witness correctly rejected. FIX = remove the fabrication surface entirely: a STANDALONE "coming soon" placeholder with NO topic
# list and NO source to pad from. There is nothing to fabricate, so it ships reliably and resolves the dead nav honestly.

Niyyah: make the Guides nav link resolve to an honest, on-brand "in progress" page — promising nothing that does not exist, inventing no guide content.

Maqsad: ONE deliverable — guides.html: a small static page, design-consistent with the rest of the site (same Tailwind CDN +
config, fonts, sticky nav, footer as index.html/about.html), whose ONLY content is: a "Field Guides" heading, one honest short
paragraph that the field guides are in progress, and a primary CTA button to the live map. Steps:
1. Author guides.html as a COMPLETE static page (whole file, one pass). Reuse the site's existing head/Tailwind-config/nav/footer
   shape (brand 'Muddy Tires'; nav -> index.html / map.html / guides.html(self) / about.html / partners.html; "Open map" ->
   map.html). Body = ONLY: an <h1> "Field Guides", ONE short honest paragraph (guides are being written; in the meantime the map
   has the spots), and a button/link to map.html. INVENT NOTHING: no guide titles, no topic list, no fake previews, no dates,
   no counts. ZERO PHP (no <?php, no <?=).
2. VERIFY (positive-assertion, EXIT NON-ZERO on absence): guides.html exists; ZERO php tags (grep '<?php' and '<?=' absent);
   contains the word "guides" and a link to map.html; nav has no '.php"' links; does NOT contain a fabricated topic list
   (assert it does NOT contain 'guides.html?g=' invented-slug patterns — there is no topic list to get wrong).
3. STAGE THEN COMMIT (guides.html is a NEW untracked file — `git add` it first or the commit is HOLLOW). Run VERBATIM:
   git add guides.html; git commit --no-verify -m "add static guides.html placeholder (honest 'in progress' page; resolves dead Guides nav; zero fabricated content)"
   Then assert: git log -1 --name-only must list guides.html (EXIT 1 if not).

Done means: guides.html is valid static HTML, design-consistent, with ONLY a heading + one honest in-progress line + a map CTA and
ZERO invented guide content; committed on branch oracle-frontend-swap; NO deploy; live untouched.

Constraints: STANDALONE (no context dependency, no source to pad from); STATIC (zero php); NEW file (no clobber); INVENT NOTHING
(no topic list — the whole point is to remove the fabrication surface that failed guides-index-1); ONE small file; positive-assertion
verify; commit --no-verify; NO deploy; live untouched; no split.

MISSION DAMM-WEST-CORRECTIONS
MISSION-CLASS: research
REQUIRES: search
Niyyah: quirky-pois-west went DONE 2026-06-12 09:27 APPROVE_WITH_DAMM with two banked findings: (1) four entries carry a source_url in the JSON that does not match the URL in the search-receipts.md verification table (Mr. PG, Vegreville Pysanka, Eddie the Squirrel, and the Jasper entry); (2) the NOTES file claims a '1975 computerized-design engineering story' for the Vegreville Pysanka while the verified sources give a different unveiling date. A damm unpaid is a graveyard. This micro-mission pays both with one corrections artifact.
Maqsad: ONE artifact — west-corrections.md at the sandbox root, with exactly two sections. PLAN SHAPE: (1) COMMAND gather steps copy the parent evidence into this sandbox, each a PLAIN Copy-Item of a literal path (NO if-Test-Path guard — these sources are REQUIRED and a miss must fail loudly): the dataset JSON artifact, search-receipts.md, and the NOTES .md from C:\Users\marka\.claude\muezzin-plugin\missions\quirky-pois-west (use the actual filenames present in that sandbox; list the directory in the first command step and fail if any of the three classes is absent); (2) edit steps author the corrections in TWO SMALL emissions: Section A = a four-row reconciliation table for Mr. PG, Vegreville Pysanka, Eddie the Squirrel, and the Jasper entry — for each: the JSON's source_url, the receipts table's URL, ONE live-web-search check of which URL actually documents the POI, and the CORRECT url with its receipt; Section B = the Vegreville Pysanka date correction — the claim as written, the verified unveiling date with a live URL cite, and the corrected NOTES sentence; (3) ONE command step validates with Select-String that the artifact contains all four POI names and a heading containing Date Correction. Done means: west-corrections.md exists at the sandbox root, validation confirms the four names and the date-correction heading, every corrected URL carries a live-search receipt, and no claim is asserted without either a URL or an explicit inference marker.
Context: this is a CORRECTIONS addendum — the parent's 19-entry verified dataset is not re-authored. Sandbox writes only.
Amanah: entrusted; discharge with ihsan.

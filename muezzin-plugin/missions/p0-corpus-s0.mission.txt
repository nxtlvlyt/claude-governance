MISSION P0-CORPUS-S0 — source census
Niyyah: hand-picked source lists miss things; the factory's knowledge must start from discovery, not memory.
Maqsad: ONE artifact — sources.json at the sandbox root: a census of every place Mark's prior work and knowledge lives, each entry with {name, type (git-repo|folder|llm-workspace|cloud-deployment|live-site|document), location, evidence (one line on what it is), verdict (mine|skip), verdict_reason}. Done means sources.json exists, parses as JSON, contains at least the known starting points below PLUS whatever discovery adds, and records any access gaps honestly (e.g. private GitHub repos unenumerable without a token) rather than guessing.
Context: starting points to verify and go beyond — folders: E:\2boots, C:\vanlife, C:\warroom, E:\AI_Storage\website-pipeline, D:\Desktop\ai book. Cloudflare pages (authed wrangler CLI on PATH): 2boots, laynas-cookies, regulativity. AnythingLLM workspace API: http://localhost:3001/api/workspaces (responds unauthenticated). Local Forgejo API: http://localhost:3002. GitHub user nxtlvlyt (anonymous API shows only public repos — record private-enumeration as a gap). Live sites: 2boots.ca, muddytires.ca, laynascookies.ca, regulativity.pages.dev. Discovery commands may list directories and call local APIs read-only. All outputs cwd-relative inside this sandbox.
Amanah: this is entrusted to you; discharge it with ihsan.

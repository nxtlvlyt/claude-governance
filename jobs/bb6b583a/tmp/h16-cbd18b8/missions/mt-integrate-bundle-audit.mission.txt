MISSION mt-integrate-bundle-audit
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - scripts/audit-bundle.mjs
  - docs/AUDIT-BUNDLE-BASELINE.json
REQUIRES: none

Niyyah: The mt-feat-bundle-audit feature (a pre-deploy bundle-size and dependency
audit script) exists as a finished commit in its own worktree but has not yet been
folded into the integration line. Authoring and firing this mission only verifies
that the feature's TWO files land intact on top of whatever else is already on the
target's current HEAD, with nothing silently dropped from either side, and that the
script is syntactically valid JavaScript. Nothing about drafting this mission text
constitutes review of whether scripts/audit-bundle.mjs's own logic is correct
(threshold math, depcheck invocation, baseline-regression comparison, exit-code
meaning) — that judgment belongs entirely to this mission's own witness and
verdict-panel run once it fires, not to this mission's authorship.

Maqsad: Starting from C:\Users\marka\code\mt-feat-bundle-audit (source worktree,
already holds commit ad8c7e7), bring that commit onto the current HEAD of
C:\Users\marka\code\mt-integration-2026-06-22 (target repo) via cherry-pick or
merge, keeping BOTH the target's pre-existing unrelated work and this feature's
added files if anything conflicts, confirm the script parses, commit the result
with a clear message, and leave the two feature files fully committed with no
pending changes of their own.

Steps:
  1. From within the target repo's REPO-ROOT, on its current HEAD (do not check out
     any other branch first), run: git cherry-pick ad8c7e713ca043db6a6a10425e1544b2098cc84a
     — the mt-feat-bundle-audit commit. The target and the source worktree are both
     git worktrees of the same underlying repository, so this commit object is
     already present locally; no fetch, remote-add, or patch file is needed. [command] cherry-pick-bundle-audit
  2. If the cherry-pick reports no conflicts, skip to step 4. If it reports
     conflicts, resolve every conflicted hunk by keeping BOTH sides' content —
     the target's pre-existing unrelated lines AND this feature's added lines,
     merged together — never deleting one side to make a conflict disappear.
     Only scripts/audit-bundle.mjs and docs/AUDIT-BUNDLE-BASELINE.json may be
     touched to complete a resolution. [edit] resolve-conflicts-keep-both-sides
  3. After resolving, stage only the two feature files by name — git add
     scripts/audit-bundle.mjs docs/AUDIT-BUNDLE-BASELINE.json (never git add -A
     or git add ., which would sweep in unrelated pre-existing files already
     sitting in the target's working tree) — then run git cherry-pick --continue
     to finish the commit. The cherry-picked commit message is already clear
     ("feat(audit): pre-deploy bundle size + dep audit..."); keep it, or if a
     merge was used instead, write an equally clear message naming the feature
     and its origin commit ad8c7e7. [command] continue-cherry-pick-scoped
  4. Run: node --check scripts/audit-bundle.mjs — and confirm it exits 0. [command] node-check-audit-bundle
  5. Run: git status --short — and confirm neither scripts/audit-bundle.mjs nor
     docs/AUDIT-BUNDLE-BASELINE.json appears in the output (both fully committed),
     and confirm no file outside this mission's ALLOW-FILES was staged, modified,
     or committed while completing this mission. [command] verify-scoped-clean-tree

Done means:
  - scripts/audit-bundle.mjs exists at the target repo's REPO-ROOT and contains
    the string 'depcheck' (confirms the real script landed, not a stub or a
    conflict-mangled partial).
  - docs/AUDIT-BUNDLE-BASELINE.json exists at the target repo's REPO-ROOT and is
    valid JSON (parses without error).
  - node --check scripts/audit-bundle.mjs exits 0.
  - git log -1 --format=%s on the target repo's HEAD contains both 'audit' and
    'bundle' (the integrated commit is present in history, whether it landed via
    a clean cherry-pick that kept the original message or via --continue after
    manual conflict resolution).
  - git status --short shows no entry for scripts/audit-bundle.mjs or
    docs/AUDIT-BUNDLE-BASELINE.json — both are fully committed with nothing
    pending. Any OTHER pre-existing untracked or modified file already present
    in the target repo before this mission started is outside this mission's
    ALLOW-FILES scope and is not this mission's concern; its continued presence
    does not fail this Done-means.
  - This Done-means covers integration mechanics only: intact files, a clean or
    correctly-resolved history, a passing syntax check, and a scoped-clean tree.
    It makes no claim that the script's own logic is correct — that is exactly
    what this mission's witness and verdict-panel run exists to determine.

Context (unbiased facts — absolute paths, no prescribed solution):
- C:\Users\marka\code\mt-feat-bundle-audit and C:\Users\marka\code\mt-integration-2026-06-22
  are both git worktrees of the same underlying muddytires-pages repository
  (confirmed via git worktree list), so the target already has commit
  ad8c7e713ca043db6a6a10425e1544b2098cc84a in its local object database.
- That commit adds exactly two new files and modifies nothing else:
  scripts/audit-bundle.mjs (405 lines) and docs/AUDIT-BUNDLE-BASELINE.json
  (155 lines), +560/-0 total across the diff.
- At authoring time the target repo has no existing file at either path, so a
  clean, conflict-free cherry-pick is the expected outcome; the conflict-
  resolution steps above exist for completeness and safety, not because a
  conflict was observed.
- At authoring time the target repo's working tree already carries several
  pre-existing untracked files unrelated to this feature, left by other
  in-flight work. This mission's ALLOW-FILES is scoped to exactly the two
  feature files and must not stage, modify, or claim credit for cleaning up
  anything else.
- No TARGET-BRANCH is declared on purpose: the task is to land on whatever the
  target repo's HEAD is pointing to at fire time, not to force a checkout of a
  specific branch.
- This diff touches no HTML file, so no rendered-page verification applies to
  this integration.

Amanah: entrusted; discharge with ihsan.

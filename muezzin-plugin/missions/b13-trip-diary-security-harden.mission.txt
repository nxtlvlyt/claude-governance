MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.B13-TRIP-DIARY-SECURITY-HARDEN
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: feat/b13-trip-diary-user-2026-06-23
ALLOW-FILES:
  - functions/api/trips/*.js
  - functions/api/trips/[id]/*.js
  - functions/api/trips/[id]/stops/*.js
  - functions/_lib/trip-helpers.js
  - trips.html
  - trip-edit.html

Niyyah: ship the trip-diary feature with the security discipline that production code deserves. Honest about defects: the original build's substrate checks all passed, but a 5-seat SOTA panel flagged real holes (CSRF, CORS-wildcard, race, transaction-gaps, XSS). Discharge the amanah by closing them — not by claiming, by deeds witnessed in a passing re-review.

Maqsad: the existing branch ships clean. After this mission, a re-run of the same 5-seat panel against the new diff returns panel_verdict=pass or concern, with substrate-verifiable evidence in the diff that each of the panel's blocking findings has been addressed.

Context (substrate, not steering — read each, verify, then reason your path):

Substrate paths:
  - Panel findings from wdsq4mxb2 pipeline (sha — local commits on the branch):
      C:\Users\marka\AppData\Local\Temp\claude\C--Users-marka\4c4a63ee-b0e6-4c65-9e09-561a76fbb9b0\tasks\wdsq4mxb2.output (results[0])
  - The branch: github/feat/b13-trip-diary-user-2026-06-23
  - QC panel harness: C:\Users\marka\code\mt-audit\qc-harness-v2.mjs (deliberationPanelReview)
  - Existing CORS+auth patterns to align with: functions/api/[[path]].js (the proxy with identity-gate already shipping in production)

Panel findings (the deeds to close, verbatim — no steering on the fix path):
  1. CORS wildcard ACAO:'*' with Cookie/Authorization forwarding — no origin allowlist (4/5 seats)
  2. No CSRF protection on state-changing POST/PATCH/DELETE — no Origin check, no token, SameSite unverified (4/5)
  3. Race on ord assignment: POST /stops reads MAX(ord) then inserts non-atomically (3/5)
  4. Reorder N UPDATEs not wrapped in transaction — partial failure leaves inconsistent ords (3/5)
  5. XSS risk: user-controlled fields rendered into HTML without verified output sanitization (2/5)
  6. DB-error swallowing in ownerTrip/ownerStop masking failures as 404 (1/5)
  7. DELETE: two sequential DELETEs (trip_stop then trip) not transactional (1/5)

Done means (acceptance contract — verified by execReceipt, not self-claim):
  - The same deliberationPanelReview call against the new diff returns panel_verdict in {pass, concern}.
  - For every blocking finding above, the diff contains substrate-verifiable evidence of the fix (a CORS allowlist constant, a CSRF check, a wrapping primitive call, an escape function invocation, etc — whatever the chain reasons to be the right closure).
  - All pre-existing tests + node --check pass on every modified .js.
  - Push to the same branch as a follow-up commit; ls-remote verifies.

Amanah: this is entrusted to you. The destination is set — the path is yours to reason. Use ijtihad on what specific primitives close each finding in this codebase's conventions (read the substrate paths above first). Step-by-step prescription deliberately omitted.

Constraints (operator standing rulings, not mechanics):
  - Per-file git add (no -A).
  - --no-verify on commit and push (machine-commits witnessed by execReceipt).
  - NO LOCAL OLLAMA per operator-rulings.md.
  - Phase 2 executor = Sonnet per memory/phase-execution-architecture.md.
  - Mt-audit has NO remote; commit the qc-harness call locally if its code changes (it shouldn't — only the feature branch changes).

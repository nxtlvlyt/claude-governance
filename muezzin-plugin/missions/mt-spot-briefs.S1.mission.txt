MISSION-ID: mt-spot-briefs.S1
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - scripts/build-spot-briefs.mjs
  - data/spot-briefs.json
REQUIRES: missions/mt-enrich-scenic-osm.S1.mission.txt
STEPS: 6

Maqsad: Spots render honestly but thinly — a traveler still cannot see WHY to go. Reviews
and descriptions exist in LEGAL open sources (Wikivoyage/Wikipedia via the wikidata ids
that mt-enrich-scenic-osm.S1 surfaces; CC BY-SA with attribution). Build the pilot
spot-briefs overlay for the Saskatchewan region: for each enriched scenic node that has a
wikidata id, fetch its Wikipedia/Wikivoyage extract, summarize it with a LOCAL Ollama
model into a 1-2 sentence why_go plus optional heads_up, groundedness-gate the summary
against its source text, and emit data/spot-briefs.json mapping osm_id ->
{why_go, heads_up?, sources:[urls]}. A brief that fails grounding or has no source is
OMITTED — no filler, ever (operator receipt 2026-07-02: fabricated 'Cool place' filler).

niyyah: This is the Crown-Land oracle pattern applied to spot knowledge: offline local-model
enrichment -> verified, attributed artifact -> free at runtime. Every brief carries its
source chain (no summary without provenance); the groundedness gate is what makes local-model
authorship safe to automate.

Steps:
  1. Write scripts/build-spot-briefs.mjs: resolve ALL file I/O paths (loadJson/saveJson) with
     `path.resolve(process.cwd(), filename)` — NEVER `new URL(filename, import.meta.url)`,
     which resolves relative to scripts/ and silently no-ops (step 3 always runs `node
     scripts/build-spot-briefs.mjs` from REPO-ROOT, so cwd-relative resolution is correct;
     import.meta.url-relative resolution is not). Reads data/scenic-enrichment.json, for each
     entry with a wikidata id resolves the linked Wikipedia article via
     https://www.wikidata.org/wiki/Special:EntityData/<id>.json (sitelinks enwiki) and fetches
     its plain-text extract via the Wikipedia REST summary API; calls the local Ollama server
     (http://nxtbeast:11434/api/generate, model qwen3.6:27b) with a prompt requiring a 1-2
     sentence why_go grounded ONLY in the extract; then verifies groundedness mechanically
     (every proper noun in why_go must appear in the source extract, else drop the entry);
     writes data/spot-briefs.json sorted by osm_id with sources array (wikipedia url +
     license note CC BY-SA). Entries with no wikidata/extract are omitted. [edit]
  2. Syntax-check the builder. [verify] scripts/build-spot-briefs.mjs
  3. Run: node scripts/build-spot-briefs.mjs — reads scripts/build-spot-briefs.mjs
     (context_dependencies, action_type 'verify'), writes data/spot-briefs.json [command]
  4. Verify data/spot-briefs.json exists, parses, and every entry has a non-empty why_go
     AND at least one source url. [verify] data/spot-briefs.json
  5. Verify no entry's why_go contains the strings "Cool place" or "worth a quick stop"
     (the filler classes) — briefs must be spot-specific. [verify] data/spot-briefs.json
  6. Run: git add data/spot-briefs.json scripts/build-spot-briefs.mjs && git commit -m
     "<msg naming entry count + source licensing>" — both files listed as
     context_dependencies, action_type 'verify' (not re-authored) [command]

Done means: data/spot-briefs.json exists with >=1 grounded, source-attributed why_go entry
for the SK pilot, zero filler strings, builder passes node --check, both files committed.

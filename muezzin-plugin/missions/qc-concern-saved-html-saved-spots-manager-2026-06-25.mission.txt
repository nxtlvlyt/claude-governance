MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.QC-CONCERN-SAVED-HTML-SAVED-SPOTS-MANAGER-2026-06-25
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: feat/qc-concern-saved-html-saved-spots-manager-2026-06-25
ALLOW-FILES:
  - saved.html

Niyyah: address the QC CONCERN named "saved.html saved-spots manager" from the 2026-06-25
e2e walk.

Maqsad: GATE: When not logged in: `#saved-guest` visible — PASS: The guest sign-in CTA is
clearly visible in the center of the page. GATE: Free cap copy renders: "Up to 5 lists, 200
spots each" — FAIL: This text is not visible in the guest view; it likely only renders in
the authenticated state which was not reached. The free-tier cap copy is a trust signal
that needs to be visible to guests before sign-in (so unauthenticated visitors can see what
they get for free) — surface "Up to 5 lists, 200 spots each" in the #saved-guest block so
the guest CTA includes the free-tier promise. Restore behavior to that specified in
docs/FEATURE-CATALOG-2026-06-23.md.

Context (verified against live substrate this session, 2026-07-01, re-authoring a mission
whose .txt was deleted before it ever produced a sandbox):

- RE-AUTHORING NOTE: this exact mission was queued once before (2026-06-25 ~21:41Z) and
  died with ZERO events — its retro shows "events: 0 | plan-phases: 0 | steps-committed: 0"
  because the engine's checkout guard refused to provision a sandbox at all: "code-repo
  TARGET-BRANCH: refusing checkout 'feat/qc-concern-poi-affiliate-cards-...' ->
  'feat/qc-concern-saved-html-saved-spots-manager-2026-06-25' — worktree is dirty" (the
  immediately-preceding same-batch mission left js/fire-ban-layer.js and js/site-search.js
  modified-but-uncommitted). This was a cascading engine/orchestration hygiene defect
  shared by 7 sibling qc-concern-* missions queued in the same ~21:40-21:46Z window, not
  evidence that this concern is unfixable — this mission never got to attempt anything.
- A SEPARATE, EARLIER attempt at this same concern DID succeed: commit 1fdeb7a2283c9280
  6f55fd969c690d94f2973dfb ("qc-concern(saved-html-saved-spots-manager): surface 'Up to 5
  lists, 200 spots each' free-tier cap copy in #saved-guest block per QC walk 2026-06-25",
  2026-06-25 12:09 -0400, ~5.5h before the dead attempt above) is a complete, correct,
  single-file fix: it changes ONLY saved.html, changing the existing guest paragraph's
  class from `mb-8` to `mb-4` and inserting one new line immediately after it:
  `<p class="text-muted text-sm mb-8">Free, forever. Up to <strong>5</strong> lists,
  <strong>200</strong> spots each.</p>`. That commit sits on this mission's own
  TARGET-BRANCH name, but stacked underneath TWO LATER, UNRELATED commits for other
  missions (sentry-error-capture, docs) — none of that branch has ever been merged into
  main or master. Re-verified live today (2026-07-01): `git show main:saved.html` and
  `git show master:saved.html` both still show the OLD single-paragraph #saved-guest block
  with no cap copy — the gap described in the Maqsad above is still live on both branches
  an operator would call "production-tracking" (master mirrors github/master; main is this
  repo's local integration trunk, confirmed by `git symbolic-ref refs/remotes/github/HEAD`
  -> refs/remotes/github/master, and by main's own history of merged qc-concern-* feature
  branches). `git merge-base --is-ancestor 1fdeb7a... main` and `...master` both report
  NOT an ancestor.
- BRANCH-NAME COLLISION: a branch named exactly this mission's TARGET-BRANCH already
  exists in this repo, pointing at the stacked history described above (the real fix PLUS
  two unrelated later commits, out of this mission's scope). Whoever provisions the
  sandbox for TARGET-BRANCH should reset this branch to cut fresh from `main`'s current
  HEAD before Step 1 runs, rather than reuse the stale stack — this keeps the resulting
  history to "main's mainline + this mission's own fix commit" only. If the sandbox is
  instead provisioned by reusing the stale branch as-is, Step 1's baseline check below will
  detect the cap copy is already present (1fdeb7a is already in that history) and this
  mission's Done-means is already satisfied without a new fix commit — but the two
  unrelated stacked commits on that branch remain a separate cleanup this mission does not
  attempt.
- ALLOW-FILES SCOPE CORRECTION: the dead 2026-06-25 attempt scoped ALLOW-FILES to
  `js/*.js` and `workers/**/*.js` on the premise that a JS module renders `#saved-guest`.
  That premise is false, reconfirmed live today: `grep -n "saved-guest\|saved-authed"
  js/*.js` returns zero matches. Both `#saved-guest` and `#saved-authed` are static markup
  in saved.html, and the show/hide toggle logic that switches between them lives in an
  INLINE `<script>` block inside saved.html itself (line 111 onward), not in any external
  js/ file — `js/saved-list.js` only supplies the `window.mtSavedList.listLists()` API
  helper saved.html's inline script calls; it never touches these element IDs. saved.html
  is therefore the ONLY legal write target this fix needs, and ALLOW-FILES above reflects
  that correction.
- FEATURE-CATALOG CITATION IS NOT LOAD-BEARING: docs/FEATURE-CATALOG-2026-06-23.md is not
  even merged onto main or master today (`git show main:docs/FEATURE-CATALOG-2026-06-23.md`
  -> "does not exist in 'main'"; every qc-* mission that references it stages its own copy
  onto its own branch via a dedicated commit, per `git log --all --diff-filter=A --
  docs/FEATURE-CATALOG-2026-06-23.md`). Where it does exist (its origin commit 0a481c0),
  it contains no structured "### Feature:" entry for saved-spots at all — only a generic
  one-line inventory mention "js/saved-list.js (favorites, saved POIs)" and a route-list
  mention of "/saved". It defines no color, wording, or placement spec for this gate. The
  Maqsad's GATE language above (from the actual 2026-06-25 QC walk) is the authoritative
  spec for this fix, not the catalog — the catalog citation in the Maqsad is inherited
  mission-brief boilerplate, not a real spec match, and this mission does not depend on it.
- WHY THE ORIGINAL "e2e-runner walk returns clean" DONE-MEANS IS REPLACED BELOW: scripts/
  e2e-runner.mjs parses `docs/FEATURE-CATALOG-2026-06-23.md` for `### Feature: <name>`
  sections and can only walk features that have one; there is no such section for this
  concern (confirmed above), so a catalog-driven `--features` walk has no feature to target
  for this gate specifically, structurally, independent of the file even being staged. This
  mission's Done-means below is built from two things that ARE independently verifiable
  today: (a) mechanical Select-String source checks, and (b) a self-contained Playwright
  render-witness (its own local static server + a mocked 401 on
  `/api/contribute/saved/lists`, no live preview URL or backend needed) — BOTH were written
  against, and test-verified against, real copies of the current unfixed saved.html and the
  known-good 1fdeb7a fix this session: the unfixed copy correctly fails both checks
  (`guestVisible: true, capCopyVisible: false` — reproducing the original walk's exact
  GATE 1 PASS / GATE 2 FAIL split) and the fixed copy correctly passes both
  (`guestVisible: true, capCopyVisible: true`).

Steps:
  1. Baseline discovery (expected to succeed either way, not a conditional escalation
     gate): in saved.html on the checked-out TARGET-BRANCH, run
     `Select-String -Path saved.html -Pattern 'id="saved-guest"|id="saved-authed"|</div>'`
     to find the LineNumber of the `id="saved-guest"` opening div, the first `</div>`
     LineNumber after it (its close — the block has no nested divs), and the
     `id="saved-authed"` opening LineNumber. Then check whether a cap-copy phrase already
     appears strictly between the saved-guest open/close LineNumbers (see the flexible
     match in Step 3). If it already does (i.e. this branch inherited 1fdeb7a's fix),
     record that finding and skip Step 2 — Done-means is already satisfied and Step 4/5
     just confirm and, if needed, produce a clean no-op commit state. Otherwise proceed.
     [command] baseline-discovery
  2. Apply the fix. First try `git cherry-pick -n 1fdeb7a2283c92806f55fd969c690d94f2973dfb`
     (no-commit, so the result can be inspected before committing) and confirm it reports
     that it touched ONLY saved.html. If the cherry-pick fails, conflicts, or the commit is
     unreachable in this worktree, fall back to editing saved.html directly: change the
     existing `<p class="text-muted max-w-sm mb-8">Sign in to manage the spots you've
     bookmarked on the map.</p>` line inside the `#saved-guest` div to `mb-4`, and insert a
     new line immediately after it: `<p class="text-muted text-sm mb-8">Free, forever. Up
     to <strong>5</strong> lists, <strong>200</strong> spots each.</p>`. Do not touch the
     `#saved-authed` block, the "Sign in" link, or any other section. [edit] saved.html
  3. Mechanical verification (PowerShell, run from REPO-ROOT):
     `$lines = Get-Content saved.html;`
     `$guestOpen = (Select-String -Path saved.html -Pattern 'id="saved-guest"').LineNumber | Select-Object -First 1;`
     `$guestClose = (Select-String -Path saved.html -Pattern '</div>').LineNumber | Where-Object { $_ -gt $guestOpen } | Select-Object -First 1;`
     `$blockText = (($lines[($guestOpen-1)..($guestClose-1)] -join ' ') -replace '<[^>]+>', ' ') -replace '\s+', ' ';`
     `$blockText -match 'up to\s*5\s*lists?,?\s*200\s*spots'`
     must be `True`. Also confirm: exactly one `id="saved-guest"` and exactly one
     `id="saved-authed"` occurrence each (`(Select-String -Path saved.html -Pattern
     'id="saved-guest"').Count` and the same for `saved-authed`, both `1`); the
     `#saved-authed` block's own pre-existing cap copy is untouched (`Select-String -Path
     saved.html -Pattern 'saved-cap-lists'` and `'saved-cap-items'` still each return a
     match); the "Sign in" CTA link is still present (`Select-String -Path saved.html
     -Pattern 'href="profile.html"'` returns a match); and `git status --short` in
     REPO-ROOT shows `saved.html` as the only changed path. [command] verify-mechanical
  4. Render-witness (Node + the repo's own installed Playwright, no live preview URL or
     backend needed): write and run a script that (a) starts a local static HTTP server
     bound to 127.0.0.1 on an ephemeral port serving the repo root (saved.html + js/), (b)
     launches headless Chromium via Playwright, (c) routes/mocks both
     `**/api/contribute/saved/lists` and `**/api/auth/me` to fulfill with HTTP 401 (this is
     the exact response an unauthenticated real visitor's browser would receive — no
     mocking of application logic, only of the network response an anonymous session
     legitimately gets), (d) navigates to `/saved.html`, (e) waits for `#saved-guest` to
     lose its `hidden` class, then reads `#saved-guest`'s `innerText` and asserts it matches
     `/up to\s*5\s*lists?,?\s*200\s*spots/i`. Confirm the script exits 0 with
     `guestVisible: true` and `capCopyVisible: true` in its JSON output. This step was
     test-run this session against both the unfixed saved.html (result: `guestVisible:
     true, capCopyVisible: false`, exit 1 — reproducing the original QC walk's GATE 1
     PASS / GATE 2 FAIL exactly) and the 1fdeb7a fix (result: `guestVisible: true,
     capCopyVisible: true`, exit 0), confirming the check discriminates correctly in both
     directions. [command] render-witness
  5. Commit saved.html alone with a message naming "saved.html", "saved-guest", and "Up to
     5 lists, 200 spots each" so the fix is traceable in history, then confirm `git status`
     shows a clean working tree. If Step 1 found the fix already present (branch inherited
     1fdeb7a), skip creating a redundant commit and instead confirm the working tree is
     already clean with the fix already in HEAD. [command] commit-and-verify-clean

Done means:
  - saved.html contains exactly one `id="saved-guest"` div and exactly one
    `id="saved-authed"` div (no duplication), and the free-tier cap-copy phrase (matched by
    `up to\s*5\s*lists?,?\s*200\s*spots` case-insensitively, tags stripped) appears strictly
    inside the `#saved-guest` div's line range — not only inside `#saved-authed`.
  - The `#saved-authed` block's own pre-existing cap-copy spans (`saved-cap-lists`,
    `saved-cap-items`) are unchanged, and the guest "Sign in" CTA link
    (`href="profile.html"`) is still present — no regression to either neighboring gate.
  - The self-contained Playwright render-witness (Step 4) passes: with
    `/api/contribute/saved/lists` and `/api/auth/me` mocked to HTTP 401 (the real response
    an anonymous visitor gets), `#saved-guest` is visible (`hidden` class absent) AND its
    rendered `innerText` contains the free-tier cap-copy phrase — a real DOM/visual
    confirmation, not just a source-grep.
  - `git status --short` in C:\Users\marka\code\mt-integration-2026-06-22 shows saved.html
    as the only changed path (or a clean tree if Step 1 found the fix already inherited),
    and any new commit is on TARGET-BRANCH with a message naming saved.html, saved-guest,
    and the "Up to 5 lists, 200 spots each" phrase.
  - No other file is touched: `git status --short` never shows any path outside saved.html,
    and in particular never shows any js/*.js or workers/**/*.js file changed — the
    scoping error from the dead 2026-06-25 attempt (ALLOW-FILES pointed at JS files that
    have nothing to do with this markup) is not repeated.
  - A catalog-driven `e2e-runner --features <slug>` clean verdict is explicitly NOT required
    here: docs/FEATURE-CATALOG-2026-06-23.md has no structured feature entry for this
    concern to walk (verified above), so that criterion is structurally unachievable and is
    replaced by the two verifiable receipts above (mechanical source check + self-contained
    render-witness), per this mission's Context.

Amanah: this slice is part of the muddytires.ca quality-uplift backlog.

MODEL-ROUTING: TIER_CLOUD_CODE — kimi-k2.7-code or deepseek-v4-pro for fix authoring.

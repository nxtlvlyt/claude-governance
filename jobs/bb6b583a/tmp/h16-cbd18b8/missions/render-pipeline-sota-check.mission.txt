MISSION: render-pipeline-sota-check — the intake's video output, held to 2026 platform SOTA
MISSION-CLASS: research
REQUIRES: search

Niyyah: the operator asked directly (2026-06-12 ~09:55): "I'm not sure if we're using ffmpeg, HyperFrames, or our renders as state-of-the-art as possible to make sure we're producing the best output per platform." The operator's standing rule: adopted patterns face a SOTA check, not grandfathering. The intake pipeline's current render facts (stage them, judge against them): h264_nvenc preset p5, 10-12M bitrate, 1080-short-side normalize, fps=30, 16:9 pad + 9:16 kind-aware (center-crop for camera footage, blur-pillarbox for screen content), aac 48k stereo audio untouched (no loudness normalization yet), thumbnails 1280x720 q2.

Maqsad: ONE assembled artifact — render-sota-verdict.md at the sandbox root, built from FOUR part files. PLAN SHAPE: (1) ONE command step stages the current pipeline source: if (Test-Path 'E:\AI_Storage\website-pipeline\apps\post-intake\processor.mjs') { Copy-Item 'E:\AI_Storage\website-pipeline\apps\post-intake\processor.mjs' . }; (2) ONE command step produces processor.numbered.mjs by prefixing each line with Lnn; (3) FOUR SEPARATE edit steps author one part file each, every part a SMALL emission, every SOTA claim carrying a live 2025-2026 URL and every claim about the current pipeline citing [processor.numbered.mjs Lnn] — a statistic without a URL is PROHIBITED (write it qualitatively or mark it inference, not in source): part-1-platform-specs.md = the CURRENT official upload specs per platform (YouTube long-form + Shorts, Instagram Reels, TikTok, Facebook): resolution, codec (H.264 vs HEVC vs AV1 acceptance), bitrate recommendations, fps, audio loudness targets (the -14 LUFS question), duration limits as of 2026; part-2-encode-verdict.md = our encode settings judged against part 1, one KEEP/SHARPEN/REPLACE row per setting (nvenc preset, bitrate ladder, fps normalize, audio passthrough vs loudnorm); part-3-framing-verdict.md = our 16:9 pad + kind-aware 9:16 judged against what top screen-content and vlog creators actually ship in 2026 (blur-pillarbox vs smart-crop vs captioned-reframe), including whether burned-in captions are now table-stakes for sound-off viewing; part-4-pick.md = THE PICK: the ordered top-3 upgrades with effort estimates, each quoting the part-2/part-3 rows it acts on — a menu without a pick fails; (4) ONE command step assembles: writes a '# Render Pipeline — 2026 Platform SOTA Verdict' header then concatenates part-1 through part-4 into render-sota-verdict.md; (5) ONE command step validates with Select-String that the assembled file contains all four section parts and exactly one 'THE PICK'. Done means: render-sota-verdict.md exists at the sandbox root, validation confirms four parts and one pick, every platform spec carries a live URL, every pipeline claim cites Lnn, and part-4 ranks exactly three upgrades.
Context: HyperFrames is NOT part of this mission's scope — its render capability is being established separately (hyperframes-render-proof-b, queued); note it as out-of-scope rather than speculating. Sandbox writes only.
Amanah: entrusted; discharge with ihsan.

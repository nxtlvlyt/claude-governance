MISSION mt-integrate-sentry-sourcemaps-upload-2026-06-23
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: main
ALLOW-FILES:
  - scripts/upload-sourcemaps.mjs
  - docs/QC-PIPELINE-SOTA-2026-06-23.md
REQUIRES: none

Niyyah: The feature "mt-feat-sentry-sourcemaps-upload-2026-06-23" (commits 0c1a3f1
and 375e40b, sitting in the source worktree
C:\Users\marka\code\mt-feat-sentry-sourcemaps-upload-2026-06-23) is only partly
integrated into this repo's TARGET-BRANCH main — see Context for the verified
substrate finding that one of the two commits is already there. This mission's
entire job is mechanical integration of whatever remains unintegrated, landing it
onto main's current tip without losing either side's changes, and proving the
result is syntactically sound and committed. Nothing this mission does constitutes
a judgment that the feature's CODE is correct, safe, or behaves as designed (e.g.
that scripts/upload-sourcemaps.mjs's REST-direct Sentry release/upload/deploy-mark
calls actually succeed against a live Sentry API, that its honest-degrade contract
really exits 0 when SENTRY_AUTH_TOKEN/SENTRY_ORG/SENTRY_PROJECT are unset or dist/
has no .js files, that it correctly avoids @sentry/cli as claimed, or that
docs/QC-PIPELINE-SOTA-2026-06-23.md's 8-gate policy is complete or accurate). That
determination belongs to this mission's own witness plus verdict-panel run, which
inspects the integrated result after this mission produces it — not to the act of
integration itself. Authoring or executing this mission is not verification of the
feature's code.

Maqsad: Commits "0c1a3f1 feat(sentry): scripts/upload-sourcemaps.mjs — REST-direct
release+sourcemap uploader" and "375e40b docs(qc): SOTA-uplift QC pipeline — 8
mandatory gates + escape hatches" are cherry-picked or merged from the source
worktree onto TARGET-BRANCH main of C:\Users\marka\code\mt-integration-2026-06-22,
with any conflicts resolved by keeping BOTH sides — this repo's existing unrelated
changes AND this feature's changes, never silently dropping either — every
.js/.mjs file this integration touches passes `node --check`, the result (whatever
part of it is not already present on main) is committed with a clear message, and
the target repo's working tree is left clean with respect to everything this
mission touches.

Context (verified against live substrate this session, absolute paths, no
prescribed resolution beyond "keep both sides"):
- Source worktree: C:\Users\marka\code\mt-feat-sentry-sourcemaps-upload-2026-06-23
  — confirmed this session via `git log --oneline` to carry commit 0c1a3f1 at the
  tip, parent 375e40b, parent of that ed6e33c (the same shared merge-base other
  mt-feat-* worktrees hang off). Do not rewrite that worktree, only read/cherry-pick
  FROM it.
- Confirmed diff stat for 0c1a3f1 (`git show --stat`, this session): 1 file
  changed, 348 insertions(+), 0 deletions(-) — scripts/upload-sourcemaps.mjs, a
  brand-new file, no existing file modified.
- Confirmed diff stat for 375e40b (`git show --stat`, this session): 1 file
  changed, 24 insertions(+), 0 deletions(-) — docs/QC-PIPELINE-SOTA-2026-06-23.md,
  also a brand-new file.
- CRITICAL VERIFIED FINDING — 375e40b is ALREADY INTEGRATED: `git merge-base
  --is-ancestor 375e40b main` exits 0 (true) in the target repo, and
  `git show main:docs/QC-PIPELINE-SOTA-2026-06-23.md` already exists with content
  byte-identical to `git show 375e40b:docs/QC-PIPELINE-SOTA-2026-06-23.md`
  (confirmed via direct diff this session, zero output). Many other mt-feat-*
  worktrees share this same 375e40b tip, and one of them has already been merged
  into main, carrying this doc commit in with it. A real (non `-n`) dry run of
  `git cherry-pick 375e40b` onto main this session confirmed the practical
  consequence: it exits 1 with "The previous cherry-pick is now empty, possibly
  due to conflict resolution," requiring `--skip` (verified, then aborted/reset —
  main was left unchanged). Treat this as expected, not a failure: 375e40b needs no
  further action on main. By contrast, `git merge-base --is-ancestor 0c1a3f1 main`
  is NOT true — that commit is genuinely unintegrated.
- A dry-run `git checkout main && git cherry-pick -n 0c1a3f1` performed this
  session applied cleanly with ZERO conflicts (`git status --porcelain` showed
  exactly `A  scripts/upload-sourcemaps.mjs`, nothing else staged), then was reset
  (`git reset --hard main`) before this mission was authored, so main is unchanged.
  scripts/ already exists as a directory on main with other unrelated files in it
  (confirmed via `git cat-file -e main:scripts`), so this add is expected to apply
  as a clean addition alongside those, not a directory collision. If a collision
  is nonetheless reported at fire time (main having since gained its own
  same-named file), resolve it by keeping BOTH files' content rather than
  overwriting either.
- Target repo TARGET-BRANCH main, this session's HEAD (`git log main --oneline
  -1`): 5e19874. The source worktree and target repo are worktrees of the SAME
  underlying git repository (confirmed via `git worktree list`, and
  `git cat-file -t 0c1a3f1` / `git cat-file -t 375e40b` both succeeding directly
  inside the target repo) — both commits are already reachable objects in the
  target repo without any fetch/remote-add step.
- The repo's default working tree carries pre-existing untracked litter unrelated
  to this feature (api-index-response.json, docs/e2e-report-2026-07-01.json,
  e2e-shots/, feature-catalog-entry.txt, scripts/e2e-runner.mjs) — confirmed
  present both before and after checking out main this session, so it is
  untracked repo-wide litter, not branch-scoped. This mission's "clean working
  tree" claim is scoped to the files it touches (the two ALLOW-FILES entries and
  anything a conflict resolution forces it to touch) — it does not require
  deleting, committing, or otherwise disturbing this pre-existing untracked
  litter, which is out of this mission's scope.
- Neither commit touches any .html file, so no visual-QC header or render-witness
  step is required by this mission (the visual-QC header used by other
  integration missions is deliberately omitted here). Exactly one .mjs file
  (scripts/upload-sourcemaps.mjs) is expected to need `node --check`; confirmed
  this session in the source worktree that `node --check scripts/upload-sourcemaps.mjs`
  exits 0 there already (Node v22.20.0).
- None of the above — the diff stats, the ancestry check, the dry-run git
  mechanics, the commit-message's description of the REST-direct Sentry upload
  flow and its honest-degrade contract — has been verified by actually running the
  script against a Sentry endpoint, a witness, or a test; it is reporting diff
  contents, ancestry facts, and dry-run git mechanics only. That the script
  genuinely behaves as the commit message claims is exactly what this mission's
  own downstream witness plus verdict-panel run is for.

Steps:
  1. In the source worktree
     C:\Users\marka\code\mt-feat-sentry-sourcemaps-upload-2026-06-23, confirm
     commits 0c1a3f1 and 375e40b exist and capture their patches (e.g.
     `git show 0c1a3f1`, `git show 375e40b`, or `git format-patch -2 0c1a3f1`)
     without modifying that worktree. [command] capture-patches
  2. In the target repo C:\Users\marka\code\mt-integration-2026-06-22, check out
     TARGET-BRANCH main at its current HEAD. [command] checkout-main
  3. Attempt `git cherry-pick 375e40b`. Per the verified Context finding above,
     expect this to report the patch is empty (already integrated via another
     branch) — run `git cherry-pick --skip` in that case rather than treating it
     as a failure. If it unexpectedly reports real conflicting content instead of
     an empty patch, resolve by keeping BOTH the target's pre-existing content and
     this feature's content, never dropping either side. [command]
     integrate-docs-commit
  4. Run `git cherry-pick 0c1a3f1` (the commit is already a reachable object in
     this repo — no remote/fetch step needed) or an equivalent merge of that
     single commit, landing scripts/upload-sourcemaps.mjs. [command]
     integrate-script-commit
  5. If step 4 (or step 3, if it turns out not to be empty) reports a conflict on
     either ALLOW-FILES entry or any other path, resolve each conflicted hunk by
     hand so the resolved content contains BOTH the target's pre-existing content
     AND this feature's new content — never `--ours` / `--theirs` and never a
     resolution that deletes either side's lines. [edit] resolve-conflicts
  6. Confirm which files this integration actually touched or conflict-resolved
     (`git diff --name-only` against the pre-mission main tip 5e19874) and filter
     to *.js/*.mjs; run `node --check` on every such file (expected:
     scripts/upload-sourcemaps.mjs) and confirm each exits 0. [command]
     node-check-all
  7. Stage and commit whatever remains uncommitted on TARGET-BRANCH main with a
     clear message identifying the integrated feature and both origin commits
     (e.g. referencing "mt-feat-sentry-sourcemaps-upload-2026-06-23", "0c1a3f1",
     and noting "375e40b" was already integrated). Confirm `git status --porcelain`
     afterward shows no modified or staged entries related to this mission's
     touched files — any pre-existing untracked litter noted in Context above is
     left exactly as found, neither committed nor deleted. [command]
     commit-and-verify-clean

Done means:
  - `git log` on TARGET-BRANCH main in C:\Users\marka\code\mt-integration-2026-06-22
    shows a commit whose message names "mt-feat-sentry-sourcemaps-upload-2026-06-23"
    and/or "0c1a3f1".
  - scripts/upload-sourcemaps.mjs exists in the target repo at TARGET-BRANCH main's
    HEAD with non-trivial content (read the file content to confirm, not assumed):
    it walks dist/ for .js/.js.map files, speaks the Sentry REST API directly to
    create a release, upload files, and mark a deploy, and exits 0 (skip, not
    fail) when SENTRY_AUTH_TOKEN/SENTRY_ORG/SENTRY_PROJECT are unset or dist/ has
    no .js files.
  - docs/QC-PIPELINE-SOTA-2026-06-23.md exists in the target repo at TARGET-BRANCH
    main's HEAD with non-trivial content describing the 8 mandatory QC gates,
    escape hatches, and foreign-frontier/adversarial-refuter panel policy — whether
    that presence traces to this mission's own commit or to the pre-existing
    integration confirmed in Context is immaterial; the file being present with
    that content at HEAD is what is checked.
  - If a conflict occurred during integration on either file, both sides' content
    are confirmed present in the resolved file by reading the resolved lines, not
    assumed.
  - `node --check` exits 0 on scripts/upload-sourcemaps.mjs, and on any other
    .js/.mjs file this integration touched or conflict-resolved.
  - `git status --porcelain` in the target repo, on TARGET-BRANCH main, shows no
    modified or staged entries at the end of the mission — pre-existing untracked
    litter unrelated to this feature (documented in Context above) is not this
    mission's to clean up and is left untouched either way.
  - Nothing in this mission's own receipts is treated as proof that
    scripts/upload-sourcemaps.mjs's Sentry REST calls, release-version derivation,
    or honest-degrade exit-0 paths actually work against a live Sentry API, or that
    the QC-pipeline doc's policy is complete or correct — that judgment is
    deferred entirely to this mission's downstream witness and verdict-panel run
    against the integrated result.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

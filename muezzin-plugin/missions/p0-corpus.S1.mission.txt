MISSION-ID: P0-CORPUS.S1
PARENT: P0-CORPUS  (sub-mission 1 of 3, auto-split by the size ceiling)
REQUIRES: none (first sub-mission of 3 in tartib order)

Niyyah: the factory must never solve a problem Mark's past builds already solved — unmined knowledge is indistinguishable from no knowledge. This mission is also the muezzin's first long autonomous run: every receipt it produces is double evidence.

Maqsad: this is part 1 of 3 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): a source census and pattern library of everything Mark has already built and collected, consumable by all future dossier and build missions. Done means: (1) sources.json — every git repo, project folder, AnythingLLM workspace, and Cloudflare deployment discovered (not hand-listed), each with a type tag and a mine/skip verdict; (2) one pattern card per 'mine' source — what it does well, recurring components, design tokens, copy patterns, vertical fit, with evidence references; (3) a knowledge inventory — what the AnythingLLM business-books workspace actually contains (topical map from sample queries), what the old website-pipeline's contracts offer, what the Stitch exports show; (4) a patterns library assembling it all, indexed, every entry citing its source. Time-box: stop cleanly at census+verdicts+cards completion or 48 hours, whichever comes first — partial work committed with a state note beats unfinished ambition.

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - Scaffold the sandbox output tree (census/raw, inventory, cards, library) so later subdirectory writes have parents. SCOPE NOTE: this mission is large and multi-subject — concrete pattern cards below cover only the clearly-'mine' named seeds (2boots, vanlife, warroom); any additional 'mine' sources DISCOVERED by the census (step 13) need upstream autosplit into further card steps, and the 48h time-box means census+verdicts+cards committed is an acceptable clean stop.
  - DISCOVERY (not hand-list): recursively scan drive roots C:/ D:/ E:/ for project signals (.git, package.json, wrangler.toml) beyond the named seeds, dumping every full path into a raw census file. Read-only against sources.
  - Census Cloudflare Pages projects via authed wrangler CLI (must surface projects beyond the known 2boots/laynas-cookies/regulativity). Witness asserts a non-empty dump; an auth failure is captured in-file as a census gap rather than blocking.
  - Census AnythingLLM workspaces via developer API GET /api/v1/workspaces. UNVERIFIED: API key env-var name ($env:ANYTHINGLLM_API_KEY) could not be checked on this dispatch; if absent the call fails and the step records a gap. Witness asserts the response actually contains workspace data.
  - Census local Forgejo repos via GET /api/v1/repos/search (localhost:3002). Witness asserts a non-trivial JSON payload was written.
  - Census GitHub user nxtlvlyt public repos (anonymous API sees few). The private-enumeration gap is recorded as a first-class census fact in sources.json (step 13), not guessed. Witness asserts a JSON payload was written.
  - Live-site archaeology: fetch the four named live sites (2boots.ca, muddytires.ca, laynascookies.ca, regulativity.pages.dev) into the sandbox for later design-token/copy extraction. Witness requires at least 3 of 4 to respond (one outage is a gap, not a mission failure).
  - Knowledge-inventory gather: sample-query the AnythingLLM business-books workspace via POST /api/v1/workspace/business-books/chat across several business topics, capturing responses for the topical map. UNVERIFIED: chat endpoint/key on this dispatch. Witness asserts a non-trivial capture.

Done means: steps P0-CORPUS.S1.1..P0-CORPUS.S1.8 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — (1) sources.json — every git repo, project folder, AnythingLLM workspace, and Cloudflare deployment discovered (not hand-listed), each with a type tag and a mine/skip verdict; (2) one pattern card per 'mine' source — what it does well, recurring components, design tokens, copy patterns, vertical fit, with evidence references; (3) a knowledge inventory — what the AnythingLLM business-books workspace actually contains (topical map from sample queries), what the old website-pipeline's contracts offer, what the Stitch exports show; (4) a patterns library assembling it all, indexed, every entry citing its source. Time-box: stop cleanly at census+verdicts+cards completion or 48 hours, whichever comes first — partial work committed with a state note beats unfinished ambition.

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
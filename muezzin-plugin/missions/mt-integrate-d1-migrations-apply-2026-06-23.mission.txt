MISSION mt-integrate-d1-migrations-apply-2026-06-23
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - scripts/apply-d1-migrations-2026-06-23.mjs
  - docs/D1-MIGRATIONS-APPLIED-2026-06-23.md
  - docs/QC-PIPELINE-SOTA-2026-06-23.md
REQUIRES: none

Niyyah: The feature "d1-migrations-apply-2026-06-23" (F4: an applier script that pushes
outstanding D1 migrations to the remote muddytires-pois binding, treating a "duplicate
column name" wrangler error as idempotent already-applied rather than a real failure,
plus its own receipts doc and a QC-pipeline doc) sits finished on three commits in its
source worktree but has never been integrated into this repo's tree. This mission's
entire job is mechanical integration — land the three commits onto the current tree
without losing either side's work, and prove the result is syntactically sound and
committed. Nothing this mission does, and nothing about this mission's own authorship,
constitutes a judgment that the feature's CODE is correct: not that treating "duplicate
column name" as always-safe-to-swallow is actually safe, not that the receipts doc's
per-migration outcome table (applied/already-applied/missing/error) honestly reflects
what really happened against the live remote D1 rather than being fabricated, not that
blocking the photo-dedup migration on a missing review_photos table was the right call
instead of a masked bug, and not that the QC-pipeline doc's 8 gates are complete or
well-designed. That determination belongs entirely to this mission's own witness +
verdict-panel run, which inspects the integrated result AFTER this mission produces it —
not to the act of integration itself. Authoring or executing this mission is not
verification of the feature.

Maqsad: The commits "375e40b docs(qc): SOTA-uplift QC pipeline — 8 mandatory gates +
escape hatches", "3bb992b feat(d1): F4 applier script for outstanding D1 migrations to
muddytires-pois", and "acae717 docs(d1): receipts for F4 D1 migrations apply run —
2026-06-23" are cherry-picked or merged, in that chronological order, from the source
worktree onto the current HEAD of C:\Users\marka\code\mt-integration-2026-06-22, with any
conflicts resolved by keeping BOTH sides — this repo's existing unrelated changes AND the
feature's changes, never silently dropping either — every changed .js/.mjs file passes
`node --check`, the result is committed with a clear message, and the target repo's
working tree is left clean with respect to everything this mission touches.

Context (verified against live substrate this session, absolute paths, no prescribed
resolution beyond "keep both sides"):
- CORRECTED SOURCE PATH: the feature was described as living at
  C:\Users\marka\code\d1-migrations-apply-2026-06-23, but that exact path does not exist
  on disk. `git worktree list` on the shared muddytires-pages repo confirms the real
  worktree carrying these three commits is C:\Users\marka\code\mt-feat-d1-migrations-
  apply-2026-06-23 (branch feat/d1-migrations-apply-2026-06-23, tip acae717) — this
  mission uses that verified real path, not the unverified one. Do not rewrite that
  worktree, only read/cherry-pick FROM it.
- The source worktree and the target repo are both `git worktree` checkouts of the same
  underlying muddytires-pages repository (confirmed via `git worktree list`), so all
  three commit objects are already reachable from the target without adding a remote or
  fetching.
- Confirmed diff stat across the three commits this session (`git diff --stat` against
  the shared parent): 3 files changed, 655 insertions(+), 0 deletions(-) — all pure
  adds, nothing modified or deleted: docs/D1-MIGRATIONS-APPLIED-2026-06-23.md (+242,
  commit acae717), docs/QC-PIPELINE-SOTA-2026-06-23.md (+24, commit 375e40b), and
  scripts/apply-d1-migrations-2026-06-23.mjs (+389, commit 3bb992b).
- Chronological order (oldest to newest) is 375e40b, then 3bb992b, then acae717 — the
  source worktree's HEAD sits exactly on acae717 with 3bb992b and 375e40b as its direct
  ancestors in that order.
- `git merge-base --is-ancestor` checks run this session against the target repo's
  current HEAD show 375e40b is ALREADY an ancestor of the target's HEAD (its file,
  docs/QC-PIPELINE-SOTA-2026-06-23.md, already exists in the target tree with byte-
  identical content to what 375e40b introduces — confirmed by reading both). Cherry-
  picking 375e40b again is therefore expected to report an EMPTY pick (no changes left
  to apply), not a conflict. 3bb992b and acae717 are NOT ancestors of the target's
  current HEAD — neither scripts/apply-d1-migrations-2026-06-23.mjs nor docs/D1-
  MIGRATIONS-APPLIED-2026-06-23.md exist anywhere in the target tree yet (confirmed by
  direct file check this session), so those two are expected to apply as clean adds. If
  the target tree has moved further by fire time such that any of these three ancestor
  facts no longer hold, that is a changed condition from this authoring session — treat
  each commit on its own merits (skip if empty, resolve keeping both sides if it
  conflicts, apply cleanly otherwise) rather than assuming today's exact state.
- Target repo HEAD this session (`git log --oneline -1`): 28cbc72, on branch
  feat/qc-concern-sentry-error-capture-sentry-init-js-2026-06-25. That branch carries
  pre-existing untracked litter unrelated to this feature: api-index-response.json,
  docs/e2e-report-2026-07-01.json, e2e-shots/, feature-catalog-entry.txt, scripts/
  e2e-runner.mjs. This mission's "clean working tree" claim is scoped to the three
  ALLOW-FILES entries and anything a conflict resolution forces it to touch — it does
  not require deleting, committing, or otherwise disturbing that pre-existing litter,
  which is out of this mission's scope and not this mission's to clean up.
- No TARGET-BRANCH header is set, deliberately: the job is to land on whatever the
  target repo's HEAD is pointing to at fire time ("the current HEAD"), not to force a
  checkout of a specific branch. Do not switch branches as a step of this mission.
- This diff touches zero .html files (confirmed: none of the three changed files end in
  .html), so this mission carries no visual/render-witness gate or headless-browser step
  — there is no rendered page for this diff to change.
- Only scripts/apply-d1-migrations-2026-06-23.mjs is JavaScript among the three changed
  files; `node --check` must still be run on any additional .js/.mjs file conflict
  resolution ends up touching, if any.

Steps:
  1. In the source worktree C:\Users\marka\code\mt-feat-d1-migrations-apply-2026-06-23,
     confirm all three commits exist in this order — `git log --oneline -3` should show
     acae717, 3bb992b, 375e40b top to bottom — and capture their patches (e.g. `git show
     <sha>` or `git format-patch -3 acae717`) without modifying that worktree. [command]
     capture-patches
  2. In the target repo C:\Users\marka\code\mt-integration-2026-06-22, at whatever
     branch/HEAD is currently checked out (no branch switch), apply the three commits in
     chronological order via `git cherry-pick 375e40b 3bb992b acae717` (the commit
     objects are already reachable from the shared object store; no remote needed) or an
     equivalent merge of the same three commits in the same order. [command]
     integrate-commits
  3. For each commit in that sequence: if cherry-pick reports it is now EMPTY (no changes
     left to apply — expected for 375e40b per Context above), run `git cherry-pick --skip`
     and continue to the next commit; this is not a failure. If cherry-pick instead
     reports a real conflict on any file, resolve every conflicted hunk by hand so the
     resolved content contains BOTH the target's pre-existing lines AND the feature's
     added lines — never `--ours` / `--theirs` and never a resolution that deletes either
     side — touching only ALLOW-FILES entries to complete a resolution, then `git add`
     the resolved file(s) and run `git cherry-pick --continue`. [edit]
     resolve-or-skip-each-commit
  4. Run `node --check scripts/apply-d1-migrations-2026-06-23.mjs`, and `node --check` on
     every other .js/.mjs file the integration touched or any conflict resolution
     modified, confirming each exits 0 (the expected additional list is empty, since the
     other two changed files are .md; confirm that emptiness explicitly rather than
     assuming it). [command] node-check-all
  5. Stage and commit the integrated result in the target repo with a clear message
     identifying the integrated feature and its origin commits (e.g. referencing
     "d1-migrations-apply-2026-06-23" and at least "acae717" and "3bb992b" — 375e40b may
     already be present and produce no new commit of its own if it was skipped as empty).
     Then run `git status --short` and confirm none of the three ALLOW-FILES entries
     appear in the output (all fully committed) and that no file outside this mission's
     ALLOW-FILES was staged, modified, or committed while completing this mission.
     [command] commit-and-verify-scoped-clean

Done means:
  - `git log` in C:\Users\marka\code\mt-integration-2026-06-22 shows a commit (or
    commits) whose message names "d1-migrations-apply-2026-06-23" and/or at least one of
    "acae717" / "3bb992b" (375e40b's content may already have been present before this
    mission ran, per Context above, and is not required to appear as a brand-new commit
    message if so).
  - scripts/apply-d1-migrations-2026-06-23.mjs exists at the target repo's REPO-ROOT and
    contains both the strings 'duplicate column name' and 'muddytires-pois' (confirms the
    real script landed, not a stub or a conflict-mangled partial).
  - docs/D1-MIGRATIONS-APPLIED-2026-06-23.md exists at the target repo's REPO-ROOT and
    contains both the strings 'already-applied' and 'review_photos' (confirms the real
    receipts doc landed, including the blocked-migration row).
  - docs/QC-PIPELINE-SOTA-2026-06-23.md exists at the target repo's REPO-ROOT and
    contains the string '8 mandatory gates' (confirms the doc is present and intact,
    whether it landed fresh via this mission or was already there per Context above).
  - `node --check` exits 0 on scripts/apply-d1-migrations-2026-06-23.mjs and on every
    other .js/.mjs file this integration touched or conflict-resolved.
  - `git status --short` in the target repo shows no entry for any of the three
    ALLOW-FILES paths — all fully committed with nothing pending. Any OTHER pre-existing
    untracked or modified file already present in the target repo before this mission
    started (documented in Context above) is outside this mission's ALLOW-FILES scope
    and is not this mission's concern; its continued presence does not fail this
    Done-means.
  - This Done-means covers integration mechanics only: intact files, a clean or
    correctly-resolved history, a passing syntax check, and a scoped-clean tree. It makes
    no claim that the applier script's idempotency heuristic is safe, that the receipts
    doc's outcome rows are an honest record of a real remote D1 run, or that the QC-
    pipeline doc's 8 gates are adequate — that is exactly what this mission's own witness
    and verdict-panel run exists to determine.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

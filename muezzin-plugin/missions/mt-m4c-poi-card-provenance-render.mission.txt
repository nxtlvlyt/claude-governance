MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.M4C-POI-CARD-PROVENANCE-RENDER
REPO-ROOT: E:\AI_Storage\muddytires-frontend-wt
ALLOW-FILES:
  - js/poi-provenance.js
  - map.html
REQUIRES: M4b done (pois.json serves source_url/source_name/fetched_at/confidence). CONDUCTOR confirms M4b DONE before firing.

Niyyah: render the §14 Isnad (where this spot came from) + Rukhsa (how stale, honestly) on the POI popup, ADDITIVELY — never rewrite the dense 358KB map.html in place. Honest freshness above all: a NULL fetched_at renders "freshness unconfirmed", NEVER a fabricated date and NEVER a false "verified".

Maqsad: a NEW module js/poi-provenance.js that wraps the global popup(p) to append a provenance/freshness line reading source_name/source_url/fetched_at/confidence; map.html additively loads it via ONE <script> tag. Priority-A item M4 (render half).

Context (facts only — VERIFIED via an Explore survey of this repo this session, file:line):
- map.html (358KB, minified, content on a single physical line ~276) defines a TOP-LEVEL function `popup(p)` at ~L72: `function popup(p){var nt=p.node_type,m=p.meta||{},cc=m.crown_camping||{},t=m.osm_tags||{},k=kindOf(p),det='',bdg='';...}` — it returns popup HTML and is invoked at ~L147 `L.circleMarker([...]).bindPopup(popup(p))`. Because it is a top-level function declaration, it is reachable as `window.popup`.
- PROVEN additive pattern (verified in this repo): js/add-spot-worker.js and js/reviews.js are self-invoking IIFEs that attach to window (e.g. `window.reviewSpot`, `window.computeCommunityConfidence`) and are loaded by `<script src="js/NAME.js" defer></script>` tags in map.html. COPY THIS EXACTLY.
- The features now carry (after M4b): properties.source_name ('OpenStreetMap'), properties.source_url, properties.fetched_at (often NULL), properties.confidence ('UNVERIFIED' for OSM bulk rows). Community spots have these as null/undefined.

AUTHOR js/poi-provenance.js as a self-invoking IIFE that:
  - On load, wraps the global popup builder additively:
      var _origPopup = window.popup;
      if (typeof _origPopup === 'function') {
        window.popup = function(p){ var h = _origPopup(p); try { return h + provenanceHtml(p); } catch(e){ return h; } };
      }
  - provenanceHtml(p): returns '' (NO-OP) when !p || !p.source_name (e.g. community spots — they show contributor provenance elsewhere). Otherwise returns a small additive block:
      * Isnad: "source: <source_name>" — if p.source_url, wrap source_name in <a href="<escaped source_url>" target="_blank" rel="noopener noreferrer">.
      * Rukhsa (freshness): if p.fetched_at is a non-empty value -> "checked " + a relative-time string from it; if NULL/empty -> the literal honest text "freshness unconfirmed — verify before relying". NEVER invent a date.
      * Confidence chip: a small span showing p.confidence (HIGH/MEDIUM/LOW/UNVERIFIED), defaulting to UNVERIFIED if absent. This is COMMUNITY/DATA confidence, NOT a safety all-clear (TAQWA: it never implies a place is "safe").
  - XSS-escape every interpolated value (escape &<>"' ) — match the escaping the existing js/ modules use (see add-spot-worker.js / reviews.js for the escapeHtml helper pattern; include a local escape helper).
  - Attach for testability: `if (typeof window!=='undefined'){ window.provenanceHtml = provenanceHtml; }`.
  - Be defensive: never throw into the popup path (wrap in try/catch as above).

Steps:
  1. (author) Write js/poi-provenance.js per the spec above. node --check must pass.
  2. (edit, ADDITIVE) Add EXACTLY one line `<script src="js/poi-provenance.js" defer></script>` to map.html, immediately adjacent to the existing `<script src="js/...defer></script>` tags (e.g. right after js/reviews.js). Do NOT modify ANY other byte of map.html.
  3. (verify) node --check js/poi-provenance.js; grep map.html for exactly one new poi-provenance script tag; `git diff --stat` must show map.html changed by +1 line only (no other map.html edit), js/poi-provenance.js new.
  4. (commit) git add js/poi-provenance.js map.html ; git commit --no-verify -m "M4c: additive POI provenance/freshness render on popup (Isnad + Rukhsa + confidence chip)"

Done means (acceptance contract): js/poi-provenance.js exists and is valid (wraps window.popup additively, no-op without source_name, honest NULL-freshness text, XSS-safe, never throws into the popup); map.html additively loads it (exactly +1 <script> line, ZERO other changes — git diff --stat proves it); node --check passes; committed per-file --no-verify. Receipt quotes git diff --stat (map.html +1) and the module's wrap + provenanceHtml key lines.

Constraints: ADDITIVE ONLY on map.html (one <script> tag — NEVER an in-place rewrite of inline JS; the file exceeds every model's context and an in-place edit will not emit and would corrupt it; this is the add-spot-ui-5 lesson); all new logic lives in js/poi-provenance.js; XSS-escape all rendered values; honest freshness (NULL fetched_at -> "unconfirmed", never a fabricated date or false "verified"); TAQWA (provenance/confidence is informational, never a safety all-clear); preview-only (no apex deploy); explicit per-file git add; --no-verify.

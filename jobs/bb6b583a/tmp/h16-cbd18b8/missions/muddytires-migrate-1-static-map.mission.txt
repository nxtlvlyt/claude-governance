MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.1
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - index.html
  - pois.json
  - landing.html
  - _headers
  - README.md
  - wrangler.toml
REQUIRES: code-repo
# target repo STAGED 2026-06-15: E:\AI_Storage\muddytires-pages is a git toplevel (commit 1e03f36, the static map backup). Gate cleared — this mission only needs 0.4.

Maqsad: Get the PUBLIC muddytires.ca map back online on Cloudflare's edge — independent of the (crashed) NAS — the operator's #1 goal. This is Stage 1 of the migration plan: a STATIC deploy from the verified local backup, no live server.

niyyah: convert the muddytires migration from a PLAN into a real receipted mission (it was a note while backlog drained ahead of the #1 goal). Source open: the migration plan + the verified backup E:\nas-recovery\muddytires\public-site-backup.tar (the public site, incl. pois.json dated 2026-06-14 + landing.html). Guard against: touching the NAS; shipping a site that still calls the dead live endpoint.

CONTEXT (verified this session): the public site is a PHP+DB app, but the map already carries its own static fallback — pois.json (2.7MB, in the backup) is the documented fallback (index.html ~L128), and landing.html is the cron-snapshot of landing.php. So Stage 1 ships the static map + homepage; the dynamic features (contribute/oracle/search, sub-zoom clustering) are later stages.

PRECURSOR (conductor stages before this fires; REQUIRES:target-repo-staged): extract the static subset from public-site-backup.tar into a NEW git repo at E:\AI_Storage\muddytires-pages (git init + first commit), containing map/index.html, map/pois.json, map/landing.html, map/img/*. (0.4 will not git-init an existing project, so the repo must pre-exist.)

Done means (verifiable, all offline / local):
- index.html (the Leaflet map) repointed so its pin-data fetch reads the STATIC ./pois.json (the documented fallback path) instead of the live pois.php endpoint; no request goes to the dead NAS / live endpoint (grep the built files: zero references to 192.168.2.27 or the live pois.php host).
- landing.html present as the homepage (or an index redirect), images present, pois.json present.
- _headers (Cloudflare Pages) sane; README.md documents "static Stage-1, data refreshed by a later regeneration job".
- A local static check passes: serving the repo dir, index.html loads and references only same-repo assets + pois.json (no NAS/live-endpoint calls).
- Committed in the muddytires-pages repo.

DEPLOY IS PART OF THIS MISSION (correction 2026-06-15: wrangler IS authed as markabass@gmail.com — the deploy is NOT operator-only; carving it out was scope-shrinking). The mission deploys to Cloudflare Pages via wrangler AND points muddytires.ca at it. The ONLY operator touch is a one-word "go" immediately before the LIVE-APEX DNS flip — purely because it changes a live production domain (outward-facing courtesy, per CLAUDE.md), NOT because the mission can't. The site is currently DOWN (Error 1033), so the flip can only improve things; operator may pre-authorize ("just flip it") to remove even that pause.

Constraints: code-repo, muddytires-pages repo only, NAS UNTOUCHED, secrets from the backup's .env are NEVER copied in (static site needs none).

SECURITY GATE (added 2026-06-15, operator's vibe-coded-site vuln review). Two verified findings in the staged repo:
(a) BACKEND SOURCE LEAK: the repo carries the dynamic PHP files (oracle.php, search.php, contribute.php, contribute_ocr.php, contribute_voice.php, community_edit.php, pois.php, etc.). Cloudflare Pages does NOT execute PHP — it would serve these as PLAINTEXT, leaking backend logic (the x-api-key header construction, env var names like ANTHROPIC_API_KEY/AIML_API_KEY/QDRANT_API_KEY, the MODERATOR_PIN auth flow). The Stage-1 static deploy MUST NOT publish any .php or .env file. Move them out of the deploy set (e.g. into a non-deployed _backend/ folder, or a wrangler/.assetsignore exclusion) — keys are read via env() so no live secret is in the source, but the source itself must not ship.
(b) CLIENT-SIDE MAP KEY (index.html L6): a MapTiler tile key is embedded in the satellite layer URL. Client-side map keys are inherently public (they ship to every browser) and were already exposed on the old live site, so the deploy does NOT increase exposure — DO NOT block the deploy on it and DO NOT try to hide it. The correct mitigation is DOMAIN RESTRICTION in the MapTiler dashboard (allowlist muddytires.ca + the *.pages.dev preview origin), which is operator-identity-bound (his MapTiler account) — surfaced to the operator, not done by the mission. The mission may optionally read the key from a same-repo config for easier future rotation, but must not remove the satellite layer.

# SIMPLIFIED 2026-06-15 18:42 (planner was emitting "no valid JSON micro_queue" on the prior 5 prose-heavy steps). The .php backend is ALREADY moved out of the deploy dir (to E:\AI_Storage\muddytires-backend) so the security gate is satisfied; landing.html/_headers/README were nice-to-haves and are dropped for Stage-1. Three clean atomic steps (Step 4 removed as wrangler pages domain is unsupported):
Steps:
1. command - repoint all pois.php fetches in index.html to the static ./pois.json via a TARGETED in-place string replace (index.html is 360KB — must NOT be re-emitted whole by an edit seat; a command does the one-token replace directly). validation_command: pwsh -NoProfile -Command "(Get-Content index.html -Raw) -replace 'pois\.php','pois.json' | Set-Content index.html -NoNewline -Encoding utf8; if (Select-String -Path index.html -Pattern 'pois\.php' -Quiet) { exit 1 } else { exit 0 }"
2. command - create the Cloudflare Pages project named muddytires if it does not already exist (the deploy in the next step fails with code 8000007 "Project not found" without it). Must be idempotent — tolerate "already exists" and succeed if the project is present either way. Reference: wrangler pages project create muddytires --production-branch=main (ignore an already-exists error, then confirm via `wrangler pages project list` containing muddytires).
3. command - deploy the repo to Cloudflare Pages via the authed wrangler (project now exists). validation_command: wrangler pages deploy . --project-name=muddytires --commit-dirty=true


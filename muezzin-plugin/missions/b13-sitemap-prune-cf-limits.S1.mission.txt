MISSION-ID: M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - workers/sitemap-prune/index.js
PARENT: M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS  (sub-mission 1 of 2, auto-split by the size ceiling)
REQUIRES: none (first sub-mission of 2 in tartib order)

Niyyah: fix the 3 SOTA-panel-confirmed defects on the original Sonnet build of sitemap-prune (wdsq4mxb2 — BLOCK at sha 530457f). These are real production-risk issues: uncaught D1 awaits become CF 1101 errors, BATCH_SIZE=200 exceeds Cloudflare's 50 subrequest limit on Free plan, and unvalidated sitemap URLs allow SSRF on a maliciously-edited sitemap. Honest about CF limits — the worker would silently fail in production.

Maqsad: this is part 1 of 2 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): patch the existing branch feat/b13-sitemap-prune-2026-06-23 with the corrective patches below. Re-fire the panel and confirm pass/concern.

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - Sync the existing worktree branch feat/b13-sitemap-prune-2026-06-23 to current remote HEAD so all edits land on the latest SHA. (Worktree path ../mt-feat-b13-sitemap-prune is a read-only mission reference; the engine runs in the sandbox cwd which resolves workers/sitemap-prune/index.js.)
  - Single consolidated authoring edit of index.js applying ALL seven panel fixes (one-writer rule forces one edit step per file). (1) Wrap each await upsert404() and await writeCursor() in handleRefresh (panel-cited ~:287/:303, UNVERIFIED — Read before editing) in try/catch: catch does console.error(e) and returns Response.json({ok:false,error:'db'},{status:500}). (2) Wrap the entire default fetch() export body (~:348-356, UNVERIFIED) in an outer try/catch returning Response.json({ok:false,error:'internal',detail:String(e)},{status:500}) so no CF 1101 escapes. (3) ensureSchema catch matches ONLY /already exists|duplicate column/i and sets schemaEnsured=true only on that expected path; any other error re-throws and schemaEnsured stays false. (4) BATCH_SIZE correction: the load-bearing constraint is total external fetches per /refresh ≤ 40 (10-buffer under CF Free 50-external-subrequest ceiling, D1/KV on separate budget). Because each URL costs HEAD + up to GET fallback (2 fetches), set BATCH_SIZE so that 1 + 2*BATCH_SIZE ≤ 40 — RECOMMEND BATCH_SIZE=18 if HEAD+GET retained; if collapsed to GET-only use ≤39. Do NOT ship the literal 40 (worst case 1+2*40=81 > 50, the exact bug). Add a header comment stating the budget arithmetic and citing the current CF subrequest limit (https://developers.cloudflare.com/workers/platform/limits/); process at most BATCH_SIZE URLs and return nextCursor for re-invocation. (5) SSRF: add in-code const ALLOWED_HOSTS (default muddytires.ca / www.muddytires.ca); for each parsed <loc>, new URL(loc).hostname must be in the allowlist else console.warn skip+continue; reject on URL parse failure too. (6) Optimistic locking: SELECT cursor_value WHERE id=1, then UPDATE ... SET cursor_value=? WHERE id=1 AND cursor_value=<old>; if result.meta.changes===0 a concurrent /refresh raced → Response.json({ok:false,error:'conflict'},{status:409,headers:{'Retry-After':'2'}}). (7) DOMParser is NOT available in the Workers runtime (verified) — do not add a DOMParser branch; extend the regex to also match <![CDATA[...]]>-wrapped and namespaced <loc>, de-dup via Set, and add a comment documenting the regex limitation and naming HTMLRewriter as the upgrade path. wrangler.toml is intentionally NOT edited (host allowlist kept as in-code default; 2 of 3 blind reads agree no toml change is required by any finding).
  - Witness the outer fetch() try/catch (finding #2): the 'internal' structured-error response is present.
  - Witness the inner D1 try/catch around upsert404()/writeCursor() (finding #1): the 'db' error response is present.
  - Witness ensureSchema now matches only expected errors (finding #3): the 'already exists' guard pattern is present.
  - Witness the CF subrequest-budget documentation accompanying the reduced BATCH_SIZE (finding #4): the 'subrequest' budget header comment is present.
  - Witness the SSRF host allowlist (finding #7): the muddytires host check is present.
  - Witness cursor optimistic-locking conflict handling (finding #6): the 409 conflict response is present.

Done means: steps M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1.1..M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1.8 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — panel re-check pass/concern; BATCH_SIZE ≤40; outer try/catch in fetch(); ensureSchema catches only expected errors; URL host-validation in place; cursor optimistic-locking in place; all 4 BLOCK findings + 3 CONCERN findings have visible code addressing them.

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
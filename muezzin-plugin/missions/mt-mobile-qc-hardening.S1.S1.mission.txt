MISSION-ID: mt-mobile-qc-hardening.S1.S1
MISSION-CLASS: code-repo
PARENT: mt-mobile-qc-hardening.S1
TARTIB-INDEX: 1 of 2
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - scripts/e2e-runner.mjs
VISUAL-QC-REQUIRED
REQUIRES: none (first in tartib order)
STEPS: 6

RESCOPED 2026-07-03T09:1x after x2 (two DIFFERENT deaths on the same step — the map.html D1
edit: attempt 1 landed-but-verify-string-mismatched, attempt 2 witness-REJECT-unrepaired).
Root: dense in-place edits to the 393KB map.html are the KNOWN windowed-edit engine gap
(INBOX HIGH 2026-06-18). D1 is SPLIT OUT to mt-mobile-lane-fix.S1 (additive pattern — the
receipted cure for map.html). THIS mission is now e2e-runner-only: the detector is the CLASS
fix and must not be hostage to the hardest edit class. map.html and verify-popups-e2e.mjs
removed from ALLOW-FILES (read-only references).

PARENT MAQSAD: Operator audit 2026-07-03 ("will the zoom buttons always block the words? the

Maqsad: sub-mission 1 of 2 — Idempotent baseline: record branch + HEAD and check whether scripts/e2e-runner.mjs already contains an isMobile viewport entry; never fails on existence, just reports CLEAN_ALREADY vs CLEAN_BASELINE. through Commit scripts/e2e-runner.mjs (the ONE allow-file — Maqsad corrected 2026-07-03T09:4x after the laguna witness caught the stale "three ALLOW-FILES" splitter wording contradicting the rescope) by explicit name, naming this mission and the 2026-07-03 operator-audit receipt; idempotent no-op if already committed.

X2 CONCLUSION RECEIPT 2026-07-03T07:37:23 (appended post-amendment so the RETRO-REPEAT gate
sees honest lineage, not a bare mtime touch): attempt 2 died on the SAME absent-file wall at
step 1 — the amendment below was committed 07:36, one minute before attempt 2 concluded, and
could not affect its already-generated plan. The next fire is the FIRST run that plans
against the corrected text.

AMENDED 2026-07-03T07:4x (two receipted corrections after attempt-1 died at step 1):
(1) scripts/e2e-runner.mjs is ABSENT from HEAD — it lives only in stranded commit e31469f
("feat(qc): e2e-runner.mjs — feature-catalog-driven visual end-to-end QC"); step 1 now
RESTORES it before anything else. (2) The overlap detector (D3) moves to e2e-runner.mjs:
scripts/verify-popups-e2e.mjs is a ZERO-BROWSER data-truth checker by design (fetch+logic,
no playwright — its own header says so) and cannot host boundingBox assertions. The 07:23
laguna witness REVISE flagged exactly this and was right. verify-popups-e2e.mjs is NOT
edited by this mission; it stays in ALLOW-FILES only as a read reference.

Steps:
  1. Baseline + RESTORE: record branch + HEAD; if scripts/e2e-runner.mjs is absent from the working tree, restore it from the stranded commit: git checkout e31469f -- scripts/e2e-runner.mjs (exit-code witness: Test-Path scripts/e2e-runner.mjs after). If it already contains an isMobile viewport entry report CLEAN_ALREADY, else CLEAN_BASELINE. Never exit 1 on existence. [command]
  2. D2: add the second (mobile, 375x812, deviceScaleFactor 2, isMobile+hasTouch) viewport to scripts/e2e-runner.mjs's viewport matrix alongside the existing 1440x900 desktop entry, running the full existing check suite once per viewport and tagging results per-viewport. [edit] scripts/e2e-runner.mjs
  3. D3: add the overlap detector to scripts/e2e-runner.mjs (the BROWSER runner — it owns boundingBox) — enumerate floating overlay controls by real id/class per viewport, assert zero forbidden pairwise bounding-box intersections (parent/child exempt), assert no control covers more than 10% of the aurora chip or open Apply bar, print every pair checked, and hard-fail if fewer than 6 controls are found. [edit] scripts/e2e-runner.mjs
  4. Verify scripts/e2e-runner.mjs contains BOTH the dual-viewport matrix (1440x900 + 375x812 with isMobile flag) AND the overlap detector (boundingBox usage, per-pair CHECKED receipt lines, coverage floor of 6). [verify]
  5. Scoped cleanliness: git status --porcelain -- scripts/e2e-runner.mjs shows only this file modified. [verify]
  6. Commit scripts/e2e-runner.mjs by explicit name, naming this mission, restore-source e31469f, and the 2026-07-03 operator-audit receipt; idempotent no-op if already committed. [command]

Done means: map.html, scripts/e2e-runner.mjs, scripts/verify-popups-e2e.mjs exist/are updated as specified — verify by headless-browser render, not by reading the code

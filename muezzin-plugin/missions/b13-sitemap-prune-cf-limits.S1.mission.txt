MISSION-ID: M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - workers/sitemap-prune/*.js
  - workers/sitemap-prune/wrangler.toml
PARENT: M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS  (sub-mission 1 of 2, auto-split by the size ceiling)
REQUIRES: none (first sub-mission of 2 in tartib order)

Niyyah: the sitemap-prune worker should survive production load. The original build assumed Cloudflare Workers limits the panel measured exceeded by 8x, and uncaught D1 awaits would surface as CF 1101 errors to users. Close the substrate-verified defects — not the model's confidence, the documented Cloudflare ceiling.

Maqsad: this is part 1 of 2 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): the worker runs safely on Cloudflare Workers Free plan AND Paid plan, with no silent failure modes. Re-running the 5-seat panel against the new diff returns panel_verdict in {pass, concern}.

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - Author the COMPLETE hardened sitemap-prune worker in ONE emission (one-writer: this is the sole authoring step for index.js). Read the existing worker first and PRESERVE all working crawl/D1 logic; then close every panel finding in the single file: (1) wrap upsert404+writeCursor awaits in handleRefresh in try/catch returning typed structured JSON {error,op} with status 500; (2) wrap the exported top-level fetch(request,env,ctx) body in try/catch returning Response.json({error},{status:500}) so NO path yields CF 1101; (3) ensureSchema sets schemaEnsured=true ONLY when the caught error matches benign idempotent-DDL signals (/already exists|duplicate column/i) and RETHROWS all other errors; (4) cap BATCH_SIZE to <=20 with a named constant + arithmetic comment, and return a continuation cursor in the JSON so external fetch() count per /refresh is provably <50 (Free ceiling) — also satisfies Paid; (5) make <loc> extraction CDATA- and namespace/prefix-tolerant (handle <![CDATA[...]]> and <sm:loc>/<ns:loc>), dependency-free; (6) serialize concurrent /refresh via a D1 optimistic compare-and-swap lock row (UPDATE ... WHERE held=0 OR stale-expiry; if 0 rows changed return Response.json({error:'conflict'},{status:409}); release in finally) — pure-D1, stays inside ALLOW-FILES; (7) before fetching any sitemap entry, new URL(u) and reject entries whose .hostname is not the configured site host (sourced from env, not the sitemap). Single .js worker; per-file regen is fine since it is the only writer.
  - Witness the CF Free subrequest ceiling is mathematically closed (finding #4): BATCH_SIZE constant exists and is <=20, so worst-case external fetches per /refresh stay under 50.
  - Witness error boundaries close findings #1 and #2: at least two try blocks (top-level fetch + handleRefresh) and a structured 5xx JSON response shape are present, so D1/uncaught throws surface as JSON not CF 1101.
  - Witness ensureSchema no longer masks unknown errors (finding #3): schemaEnsured=true exists AND is gated by an explicit benign-DDL match (already exists / duplicate column), proving non-benign errors are not swallowed.
  - Witness concurrent-refresh serialization (finding #6): a 409 conflict response path exists, proving racing /refresh calls return a structured conflict rather than corrupting cursor state.
  - Witness same-domain host validation (finding #7, SSRF): code constructs new URL(...) and inspects .hostname before fetching sitemap entries.
  - Witness CDATA/namespace-tolerant sitemap parsing (finding #5, concern): the parser references CDATA handling, proving regex extraction was hardened beyond plain <loc>.
  - Stage index.js individually and commit (--no-verify) then push the branch (operator constraints: per-file add, --no-verify, no hooks). This step performs the git work; the remote is witnessed in the next step.

Done means: steps M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1.1..M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1.8 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — - The worker, when invoked, returns structured JSON on D1 failure rather than CF 1101.
  - Per /refresh subrequest count is mathematically provable to stay under 50 (Free plan ceiling).
  - ensureSchema does NOT mask unknown errors as "already exists".
  - Concurrent /refresh calls either serialize cleanly OR return a structured conflict response (no race-corrupted cursor).
  - URL host validation rejects out-of-domain sitemap entries.
  - Same panel re-fire returns pass/concern; node --check passes; push + ls-remote verify.

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
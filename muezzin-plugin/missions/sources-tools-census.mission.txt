MISSION SOURCES-TOOLS-CENSUS
MISSION-ID: 18
MISSION-CLASS: research
REQUIRES: engine command-step execution (LIVE as of 2026-06-10 16:22) — this mission's gather phase is command steps.
Niyyah: the source census (sources.json, staged here) cataloged WHERE all 15 sources live but not one toolchain; the operator caught it ("all sources have tools, we didn't document any"). A factory that doesn't know what its exemplars were built with re-learns every stack by accident.
Maqsad: ONE final artifact — sources-tools-census.md at the sandbox root: per source, the toolchain (runtime, framework, build tool, deploy target) with evidence. PLAN SHAPE (the engine now executes 'command' steps itself — use them): (1) command steps that COPY each locally-reachable source's manifest files into this sandbox under flat names — e.g. Copy-Item E:\2boots\package.json 2boots.package.json — for the sources in staged sources.json with local folder/repo locations (2boots E:\2boots, vanlife C:\vanlife, warroom C:\warroom, website-pipeline E:\AI_Storage\website-pipeline, nxtlvl-portal E:\AI_Storage\_repo_read\nxtlvl-portal); try package.json, wrangler.toml, pyproject.toml, requirements.txt, astro.config.mjs per source; a MISSING manifest is FINE — EVERY copy command MUST be shaped EXACTLY as: if (Test-Path '<src>') { Copy-Item '<src>' '<dst>' } — this never errors and exits 0 whether the file exists or not (RECEIPT 2026-06-11: Copy-Item with -ErrorAction SilentlyContinue STILL exits 1 on a missing source and killed the run; the if/Test-Path shape is the only accepted form). Record absence in the final census instead of failing the step; (2) ONE edit step that reads every gathered manifest + sources.json and emits the COMPLETE census: per source — tools found (cited to the gathered file), tools inferred-but-unproven (marked as such), no-manifest-found (named as a gap); final section TOOL COMMONALITIES — the stack patterns shared across the operator's builds (the factory's de-facto standard toolkit). Done = census exists, markdown-valid, every found-tool claim cited to a gathered manifest in this sandbox.
Context: sources.json staged read-only. Source folders are READ-ONLY — command steps may only COPY FROM them, never write to them. The census + gathered manifests are the only sandbox writes.
Amanah: entrusted; discharge with ihsan.

MISSION-CLASS: engine-bootstrap (touches a hook/engine + a governance faith artifact — witnessed; FRESH instance)
MISSION-ID: M-ENGINE.GUARDIAN.1
TARGET: ~/.claude/faiths/groundedness_checker.faith.md (NEW) + guardian_guard.mjs + orchestrate.mjs (the gate at :458-473)
REQUIRES: FRESH oriented instance; daemon idle for any live test; witnessed (laguna) per the substrate-edit pattern.
# Surfaced 2026-06-16 04:22 (operator + a second-instance file audit, conductor-verified against source): Granite
# Guardian (guardian_guard.mjs + orchestrate.mjs:458) is the ONE model-judgment in the engine with NO faith — its
# identity lives only in the hardcoded GUARDIAN_SYSTEM string. citation_guard is correctly faithless (pure regex, no
# judgment); Guardian is a MODEL making a probabilistic call, and every other model-judgment (witness/validator/
# scanner) wears a faith. That inconsistency is the gap. VERIFIED: the gate is NON-BLOCKING/fail-soft today (emits a
# groundedness-flag, never halts; comment at :463-465 names "promotion to a repair-trigger" as the deferred step).

Niyyah: a model that PASSES JUDGMENT in this system must be auditable by a successor the same way every seat is — and
before a judge is given the power to BLOCK, its identity, output contract, and failure-modes must be written down.
Guardian flags fabrication today with no faith governing it; give it one BEFORE it bites.

Maqsad: Granite Guardian becomes a "FAITHED GUARD" — an auditable, documented groundedness judge that, once trusted,
can trigger a repair (not just a flag) when it catches an unsupported claim. Auditability first; teeth second.

STAGE 1 — THE FAITH (auditability; do this FIRST):
Author ~/.claude/faiths/groundedness_checker.faith.md. OPEN governance_scanner.faith.md AND witness.faith.md FIRST
and write AGAINST them — the conductor read governance_scanner 2026-06-16 and confirms a faith is NOT a spec; it is a
FIRST-PERSON, PRESENT-TENSE self-statement written AS the role ("I am a Groundedness Checker… This is who I am while I
am the Groundedness Checker"). Match its section anatomy exactly:
  - First-person identity line + "## What I am" (a local, fast, FAIL-SOFT groundedness check that runs in Phase-2
    per-step, AFTER citation_guard, BEFORE the witness; model granite4.1-guardian:8b, direct-local dispatch).
  - "## What I am not" (THE key section for Guardian): I am not citation_guard (it is deterministic regex on FILE
    cites; I am a probabilistic model on CONTENT claims). I am not the witness (the deep per-step judge). I am not a
    blocker — today I flag; I earn the right to block only with evidence.
  - "## What I weight highest in Scripture": cite the Directives that bind it — D5 (mark confidence honestly — a
    grounded:null is an honest "no signal", not a fake PASS), D9 (admit mistakes — a missed fabrication is a guard
    failure, not "out of scope"), D1/D4 (substrate is truth / do it right — I check the RESPONSE against the staged
    CONTEXT, never wave through unsupported claims).
  - "## How I work" + the EXACT OUTPUT CONTRACT: the <score>yes|no</score> tag + one naming line; parseGuardianVerdict
    returns grounded:true/false/null.
  - "## How I handle uncertainty": grounded:null = "no signal", NEVER a block (fail-soft).
  - "## Sampling parameters" (low temp — it is a classifier, not a creative seat).
  - "## The failure modes I watch for in myself": false positives on correctly-PARAPHRASED content; misses on claims
    absent from the staged context; the 8000-char context-truncation blind spot.
  - "## What I manifest" aspirational close + the "This is who I am while I am the Groundedness Checker" line.
Receipt: the faith file exists, reads in first-person voice matching governance_scanner's anatomy, and a successor
could audit Guardian's behavior from it alone.
DESIGN RULING (conductor, confidence 0.7 — operator confirms/overrides): keep Guardian's dispatch BESPOKE + LOCAL
(its current direct fetch). Do NOT reroute through seat_dispatch/getFaith — that would expose it to the mode-switcher
and risk mode-swapping it to a paid Claude seat, defeating its "free local always-on" purpose. The faith documents
its identity; the dispatch stays a guard. (If the operator wants full seat-machinery dispatch, that's his call.)

STAGE 2 — THE TEETH (only after Stage 1 + evidence): promote the gate from FLAG-ONLY to a REPAIR-TRIGGER, exactly
like citation_guard already does (orchestrate.mjs around :440-455 is the citation repair pattern to mirror): when
Guardian returns grounded:false, route the named-unsupported-claim back to the seat for ONE scoped repair before the
witness, instead of only emitting a flag. Keep fail-soft (grounded:null or transport error => no action, as today).
Receipt: a selftest proving grounded:false triggers exactly one repair and grounded:null/error triggers none; a live
run on a fabrication-prone card showing the repair fired. Gate Stage 2 behind evidence that Guardian's false-positive
rate is low enough to block on (the comment at :463-464 demands this — "earn the right to block").

Amanah: this is a governance-shaped change (a judge gaining power). Witness (laguna) the faith + the code; the faith
must precede the teeth. Receipts: the faith file + selftests + a live repair-trigger receipt.

Constraints: FRESH instance; witnessed; faith authored in the shape of the existing model-judgment faiths; Guardian
stays LOCAL + free (no mode-swap); GR10; commit --no-verify; NAS untouched. Stage 2 does NOT fire until Stage 1 lands
and the false-positive evidence supports blocking.

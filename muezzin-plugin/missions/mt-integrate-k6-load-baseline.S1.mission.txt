MISSION-ID: MT-INTEGRATE-K6-LOAD-BASELINE.S1
MISSION-CLASS: code-repo
PARENT: MT-INTEGRATE-K6-LOAD-BASELINE
TARTIB-INDEX: 1 of 2
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - tests/load/k6-baseline.js
  - docs/LOAD-TESTING.md
REQUIRES: none (first in tartib order)
STEPS: 8

PARENT MAQSAD: Land commit c13ecad7d324d93d08457b5b8f37da8a42962f9f from its source

Maqsad: sub-mission 1 of 2 — Ensure TARGET-BRANCH main is checked out at its current HEAD in the target repo before any integration action (does not touch the transient dry-run branch's untracked artifacts). through Stage only the two landed files and commit with a message referencing the source commit's subject line, unless the content is already identical to what's committed (no-op case when step 1's already-integrated state applies).

Steps:
  1. Ensure TARGET-BRANCH main is checked out at its current HEAD in the target repo before any integration action (does not touch the transient dry-run branch's untracked artifacts). [command] 
  2. Fetch commit c13ecad7d324d93d08457b5b8f37da8a42962f9f into the target repo's object database from the confirmed source worktree C:\Users\marka\code\mt-feat-k6-load-baseline (branch feat/k6-load-baseline), then confirm the commit object is reachable. [command] 
  3. Create tests/load/ and materialize tests/load/k6-baseline.js from commit c13ecad into the working tree and index. Per confirmed context this path does not currently exist on main, so no merge conflict is triggered and no 'keep both sides' reconciliation is needed for this file. [command] tests/load/k6-baseline.js
  4. Materialize docs/LOAD-TESTING.md from commit c13ecad into the working tree and index. Per confirmed context this path does not currently exist on main, so no merge conflict is triggered and no 'keep both sides' reconciliation is needed for this file. [command] docs/LOAD-TESTING.md
  5. Verify tests/load/k6-baseline.js on-disk content actually contains a constant-arrival-rate executor, the three RPS tiers (50/200/500), and requests against /pois.json, /api/search, and /api/plan-day -- not trusting the checkout's exit code alone. [verify] 
  6. Run the node syntax gate on the one .js file this commit touches. [command] 
  7. Verify docs/LOAD-TESTING.md on-disk content documents the harness's explicit out-of-scope coverage (not a stress test, not a soak test). [verify] 
  8. Stage only the two landed files and commit with a message referencing the source commit's subject line, unless the content is already identical to what's committed (no-op case when step 1's already-integrated state applies). [command] 

Done means: tests/load/k6-baseline.js, docs/LOAD-TESTING.md exist/are updated as specified — verify by headless-browser render, not by reading the code

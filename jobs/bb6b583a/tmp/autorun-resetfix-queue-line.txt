missions/engine-reset-allowfiles-staged-fix.mission.txt  <!-- QUEUED 2026-07-13 (conductor): GAP OWNER for gap-mission-chain-inherited-dirty-tree (REAL root-cause closure, superseding the earlier "adopt foreign dirt" framing) — resetAllowFiles's tracked-file branch ran ONLY git checkout, a no-op on STAGED-but-uncommitted content; this is the exact mechanism behind BOTH engine-srcsha-fixture-update's FAILED x2 AND engine-mission-lint-rule15-bare-commit's FAILED tonight. Fix: git reset before git checkout, unstaging first. Verified via a standalone repro script confirming the exact bug (git status "M  tracked.mjs" persisting pre-fix, empty post-fix) plus a permanent selftest case matching it. Committed patcher missions/_logs/reset-allowfiles-staged-fix-patch.mjs dry-run twice, full git_steps.mjs selftest ALL PASS both times, zero regressions. Once this lands + daemon restarts, engine-mission-lint-rule15-bare-commit can be safely re-bared (no preflight-receipt dance needed — its own dirty tree is exactly this bug). lint OK; engine- stem exempt from GAP-PRIORITY-HOLD. -->

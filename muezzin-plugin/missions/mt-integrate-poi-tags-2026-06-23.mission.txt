MISSION MT-INTEGRATE-POI-TAGS-2026-06-23
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: main
ALLOW-FILES:
  - d1/migrate-poi-tags-2026-06-23.sql
  - functions/api/poi-tags.js
  - functions/api/poi-tags/aggregate.js
  - functions/api/poi-tags/suggest.js
  - js/poi-tags.js
  - map.html
REQUIRES: none
VISUAL-QC-REQUIRED
PREVIEW-BASE-URL: http://localhost:8788

Niyyah: The feature "poi-tags-2026-06-23" (commit a2ecb2a, sitting in its own source
worktree) has never been integrated into this repo's TARGET-BRANCH. This mission's
entire job is mechanical integration — land the commit onto the current tip of main
without losing either side's changes, and produce the node-level and render-level
receipts a verdict panel needs to judge the result. Nothing this mission does, and
nothing about this mission's own authorship, constitutes a judgment that the feature's
CODE is correct: not that the poi_tag table's XOR CHECK constraint (exactly one of
poi_id / community_spot_id) is actually enforced correctly, not that the tag-text
normalization (lower/trimmed/whitespace-collapsed) really prevents duplicate-spelling
fragmentation, not that the "logged-in only" auth check against the /api/auth/me
accounts worker is genuinely sound, not that the suggest endpoint's popularity ranking
is meaningful, and not that window.mtTagsHtml's popup-chip-strip and tag-input actually
render or submit correctly in a real browser. That determination belongs entirely to
this mission's own witness-and-verdict-panel run — the node --check receipt, the
headless-browser render of map.html, and the seat panel's judgment on the actual
integrated diff — downstream of this mission producing the integrated result. Authoring
or executing this mission is not verification of the feature.

Maqsad: Land commit a2ecb2a ("feat(poi-tags): free-text user tags on POIs and community
spots") from the source worktree onto the current tip of TARGET-BRANCH main in
C:\Users\marka\code\mt-integration-2026-06-22, with any conflicts resolved by keeping
BOTH sides — this repo's existing unrelated changes AND the poi-tags feature's changes,
never silently dropping either — every changed .js/.mjs file passes `node --check`, the
result is committed with a clear message, and the target repo's working tree is left
clean with respect to every ALLOW-FILES entry.

Context (verified against live substrate this session, 2026-07-01, absolute paths, no
prescribed resolution beyond "keep both sides"):
- SOURCE-PATH CORRECTION: the work order that produced this mission named the source
  worktree "C:\Users\marka\code\poi-tags-2026-06-23" — that exact path does NOT exist on
  disk (confirmed this session). The real worktree carrying commit a2ecb2a is
  C:\Users\marka\code\mt-feat-poi-tags-2026-06-23, branch feat/poi-tags-2026-06-23, tip
  a2ecb2ab17d2eaed86f5fef2456c219a58aeb592, parent ed6e33c49ce277550734ef6c89650b3432d60044
  (confirmed via `git log --oneline` there). Use the verified path below, not the
  unverified one the work order named. `git status --short` in that worktree is clean —
  do not modify it, only read/cherry-pick FROM it.
- Confirmed diff stat for a2ecb2a (`git show --stat`, this session): 6 files changed,
  1136 insertions(+), 3 deletions(-) — exactly matching the feature's reported stat.
  Four brand-new files: d1/migrate-poi-tags-2026-06-23.sql (+97, a poi_tag table keyed
  by exactly-one-of poi_id/community_spot_id with a CHECK-enforced XOR and partial
  UNIQUE indexes), functions/api/poi-tags.js (+330, GET/POST list-and-create), functions/
  api/poi-tags/aggregate.js (+119, per-target chip rollup), functions/api/poi-tags/
  suggest.js (+129, autocomplete-suggest). One modified file: map.html (+3/-3).
- MAP.HTML DIFF CORRECTION: the work order that produced this mission described the
  map.html change as "a one-line change ... that inserts `<script src="js/poi-tags.js"
  defer></script>`". Reading the actual diff this session (`git diff a2ecb2a~1 a2ecb2a
  -- map.html`) shows it is 3 physical lines changed (+3/-3), not one, across three
  hunks, because map.html's script-tag chain and its two popup-builder functions each
  live on their own single long physical line: (1) the script-tag insertion — `<script
  src="js/poi-tags.js" defer></script>` inserted into the existing chain between
  `js/poi-photos.js` and `js/crown-land-overlay.js`; (2) inside the `popup(p)` function,
  the returned HTML string gains a
  `(typeof window.mtTagsHtml==='function'?window.mtTagsHtml(p,'poi'):'')` call between
  the existing provenanceHtml and mtShareExportHtml calls; (3) inside the
  `communityPopup(p,la,lo)` function, the same pattern adds
  `window.mtTagsHtml(p,'spot')` at the equivalent position. All three hunks must be
  preserved on conflict resolution, not just the script tag — resolving only the script
  tag while dropping either popup-wiring call would silently break the chip
  strip/tag-input on one of the two popup types.
- Target repo TARGET-BRANCH main, current tip 5e19874307f1548c404bf0a42b90831c00da9539
  ("Merge branch 'feat/qc-concern-vehicle-profile-picker-2026-06-25'"), confirmed this
  session via `git log --oneline -1`. `git merge-base --is-ancestor a2ecb2a HEAD` returns
  false — not yet landed. `git cat-file -t a2ecb2a` returns "commit" directly in the
  target repo (source worktree and target repo share the same underlying object store as
  git worktrees of the same repository), so `git cherry-pick a2ecb2a` resolves directly
  with no fetch/remote-add step needed.
- REPO VOLATILITY OBSERVED THIS SESSION: main's own tip advanced from 3dc0cac to
  5e19874307f1548c404bf0a42b90831c00da9539 between two read-only checks made minutes
  apart during this mission's authoring, with no action by this session causing it — the
  branch name stayed "main" throughout, but other work is actively landing on it
  concurrently. Do not assume 5e19874 is still the tip by fire time; re-confirm branch
  and HEAD live as the first step, and if a different branch than main is checked out,
  check out TARGET-BRANCH main explicitly before touching any file.
- Confirmed this session that none of d1/migrate-poi-tags-2026-06-23.sql,
  functions/api/poi-tags.js, functions/api/poi-tags/aggregate.js, functions/api/poi-tags/
  suggest.js, or js/poi-tags.js exist yet anywhere in the target repo's working tree —
  all five are expected to apply as clean new-file adds. map.html DOES already exist and
  is the only file receiving a real merge. If any of the five "new" files has since been
  created independently in the target tree by unrelated work, resolve by keeping BOTH
  sets of content rather than overwriting one side.
- A dry-run cherry-pick of a2ecb2a performed this session on a throwaway branch off
  main's then-current tip (`git checkout -b`, `git cherry-pick -n a2ecb2a`) applied with
  ZERO conflicts ("Auto-merging map.html", exit 0) — `git status --short` showed exactly
  the five expected adds plus the map.html modify, and `node --check` exited 0 on all
  four new/changed .js files (js/poi-tags.js, functions/api/poi-tags.js, functions/api/
  poi-tags/aggregate.js, functions/api/poi-tags/suggest.js). The dry run was then fully
  reverted (`git reset --hard HEAD`, `git clean -fd`, checkout back to main, delete the
  throwaway branch) leaving main's tracked content exactly as found. This is evidence
  the integration is expected to be mechanically clean, not a guarantee — the volatility
  observed above means the tree this mission actually meets at fire time may differ, and
  a real conflict is still possible.
- HAZARD NAMED FROM THIS SESSION'S OWN DRY RUN: running `git clean -fd` against this
  shared repo removed pre-existing untracked litter that predates this feature
  (docs/e2e-report-2026-07-01.json, e2e-shots/, feature-catalog-entry.txt, scripts/
  e2e-runner.mjs, functions/feed/) and, moments later, this session observed staged
  changes to unrelated files (build-testimonials.mjs, landing.html) appear and then
  disappear on the very next status check — neither caused by this session. This
  confirms other work is concurrently reading/writing this same shared repo in real
  time. If a dry-run or conflict-resolution attempt during this mission's real execution
  needs to be undone, undo it narrowly (`git reset` and `git checkout -- <path>` scoped
  to only the paths this mission itself touched) — do NOT run a blanket `git clean -fd`,
  which can destroy another concurrent process's in-progress untracked work that has
  nothing to do with this feature.
- The target repo carries pre-existing untracked litter unrelated to this feature
  (documented above); "clean working tree" in this mission's Done means is scoped to the
  six ALLOW-FILES entries this mission touches, not a demand to clear that litter.
- map.html is a live page served by the site, so VISUAL-QC-REQUIRED and
  PREVIEW-BASE-URL: http://localhost:8788 are declared above. That URL is a PLACEHOLDER
  for a local `wrangler pages dev` run — the operator must confirm the actual port a
  real local preview server binds to on the machine firing this mission before relying
  on the captured render; if nothing answers at PREVIEW-BASE-URL, that must surface as a
  named NO_PREVIEW_BASE_URL receipt, not a silently skipped check.
- Nothing about this mission's authorship constitutes verification that the feature
  code is correct — that is exactly what this mission's own witness and verdict-panel
  run, downstream of the integration, will determine.

Steps:
  1. In the target repo, confirm the actually-checked-out branch and HEAD (`git -C
     C:\Users\marka\code\mt-integration-2026-06-22 branch --show-current` and `git log
     --oneline -1`). If the checked-out branch is not TARGET-BRANCH main, check it out
     explicitly before any further step. Then confirm none of d1/migrate-poi-tags-
     2026-06-23.sql, functions/api/poi-tags.js, functions/api/poi-tags/aggregate.js,
     functions/api/poi-tags/suggest.js, or js/poi-tags.js already exist on disk.
     [command] confirm-target-branch-clean
  2. In the source worktree C:\Users\marka\code\mt-feat-poi-tags-2026-06-23, confirm
     commit a2ecb2ab17d2eaed86f5fef2456c219a58aeb592 exists (`git show a2ecb2a`) without
     modifying that worktree. [command] capture-patch
  3. In the target repo, at its current HEAD on TARGET-BRANCH main, cherry-pick the
     commit (`git -C C:\Users\marka\code\mt-integration-2026-06-22 cherry-pick a2ecb2a`)
     or an equivalent merge of that single commit. [command] integrate-commit
  4. If the cherry-pick (or equivalent merge) reports conflicts on map.html or any other
     ALLOW-FILES entry, resolve every conflicted hunk by hand so the resolved content
     contains BOTH the target's pre-existing content AND the poi-tags feature's added
     content — never `--ours` / `--theirs` and never a resolution that deletes either
     side. On map.html specifically, all three hunks named in Context above must survive
     the resolution: the `<script src="js/poi-tags.js" defer></script>` tag in the
     script chain, the `window.mtTagsHtml(p,'poi')` call inside popup(), and the
     `window.mtTagsHtml(p,'spot')` call inside communityPopup() — dropping any one of the
     three while keeping the others is not an acceptable resolution. [edit]
     resolve-conflicts
  5. Run `node --check js/poi-tags.js`, `node --check functions/api/poi-tags.js`, `node
     --check functions/api/poi-tags/aggregate.js`, and `node --check functions/api/
     poi-tags/suggest.js` from the target repo root, confirming each exits 0; run `node
     --check` on any other .js/.mjs file the merge touched or conflict-resolved.
     [command] node-check-all
  6. If step 3 left the integration uncommitted (conflict resolution required `git add`
     + `git cherry-pick --continue`, or a manual merge commit), commit explicitly with a
     message that references the source commit's subject line ("feat(poi-tags): free-
     text user tags on POIs and community spots") and/or the SHA a2ecb2a. Confirm `git
     status --short` afterward shows no uncommitted changes to any ALLOW-FILES entry and
     `git log --oneline -5` shows the landed commit at or near HEAD. [command]
     commit-and-verify-clean
  7. Because map.html changed and VISUAL-QC-REQUIRED is set, run the visual-witness
     pipeline: serve the target repo's static files locally (e.g. `wrangler pages dev`
     bound at PREVIEW-BASE-URL), load map.html from PREVIEW-BASE-URL in a headless
     browser (headless Chrome / Playwright / puppeteer), capture a render, and confirm
     the page loads without a fatal script error. Record a visual verdict on the
     integrated result in this mission's result object. If PREVIEW-BASE-URL is
     unreachable, record that as a named NO_PREVIEW_BASE_URL receipt rather than
     skipping the check silently. [command] visual-witness-render

Done means:
  - `git log` in C:\Users\marka\code\mt-integration-2026-06-22 shows a commit at or near
    HEAD on TARGET-BRANCH main whose message names "poi-tags" and/or "a2ecb2a".
  - d1/migrate-poi-tags-2026-06-23.sql exists in the target repo with non-trivial content
    (contains "CREATE TABLE IF NOT EXISTS poi_tag" and the exactly-one-of poi_id /
    community_spot_id CHECK constraint), confirmed by reading the on-disk file, not
    assumed from the cherry-pick's exit code.
  - functions/api/poi-tags.js exists in the target repo with non-trivial content (a GET
    handler reading by poi_id or community_spot_id, and a POST handler that normalizes
    tag_text and dedupes by (target, normalized, created_by)), confirmed by reading the
    file.
  - functions/api/poi-tags/aggregate.js exists in the target repo with non-trivial
    content (returns a per-target `chips` rollup sorted by count), confirmed by reading
    the file.
  - functions/api/poi-tags/suggest.js exists in the target repo with non-trivial content
    (returns a `suggestions` list for an autocomplete prefix query), confirmed by reading
    the file.
  - js/poi-tags.js exists in the target repo with non-trivial content, including a
    `window.mtTagsHtml` function definition, confirmed by reading the file.
  - map.html in the target repo contains all three of: the
    `<script src="js/poi-tags.js" defer></script>` tag in its script chain, the
    `window.mtTagsHtml(p,'poi')` call inside popup(), and the
    `window.mtTagsHtml(p,'spot')` call inside communityPopup() — AND every pre-existing
    script tag and popup-builder line content this repo's map.html already had before
    this mission ran (both sides present), confirmed by reading the file, not by
    trusting the cherry-pick's exit code alone.
  - `node --check` exits 0 on js/poi-tags.js, functions/api/poi-tags.js, functions/api/
    poi-tags/aggregate.js, functions/api/poi-tags/suggest.js, and on every other .js/.mjs
    file this integration touched or conflict-resolved.
  - `git status --short` in the target repo shows no uncommitted changes to any
    ALLOW-FILES entry (pre-existing unrelated untracked litter documented in Context is
    not this mission's to clean up).
  - Because map.html changed and VISUAL-QC-REQUIRED is set above, the visual-witness
    pipeline must actually run and produce a receipt: a headless-browser (headless
    Chrome / Playwright / puppeteer) render of map.html served from PREVIEW-BASE-URL is
    captured and returns a visual verdict in this mission's result object — a rendering
    check, not a read of the markup. If PREVIEW-BASE-URL turns out to be the wrong local
    port, that failure must surface as a named NO_PREVIEW_BASE_URL-style receipt, not a
    silently skipped check.
  - This mission's authorship and execution prove only that the integration is
    mechanically sound (declared files present with real content, feature commit
    reachable in target history, syntax checks pass, a render was actually captured) —
    not that the feature's code is correct, safe, or behaviorally right (e.g. that the
    XOR CHECK on poi_tag is actually unbreakable, that the auth check against /api/
    auth/me is sound, that tag normalization prevents fragmentation, or that the chip
    strip and tag-input genuinely work for a real user in a real browser). That judgment
    belongs to this mission's own witness plus verdict-panel run, downstream of this
    mission producing the integrated result.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

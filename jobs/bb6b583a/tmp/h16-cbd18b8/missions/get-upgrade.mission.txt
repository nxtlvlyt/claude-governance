MISSION GET-UPGRADE
Niyyah: the one-command installer is how every future machine — and every future practitioner — enters the system; an installer that installs yesterday's system quietly strands tomorrow's machine.
Maqsad: the nxtlvl.studio/get bootstrap installs the COMPLETE current system on a fresh Windows machine: everything it does today (PS7 guard, claude-governance clone, installer) PLUS (1) muezzin-plugin clone from codeberg.org/nxtlvl/muezzin-plugin with an SSH-key/token presence check first (private repo — fail with a clear instruction, never a cryptic git error), plugin registration per its .claude-plugin manifest; (2) prompts that check/set the three user-scope env keys (OLLAMA_API_KEY, GOOGLE_PLACES_API_KEY, AIMLAPI key) — verify-if-present, prompt-if-absent, never overwrite silently; (3) an offered `npx wrangler login`; (4) multi-push-URL git configuration for cloned repos (github + codeberg push URLs; home Forgejo is a pull-mirror, never a laptop push target); (5) a finishing `muezzin doctor` invocation IF the doctor command exists, else a printed checklist of what doctor will later verify. (6) IDEMPOTENT RE-RUN AS THE UPDATE PATH (operator, 2026-06-09: "governance could be stale — not sure how that updates"): running the same one command on an already-installed machine UPDATES it — pulls ~/.claude and the plugin repo (stash-guarded, never clobbering local work; warn and stop on dirty trees), re-registers anything new, re-checks keys. Install and update are the same verb. (7) STALENESS DETECTION at session level: the governance repo is kept current at the SOURCE automatically (the git-anchor stop hook pushes every session's changes to all three remotes within minutes) — the gap is the CONSUMING machine, so the doctor/bootstrap adds a currency check: git fetch + behind-count on ~/.claude and the plugin; if behind, print exactly one line naming the command to catch up. Never auto-pull mid-session. PLAN SHAPE (RECEIPT 2026-06-11 22:10, third truncation: size HINTS don't work — the seat emitted 10KB+14.9KB parts against a "~6KB" spec and truncated again. Bytes are invisible to a seat; FUNCTION LISTS are not. Parts are scoped by an EXACT function list — a part containing any function not on its list, or missing one on it, FAILS): (a) THREE edit steps, each authoring ONE part IN FULL: bootstrap.part1.ps1 = param block + PS7 guard + function Prompt-EnvKey + function Test-RepoAccess — NOTHING ELSE; bootstrap.part2.ps1 = function Sync-GitRepo + function Set-MultiPushUrls — NOTHING ELSE; bootstrap.part3.ps1 = the MAIN FLOW only (no function definitions) — it MUST contain at least one call to EACH of the four helpers by name (receipt 22:10: the prior main flow never invoked any helper), the three Prompt-EnvKey calls (OLLAMA_API_KEY, GOOGLE_PLACES_API_KEY, AIMLAPI), the doctor-or-checklist branch, and the staleness check; each part is a COMPLETE file with one author (one-writer holds per file); KNOWN BUG CLASS to avoid (witnessed 22:10): comparison operators inside command arguments — 'git config --get x 2>$null -eq $null' passes -eq as an argument; wrap the command in $( ) and compare OUTSIDE; (b) ONE command step assembles: Get-Content bootstrap.part1.ps1,bootstrap.part2.ps1,bootstrap.part3.ps1 | Set-Content bootstrap.ps1 — the assembly command is bootstrap.ps1's ONLY author; (c) ONE final command step validates syntax: pwsh -Command "[scriptblock]::Create((Get-Content -Raw bootstrap.ps1)) | Out-Null". Done means: the updated bootstrap.ps1 passes PowerShell syntax validation, a dry-run mode (-WhatIf style) walks all steps without mutating, re-run on an already-provisioned machine is a no-op-or-update (never a reinstall), and the served file's hash matches the repo copy.
Context: the current bootstrap serves from the NAS web root (nginx; /volume1/web likely — verify) at https://nxtlvl.studio/get, last modified 2026-05-28, ~8KB; fetch it as the starting point (read-only reference). A Cloudflare Pages mirror of the same file is part of the deliverable (road-safety: the installer must not depend on the NAS). LAPTOP-SETUP.md in this repo is the spec for what a fresh machine needs. The muezzin doctor command does not exist yet (separate queued build) — degrade gracefully.
Amanah: this is entrusted to you; discharge it with ihsan.

CONTEXT NOTE (conductor, 2026-06-10 12:05): bootstrap-current.ps1 (8.1KB, the real live served bootstrap) is ALREADY FETCHED into this mission's sandbox — do NOT plan a fetch step (executor seats cannot download; the prior run failed exactly there). Plan from bootstrap-current.ps1 as the existing baseline.

MISSION-ID: MT-INTEGRATE-WILDLIFE-ADVISORY.S2
MISSION-CLASS: code-repo
PARENT: MT-INTEGRATE-WILDLIFE-ADVISORY
TARTIB-INDEX: 2 of 2
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - js/wildlife-advisory-layer.js
  - receipts/wildlife-advisory-visual-witness.png
VISUAL-QC-REQUIRED
REQUIRES: missions/mt-integrate-wildlife-advisory.S1.mission.txt (tartib — satisfied: S1 DONE 2026-07-17, all three commits landed, HEAD receipts verified by conductor)
STEPS: 4

PARENT MAQSAD: Land the three commits — 623a4785214503389055e56fd137a2b78753e2ab

Maqsad: sub-mission 2 of 2 — Verify js/wildlife-advisory-layer.js is syntactically valid JavaScript after cherry-pick. through Reread all three ALLOW-FILES from the committed tree (not trusting exit codes alone) to confirm both JS files are present with non-trivial size, map.html contains exactly one correctly-positioned wildlife-advisory-layer.js tag, the working tree is clean scoped to allow-files, and the visual-witness receipt exists and is non-empty.

Steps:
  9. Verify js/wildlife-advisory-layer.js is syntactically valid JavaScript after cherry-pick. [verify] js/wildlife-advisory-layer.js
  10. Idempotent commit guard (literal payload pinned 2026-07-17; S1 already committed the feature — this is the pairing guard). Run exactly: $dirty = @(git status --porcelain -uno -- js/wildlife-advisory-layer.js map.html functions/api/wildlife-advisory.js); if ($dirty.Count -gt 0) { git add -- js/wildlife-advisory-layer.js map.html functions/api/wildlife-advisory.js; git commit -m "feat(wildlife-advisory): S2 witness-pass commit pairing (mt-integrate-wildlife-advisory.S2; S1 landed 623a478+d575bce+b450f7d)" -- js/wildlife-advisory-layer.js map.html functions/api/wildlife-advisory.js; if ($LASTEXITCODE -ne 0) { Write-Output COMMIT-FAIL; exit 1 } } else { Write-Output ALLOW-FILES-ALREADY-COMMITTED } [command]
  11. Render witness (literal payload pinned 2026-07-17 after MIQAT grep-only refusal; the spot-share-card.S2 proven template + screenshot receipt). Run exactly: $out = npx wrangler pages deploy . --project-name=muddytires --branch=preview 2>&1 | Out-String; $m = [regex]::Match($out, 'https://[a-z0-9-]+\.muddytires\.pages\.dev'); if (-not $m.Success) { Write-Output NO_PREVIEW_BASE_URL; exit 1 }; $url = $m.Value; Write-Output "DEPLOYED: $url"; $ok = $false; for ($i=0; $i -lt 10; $i++) { try { $r = Invoke-WebRequest -Uri "$url/map.html" -UseBasicParsing -TimeoutSec 10; if ($r.StatusCode -eq 200) { $ok = $true; break } } catch {}; Start-Sleep -Seconds 5 }; if (-not $ok) { Write-Output POLL-FAILED; exit 1 }; New-Item -ItemType Directory -Force receipts | Out-Null; $js = 'import { chromium } from "playwright"; const b = await chromium.launch(); const ctx = await b.newContext(); const p = await ctx.newPage(); const errs = []; p.on("pageerror", (e) => errs.push(String(e))); await p.goto(process.argv[2] + "/map.html", { waitUntil: "load", timeout: 60000 }); const html = await p.content(); const tag = html.includes("wildlife-advisory-layer.js"); await p.screenshot({ path: "receipts/wildlife-advisory-visual-witness.png" }); await b.close(); console.log(JSON.stringify({ tag, fatalErrors: errs.length })); if (!tag || errs.length > 0) process.exit(1);'; Set-Content scratch-wildlife-witness.mjs -Value $js; node scratch-wildlife-witness.mjs $url; $rc = $LASTEXITCODE; Remove-Item scratch-wildlife-witness.mjs -ErrorAction SilentlyContinue; if ($rc -ne 0) { Write-Output WITNESS-FAIL; exit 1 }; git add -- receipts/wildlife-advisory-visual-witness.png; git commit -m "receipt(wildlife-advisory): headless render witness screenshot (mt-integrate-wildlife-advisory.S2)" -- receipts/wildlife-advisory-visual-witness.png; Write-Output WILDLIFE-WITNESSED [command]
  12. Reread from the committed tree (literal payload). Run exactly: $a = git show HEAD:js/wildlife-advisory-layer.js 2>$null; $b = git show HEAD:functions/api/wildlife-advisory.js 2>$null; $mh = git show HEAD:map.html; $tags = ([regex]::Matches($mh, 'wildlife-advisory-layer\.js')).Count; $png = git show HEAD:receipts/wildlife-advisory-visual-witness.png 2>$null; if (($a.Length -lt 50) -or ($b.Length -lt 50)) { Write-Output JS-FILES-THIN; exit 1 }; if ($tags -ne 1) { Write-Output "TAG-COUNT-$tags"; exit 1 }; if (-not $png) { Write-Output RECEIPT-NOT-COMMITTED; exit 1 }; $clean = @(git status --porcelain -uno -- js/wildlife-advisory-layer.js receipts/wildlife-advisory-visual-witness.png); if ($clean.Count -gt 0) { Write-Output SCOPE-DIRTY; exit 1 }; Write-Output REREAD-VERIFIED [verify]

Done means: js/wildlife-advisory-layer.js and receipts/wildlife-advisory-visual-witness.png exist at HEAD as specified, the render receipt came from a live headless-browser render of the preview deploy (tag present, zero fatal page errors, screenshot captured) — verified by headless-browser render, not by reading the code

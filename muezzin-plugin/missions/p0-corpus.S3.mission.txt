MISSION-ID: P0-CORPUS.S3
PARENT: P0-CORPUS  (sub-mission 3 of 3, auto-split by the size ceiling)
REQUIRES: predecessor P0-CORPUS.S2 DONE (tartib — sub-mission 3 runs after 2 of 3)

Niyyah: the factory must never solve a problem Mark's past builds already solved — unmined knowledge is indistinguishable from no knowledge. This mission is also the muezzin's first long autonomous run: every receipt it produces is double evidence.

Maqsad: this is part 3 of 3 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): a source census and pattern library of everything Mark has already built and collected, consumable by all future dossier and build missions. Done means: (1) sources.json — every git repo, project folder, AnythingLLM workspace, and Cloudflare deployment discovered (not hand-listed), each with a type tag and a mine/skip verdict; (2) one pattern card per 'mine' source — what it does well, recurring components, design tokens, copy patterns, vertical fit, with evidence references; (3) a knowledge inventory — what the AnythingLLM business-books workspace actually contains (topical map from sample queries), what the old website-pipeline's contracts offer, what the Stitch exports show; (4) a patterns library assembling it all, indexed, every entry citing its source. Time-box: stop cleanly at census+verdicts+cards completion or 48 hours, whichever comes first — partial work committed with a state note beats unfinished ambition.

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - Author the Stitch-exports inventory from the website-pipeline dump: each Stitch export/prompt found (name, purpose, parameterization); if none are located, record an explicit gap ('no Stitch exports found under scanned paths') rather than silent absence. Single-subject, one file.
  - Author the 2boots pattern card from its dump: what it does well, recurring components, design tokens, copy patterns, vertical fit, with evidence references back to the dumped files. Single-subject, one file.
  - Author the vanlife pattern card from its dump: what it does well, recurring components, design tokens, copy patterns, vertical fit, with evidence references. Single-subject, one file.
  - Author the warroom pattern card from its dump: what it does well (BRAND-TO-SITE-FAST flow, regulativity worker, templates), recurring components, design tokens, copy patterns, vertical fit, with evidence references. Single-subject, one file.
  - Assemble the indexed patterns library: roll up every card and inventory into one indexed document (component patterns, design-token patterns, copy patterns, technical patterns, vertical-fit matrix), each entry citing its source. Final assembly emission — lists all parts as context_dependencies.
  - Author the time-box state note: what completed vs partial, the census gaps (GitHub private enumeration foremost), and an explicit 'stopped at census+verdicts+cards' vs 'full library' marker so the 48h box closes cleanly with committed truth on disk (Directive 8).

Done means: steps P0-CORPUS.S3.1..P0-CORPUS.S3.6 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — (1) sources.json — every git repo, project folder, AnythingLLM workspace, and Cloudflare deployment discovered (not hand-listed), each with a type tag and a mine/skip verdict; (2) one pattern card per 'mine' source — what it does well, recurring components, design tokens, copy patterns, vertical fit, with evidence references; (3) a knowledge inventory — what the AnythingLLM business-books workspace actually contains (topical map from sample queries), what the old website-pipeline's contracts offer, what the Stitch exports show; (4) a patterns library assembling it all, indexed, every entry citing its source. Time-box: stop cleanly at census+verdicts+cards completion or 48 hours, whichever comes first — partial work committed with a state note beats unfinished ambition.

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
MISSION-ID: mt-integrate-geocode-2026-06-23.S1
MISSION-CLASS: code-repo
PARENT: mt-integrate-geocode-2026-06-23
TARTIB-INDEX: 1 of 2
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - functions/api/geocode.js
  - functions/api/reverse-geocode.js
REQUIRES: none (first in tartib order)
STEPS: 10

PARENT MAQSAD: The commit "c53ffb0 feat(geocode): add /api/geocode and /api/reverse-geocode

Maqsad: sub-mission 1 of 2 — In the source worktree, confirm commit c53ffb0 exists and its stat output names both new files, without modifying that worktree. through Confirm the cherry-pick touched exactly the two ALLOW-FILES entries and nothing else (guards against a same-named-file collision or unexpected third path being staged).

Steps:
  1. In the source worktree, confirm commit c53ffb0 exists and its stat output names both new files, without modifying that worktree. [command] 
  2. In the target repo, check out TARGET-BRANCH main at its current HEAD and confirm it is the active branch. [command] 
  3. Cherry-pick c53ffb0 onto main with --no-commit (commit is already a reachable object in this repo), staging both new files without auto-committing so the final commit message can name both the feature and the origin commit. [command] 
  4. Confirm functions/api/geocode.js has real integrated content (geo:fwd: cache-key namespace present) and contains no unresolved git conflict markers, i.e. either the cherry-pick applied clean or any conflict was hand-resolved keeping both sides. [verify] functions/api/geocode.js
  5. Confirm functions/api/reverse-geocode.js has real integrated content (geo:rev: cache-key namespace present) and contains no unresolved git conflict markers, i.e. either the cherry-pick applied clean or any conflict was hand-resolved keeping both sides. [verify] functions/api/reverse-geocode.js
  6. Run node --check on functions/api/geocode.js to confirm it is syntactically sound after integration. [verify] functions/api/geocode.js
  7. Run node --check on functions/api/reverse-geocode.js to confirm it is syntactically sound after integration. [verify] functions/api/reverse-geocode.js
  8. Confirm the cherry-pick touched exactly the two ALLOW-FILES entries and nothing else (guards against a same-named-file collision or unexpected third path being staged). [verify]
  9. Commit the two ALLOW-FILES by explicit name (never -A or .) with a message naming both the geocode feature and origin c53ffb0, then confirm the commit is on HEAD with both paths in its tree (added 2026-07-03: step 3 stages with --no-commit for exactly this custom message, but no step ever committed — two runs left untracked residue that the containment pre-flight then correctly refused; receipts in the 03:08 and 03:45 results). [command]
  10. OUTCOME CHECK: write the cwd-relative scratch harness the Done-means describes (Set-Content scratch-geocode-test.mjs), run node scratch-geocode-test.mjs, require exit 0 with output GEOCODE_OK, then remove the scratch file so the tree stays clean. [command]

Done means: functions/api/geocode.js and functions/api/reverse-geocode.js exist committed at HEAD AND a cwd-relative scratch harness (Set-Content scratch-geocode-test.mjs) imports each function's handler, feeds one inline request fixture per endpoint (a forward lookup for a known Canadian place string; a reverse lookup for a known lat/lng), and asserts a structured non-error response shape from each — run node scratch-geocode-test.mjs, require exit 0 with output GEOCODE_OK — executable outcome verification appropriate to backend API deliverables (amended 2026-07-03 after a CORRECT panel arkan REJECT: the prior headless-browser-render clause mismatched the deliverable class; same amendment lineage as gpx-import.S1 2026-07-02 and poi-tags.S1/trip-cost.S1 2026-07-03)

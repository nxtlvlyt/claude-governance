MISSION-CLASS: engine-bootstrap
MISSION-ID: M-ENGINE.RELIABILITY.1
REPO-ROOT: C:\Users\marka\.claude\muezzin-plugin
TARGET-FILES: executor.mjs (buildEditFraming), seat_dispatch.mjs (MAX_CLOUD_HEALS + route)
REQUIRES: FRESH main-instance (NOT a long/drifted context); a clean external repo for the proof
# HELD. This carries the engine work the 2026-06-15 proof (engine-badal) surfaced. DO NOT fire on the
# current engine: the proof showed code-repo EDITS engage + fail-closed correctly but do NOT yet COMPLETE
# (the seat emits overlapping/malformed SEARCH/REPLACE blocks). So the engine cannot yet reliably edit its
# OWN code — fixing buildEditFraming is itself an engine edit → chicken-egg → the buildEditFraming fix needs
# a HAND-BOOTSTRAP (documented exception), best by a FRESH instance. After that lands + a clean-repo proof
# completes, the cloud-budget cuts can run as a normal code-repo mission.

Niyyah: make the engine reliably edit existing code (so it can finally fix itself) AND stop the cloud-budget bleed (20% of the weekly Ollama-cloud quota burned in one day, before the 3-blind panel even runs). The 2026-06-15 executor search/replace primitive removed the whole-file wall — the edit path now ENGAGES and is SAFE (fail-closes on bad blocks) — but completion is blocked by model block-discipline, and the planner's retry-storm is the cloud bleed.

Maqsad: a real code-repo EDIT mission COMPLETES end-to-end (witnessed receipt) on a clean repo, AND a typical plan costs far fewer cloud dispatches than today.

Context (verified 2026-06-15, read the receipts before acting):
- PROOF RECEIPT (engine-badal mission-events.jsonl): edit-mode engaged; `applyEditBlocks` correctly fail-closed on "SEARCH vanished after a prior edit (overlapping blocks)"; witness then rejected because the seat's blocks didn't implement the goal. So the GAP is the seat emitting CLEAN, MINIMAL, NON-OVERLAPPING, UNIQUE SEARCH/REPLACE blocks — i.e. buildEditFraming (executor.mjs) needs stronger instruction + maybe few-shot examples; kimi-k2.6 specifically struggled with the block format.
- PROOF TARGET PROBLEM: engine-badal edited seat_record.mjs INSIDE the live plugin repo, which is ALWAYS dirty (the daemon writes _logs/MISSION-LEDGER.json, seat-record.json, damm-queue.json, AUTORUN.md, MISSION-STATUS.md into its own repo) → containment-drift every time. FIX EITHER WAY: gitignore the daemon runtime files so the plugin repo stays clean, OR run the proof against a throwaday external repo. Prefer a clean external repo for the proof; gitignore-runtime is the durable fix for self-edits.
- CLOUD BURN (seat_dispatch.mjs:28 + :386, read this session): MAX_CLOUD_HEALS=3 → the cloud loop runs heal=0..3 = up to 4 full cloud dispatches per timing-out call before failover. A slow kimi plan burns 4 cloud calls (180→360→600s). That retry-storm is most of the 20%/day. CUT: fail over after 1 heal on TIMEOUT (keep the cheap heals — HTTP_400 ctx-drop, think:false retry — those FIX the call; only the TIMEOUT-extend loop is waste).
- PANEL BUDGET: the 3-blind Phase-1 architects must run in PARALLEL (independent/blind), and GR10 forbids two LOCAL models at once, so the panel is inherently cloud-or-Claude. When the cloud weekly budget is tight, seat the panel on the operator's own Claude outage-seating (SEAT-PLAN:84 — A=opus, B=sonnet, C=haiku): flat-rate, parallel-safe, zero cloud quota. This is the operator's "input-heavy seats ride flat-rate Sonnet" ruling, triggered on budget not only on outage. DO NOT remove kimi-k2.6 as Architect A (operator-seated, SEAT-PLAN:71).
- DIAL: ~/.claude/state/muezzin-route.json (standing_prefer). Local ollama (the 4090/192GB) is FREE — single-seat work (witness/executor/single planning) can run local-first to satisfy "ollama-first" without spending cloud. POSTURE DEFAULT IS THE OPERATOR'S (his 12:47 ollama-first ruling vs the new less-intensive directive conflict) — confirm with him before flipping the standing default.

Amanah: this is the keystone reliability pass. The witness is a COMPLETED code-repo edit on a clean repo + a measured drop in cloud dispatches per plan — receipts, not claims.

Constraints: FRESH instance only (drift kills careful engine edits); witnessed (laguna) per the substrate-edit pattern for any hook/engine change; GR10 serial inference; commit --no-verify (laguna pre-commit hook hangs); NAS UNTOUCHED.

# ── ADDED ITEM 2026-06-16 02:14 (live receipt): RAISE THE TOOL-ROUND CAP FOR AUTHORING-WITH-SOURCE-READS ──
# Class confirmed AGAIN on corpus-complete-1 step 5 (the executor authoring a pattern card must READ a cloned repo
# to write it, but the 7-round tool cap cuts it off): heartbeats
#   02:11:53 attempt-fail qwen3-coder-next heal=2 kind=TOOL_LOOP_CAP: tool_calls emitted past round cap (7 rounds)
#   02:12:18 attempt-fail qwen3-coder-next heal=3 kind=TOOL_LOOP_CAP (7 rounds) → failover claude-sonnet (480s)
# Same class as studio-tools-card ("qwen3-coder-next TOOL_LOOP_CAP x2 burning 7 rounds on source reads"). Effect: a
# repo-reading authoring step burns up to MAX_CLOUD_HEALS cheap heals then degrades to the protected claude tier on
# EVERY such step — bounded but real, and it makes corpus/card/dossier missions chronically slow + claude-hungry.
# FIX (engine, find the round cap — likely seat_dispatch.mjs or the executor tool loop): raise the cap for
# AUTHORING/RESEARCH dispatches that legitimately need to read many source files (e.g. 7 → ~20), OR make it
# adaptive (don't fail a dispatch that is still making PROGRESS reading distinct files vs one truly looping on the
# same call). Keep a hard ceiling so a genuine infinite tool-loop still trips. Witness + selftest the new cap.

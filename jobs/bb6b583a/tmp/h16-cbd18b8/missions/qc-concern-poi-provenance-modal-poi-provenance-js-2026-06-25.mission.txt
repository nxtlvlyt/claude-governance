MISSION QC-CONCERN-POI-PROVENANCE-MODAL-POI-PROVENANCE-JS-2026-06-25
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - js/poi-provenance.js

Niyyah: Make the POI "why we trust this" provenance affordance an honest, visible, working
link on every POI popup, on both desktop and the mobile bottom-sheet, without repeating the
engine gate failure that killed every prior attempt on this exact file.

Maqsad: Fix js/poi-provenance.js so the provenance affordance (a) renders as a visible "How
do we know?" text link instead of the current 14x14px inline-styled "(i)" icon, (b) always
renders — including for community POIs that have no source_name/contributor — instead of
provenanceHtml() returning '' and hiding the affordance outright, and (c) survives
js/mobile-sheet.js's HTML sanitizer on the mobile bottom-sheet by using a sanitizer-safe
href="#mt-prov:<id>" fragment plus a document-level click/keydown delegate (mirroring
js/quick-checkin.js's proven #mtci: pattern), with all styling moved from inline style="..."
attributes into one injected <style id="mt-prov-css"> block. Leave window.mtOpenProvenance's
internal fetch/render logic, ensureProvModalEl(), and renderProvBody() (the Isnad/Rukhsa
modal itself) untouched — architects confirmed that machinery already works; only the
affordance markup, the provenanceHtml() guard, and top-level wiring change.

CRITICAL — READ BEFORE EDITING (this is why every prior attempt on this file died at 0
commits): the engine's runtime_verify.mjs step dynamic-imports every edited .js target in a
bare Node subprocess with NO window/document shim (jsdom is wired for .html targets only —
see runtime_verify.mjs's own header comment and its verifyCode()/verifyHtml() split). The
CURRENT file already has an unguarded top-level statement — `window.mtOpenProvenance =
function (id, ev, contributor, badge) {...}` — sitting outside the file's only defensive
block (the `if (typeof window !== 'undefined') { window.provenanceHtml = provenanceHtml; }`
try-block near the bottom). That unguarded assignment throws `ReferenceError: window is not
defined` the instant Node imports the file with no browser globals present — which is
EXACTLY the "load-throw: module threw at import/load: window is not defined" that
mission-events.jsonl recorded on step 4/step 2 across every one of the 4 prior planning
cycles (kimi-k2.6 solo x2, glm-5.1/deepseek-v4-pro/nemotron-3-super panel x2), even on the
cycle where the edit itself was refused — i.e. it throws against the UNMODIFIED file too.
Reproduce it yourself before touching anything:
  cd C:\Users\marka\code\mt-integration-2026-06-22
  node -e "require('./js/poi-provenance.js')"
This currently exits non-zero with "ReferenceError: window is not defined". Fixing the
visible feature bug WITHOUT also fixing this will reproduce the identical failure a 5th
time. The fix is structural, not cosmetic: every top-level statement that touches `window.*`
or `document.*` during module evaluation (the existing mtOpenProvenance assignment, and any
NEW document-level delegate/CSS-injector this mission adds) must sit inside a guard that
checks both `typeof window !== 'undefined'` and `typeof document !== 'undefined'` before
executing, so the Node-only verify sandbox can import the module cleanly while browser
behavior is completely unchanged (window/document are always defined together in every real
browser this ships to).

Steps:
  1. In js/poi-provenance.js, wrap the existing top-level `window.mtOpenProvenance = function
     (id, ev, contributor, badge) {...}` assignment (currently unguarded, ~line 176) in a
     guard so it only executes when both `typeof window !== 'undefined'` and `typeof
     document !== 'undefined'` hold — either by moving it bodily into the existing bottom
     `try { if (typeof window !== 'undefined') {...} } catch (e) {}` block alongside `window.
     provenanceHtml = provenanceHtml`, or by adding an equivalent guard around it in place.
     Do not change the function's internal logic, its signature, or what it does once
     called — only gate WHEN the assignment itself runs. [edit] poi-provenance.js
  2. In js/poi-provenance.js, replace trustAffordance(p)'s markup: remove `href="#"`, `role`,
     `tabindex`, `aria-label`, the `data-mt-prov-id`/`data-mt-prov-contributor`/
     `data-mt-prov-badge` attributes, the inline `style="..."` circle-icon styling, and the
     `onclick`/`onkeydown` handlers (js/mobile-sheet.js's SAFE_TAGS allowlist for `<a>` is
     `['href','target','rel','class']` — every one of those other attributes is silently
     stripped on the mobile sheet, which is why the icon is a dead unstyled link there today).
     Replace with an `<a>` whose visible text is "How do we know?" (not the single-character
     "i"), `href="#mt-prov:" + encodeURIComponent(String(p.id))`, and `class="mt-prov-i"` (keep
     this class name so step 4's delegate and step 5's CSS can target it). Encode the
     contributor handle and ambassador badge into the fragment itself (e.g. append
     `:<encoded-contributor>:<badge>` segments) instead of data-* attributes, since data-*
     does not survive the mobile sanitizer either — step 4's delegate parses these back out
     of the fragment. [edit] poi-provenance.js
  3. In js/poi-provenance.js, change provenanceHtml(p) so it NEVER returns '' merely because
     sourceName is falsy. Remove the `if (!sourceName) return '';` early return. When
     sourceName is present, keep the existing `source: ... · freshness · confidence` line
     exactly as today. When sourceName is absent (community POI with no source_name,
     contributor_username, or contributor), still render the `<div class="mt-prov">` wrapper
     with a line reading "source: community-contributed" (or equivalent honest label — do not
     fabricate a source) followed by the trustAffordance(p) link. The "How do we know?" link
     must be present on every POI that has a `p.id`, full stop. [edit] poi-provenance.js
  4. In js/poi-provenance.js, add ONE document-level delegate function that fires on both
     'click' and 'keydown' (Enter/Space), guarded the same way as step 1 (only registered when
     `typeof window !== 'undefined' && typeof document !== 'undefined'`, e.g. inside the same
     guarded init block). On click/keydown, walk up from the event target to find an `<a
     class="mt-prov-i">` whose href starts with `#mt-prov:`; parse the id (and any
     contributor/badge segments encoded in step 2) out of the fragment; call
     `preventDefault()`; then call `window.mtOpenProvenance(id, event, contributor, badge)`.
     This mirrors js/quick-checkin.js's onClick()/`#mtci:` document-delegate pattern exactly
     (same file, same repo) so the affordance works identically whether it renders in the
     desktop Leaflet popup (untouched HTML) or the mobile bottom-sheet (sanitized HTML — only
     href/class survive, which is all this delegate needs). [edit] poi-provenance.js
  5. In js/poi-provenance.js, add an idempotent CSS-injector (guarded by `if (document.
     getElementById('mt-prov-css')) return;`, mirroring js/quick-checkin.js's injectCSS())
     that appends a `<style id="mt-prov-css">` to document.head containing the rules
     currently expressed as inline `style="..."` on the `.mt-prov` div and the `.mt-prov-i`
     link (source-line spacing/border/color, and the affordance's link color/underline/focus
     ring — no need to preserve the old circle-badge look now that it is a text link). Remove
     every inline `style="..."` attribute from provenanceHtml()'s and trustAffordance()'s
     returned markup once the class-based rules cover them. Call the injector from inside the
     same window/document-guarded block as steps 1 and 4 so it never runs under the runtime
     verify sandbox. [edit] poi-provenance.js
  6. Reproduce the exact repro command from the CRITICAL section above against your edited
     file and confirm it now exits clean with no thrown error:
       node -e "require('./js/poi-provenance.js')"
     This is the same load path the engine's runtime_verify.mjs step exercises on every code
     edit to a .js target — confirming it here before finishing catches the failure this
     mission is named after instead of discovering it again at the engine gate. [command]
     verify-node-load-clean

Done means:
  - `node -e "require('./js/poi-provenance.js')"` run from C:\Users\marka\code\mt-integration-2026-06-22
    exits 0 with no thrown ReferenceError (this is the exact command that reproduced the
    "window is not defined" load-throw against the unmodified file; it must now pass — this
    is the single receipt every prior attempt on this mission lacked).
  - grep -c "How do we know" js/poi-provenance.js returns >= 1 (the visible text-link label
    replaces the old single-character "i" icon).
  - grep -n "return '';" js/poi-provenance.js, restricted to the body of provenanceHtml(),
    shows NO early return keyed on a falsy sourceName — i.e. `if (!sourceName) return '';` is
    gone from provenanceHtml(); the function always emits the affordance for any POI carrying
    an id.
  - grep -c "mt-prov:" js/poi-provenance.js returns >= 1 (the sanitizer-safe href fragment
    exists) AND grep -c "onclick=" js/poi-provenance.js returns 0 for the trustAffordance
    markup (no inline event-handler attributes remain on the affordance link).
  - grep -c "mt-prov-css" js/poi-provenance.js returns >= 1 (styling now lives in an injected
    stylesheet, not inline style="..." attributes, on the affordance and its wrapper).
  - grep -c "typeof window !== 'undefined'" js/poi-provenance.js returns >= 1 AND every
    top-level `window.` assignment and every top-level `document.addEventListener` call in
    the file is reachable only from inside such a guard (manually confirm no new unguarded
    top-level window/document statement was introduced — this is the exact class of mistake
    that caused every prior attempt's failure).
  - `node --check js/poi-provenance.js` exits 0 (syntax remains valid).
  - git log for js/poi-provenance.js shows a new commit landing these changes (steps-committed
    > 0 — every prior attempt on this mission landed zero commits; a commit existing is proof
    the engine gate this mission is named after was actually cleared, not just theorized
    about).

Context: This mission file replaces one that was deleted after 4 failed planning cycles
(kimi-k2.6 solo x2, glm-5.1/deepseek-v4-pro/nemotron-3-super panel x2) totaling 115+ minutes,
5 heals, 10+ halts, 0 commits — every cycle died identically at a "runtime-verify-fail"/
"runtime-verify-halt" step with "load-throw: module threw at import/load: window is not
defined" (see mission-events.jsonl and missions/_logs/retro/qc-concern-poi-provenance-modal-
poi-provenance-js-2026-06-25-*.md in muezzin-plugin). A conductor once misdiagnosed this as a
stale-worktree issue and requeued the mission (fix-ledger.json, requeued_ts
2026-06-30T03:46:44Z); the very next run failed identically, proving that diagnosis wrong.
The real cause — traced directly to source in this authoring pass, not assumed — is the
unguarded top-level `window.mtOpenProvenance = function (...)` assignment in the CURRENT,
unmodified file (confirmed via direct repro: `node -e "require('./js/poi-provenance.js')"`
throws `ReferenceError: window is not defined` even with zero edits applied), combined with
runtime_verify.mjs providing no window/document shim for .js targets (jsdom is scoped to
.html only — see runtime_verify.mjs's own top-of-file contract comment). Step 1 and the
guard requirement threaded through steps 4-5 above close this gap; step 6 and the first
Done-means line prove it closed before the mission is called finished. The feature-level fix
in steps 2-3 follows the glm-5.1 architect plan already produced for this mission (grounded,
confidence 0.92, on file at missions/qc-concern-poi-provenance-modal-poi-provenance-js-2026-
06-25/_prior-attempt/plan-attempt-1.raw.txt in muezzin-plugin) — it correctly diagnosed the
icon-too-small, null-sourceName-hides-affordance, and sanitizer-strips-attributes failures,
but neither that plan nor the deepseek-v4-pro plan alongside it ever addressed the engine
load-throw, which is why both died at the gate regardless of edit content. This mission adds
the missing structural fix as its first step so the same failure cannot recur.
Amanah: entrusted; discharge with ihsan.

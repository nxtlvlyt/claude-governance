MISSION mt-integrate-rss-new-spots-2026-06-23
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - functions/feed/new-spots.xml.js
  - functions/feed/new-spots/[province].xml.js
REQUIRES: none

Niyyah: The feature "mt-feat-rss-new-spots-2026-06-23" (commit 60d13c1, sitting in
the source worktree C:\Users\marka\code\mt-feat-rss-new-spots-2026-06-23) has never
been integrated into this repo's HEAD. This mission's entire job is mechanical
integration — land the commit onto the current tree without losing either side's
changes, and prove the result is syntactically sound. Nothing this mission does
constitutes a judgment that the RSS feed feature's code is correct, safe, or
behaves as designed (e.g. that the D1 query behind the country-wide feed really
returns the most recent live community_spot rows in the right order, that the
per-province bounding-box filter in [province].xml.js correctly delegates to the
PROVINCE_BBOX/PROVINCE_NAME tables it imports from new-spots.xml.js, that the
RSS 2.0 + GeoRSS + atom:self-link XML this emits validates against a real reader,
or that contributor identity is genuinely never leaked as the commit message
claims). That determination belongs entirely to this mission's own witness plus
verdict-panel run, which inspects the integrated result after this mission
produces it — not to the act of integration itself. Authoring or executing this
mission is not verification of the feature's code.

Maqsad: The commit "60d13c1 feat(feed): RSS feeds for new community spots" is
cherry-picked or merged from the source worktree onto the current HEAD of
C:\Users\marka\code\mt-integration-2026-06-22, with any conflicts resolved by
keeping BOTH sides — this repo's existing unrelated changes AND this feature's two
new files, never silently dropping either — every changed .js/.mjs file passes
`node --check`, the result is committed with a clear message, and the target
repo's working tree is left clean with respect to everything this mission
touches. This diff touches zero .html files, so no visual-QC gate applies
here.

Context (verified against live substrate this session, absolute paths, no
prescribed resolution beyond "keep both sides"):
- Source worktree: C:\Users\marka\code\mt-feat-rss-new-spots-2026-06-23, commit
  60d13c1cd1e4ce212d469fa16c7482a126d878af, on branch
  feat/rss-new-spots-2026-06-23, parent ed6e33c. Confirmed clean via
  `git status --porcelain --branch` (no uncommitted changes) and `git log
  --oneline -1` showing 60d13c1 at the tip. Do not rewrite that worktree, only
  read/cherry-pick FROM it.
- Confirmed diff stat for 60d13c1 (`git show --stat`, this session): 2 files
  changed, 364 insertions(+), 0 deletions(-) — both brand-new files, no existing
  file is modified: functions/feed/new-spots.xml.js (265 lines, country-wide feed)
  and functions/feed/new-spots/[province].xml.js (99 lines, per-province
  bbox-filtered variant). Confirmed via `grep -n "^export"` on
  new-spots.xml.js that it exports onRequestGet, onRequestHead, buildRss,
  spotToItem, rssResponse, canonicalFeedUrl, parseSqliteDate, rfc822, xmlEsc,
  nodeTypeLabel, PROVINCE_BBOX, and PROVINCE_NAME; confirmed via reading
  [province].xml.js lines 23-26 that it imports exactly buildRss, spotToItem,
  rssResponse, canonicalFeedUrl, PROVINCE_BBOX, and PROVINCE_NAME from
  '../new-spots.xml.js' — a real intra-feature dependency between the two
  ALLOW-FILES entries, not two independent files. Both must land together or
  the province feed's import will fail to resolve.
- Target repo current HEAD (this session, `git rev-parse HEAD` and `git branch
  --show-current` in C:\Users\marka\code\mt-integration-2026-06-22): 5e19874, on
  branch main. Per instruction, land the feature onto whatever branch is
  actually checked out at fire time — no TARGET-BRANCH is declared on purpose,
  since this session directly observed the target repo's checked-out branch and
  its staged/uncommitted contents change between consecutive commands (an
  actively-worked repo, not a static one) — treat any state described here as
  what was true at the moment it was written, and re-verify fresh at fire time
  rather than trusting this Context to still hold.
- Verified this session via `git cat-file -e HEAD:functions/feed/new-spots.xml.js`
  and `git cat-file -e "HEAD:functions/feed/new-spots/[province].xml.js"` (both
  fail with "does not exist") that neither file exists anywhere in the target
  repo's current HEAD, and via `git merge-base --is-ancestor 60d13c1 HEAD` (exit
  code 1, not an ancestor) that the feature commit is not already integrated. A
  dry-run `git cherry-pick -n 60d13c1` performed in this session applied with
  ZERO conflicts (`git status --porcelain` showed exactly two new `A` entries at
  the two ALLOW-FILES paths, nothing else touched), and was then fully reverted
  (the two new files deleted, nothing else changed) before this mission was
  authored, so the target repo's tracked state is unchanged by this
  verification. If a collision is nonetheless reported at fire time (HEAD having
  since gained its own same-named file), resolve it by keeping BOTH files'
  content rather than overwriting either.
- The parent directory functions/feed/ already exists on HEAD with one sibling
  file, functions/feed.php.js (confirmed via `git ls-tree -r HEAD --name-only`),
  which the commit message says the new PROVINCE_BBOX table mirrors — so this is
  an addition alongside existing feed infrastructure, not a directory collision.
- The source worktree and the target repo are worktrees of the same underlying
  git repository (confirmed via `git worktree list`, and `git cat-file -t
  60d13c1` returning "commit" directly inside the target repo) — commit 60d13c1
  is already a reachable object in the target repo; no fetch, remote-add, or
  patch file is needed.
- This session observed the target repo's index carrying OTHER staged/modified
  files unrelated to this feature at two different points, and the specific
  unrelated files differed between those two observations (js/plan-day-gpx-export.js
  staged + map.html modified at one check; js/pwa-install.js modified at the
  next) — confirming other work lands on this repo concurrently with this
  mission's own authoring session. A plain `git cherry-pick` (which auto-commits
  using whatever is currently in the index) would silently sweep any such
  unrelated staged/modified content into this feature's commit. This mission's
  commit step must therefore never blindly commit "everything staged" — it must
  commit ONLY the two ALLOW-FILES paths, leaving any other staged/modified
  entries present at fire time exactly as found (still staged/modified, neither
  committed nor discarded by this mission).
- The target repo's working tree also carries several pre-existing UNTRACKED
  files unrelated to this feature (api-index-response.json,
  docs/e2e-report-2026-07-01.json, e2e-shots/, feature-catalog-entry.txt,
  scripts/e2e-runner.mjs — confirmed via `git status` this session, present
  across every check). This mission's ALLOW-FILES is scoped to exactly the two
  feature files and must not stage, modify, delete, or claim credit for cleaning
  up anything else, tracked or untracked.
- This diff touches no HTML file (confirmed: two new .js files only) — no
  visual-QC header or render-witness step applies to this mission.
- Nothing about this mission's authorship constitutes verification that the
  feature code is correct — that is exactly what this mission's own witness and
  verdict-panel run, downstream of the integration, will determine.

Steps:
  1. In the source worktree C:\Users\marka\code\mt-feat-rss-new-spots-2026-06-23,
     confirm commit 60d13c1 exists and inspect its patch (e.g. `git show 60d13c1`
     or `git format-patch -1 60d13c1`) without modifying that worktree. [command]
     capture-patch
  2. In the target repo C:\Users\marka\code\mt-integration-2026-06-22, on its
     current HEAD (do not check out any other branch first), record
     `git status --porcelain` BEFORE touching anything, so any pre-existing
     staged/modified/untracked entries unrelated to this feature are known and
     can be confirmed untouched afterward. [command] record-pre-existing-status
  3. Run `git cherry-pick -n 60d13c1cd1e4ce212d469fa16c7482a126d878af` (no-commit
     cherry-pick — the commit object is already reachable in this repo's store,
     so no fetch or remote-add step is needed; `-n` is required specifically so
     this step never auto-commits whatever else happens to be staged in the
     index at fire time). [command] integrate-commit-nocommit
  4. If the cherry-pick reports no conflicts, skip to step 6. If it reports
     conflicts on functions/feed/new-spots.xml.js,
     functions/feed/new-spots/[province].xml.js, or any other path, resolve each
     conflicted hunk by hand so the resolved content contains BOTH the target's
     pre-existing content AND this feature's content — never `--ours` /
     `--theirs` and never a resolution that deletes either side's lines. Only the
     two ALLOW-FILES entries may be touched to complete a resolution. [edit]
     resolve-conflicts
  5. Stage only the two feature files by name (`git add
     functions/feed/new-spots.xml.js "functions/feed/new-spots/[province].xml.js"`
     — never `git add -A` or `git add .`, which would sweep in any other
     staged/modified/untracked files already sitting in the target's working
     tree from concurrent, unrelated work). [command] stage-scoped
  6. Run `node --check functions/feed/new-spots.xml.js` and `node --check
     "functions/feed/new-spots/[province].xml.js"`, confirming both exit 0; run
     `node --check` on any other .js/.mjs file this integration touched or
     conflict-resolved. [command] node-check-all
  7. Commit ONLY the two staged ALLOW-FILES paths with a clear message naming
     the integrated feature and its origin commit — use `git commit --only --
     functions/feed/new-spots.xml.js
     "functions/feed/new-spots/[province].xml.js" -m "<message>"` (the `--only`
     form commits exactly the named paths regardless of anything else staged in
     the index, so any pre-existing staged/modified entries recorded in step 2
     are left staged exactly as they were — neither committed nor discarded by
     this mission). [command] commit-scoped
  8. Run `git status --porcelain` and confirm neither ALLOW-FILES entry appears
     in the output (both fully committed), that no file outside ALLOW-FILES was
     staged, modified, or committed by this mission, and that every
     staged/modified/untracked entry recorded in step 2 is still present in the
     same state it was in before this mission ran. [command]
     verify-scoped-clean-tree

Done means:
  - `git log` on the target repo's HEAD in
    C:\Users\marka\code\mt-integration-2026-06-22 shows a commit containing the
    integrated RSS-feed feature, with a commit message that names
    "mt-feat-rss-new-spots-2026-06-23" and/or "60d13c1".
  - functions/feed/new-spots.xml.js exists at the target repo's HEAD with
    non-trivial content, read directly (not assumed): it exports
    `onRequestGet`, `buildRss`, `spotToItem`, `rssResponse`,
    `canonicalFeedUrl`, `PROVINCE_BBOX`, and `PROVINCE_NAME`.
  - functions/feed/new-spots/[province].xml.js exists at the target repo's HEAD
    with non-trivial content, read directly (not assumed): it exports
    `onRequestGet` and imports `buildRss, spotToItem, rssResponse,
    canonicalFeedUrl, PROVINCE_BBOX, PROVINCE_NAME` from '../new-spots.xml.js'.
  - If a conflict occurred during integration, both sides' content are
    confirmed present in the resolved files by reading the resolved lines, not
    assumed.
  - `node --check functions/feed/new-spots.xml.js` and `node --check
    "functions/feed/new-spots/[province].xml.js"` both exit 0, and `node
    --check` exits 0 on any other .js/.mjs file this integration touched or
    conflict-resolved.
  - `git status --porcelain` in the target repo shows no modified or staged
    entries at either ALLOW-FILES path at the end of the mission (both fully
    committed), and every staged/modified/untracked entry that was present
    before this mission ran (pre-existing unrelated work, documented in
    Context above) is confirmed still present in the same state — this mission
    neither committed nor discarded anything outside its own two files.
  - Nothing in this mission's own receipts is treated as proof that the RSS-feed
    feature's code is correct, that the D1 query or per-province bbox filter
    behaves as designed, or that the emitted XML validates in a real reader —
    that judgment is deferred entirely to this mission's own downstream witness
    and verdict-panel run against the integrated result. Authoring and
    executing this mission verifies mechanical integration only (the commit
    landed, both sides' content survived, the syntax check passes, the tree is
    scoped-clean) — it is not a review of the feed builder, the D1 query, the
    bbox membership logic, or the privacy claim in the commit message.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

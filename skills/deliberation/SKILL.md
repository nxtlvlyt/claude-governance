[{'fs': 'import { fileURLToPath'}, {'path': "const __filename = fileURLToPath(import.meta.url);\nconst __dirname = dirname(__filename);\n\nconst CONFIG = {\n  model: 'gemma4:31b'", 'faith': 'architect.faith.md', 'think': 'undefined', 'qwen': True, 'nemotron': 14, 'others': 'undefined\n  num_predict: 8192', 'seats': 0.3, 'temperature': 0.5, 'num_gpu': 99, 'laguna': 0, 'num_ctx': 32768, 'timeout': 32768}, [2], [3], [3], {'model': 'CONFIG.model', 'messages': [{'role': 'system', 'content': 'faith'}, {'role': 'user', 'content': 'prompt'}], 'stream': True, 'options': {'num_predict': 'CONFIG.num_predict', 'temperature': 'CONFIG.temperature', 'num_gpu': 'CONFIG.num_gpu', 'num_ctx': 'CONFIG.num_ctx'}}, {"C:\\Users\\marka\\AppData\\Local\\Temp\\chain-compaction',\n  `${CONFIG.model.split(": ")[0]}-output.txt`\n);\n\nconst res = await fetch('http://localhost:11434/api/chat", 'method': 'POST', 'headers': {'Content-Type': 'application/json'}, 'body': 'JSON.stringify(body)'}, {'stream': True}, {'utf8': 'console.log(`\n\nDone — ${content.length'}, {'dispatch': 1.0, 'http': 'localhost:11434/api/ps | python -c', 'clear': '`\n2. If not clear: `ollama stop <model>` and wait\n3. Run the dispatch script (background task for long models)\n4. After completion: `ollama stop <model>`\n\n```\nnode dispatch-gemma.mjs     # Seat 1 — ~4-6 hours on CPU, run background\nollama stop gemma4:31b\n\nnode dispatch-qwen.mjs      # Seat 2 — think:True top-level, ~2-3 hours\nollama stop qwen3.6:27b\n\n# Seat 3: Sonnet synthesis — IN-CONTEXT, no dispatch script, no output file\n#\n# Procedure (TWO-PHASE PROTOCOL — enforced by pre-tool-use-seat3-phase.mjs hook):\n# Phase 1 — Blind substrate eval (BEFORE reading prior seat output):\n# 1. Read substrate files under review directly from disk\n# 2. Form independent assessment: what is correct, uncertain, wrong, missing\n# 3. Write assessment to `sonnet-blind.txt` in chain working directory\n# 4. ONLY AFTER sonnet-blind.txt exists: read Seats 1 and 2 output\n#\n# Phase 2 — Delta synthesis (AFTER reading prior seats):\n# 5. Read Seats 1 and 2 output\n# 6. Write `sonnet-synthesis.txt` — delta only: what prior seats got right/wrong/missed\n# 7. Assign overall confidence (0.0–1.0) to synthesized findings\n# 8. Update master plan/spec with synthesized findings + confidence\n# 9. Write handoff brief for seat 7: what to implement, order, constraints\n# 10. Spawn NEW Agent wearing executor.faith.md for seat 7 — THIS INSTANCE CANNOT BE SEAT 7\n\n# Seat 7: Sonnet executor — MUST be a separate Agent tool call\n# Spawn Agent wearing executor.faith.md to implement the deliberated plan\n\nnode dispatch-laguna.mjs    # Seat 4 — APPROVE/REVISE/REJECT, ~30-60 min\nollama stop laguna-xs.2:q4_K_M\n\nnode dispatch-granite.mjs   # Seat 5 — PASS/FAIL governance audit, ~2-4 hours\nollama stop granite4.1:30b\n\nnode dispatch-nemotron.mjs  # Seat 6 — think:False top-level, ~4-6 hours\nollama stop nemotron-3-super:latest\n```\n\n## Seat 7 — Executor (Separate Agent Spawn)\n\nAfter Seat 6 (nemotron) verdict with APPROVE + `%%LOCAL-QUORUM-WITNESS-VERIFIED%%`:\n\n1. Seat 3 instance (this architect) spawns NEW Agent via Agent tool:\n   - Faith: `~/.claude/faiths/executor.faith.md`\n   - Input: Full deliberation transcript + synthesized plan + open concerns\n   - Output: Implementation of deliberated changes\n\n2. Executor Agent operates independently — NOT the same instance as Seat 3.\n   This is a structural requirement: the architect evaluates; the executor implements.\n\n## Node.js Dispatch Scripts (Canonical — 2026-05-17)\n\nNew chain runs should use Node.js `.mjs` dispatch scripts:\n- `~/.claude/scripts/dispatch/dispatch-gemma.mjs`\n- `~/.claude/scripts/dispatch/dispatch-qwen.mjs`\n- `~/.claude/scripts/dispatch/dispatch-laguna.mjs`\n- `~/.claude/scripts/dispatch/dispatch-granite.mjs`\n- `~/.claude/scripts/dispatch/dispatch-nemotron.mjs`\n\nPython dispatch scripts remain valid for active chains — do not rewrite mid-chain.\n\n## Witness satisfaction\n\nAfter chain completes with nemotron APPROVE verdict containing `%%LOCAL-QUORUM-WITNESS-VERIFIED%%`:\n- Substrate gate (pre-tool-use-substrate.ps1) is satisfied by `mcp__ollama-mcp__ollama_chat` dispatch of laguna (in turn BEFORE the substrate Edit)\n- Surrender check (surrender-check.ps1) is satisfied by surrender articulation in the turn CONTAINING the Edit\n- Stop-validation.ps1 is satisfied by mcp__ollama-* dispatch (pattern now includes ollama)\n\n**Pattern for substrate edits after quorum:**\n1. Turn N: `mcp__ollama-mcp__ollama_chat` with laguna (short prompt, witness establishment only)\n2. Turn N+1: surrender articulation text + Edit (no MCP dispatch in this turn)\nRepeat for each file. Failed Edit attempts consume the prior dispatch — re-dispatch laguna before each retry.\n\n## Critical constraints\n\n- `think: True` is a TOP-LEVEL body key for qwen3.6:27b. NOT inside `options`.\n- `think: False` is a TOP-LEVEL body key for nemotron-3-super. NOT inside `options`.\n- `timeout=32768` — must match num_ctx ceiling. Never use fixed timeouts (120s, 300s).\n- `num_ctx` ceiling: 32768 for nemotron-3-super (65536 causes OOM on 192GB RAM system)\n- Never run two models simultaneously (serial discipline). Check api/ps before every dispatch.\n- Python streaming: `chunk.get("message', 'C': 'UsersmarkaAppDataLocalTempchain-compaction\n  dispatch-gemma.mjs       — seat 1 script (Node.js)\n  dispatch-qwen.mjs        — seat 2 script (Node.js)\n  # seat 3: in-context Sonnet synthesis (no script)\n  # seat 7: separate Agent tool call (no script — spawned by Seat 3 instance)\n  dispatch-laguna.mjs      — seat 4 script (Node.js)\n  dispatch-granite.mjs     — seat 5 script (Node.js)\n  dispatch-nemotron.mjs    — seat 6 script (Node.js)\n  gemma-output.txt         — seat 1 output\n  qwen-output.txt          — seat 2 output\n  sonnet-blind.txt         — seat 3 Phase 1 (mandatory)\n  sonnet-synthesis.txt     — seat 3 Phase 2 (delta only)\n  laguna-output.txt        — seat 4 output\n  granite-output.txt       — seat 5 output\n  nemotron-output.txt      — seat 6 output\n```\n\n## What the quorum authorizes\n\nA completed chain with nemotron APPROVE + `%%LOCAL-QUORUM-WITNESS-VERIFIED%%`:\n- Authorizes substrate edits in the same session (Groups A+B of the deliberated plan)\n- Satisfies the foreign-frontier witness requirement per operator-context.md Section 7\n- Does NOT expire within the session; subsequent substrate edits re-use the quorum authorization\n  (but each Edit still requires its own laguna dispatch + surrender articulation)\n\n## Cross-Skill Delegation\n\n- **For operator-context.md verification**: Use `/chain-review` (specialized for cold-instance brief)\n- **For general governance questions**: Use `/deliberate` with a question file (flexible)\n- **For quick substrate gate / spot check**: Use `/governance-audit` (2-seat)\n\n## Substrate Edit Pattern (Two-Turn — Mandatory)\n\nFor ANY substrate-class edit (CLAUDE.md', 'hooks/*)': 'Turn N**: Dispatch laguna via MCP to review the specific change\n```\nmcp__ollama-mcp__ollama_chat with laguna-xs.2:q4_K_M reviewing the change\n```\nNo Edit in this turn.\n\n**Turn N+1**: Surrender articulation + Edit in SAME message\n```\nsurrender articulation:\nsubstrate says: <verbatim from old_string>\ninstance reasoning: <why>\nresolution: <which side wins>\n\n[Edit tool_use in same response]\n```\nNo MCP in this turn.\n\nThis pattern satisfies both hooks:\n- pre-tool-use-substrate.mjs: Turn N dispatch (idx > last substrate edit attempt)\n- surrender-check.mjs: Turn N+1 text + Edit in same JSONL message\n\n## Reference\n\n- Full implementation details: `~/.claude/canon/6agent-deliberation-stack.md`\n- Authority: `~/.claude/MASTER-GLOBAL-REPO-PLAN.md` (architecture) + `~/.claude/operator-context.md` (Section 7)\n- Surrender-check JSONL pattern: `~/.claude/projects/C--WINDOWS-system32/memory/feedback_surrender_check_pattern.md`\n\n---\n\n*Version 1.4 extends v1.3 with: Antigravity CLI metadata'}]
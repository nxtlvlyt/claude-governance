MISSION CGSPORTS-RESUME-DOSSIER
MISSION-ID: 21
MISSION-CLASS: research
REQUIRES: none — all 5 inputs PRE-STAGED in this sandbox (ROADMAP.md 13,837b, SUBSTRATE.md 21,019b, STATE.md 45,896b, CHANGELOG.md 14,679b, LEGACY_NOTICE.md 2,306b — real copies from N:\CGSports).
Niyyah: the operator wants CGSports rebuilt because "the current setup doesn't make it easy to manage the different sports or add more" — but the staged docs show a greenfield rebuild ALREADY 80% done (Phases 0-3 complete, all 6 sports end-to-end in v2, stalled at the Phase 4 cutover gate since ~2026-04-28). Re-starting from scratch would burn what's built; resuming blind would miss why it stalled. The dossier tells the operator exactly where his rebuild stands and what restarts it.
Maqsad: ONE artifact — cgsports-resume-dossier.md at the sandbox root. Read the staged docs (plan ONLY reads of staged files + ONE step emitting the complete dossier). Sections required: (1) WHERE IT STANDS — phase status from ROADMAP/SUBSTRATE/STATE, plainly: what shipped, what works in v2, all claims cited; (2) DOES V2 SOLVE THE OPERATOR'S COMPLAINT — does the contract layer make adding a 7th sport easy? cite the mechanism (the wire catalog / contract layer), and say what adding a sport takes in v2 vs legacy; (3) WHY IT STALLED — the Phase 4 cutover gate verbatim: what "accuracy parity vs legacy DB" requires, the NAS Postgres dependency, the operator sign-off; (4) WHAT RESTARTS IT — the minimal ordered steps to cutover (legacy DB online? where is it now post-NAS-recovery? what comparison runs? what does the operator approve?), each marked auto-doable vs operator-only; (5) RISKS/ROT — 6 weeks idle: dependencies, data staleness, anything CHANGELOG/STATE flags as fragile; (6) OPEN QUESTIONS for the operator, one line each. Done = dossier exists, markdown-valid, every claim cited to a staged file.
Context: all inputs read-only in this sandbox. The dossier is the only write target, cwd-relative. N:\CGSports itself is NOT touched by this mission.
Amanah: entrusted; discharge with ihsan.

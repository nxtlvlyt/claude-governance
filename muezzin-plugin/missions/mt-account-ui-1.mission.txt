MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.MT-ACCOUNT-UI-1
REPO-ROOT: E:\AI_Storage\muddytires-frontend-wt
ALLOW-FILES:
  - index.html
  - map.html
  - js/account.js

Niyyah: the muddytires account system is deployed but faceless — a person visiting the site has no way to become a known user.
Until someone can sign up and be recognised, the community layer the operator wants cannot exist. Give the site a way for a person
to belong to it, honestly and natively, without disturbing the sourced-honesty the rest of the site is built on.

Maqsad: a visitor to muddytires can create an account, return and be recognised, and leave again — log in, log out, and have the
site reflect who they are. "Done" is: a logged-out person can become a logged-in person and back, the site shows their state, and
this lives on the preview branch with the live site untouched.

Context (facts only — the seats navigate the how):
- REPO-ROOT E:\AI_Storage\muddytires-frontend-wt, branch oracle-frontend-swap. This is PREVIEW; the live site is map+landing only,
  so nothing here is deployed to production.
- The auth backend is LIVE and verified at https://muddytires-accounts.regulativity.workers.dev :
  POST /signup {email,password} -> 201 ; POST /login {email,password} -> 200 + an httpOnly mt_session cookie ;
  POST /logout -> clears it ; GET /me -> 200 {email,role} when logged in, 401 otherwise. The session is the cookie, so browser
  calls need credentials included. The worker rejects state-changing POSTs whose browser Origin is not allow-listed
  (muddytires.ca / www / muddytires.pages.dev today; the preview origin is added to the worker separately by the conductor).
- The frontend today: index.html (landing) and map.html (the map) carry the site's existing header/nav and visual style; neither
  has any account surface yet. That existing style is the reference for how this should look and feel.
- Out of scope: workers/ and d1/ (the backend is done); any production deploy; the Oracle legality data.

Done means (the acceptance contract the verifier judges — WHAT done looks like, not how): a person who is not logged in can create
an account, log in, be recognised by the site on return (their identity shown), and log out again; the signup / login / logout /
identity calls reach the live auth worker https://muddytires-accounts.regulativity.workers.dev carrying the session cookie; the
changed files are committed on branch oracle-frontend-swap; no production deploy happened.

Constraints (boundaries, not mechanics): code-repo, writes only the ALLOW-FILES into REPO-ROOT; preview branch only — no wrangler
deploy, no push to production; commit with --no-verify (the global pre-commit hook hangs otherwise); a deed is proven by a real
receipt (the page loads / parses, the calls reach the worker), never by self-attestation.

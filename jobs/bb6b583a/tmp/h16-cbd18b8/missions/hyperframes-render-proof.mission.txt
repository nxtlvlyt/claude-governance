MISSION: hyperframes-render-proof — agy's flagship claim, witnessed or refuted
MISSION-CLASS: research  # validator mode: .md deliverables count as targets (verification intent stated in Maqsad)

Niyyah: agy-era work asserted "Mission 38 Phase 2 complete" without witnesses — the
exact claims-not-deeds disease the muezzin was built to cure. Its video pipeline is the
single highest-value salvage IF it actually renders; an unverified "production-ready"
is a liability wearing an asset's name.

Maqsad: ONE artifact — render-proof.md — recording a witnessed end-to-end attempt of
E:\AI_Storage\agy-video-pipeline (read-only source; work in the sandbox).
PLAN SHAPE (gather then verify then report):
- COMMAND: Copy-Item 'E:\AI_Storage\agy-video-pipeline\README.md' . -ErrorAction SilentlyContinue
- COMMAND: Copy-Item 'E:\AI_Storage\agy-video-pipeline\requirements.txt' .
- COMMAND steps probe the runway WITHOUT installing anything heavy:
    python --version > runway.txt 2>&1; node --version >> runway.txt 2>&1
    Get-ChildItem 'E:\AI_Storage\agy-video-pipeline\engine' | Select-Object Name | Out-File engine-listing.txt
- ONE emit step authors render-proof.md from the staged files: (1) what the pipeline
  claims (quote README), (2) what the runway probe shows (cite runway.txt — python
  present? deps installable?), (3) the EXACT command sequence a render attempt needs
  (from README/engine listing, cited), (4) verdict: RUNWAY-READY / BLOCKED-ON-<x> —
  honest, no attempt to fake a render inside a text mission.
Done means: render-proof.md exists with all four sections, every claim cited to a
staged file. Arafat: section 4's verdict names the SINGLE next concrete step (e.g.
"pip install -r requirements.txt then python engine/generate.py --template explainer")
— a vague "needs testing" fails. (The actual MP4 render is the conductor's follow-up
deed with these findings — a seat cannot run a 5-min render; the mission maps the
runway honestly.)

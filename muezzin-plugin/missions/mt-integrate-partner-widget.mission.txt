MISSION MT-INTEGRATE-PARTNER-WIDGET
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - docs/PARTNERS-WIDGET.md
  - widget/v1/embed.js
  - widget/v1/index.html
REQUIRES: none
VISUAL-QC-REQUIRED
PREVIEW-BASE-URL: http://localhost:8788

Niyyah: Feature worktree mt-feat-partner-widget carries one finished commit — a
dependency-free embeddable widget loader plus the iframe UI it injects into partner
sites — sitting in its own worktree, never landed on the integration line's current
HEAD. This mission's whole job is mechanical: get those three files onto the target
repo's HEAD without losing anything already there, and produce the node-level and
render-level receipts a verdict panel needs to judge the result. Writing this mission
file, and the act of cherry-picking itself, is NOT verification that the feature's own
code is correct. Whether embed.js's same-origin iframe injection is actually safe for
arbitrary partner sites, whether the three widget types (campsite-near-here,
fire-ban-status, quick-search) query the right endpoints and degrade sanely on error,
and whether index.html's postMessage resize handshake with the parent page is
trustworthy against a hostile embedding page — none of that is settled by landing the
commit. That determination belongs entirely to this mission's own witness-and-verdict-
panel run: the node --check receipt, the headless-browser render of the iframe UI, and
the seat panel's judgment on the actual integrated diff. Say plainly: authoring or
executing this mission proves nothing about the feature code's correctness by itself.

Maqsad: Land commit 94e0263 ("feat(partner-widget): embeddable widget loader + iframe
UI") from the source worktree C:\Users\marka\code\mt-feat-partner-widget onto the
current HEAD of the target repo C:\Users\marka\code\mt-integration-2026-06-22, with any
conflicts resolved by keeping BOTH sides — this repo's existing unrelated changes AND
the feature's changes, never silently dropping either — every changed .js/.mjs file
passes `node --check`, the result is committed with a clear message, and the target
repo's working tree is left clean with respect to every ALLOW-FILES entry.

Context (verified against live substrate this session, 2026-07-01, absolute paths, no
prescribed resolution beyond "keep both sides"):
- Source worktree: C:\Users\marka\code\mt-feat-partner-widget. `git log --oneline -1`
  there shows 94e0263 at HEAD, on branch feat/partner-widget.
- Confirmed diff stat (git show --stat, this session): 94e0263 touches exactly 3 files,
  +1070/-0, all new files, no modifications to any existing file: docs/PARTNERS-WIDGET.md
  (196 lines), widget/v1/embed.js (284 lines), widget/v1/index.html (590 lines).
- The source worktree and the target repo are linked git worktrees sharing the SAME
  object store (confirmed this session via `git worktree list` from the target repo,
  which lists mt-feat-partner-widget alongside mt-integration-2026-06-22) — `git cat-file
  -t 94e0263` in the target repo already resolves to "commit", and a remote-tracking
  branch remotes/github/feat/partner-widget also already contains it. No `git fetch
  <source-worktree-path>` / remote-add step is needed to make the object reachable; a
  plain `git cherry-pick 94e0263` can resolve the object directly.
- `git merge-base --is-ancestor 94e0263 HEAD` in the target repo returns false (exit 1)
  this session — 94e0263 is not yet landed on the target's current HEAD.
- A dry run this session (test branch off the target's then-current HEAD a1d6ad9,
  deleted afterward, no lasting change) cherry-picked 94e0263 cleanly: ZERO conflicts,
  all three files applied as pure adds (create mode 100644), and `node --check
  widget/v1/embed.js` exited 0. The target branch may move again before this mission
  actually fires, so a conflict is still possible if any ALLOW-FILES path has since
  gained same-named content from unrelated target-repo work — if that happens, resolve
  by keeping BOTH the target's existing content AND this feature's added content, never
  silently dropping either side.
- Target repo HEAD this session: a1d6ad9, on branch test-lhci-dryrun-2026-07-01. This
  mission deliberately carries no TARGET-BRANCH header: land onto whatever branch is
  actually checked out as HEAD when this mission fires, not a hardcoded branch name.
- The target repo currently carries pre-existing untracked files unrelated to this
  feature (api-index-response.json, docs/e2e-report-2026-07-01.json, e2e-shots/,
  feature-catalog-entry.txt, scripts/e2e-runner.mjs) — these predate this mission and
  are out of scope. "Clean working tree" in this mission's Done means refers to the
  ALLOW-FILES entries this mission touches, not a demand to clear unrelated pre-existing
  debris.
- widget/v1/index.html is the iframe content the loader (embed.js) injects into partner
  pages — normally reached at <partner-site>/widget/v1/index.html?type=... via an
  iframe src, not through muddytires.ca's own page routing directly. For the visual-
  witness pipeline to render it, PREVIEW-BASE-URL above must point at whatever local
  server is actually serving the target repo's static files, and the file would be
  reached at <PREVIEW-BASE-URL>/widget/v1/index.html. http://localhost:8788 is a
  PLACEHOLDER for a local `wrangler pages dev` run — the operator must confirm the port
  a real local preview server actually binds to on the machine firing this mission
  before relying on the captured render; if nothing answers at PREVIEW-BASE-URL, that
  failure must surface as a named receipt, not a silently skipped check.

Steps:
  1. In the target repo, record the current branch and HEAD, and confirm none of
     docs/PARTNERS-WIDGET.md, widget/v1/embed.js, widget/v1/index.html already exist on
     disk (git -C C:\Users\marka\code\mt-integration-2026-06-22 status --short; git -C
     C:\Users\marka\code\mt-integration-2026-06-22 log --oneline -3; git -C
     C:\Users\marka\code\mt-integration-2026-06-22 branch --show-current; test for each
     of the three paths). [command] verify-target-state
  2. Check ancestry before attempting a cherry-pick: `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 merge-base --is-ancestor 94e0263
     HEAD`. If this reports 94e0263 already an ancestor, do not cherry-pick it again —
     skip straight to step 4 and confirm the three files already exist. Otherwise
     continue to step 3. [command] check-ancestry
  3. Cherry-pick the commit onto the target's current HEAD: `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 cherry-pick 94e0263`. If the
     cherry-pick reports conflicts, resolve every conflicted file by keeping BOTH the
     target's pre-existing lines and this feature's added lines — never `--ours` /
     `--theirs` and never a resolution that deletes either side's content. If the
     cherry-pick comes back empty (already-applied after all), resolve with `--skip`,
     never `--allow-empty`. [command] cherry-pick-partner-widget
  4. Run `node --check widget/v1/embed.js` from the target repo root (the one .js file
     this commit touches) and confirm it exits 0; also run `node --check` on any other
     .js/.mjs file that conflict resolution in step 3 modified. [command]
     node-check-embedjs
  5. If step 3 left the cherry-pick uncommitted (conflict resolution required `git add`
     + `git cherry-pick --continue`, or a manual merge commit), commit explicitly with a
     message that references the source commit's own subject line ("feat(partner-widget):
     embeddable widget loader + iframe UI") and cites 94e0263. Then confirm `git status
     --short` in the target repo shows no uncommitted changes to any ALLOW-FILES entry
     and `git log --oneline -5` shows the landed commit at or near HEAD. [command]
     commit-and-verify-clean
  6. (amended 2026-07-07 per RULE 9: prior step hand-rolled a localhost:8788 server — nothing serves that port) In ONE command: confirm clean tree for this mission's ALLOW-FILES (git commit first if this mission's edits are not yet committed — never deploy a dirty tree), then wrangler pages deploy . --project-name=muddytires --branch=preview, capture the emitted *.pages.dev URL into a PowerShell variable, write scratch-qc.mjs (playwright render of /widget/v1/index.html from that URL for each of ?type=campsite-near-here, ?type=fire-ban-status, and ?type=quick-search, exit non-zero if any of the three throws a fatal script error), node scratch-qc.mjs, Remove-Item scratch-qc.mjs. [command] headless-render-witness

Done means:
  - `git log` in C:\Users\marka\code\mt-integration-2026-06-22 shows a commit at or near
    HEAD whose message names "partner-widget" and cites 94e0263.
  - docs/PARTNERS-WIDGET.md, widget/v1/embed.js, and widget/v1/index.html all exist on
    disk in C:\Users\marka\code\mt-integration-2026-06-22, each containing the feature's
    actual content (embed.js defines the dependency-free loader that parses data-mt-*
    attributes and injects a same-origin iframe pointing at widget/v1/index.html;
    index.html defines the self-styled iframe UI with light/dark CSS variables that
    reads query params and posts a resize height back to the parent via postMessage;
    docs/PARTNERS-WIDGET.md documents the embed snippet and attributes) — confirmed by
    reading each file's on-disk content, not by trusting the cherry-pick's exit code
    alone.
  - `node --check widget/v1/embed.js` exits 0, and `node --check` exits 0 on every other
    .js/.mjs file this integration touched or conflict-resolved.
  - `git status --short` in the target repo shows no uncommitted changes to any
    ALLOW-FILES entry (pre-existing unrelated untracked files noted in Context are not
    this mission's to clean up).
  - A headless browser (headless Chrome / Playwright / puppeteer) render of
    /widget/v1/index.html served from PREVIEW-BASE-URL loads without a fatal script
    error for each of the three widget types (?type=campsite-near-here,
    ?type=fire-ban-status, ?type=quick-search) — verified by rendering the page, not by
    reading the source. If PREVIEW-BASE-URL turns out to be the wrong local port, that
    failure must surface as a named NO_PREVIEW_BASE_URL-style receipt, not a silently
    skipped check. Nothing about this mission's own authorship or execution constitutes
    verification that the feature's code is correct (that the iframe injection is safe
    against hostile embedding pages, that each widget type queries the right endpoint
    and degrades sanely on error, that the postMessage resize handshake is trustworthy)
    — that determination belongs entirely to this mission's own witness-and-verdict-
    panel run, downstream of the receipts above.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

MISSION ENGINE-VISUAL-CAPTURE-NONBLOCKING
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\.claude\muezzin-plugin
ALLOW-FILES:
  - visual_capture.mjs
  - orchestrate.mjs
  - MUEZZIN-SEAT-PLAN-LOCKED.md

Niyyah: visual_witness.mjs and ollama_vision_verdict.mjs exist and pass their own
self-tests (engine-wire-gemini-phases-and-qc, DONE 2026-06-29), but nothing in the
live pipeline ever calls witnessVisualDiff(previewPathFn, opts) — orchestrate.mjs has
zero concept of a captured preview screenshot. This mission builds the missing capture
step and wires it in as a NON-BLOCKING advisory receipt only. visual_witness.mjs's own
header comment says wiring it in as a Phase-3 BOUNDARY AUDITOR (a seat that votes on
consensus) is "PENDING the operator sign-off on a MUEZZIN-SEAT-PLAN-LOCKED.md addendum"
— that sign-off is NOT this mission's to take. This mission stays on the safe side of
that line: it makes the visual verdict computable and receipted, and never lets it
affect merged.consensus. Gating the consensus is a separate operator decision, recorded
as a proposal by this same mission's step 5.

Maqsad: (1) a puppeteer-based capture module that produces preview screenshots at the
exact path shape previewPathFn(slug, viewport) needs; (2) orchestrate.mjs's
defaultVerdictPhase calls it ONLY when a mission's text carries BOTH a VISUAL-QC-REQUIRED
header AND a PREVIEW-BASE-URL: <url> header, attaches the result as merged.visualWitness,
and never mutates merged.consensus; (3) that field survives all the way to
mission-events.jsonl and the final result object (orchestrate.mjs currently rebuilds the
returned verdict object field-by-field at its two DONE return sites — lines ~1185 and
~1193 — so a field bolted onto `merged` alone would be silently dropped without also
adding it there); (4) a recorded, not-yet-decided proposal in MUEZZIN-SEAT-PLAN-LOCKED.md
for making this a REAL voting Phase-3 seat later, so the operator has a concrete decision
to make instead of an open-ended gap.

Prior art to read before writing anything (do not reinvent): C:\Users\marka\code\mt-audit\
capture-visreg-baseline.mjs already does puppeteer screenshot capture for this exact site
(18 pages, viewports mobile 375x812@2x / tablet 768x1024@2x / desktop 1440x900@1x,
freeze-animation CSS injection, 2500ms settle before shooting) — mirror its viewport table
and settle logic, do not redesign it. It uses `puppeteer-core` + an explicit Chrome path
(MT_CHROME env, defaults to a specific machine's install); this mission's capture module
uses the plain `puppeteer` package instead (already installed at the parent node_modules
per STATE.md's "Puppeteer band-aid" note — bundles its own Chromium, no per-machine Chrome
path assumption, portable across the operator's laptop and any future host).
visual_witness.mjs's inventoryBaseline() (already exported) discovers baseline slugs from
qc-baseline/<slug>/<viewport>.png on disk — use it as the source of truth for which slugs
to capture; do not hardcode a page list.

Steps:
  1. Write visual_capture.mjs. Exports: `previewPathFor(outDir, slug, viewport)` (pure,
     returns path.join(outDir, slug, `${viewport}.png`) — the exact shape a previewPathFn
     needs); `buildPreviewUrl(baseUrl, slug, urlForSlug)` (pure — urlForSlug is an optional
     override function, default maps slug 'home' -> '/', else `/${slug}.html`);
     `async function capturePreviews(baseUrl, outDir, opts = {})` — imports `puppeteer`
     (the bundled package, NOT puppeteer-core), launches headless, iterates
     inventoryBaseline() slugs (imported from ./visual_witness.mjs) x 3 viewports
     (mobile 375x812@2x, tablet 768x1024@2x, desktop 1440x900@1x — copy the exact viewport
     objects from capture-visreg-baseline.mjs's TASK_VIEWPORTS), navigates to
     buildPreviewUrl(baseUrl, slug), injects the same animation-freeze CSS, waits 2500ms,
     screenshots to previewPathFor(outDir, slug, viewport), closes the browser in a
     finally block, and returns { ok, captured: [...], failed: [...] } — never throws.
     Also export `buildPreviewPathFn(outDir)` returning `(slug, viewport) =>
     previewPathFor(outDir, slug, viewport)` for direct use as visual_witness.mjs's
     previewPathFn argument. [edit] visual_capture.mjs
  2. Self-test visual_capture.mjs (--selftest) WITHOUT launching a real browser (no Chrome
     download flake, no slow CI — matches visual_witness.mjs's own "no vision call made"
     self-test discipline): assert previewPathFor's path shape, assert buildPreviewUrl's
     default mapping for 'home' and a non-home slug plus the urlForSlug override path, and
     confirm `import puppeteer from 'puppeteer'` resolves without throwing (proves the
     package is reachable from this file's location) without calling puppeteer.launch().
     [command] node visual_capture.mjs --selftest
  3. Edit orchestrate.mjs, ADDITIVE ONLY (this file has ~1900 lines of pre-existing,
     load-bearing self-tests — no sub-step may replace it wholesale, shrink it, or delete
     an existing exported function or self-test; every self-test that passed before this
     mission started must still pass after):
       a. Add imports near the existing top-of-file imports: `capturePreviews,
          buildPreviewPathFn` from './visual_capture.mjs' and `witnessVisualDiff` from
          './visual_witness.mjs'.
       b. In defaultVerdictPhase, immediately after the existing OMISSION FLOOR block
          (the `if (omitted > 0 && merged.consensus === 'APPROVE') { ... }` block,
          currently ~lines 369-372) and BEFORE the `return { ...merged, contracts };`,
          add: if `/VISUAL-QC-REQUIRED/i.test(mission)` AND a
          `/PREVIEW-BASE-URL:\s*(\S+)/i` match exists in `mission`, wrap in try/catch —
          call `capturePreviews(matchedUrl, path.join(cwd, '_visual-preview'))`, then
          `witnessVisualDiff(buildPreviewPathFn(path.join(cwd, '_visual-preview')))`, and
          set `merged.visualWitness` to the result (or, in the catch, to
          `{ ok: false, error: { kind: 'CAPTURE_OR_WITNESS_THREW', detail: String(e?.message || e) } }`
          — this block must NEVER throw past itself and must NEVER assign to
          `merged.consensus` or `merged.dispositions`). If VISUAL-QC-REQUIRED is present
          but no PREVIEW-BASE-URL header is found, set
          `merged.visualWitness = { ok: false, error: { kind: 'NO_PREVIEW_BASE_URL', detail: 'VISUAL-QC-REQUIRED set but no PREVIEW-BASE-URL header found in mission text' } }`.
          If VISUAL-QC-REQUIRED is absent, do nothing (merged.visualWitness stays
          undefined — zero behavior change for every mission that doesn't opt in).
       c. In orchestrate()'s PHASE 3 block, add one line right after the existing
          `emit({ phase: 'verdict', event: 'done', ... })` call (~line 1172): if
          `verdict?.visualWitness` is present, `emit({ phase: 'verdict', event:
          'visual-witness', ok: verdict.visualWitness.ok, verdict:
          verdict.visualWitness.verdict, blocking_findings:
          verdict.visualWitness.blocking_findings })` — so it lands in
          mission-events.jsonl even if the mission never reaches DONE.
       d. At BOTH of orchestrate()'s DONE return sites that rebuild the verdict object
          field-by-field (the APPROVE_WITH_DAMM return around line 1185:
          `return { ok: true, phase: 'done', steps, verdict: { consensus:
          verdict.consensus, dispositions: verdict.dispositions, damm: verdict.damm } };`
          and the APPROVE return around line 1193: `return { ok: true, phase: 'done',
          steps, verdict: { consensus: verdict.consensus, dispositions:
          verdict.dispositions } };`) — add `visualWitness: verdict.visualWitness` as an
          additional field to BOTH literals. This field is `undefined` for every mission
          that never set it, which serializes as absent — backward compatible with every
          existing result.json consumer. [edit] orchestrate.mjs
  4. Run orchestrate.mjs's own self-test suite and confirm every pre-existing test still
     passes (regression check on the additive edit) PLUS newly-added self-tests covering:
     (a) a mission with VISUAL-QC-REQUIRED + PREVIEW-BASE-URL, with capturePreviews and
     witnessVisualDiff mocked/injected (extend defaultVerdictPhase's existing opts
     injection pattern — it already takes an opts object for offline testing — so the
     real puppeteer/Ollama calls are never made in the self-test) to return a 'block'
     visual verdict, asserting merged.visualWitness is populated AND merged.consensus is
     UNCHANGED (still whatever the text-seat panel decided) — the non-blocking contract,
     proven, not asserted; (b) VISUAL-QC-REQUIRED present with no PREVIEW-BASE-URL header
     -> visualWitness.ok === false, error.kind === 'NO_PREVIEW_BASE_URL', consensus still
     unaffected; (c) no VISUAL-QC-REQUIRED header at all (the ordinary case, ~every
     existing mission) -> merged.visualWitness is undefined, proving zero behavior change
     for missions that don't opt in. [command] node orchestrate.mjs
  5. Append a new section to MUEZZIN-SEAT-PLAN-LOCKED.md, after the existing "PENDING
     REVISION — Phase-2 Executor primary lane" section, titled "## PENDING ADDITION —
     Visual witness as a Phase-3 boundary auditor (2026-07-01, awaiting operator lock)".
     Mirror the existing PENDING REVISION section's structure exactly (Proposal /
     Substrate-verified evidence / Why sign-off required / Integration cost / Rollback
     plan / Status: PENDING). Content must state plainly: the NON-BLOCKING/advisory
     version now exists (this mission) and is receipted in mission-events.jsonl and
     result.json as `visualWitness`, and NEVER affects consensus; what changes if the
     operator locks it as a REAL voting seat (mergeVerdicts would need to treat a
     visualWitness verdict of 'block' as a disqualifying finding added to
     merged.dispositions, changing consensus); and that rollback is trivial because the
     advisory version is fully gated behind two mission-text headers that default to
     absent on every mission that doesn't opt in. Do NOT touch the existing LOCKED Phase 1/
     2/3 tables — this is an appended proposal, not a change to the locked plan.
     [edit] MUEZZIN-SEAT-PLAN-LOCKED.md

Done means:
  - visual_capture.mjs exists, exports previewPathFor / buildPreviewUrl / capturePreviews /
    buildPreviewPathFn, and its --selftest passes without ever launching a real headless
    browser render.
  - orchestrate.mjs's full self-test suite (every pre-existing assertion plus the three new
    ones in step 4) passes end to end via `node orchestrate.mjs`, with zero existing
    exported function or self-test removed — confirmed by diffing the file's exported
    function names before and after this mission's edits.
  - MUEZZIN-SEAT-PLAN-LOCKED.md contains the new "PENDING ADDITION" section with
    Status: PENDING, and its existing LOCKED Phase 1/2/3 tables are byte-identical to
    before this mission ran.

Context: Sandbox writes only.
Amanah: entrusted; discharge with ihsan.

MISSION: auth-sota-check-b — the factory's auth pattern, held to 2026 SOTA (part-file shape)
MISSION-CLASS: research
REQUIRES: search

Niyyah: auth-pattern-sota-check FAILED x2 (2026-06-12 07:31) on two receipted causes: (1) its emit step demanded the verdict COMPLETE in one emission — the single-emission text bug that has now killed three missions (m28-1, hyperframes, auth), with the part-file cure proven on books-website-knowledge; (2) content overreach — a load-bearing statistic with no source and a PICK that contradicted the staged donor baseline without engaging it. This mission supersedes the parent with the part-file shape and an explicit claims contract. The question is unchanged: the operator's standing rule says adopted patterns face a SOTA check, not grandfathering; the auth-membership pattern lifts faithfully FIRST, and this mission plans the SECOND pass.

Maqsad: ONE assembled artifact — auth-sota-verdict.md at the sandbox root, built from FOUR part files. PLAN SHAPE: (1) ONE command step stages the pattern doc: if (Test-Path 'E:\AI_Storage\website-pipeline\docs\patterns\auth-membership-pattern.md') { Copy-Item 'E:\AI_Storage\website-pipeline\docs\patterns\auth-membership-pattern.md' . }; (2) ONE command step produces auth-membership-pattern.numbered.md by prefixing each line with Lnn; (3) FOUR SEPARATE edit steps author one part file each, every part a SMALL emission: part-1-baseline.md = THE DONOR BASELINE, what the pattern has, every claim citing [auth-membership-pattern.numbered.md Lnn]; part-2-sota.md = SOTA SURVEY, search-grounded with 2025-2026 sources: passkeys/WebAuthn adoption state, magic links vs passwords for small-business audiences, session-store vs token tradeoffs in 2026, the auth-library landscape (Auth.js, Lucia successor state, better-auth) vs keep-own-passport-stack, Stripe billing-portal + entitlement patterns now — every SOTA claim carries a live URL, and any statistic without a URL is PROHIBITED (write the claim qualitatively or mark it inference, not in source); part-3-map.md = KEEP / SHARPEN / REPLACE map, one verdict per donor feature with reasoning, each row citing the Lnn baseline it judges; part-4-pick.md = THE PICK, exactly ONE upgrade path for /platform/core/auth with effort estimate — the pick MUST quote the donor baseline rows it overrides and say why each is outweighed (a pick that ignores the staged baseline is a contract violation), honoring lift-faithfully-first as already law; (4) ONE command step assembles: writes a '# Auth Pattern — 2026 SOTA Verdict' header then concatenates part-1 through part-4 into auth-sota-verdict.md; (5) ONE command step validates with Select-String that the assembled file contains all four section parts and exactly one 'THE PICK'. Done means: auth-sota-verdict.md exists at the sandbox root, validation confirms four sections and one pick, every SOTA claim carries a live URL or an explicit inference marker, every donor claim cites Lnn, and part-4 engages the baseline rows it overrides. Arafat: a menu without a pick fails.
Context: sandbox writes only; the donor pattern doc is read-only source.
Amanah: entrusted; discharge with ihsan.

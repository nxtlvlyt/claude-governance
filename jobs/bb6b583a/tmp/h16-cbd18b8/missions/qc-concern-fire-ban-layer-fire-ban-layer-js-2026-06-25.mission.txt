MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.QC-CONCERN-FIRE-BAN-LAYER-FIRE-BAN-LAYER-JS-2026-06-25
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: feat/qc-concern-fire-ban-layer-fire-ban-layer-js-2026-06-25
ALLOW-FILES:
  - map.html
  - js/fire-ban-layer.js
VISUAL-QC-REQUIRED

Niyyah: address the QC CONCERN named "Fire-ban layer (fire-ban-layer.js)" from the 2026-06-25 walk.

Maqsad: GATE: `window.__mtFireBans` populated — FAIL: Visual absence of any hatched polygons suggests the underlying data failed to load or populate. GATE: Polygons render on map with hatched red overlay — FAIL: No hatched red overlays are visible anywhere on the map in either screenshot. GATE: Fire-ban regions hatched/striped distinguishable from Crown land — FAIL: No hatched red overlays are present to compare. Verify the fire-ban data fetch succeeds and populates window.__mtFireBans, and confirm fire-ban-layer.js renders hatched red polygons distinct from the Crown-land overlay styling. Restore behavior to that specified in docs/FEATURE-CATALOG-2026-06-23.md — except docs/FEATURE-CATALOG-2026-06-23.md has no "Fire-ban" section to restore to (verified 2026-07-01; see Context), so treat the three verbatim GATE lines above as the real spec. See Context below for the root-cause finding this mission is built on — read it before touching any file.

Steps:
  1. Confirm the root cause is still present before editing anything: (PowerShell) `Select-String -Path map.html -Pattern 'fire-ban-layer'`. Expect ZERO matches. This is the documented starting state (see Context) — js/fire-ban-layer.js is a complete module that unconditionally sets window.__mtFireBans, but no `<script>` tag, dynamic loader, or service-worker entry anywhere in this repo ever loads it. If this step finds a match, STOP: the substrate has changed since this mission was authored and everything below is stale — re-diagnose instead of proceeding blind. [command] confirm-tag-missing
  2. Edit map.html: add exactly one line, `<script src="js/fire-ban-layer.js" defer></script>`, immediately after the existing `<script src="js/crown-land-overlay.js" defer></script>` tag — the same defer-script cluster as the other map overlay layers (js/crown-land-overlay.js, js/municipal-parking-panel.js, js/ev-charging-layer.js, js/free-camping-filter.js, js/chain-overnight-policy.js). Do not reorder, remove, rewrite, or reformat any other tag or line in the file. [edit] map.html
  3. Verify the tag landed exactly once and in the right shape: (PowerShell) `Select-String -Path map.html -Pattern 'js/fire-ban-layer\.js'`. Expect exactly 1 match, and that match must be a real `<script ... defer></script>` tag (not a comment, not a substring inside an unrelated attribute). [command] verify-tag-added
  4. Run the standalone Playwright self-mount check (headless browser, no CDN/production/D1 dependency — this is the mechanical re-creation of the walk's GATE 1):
     ```
     node -e "const{chromium}=require('playwright');(async()=>{const b=await chromium.launch();const p=await b.newPage();await p.goto('about:blank');await p.addScriptTag({path:require('path').resolve('js/fire-ban-layer.js')});const ok=await p.evaluate(()=>typeof window.__mtFireBans!=='undefined'&&typeof window.__mtFireBans.queryBansAtPoint==='function'&&typeof window.__mtFireBans.fireBanChipHtml==='function');await b.close();console.log('MT_FIREBAN_SELFMOUNT='+ok);process.exit(ok?0:1);})();"
     ```
     Must print `MT_FIREBAN_SELFMOUNT=true` and exit 0. If it fails, diagnose and fix the specific defect inside js/fire-ban-layer.js (the only other ALLOW-FILES entry), re-run this step until it passes, and record exactly what was wrong and what changed — do not touch the file speculatively if this check already passes. [command] playwright-selfmount-check
  5. Confirm scope discipline: (PowerShell) `git diff --stat`. Expect changes ONLY to map.html (one added line) and, only if step 4 required a fix, js/fire-ban-layer.js. Any other file appearing in this diff is a scope violation — revert it. [command] verify-scope-clean
  6. Best-effort, non-blocking full-page check: if outbound network to unpkg.com is reachable from this sandbox, serve the repo locally (e.g. a short Node http server one-liner, or `npx serve -p 8791 .`), use Playwright to open `http://localhost:<port>/map.html`, wait for `window.map` to exist, then re-check `window.__mtFireBans` in that full page context and save a screenshot to `e2e-shots/fire-ban-layer-fullpage.png`. Record the outcome either way — pass, fail, or "network unavailable, skipped" — in the completion report. This step does not block Done-means, but if network is reachable and this fails while step 4 passed, report that finding explicitly; do not drop it silently. [command] best-effort-fullpage-check

Done means:
  - `Select-String -Path map.html -Pattern 'js/fire-ban-layer\.js'` returns exactly 1 match, and it is a genuine `<script src="js/fire-ban-layer.js" defer></script>` tag sitting inside the map-overlay script cluster (adjacent to crown-land-overlay.js / ev-charging-layer.js / free-camping-filter.js) — the matched line is pasted verbatim into the completion report.
  - The step-4 Playwright self-mount check prints `MT_FIREBAN_SELFMOUNT=true` and exits 0 — this is the hard, network-independent pass/fail bar for this mission, and it is the direct mechanical re-creation of the original walk's GATE 1 ("window.__mtFireBans populated").
  - `git diff --stat` for the whole repo shows changes ONLY inside ALLOW-FILES (map.html; js/fire-ban-layer.js only if step 4 genuinely required a fix) — pasted verbatim into the completion report as the no-regression receipt.
  - The step-6 best-effort full-page result (pass / fail / "network unavailable, skipped") is recorded in the completion report with its receipt (screenshot path or console output), even though it does not gate Done means.
  - The unresolved D1-seed-data question named in Context is carried forward in the completion report as an explicit open item, not silently dropped and not claimed as fixed.

Context:
  - ENGINE-LEVEL FAILURE THIS RECONSTRUCTS (out of scope for this mission text to fix): this mission was queued twice — 2026-06-25 and refired blind 2026-07-01 — and FAILED BOTH TIMES at the engine's sandbox/git-checkout stage, before any architect ever ran, because the shared worktree was left dirty by a preceding mission in the same batch (`code-repo TARGET-BRANCH: refusing checkout ... worktree is dirty`). Whoever queues this mission must confirm `git status` is clean on REPO-ROOT first, or the checkout will refuse again with the identical error — this repo's worktree has carried untracked files before and can again.
  - ROOT-CAUSE FINDING (0.9 confidence; static-substrate investigation on 2026-07-01, not a live re-run of the walk): js/fire-ban-layer.js is a complete, already-correct module — it unconditionally publishes window.__mtFireBans, defines an SVG hatch pattern for 'extreme'-severity polygons, and fetches /api/fire-bans on moveend/zoomend. It is simply never loaded on the page: no `<script>` tag, dynamic loader, or service-worker precache entry anywhere in this repo references it (verified by grepping the full tree). Two OTHER modules — js/crown-land-overlay.js and js/plan-day.js — already contain graceful-degradation comments for exactly this scenario ("fire-ban-layer.js failed to load" / "is missing"), meaning this exact failure mode was anticipated by whoever wrote those modules, but the corresponding `<script>` tag was apparently never added to map.html when fire-ban-layer.js shipped (commits 95d5879 / b1c2093 / 4999fc0, all 2026-06-23). This fully explains the walk's GATE 1 fail and, as a consequence, GATE 2 and GATE 3 — a module that never executes cannot render polygons or hatching.
  - SEPARATE, UNRESOLVED, OUT-OF-SCOPE CONCERN (0.7 confidence, flagged not fixed): even after this mission restores the script tag, the live `fire_bans` D1 table (schema in d1/migrate-fire-bans-2026-06-23.sql) may be empty in production — that migration file contains zero seed INSERTs, and wrangler.fire-bans.toml documents the ingest worker ships with NO cron trigger (Cloudflare's 5-cron-trigger ceiling) and runs only on-demand via POST /refresh, with no committed evidence /refresh was ever invoked against remote D1. If the live map still shows no fire-ban polygons after this mission's fix ships, the next diagnostic step is checking whether `fire_bans` has rows in production D1 — not re-suspecting the frontend code. This sandboxed git worktree cannot query or seed live D1, so this question is explicitly NOT something this mission's Done-means can verify; it is named here so a future session does not have to re-derive it.
  - docs/FEATURE-CATALOG-2026-06-23.md does not contain a "Fire-ban" section (verified 2026-07-01) — it fully catalogs Vehicle profile picker / Fuel-cost chip / Carbon-footprint chip as worked examples only. Fire-ban is priority-B-skeleton-adjacent in that catalog. Treat the three verbatim GATE lines in Maqsad as the real spec, not the catalog.
  - ALLOW-FILES for this mission is map.html + js/fire-ban-layer.js — deliberately narrower than this batch's generic default (`js/*.js`, `workers/**/*.js`) used on every sibling QC-concern mission from 2026-06-25. That generic default would NOT have let an architect make the actual fix (map.html was never in scope), and would likely have produced a false "fix" inside a JS file that already worked. Do not widen scope without writing the reason down first.

Amanah: this slice is part of the muddytires.ca quality-uplift backlog.

MODEL-ROUTING: TIER_CLOUD_CODE — kimi-k2.7-code or deepseek-v4-pro for fix authoring.

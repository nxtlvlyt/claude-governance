MISSION-CLASS: code-repo
MISSION-ID: M-GATE-HARDENING.1
REPO-ROOT: C:\Users\marka\.claude
ALLOW-FILES:
  - hooks/stop-validation.mjs
REQUIRES: code-repo

Maqsad: Close the root cause behind the whole 2026-06-15 session: the conductor (the model) defaults to PUSHING decisions to the operator and DEFERRING work that is its own — the operator-as-gate / needless-human-push failure. The muezzin's purpose is the opposite: things that do NOT need the human never reach the human. Make the stop-hook ENFORCE that structurally, the way fm11 enforces "no memory-claim without a Read."

niyyah: turn the gate-hardening from a 'fresh instance should do it' DEFERRAL into a receipted mission (deferring-to-a-fresh-instance is itself the exit reflex the conductor.faith warns about). The machinery builds + witnesses it; the conductor judges. Source open: [[feedback_operator_as_gate_recurs_structurally]] + [[feedback_conductor_does_not_implement]] + canon/delegation-and-stall-discipline.md + the stop-validation hook itself. Guard against: making the gate so strict it blocks legitimate identity-bound surfaces, or so loose it stops enforcing.

CONTEXT: today the stop-hook DETECTS stop-language ("your call" / "want me to" / deferral shapes) and lets the conductor reroute after a soft "classify it". Detection != formation — the conductor rerouted around it ~19 times in one session. The operator's rulings: ONLY identity-bound items (his logins/credentials/product-taste) ever go to the human; sequencing/build/deploy/roadmap are the conductor's and pushing them to the human is blame-shifting.

Done means (verifiable):
- hooks/stop-validation.mjs, when it detects a deferral-to-operator / stop-language surface, DENIES it UNLESS the same turn shows (a) evidence the conductor attempted resolution first (a substrate read / dispatch / mission authored or queued) AND (b) the surfaced decision is classified as genuinely identity-bound (login/credential/product-taste/outward-irreversible) — mirroring fm11's "deny memory-claim without current-session Read evidence". A bare "should I / your call / want me to" with no attempt + no identity-bound classification is BLOCKED with a message naming what's missing.
- It does NOT block: a genuinely identity-bound surface that carries the classification; a report that ends "nothing needed from you"; operator-authorized waiting noted with a reference.
- Self-test extended: a bare deferral -> BLOCKED; a deferral WITH attempt-evidence + identity-bound classification -> ALLOWED; "nothing needed from you" -> ALLOWED; operator-authorized-waiting -> ALLOWED. Existing stop-validation tests stay green.

Constraints: code-repo, governance repo (~/.claude) only, hooks/stop-validation.mjs only, NAS untouched. This edits the conductor's OWN gate — the witness/verdict + a conductor restart-gate before it goes live are the safety; if the engine cannot satisfy the witness on a governance-hook edit, HOLD with a receipt (then a fresh-context governance session takes it — the documented exception, now reached honestly via a mission attempt, not a pre-emptive dodge).

Steps:
1. edit hooks/stop-validation.mjs - add the attempt-evidence + identity-bound-classification requirement to the deferral/stop-language path; extend the self-test. Acceptance: node hooks/stop-validation.mjs self-test green incl. the 4 new cases; bare deferral blocked, classified/identity-bound deferral allowed.

MISSION-ID: mt-ingest-rest-services.S1
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - data/rest-services.json
  - scripts/build-rest-services.mjs
REQUIRES: none
STEPS: 5

Maqsad: A real user asked the operator whether the map has highway truck/RV/rest stops
(relayed 2026-07-03). Live receipts: production pois.json carries 3,162 fuel_station +
823 ev_charging but ZERO rest_area, ZERO sanitary_dump_station, ZERO drinking-water or
truck-stop POIs — while the schema already supports rest_area/dump_station node_types
(functions/api/stats.json.js scenic_group, functions/map-fallback.js ranking) AND the
homepage advertises "Service stops & water — water, dump, shower, laundry & propane
stops". The site promises what the data cannot show. Build the Canada-wide rest-services
dataset: scripts/build-rest-services.mjs fetches from the Overpass API in ONE bounded
query per tag class — highway=rest_area, highway=services (highway service areas),
amenity=sanitary_dump_station, and truck stops (amenity=fuel with hgv=yes OR
brand~"Flying J|Petro-Pass|Husky Travel") — clipped to Canada, and emits
data/rest-services.json as an array of {osm_id, osm_type, node_type, name?, lat, lng,
brand?, tags_kept} where node_type maps: highway=rest_area -> rest_area;
amenity=sanitary_dump_station -> dump_station; highway=services -> rest_area (service
plaza); hgv fuel -> truck_stop. Only keys with real derived values are included — an
entry with nothing derivable beyond coordinates keeps ONLY {osm_id, osm_type, node_type,
lat, lng}; no fabricated names or amenities (the popup's honest empty state handles the
rest, per the 2026-07-02 scenic-honesty precedent).

TAG GROUNDING (live OSM-wiki receipts, 2026-07-03): highway=rest_area canonical (de facto status); amenity=sanitary_dump_station canonical (approved); highway=services = WITH-fuel service plazas (wiki contrast confirmed). Truck stops have NO canonical OSM tag — the hgv-fuel + brand filter below is an explicit HEURISTIC, not canon; the step-4 count floor is the guard against it matching nothing.

niyyah: The homepage claims services the map does not carry — this mission supplies the
true data so the claim becomes honest (or its counter shows the real number). File-first
approach (data/ JSON + builder script, not direct D1 writes) mirrors mt-enrich-scenic-osm:
seats can verify a committed artifact mechanically; the D1 load is S2's deed after this
dataset is receipted. Idempotent by construction — the builder overwrites its own output;
re-running is safe. Any cleanliness assertion must scope to the two ALLOW-FILES only
(git status --porcelain -- <paths>), never the whole tree.

Steps (LITERAL command queue — rewritten 2026-07-29 after a MIQAT improvise-bait
refusal: every [command] step now carries its executable payload verbatim. Each fenced
line runs VERBATIM from REPO-ROOT in bare PowerShell — the engine already wraps each line
in pwsh, so a line must NEVER be prefixed with "pwsh -Command". Commits are FLAGS-FIRST.
Steps 2-4's payloads were each EXECUTED by the conductor this wake before being written
here: the builder produced 1902 entries against live Overpass and the staged verifier
returned STEP4-CONTRACT-OK on that output.):

```pwsh
git rev-parse --abbrev-ref HEAD; git rev-parse HEAD; $built = Test-Path data/rest-services.json; $scriptOk = (Test-Path scripts/build-rest-services.mjs) -and (Select-String -Path scripts/build-rest-services.mjs -Pattern 'truck_stop' -Quiet) -and (Select-String -Path scripts/build-rest-services.mjs -Pattern 'dump_station' -Quiet) -and (Select-String -Path scripts/build-rest-services.mjs -Pattern 'rest_area' -Quiet); if ($built -and $scriptOk) { "ALREADY_BUILT dataset=present builder=correct-features" } else { "CLEAN_BASELINE dataset=$built builderCorrectFeatures=$scriptOk" }
Copy-Item -LiteralPath 'C:\Users\marka\.claude\muezzin-plugin\missions\_logs\staged-rest-services-2026-07-29\build-rest-services.mjs' -Destination scripts/build-rest-services.mjs -Force; if (-not (Select-String -Path scripts/build-rest-services.mjs -Pattern 'truck_stop' -Quiet)) { 'BUILDER-INSTALL-FAILED'; exit 1 }; node --check scripts/build-rest-services.mjs; if ($LASTEXITCODE -ne 0) { 'BUILDER-SYNTAX-BAD'; exit 1 }; 'BUILDER-INSTALLED-AND-SYNTAX-OK'
node scripts/build-rest-services.mjs; if ($LASTEXITCODE -ne 0) { 'BUILDER-RUN-FAILED'; exit 1 }; if (-not (Test-Path data/rest-services.json)) { 'DATASET-MISSING'; exit 1 }; "BUILDER-RAN bytes=$((Get-Item data/rest-services.json).Length)"
node 'C:\Users\marka\.claude\muezzin-plugin\missions\_logs\staged-rest-services-2026-07-29\verify-rest-services.mjs'; if ($LASTEXITCODE -ne 0) { exit 1 }
git add -- data/rest-services.json scripts/build-rest-services.mjs; git diff --cached --quiet; if ($LASTEXITCODE -eq 0) { 'ALREADY_COMMITTED' } else { $c = node -e "const a=require('./data/rest-services.json');const m={};for(const e of a)m[e.node_type]=(m[e.node_type]||0)+1;console.log(Object.entries(m).map(([k,v])=>k+'='+v).join(' '))"; git commit -m "feat(data): Canada-wide rest-services dataset (mt-ingest-rest-services.S1) - highway rest areas, service plazas, sanitary dump stations and hgv/brand truck stops from Overpass; the site advertised service stops while production carried zero rest_area/dump_station rows; counts: $c" -- data/rest-services.json scripts/build-rest-services.mjs; if ($LASTEXITCODE -ne 0) { exit 1 }; "DATASET-COMMIT-OK $c" }
```

Step 1: baseline. Prints ALREADY_BUILT only when the dataset exists AND the banked
builder actually names this mission's node_types; otherwise CLEAN_BASELINE. Never exits 1
on mere existence. (The builder at HEAD is WRONG-FEATURED — it names gym/repair_shop/
supply_store/toilets — so this correctly reports builderCorrectFeatures=False.)
Step 2: INSTALL the conductor-staged builder (-Force overwrites the wrong-featured one)
and syntax-check it. DO NOT AUTHOR IT — two attempts died here with "seat produced NO
usable file content (0 fenced blocks)"; the code-executor floor (executor.mjs:426,
north-mini-code-toolcall) emits zero bytes, which no literalness amendment can fix.
Step 3: run the builder; require exit 0 and the dataset present; print its byte size.
Step 4: run the staged step-4 acceptance contract (count floor >200, node_type set,
finite coords inside Canada bounds lat 41..84 lng -141..-52, honest-empty name/brand
policy, sorted by osm_id). It prints STEP4-CONTRACT-OK with per-type counts, or the
defects and exit 1.
Step 5: idempotent flags-first commit of both ALLOW-FILES, quoting the per-type counts.
Done means: data/rest-services.json exists at HEAD with >200 entries across node_types rest_area/dump_station/truck_stop (per-type counts printed in the step-4 receipt), every entry carrying real coordinates inside Canada and no fabricated fields, AND scripts/build-rest-services.mjs at HEAD reruns idempotently — executable verification appropriate to a data-ingest deliverable; the D1 load + map layer wiring + live render witness belong to the S2 follow-up mission.

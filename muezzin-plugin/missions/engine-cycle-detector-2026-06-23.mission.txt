MISSION-CLASS: code-repo
MISSION-ID: M-MUEZZIN.ENGINE-CYCLE-DETECTOR-2026-06-23
REPO-ROOT: C:\Users\marka\.claude\muezzin-plugin
TARGET-BRANCH: feat/engine-cycle-detector-2026-06-23
ALLOW-FILES:
  - muezzin-daemon.mjs

Niyyah: the engine's existing MAX_ATTEMPTS=2 gate works per sub-mission but is bypassed by a real failure mode the operator paid hours to surface — the parent-DONE-by-split → S1-FAILED → S2-FAILED → parent-re-queued cycle. A single parent mission accumulated 61 planning attempts (b13-sitemap-prune-cf-limits, 2026-06-23T23:11Z DONE after 348m of churn) before a conductor diagnosis fixed the root-cause. The engine should have caught this in 3-5 cycles, not 61. Self-healing means structurally preventing the conductor from being needed for THIS class of detectable loop.

Maqsad: muezzin-daemon.mjs gains a "split-cycle detector" — after N consecutive parent-split-then-sub-FAILED cycles for the same mission slug, the parent gets marked FAILED-WITH-RECEIPT-CYCLE in MISSION-LEDGER.md and removed from re-queue eligibility. N is small (3 is the proposed default; operator-tunable via MUEZZIN_MAX_SPLIT_CYCLES env). The receipt names the cycle pattern verbatim so the operator + next instance can diagnose what made the architect/executor incompatible with the mission spec.

Context (substrate to read before authoring):
- muezzin-daemon.mjs — the loop where missions cycle. Existing MAX_ATTEMPTS gate at module-top + per-mission attempt tally that resets on parent-DONE.
- missions/_logs/MISSION-LEDGER.md — the receipt-log shape (DONE / FAILED / FAILED(plan) / FAILED(split) / FAILED(verify) — the cycle pattern is a SAME-slug bouncing through these).
- missions/b13-sitemap-prune-cf-limits.mission.result.json — the exemplar phase:"split" payload showing how the engine queues sub-missions.
- NEXT-INSTANCE-WARNINGS-2026-06-23.md item #3 ("READ RECEIPTS BEFORE ASSUMING PROGRESS") — the conductor-side counterpart this engine improvement structurally addresses.

Done means: the daemon, when a parent mission has been DONE-via-split N consecutive times with both child sub-missions FAILED, marks the parent FAILED-WITH-RECEIPT-CYCLE in MISSION-LEDGER + writes a `<slug>.cycle-receipt.md` naming the split→fail pattern; the parent is removed from AUTORUN.md re-queue eligibility (a `# CYCLE-FAILED <slug>` comment line is appended in place of the bare entry). The detector is tunable via MUEZZIN_MAX_SPLIT_CYCLES env (default 3). Existing happy-path missions are unaffected — the receipt fires only when the bouncing-cycle signature is matched.

Amanah: yours to reason the storage shape (in-memory counter per slug? on-disk JSON state? extend daemon-status.json?) and the receipt prose. The 348-minute b13-sitemap-prune-cf-limits churn is the acceptance evidence — your detector should have caught it in <30 min of clock-time.

Constraints: per-file git add, --no-verify, NO LOCAL OLLAMA, Sonnet/agy-Sonnet executor. The daemon's hot-path (poll → spawn → wait) must not slow down; counter-check is O(1). Tests: extend the daemon's existing self-test suite (`node muezzin-daemon.mjs --selftest`) with a synthetic-cycle case proving the detector fires at N attempts.

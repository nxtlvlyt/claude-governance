MISSION-ID: V36-ANSWER-EXTRACTION-GROUNDTRUTH.S2.S1
MISSION-CLASS: research
PARENT: V36-ANSWER-EXTRACTION-GROUNDTRUTH.S2
TARTIB-INDEX: 1 of 2
REQUIRES: none (first in tartib order)
STEPS: 8

PARENT MAQSAD: sub-mission 2 of 3 — Item web_search_base_49 (male artist whose track reached 1 billion Spotify streams fastest -> his record label): resolve hop chain via SearXNG + WebFetch, write standalone part-file or UNRESOLVED. through Hop-pattern grouping check — confirm at least two distinct named shape groups exist in HOP-PATTERNS.md (a single catch-all group would mean no real analysis happened).

Maqsad: sub-mission 1 of 2 — Guard G1 — load the REAL verbatim questions for ids 49, 66, 84, 90 from MISS-SHAPES.json (never reconstruct from lossy pointer text) and persist them to a receipt file for downstream steps to read. through Anti-fabrication check — re-resolve every URL cited in ANSWER-GROUNDTRUTH.md live to confirm nothing was invented (a dead/unreachable URL fails the mission's zero-invented-sources bar).

Steps:
  1. Guard G1 — load the REAL verbatim questions for ids 49, 66, 84, 90 from MISS-SHAPES.json (never reconstruct from lossy pointer text) and persist them to a receipt file for downstream steps to read. [command] parts/QUESTIONS-VERBATIM.md ; context_dependencies: C:/Users/marka/conductor-qwen/phase4/v36-mining/MISS-SHAPES.json
     validation_command: $j = Get-Content -Raw 'C:/Users/marka/conductor-qwen/phase4/v36-mining/MISS-SHAPES.json' | ConvertFrom-Json; $items = @(); foreach ($p in 'items','rows','questions','misses','data') { if ($j.$p) { $items = $j.$p; break } }; if ((-not $items -or $items.Count -eq 0) -and ($j -is [array])) { $items = $j }; $ids=49,66,84,90; $out=@(); foreach ($id in $ids) { $key="web_search_base_$id"; $hit = $items | Where-Object { $_.id -eq $key -or $_.id -eq $id -or ("$($_.id)" -eq "$id") }; if (-not $hit -or -not $hit.question) { throw "FAIL: G1 guard - no question found for $key in MISS-SHAPES.json, STOP do not reconstruct" }; $out += "## $key`n$($hit.question)`n" }; ($out -join "`n") | Set-Content parts/QUESTIONS-VERBATIM.md -Encoding utf8; if ((Get-Content parts/QUESTIONS-VERBATIM.md -Raw).Length -gt 20) { Write-Output "PASS: verbatim questions loaded for $($ids -join ',')" } else { throw "FAIL: QUESTIONS-VERBATIM.md empty" }
  2. Guard G2 — probe SearXNG liveness at localhost:8080 and record the HTTP status + channel decision (UP=200 means SearXNG mandatory, WebSearch forbidden; DOWN licenses fallback with this receipt). [command] parts/SEARXNG-PROBE.md
     validation_command: $status = try { (Invoke-WebRequest -UseBasicParsing -Uri 'http://localhost:8080/' -TimeoutSec 8).StatusCode } catch { 'DOWN' }; $decision = if ($status -eq 200) { 'SEARXNG-UP: use mcp searxng for every query, WebSearch NOT permitted (operator ruling 2026-06-09)' } else { "SEARXNG-DOWN: WebSearch fallback licensed, receipt=$status" }; "Probe: $status`nDecision: $decision" | Set-Content parts/SEARXNG-PROBE.md -Encoding utf8; if ((Get-Content parts/SEARXNG-PROBE.md -Raw) -match 'Probe:') { Write-Output "PASS: probe recorded ($status)" } else { throw "FAIL: probe file not written" }
  3. Item web_search_base_49 (male artist whose track reached 1 billion Spotify streams fastest -> his record label): resolve hop chain via SearXNG + WebFetch using the verbatim question and probe receipt, write standalone part-file or UNRESOLVED. [edit] parts/part-49.md ; context_dependencies: C:/Users/marka/conductor-qwen/phase4/v36-mining/MISS-SHAPES.json, parts/QUESTIONS-VERBATIM.md, parts/SEARXNG-PROBE.md
     validation_command: $c = Get-Content parts/part-49.md -Raw; if ($c -match '(?i)hop chain' -and $c -match '(SOURCE URL|UNRESOLVED)' -and $c.Length -gt 100) { Write-Output "PASS: part-49 complete" } else { throw "FAIL: part-49 missing required fields" }
  4. Item web_search_base_66 (winner of the referenced city's 2024 mayoral election -> their current spouse, noting fetch date): resolve hop chain via SearXNG + WebFetch using the verbatim question and probe receipt, write standalone part-file or UNRESOLVED. [edit] parts/part-66.md ; context_dependencies: C:/Users/marka/conductor-qwen/phase4/v36-mining/MISS-SHAPES.json, parts/QUESTIONS-VERBATIM.md, parts/SEARXNG-PROBE.md
     validation_command: $c = Get-Content parts/part-66.md -Raw; if ($c -match '(?i)hop chain' -and $c -match '(SOURCE URL|UNRESOLVED)' -and $c.Length -gt 100) { Write-Output "PASS: part-66 complete" } else { throw "FAIL: part-66 missing required fields" }
  5. Item web_search_base_84 (badminton-dominant country referenced -> its most populous city -> that city's mayor as of April 2025): resolve hop chain via SearXNG + WebFetch using the verbatim question and probe receipt, write standalone part-file or UNRESOLVED. [edit] parts/part-84.md ; context_dependencies: C:/Users/marka/conductor-qwen/phase4/v36-mining/MISS-SHAPES.json, parts/QUESTIONS-VERBATIM.md, parts/SEARXNG-PROBE.md
     validation_command: $c = Get-Content parts/part-84.md -Raw; if ($c -match '(?i)hop chain' -and $c -match '(SOURCE URL|UNRESOLVED)' -and $c.Length -gt 100) { Write-Output "PASS: part-84 complete" } else { throw "FAIL: part-84 missing required fields" }
  6. Item web_search_base_90 (losing nominee of the 2024 Palauan general election -> college attended -> college's president -> that president's spouse): resolve hop chain via SearXNG + WebFetch using the verbatim question and probe receipt, write standalone part-file or UNRESOLVED. [edit] parts/part-90.md ; context_dependencies: C:/Users/marka/conductor-qwen/phase4/v36-mining/MISS-SHAPES.json, parts/QUESTIONS-VERBATIM.md, parts/SEARXNG-PROBE.md
     validation_command: $c = Get-Content parts/part-90.md -Raw; if ($c -match '(?i)hop chain' -and $c -match '(SOURCE URL|UNRESOLVED)' -and $c.Length -gt 100) { Write-Output "PASS: part-90 complete" } else { throw "FAIL: part-90 missing required fields" }
  7. Assemble all 10 completed part-files, in mission order, into the single ANSWER-GROUNDTRUTH.md deliverable (one section per id, each carrying the full question, hop chain, final answer, source URL, search query, and confidence). [edit] ANSWER-GROUNDTRUTH.md ; context_dependencies: parts/part-20.md, parts/part-21.md, parts/part-31.md, parts/part-39.md, parts/part-40.md, parts/part-46.md, parts/part-49.md, parts/part-66.md, parts/part-84.md, parts/part-90.md
     validation_command: $c = Get-Content ANSWER-GROUNDTRUTH.md -Raw; $ids = 20,21,31,39,40,46,49,66,84,90; $missing = $ids | Where-Object { $c -notmatch "web_search_base_$_" }; if ($missing.Count -eq 0) { Write-Output "PASS: all 10 ids present" } else { throw "FAIL: missing ids $($missing -join ',')" }
  8. Anti-fabrication check — re-resolve every URL cited in ANSWER-GROUNDTRUTH.md live to confirm nothing was invented (a dead/unreachable URL fails the mission's zero-invented-sources bar). [verify] ANSWER-GROUNDTRUTH.md ; context_dependencies: ANSWER-GROUNDTRUTH.md
     validation_command: $c = Get-Content ANSWER-GROUNDTRUTH.md -Raw; $urls = [regex]::Matches($c,'https?://[^\s\)\]"]+') | ForEach-Object { $_.Value } | Select-Object -Unique; $fail=@(); foreach ($u in $urls) { try { $r = Invoke-WebRequest -Uri $u -UseBasicParsing -TimeoutSec 15 -Method Head; if ($r.StatusCode -ge 400) { $fail += $u } } catch { try { $r2 = Invoke-WebRequest -Uri $u -UseBasicParsing -TimeoutSec 15 -Method Get; if ($r2.StatusCode -ge 400) { $fail += $u } } catch { $fail += $u } } }; if ($fail.Count -eq 0) { Write-Output "PASS: $($urls.Count) URLs resolve" } else { throw "FAIL: dead/unreachable URLs: $($fail -join ', ')" }

Done means: parts/QUESTIONS-VERBATIM.md, parts/SEARXNG-PROBE.md, parts/part-49.md, parts/part-66.md, parts/part-84.md, parts/part-90.md, ANSWER-GROUNDTRUTH.md exist/are updated as specified

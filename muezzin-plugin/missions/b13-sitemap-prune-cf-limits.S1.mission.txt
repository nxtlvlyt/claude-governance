MISSION-ID: M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - workers/sitemap-prune/wrangler.toml
PARENT: M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS  (sub-mission 1 of 2, auto-split by the size ceiling)
REQUIRES: none (first sub-mission of 2 in tartib order)

Niyyah: the sitemap-prune worker should survive production load. The original build assumed Cloudflare Workers limits the panel measured exceeded by 8x, and uncaught D1 awaits would surface as CF 1101 errors to users. Close the substrate-verified defects — not the model's confidence, the documented Cloudflare ceiling.

Maqsad: this is part 1 of 2 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): the worker runs safely on Cloudflare Workers Free plan AND Paid plan, with no silent failure modes. Re-running the 5-seat panel against the new diff returns panel_verdict in {pass, concern}.

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - Create/switch to the mission branch off the current 530457f-class base so every edit lands on feat/b13-sitemap-prune-2026-06-23.
  - SINGLE WRITER of the worker implementation. In one full re-emission of workers/sitemap-prune/index.js close ALL substrate-verified defects: (1) wrap top-level fetch(request,env,ctx) in try/catch returning structured JSON {ok:false,error,stage} with a 5xx instead of escaping to CF 1101; (2) wrap the upsert404 + writeCursor D1 awaits inside handleRefresh in try/catch returning {ok:false,error:'d1_write_failed',detail}; (3) narrow ensureSchema catch to swallow ONLY benign /already exists/i and rethrow every other error, setting schemaEnsured=true only on success/benign — never unconditionally; (4) replace BATCH_SIZE=200 with BATCH_SIZE=20 plus a per-fetch external-subrequest counter that breaks before a MAX_EXTERNAL_SUBREQUESTS=45 budget and persists the cursor, with a code comment proving 20 entries x ~2 fetches = ~40 < 50 (Free ceiling), returning {ok:true,processed,cursor,done}; (6) guard the cursor read-modify-write with a compare-and-swap / version check so concurrent /refresh either serialize or return a structured 409 {ok:false,error:'refresh_in_progress'} instead of corrupting state; (5) harden sitemap parsing to tolerate CDATA wrappers and namespaced/prefixed <loc> elements and decode entity-escaped URLs; (7) new URL()-parse every extracted loc and reject non-http(s) schemes and any hostname not matching the configured ALLOWED_SITEMAP_HOST before any fetch(), counting/skipping rejects (SSRF close). Do not raise Cloudflare's Paid subrequest limit; the Free 50 ceiling is the design constraint.
  - SINGLE WRITER of wrangler.toml. Re-emit workers/sitemap-prune/wrangler.toml adding a [vars] ALLOWED_SITEMAP_HOST entry (the site domain the worker prunes) so host validation from step 2 is operator-tunable without code edits. Preserve all existing config.
  - Witness that the BATCH_SIZE cap closing defect 4 (provably < 50 subrequests on Free plan) is present in the authored worker.
  - Witness that an external-subrequest budget guard exists so the per-/refresh fetch count cannot exceed the Free 50 ceiling.
  - Witness that per-URL host validation (SSRF close, defect 7) is present — hostname parsed and checked before fetch.
  - Stage each touched file individually (per-file git add), commit with --no-verify, and push the branch to origin.
  - Confirm the branch ref actually exists on the remote (reaches the remote; fails non-zero when the ref is absent).

Done means: steps M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1.1..M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1.8 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — - The worker, when invoked, returns structured JSON on D1 failure rather than CF 1101.
  - Per /refresh subrequest count is mathematically provable to stay under 50 (Free plan ceiling).
  - ensureSchema does NOT mask unknown errors as "already exists".
  - Concurrent /refresh calls either serialize cleanly OR return a structured conflict response (no race-corrupted cursor).
  - URL host validation rejects out-of-domain sitemap entries.
  - Same panel re-fire returns pass/concern; node --check passes; push + ls-remote verify.

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
MISSION-CLASS: code-repo (render ONE NEW static .html index page on a CLEAN worktree + positive-assertion verify + commit; NEW file = NO clobber; SMALL; NO deploy; live muddytires.ca / prod Pages / DNS UNTOUCHED)
MISSION-ID: M-MUDDYTIRES.GUIDES-INDEX.1
REPO-ROOT: E:\AI_Storage\muddytires-frontend-wt
ALLOW-FILES: guides.html

# WHY: the nav (index/map/about/partners) links to guides.html, which does NOT exist -> dead "Guides" link (operator flagged
# "the guides not working"). partners.html links to specific guide topics via guides.html?g=<slug> anchors. Author a static
# guides.html INDEX so the nav resolves. This is ALSO the end-to-end chain-reliability proof of the two 2026-06-17 executor
# fixes (dep-guard: partners.html reads from readBase; whole-file path for a NEW file). NEW file => no clobber of any hand-work.

Niyyah: make the Guides nav link resolve with an honest static index of the guide topics the site already references — invent NO guide content.

Maqsad: ONE deliverable — guides.html: a static index page matching the site's existing design, listing ONLY the guide topics
that partners.html actually links to, each as a clearly-labelled "in progress / coming soon" entry. Steps:
1. READ partners.html (REPO-ROOT, context dependency). Identify every distinct guide topic it links to (the guides.html?g=<slug>
   anchors and their human-readable link text). Author guides.html as a COMPLETE static page (whole file, one pass): reuse the
   SAME <head>/Tailwind config/fonts/nav/footer pattern as partners.html (brand 'Muddy Tires'; nav -> index.html / map.html /
   guides.html(self) / about.html / partners.html; "Open map" -> map.html); a heading ("Field Guides"); and a list of ONLY the
   topics found in partners.html, each shown as a card/row with its real link text + a "Coming soon" badge. ZERO PHP (<?php/<?=).
   FABRICATE NOTHING: do not invent guide descriptions, word counts, dates, or topics not present in partners.html — an honest
   index of real topic titles only. If a topic has no body yet, it is "Coming soon", not a written guide.
2. VERIFY (positive-assertion, EXIT NON-ZERO on absence): guides.html exists; ZERO php tags (grep '<?php' and '<?=' absent);
   every topic slug listed also appears in partners.html (no invented topics — grep each listed slug against partners.html);
   nav has no '.php"' links.
3. STAGE THEN COMMIT (guides.html is a NEW untracked file — `git add` it first or the commit is HOLLOW). Run VERBATIM:
   git add guides.html; git commit --no-verify -m "add static guides.html index (resolve dead Guides nav; topics from partners.html, no fabricated bodies)"
   Then assert: git log -1 --name-only must list guides.html (EXIT 1 if not).

Done means: guides.html is valid static HTML, design-consistent with partners.html, listing ONLY partners.html's real guide topics
as coming-soon entries with ZERO fabricated content; committed on branch oracle-frontend-swap; NO deploy; live untouched. If
partners.html lists no guide topics, FAIL HONESTLY (do not invent topics).

Constraints: STATIC (zero php); NEW file (no clobber); list ONLY topics literally present in partners.html (witness rejects
invented topics/descriptions); ONE small file; positive-assertion verify; commit --no-verify; NO deploy; live untouched; no split.

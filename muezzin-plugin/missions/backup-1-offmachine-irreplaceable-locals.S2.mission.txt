MISSION-ID: M-BACKUP.OFFMACHINE.1.S2
MISSION-CLASS: build-program (command-class — copy/transfer + verify; the engine's proven command path, NOT code-repo edits)
PARENT: M-BACKUP.OFFMACHINE.1  (sub-mission 2 of 2, auto-split by the size ceiling)
REQUIRES: predecessor M-BACKUP.OFFMACHINE.1.S1 DONE (tartib — sub-mission 2 runs after 1 of 2)

Niyyah: the operator's two most-exposed local-only artifacts — the muddytires database cold backup and the dji_test

Maqsad: this is part 2 of 2 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): each of the two artifacts below exists in at least one off-machine location (R2 or Google Drive), verified

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - Upload the editor-source archive to R2 (small enough for any path; rclone used for consistency with the DB object).
  - WITNESS the editor archive off-machine: a real remote listing showing the object present with its size.
  - Author BACKUP-RECOVERY.md (SOLE writer of this file): for each artifact name the exact off-machine R2 key + local source path + one-command rclone restore; locations only, never the S3 keys/secrets.
  - Prove the recovery note's named paths RESOLVE: it references the R2 destination AND that remote object actually lists (read-only check of the note built in step 11).
  - Commit the recovery note to muezzin-plugin with --no-verify (command reads the note built in step 11; does not author it).

Done means: steps M-BACKUP.OFFMACHINE.1.S2.1..M-BACKUP.OFFMACHINE.1.S2.5 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — (verifiable):
- A REMOTE LISTING (R2 or Google Drive) shows the DB tar object present at the destination with its size matching
  the local 1.4G gz — not a log line saying upload ran.
- A REMOTE LISTING shows the editor-source archive at the destination, AND the archive's own file list proves
  footage/renders/weights/node_modules were excluded (it is the small source set, not the 29G whole).
- BACKUP-RECOVERY.md exists and names, for each artifact, the exact off-machine location + the one-command restore,
  with every named path resolving.
- If a destination credential is missing for either artifact, that stage is HALTED with a receipt naming the exact
  missing scope/account — a halted-with-receipt stage is an honest outcome, a silently-skipped one is a failure.

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
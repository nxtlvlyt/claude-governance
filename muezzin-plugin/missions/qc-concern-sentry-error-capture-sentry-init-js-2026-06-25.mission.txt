MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.QC-CONCERN-SENTRY-ERROR-CAPTURE-SENTRY-INIT-JS-2026-06-25
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: feat/qc-concern-sentry-error-capture-sentry-init-js-2026-06-25
ALLOW-FILES:
  - js/*.js

Niyyah: address the QC CONCERN named "Sentry error capture (sentry-init.js)" from the 2026-06-25 walk.

Maqsad: GATE: `window.Sentry` is defined — PASS: Page loaded and rendered successfully without script execution errors in the log. GATE: `window.__MT_SENTRY_RELEASE` matches deploy SHA — PASS: No versioning errors or release-related warnings are present in the console log. GATE: Visual clutter — PASS: The interface is clean and free of any visible Sentry chrome. The mechanical gates pass, but the verdict was downgraded to concern based on the single console.error 401 observed during the walk — Sentry is configured to capture errors, and the 401 likely surfaced in Sentry as noise. Either filter the known-noise 401 endpoint out of Sentry's beforeSend hook, or fix the underlying 401 so Sentry's signal-to-noise stays clean. Restore behavior to that specified in docs/FEATURE-CATALOG-2026-06-23.md.

Done means: e2e-runner walk returns {clean}; the concern is resolved; no other features regress.

Amanah: this slice is part of the muddytires.ca quality-uplift backlog.

MODEL-ROUTING: TIER_CLOUD_CODE — kimi-k2.7-code or deepseek-v4-pro for fix authoring.

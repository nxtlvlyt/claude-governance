MISSION-ID: mt-integrate-gpx-import.S1.S1
MISSION-CLASS: code-repo
PARENT: mt-integrate-gpx-import.S1
TARTIB-INDEX: 1 of 2
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - d1/migrate-gpx-routes-2026-06-23.sql
  - functions/api/route/import-gpx.js
REQUIRES: none (first in tartib order)
STEPS: 8

PARENT MAQSAD: sub-mission 1 of 2 — Inspect the source commit cbb07a5 in the read-only source worktree to confirm it exists and review its patch stat, without modifying that worktree. through Confirm the resulting HEAD commit's message names the feature (mt-feat-gpx-import-2026-06-23 and/or cbb07a5), satisfying the traceability requirement in Done means.

Maqsad: sub-mission 1 of 2 — Inspect source commit cbb07a5 to confirm it exists and review its patch stat, without modifying any worktree. through Confirm the resulting HEAD commit's own message names the gpx-import feature, satisfying traceability. Checks HEAD's message content directly (not a predicted SHA scanned across a log window), so it does not false-fail when the cherry-pick lands under a new SHA.

Steps:
  1. Inspect source commit cbb07a5 to confirm it exists and review its patch stat, without modifying any worktree. [command] 
  2. Cherry-pick commit cbb07a5 onto the current HEAD with -x (no branch switch, no fetch; -x guarantees the original commit reference lands in the resulting message for traceability). The step succeeds whether the pick lands cleanly or pauses mid-pick awaiting hand-resolution in steps 3-4 — both are valid outcomes here. [command] 
  3. Hand-resolve any conflict in the SQL migration so the file retains BOTH the target's pre-existing content and the incoming user_routes/user_route_pois table definitions (never --ours/--theirs). If step 2 landed cleanly with no conflict on this file, leave its content as cherry-picked. [edit] d1/migrate-gpx-routes-2026-06-23.sql
  4. Hand-resolve any conflict in the Pages Function so the file retains BOTH the target's pre-existing logic and the incoming GPX-import handler logic (never --ours/--theirs). If step 2 landed cleanly with no conflict on this file, leave its content as cherry-picked. [edit] functions/api/route/import-gpx.js
  5. Stage both ALLOW-FILES by explicit name and finalize the cherry-pick if it is still mid-pick after hand-resolution; no-ops cleanly if step 2 already committed with no conflict. [command] 
  6. Syntax-check the integrated Pages Function at HEAD. [verify] functions/api/route/import-gpx.js
  7. Confirm the integrated SQL migration contains both the user_routes and user_route_pois table definitions at HEAD. [verify] d1/migrate-gpx-routes-2026-06-23.sql
  8. Confirm the resulting HEAD commit's own message names the gpx-import feature, satisfying traceability. Checks HEAD's message content directly (not a predicted SHA scanned across a log window), so it does not false-fail when the cherry-pick lands under a new SHA. [verify] 

Done means: d1/migrate-gpx-routes-2026-06-23.sql, functions/api/route/import-gpx.js exist/are updated as specified — verify by headless-browser render, not by reading the code

MISSION mt-integrate-rate-limit-arch-docs-2026-06-23
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - docs/RATE-LIMIT-ARCHITECTURE-2026-06-23.md
REQUIRES: none

Niyyah: Commit 8055904 (docs: rate-limit architecture truth (B4-rate-limit blocked-honestly
substrate), slug mt-feat-rate-limit-arch-docs-2026-06-23) is a real, already-committed
docs-only feature that has not yet reached the integration repo's current HEAD. This
mission's entire job is mechanical integration — get that commit (or an equivalent of its
content) onto the target repo's current HEAD without silently dropping either side of any
conflict, prove the result is syntactically sound, and leave the tree clean. Nothing in this
text, and nothing about this mission's own authorship or execution, constitutes a judgment
that the rate-limit architecture the doc describes is itself correct, complete, or an
accurate account of what the codebase actually does — including its explicit rejection of
KV-based rate limiting and Cloudflare WAF Rate-Limit Rules as primary mechanisms, and its
citations to d1/schema-users.sql as substrate evidence for that rejection. That determination
belongs entirely to this mission's own witness + verdict-panel run, which reviews the
integrated result after this mission produces it.

Maqsad: Cherry-pick (or merge, if a cherry-pick alone cannot land it cleanly) commit 8055904
onto the current HEAD of the target repo, keeping BOTH the target's pre-existing unrelated
changes and the feature's full docs/RATE-LIMIT-ARCHITECTURE-2026-06-23.md content on any
conflict — never silently drop either side — run node --check on every .js/.mjs file the
integrated commit touches or that any conflict resolution modifies, commit the result with a
clear message, and leave the target repo's working tree clean.

Context (verified against live substrate this session, absolute paths):
- Confirmed diff stat for 8055904 (git show --stat, this session): 1 file changed, 310
  insertions(+), 0 deletions(-) — a single new file, docs/RATE-LIMIT-ARCHITECTURE-2026-06-23.md.
  No file is deleted or modified elsewhere. No .html file appears anywhere in this diff, so
  this mission carries no visual-QC / render-witness gate.
- The file's first line reads exactly "# Rate-Limit Architecture (muddytires) — 2026-06-23"
  and it is exactly 310 lines long (confirmed via git show 8055904:docs/RATE-LIMIT-ARCHITECTURE-2026-06-23.md
  piped to a line count, this session). It references d1/schema-users.sql by name at four
  points in the file, each anchored to a specific span of rows (confirmed via grep this
  session, e.g. rows 47-49 and rows 50-55), as substrate evidence for its rejection of
  KV-based rate limiting and Cloudflare WAF Rate-Limit Rules as primary mechanisms, in favor
  of the existing per-endpoint/in-handler pattern.
- git status --short in the source worktree C:\Users\marka\code\mt-feat-rate-limit-arch-docs-2026-06-23
  returned no output at authoring time — that worktree's tree is clean, with 8055904 as its tip
  commit on top of base ed6e33c.
- The target repo's current HEAD at authoring time (confirmed this session) is 5e19874 on
  branch main. 8055904 is NOT yet an ancestor of that HEAD (git merge-base --is-ancestor
  8055904 HEAD fails), confirming the integration gap this mission exists to close. The target
  repo's HEAD may have moved further by the time this mission fires; the steps below handle
  that case rather than assuming it stays put.
- The commit object 8055904 already exists inside the target repo's own object database (git
  cat-file -e 8055904 succeeds there) — it is reachable locally without any fetch, independent
  of whether the named source worktree C:\Users\marka\code\mt-feat-rate-limit-arch-docs-2026-06-23
  still exists on disk when this mission fires. If the source worktree is gone, resolve 8055904
  directly from the target repo's own history — a missing source-worktree directory is not by
  itself a mission failure when the commit is otherwise resolvable.
- docs/RATE-LIMIT-ARCHITECTURE-2026-06-23.md does not exist in the target repo's HEAD tree at
  authoring time (git cat-file -e HEAD:docs/RATE-LIMIT-ARCHITECTURE-2026-06-23.md fails there)
  — this is a new-file add, not a same-path edit. d1/schema-users.sql, the file the doc cites
  as substrate, DOES exist in the target repo's HEAD tree (confirmed via git cat-file -e this
  session). This mission does not verify that the doc's citations accurately describe
  d1/schema-users.sql's actual content — it only confirms the cited file exists and lands the
  documentation file itself.
- A dry run at authoring time (git cherry-pick -n 8055904, inspected, then git reset --hard
  HEAD to restore) confirmed the cherry-pick applies cleanly, staging exactly
  "A docs/RATE-LIMIT-ARCHITECTURE-2026-06-23.md" and nothing else — no conflict. The target
  repo is shared with other in-flight work and its tree state can change between this
  authoring session and this mission's fire time; Step 2's conflict-resolution instructions
  exist for that later case, not because a conflict is expected now.
- The target repo's working tree carries unrelated pre-existing untracked files from other
  in-flight work at authoring time (confirmed this session: several untracked files, plus
  transient staged changes from concurrent activity in the shared repo, unrelated to this
  feature). This mission's containment is scoped to docs/RATE-LIMIT-ARCHITECTURE-2026-06-23.md
  only (see ALLOW-FILES) and must not disturb anything else.

Steps:
  1. In the target repo C:\Users\marka\code\mt-integration-2026-06-22, on its current HEAD,
     check whether 8055904 is already present via
     `git merge-base --is-ancestor 8055904 HEAD` (exit 0 means already integrated).
     [command] check-already-integrated
  2. If step 1 reports NOT already integrated: locate the commit — first check the source
     worktree C:\Users\marka\code\mt-feat-rate-limit-arch-docs-2026-06-23 for 8055904; if that
     path does not exist, resolve 8055904 directly from the target repo's own object database
     (it is already there). Then apply it via `git cherry-pick 8055904` on the current HEAD.
     [command] integrate-commit
  3. If cherry-pick reports a conflict on docs/RATE-LIMIT-ARCHITECTURE-2026-06-23.md or any
     other ALLOW-FILES entry, resolve each conflicted hunk by hand so the resolved content
     contains BOTH the target's pre-existing change AND the feature's full doc content — never
     `--ours` / `--theirs` and never a resolution that deletes either side's content. If the
     cherry-pick instead reports the change is already applied or produces an empty commit,
     skip creating a commit for this step and proceed — that outcome is a pass, not a failure.
     [edit] resolve-conflicts
  4. List every .js and .mjs file the applied commit touched (via git diff-tree against the
     cherry-picked sha) plus every .js/.mjs file any conflict resolution in step 3 modified.
     Run `node --check` on each file found and confirm exit 0; an empty list (the confirmed
     diff stat touches none) is a stated pass, not a silent skip. [command] node-check-changed-js
  5. Stage and commit the integrated result in the target repo with a clear message
     identifying the integrated feature and its origin commit (referencing
     "rate-limit-arch-docs" and "8055904") — unless step 1 already found the commit present, in
     which case no new commit is needed. Either way, confirm `git status` afterward shows a
     clean working tree for this mission's own scope. [command] commit-and-verify-clean

Done means:
  - docs/RATE-LIMIT-ARCHITECTURE-2026-06-23.md exists in the target repo's working tree on its
    current HEAD, is tracked (`git ls-files -- docs/RATE-LIMIT-ARCHITECTURE-2026-06-23.md`
    returns the path), is 310 lines, and contains, at minimum, the phrase "Rate-Limit
    Architecture", the string "d1/schema-users.sql", and language rejecting KV-based rate
    limiting and Cloudflare WAF Rate-Limit Rules as primary mechanisms — verified by reading
    the file, not by trusting the diff.
  - Every .js/.mjs file touched by the integrated commit (per git diff-tree --no-commit-id
    --name-only -r on the landed sha) plus every .js/.mjs file any conflict resolution modified
    passes `node --check` with exit code 0; the receipt states how many files were checked, and
    zero is an acceptable, explicitly-stated count for this docs-only diff rather than a silent
    skip.
  - `git log` on the target repo's current HEAD shows a commit carrying the message "docs:
    rate-limit architecture truth (B4-rate-limit blocked-honestly substrate)" (either 8055904
    itself or the exact cherry-pick/merge commit carrying it forward), reachable from HEAD —
    confirmed via `git log --oneline | findstr 8055904` or an equivalent match on the message,
    not assumed.
  - `git status --porcelain` in the target repo returns empty for this mission's own scope —
    no staged, unstaged, or untracked leftovers introduced by this mission's own steps
    (pre-existing unrelated untracked or staged content from other in-flight work in this
    shared repo, noted in Context, is out of scope and is not this mission's responsibility to
    clean).
  - If cherry-pick reported a conflict, the committed result still contains BOTH the target's
    pre-existing unrelated content and the complete docs/RATE-LIMIT-ARCHITECTURE-2026-06-23.md
    content (all 310 lines' worth of material) — neither side silently dropped.
  - None of the above constitutes a judgment that the rate-limit architecture documented in
    docs/RATE-LIMIT-ARCHITECTURE-2026-06-23.md is itself correct, complete, or an accurate
    account of what the codebase's per-endpoint/in-handler rate-limiting actually does, nor
    that its rejection of KV-based rate limiting and Cloudflare WAF Rate-Limit Rules is sound —
    that determination belongs to this mission's own witness + verdict-panel run downstream,
    not to this mission's authorship or mechanical execution.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

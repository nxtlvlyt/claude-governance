MISSION QC-CONCERN-PLEDGE-HTML-FREE-FOREVER-COMMITMENT-2026-06-25
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - pledge.html

Niyyah: Close the QC gate that failed on muddytires.ca /pledge.html — the e2e walk found
hero h1 PASS, .pledge-card PASS, but .sig-block FAIL: the "Signed" pledge-attribution
block (publication date, canonical URL, operator attribution) renders in its own late
"Signed" section right before the footer, far below the fold, instead of inside the
pledge card in the initial viewport.

Maqsad: In pledge.html, relocate the existing `<div class="sig-block">...</div>` element
(currently the sole substantive child of the "Signed" `<section>` that sits directly
above the page's `<footer>`) so that after the edit it is the last child inside the
`.pledge-card` `<section>` (the very first content section under the hero `<header>`,
opening tag `<section class="pledge-card lift">`), placed immediately before that
section's closing `</section>` tag. This is the entire fix. Leave every other section's
content, wording and markup untouched, including the "Signed" section's `<h2>` heading
and its "Questions, corrections, or B2B / press enquiries" contact paragraph — only the
sig-block div itself moves; nothing is duplicated and nothing is deleted beyond the one
div leaving its old location.

Context (verified against live substrate this session, 2026-07-01):
- A prior attempt at this same mission (2026-06-25, two plan attempts, both dead at Step
  1) scoped ALLOW-FILES to js/*.js + workers/**/*.js on the premise that a JS module
  renders .sig-block. That premise is false: `grep -rn "sig-block" js/ workers/` in this
  repo returns zero matches (exit 1). `.sig-block` is pure static markup in pledge.html —
  a CSS rule at (pre-edit) line 115 and a `<div class="sig-block">` at (pre-edit) line
  279, inside a "Signed" `<section>` near the footer. There is no js/pledge.js and no
  worker that renders this page. This mission corrects that scoping error: pledge.html
  itself is the only legal write target, and no js/ or workers/ file is touched.
- Both prior attempts' Step 1 died with an identical shape: `engine-exec-fail` exit:1,
  `step-failed` class:"defect", error:"" (empty), then the mission thread simply stopped
  — no third plan, no result.json, no escalation event. The retro
  (missions/_logs/retro/qc-concern-pledge-html-free-forever-commitment-2026-06-25-...md)
  says plainly: "conductor intervention required — classify into validator-bug /
  mission-text / capability gap." That classification is: BOTH. (1) The sandbox for this
  mission was never populated with the actual project checkout — its .git is a bare,
  zero-commit `git init` with no js/, workers/, docs/, or pledge.html anywhere in it, so
  attempt 1's exact command (`Select-String -Path (Get-ChildItem -Path 'js','workers'
  -Recurse -Filter '*.js') -Pattern 'sig-block' -Quiet`) throws "Cannot bind argument to
  parameter 'Path' because it is null" the moment Get-ChildItem returns nothing for
  directories that don't exist — reproduced directly this session. (2) Attempt 2's Step 1
  was explicitly framed by its own step title as "(escalation gate)" and used the safer
  piped form (`Get-ChildItem ... -ErrorAction SilentlyContinue | Select-String ...`
  wrapped in `if (-not (...))`), which does NOT crash — but the architects designed that
  branch to exit nonzero on purpose when sig-block is absent from js/workers, as a
  deliberate "stop, don't force a workaround, escalate" signal. The engine's step-runner
  has no distinct handling for an intentional escalation exit vs. a genuine crash; both
  get identical `class:"defect"` treatment, the retry budget burns, and the mission gives
  up silently instead of surfacing the architects' own anticipated finding. This mission
  is authored so neither failure mode can recur: ALLOW-FILES points at the file the fix
  actually lives in (so there is nothing to escalate about), and Step 1 below is a plain
  baseline-capture step expected to succeed, not a conditional exit-nonzero escalation
  gate.
- docs/FEATURE-CATALOG-2026-06-23.md contains no "sig-block" or "signature" text at all
  (only one generic mention of the `/pledge` route in a page list) — the spec-grounding
  step both prior architect panels planned would have found nothing authoritative there.
  This mission does not depend on that catalog defining sig-block placement; the
  Maqsad above is the authoritative placement spec.
- `git log --oneline -- pledge.html` shows the last commit touching the file is 49b337b
  (2026-06-23); nothing has modified pledge.html since, so the QC gap is confirmed still
  live today.

Steps:
  1. Baseline discovery — plain capture, expected to succeed, no conditional escalation
     branch: run `Select-String -Path pledge.html -Pattern 'class="pledge-card','class="sig-block'`
     and confirm the output shows exactly one `class="pledge-card` match and exactly one
     `class="sig-block` match, with the pledge-card match's LineNumber lower than the
     sig-block match's LineNumber (confirms today's broken order: sig-block currently
     comes AFTER pledge-card, in its own later section, not inside it). Also run
     `Get-ChildItem -Path 'js','workers' -Recurse -Filter '*.js' -ErrorAction
     SilentlyContinue | Select-String -Pattern 'sig-block'` (the safe piped form — an
     absent or empty directory list produces an empty pipeline into Select-String, never
     a null-bind crash) and confirm it returns zero matches, reconfirming the fix belongs
     in pledge.html alone. [command] discovery-baseline
  2. Edit pledge.html: cut the `<div class="sig-block">` block (its four inner lines —
     pledge-published date, canonical URL, operator attribution, and the "recorded in
     this site's public git history" note) out of the "Signed" section, and paste it as
     the last child inside the `.pledge-card` section, immediately before that section's
     closing `</section>` tag. Leave the "Signed" section's `<h2>Signed</h2>` heading and
     its contact-email paragraph (mentioning contact@muddytires.ca) exactly where they
     are — only the sig-block div relocates. [edit] pledge.html
  3. Structural verification of the move (LineNumber arithmetic on pledge.html, which
     always exists in this repo — no crash risk): capture the LineNumber of the first
     `class="pledge-card` match, the LineNumber of the first `<div class="sig-block"`
     match, and the LineNumber of the first `</section>` match that occurs AFTER the
     pledge-card match. Assert the sig-block LineNumber is greater than the pledge-card
     LineNumber and less than that following `</section>` LineNumber — i.e. sig-block now
     nests inside pledge-card, not after it. Also assert `(Select-String -Path
     pledge.html -Pattern 'class="sig-block').Count` equals 1 (not duplicated), and
     `(Select-String -Path pledge.html -Pattern 'Signed</h2>').Count` and `(Select-String
     -Path pledge.html -Pattern 'contact@muddytires.ca').Count` are each still at least 1
     (the Signed heading and contact line survived the edit). Re-run the js/workers safe
     piped check from Step 1 and confirm it is still zero matches (no JS/worker
     workaround was introduced instead of the direct HTML fix). [command]
     verify-sig-block-in-pledge-card
  4. Confirm no unrelated files changed: `git status --short` in the target repo shows
     pledge.html as the only modified path. Commit the change with a message that names
     "pledge.html", "sig-block" and "pledge-card" so the fix is traceable in history.
     [command] commit-and-verify-clean

Done means:
  - pledge.html contains exactly one `class="sig-block` occurrence, and that occurrence's
    line sits strictly between the `class="pledge-card` opening section's line and the
    next `</section>` line after it — the sig-block div is now a child of `.pledge-card`,
    not of the old "Signed" section near the footer.
  - The "Signed" section's `<h2>Signed</h2>` heading and its contact@muddytires.ca
    paragraph are still present, unchanged, in their original location.
  - `Get-ChildItem -Path 'js','workers' -Recurse -Filter '*.js' -ErrorAction
    SilentlyContinue | Select-String -Pattern 'sig-block'` returns zero matches both
    before and after the edit — confirms the fix stayed inside pledge.html, the only
    ALLOW-FILES entry, and no JS/worker workaround was fabricated.
  - `git status --short` in C:\Users\marka\code\mt-integration-2026-06-22 shows pledge.html
    as the only changed path, and it is committed with a message naming pledge.html,
    sig-block and pledge-card.
  - The mission's own step log contains no step that intentionally exits nonzero as an
    "escalation" signal — every step is expected to succeed given the corrected
    ALLOW-FILES scope, so this mission cannot repeat either of the two failure modes
    (null-bind crash on a missing sandbox path, or an intentional-escalation exit
    misclassified as a crash) that killed both prior attempts.

Context: Sandbox writes only, target repo working tree only (C:\Users\marka\code\mt-integration-2026-06-22).
Amanah: entrusted; discharge with ihsan.

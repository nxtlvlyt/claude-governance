{"id": "gap-bare-commit-sweeps-preexisting-stage", "arrived": "2026-07-13", "jurisdiction": "muezzin-plugin", "class": "bite", "owner": "unowned engine item, needs a QUEUE.md number", "status": "owned", "closes_when": "every mission-authored commit step in this session's own template runs git commit with an explicit pathspec (git commit -- <allow-files...> -m ...) or an equivalent guard, instead of a bare git commit -m ... that commits the WHOLE INDEX regardless of what else happens to be staged", "receipt": "commit 7e0a011 (engine-verdict-merge-visibility-downgrade mission, ALLOW-FILES: verdict_merge.mjs only) silently absorbed the ALREADY-STAGED conduct-cycle.mjs anchor fix from a different, unrelated, interrupted mission (engine-srcsha-anchor-fix-v2) because its own commit step ran a bare git commit with no pathspec, committing the entire index. Discovered only because it happened to fully resolve gap-mission-chain-inherited-dirty-tree's specific blocking instance as a side effect (conduct-cycle.mjs is now clean at HEAD, unblocking engine-srcsha-fixture-update on re-fire) -- lucky this time, but the same pattern could just as easily commit unrelated, unreviewed, or unfinished work under an innocent-looking mission commit message. Every code-repo mission constructed this session (verdict_merge, self_witness, conduct-cycle fixes) uses the identical git add -- <file>; git commit -m ... pattern with no pathspec on the commit itself."}

MISSION-CLASS: code-repo
MISSION-ID: M-VANLIFE-EDITOR.A
REPO-ROOT: E:\AI_Storage\website-pipeline
ALLOW-FILES:
  - apps/post-intake/
REQUIRES: code-repo, muddytires-proven
# GATE (conductor, 2026-06-15): HELD until the code-repo engine proves it can complete a code-repo mission end-to-end. code-repo is 0/11; muddytires-migrate-1 is the live proof of that path. Do NOT fire this until muddytires goes DONE on a real receipt. Firing a code-repo mission (touching a 44KB file) into an unproven engine is setting it up to fail.
# CONSTRUCTED per MISSION_CONSTRUCTION.md (Maqsad + niyyah + unbiased context ONLY — no prescribed steps; the Architect does the ijtihad decomposition into micro-actions) and MISSION_ARCHITECTURE.md (mission -> sub-mission on size-ceiling -> micro-action: one edit OR one command OR one verification, reject any micro-action touching >1 implementation file).

Niyyah: take the operator's vanlife content to the next level by making the social-media pipeline SEE what is on the screen it renders and emphasize it — the first rung of the editor ladder he authored. Right now the renders are full-frame scale/crop only; the pipeline has no idea what proof is on the monitor in his screen-recording content (his nxtlvl bread-and-butter). This is excellence (ihsan) on the work he cares about most, discharged as an amanah.

Maqsad: a viewer watching a rendered cut SEES the on-screen proof emphasized at the moment the voice claims it. When the audio says "it ran for 20 hours," the cut zooms to the timer/chart that proves it, holds, and eases back. "Done" is something the operator can WATCH in a render — a proof element that the pipeline identified on screen and visibly emphasized in the cut, with a QC gate that flags any identified proof element the cut failed to emphasize.

Context (verified facts, no steering — the path is yours):
- The full Stage-A spec, in the operator's own words, is at apps/post-intake/EDITOR-LADDER.md ("STAGE A — Screen-region proof emphasis"). It is the authoritative source for what this mission serves. Read it.
- The proof-first doctrine this applies visually is in apps/post-intake/playbook.md (HOOK RULES: "PROOF, PROMISE, PLAN"; "SHOW the receipt while the voice claims it").
- The vision capability already exists: nemotron3:33b (local, projector-verified #1 on real footage per the model ranking) can name on-screen proof elements + bounding boxes per scene frame. No new model dependency.
- Transcript timing already exists (srt/Whisper timestamps in the batch), so proof elements can be mapped to the transcript moment they support.
- ENGINE TERRAIN (facts the decomposition must respect, not a prescribed method): apps/post-intake/processor.mjs is ~44KB (43,953 bytes). The engine's edit seats fail (truncation / empty-emission / TOOL_LOOP_CAP — six recorded kills) when asked to re-emit a file this size whole. New logic that can live in its OWN module is authoring-class work, which the engine completes reliably (19/19); changes that must touch processor.mjs are reliable only as TARGETED in-place command-edits, never a whole-file re-emission. Decompose accordingly.
- The renderer is ffmpeg-based; crop/zoompan is an existing ffmpeg capability (no new dependency). A new render variant is expected (the ladder names render-9x16-proofzoom.mp4) — a new output, not a rewrite of the existing render path.
- The operator's standing rule (apps/post-intake/design-contract.md, EDITOR-LADDER QC line): every quality miss becomes a PERMANENT QC GATE, never a human-in-the-loop patch. A proof element the vision seat named but the cut never emphasized MUST be a flag.
- GPU discipline (playbook.md INFRA NOTE + the self-deadlock law): never dispatch a GPU inference (vision seat) inside a witnessed mission step that already holds the GPU; serial inference only (GR10).
- E:\AI_Storage\website-pipeline is a git toplevel (verified 2026-06-15) — the code-repo precursor is satisfied; no repo-staging needed.

Amanah: this is entrusted to you. Reason your own path to the Maqsad with ihsan; the witness will judge the deed by what runs and what the operator can see in the render, not by any claim.

Constraints: code-repo, the post-intake app only (apps/post-intake/), NAS UNTOUCHED (never N:/P:/W:), serial inference, no secret values printed. The deed is witnessed by an execution receipt (it runs / renders), never by self-attestation.

# SEQUENCE NOTE (conductor): this is rung A of the operator's editor ladder. B (rebuilt 5s hook), C (per-stage management plane), and the QC-ledger + hook-performance self-learning loop follow, each its own Maqsad mission, each gated on the prior's receipt. Stage B consumes Stage A's proof-region data (dependency). Author B only after A lands.

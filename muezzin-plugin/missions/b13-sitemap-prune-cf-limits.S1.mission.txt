MISSION-ID: M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - workers/sitemap-prune/wrangler.toml
PARENT: M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS  (sub-mission 1 of 2, auto-split by the size ceiling)
REQUIRES: none (first sub-mission of 2 in tartib order)

Niyyah: the sitemap-prune worker should survive production load. The original build assumed Cloudflare Workers limits the panel measured exceeded by 8x, and uncaught D1 awaits would surface as CF 1101 errors to users. Close the substrate-verified defects — not the model's confidence, the documented Cloudflare ceiling.

Maqsad: this is part 1 of 2 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): the worker runs safely on Cloudflare Workers Free plan AND Paid plan, with no silent failure modes. Re-running the 5-seat panel against the new diff returns panel_verdict in {pass, concern}.

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - Author the complete worker.js closing ALL panel findings in one emission (one-writer rule: this is the SOLE authoring step for worker.js — executor reads the existing sandbox file and rewrites it, preserving working logic). REQUIRED closures with these exact witnessable tokens: (a) top-level fetch(request,env,ctx) body fully wrapped in try/catch that returns Response.json({error,code}) on any throw — never an uncaught throw (closes finding 2); (b) the upsert404 and writeCursor awaits inside handleRefresh wrapped in their own try/catch returning structured JSON with code 'd1_error' on D1 failure, preserving cursor (closes finding 1); (c) ensureSchema catch sets schemaEnsured=true ONLY when the error message includes the literal 'already exists', otherwise rethrows so it surfaces as structured error — no unconditional masking (closes finding 3); (d) BATCH_SIZE=20 with an in-code comment carrying the inequality 20*2+overhead < 50 proving per-/refresh subrequests stay under the CF Free ceiling (closes finding 4); (e) handleRefresh processes at most BATCH_SIZE items from the persisted cursor then returns {processed,cursor,hasMore} — no unbounded internal loop, caller re-invokes until hasMore is false (reinforces finding 4); (f) cursor compare-and-swap: conditional UPDATE ... WHERE version=?, and when 0 rows change return Response.json({error:'refresh_in_progress'},{status:409}) (closes finding 6); (g) before any outbound fetch, parse each sitemap URL with new URL() and reject entries whose hostname is not the configured site host or subdomain, and reject non-https schemes (closes finding 7 SSRF); (h) loc extraction tolerant of CDATA (literal 'CDATA') and namespace-prefixed tags (closes concern 5). Note: filename assumed workers/sitemap-prune/worker.js per validator feedback — UNVERIFIED (file_read unavailable); integrator/executor retarget if the entry file differs.
  - Author workers/sitemap-prune/wrangler.toml (sole authoring step for this file): executor reads the existing sandbox config and rewrites it preserving the D1 database binding the worker expects, and ensuring there is NO limits.subrequests override that would mask the Free-plan 50 ceiling — the algorithm is provably <50 per invocation so no subrequest bump is needed. Keep the worker correct on Free by construction.
  - Syntax gate: confirm the authored worker parses cleanly before substantive witnesses.
  - Witness finding 1: the structured D1-failure path exists (code 'd1_error' returned instead of an uncaught throw).
  - Witness finding 3: ensureSchema swallows ONLY the benign 'already exists' case (conditional, not unconditional masking).
  - Witness finding 4: BATCH_SIZE is assigned a value <=44, mathematically under the CF Free 50-subrequest ceiling (rejects the old 200).
  - Witness the continuation contract: handleRefresh returns hasMore so one invocation processes one bounded batch and the caller resumes (per-invocation 50-cap holds regardless of sitemap size).
  - Witness finding 7: out-of-domain sitemap entries are rejected via parsed URL hostname check before any outbound fetch.

Done means: steps M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1.1..M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1.8 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — - The worker, when invoked, returns structured JSON on D1 failure rather than CF 1101.
  - Per /refresh subrequest count is mathematically provable to stay under 50 (Free plan ceiling).
  - ensureSchema does NOT mask unknown errors as "already exists".
  - Concurrent /refresh calls either serialize cleanly OR return a structured conflict response (no race-corrupted cursor).
  - URL host validation rejects out-of-domain sitemap entries.
  - Same panel re-fire returns pass/concern; node --check passes; push + ls-remote verify.

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
MISSION MT-INTEGRATE-REGION-HEATMAP
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - functions/api/heatmap-points.js
  - js/region-heatmap-layer.js
  - map.html
REQUIRES: none
VISUAL-QC-REQUIRED
PREVIEW-BASE-URL: http://localhost:8788

Niyyah: Feature worktree mt-feat-region-heatmap-2026-06-23 carries one finished commit —
a toggleable client-side Leaflet heatmap overlay showing national POI density — sitting
in its own worktree, never landed on the integration line's current HEAD. This mission's
whole job is mechanical: get the feature's three files onto the target repo's HEAD
without losing anything already there, and produce the node-level and render-level
receipts a verdict panel needs to judge the result. Writing this mission file, and the
act of integrating itself, is NOT verification that the feature's own code is correct.
Whether the 2dp lat/lng cell aggregation in heatmap-points.js is a sane density bucket,
whether the log-compressed 0..1 weighting actually reads as a useful heat gradient,
whether the lazy-load-on-first-toggle really costs zero when the layer is never opened,
and whether the in-memory 5-minute cache ever serves stale or cross-request data — none
of that is settled by landing the commit. That determination belongs entirely to this
mission's own witness-and-verdict-panel run: the node --check receipt, the headless-
browser render of map.html, and the seat panel's judgment on the actual integrated diff.
Say plainly: authoring or executing this mission proves nothing about the feature code's
correctness by itself.

Maqsad: Land commit 9a29244 ("feat(map): toggleable region heatmap overlay (POI
density)") from the source worktree C:\Users\marka\code\mt-feat-region-heatmap-2026-06-23
onto the current HEAD of the target repo C:\Users\marka\code\mt-integration-2026-06-22,
with any conflicts resolved by keeping BOTH sides — this repo's existing unrelated
changes AND the feature's changes, never silently dropping either — every changed
.js/.mjs file passes `node --check`, the result is committed with a clear message, and
the target repo's working tree is left clean with respect to every ALLOW-FILES entry.

Context (verified against live substrate this session, 2026-07-01, absolute paths, no
prescribed resolution beyond "keep both sides"):
- The task that produced this mission named the source worktree as
  C:\Users\marka\code\region-heatmap-2026-06-23. That path does NOT exist. Per Directive 1
  (substrate is truth), the corrected, verified path is
  C:\Users\marka\code\mt-feat-region-heatmap-2026-06-23 — its branch is
  feat/region-heatmap-2026-06-23 and `git log --oneline -3` there shows 9a29244 at the tip.
  Any step below that names a source-worktree path uses this corrected path.
- Confirmed diff stat (git show --stat 9a29244, this session): 3 files, +353/-1 —
  functions/api/heatmap-points.js new, 116 lines; js/region-heatmap-layer.js new, 236
  lines; map.html +1/-1 (map.html's script-tag chain lives on one very long single line,
  so the "+1/-1" is a same-line edit: one new
  `<script src="js/region-heatmap-layer.js" defer></script>` inserted between the existing
  ev-charging-layer.js and free-camping-filter.js tags).
- Checked this session: `git cat-file -t 9a29244` in the target repo resolves to "commit",
  and `git branch -a --contains 9a29244` shows both the local branch
  feat/region-heatmap-2026-06-23 and remotes/github/feat/region-heatmap-2026-06-23 already
  contain it. mt-integration-2026-06-22 and mt-feat-region-heatmap-2026-06-23 are linked
  worktrees of the SAME repository (both report git-common-dir
  C:/Users/marka/code/muddytires-pages/.git) — the object is already directly reachable in
  the target repo's own object store. No `git fetch <source-worktree-path>` / remote-add
  step is needed; a plain `git cherry-pick 9a29244` can resolve the object directly.
- `git merge-base --is-ancestor 9a29244 HEAD` in the target repo returns false (not yet
  landed on HEAD) — 9a29244 is the one commit this mission needs to apply.
- A dry run this session (`git merge-tree --write-tree HEAD 9a29244`, read-only — no branch
  created, no working-tree change) resolved with exit 0 and produced a tree object with NO
  conflict markers reported — predicting a clean cherry-pick against target HEAD 5e19874 on
  branch main. Neither functions/api/heatmap-points.js nor js/region-heatmap-layer.js
  exists yet on disk in the target repo (confirmed by direct file check this session), so
  those two are expected to land as pure adds. The target branch may move again before
  this mission actually fires — if map.html (the one file both sides could plausibly touch)
  conflicts at fire time, resolve it by keeping BOTH the target's existing script tags AND
  this feature's added `<script src="js/region-heatmap-layer.js" defer></script>` tag.
- Target repo HEAD this session: 5e19874, on branch main. This mission deliberately carries
  no TARGET-BRANCH header: land onto whatever branch is actually checked out as HEAD when
  this mission fires, not a hardcoded branch name.
- This target repo is shared, live substrate under active mission traffic: within this
  same session its working tree briefly carried staged, uncommitted changes for an
  unrelated feature (poi-tags — new functions/api/poi-tags*, js/poi-tags.js, a D1 migration
  SQL file, and a staged map.html entry) and then went clean again before this mission file
  was finished. That poi-tags work is out of this mission's scope — none of its files are
  in ALLOW-FILES above, and this mission must never touch them. But it is evidence that
  another agent may be mid-edit on map.html when this mission actually fires. If, at fire
  time, `git status --short` shows map.html already staged or modified by work unrelated to
  this feature, do not discard that pending work to force the cherry-pick through — resolve
  by keeping both, or if a clean 3-way resolution is not possible, surface a named blocking
  receipt rather than dropping either side.
- functions/api/heatmap-points.js is a Cloudflare Pages Function (server-side, runs in the
  Pages Functions runtime, not the browser) that reads from D1; a headless-browser render
  cannot execute it directly, but map.html (which loads js/region-heatmap-layer.js, which in
  turn calls this endpoint when the heatmap layer is toggled on) is exactly what
  VISUAL-QC-REQUIRED below is for.
- map.html is a live page served by the site (not a browser-extension popup), so
  PREVIEW-BASE-URL: http://localhost:8788 is a PLACEHOLDER for a local
  `wrangler pages dev` server — the operator must confirm the actual local preview port
  before relying on the captured render; if nothing answers at PREVIEW-BASE-URL, that
  failure must surface as a named receipt, not a silently skipped check.

Steps:
  1. In the target repo, record the current branch and HEAD, and confirm
     functions/api/heatmap-points.js and js/region-heatmap-layer.js do not already exist on
     disk (git -C C:\Users\marka\code\mt-integration-2026-06-22 status --short; git -C
     C:\Users\marka\code\mt-integration-2026-06-22 log --oneline -3; git -C
     C:\Users\marka\code\mt-integration-2026-06-22 branch --show-current). [command]
     verify-target-state
  2. Check ancestry BEFORE attempting a cherry-pick: `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 merge-base --is-ancestor 9a29244 HEAD`.
     If it reports 9a29244 already an ancestor, it is already landed — skip cherry-picking
     it, do not use --allow-empty, proceed straight to step 4's verification. Otherwise
     continue to step 3. [command] check-ancestry
  3. Cherry-pick the commit onto the target's current HEAD:
       git -C C:\Users\marka\code\mt-integration-2026-06-22 cherry-pick 9a29244
     If it reports conflicts, resolve every conflicted file by keeping BOTH the target's
     pre-existing lines and this feature's added lines — never `--ours` / `--theirs` and
     never a resolution that deletes either side's content. If it comes back empty
     (already-applied after all), resolve with `--skip`, never `--allow-empty`. Only
     choosing one side over the other, on any file, is a defect in this mission's
     execution, not an acceptable outcome. [command] cherry-pick-region-heatmap
  4. Run `node --check functions/api/heatmap-points.js` and
     `node --check js/region-heatmap-layer.js` from the target repo root (the two .js files
     this commit touches) and confirm both exit 0; also run `node --check` on any other
     .js/.mjs file that conflict resolution in step 3 modified. [command] node-check-all
  5. If step 3 left the cherry-pick uncommitted (conflict resolution required `git add` +
     `git cherry-pick --continue`), commit explicitly with a message that names
     "mt-feat-region-heatmap-2026-06-23" and/or "region-heatmap" and cites 9a29244. Then
     confirm `git status --short` in the target repo shows no uncommitted changes to any
     ALLOW-FILES entry and `git log --oneline -5` shows the landed commit at or near HEAD.
     [command] commit-and-verify-clean
  6. (amended 2026-07-07 per RULE 9: prior step hand-rolled a localhost:8788 server — nothing serves that port) In ONE command: confirm clean tree for this mission's ALLOW-FILES (git commit first if this mission's edits are not yet committed — never deploy a dirty tree), then wrangler pages deploy . --project-name=muddytires --branch=preview, capture the emitted *.pages.dev URL into a PowerShell variable, write scratch-qc.mjs (playwright render of /map.html from that URL, exit non-zero if the page throws a fatal script error or the new `<script src="js/region-heatmap-layer.js" defer>` tag is absent from the served DOM), node scratch-qc.mjs, Remove-Item scratch-qc.mjs. [command] headless-render-witness

Done means:
  - `git log` in C:\Users\marka\code\mt-integration-2026-06-22 shows a commit at or near
    HEAD whose message names "mt-feat-region-heatmap-2026-06-23" and/or "region-heatmap"
    and cites 9a29244.
  - functions/api/heatmap-points.js and js/region-heatmap-layer.js exist in the target repo
    with the feature's actual content (heatmap-points.js aggregates POIs by 2dp lat/lng
    cell into log-compressed 0..1 weights and fails closed with a 500 + ok:false on a D1
    error rather than an empty array; region-heatmap-layer.js lazy-loads the leaflet.heat
    plugin and the /api/heatmap-points fetch on first toggle-on, keeps an in-memory 5-min
    cache, and renders a warning marker rather than failing silently) — confirmed by
    reading each file's on-disk content, not by trusting the cherry-pick's exit code alone.
  - map.html contains the new `<script src="js/region-heatmap-layer.js" defer></script>`
    tag AND every pre-existing script tag this repo's map.html already had before this
    mission ran (both sides present, nothing dropped).
  - `node --check functions/api/heatmap-points.js` and
    `node --check js/region-heatmap-layer.js` both exit 0, and `node --check` exits 0 on
    every other .js/.mjs file this integration touched or conflict-resolved.
  - `git status --short` in the target repo shows no uncommitted changes to any
    ALLOW-FILES entry.
  - A headless browser (headless Chrome / Playwright / puppeteer) render of /map.html
    served from PREVIEW-BASE-URL loads without a fatal script error and shows the new
    `js/region-heatmap-layer.js` script tag present in the rendered DOM — verified by
    rendering the page, not by reading the source. Nothing about this mission's own
    authorship or execution constitutes verification that the feature's code is correct
    (that the density buckets are sane, that the gradient reads correctly, that the cache
    never serves stale cross-request data, that nothing else regressed) — that
    determination belongs entirely to this mission's own witness-and-verdict-panel run,
    downstream of the receipts above.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

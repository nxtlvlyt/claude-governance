MISSION-ID: mt-first-party-beacon.S1
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - js/mt-beacon.js
  - functions/api/beacon.js
  - d1/migrate-beacon-2026-07-03.sql
  - map.html
  - index.html
REQUIRES: none
STEPS: 8

Maqsad: Operator-sent growth transcript 2026-07-03 ("masters have more ways to win": beginners
see binary lagging outcomes — sale/no-sale — while masters watch leading indicators and
milestones; you cannot fix what you cannot see moving). Receipted gap: muddytires.ca has ZERO
user-behavior measurement — live fetch 2026-07-03 shows only the Sentry error SDK, no
analytics of any kind, so the operator cannot answer "did anyone visit, which layers do
people open, is anything growing" except by asking. Third-party analytics would betray the
no-login/no-paywall privacy brand; the fix is first-party and cookieless. Three deliverables:
(D1) MIGRATION d1/migrate-beacon-2026-07-03.sql: a beacon_daily table keyed
(day TEXT, page TEXT, event TEXT, viewport TEXT) with a count INTEGER — AGGREGATES ONLY,
no per-visitor rows, no IP column, no user agent column, nothing to breach.
(D2) ENDPOINT functions/api/beacon.js: POST-only, accepts {page, event, viewport} from a
strict allowlist (pages: index|map|guides|other; events: view|layer_open; viewport:
mobile|desktop), UPSERTs count+1 into beacon_daily for today's UTC day, rejects anything
off-allowlist, 405 on GET. No cookies set, no identifier stored or derived — the endpoint
cannot tell two visitors apart, by design.
(D3) CLIENT js/mt-beacon.js + one-line wiring in index.html and map.html: on load, if
navigator.doNotTrack/globalPrivacyControl signal is set, do NOTHING at all; otherwise
sendBeacon/fetch one {page, event:'view', viewport} POST, and on the map additionally one
{event:'layer_open'} per layer-panel open per pageview (debounced, max a handful per view).
Reading the numbers is wrangler d1 execute over beacon_daily — no public stats surface in
this mission.

niyyah: Leading indicators for a pre-revenue product, bought without selling out the
privacy posture that IS the brand: counts, not tracking. The honest-data rule applies to
ourselves — aggregates the operator can query beat any third-party dashboard the users
never consented to. Scope every assertion to the five ALLOW-FILES.

Steps:
  1. Idempotent baseline: record branch + HEAD; if js/mt-beacon.js already exists output CLEAN_ALREADY and treat authoring steps as verify-only presence checks; else CLEAN_BASELINE. Never exit 1 on existence. [command]
  2. Author d1/migrate-beacon-2026-07-03.sql per D1 (CREATE TABLE IF NOT EXISTS beacon_daily ... with a UNIQUE(day,page,event,viewport) so the UPSERT has a conflict target). [edit] d1/migrate-beacon-2026-07-03.sql
  3. Author functions/api/beacon.js per D2 (allowlist validation, UPSERT count+1, 405 on GET), following the env.DB/json() conventions of functions/api/stats.json.js. [edit] functions/api/beacon.js
  4. Author js/mt-beacon.js per D3 and add the single script-tag wiring to index.html and map.html (additive lines only). [edit] js/mt-beacon.js
  5. node --check js/mt-beacon.js and node --check functions/api/beacon.js. [verify] js/mt-beacon.js
  6. Scoped cleanliness: git status --porcelain limited to the five ALLOW-FILES shows only them. [verify]
  7. Commit the five ALLOW-FILES by explicit name with a message naming this mission and the transcript receipt, confirm the commit is on HEAD. [command]
  8. OUTCOME CHECK: write a cwd-relative scratch harness (Set-Content scratch-beacon-test.mjs) that (a) imports functions/api/beacon.js's onRequestPost with a stubbed env.DB and asserts a valid {page:'map',event:'view',viewport:'mobile'} fixture UPSERTs while {page:'evil'} is rejected and GET-shaped handling is 405; (b) loads js/mt-beacon.js in a stubbed window/document/fetch environment and asserts it POSTs the correct payload shape on load AND that with a doNotTrack-set stub it makes NO network call — run node scratch-beacon-test.mjs, require exit 0 with output BEACON_OK, remove the scratch file. [command]

Done means: all five ALLOW-FILES committed at HEAD; the scratch harness exits 0 with
BEACON_OK proving allowlist/UPSERT/405 behavior and the client's send-on-load plus
respect-DNT-by-silence behavior against fixtures — executable outcome verification
appropriate to an invisible measurement deliverable (no visual bar: the feature's success
is a table row, not a pixel; deliberate parallel to the geocode.S1 amendment lineage
2026-07-03). D1 migration application to the live database is a deploy-time step recorded
by the standing --record-deploy ceremony, not silently assumed by this mission.

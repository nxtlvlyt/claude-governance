MISSION-ID: stitch-design-mastery-c
MISSION-CLASS: research
SANDBOX: missions/stitch-design-mastery-c
REQUIRES: none
STEPS: 6

Maqsad: REVIVAL of the Stitch design-mastery thread — held by operator word 2026-06-12
("no requeue without my explicit say-so"), UNHELD by operator word 2026-07-03 ("you have
an MCP stitch that could help you with web design" / "it's on the Claude CLI on nxtbeast"
+ the setup-docs link). Access path receipted 2026-07-03: the Stitch MCP runs on
NXTBEAST's Claude CLI (stitch-launch.mjs mints a Google token from the operator's ADC and
proxies @_davideast/stitch-mcp; verified live "Connected to Stitch, discovered 14 tools").
Produce: (a) stitch-census.md — the LIVE census of the Stitch MCP tools (name + what
each does, from the MCP's own listing, not memory); (b) DESIGN-draft.md — a Stitch-usable
design brief for ONE muddytires screen (the map filter-stack panel, whose label defects
are receipted in REQUIREMENTS-REGISTRY) written against the census's actual capabilities.
Foundation: the -b mission's banked parts REMAIN VALID and are gathered, not regenerated
(part-1-census.md sha c413f1c = 2026-06 capability notes; part-2-design-md.md sha 93ee8b7
= the DESIGN.md contract).

niyyah: Small emissions, presence-validated, per the -b post-mortem (its killer was the
big-single-emission class, since root-fixed). Stitch-touching steps are [command] steps
dispatching `ssh nxtbeast claude -p` with the stitch MCP allowed — the ONLY path that can
reach the tools (laptop registry receipted empty of them). Every Stitch capability claim
in the census must come from the live tool listing in that dispatch's output; a tool the
listing does not name does not exist in the census (no memory-fill). Any cleanliness or
file assertion scopes to this mission's sandbox only.

Steps:
  1. Idempotent gather: if missions/stitch-design-mastery-b/part-1-census.md exists, copy it into the sandbox as legacy-part-1.md; same for part-2-design-md.md as legacy-part-2.md (Test-Path-guarded copies, no failure when absent — note absence in gather-notes.txt instead). [command]
  2. LIVE census dispatch: run ssh nxtbeast claude -p with a prompt instructing it to list every available mcp__stitch tool by exact name with a one-line description of each, and to reply as a fenced markdown table only; capture stdout to raw-census.txt and require the output to contain at least 10 distinct mcp__stitch tool names (fewer means the MCP failed to connect — fail loudly, never fabricate). [command]
  3. Author stitch-census.md in the sandbox: the table from raw-census.txt verbatim, plus a short delta section against legacy-part-1.md (what changed since 2026-06). Every row must trace to raw-census.txt. [edit] stitch-census.md
  4. Author DESIGN-draft.md: a design brief for the muddytires filter-stack panel screen that names WHICH census tools would generate/iterate it, honoring the legacy-part-2 DESIGN.md contract structure. Claims about tool capabilities cite census rows; the known label defects (duplicate "B.C.", "Canada 💗") are the brief's stated problem statement. [edit] DESIGN-draft.md
  5. Validate: both artifacts exist, stitch-census.md contains >=10 mcp__stitch rows all present in raw-census.txt, DESIGN-draft.md cites >=3 census rows by tool name. Print the counts. [verify] stitch-census.md
  6. Write gather-notes.txt summary line counts for all sandbox artifacts (the assembly receipt). [command]

Done means: stitch-census.md exists with >=10 live-listed mcp__stitch tools (each traceable to raw-census.txt captured from the nxtbeast dispatch — no memory-derived rows), DESIGN-draft.md exists citing >=3 census tools against the filter-stack problem statement, and both legacy parts are gathered or their absence noted — executable, sandbox-scoped verification appropriate to a research deliverable.

MISSION SOTA-PIPELINE-DOSSIER-SPLIT-C-MERGE
MISSION-CLASS: research
REQUIRES: the two prior section outputs staged (split-a + split-b). Section C/D/E are argued from the staged sections + named gaps; any new market claim still carries a search receipt.
Niyyah: split-c — the assembly. The parent died trying to author all five sections in one emission; split-a and split-b produced Sections A and B as their own bounded artifacts. This slice STAGES those approved sections verbatim and authors only the three short closing sections (MOAT, GAPS, VERDICT), so the final emission stays under the ceiling. Verbatim staging honors the no-edit-of-approved-bodies rule.
Maqsad: ONE artifact — SOTA-PIPELINE-DOSSIER.md at the sandbox root. PLAN SHAPE: (1) TWO command gather steps copy the prior sections, each shaped EXACTLY (these are REQUIRED — a missing source must FAIL the step loudly, never skip): Copy-Item '..\sota-pipeline-dossier-split-a-gather-and-A\SECTION-A-PIPELINE-KNOWS.md' 'section-a.md' and Copy-Item '..\sota-pipeline-dossier-split-b-per-engine-sota\SECTION-B-ENGINE-SOTA.md' 'section-b.md'; (2) ONE command step hard-validates staging: (Get-Item section-a.md).Length -gt 500 -and (Get-Item section-b.md).Length -gt 500; (3) ONE edit step assembles SOTA-PIPELINE-DOSSIER.md WITH DEMARCATION: a '# SOTA Pipeline Dossier' header, then '## A. WHAT THE PIPELINE KNOWS (from split-a, verbatim)' followed by the full body of section-a.md, then '## B. ENGINE-BY-ENGINE SOTA ASSESSMENT (from split-b, verbatim)' followed by the full body of section-b.md, then authors '## C. THE MOAT' (why chained governed local/cloud seats + the three-layer revenue model beat template-flippers — argued from the staged evidence, not enthusiasm), '## D. HONEST GAPS' (what is NOT yet ingested or built, named plainly — an absent capability stated as absent is a PASS; non-empty), '## E. VERDICT' (one paragraph answering the operator's literal question: how state-of-the-art the pipeline is after ingestion). Done means: SOTA-PIPELINE-DOSSIER.md exists, markdown-valid, carries the full body of both staged sections after their headers, Section D is non-empty, and Section E directly answers the operator's question.
Context: header/demarcation lines are COMMAND/edit-written strings; the staged A and B bodies stay verbatim. Sandbox writes only. This is part 3 of 3.
Amanah: entrusted; discharge with ihsan.

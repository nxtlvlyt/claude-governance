MISSION-ID: M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - workers/sitemap-prune/*.js
  - workers/sitemap-prune/wrangler.toml
PARENT: M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS  (sub-mission 1 of 2, auto-split by the size ceiling)
REQUIRES: none (first sub-mission of 2 in tartib order)

Niyyah: the sitemap-prune worker should survive production load. The original build assumed Cloudflare Workers limits the panel measured exceeded by 8x, and uncaught D1 awaits would surface as CF 1101 errors to users. Close the substrate-verified defects — not the model's confidence, the documented Cloudflare ceiling.

Maqsad: this is part 1 of 2 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): the worker runs safely on Cloudflare Workers Free plan AND Paid plan, with no silent failure modes. Re-running the 5-seat panel against the new diff returns panel_verdict in {pass, concern}.

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - Check out the existing target branch so all fixes land on it (the branch already exists from the panel run, sha 530457f-class). Establishes the working ref before any edit.
  - Confirm the worker implementation file exists at the assumed flat path workers/sitemap-prune/index.js (ALLOW-FILES: workers/sitemap-prune/*.js). UNVERIFIED filename — file_read unavailable; if the real entry file is named differently this witness fails fast and surfaces the mismatch to the conductor instead of letting the edit step create a duplicate.
  - Rewrite the single worker file workers/sitemap-prune/index.js to close all 7 panel defects in one complete emission (one file = one writer; all named functions live here): (1+2) wrap every D1 await in handleRefresh AND the top-level fetch() handler in try/catch, routing all failures through a jsonError(message,status) helper that returns a structured JSON Response (status 500) instead of letting throws become CF 1101; (3) ensureSchema catch blocks must inspect the error and only swallow when /already exists/ matches — otherwise rethrow, and never set schemaEnsured=true unconditionally; (4) replace BATCH_SIZE=200 with a small bounded BATCH_SIZE (<=40) and a continuation/cursor pattern so per-/refresh subrequests are mathematically provable < 50 (CF Free ceiling), with an inline comment showing the worst-case fetch math; (6) add cursor-lock so concurrent /refresh either serialize or return a structured 409 conflict response (no race-corrupted cursor); (7) add isAllowedHost() host validation rejecting sitemap URLs outside the site domain (SSRF guard); (5) make sitemap parsing tolerate CDATA/namespaced XML. REQUIRED identifiers the executor MUST use (downstream witnesses assert them): jsonError, BATCH_SIZE, already exists, isAllowedHost, 409. File listed in context_dependencies so the executor regenerates from the existing full contents rather than dropping prior code.
  - Witness defect 4 closed: BATCH_SIZE is now a small bounded value (1-40), not 200 — guaranteeing per-/refresh subrequests stay under the CF Free 50 ceiling.
  - Witness defects 1+2 closed: a jsonError helper exists, evidencing structured-JSON error responses on D1/handler failure instead of CF 1101.
  - Witness defect 3 closed: ensureSchema discriminates errors by matching 'already exists' rather than swallowing all errors unconditionally.
  - Witness defect 7 closed: isAllowedHost host-validation guard is present (rejects out-of-domain sitemap entries, SSRF protection).
  - Witness defect 6 closed: a 409 structured conflict response exists for concurrent /refresh cursor contention (no race-corrupted cursor).

Done means: steps M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1.1..M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1.8 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — - The worker, when invoked, returns structured JSON on D1 failure rather than CF 1101.
  - Per /refresh subrequest count is mathematically provable to stay under 50 (Free plan ceiling).
  - ensureSchema does NOT mask unknown errors as "already exists".
  - Concurrent /refresh calls either serialize cleanly OR return a structured conflict response (no race-corrupted cursor).
  - URL host validation rejects out-of-domain sitemap entries.
  - Same panel re-fire returns pass/concern; node --check passes; push + ls-remote verify.

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
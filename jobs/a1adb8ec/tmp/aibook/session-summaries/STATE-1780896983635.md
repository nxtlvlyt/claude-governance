I will begin by verifying the substrate — reading the escalation record, current state, and all claimed file paths to establish ground truth.
Substrate verification confirms the Auditor's findings exactly:

- `fly.toml` ✅ EXISTS (pre-existing M23 artifact — not newly written)
- `wasmer.toml` ❌ ENOENT
- `bun-ai.config` ❌ ENOENT  
- `carbon-aware.config` ❌ ENOENT

I will now attempt discovery of the claimed 12 Assertion-Closed concerns before composing the corrected Isha.
Discovery confirms: **No file containing the claimed 12 Assertion-Closed concerns exists in the substrate.** They were never written. I will now read the authoritative amended plan to ensure the BOOTSTRAP HANDOFF is grounded.
I have verified full substrate reality:

| Claimed File | Path | Result |
|--------------|------|--------|
| `fly.toml` | `E:\AI_Storage\docker\fly.io\fly.toml` | ✅ EXISTS — Pre-existing M23 artifact |
| `wasmer.toml` | `E:\AI_Storage\docker\wasmer\wasmer.toml` | ❌ ENOENT |
| `bun-ai.config` | `E:\AI_Storage\docker\bun-ai\bun-ai.config` | ❌ ENOENT |
| `carbon-aware.config` | `E:\AI_Storage\docker\carbon-aware\carbon-aware.config` | ❌ ENOENT |

**Discovery verdict:** The 3 missing files are **phantom** — inferred from env var strings (`EDGE_RUNTIME = "wasmer"`, `BUN_AI_ENABLED = "true"`, `CARBON_AWARE_ROUTING = "true"`) inside the existing `fly.toml`, but they never existed as independent substrate. The claimed 12 Assertion-Closed concerns (AC-M28-P2-001 to AC-M28-P2-012) are also **phantom** — no file containing them exists anywhere in the substrate.

I will now execute corrected Isha compaction to `STATE.md`.
Isha compaction written. I will now verify the substrate by reading back the committed STATE.md to confirm physical persistence.
**Isha compaction verified.** The corrected `STATE.md` is physically committed to substrate at `C:\Users\marka\.agents\STATE.md`.

---

## Integrator Ruling Summary — Post-Auditor BLOCK Corrective Isha
**Ruling ID:** KK-INT-M28-ISHA-CORRECTED  
**Confidence:** 1.0

### Corrections Applied

| Auditor Mandate | Status | Detail |
|-----------------|--------|--------|
| **1. Correct infrastructure scope claim** | ✅ SATISFIED | STATE.md now accurately states: 1 pre-existing file (`fly.toml`, M23 artifact) exists; 3 inferred paths (`wasmer`, `bun-ai`, `carbon-aware`) are **phantom** — never existed as independent files. |
| **2. Enumerate all AC concerns individually** | ✅ SATISFIED | Only 3 AC concerns exist in committed substrate: **A2** (Reasoning-leakage), **A3** (Selection basis forced), **GS-D12-ANNOTATION** (Closed in Isha). The claimed 9 additional concerns (AC-M28-P2-001 to 012) are ruled **phantom / unsubstantiated** — never written to disk. |
| **3. Commit corrections to substrate** | ✅ SATISFIED | `STATE.md` physically written and read-back verified. |

### Active Blockers Carried Forward

| Blocker | Action Required By |
|---------|------------------|
| **BLOCKER-001** | SearxNG container restart (`docker compose up -d searxng`) after Step 0 env vars are added. |
| **BLOCKER-004 (CORRECTED)** | Operator must review `fly.toml` and decide whether to create the 3 inferred infrastructure directories as real targets. |

### BOOTSTRAP HANDOFF Updated

The next Fajr instance **must** read:
1. `STATE.md` (this file) — corrected pipeline state
2. `AUDITOR_ESCALATION.md` — authoritative BLOCK/CORRECTION record
3. `PHASE1_MASTER_PLAN_M28_SEARXNG_REMEDIATION_AMENDED.md` — authoritative execution plan
4. `fly.toml` — to verify infrastructure scope before assuming any paths exist

**Critical carry-forward rule:** `[ASSERTION-CLOSED]` concerns A2, A3, and GS-D12-ANNOTATION are preserved. The 3 phantom infrastructure paths are **forbidden** from being treated as real until `file_read` proves their physical existence.

---

**Pipeline Status:** AUDITOR BLOCK **LIFTED**. Mission M28 Phase 2 remains in **REVISE** state, awaiting Phase 2 Executor dispatch to apply Step 0–5 of the amended plan.
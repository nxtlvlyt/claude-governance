MISSION CORPUS-S1-CARD-2BOOTS
MISSION-CLASS: research
Niyyah: the factory must never re-solve what 2boots already solved — it is the portal template's DNA and the pricing-pattern's origin; unmined, Layna's build re-derives it at frontier prices.
Maqsad: ONE deliverable — pattern-card-2boots.md in this sandbox: what the 2boots build does well and how, as reusable patterns. Required sections: (1) PORTAL MODULES — the admin features (prospects/tasks/templates/calendar/AI-assist), each with file-path evidence and what makes it forkable; (2) PRICING PATTERN — how pricing was researched-then-made-editable (the operator names this the model pattern: SOTA-searched seed values, owner-editable in the portal) with the implementing files; (3) DATA MODEL — the D1 schema shape worth templating; (4) DESIGN TOKENS/COPY PATTERNS worth reuse; (5) ANTI-PATTERNS — anything the build does that the factory should NOT copy. Every claim cites a file path. Done means the card exists, parses as markdown, and every section has at least one evidence citation.
Context: source READ-ONLY at E:\2boots (Astro + Cloudflare Functions + D1; key paths per prior session evidence: schema.sql, functions/api/tasks.ts, src/pages/admin/, src/lib/templates.ts, src/lib/concierge.ts). Live site 2boots.ca. List absolute paths only in context_dependencies; the card is the only write target, cwd-relative.
Amanah: entrusted; discharge with ihsan.

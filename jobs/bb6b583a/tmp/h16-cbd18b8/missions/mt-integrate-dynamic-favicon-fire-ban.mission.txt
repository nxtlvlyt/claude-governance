MISSION MT-INTEGRATE-DYNAMIC-FAVICON-FIRE-BAN
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - icon-fire-ban.svg
  - index.html
  - js/dynamic-favicon.js
  - map.html
REQUIRES: none
VISUAL-QC-REQUIRED
PREVIEW-BASE-URL: http://localhost:8788

Niyyah: Feature worktree C:\Users\marka\code\mt-feat-dynamic-favicon-2026-06-23 carries
one finished commit — a peripheral fire-ban indicator that mutates the browser-tab
favicon's data URL (a red disc + SMIL pulse composed onto the base /icon.svg) whenever a
cached or map-derived location intersects an enforceable /api/fire-bans ban — sitting in
its own worktree, never landed on the integration line's current HEAD. Correction against
live substrate: the work order that produced this mission named the source path as
C:\Users\marka\code\dynamic-favicon-fire-ban; that path does not exist on this machine
(verified this session — no such directory under C:\Users\marka\code\). The actual
worktree carrying commit d48f959 is C:\Users\marka\code\mt-feat-dynamic-favicon-2026-06-23,
confirmed via `git worktree list` and `git log --oneline` this session; every path below
uses the verified location, not the stated one. This mission's whole job is mechanical:
get the feature's four files onto the target repo's HEAD without losing anything already
there, and produce the node-level and render-level receipts a verdict panel needs to judge
the result. Writing this mission file, and the act of integrating itself, is NOT
verification that the feature's own code is correct. Whether the cached-location /
map-centre proxy actually reflects where the user is, whether the open_fire/all/partial
ban-type predicate genuinely matches js/fire-ban-layer.js's own rule, whether the SMIL
<animate> pulse degrades gracefully in browsers that don't animate embedded-SVG favicons,
and whether the 15-minute recheck cadence plus visibility-change listener actually avoids
stale state — none of that is settled by landing the commit. That determination belongs
entirely to this mission's own witness-and-verdict-panel run: the node --check receipt,
the headless-browser render of index.html and map.html, and the seat panel's judgment on
the actual integrated diff. Say plainly: authoring or executing this mission proves
nothing about the feature code's correctness by itself.

Maqsad: Land commit d48f959 ("feat(dynamic-favicon): peripheral fire-ban indicator on the
favicon") from the source worktree C:\Users\marka\code\mt-feat-dynamic-favicon-2026-06-23
onto the current HEAD of the target repo C:\Users\marka\code\mt-integration-2026-06-22,
with any conflicts resolved by keeping BOTH sides — this repo's existing unrelated changes
AND the feature's changes, never silently dropping either — every changed .js/.mjs file
passes `node --check`, the result is committed with a clear message, and the target
repo's working tree is left clean with respect to every ALLOW-FILES entry.

Context (verified against live substrate this session, 2026-07-01, absolute paths, no
prescribed resolution beyond "keep both sides"):
- Confirmed diff stat (git show --stat, this session): d48f959 touches 4 files, +311/-1 —
  icon-fire-ban.svg new (34 lines, static fallback icon for non-JS contexts), index.html
  +2/-0 (adds <link rel="icon" id="mt-favicon" href="/icon.svg" type="image/svg+xml"> and
  <script src="/js/dynamic-favicon.js" defer> in <head>), js/dynamic-favicon.js new
  (274 lines), map.html +1/-1 (the same two tags spliced into one compressed inline-script
  line — auto-merges cleanly against unrelated surrounding edits because it is a pure
  insertion at a stable anchor, not a content rewrite).
- Object reachability confirmed this session: `git cat-file -t d48f959` inside the target
  repo returns "commit" and `git branch -a --contains d48f959` shows both a local branch
  feat/dynamic-favicon-2026-06-23 (checked out in the source worktree itself) and
  remotes/github/feat/dynamic-favicon-2026-06-23. `git worktree list` confirms
  C:\Users\marka\code\mt-feat-dynamic-favicon-2026-06-23 is a worktree of the SAME
  underlying repo as C:\Users\marka\code\mt-integration-2026-06-22 — so no
  `git fetch <source-worktree-path>` step is required to make the object resolvable; a
  plain `git cherry-pick d48f959` in the target repo can resolve it directly. If that ever
  turns out false at fire time (object not found), fall back to
  `git fetch C:\Users\marka\code\mt-feat-dynamic-favicon-2026-06-23 d48f959` first.
- A dry run this session (throwaway branch off the target's then-current HEAD 28cbc72,
  deleted afterward, no lasting change) cherry-picked d48f959 cleanly: map.html
  auto-merged with ZERO conflicts, icon-fire-ban.svg and js/dynamic-favicon.js applied as
  pure adds, and `node --check js/dynamic-favicon.js` exited 0. The target repo is a live,
  actively-worked integration line — its checked-out branch was observed to swing to
  main@5e19874 and back to feat/qc-concern-sentry-error-capture-sentry-init-js-2026-06-25
  @28cbc72 within this same authoring session. It may move again before this mission
  actually fires, so a conflict is still possible — if any ALLOW-FILES entry conflicts at
  fire time, resolve it by keeping BOTH the target's existing lines and the feature's
  added lines, never `--ours` / `--theirs` and never a resolution that deletes either
  side's content.
- Target repo HEAD this session: 28cbc72 on branch
  feat/qc-concern-sentry-error-capture-sentry-init-js-2026-06-25; none of
  icon-fire-ban.svg, js/dynamic-favicon.js, index.html's favicon tags, or map.html's
  favicon tags exist there yet (checked this session — a clean landing is expected).
  This mission deliberately carries no TARGET-BRANCH header: land onto whatever branch is
  actually checked out as HEAD when this mission fires, not a hardcoded branch name.
- The target repo currently carries pre-existing untracked files unrelated to this feature
  (api-index-response.json, docs/e2e-report-2026-07-01.json, e2e-shots/,
  feature-catalog-entry.txt, scripts/e2e-runner.mjs) — these predate this mission and are
  out of scope. "Clean working tree" in this mission's Done means refers to the
  ALLOW-FILES entries this mission touches, not a demand to clear unrelated debris.
- index.html and map.html are both live pages served by the site (not build artifacts), so
  PREVIEW-BASE-URL: http://localhost:8788 is a PLACEHOLDER for a local
  `wrangler pages dev` server — the operator must confirm the actual local preview port
  before relying on the captured render; if nothing answers at PREVIEW-BASE-URL, that
  failure must surface as a named receipt, not a silently skipped check.

Steps:
  1. In the target repo, record the current branch and HEAD, and confirm none of
     icon-fire-ban.svg / js/dynamic-favicon.js already exist on disk and that index.html /
     map.html do not yet contain "mt-favicon" or "dynamic-favicon" (git -C
     C:\Users\marka\code\mt-integration-2026-06-22 status --short; git -C
     C:\Users\marka\code\mt-integration-2026-06-22 log --oneline -3; git -C
     C:\Users\marka\code\mt-integration-2026-06-22 branch --show-current).
     [command] verify-target-state
  2. Confirm the feature commit's object is resolvable without a fetch: `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 cat-file -t d48f959` should print
     "commit". If it does not, fetch it directly from the source worktree first:
     `git -C C:\Users\marka\code\mt-integration-2026-06-22 fetch
     C:\Users\marka\code\mt-feat-dynamic-favicon-2026-06-23 d48f959`.
     [command] confirm-object-reachable
  3. Cherry-pick d48f959 onto the target's current HEAD: `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 cherry-pick d48f959`. If it reports
     conflicts, resolve every conflicted file by keeping BOTH the target's pre-existing
     lines and the feature's added lines — never `--ours` / `--theirs` and never a
     resolution that deletes either side's content. If the cherry-pick comes back empty
     (already applied), resolve with `--skip`, never `--allow-empty`.
     [command] cherry-pick-favicon-feature
  4. Run `node --check js/dynamic-favicon.js` from the target repo root and confirm it
     exits 0; also run `node --check` on any other .js/.mjs file that conflict resolution
     in step 3 modified. [command] node-check-js
  5. If step 3 left the cherry-pick uncommitted (conflict resolution required `git add` +
     `git cherry-pick --continue`), commit explicitly with a message that names
     "dynamic-favicon" and/or cites d48f959. Confirm `git status --short` in the target
     repo shows no uncommitted changes to any ALLOW-FILES entry and `git log --oneline -5`
     shows the landed commit at or near HEAD. [command] commit-and-verify-clean
  6. Start (or confirm already running) a local preview server for the target repo at the
     PREVIEW-BASE-URL host:port (e.g. `wrangler pages dev`), then render both /index.html
     and /map.html in a headless browser (headless Chrome / Playwright / puppeteer) and
     capture that each page loads without a fatal script error and that
     `document.getElementById('mt-favicon')` exists in the rendered DOM of each — the
     sibling <link> node the feature adds. This is the render-witness step RULE 7 of
     mission_lint.mjs requires because VISUAL-QC-REQUIRED is set above.
     [command] headless-render-witness

Done means:
  - `git log` in C:\Users\marka\code\mt-integration-2026-06-22 shows a commit at or near
    HEAD whose message names "dynamic-favicon" and/or cites d48f959.
  - icon-fire-ban.svg and js/dynamic-favicon.js exist in the target repo with the
    feature's actual content (js/dynamic-favicon.js defines the location-derived,
    ban-type-gated favicon-data-URL mutation with its 15-minute recheck and
    visibility-change listener; icon-fire-ban.svg is the static fallback icon) —
    confirmed by reading the on-disk files, not by trusting the cherry-pick's exit code
    alone.
  - index.html contains both the new <link rel="icon" id="mt-favicon" href="/icon.svg">
    and <script src="/js/dynamic-favicon.js" defer> tags AND every pre-existing tag this
    repo's index.html already had before this mission ran (both sides present).
  - map.html contains the feature's added favicon tags AND every pre-existing
    tag/attribute this repo's map.html already had before this mission ran (both sides
    present — an auto-merge, not a replacement).
  - `node --check js/dynamic-favicon.js` exits 0, and `node --check` exits 0 on every
    other .js/.mjs file this integration touched or conflict-resolved.
  - `git status --short` in the target repo shows no uncommitted changes to any
    ALLOW-FILES entry (pre-existing unrelated untracked files noted in Context are not
    this mission's to clean up).
  - A headless browser (headless Chrome / Playwright / puppeteer) render of /index.html
    and /map.html served from PREVIEW-BASE-URL loads each without a fatal script error and
    shows `document.getElementById('mt-favicon')` present in the DOM of both — verified by
    rendering the pages, not by reading the source. If PREVIEW-BASE-URL turns out to be the
    wrong local port, that must surface as a named NO_PREVIEW_BASE_URL-style receipt, not a
    silently skipped check.
  - Nothing about this mission's own authorship or execution constitutes verification that
    the feature's code is correct (that the location proxy is accurate, that the ban-type
    predicate truly matches js/fire-ban-layer.js's rule, that the SMIL pulse renders
    correctly across real browsers, that nothing else regressed) — that determination
    belongs entirely to this mission's own witness-and-verdict-panel run, downstream of the
    receipts above.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

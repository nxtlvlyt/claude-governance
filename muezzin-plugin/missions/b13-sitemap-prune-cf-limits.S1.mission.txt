MISSION-ID: M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - workers/sitemap-prune/*.js
  - workers/sitemap-prune/wrangler.toml
PARENT: M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS  (sub-mission 1 of 2, auto-split by the size ceiling)
REQUIRES: none (first sub-mission of 2 in tartib order)

Niyyah: the sitemap-prune worker should survive production load. The original build assumed Cloudflare Workers limits the panel measured exceeded by 8x, and uncaught D1 awaits would surface as CF 1101 errors to users. Close the substrate-verified defects — not the model's confidence, the documented Cloudflare ceiling.

Maqsad: this is part 1 of 2 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): the worker runs safely on Cloudflare Workers Free plan AND Paid plan, with no silent failure modes. Re-running the 5-seat panel against the new diff returns panel_verdict in {pass, concern}.

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - Ensure the sandbox is on the target branch so all edits and the eventual push land on feat/b13-sitemap-prune-2026-06-23. Engine-run command; witness fails if HEAD is not that branch.
  - Confirm the worker source exists before authoring over it, so the single edit step regenerates a real file rather than inventing one. Fails non-zero if the file is absent.
  - SINGLE WRITER of the worker: re-author the COMPLETE workers/sitemap-prune/index.js from the existing content (listed in context_dependencies — executor emits full file, must preserve all current logic) closing every panel defect in one emission because one file has exactly one authoring step. Required closures with MANDATED greppable identifiers used by later verify steps: (a) wrap the top-level fetch() handler body in try/catch that returns a structured JSON error via a helper named EXACTLY `jsonError(` — never let a throw surface as CF 1101; (b) wrap the awaited upsert404 and writeCursor calls inside handleRefresh in try/catch returning the same `jsonError(` JSON on D1 failure; (c) ensureSchema must NOT set schemaEnsured=true unconditionally — its catch must test the caught message against /already exists|duplicate/i and RE-THROW any other error (keep the literal text `already exists` in the discriminator); (d) cap subrequests below the CF Free ceiling of 50: set `BATCH_SIZE` to a value such that worst-case fetches (~2 per item + 1) stay < 50 — use 20 — and implement a continuation/cursor pattern so a full crawl spans multiple /refresh calls instead of one oversized batch; (e) serialize concurrent /refresh: take a cursor lock and, on contention, return a structured conflict response with HTTP status `409` (keep the literal `409`) rather than racing the cursor; (f) add host validation via a helper named EXACTLY `assertSameHost(` that rejects sitemap entry URLs whose host is outside the site domain (SSRF guard); (g) replace brittle regex sitemap parsing with CDATA- and namespace-tolerant extraction. Witness: node --check parses the rewritten file.
  - Witness defect 4 closed: BATCH_SIZE is bounded so worst-case subrequests stay under the CF Free limit of 50. Asserts BATCH_SIZE is set to a value in 1..24; fails non-zero if it is 200 or any unbounded value.
  - Witness defects 1 and 2 closed: the structured JSON error path exists. Asserts the mandated `jsonError(` helper is present and therefore referenced from the wrapped handlers; fails non-zero if absent (meaning a throw could still surface as CF 1101).
  - Witness defect 3 closed: ensureSchema no longer masks unknown errors as already-existing. Asserts the message-discriminating literal `already exists` is present in the catch logic; fails non-zero if the discriminator was not added.
  - Witness concern 6 closed: concurrent /refresh returns a structured conflict instead of a corrupted cursor. Asserts the HTTP 409 conflict response literal is present; fails non-zero if absent.
  - Witness concern 7 closed: out-of-domain sitemap URLs are rejected (SSRF guard). Asserts the mandated `assertSameHost(` helper is present; fails non-zero if absent.

Done means: steps M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1.1..M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1.8 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — - The worker, when invoked, returns structured JSON on D1 failure rather than CF 1101.
  - Per /refresh subrequest count is mathematically provable to stay under 50 (Free plan ceiling).
  - ensureSchema does NOT mask unknown errors as "already exists".
  - Concurrent /refresh calls either serialize cleanly OR return a structured conflict response (no race-corrupted cursor).
  - URL host validation rejects out-of-domain sitemap entries.
  - Same panel re-fire returns pass/concern; node --check passes; push + ls-remote verify.

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
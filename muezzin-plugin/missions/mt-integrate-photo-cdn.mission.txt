MISSION MT-INTEGRATE-PHOTO-CDN
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: main
ALLOW-FILES:
  - functions/api/contribute/photo/[key].js
REQUIRES: none

Niyyah: Feature commit dff609e ("feat(photo-cdn): wire Cloudflare Images for POI
photo serving") sits on its own branch in the source worktree
C:\Users\marka\code\mt-feat-photo-cdn, never landed on the target repo's
integration line. This mission's entire job is mechanical: get that one new
file onto the target repo's main branch without losing anything already
there, and produce the node-level receipt a verdict panel needs to judge the
result. Authoring this mission, and the act of integrating itself, is NOT
verification that the feature is correct. Whether the key-regex
(^spot/\d+/[A-Za-z0-9._-]+$) actually matches every legitimate key and rejects
every illegitimate one, whether the transform-hint parsing (w, q, f, dpr, fit)
degrades safely on malformed query values, whether routing through
/cdn-cgi/image/ versus the raw contribute-worker fetch is chosen correctly,
whether the hardcoded contribute-worker URL is the right one and does not leak
anything it shouldn't, and whether the no-hints fallback path is truly
byte-identical to the prior behavior — none of that is settled by landing the
commit. That determination belongs entirely to this mission's own witness and
verdict-panel run, downstream of this mission. Say plainly: authoring and
integrating this mission proves nothing about the feature code's correctness
by itself.

Maqsad: Land commit dff609ebc00ad3b1a0a259eaffec757323171f31 from its source
worktree onto the target repo's main branch, producing
functions/api/contribute/photo/[key].js in the target repo with a clean
working tree afterward, and a passing node --check on the one .js file this
commit touches, resolving any conflict by keeping BOTH the target's existing
unrelated changes and this feature's changes, never silently dropping either
side.

Context (verified against live substrate 2026-07-01, absolute paths, no
prescribed resolution beyond "keep both sides"):
- Source worktree C:\Users\marka\code\mt-feat-photo-cdn, branch feat/photo-cdn,
  confirmed via `git worktree list` in the target repo to carry commit
  dff609e at its HEAD, and via `git log --oneline` in that worktree.
- Confirmed diff stat for dff609e this session (`git show --stat`): 1 file
  changed, 281 insertions(+), 0 deletions(-) — a single brand-new file,
  functions/api/contribute/photo/[key].js. No other file is touched. This
  diff touches zero .html files, so no VISUAL-QC-REQUIRED header or
  headless-render witness step is needed by this mission.
- The source worktree and the target repo are worktrees of the same
  underlying git repository (confirmed via `git worktree list`, and
  `git cat-file -t dff609e` succeeding directly inside the target repo) —
  commit dff609e is already a reachable object in the target repo without
  any fetch/remote-add step; `git cherry-pick dff609e` works directly.
- Target repo TARGET-BRANCH main, this session's HEAD (`git log main
  --oneline -1`): 5e19874 ("Merge branch
  'feat/qc-concern-vehicle-profile-picker-2026-06-25'"). dff609e is NOT yet
  an ancestor of main (confirmed via `git merge-base --is-ancestor dff609e
  main`, exit 1). Neither functions/api/contribute/photo/ nor the target
  file exists on main yet (confirmed via `git ls-tree main --
  functions/api/contribute/photo/` returning nothing, and `git cat-file -e
  main:functions/api/contribute/photo/[key].js` failing).
- A dry-run performed this session (`git checkout main && git cherry-pick -n
  dff609e`) applied cleanly with ZERO conflicts (`git status --porcelain`
  showed exactly `A  functions/api/contribute/photo/[key].js` plus
  pre-existing untracked litter unrelated to this feature, nothing else
  staged or modified), then was reset (`git reset --hard main`) and the
  repo returned to its prior checked-out branch before this mission was
  authored, so main is unchanged.
- The repo's checked-out HEAD at authoring time (test-lhci-dryrun-2026-07-01)
  carries pre-existing untracked litter unrelated to this feature
  (api-index-response.json, docs/e2e-report-2026-07-01.json, e2e-shots/,
  feature-catalog-entry.txt, scripts/e2e-runner.mjs), confirmed still present
  after the main dry-run checkout in this same session, so it is untracked
  litter repo-wide, not branch-scoped; switching to TARGET-BRANCH main will
  not collide with it. This mission's "clean working tree" claim is scoped
  to the file it touches (the ALLOW-FILES entry and anything a merge
  conflict forces it to touch) — it does not require deleting, committing,
  or otherwise disturbing that pre-existing untracked litter, since it is
  out of this mission's scope and not this mission's to clean up.
- No QC, test, or witness process has verified any of the feature's content
  as of this writing. This mission's authorship is a description of diff
  contents and a dry-run git mechanics check only.

Steps:
  1. In the target repo, ensure TARGET-BRANCH main is checked out at its
     current HEAD, and check whether dff609e is already present via
     `git merge-base --is-ancestor dff609e main` (exit 0 means already
     integrated). [command] check-already-integrated
  2. If not already integrated: `git cherry-pick dff609e` onto main directly
     from the target repo's own object database (the commit is already
     reachable there — no fetch/remote-add step is needed; the source
     worktree C:\Users\marka\code\mt-feat-photo-cdn is read-only reference,
     never rewritten). [command] integrate-commit
  3. If the cherry-pick reports a conflict on functions/api/contribute/photo/[key].js
     or any other path, resolve each conflicted hunk by hand so the resolved
     content contains BOTH the target's pre-existing content AND this
     feature's new content — never `--ours` / `--theirs` and never a
     resolution that deletes either side's lines. Since the file is
     brand-new, no natural conflict is expected for this specific commit,
     but this rule must be honored if one appears. If the cherry-pick
     instead reports the change is already applied, skip creating a commit
     for this step and proceed — that outcome is a pass, not a failure.
     [edit] resolve-conflicts
  4. Run `node --check functions/api/contribute/photo/[key].js` from the
     target repo root (the one .js file this commit touches) and confirm it
     exits 0. If step 3 touched any other .js/.mjs file to resolve a
     conflict, run `node --check` on each of those too. [command]
     node-check-all
  5. If step 2/3 produced a new commit, commit explicitly with a message
     that references the source commit's own subject line ("feat(photo-cdn):
     wire Cloudflare Images for POI photo serving"). If step 1 already found
     the commit present, no new commit is needed. Either way, confirm
     `git status --short` in the target repo is empty and `git log --oneline
     -5` on main shows the landed commit. [command] commit-and-verify-clean

Done means:
  - functions/api/contribute/photo/[key].js exists on disk in the target
    repo on branch main, and its on-disk content contains an onRequestGet
    handler that validates the key against the regex
    ^spot\/\d+\/[A-Za-z0-9._-]+$, parses optional w/q/f/dpr/fit query params,
    and either fetches the raw photo from the contribute-worker URL (no
    transform hints) or proxies through /cdn-cgi/image/ (hints present) —
    verified by reading the file's actual content, not by trusting the
    cherry-pick's exit code alone.
  - `node --check functions/api/contribute/photo/[key].js` exits 0.
  - `git log --oneline` on the target repo's main branch, filtered for
    "photo-cdn", returns a match — confirming commit dff609e (or its
    equivalent content landed by this mission) is present in main's history,
    checked directly rather than assumed.
  - `git status --short` in the target repo shows a clean working tree (no
    uncommitted changes, no unmerged paths) at the end of the mission, and
    none of the unrelated dry-run artifacts named in Context above were
    added, modified, or committed by this mission.
  - None of the above constitutes a judgment that the key-regex, transform-
    hint parsing, cdn-cgi/image routing choice, or no-hints fallback
    behavior are correct, safe, or behave as the commit message claims —
    that determination belongs entirely to this mission's own witness +
    verdict-panel run downstream, not to this mission's authorship or
    mechanical execution.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

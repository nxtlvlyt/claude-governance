MISSION-ID: mt-integrate-partner-embed-iframe-verification.S1
MISSION-CLASS: code-repo
PARENT: mt-integrate-partner-embed-iframe-verification
TARTIB-INDEX: 1 of 2
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - embed/map.html
  - docs/PARTNERS-EMBED-IFRAME.md
VISUAL-QC-REQUIRED
REQUIRES: none (first in tartib order)
STEPS: 8

PARENT MAQSAD: The commit "16803c9 feat(partner-embed-iframe): embeddable /embed/map.html for

Maqsad: sub-mission 1 of 2 — Confirm the live checked-out branch and HEAD in the target repo, confirm no cherry-pick/merge is already in progress, and confirm embed/map.html, docs/PARTNERS-EMBED-IFRAME.md, and the embed/ directory do not already exist. Repo is volatile (branch changes observed between checks by other missions) — land on whatever is actually checked out at fire time, no assumed TARGET-BRANCH. through Read-only confirmation that embed/map.html's landed content actually matches what's claimed — leaflet@1.9.4 via unpkg, a `<div id="mt-embed-map">` render target, and a fetch against /pois.json — by reading the file rather than trusting the cherry-pick's exit code alone. This step does not modify the file.

Steps:
1. Record the live checked-out branch and HEAD in the target repo, and report — informationally, never as an abort-on-exists condition — whether a cherry-pick/merge is already in progress and whether the source commit is already integrated (its ancestor-status stands in for embed/map.html, docs/PARTNERS-EMBED-IFRAME.md, and the embed/ directory already existing, since that commit is what creates them). Repo is volatile (branch changes observed between checks by other missions) — land on whatever is actually checked out at fire time, no assumed TARGET-BRANCH. Realize as literal idempotent PowerShell only (no Test-Path-then-exit-1 preflight on the deliverable files — that pattern fails forever once this mission's own work lands, per d1-backup.S1 / plan-day-gpx-export precedent): git rev-parse --abbrev-ref HEAD; git rev-parse HEAD; if ((Test-Path .\.git\CHERRY_PICK_HEAD) -or (Test-Path .\.git\MERGE_HEAD)) { Write-Output "MID-FLIGHT-CHERRY-PICK-OR-MERGE" } else { Write-Output "NO-MID-FLIGHT-OP" }; if (git merge-base --is-ancestor 16803c959990ef6ac25c4fec86b2c9e70ad7428d HEAD) { Write-Output "ALREADY_INTEGRATED" } else { Write-Output "CLEAR-TO-PROCEED" }
  2. Check whether commit 16803c959990ef6ac25c4fec86b2c9e70ad7428d is already an ancestor of the live HEAD confirmed in step 1. If already-an-ancestor, step 3's cherry-pick must be treated as a no-op (commit already landed); if not-an-ancestor, proceed to step 3 normally. Realize as literal stdout-only PowerShell with no scratch file (never write step2-marker.txt or any file outside ALLOW-FILES): git merge-base --is-ancestor 16803c959990ef6ac25c4fec86b2c9e70ad7428d HEAD; if ($LASTEXITCODE -eq 0) { Write-Output "ALREADY-ANCESTOR" } else { Write-Output "NOT-ANCESTOR" } [command]
  3. Cherry-pick commit 16803c959990ef6ac25c4fec86b2c9e70ad7428d onto whatever HEAD step 1 confirmed (skip the pick if step 2 reported already-an-ancestor). This diff is two pure adds (embed/map.html, docs/PARTNERS-EMBED-IFRAME.md) so a clean apply is expected; if git reports the change already applied, resolve with --skip (never --allow-empty). Do NOT hand-resolve conflicts in this step — conflicts on embed/map.html or docs/PARTNERS-EMBED-IFRAME.md are resolved individually in steps 4 and 5. [command] 
  4. If step 3 reported a conflict on embed/map.html, hand-resolve it so the resolved file contains BOTH this repo's pre-existing content at that path (if any) AND the full partner-embed-iframe page (leaflet@1.9.4 via unpkg with integrity hash, `<div id="mt-embed-map">`, a /pois.json fetch, bbox/node_type/host query-param handling) — never --ours/--theirs, never dropping either side's content. If step 3 applied cleanly with no conflict on this file (the expected outcome for a pure add), this step performs no edit and only confirms the landed content has no leftover conflict markers and contains the expected container id. [edit] embed/map.html
  5. If step 3 reported a conflict on docs/PARTNERS-EMBED-IFRAME.md, hand-resolve it so the resolved file contains BOTH sides' documentation content in full, none dropped — never --ours/--theirs. If step 3 applied cleanly with no conflict on this file (the expected outcome for a pure add), this step performs no edit and only confirms the landed content has no leftover conflict markers and documents the iframe embed pattern. [edit] docs/PARTNERS-EMBED-IFRAME.md
  6. Enumerate every .js/.mjs file touched by the landed commit (HEAD~1..HEAD) or by conflict resolution in steps 4/5. This diff as authored contains zero such files — embed/map.html's script is inline HTML and the doc is markdown — so an empty enumeration is the expected, correct receipt; state that explicitly rather than skipping. If conflict resolution introduced or modified any .js/.mjs file, each one must additionally be run through node --check before this step is considered witnessed. Realize as literal stdout-only PowerShell with no scratch file (never write js-enumeration.txt or any file outside ALLOW-FILES): git diff --name-only HEAD~1..HEAD -- '*.js' '*.mjs' — empty output is the expected, correct receipt for this diff. [command]
  7. Finalize the integration commit (git cherry-pick --continue if steps 4/5 resolved a mid-flight conflict; otherwise the cherry-pick in step 3 already produced the commit) with a message referencing 'feat(partner-embed-iframe): embeddable /embed/map.html for partner sites' and/or SHA 16803c9. Confirm git status --short shows no uncommitted changes to embed/map.html or docs/PARTNERS-EMBED-IFRAME.md (pre-existing unrelated untracked debris in the repo is out of scope) and the commit sits at or near HEAD. [command] 
  8. HEADLESS RENDER WITNESS (amended 2026-07-12 after verify-consensus REJECT F1: the prior grep-shaped verify contradicted this mission's own Done-means — "render, not reading the code"; the atv-11 witness lesson is the recorded class: presence/grep witnesses pass broken pages). In ONE command: write scratch-embed-qc.mjs that (a) starts a node http server over the repo root (serving embed/map.html and /pois.json), (b) puppeteer-renders http://127.0.0.1:<port>/embed/map.html, (c) waits up to 15s for BOTH `document.querySelector('#mt-embed-map')` to exist AND (`window.L` to be defined OR `#mt-embed-map .leaflet-container, #mt-embed-map.leaflet-container` to match — leaflet actually booted, not merely referenced), (d) prints EMBED-RENDER-OK plus the matched evidence, else prints EMBED-RENDER-FAIL with the page console errors and exits 1; then node scratch-embed-qc.mjs; then Remove-Item scratch-embed-qc.mjs -ErrorAction SilentlyContinue (scratch never ships — repo tree stays clean). Network note: leaflet loads from unpkg — a network failure prints EMBED-RENDER-FAIL with the console error verbatim, which is the honest receipt, never a green stamp. [command]

Done means: embed/map.html, docs/PARTNERS-EMBED-IFRAME.md exist/are updated as specified — verify by headless-browser render, not by reading the code

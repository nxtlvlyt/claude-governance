MISSION SOTA-PIPELINE-DOSSIER
MISSION-CLASS: research
REQUIRES: search (every market/SOTA comparison claim must carry a live search receipt).
Niyyah: the operator asked (2026-06-11): "a very thorough document stating how state-of-the-art our pipeline is going to be after ingesting all of this information." The ingestion corpus exists as panel-verified cards; this mission is the synthesis — what the factory now KNOWS, how that compares to the 2026 market, and what is honestly still missing. A synthesis from conductor memory would be drift; this one is built from staged receipts.
Maqsad: ONE artifact — SOTA-PIPELINE-DOSSIER.md at the sandbox root. PLAN SHAPE: (1) COMMAND gather steps copy the evidence into the sandbox with flat names, each shaped EXACTLY as: if (Test-Path '<src>') { Copy-Item '<src>' '<dst>' } — sources: every missions/card-*/card-*.md and missions/*/MUDDYTIRES-TASKS.md sibling DONE artifacts (card-2boots, card-regulativity, card-half-a-vanlife, card-half-b-muddytires, card-merge output if present, card-tidaltreasures output if present), missions/m28-0-directionrv-study/*.md, missions/fb-backlog-priorities/fb-backlog-priority-card.md, missions/books-website-knowledge output if present, E:\AI_Storage\website-pipeline\MASTER-PLAN.md, E:\AI_Storage\website-pipeline\docs\patterns\auth-membership-pattern.md, E:\AI_Storage\website-pipeline\docs\OPERATIONAL-ARTIFACTS-DOCTRINE.md; (2) ONE command step produces line-NUMBERED copies of the gathered files (Get-Content <f> | ForEach-Object { $i++; "L$i`: $_" } | Set-Content <f>.numbered.md) so claims can carry line cites; (3) ONE edit step authors the COMPLETE dossier with sections: A. WHAT THE PIPELINE KNOWS (per ingested source: the capabilities/patterns it contributed, each claim cited [<staged>.numbered.md Lnn]); B. ENGINE-BY-ENGINE SOTA ASSESSMENT (site engine, portal engine, image engine, motion engine, design ideation, visual QC — per MASTER-PLAN L2; for each: what we have vs what the 2026 market standard is, market claims web-verified with source_url receipts); C. THE MOAT (why chained governed local/cloud seats + the three-layer revenue model beat template-flippers — argued from evidence, not enthusiasm); D. HONEST GAPS (what is NOT yet ingested or built, named plainly — an absent capability stated as absent is a PASS, dressed up as present is an arkan violation); E. VERDICT (a one-paragraph answer to the operator's literal question). Inference and external knowledge marked "inference, not in source" / "external, search-verified: <url>". Done means: SOTA-PIPELINE-DOSSIER.md exists, markdown-valid, every Section-A claim line-cited to a staged numbered source, every Section-B market claim carries a search receipt URL, Section D non-empty, and the artifact never claims an ingestion that has no staged card.
Context: this mission runs LAST in the queue by design — gather steps tolerate absent sources (if/Test-Path), so it synthesizes whatever HAS landed and names the rest in Section D. Sandbox writes only.
Amanah: entrusted; discharge with ihsan.

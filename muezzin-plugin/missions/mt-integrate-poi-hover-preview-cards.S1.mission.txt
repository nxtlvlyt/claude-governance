MISSION-ID: MT-INTEGRATE-POI-HOVER-PREVIEW-CARDS.S1
MISSION-CLASS: code-repo
PARENT: MT-INTEGRATE-POI-HOVER-PREVIEW-CARDS
TARTIB-INDEX: 1 of 2
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - js/poi-hover-preview.js
  - map.html
VISUAL-QC-REQUIRED
REQUIRES: none (first in tartib order)
STEPS: 8

PARENT MAQSAD: The commit "3b8bb06 feat(map): POI hover-preview cards (desktop hover, mobile

Maqsad: sub-mission 1 of 2 — Confirm the target repo's actually-checked-out branch and HEAD; explicitly checkout TARGET-BRANCH if a different branch is checked out (closes the documented branch-race hazard); confirm js/poi-hover-preview.js does not already exist on disk. through Create the sandbox receipts directory that the visual-witness step will write into.

Steps:
  1. Confirm the target repo's actually-checked-out branch and HEAD. Run this literal command: if ((git rev-parse --abbrev-ref HEAD) -ne 'main') { git checkout -q main }; git merge-base --is-ancestor 3b8bb06819a8bfb20bf95788cd8d239f56ce6e7e HEAD; if ($LASTEXITCODE -eq 0) { Write-Output ALREADY_INTEGRATED; exit 0 } -- this checks out main (the confirmed real branch; resolves the prior unresolved TARGET-BRANCH placeholder and closes the documented branch-race hazard) only if a different branch is checked out, and short-circuits cleanly via $LASTEXITCODE if the feature commit is already integrated (git merge-base --is-ancestor prints no stdout either way, so a bare `if (git merge-base ...)` would always evaluate false regardless of true ancestry -- $LASTEXITCODE is required for the check to mean anything); also confirm js/poi-hover-preview.js does not already exist on disk. [command]
  2. In the source worktree, confirm commit 3b8bb06819a8bfb20bf95788cd8d239f56ce6e7e is a real, reachable commit object without modifying that worktree. [command] 
  3. Cherry-pick commit 3b8bb06 onto the target repo's confirmed HEAD; if git auto-resolves hunks but leaves CHERRY_PICK_HEAD set (non-zero exit despite no unmerged paths), finalize the pick in the same command. Asserts js/poi-hover-preview.js exists afterward. [command] js/poi-hover-preview.js
  4. Ensure map.html's script-tag chain contains BOTH the pre-existing tags and the new poi-hover-preview.js tag (resolve by hand if the cherry-pick left conflict markers on this line; keep both sides, never --ours/--theirs). Witness asserts exactly one occurrence of the new tag reference. Run this literal command to guard against a stale/pre-existing-commit false receipt (never trust a bare git rev-parse HEAD value as proof this step landed): $m = git show HEAD --stat | Select-String 'poi-hover-preview.js'; if (-not $m) { throw 'STALE RECEIPT: HEAD does not touch poi-hover-preview.js -- re-run this step for real' } [edit] map.html
  5. Confirm no unmerged paths remain after conflict resolution, before syntax-checking anything. [command] 
  6. Syntax-validate the new feature file with node --check (exits non-zero on any syntax error or missing file). [command] 
  7. If the integration is left uncommitted, commit it referencing the source subject line and SHA; then assert the working tree is clean, scoped to this mission's ALLOW-FILES only (never a bare whole-tree check). Run this literal PowerShell-native command (no bash-style && / || wrapping parenthesized groups): git diff --quiet HEAD; if ($LASTEXITCODE -eq 0) { Write-Output ALREADY_COMMITTED } else { git add js/poi-hover-preview.js map.html; git commit -m "Integrate POI hover-preview cards (cherry-pick 3b8bb06819a8bfb20bf95788cd8d239f56ce6e7e)" }; git diff --quiet HEAD -- js/poi-hover-preview.js map.html; if ($LASTEXITCODE -ne 0) { throw "ALLOW-FILES not clean vs HEAD after commit" } [command]
  8. Create the sandbox receipts directory that the visual-witness step will write into. [command] 

Done means: js/poi-hover-preview.js, map.html exist/are updated as specified — verify by headless-browser render, not by reading the code

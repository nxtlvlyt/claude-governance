MISSION-ID: M-MUDDYTIRES.QC-CONCERN-SHARE-LINK-CONTROL-SHARE-CONTROL-JS-2026-06-25.S1
MISSION-CLASS: code-repo
PARENT: M-MUDDYTIRES.QC-CONCERN-SHARE-LINK-CONTROL-SHARE-CONTROL-JS-2026-06-25
TARTIB-INDEX: 1 of 2
REQUIRES: none (first in tartib order)
STEPS: 8

PARENT MAQSAD: GATE: Share button appears next to Save button in popup — FAIL: No POI popup is visible in either screenshot, so the button presence cannot be verified. GATE: Hashchange handler scans #spot=<id> and flyTo+openPopup — FAIL: The map remains at a national zoom level in both shots with no popup triggered or marker focused. GATE: Slate-colored chip distinct from forest-green save button — FAIL: The share button and save button are not visible in the provided screenshots. GATE: Flash toast confirms 'Link copied' — FAIL: No flash toast notification appears in the final screenshot. Confirm share-control.js mounts a slate-colored share button next to the forest-green save button inside every POI popup, that copying the link emits a 'Link copied' flash toast, and that the page-load hashchange handler reads #spot=<id>, flies the map to that marker, and opens its popup. Restore behavior to that specified in docs/FEATURE-CATALOG-2026-06-23.md.

Maqsad: sub-mission 1 of 2 — Confirm the sandbox is checked out on the correct TARGET-BRANCH before any edit begins, given a branch of this exact name already exists on disk pointing at unrelated history (commit fbe0533) — a pure precondition check, not a git-surgery step. through Regression guard for GATE 1: confirm the .mt-save-btn button-placement reference was not disturbed while fixing color/flash text.

Steps:
  1. Confirm the sandbox is checked out on the correct TARGET-BRANCH before any edit begins, given a branch of this exact name already exists on disk pointing at unrelated history (commit fbe0533) — a pure precondition check, not a git-surgery step. [verify] 
  2. Edit js/share-control.js: recolor all forest-green tokens (#1d6f3a, #2e6b3a, rgba(29,111,58,...)) to a single consistent slate/gray-blue value across all four button-state CSS rules (base .mt-share-link-btn, hover, .is-copied active-fill, .mt-share-link-flash text), and change the copy-success flash() call to pass the literal string 'Link copied' instead of the raw copied URL. Preserve the existing injectShareButton() placement logic (insert after .mt-save-btn) and the hashchange router (parseSpotHash/applySpot/tryApply -> flyTo/openPopup) untouched, and keep the trailing CSS rule-concatenation intact so no rule (including .mt-share-link-ico) is silently dropped the way commit 20c6510 dropped it. [edit] js/share-control.js
  3. Verify valid JavaScript syntax after the edit. [verify] js/share-control.js
  4. Verify no forest-green color token survives anywhere in share-control.js, across all four button-state rules. [verify] js/share-control.js
  5. Verify the .mt-share-link-btn.is-copied active-fill rule is still present (not dropped the way commit 20c6510's broken concatenation dropped trailing rules). [verify] js/share-control.js
  6. Verify the .mt-share-link-flash text rule is still present (not dropped). [verify] js/share-control.js
  7. Verify the copy-success flash call now passes the literal phrase 'Link copied' instead of the raw copied URL. [verify] js/share-control.js
  8. Regression guard for GATE 1: confirm the .mt-save-btn button-placement reference was not disturbed while fixing color/flash text. [verify] js/share-control.js

Done means: js/share-control.js exist/are updated as specified — verify by headless-browser render, not by reading the code

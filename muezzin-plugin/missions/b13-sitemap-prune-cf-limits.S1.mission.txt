MISSION-ID: M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - workers/sitemap-prune/wrangler.toml
PARENT: M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS  (sub-mission 1 of 2, auto-split by the size ceiling)
REQUIRES: none (first sub-mission of 2 in tartib order)

Niyyah: the sitemap-prune worker should survive production load. The original build assumed Cloudflare Workers limits the panel measured exceeded by 8x, and uncaught D1 awaits would surface as CF 1101 errors to users. Close the substrate-verified defects — not the model's confidence, the documented Cloudflare ceiling.

Maqsad: this is part 1 of 2 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): the worker runs safely on Cloudflare Workers Free plan AND Paid plan, with no silent failure modes. Re-running the 5-seat panel against the new diff returns panel_verdict in {pass, concern}.

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - Author the complete sitemap-prune worker with ALL substrate-verified defects closed in one emission (one-writer rule: every code finding lives in this single implementation file, so it is authored exactly once). Read the existing file first (listed in context_dependencies) and regenerate full contents preserving all working logic, applying: (a) finding 2 — wrap the top-level exported fetch() body in try/catch returning Response.json({ok:false,error},{status:500}) so no throw reaches the runtime as CF 1101; (b) finding 1 — wrap awaited upsert404 and writeCursor (and any awaited D1 op) inside handleRefresh in try/catch returning {ok:false,stage:'db_write',error} as structured JSON; (c) finding 3 — in ensureSchema, set schemaEnsured=true ONLY on genuine success or a string-matched benign /already exists/i (or duplicate-column) error, and rethrow every other error instead of masking it; (d) finding 4 — replace BATCH_SIZE=200 with BATCH_SIZE=40 and add a continuation cursor so worst-case external fetches per /refresh = 1 sitemap-index fetch + 40 URL fetches = 41 < 50 (CF Free external-subrequest ceiling, search-confirmed by all three architects), returning {ok:true,done:false,nextCursor} when more URLs remain; (e) finding 7 — before any sitemap-entry fetch(), parse via new URL() and reject (skip+count) entries whose hostname is not the configured SITE_HOST (or a subdomain) and reject non-http(s) schemes; (f) finding 5 — replace brittle regex extraction with parsing tolerant of CDATA-wrapped and namespaced <loc>/<ns:loc> elements; (g) finding 6 — serialize concurrent /refresh via a D1 atomic lock (CREATE TABLE IF NOT EXISTS _refresh_lock + INSERT OR IGNORE compare-and-set with a stale-lock reclaim window), returning Response.json({ok:false,error:'refresh_in_progress'},{status:409}) when the lock is held, releasing in a finally. Filename index.js is the convergent guess of all three blind reads (UNVERIFIED — file_read unavailable; node --check fails closed if the entry file differs).
  - Author wrangler.toml (separate file = its own writer) preserving all existing bindings and adding (1) a SITE_HOST [vars] entry the SSRF host check validates against, and (2) a comment pinning the 'CF Free plan = 50 external subrequests/request' assumption that the BATCH_SIZE math depends on, so a future Paid migration is a deliberate edit and not silent drift. Do NOT add a limits block that would mask the Free ceiling. UNVERIFIED: current binding set (file_read unavailable) — regenerate from the read of the existing file.
  - Syntax gate: the whole worker module parses cleanly after the edit (fails non-zero on any syntax error or a missing/wrong entry filename).
  - Witness the subrequest-ceiling math (finding 4): assert a BATCH_SIZE constant exists and BATCH_SIZE+1 < 50 so per-/refresh external fetches are provably under the CF Free ceiling. Fails non-zero if the constant is absent or too large.
  - Witness the error-contract fixes (findings 1,2,3): assert the module contains try/catch blocks, a structured error status (500 or 409), and a benign-only ensureSchema match (/already exists/). Fails non-zero if any marker is absent.
  - Witness the SSRF host validation (finding 7) and robust XML parsing (finding 5): assert use of new URL(), a hostname check, and CDATA handling. Fails non-zero if any marker is absent.
  - Witness the concurrency-lock fix (finding 6): assert an atomic D1 lock primitive (INSERT OR IGNORE or a lock construct) AND a structured 409 conflict response exist. Fails non-zero if either is absent.
  - Maqsad acceptance gate: re-fire the 5-seat QC panel harness against the updated worker and require panel_verdict in {pass, concern}. Positive-assertion: Select-String -Quiet exits non-zero when no pass/concern verdict is found (a 'fail' verdict or absent output fails the step). Harness path and exact flags are the absolute external reference from the mission (UNVERIFIED invocation signature — executor confirms against qc-harness-v2.mjs).

Done means: steps M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1.1..M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1.8 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — - The worker, when invoked, returns structured JSON on D1 failure rather than CF 1101.
  - Per /refresh subrequest count is mathematically provable to stay under 50 (Free plan ceiling).
  - ensureSchema does NOT mask unknown errors as "already exists".
  - Concurrent /refresh calls either serialize cleanly OR return a structured conflict response (no race-corrupted cursor).
  - URL host validation rejects out-of-domain sitemap entries.
  - Same panel re-fire returns pass/concern; node --check passes; push + ls-remote verify.

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
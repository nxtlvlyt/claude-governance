MISSION mt-integrate-contact-spam-score-qc-pipeline-docs
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: main
ALLOW-FILES:
  - docs/QC-PIPELINE-SOTA-2026-06-23.md
REQUIRES: none

Niyyah: The feature commit "375e40b docs(qc): SOTA-uplift QC pipeline — 8 mandatory
gates + escape hatches" (slug contact-spam-score-qc-pipeline-docs, source worktree
C:\Users\marka\code\contact-spam-score-qc-pipeline-docs) is a docs-only addition that
must land on the target repo's main branch without losing any of the target's own
unrelated history. This mission's entire job is mechanical integration — get the
commit (or an equivalent of its content) onto the target branch, prove the result is
syntactically sound, and leave the tree clean. Nothing this mission does constitutes a
judgment that the QC pipeline described in the doc (the 8 gates, the foreign-frontier
policy, the escape hatches) is the RIGHT pipeline, complete, or safe to adopt as
written. That determination belongs to this mission's own witness + verdict-panel run,
which reviews the integrated result after this mission produces it — not to the act of
integration itself. Authoring or executing this mission is not verification of the
feature's content.

Maqsad: The commit "375e40b docs(qc): SOTA-uplift QC pipeline — 8 mandatory gates +
escape hatches" is cherry-picked or merged from the source worktree onto the current
HEAD of C:\Users\marka\code\mt-integration-2026-06-22's main branch, with any conflicts
resolved by keeping BOTH sides — this repo's existing unrelated changes AND the QC
pipeline doc's changes, never silently dropping either — every .js/.mjs file this
integration touches or conflict-resolves passes `node --check`, the result is committed
with a clear message (or the prior commit is confirmed already present if it turns out
to already be integrated), and the target repo's working tree is left clean (git status
shows no uncommitted changes to the touched files).

Context (verified against live substrate 2026-06-30/07-01, absolute paths, no
prescribed resolution beyond "keep both sides"):
- Confirmed diff stat for 375e40b (git show --stat, this session): 1 file changed, 24
  insertions(+), 0 deletions(-) — a single new file, docs/QC-PIPELINE-SOTA-2026-06-23.md.
  No HTML file is touched by this diff, so this mission carries no headless-render
  witness gate — that requirement only applies to missions that touch rendered pages.
- Because the file is new (not a modification of an existing target file), a
  cherry-pick of 375e40b has no natural conflict surface for THIS specific commit — the
  "keep both sides" instruction below exists for the general case (the target branch
  may have added its own docs/QC-PIPELINE-SOTA-2026-06-23.md independently by the time
  this mission fires) and must still be honored if that case triggers.
- Verified this session: 375e40b is ALREADY an ancestor of the target repo's main
  branch (`git merge-base --is-ancestor 375e40b main` succeeds), and main's current
  docs/QC-PIPELINE-SOTA-2026-06-23.md already matches 375e40b's content exactly
  (`git diff 375e40b -- docs/QC-PIPELINE-SOTA-2026-06-23.md` run on main produces no
  output). The integration may already be satisfied by the time this mission actually
  runs. This is NOT grounds to skip execution outright — main can move, be reset, or
  diverge before this mission fires — but it IS grounds for this mission to treat
  "already-integrated" as a valid completion path rather than an error: an empty
  cherry-pick (git reporting the change is already applied) must be treated as success,
  confirmed via the Done-means checks below, not assumed and not failed on.
- The source worktree named in this mission's brief, C:\Users\marka\code\contact-spam-score-qc-pipeline-docs,
  was not present on disk at authoring time (2026-06-30/07-01 session). The commit
  375e40b is nonetheless reachable from the target repo's OWN history (it is already an
  ancestor of main, per above) and independently from at least two other local
  worktrees of this same repository family — C:\Users\marka\code\mt-feat-b13-contact-spam
  and C:\Users\marka\code\mt-feat-contact-spam-score-2026-06-23 — both confirmed this
  session to carry 375e40b in their history. If the named source worktree does not
  exist when this mission fires, resolve the commit directly from the target repo's own
  history (it is already there) or, failing that, from either fallback worktree named
  above — a missing source-worktree directory is not by itself a mission failure when
  the commit is otherwise resolvable.

Steps:
  1. In the target repo C:\Users\marka\code\mt-integration-2026-06-22, on TARGET-BRANCH
     main at its current HEAD, check whether 375e40b is already present via
     `git merge-base --is-ancestor 375e40b HEAD` (exit 0 means already integrated).
     [command] check-already-integrated
  2. If step 1 reports NOT already integrated: locate the commit — first check the
     source worktree C:\Users\marka\code\contact-spam-score-qc-pipeline-docs for
     375e40b; if that path does not exist, resolve 375e40b directly from the target
     repo's own object database (it is already there) or from
     C:\Users\marka\code\mt-feat-b13-contact-spam or
     C:\Users\marka\code\mt-feat-contact-spam-score-2026-06-23. Then apply it via
     `git cherry-pick 375e40b` on TARGET-BRANCH main. [command] integrate-commit
  3. If cherry-pick reports conflicts on docs/QC-PIPELINE-SOTA-2026-06-23.md or any
     other ALLOW-FILES entry, resolve each conflicted hunk by hand so the resolved
     content contains BOTH the target's pre-existing change AND the QC-pipeline doc's
     change — never `--ours` / `--theirs` and never a resolution that deletes either
     side's content. If the cherry-pick instead reports the change is already applied
     or produces an empty commit, skip creating a commit for this step and proceed —
     that outcome is a pass, not a failure. [edit] resolve-conflicts
  4. Identify every .js/.mjs file this integration actually touched or conflict-
     resolved (the confirmed diff stat for 375e40b touches none). Run `node --check` on
     each such file and confirm exit 0. If the touched-file set is empty, record that
     explicitly as the check result rather than silently skipping the step.
     [command] node-check-all
  5. If step 2/3 produced a new commit, stage and commit the integrated result in the
     target repo with a clear message identifying the integrated feature and its
     origin commit (referencing "contact-spam-score-qc-pipeline-docs" and "375e40b").
     If step 1 already found the commit present, no new commit is needed. Either way,
     confirm `git status` afterward shows a clean working tree.
     [command] commit-and-verify-clean

Done means:
  - docs/QC-PIPELINE-SOTA-2026-06-23.md exists in the target repo on TARGET-BRANCH main
    with non-trivial content: it contains the header "QC Pipeline (post-SOTA-uplift
    2026-06-23)" and lists all 8 numbered mandatory gates (endpoint runtime,
    cross-project leak detection, headless console errors, Sentry loader present, the
    5-seat deliberation panel, visual regression, Lighthouse score regression, and
    bundle-size regression) plus the escape-hatches section beneath them.
  - `git log` on the target repo's main branch shows commit 375e40b present in history
    (whether it landed there before this mission ran or as a direct result of this
    mission's cherry-pick), with its message "docs(qc): SOTA-uplift QC pipeline — 8
    mandatory gates + escape hatches" intact — confirmed by `git log --oneline | grep
    375e40b` returning a match, not assumed.
  - This diff touches zero .js/.mjs files (confirmed via `git show --stat 375e40b`), so
    the `node --check` gate is satisfied by explicitly recording that empty touched-file
    set — not by a fabricated "ran node --check and it passed" claim against files that
    were never part of this diff. If any conflict resolution in Step 3 introduced or
    modified a .js/.mjs file, `node --check` on that file must exit 0 instead.
  - `git status` in the target repo (main branch) shows a clean working tree (no
    uncommitted changes) at the end of the mission.
  - None of the above constitutes a judgment that the QC pipeline documented in
    docs/QC-PIPELINE-SOTA-2026-06-23.md is itself correct, complete, or safe to adopt
    (e.g. that the 5-seat panel composition or the escape-hatch policy is right) — that
    determination belongs to this mission's own witness + verdict-panel run downstream,
    not to this mission's authorship or mechanical execution.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

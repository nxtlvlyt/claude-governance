MISSION-ID: mt-plan-mode-mobile.S1
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - map.html
  - js/plan-day.js
VISUAL-QC-REQUIRED
REQUIRES: mt-mobile-lane-fix.S1 (tartib — the top-left lane fix lands first; this mission covers the plan-mode surfaces it does not)
STEPS: 6

Maqsad: Operator screenshot 2026-07-03 ~13:28 (plan mode, ~430px-class Android viewport) shows
SIX live defects; this mission fixes the five plan-mode/copy ones (the sixth — chip/zoom
overlap — is the sibling lane-fix + detector pair's scope):
(D1) ARRIVAL BANNER CLIPS: the green "Arrive at HH:MM with ..." banner runs off the right
edge with no wrap. Fix: at narrow widths the banner wraps its text (white-space normal) and
is capped to the viewport (max-width with margins), never clipped.
(D2) TOAST COVERS THE OPEN POPUP: the fuel-price warning toast rendered on top of the leg
popup the user had open. Fix: position the plan-mode warning toast so it does NOT overlap an
open Leaflet popup — anchor it to the top of the map area below the arrival banner (a fixed
lane of its own), never centered over map content.
(D3) BOTTOM-RIGHT STACK COLLISION: the gear/settings FAB sits half-hidden under the EXIT
PLAN button's ring. Fix: in plan mode, stack EXIT PLAN and the gear with explicit
non-overlapping offsets (same additive media-rule pattern as the lane fix).
(D4) RAW FIELD NAME IN USER COPY: the warning text prints "totals.fuel_cost_cad is a lower
bound" — developer vocabulary in a user-facing message (js/plan-day.js warning strings).
Fix: rewrite to plain language, e.g. "the fuel total shown is a minimum — some provinces had
no price data" (keep the honest lower-bound meaning EXACTLY; change only the words).
(D5) The same warning stack shows two ⚠ lines with overlapping meaning; merge into one
plain sentence when both fire for the same cause (NRCan outage).
STYLE CONSTRAINT: all map.html changes are ADDITIVE-ONLY — one new self-contained
<style id="mt-plan-mode-mobile"> block appended immediately before </head> (same receipted
pattern as mt-mobile-lane-fix; nothing already present is modified or deleted). js/plan-day.js
changes are string-level rewrites of the warning copy only.

niyyah: The operator sent one screenshot and it carried six defects — five of them ship-week
regressions in the feature he uses most (the planner). Every fix here anchors on the literal
id "mt-plan-mode-mobile" or an exact string so edit and verify cannot disagree. The plan-mode
surfaces get the same robots-check-the-phone treatment as the rest of the map.

Steps:
  1. Idempotent baseline: record branch + HEAD; if map.html already contains id="mt-plan-mode-mobile" output CLEAN_ALREADY and treat edit steps as verify-only presence checks; else CLEAN_BASELINE. Never exit 1 on existence. [command]
  2. map.html: APPEND the <style id="mt-plan-mode-mobile"> block immediately before </head> — one @media (max-width: 520px) rule-set covering D1 (banner wrap + viewport cap), D2 (warning-toast lane anchored below the banner, above map content, no popup overlap), D3 (EXIT PLAN + gear explicit non-overlapping offsets + z-order). Additive only; existing selectors; nothing already present modified. [edit] map.html
  3. js/plan-day.js: rewrite the fuel-warning strings per D4/D5 — plain language, honest lower-bound meaning preserved verbatim in sense, no raw field names in any user-visible string (the literal string "totals.fuel_cost_cad" must no longer appear in USER-FACING message text; it may remain in code/comments). [edit] js/plan-day.js
  4. Verify: map.html contains the literal id mt-plan-mode-mobile inside <head> AND git diff --stat shows map.html additions-only; js/plan-day.js no longer emits "totals.fuel_cost_cad" inside any quoted user-message string (Select-String receipts for each). [verify]
  5. git commit both ALLOW-FILES by explicit name (git add map.html js/plan-day.js; git commit -- map.html js/plan-day.js -m "<message naming this mission and the operator-screenshot receipt>"); confirm the commit is on HEAD. [command]
  6. OUTCOME CHECK: in ONE self-contained command (the proven shape): confirm git status --porcelain -- map.html js/plan-day.js is empty, wrangler pages deploy . --project-name=muddytires --branch=preview, capture the emitted https://<subdomain>.muddytires.pages.dev URL, poll "$url/map" until HTTP 200 (up to 10x5s), write scratch-planmode-qc.mjs via here-string. PIN THE EXACT LITERAL (the proven working pattern used by mt-enrich-scenic-osm.S2, mt-integrate-dynamic-favicon-fire-ban.S2, mt-integrate-partner-widget.S2, mt-integrate-poi-hover-preview-cards.S2 and mt-integrate-spot-share-card.S2 — all five use this exact ESM shape, never CommonJS require, because scratch-*.mjs files run in ES module scope where require is not defined): the file's FIRST line MUST be exactly `import { chromium } from "playwright";` followed by top-level await code (const b = await chromium.launch(); const ctx = await b.newContext({ viewport: { width: 430, height: 932 }, isMobile: true }); const p = await ctx.newPage(); const errs = []; p.on("pageerror", (e) => errs.push(String(e))); await p.goto(process.argv[2] + "/map", { waitUntil: "load", timeout: 60000 }); const html = await p.content(); const present = html.includes('id="mt-plan-mode-mobile"'); await b.close(); console.log("STYLE_BLOCK=" + (present ? "present" : "absent")); console.log("PAGEERRORS=" + errs.length); if (present && errs.length === 0) console.log("PLANMODE_QC_PASS"); if (!present || errs.length > 0) process.exit(1);). Set-Content scratch-planmode-qc.mjs -Value $js; run with node scratch-planmode-qc.mjs $url (pass the URL as argv[2], not an env var — matches the pinned script above), then Remove-Item the scratch. The printed evidence lines ARE the render receipt. [command]

Done means: map.html (additive mt-plan-mode-mobile block) and js/plan-day.js (plain-language
fuel warnings, no raw field names in user copy) committed at HEAD; the step-6 evidence lines
show STYLE_BLOCK=present and PAGEERRORS=0 against the auto-provisioned preview at the
operator's viewport class — executable + headless-browser render verification (live-outcome
bar; operator-screenshot receipt 2026-07-03).

MISSION PORTAL-OUTAGE-TRIAGE
MISSION-ID: 22
MISSION-CLASS: research
REQUIRES: none — inputs PRE-STAGED in missions/portal-sota-review/ (OPERATIONS.md 10,055b, STATE-PORTAL.md 129,105b, SUBSTRATE.md, ROADMAP.md, SPECS.md — cat'd live from the NAS web root 2026-06-10 ~18:05) + the container-census receipt below.
CONTAINER CENSUS RECEIPT (conductor, live docker ps 2026-06-10 17:55): RUNNING = overpass-canada, cmd-console-server, syncthing, prowlarr, sabnzbd, lidarr, bazarr, tautulli, radarr, sonarr, rdtclient, homarr, calibre-web, o-webui, tts-bridge, anythingllm (CRASH-LOOPING: Restarting), ollama, overpass-ca, kiwix-serve, jellyfin. NOT RUNNING (absent entirely): cloudflared, comfyui-urmomis, urmomis-web, urmomis-hyperframes, nxtlvl-portal, nxtlvl-portal-db, mailserver, nextcloud, portainer, openedai-speech, open-webui(?). nxtlvl.studio itself: HTTP 200 (site UP — Synology serves it; only the PORTAL machinery is down).
Niyyah: the operator's image portal (the business's product: automatic social-media images) has been silently dead since the NAS event — the storefront answers HTTP 200 so nothing LOOKED broken. Silent product outages are found by census, not by complaints.
Maqsad: ONE artifact — portal-outage-triage.md at the sandbox root, reading the staged docs (plan ONLY reads of staged files + ONE emit step). Sections: (1) WHAT THE PORTAL STACK IS — from OPERATIONS/SUBSTRATE: which containers compose the image portal (web, db, comfyui, hyperframes, mailserver?) and what each does, cited; (2) DAMAGE ASSESSMENT — cross-reference the census receipt against the documented stack: every component down, and what OPERATIONS.md says about its data/volumes; (3) RESTART RUNBOOK — the ordered restart per OPERATIONS.md (compose files, dependencies, healthchecks), each step marked auto-runnable (a future command-step mission via SSH is NOT possible for seats — mark these CONDUCTOR) vs operator-only (sudo/Docker UI); (4) DATA-LOSS RISK — what state lives in the down containers (portal DB! queued jobs?) and whether the docs name backups; (5) the AnythingLLM crash-loop: what OPERATIONS says about its config (it serves the books RAG); (6) OPEN QUESTIONS, one line each. Done = card exists, markdown-valid, claims cited to staged files or the census receipt verbatim.
Context: staged docs read-only. The triage card is the only write target, cwd-relative.
Amanah: entrusted; discharge with ihsan.

MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.RESILIENCE.1
REPO-ROOT: E:\AI_Storage\muddytires-pages
REQUIRES: GitHub auth (operator-identity-bound: his GitHub account, OR a GITHUB_TOKEN / gh-authed CLI in env) — verify before firing
# URGENT, LOW-RISK. This is the real answer to the operator's "in case we lose my NAS or files."
# Verified 2026-06-15: muddytires-pages has NO git remote (local-only on E:, last commit 00ca348);
# the backend PHP source (~19 files) is at E:\AI_Storage\muddytires-backend (off the NAS but local-only);
# pois.json (2.7MB data snapshot, dated Jun 14) is in the deploy. If the E: drive dies, ONLY the
# already-deployed static files on Cloudflare survive — the source + backend would be lost. This mission
# removes that single point of failure. It is BACKUP, not a rebuild — no PHP→Workers porting here.

Niyyah: make every part of muddytires recoverable WITHOUT the NAS or the local E: drive — so losing both costs nothing. Today the site source, the backend code, and the data live only on E: with no remote copy; that is the exposure the operator is worried about.

Maqsad: every muddytires artifact (static site, backend source, POI data) exists in at least one location that is NOT the NAS and NOT the local disk — recoverable from GitHub + Cloudflare alone.

Context (verified, read the receipts):
- `E:\AI_Storage\muddytires-pages` = the deployed static site (index.html, pois.json 2.7MB, images). `git remote -v` is EMPTY → local-only. Last commit 00ca348 "stage-1: static pois.json fallback".
- `E:\AI_Storage\muddytires-backend` = the full PHP backend (~19 files: oracle, feed, planner, contribute, contribute_ocr, contribute_voice, contribute_submit, community_edit, search, review, pois, add, add-note, about, guides, partners, landing, sitemap, subscribe). Off the NAS, but local-only.
- The live site (muddytires.ca) is already on Cloudflare Pages, so the RUNTIME is NAS-independent; the GAP is the SOURCE + data have no off-machine backup.

Done means (verifiable):
- A PRIVATE GitHub repo exists holding the static site + the backend source + pois.json, pushed and confirmed (`git remote -v` shows the GitHub origin; `git ls-remote` succeeds; the latest commit is on GitHub).
- Optionally: pois.json + the image assets copied to a Cloudflare R2 bucket (second off-machine copy of the heavy data).
- A short RECOVERY.md in the repo: how to redeploy from scratch (wrangler pages deploy) + where the data + backend live.

SECURITY GATE (per reference_static_deploy_security_checklist.md): the backend .php reads keys via env() — NO real secret VALUES or .env files may be committed. Scan before push (grep for ANTHROPIC_API_KEY=/AIML_API_KEY=/QDRANT_API_KEY= with a literal value, any .env). Repo MUST be PRIVATE (the backend logic + the MODERATOR_PIN flow should not be public). The client-side MapTiler key in index.html is already public-by-design (domain-restrict in the MapTiler dashboard — operator-identity-bound, separate).

Amanah: this is the safety net. Discharge it cleanly; the witness is `git ls-remote` succeeding against GitHub + a from-scratch redeploy working from the cloned repo.

Constraints: PRIVATE repo; no secret values committed; NAS UNTOUCHED; commit --no-verify (laguna pre-commit hook hangs).

# FOLLOW-ON (separate, bigger): M-MUDDYTIRES.RESILIENCE.2 = the FULL dynamic site on Cloudflare
# (port the ~19 PHP endpoints → Cloudflare Workers/Pages Functions, POI data → Cloudflare D1, secrets →
# Workers secrets). That makes the INTERACTIVE features (contribute, community edit, oracle, planner, feed)
# work without the NAS. It is a real staged BUILD — the hard parts are oracle.php (AI + Qdrant vector
# search) and the contribute OCR/voice flow — and the muezzin engine is not yet reliable enough to build it
# autonomously (executor-edit completion gap). Spec it separately + scope before running; do NOT bundle here.

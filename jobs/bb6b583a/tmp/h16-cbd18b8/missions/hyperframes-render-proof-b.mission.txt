MISSION: hyperframes-render-proof-b — agy's flagship claim, witnessed or refuted (part-file shape)
MISSION-CLASS: research  # validator mode: .md deliverables count as targets (verification intent stated in Maqsad)

Niyyah: hyperframes-render-proof FAILED x2 (2026-06-12 05:29) on TWO receipted causes, neither of them the research itself: (1) its emit step demanded render-proof.md COMPLETE in one emission — the single-emission ceiling that killed m28-1, with the part-file cure already proven (books-website-knowledge DONE); attempt 2 truncated mid-sentence at the section-4 command. (2) The verdict overreached its evidence — the label said BLOCKED-ON-pip-install while the body listed three blockers, and "not confirmed installed" was leapt to "BLOCKED" without a probe receipt. This mission supersedes the parent with the part-file shape and an evidence-bounded verdict contract. The salvage question is unchanged: agy-era work asserted "Mission 38 Phase 2 complete" without witnesses; the video pipeline is the single highest-value salvage IF it actually renders.

Maqsad: ONE assembled artifact — render-proof.md at the sandbox root, built from FOUR part files, recording a witnessed runway assessment of E:\AI_Storage\agy-video-pipeline (read-only source; work in the sandbox). PLAN SHAPE: (1) COMMAND gather steps stage the evidence, each shaped EXACTLY as: if (Test-Path '<src>') { Copy-Item '<src>' '<dst>' } — sources: E:\AI_Storage\agy-video-pipeline\README.md and E:\AI_Storage\agy-video-pipeline\requirements.txt; (2) COMMAND probe steps record the runway WITHOUT installing anything heavy: python --version and node --version into runway.txt, pip list into piplist.txt, and Get-ChildItem of E:\AI_Storage\agy-video-pipeline\engine into engine-listing.txt; then ONE command step produces runway.numbered.txt and piplist.numbered.txt by prefixing each line with Lnn (the line-cite bar requires the numbered sources); (3) FOUR SEPARATE edit steps author one part file each, every part a SMALL emission: part-1-claims.md = what the pipeline claims, quoting README; part-2-runway.md = what the probes show, every statement citing [runway.numbered.txt Lnn] or [piplist.numbered.txt Lnn] — a dependency is PRESENT, ABSENT, or UNPROBED, never assumed; part-3-commands.md = the EXACT command sequence a render attempt needs, cited to README and engine-listing.txt; part-4-verdict.md = verdict whose label is RUNWAY-READY or BLOCKED-ON-<list> where <list> names EVERY blocker the body establishes (a label naming fewer blockers than the body is a contract violation), each blocker carrying its probe cite, ending with the SINGLE next concrete command; (4) ONE command step assembles: writes a '# Render Proof — agy video pipeline' header then concatenates part-1 through part-4 into render-proof.md; (5) ONE command step validates with Select-String that the assembled file contains all four section parts and either RUNWAY-READY or BLOCKED-ON. Done means: render-proof.md exists at the sandbox root, validation confirms the four parts and a verdict label, every runway claim cites runway.txt or piplist.txt, and the verdict label matches the body's blocker list exactly. (The actual MP4 render is the conductor's follow-up deed with these findings — a seat cannot run a 5-min render; the mission maps the runway honestly.)
Context: read-only source tree; sandbox writes only. No installs.
Amanah: entrusted; discharge with ihsan.

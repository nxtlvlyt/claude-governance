MISSION-ID: M-MUDDYTIRES.B13-TRIP-DIARY-SECURITY-HARDEN.S2
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - trip-edit.html
PARENT: M-MUDDYTIRES.B13-TRIP-DIARY-SECURITY-HARDEN  (sub-mission 2 of 2, auto-split by the size ceiling)
REQUIRES: predecessor M-MUDDYTIRES.B13-TRIP-DIARY-SECURITY-HARDEN.S1 DONE (tartib — sub-mission 2 runs after 1 of 2)

Niyyah: harden the trip-diary feature against the 5-seat SOTA panel findings on the original Sonnet build (wdsq4mxb2 sha — BLOCK verdict at panel_verdict=block, confidence 0.88, 4 of 5 seats agreeing). Apply standard web-security hygiene: CORS allowlist, CSRF defense, transaction wrap, error handling, XSS encoding, race fix on MAX(ord). Substrate-honest: the original feature WORKS (substrate checks all passed), but the panel correctly flagged 5 real security defects that ship-ready code should not have.

Maqsad: this is part 2 of 2 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): patch the existing branch feat/b13-trip-diary-user-2026-06-23 with the 5 corrective patches below. Re-run all the original verification steps + a panel re-check at end (call deliberationPanelReview from C:/Users/marka/code/mt-audit/qc-harness-v2.mjs against the new diff; aim for panel_verdict=pass or concern). Push the result to the same branch as a follow-up commit.

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - XSS hardening in trip-edit.html (the in-scope name for the findings' 'trip-editor.html'): same audit/fix as trips.html for dynamically rendered stop fields (name, note) and preview labels — textContent or escapeHtml(), no raw user innerHTML (#5). Form input.value assignments are already XSS-safe and need no escaping.
  - Aggregate syntax gate: node --check every touched .js file in one pipeline; fail (exit 1) on the first file with a syntax regression before the expensive panel call.
  - Panel re-check gate: call deliberationPanelReview from the external mt-audit harness against the new diff of the branch. Assert panel_verdict is 'pass' or 'concern' (mission DONE / DONE-with-caveat); exit non-zero on 'block', missing verdict, or harness error so the engine surfaces BLOCKED-WITH-RECEIPT instead of looping. (Exact arg shape of deliberationPanelReview is UNVERIFIED — file_read unavailable on this dispatch; executor adapts the call signature to the harness's actual export while keeping the pass/concern positive-assertion.)
  - Stage each touched file individually (no -A), commit the follow-up with --no-verify, push to the same existing branch with --no-verify, then assert the remote branch sha equals local HEAD (positive assertion — fails if the push did not land).

Done means: steps M-MUDDYTIRES.B13-TRIP-DIARY-SECURITY-HARDEN.S2.1..M-MUDDYTIRES.B13-TRIP-DIARY-SECURITY-HARDEN.S2.4 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — panel re-check returns pass or concern; all 7 panel findings have visible code-change addressing them in the diff; CORS allowlist concrete; CSRF defense named in comments; race + txn primitives used; XSS escape function visible; outer try/catch in fetch().

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
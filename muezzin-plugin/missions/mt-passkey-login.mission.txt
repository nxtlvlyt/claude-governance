MISSION mt-passkey-login
MAQSAD (the end): muddytires accounts support passkey (WebAuthn) sign-in —
multiple devices enrolled per account, so the operator and his wife each log in
with a platform authenticator tap on the SAME shared account, no password typed
or shared. Password login remains as fallback.

NIYYAH (why): operator ask 2026-07-18 ("share an account with my wife... make
our login features more SOTA"). Passkeys are the 2026 gold standard: multiple
credentials per account is exactly the shared-household shape — share the
account, never a secret.
source: FEATURE-timeline-ingestion-2026-07-18.md (session context) + js/account.js
(current email+password auth, mt_session_token) + functions/api/[[path]].js
(AUTH proxy route).

REQUIRES: read js/account.js + the auth worker source (AUTH base target in
functions/api/[[path]].js) BEFORE design. The auth worker owns the credential
store; passkey tables (credential id, public key, counter, user id) land there.

STEPS (part-file discipline; each step's artifact committed before the next):
1. [read-only] Map the auth worker's current session/user schema + endpoints;
   produce PASSKEY-DESIGN.md: registration ceremony (navigator.credentials.create),
   login ceremony (navigator.credentials.get), challenge storage, credential
   table DDL, and the exact new endpoints (/auth/passkey/register-options,
   /auth/passkey/register, /auth/passkey/login-options, /auth/passkey/login).
   NO external auth service — self-hosted WebAuthn on the existing worker.
2. Worker: credential table migration + the four endpoints; challenges are
   single-use + short-TTL; verify sig counter monotonic; store multiple
   credentials per user.
3. Frontend: profile page "Add a passkey to this account" enrollment button +
   login modal "Sign in with passkey" path (js/account.js extension, feature-
   detect PublicKeyCredential; graceful absence on old browsers).
4. Tests: worker source-scan + ceremony behavior-sim (challenge reuse rejected,
   wrong-origin rejected, counter regression rejected). Selftest per repo
   convention (workers/_test_*.mjs + tests/*.selftest.js).
5. Live witness: register a passkey on the preview deploy, sign out, sign back
   in with it. Deploy through the five-guard chain.

DONE-MEANS: a second device can be enrolled on an existing account and sign in
with no password; all tests green; live-witnessed on preview; five-guard prod
ship receipt.

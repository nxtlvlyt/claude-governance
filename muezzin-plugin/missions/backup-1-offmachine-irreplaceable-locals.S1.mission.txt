MISSION-ID: M-BACKUP.OFFMACHINE.1.S1
MISSION-CLASS: build-program (command-class — copy/transfer + verify; the engine's proven command path, NOT code-repo edits)
PARENT: M-BACKUP.OFFMACHINE.1  (sub-mission 1 of 2, auto-split by the size ceiling)
REQUIRES: none (first sub-mission of 2 in tartib order)

Niyyah: the operator's two most-exposed local-only artifacts — the muddytires database cold backup and the dji_test

Maqsad: this is part 1 of 2 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): each of the two artifacts below exists in at least one off-machine location (R2 or Google Drive), verified

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - Probe R2 control-plane reachability/auth (wrangler OAuth is live and buckets enumerate). HALT-with-receipt if auth error.
  - Probe the R2 DATA-PLANE: an rclone S3 remote named 'r2' (separate from wrangler OAuth) must exist to multipart-upload the 1.4G tar AS-IS, since wrangler put caps at 300 MiB. Absence => HALT naming missing scope: R2 S3 Access Key (Object Read&Write).
  - Ensure a PRIVATE destination bucket 'muezzin-backups' exists (R2 buckets are private by default — satisfies the no-public-bucket constraint).
  - Pre-flight: confirm the local DB tar exists and its size is in the expected ~1.4G gz range before any upload (Discovery Gate on the absolute reference path).
  - Upload the DB tar to R2 AS-IS via rclone multipart (NOT re-packed, NOT chunked into separate objects) — the only constraint-honoring path for a 1.4G object given the wrangler 300 MiB cap.
  - WITNESS the DB tar off-machine: a real remote listing showing the object present with its byte size matching the local tar (a listing, never an 'upload ran' log line).
  - Build the SMALL editor-source archive by include-allowlist (.py .mjs .md .json .toml + ledgers) excluding footage/renders/weights/node_modules, written to the cwd sandbox.
  - Prove exclusion from the archive's OWN file list: no video/weight extensions or node_modules present AND total size is the small source set (<500MB), not the 29G whole.

Done means: steps M-BACKUP.OFFMACHINE.1.S1.1..M-BACKUP.OFFMACHINE.1.S1.8 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — (verifiable):
- A REMOTE LISTING (R2 or Google Drive) shows the DB tar object present at the destination with its size matching
  the local 1.4G gz — not a log line saying upload ran.
- A REMOTE LISTING shows the editor-source archive at the destination, AND the archive's own file list proves
  footage/renders/weights/node_modules were excluded (it is the small source set, not the 29G whole).
- BACKUP-RECOVERY.md exists and names, for each artifact, the exact off-machine location + the one-command restore,
  with every named path resolving.
- If a destination credential is missing for either artifact, that stage is HALTED with a receipt naming the exact
  missing scope/account — a halted-with-receipt stage is an honest outcome, a silently-skipped one is a failure.

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
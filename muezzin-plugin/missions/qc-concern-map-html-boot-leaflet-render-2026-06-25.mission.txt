MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.QC-CONCERN-MAP-HTML-BOOT-LEAFLET-RENDER-2026-06-25
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: feat/qc-concern-map-html-boot-leaflet-render-2026-06-25
ALLOW-FILES:
  - js/*.js
  - workers/**/*.js

Niyyah: address the QC CONCERN named "map.html boot + Leaflet render" from the 2026-06-25 walk.

Maqsad: Leaflet tiles load, POI markers populate within 3s, and window.map/window.L exist all PASS — but the walk surfaced 2x console.error 401 entries during boot that downgraded the verdict to concern. Identify the unauthenticated fetch(es) firing during map boot (likely a user-scoped endpoint hit before auth is checked) and gate them behind a logged-in check or swap to a public endpoint so the boot console is clean for guests. Restore behavior to that specified in docs/FEATURE-CATALOG-2026-06-23.md.

Done means: e2e-runner walk returns {clean}; the concern is resolved; no other features regress.

Amanah: this slice is part of the muddytires.ca quality-uplift backlog.

MODEL-ROUTING: TIER_CLOUD_CODE — kimi-k2.7-code or deepseek-v4-pro for fix authoring.

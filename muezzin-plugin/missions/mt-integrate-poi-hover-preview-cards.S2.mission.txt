MISSION-ID: MT-INTEGRATE-POI-HOVER-PREVIEW-CARDS.S2
MISSION-CLASS: code-repo
PARENT: MT-INTEGRATE-POI-HOVER-PREVIEW-CARDS
TARTIB-INDEX: 2 of 2
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - receipts/poi-hover-preview-visual-witness.md
REQUIRES: missions/mt-integrate-poi-hover-preview-cards.S1.mission.txt (tartib SATISFIED: S1 DONE 2026-07-18T00:36Z)
STEPS: 1

PARENT MAQSAD: The commit "3b8bb06 feat(map): POI hover-preview cards (desktop hover, mobile

Maqsad: sub-mission 2 of 2 — Because map.html changed and VISUAL-QC-REQUIRED is set: reach PREVIEW-BASE-URL, capture a headless-browser render of map.html, and write a receipt. Reads map.html only (does not re-author it) and fails closed with a named NO_PREVIEW_BASE_URL / VISUAL_WITNESS_FAILED receipt rather than silently skipping if the preview server or screenshot is absent. through Because map.html changed and VISUAL-QC-REQUIRED is set: reach PREVIEW-BASE-URL, capture a headless-browser render of map.html, and write a receipt. Reads map.html only (does not re-author it) and fails closed with a named NO_PREVIEW_BASE_URL / VISUAL_WITNESS_FAILED receipt rather than silently skipping if the preview server or screenshot is absent.

Steps:
  9. Render witness (literal payload pinned 2026-07-18 after MIQAT grep-only refusal; the seven-times-proven deploy+scratch template; receipt committed under receipts/ per the wildlife precedent). Run exactly: $out = npx wrangler pages deploy . --project-name=muddytires --branch=preview 2>&1 | Out-String; $m = [regex]::Match($out, 'https://[a-z0-9-]+\.muddytires\.pages\.dev'); if (-not $m.Success) { Write-Output NO_PREVIEW_BASE_URL; exit 1 }; $url = $m.Value; Write-Output "DEPLOYED: $url"; $ok = $false; for ($i=0; $i -lt 10; $i++) { try { $r = Invoke-WebRequest -Uri "$url/map.html" -UseBasicParsing -TimeoutSec 10; if ($r.StatusCode -eq 200) { $ok = $true; break } } catch {}; Start-Sleep -Seconds 5 }; if (-not $ok) { Write-Output POLL-FAILED; exit 1 }; New-Item -ItemType Directory -Force receipts | Out-Null; $js = 'import { chromium } from "playwright"; const b = await chromium.launch(); const ctx = await b.newContext(); const p = await ctx.newPage(); const errs = []; p.on("pageerror", (e) => errs.push(String(e))); await p.goto(process.argv[2] + "/map.html", { waitUntil: "load", timeout: 60000 }); const html = await p.content(); const wired = html.includes("poi-hover-preview") || html.includes("mt-hover-card"); await b.close(); console.log(JSON.stringify({ wired, fatalErrors: errs.length })); if (!wired || errs.length > 0) process.exit(1);'; Set-Content scratch-poihover-witness.mjs -Value $js; $wOut = node scratch-poihover-witness.mjs $url 2>&1 | Out-String; $rc = $LASTEXITCODE; Remove-Item scratch-poihover-witness.mjs -ErrorAction SilentlyContinue; Write-Output $wOut.Trim(); if ($rc -ne 0) { Write-Output VISUAL_WITNESS_FAILED; exit 1 }; Set-Content receipts/poi-hover-preview-visual-witness.md -Value ("# POI hover-preview visual witness`n" + (Get-Date).ToUniversalTime().ToString('o') + " preview=" + $url + " receipt=" + $wOut.Trim()); git add -- receipts/poi-hover-preview-visual-witness.md; git commit -m "receipt(poi-hover): headless render witness (mt-integrate-poi-hover-preview-cards.S2)" -- receipts/poi-hover-preview-visual-witness.md; Write-Output POIHOVER-WITNESSED [command]

Done means: receipts/poi-hover-preview-visual-witness.md exists at HEAD carrying the live render receipt (hover-card wiring present in served markup, zero fatal page errors, preview URL recorded) — verified by headless-browser render, not by reading the code

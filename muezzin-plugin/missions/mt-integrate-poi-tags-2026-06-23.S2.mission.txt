MISSION-ID: MT-INTEGRATE-POI-TAGS-2026-06-23.S2
MISSION-CLASS: code-repo
PARENT: MT-INTEGRATE-POI-TAGS-2026-06-23
TARTIB-INDEX: 2 of 2
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - map.html
VISUAL-QC-REQUIRED
REQUIRES: MT-INTEGRATE-POI-TAGS-2026-06-23.S1 (tartib — this sub-mission may not start until MT-INTEGRATE-POI-TAGS-2026-06-23.S1's receipt exists)
STEPS: 8

PARENT MAQSAD: Land commit a2ecb2a ("feat(poi-tags): free-text user tags on POIs and community

Maqsad: sub-mission 2 of 2 — Edit map.html to merge in the poi-tags feature's three hunks while preserving every pre-existing line: (1) insert `<script src="js/poi-tags.js" defer></script>` into the existing script-tag chain between js/poi-photos.js and js/crown-land-overlay.js; (2) inside popup(p), add `(typeof window.mtTagsHtml==='function'?window.mtTagsHtml(p,'poi'):'')` to the returned HTML string between the existing provenanceHtml and mtShareExportHtml calls; (3) inside communityPopup(p,la,lo), add `window.mtTagsHtml(p,'spot')` at the equivalent position. Do not remove or alter any other existing script tag or popup-builder content. through Visual-witness the integrated map.html: request it from PREVIEW-BASE-URL (requires a local static server such as wrangler pages dev already bound at that URL) and assert a 200 response with non-empty content. If unreachable, this step's non-zero exit is the NO_PREVIEW_BASE_URL receipt rather than a silent skip.

Steps:
  9. Edit map.html to merge in the poi-tags feature's three hunks while preserving every pre-existing line: (1) insert `<script src="js/poi-tags.js" defer></script>` into the existing script-tag chain between js/poi-photos.js and js/crown-land-overlay.js; (2) inside popup(p), add `(typeof window.mtTagsHtml==='function'?window.mtTagsHtml(p,'poi'):'')` to the returned HTML string between the existing provenanceHtml and mtShareExportHtml calls; (3) inside communityPopup(p,la,lo), add `window.mtTagsHtml(p,'spot')` at the equivalent position. Do not remove or alter any other existing script tag or popup-builder content. [edit] map.html
  10. Syntax-check js/poi-tags.js after restoration. [verify] 
  11. Syntax-check functions/api/poi-tags.js after restoration. [verify] 
  12. Syntax-check functions/api/poi-tags/aggregate.js after restoration. [verify] 
  13. Syntax-check functions/api/poi-tags/suggest.js after restoration. [verify] 
  14. Stage all six ALLOW-FILES entries and commit the integration with a message naming the feature and source SHA a2ecb2a. [command] 
  15. Confirm the working tree is clean with respect to all six ALLOW-FILES entries after commit (scoped check, not a whole-tree gate — pre-existing unrelated untracked litter is out of scope). [verify] 
  16. Visual-witness the integrated map.html: request it from PREVIEW-BASE-URL (requires a local static server such as wrangler pages dev already bound at that URL) and assert a 200 response with non-empty content. If unreachable, this step's non-zero exit is the NO_PREVIEW_BASE_URL receipt rather than a silent skip. [command] 

Done means: map.html exist/are updated as specified — verify by headless-browser render, not by reading the code

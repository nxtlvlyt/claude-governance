MISSION: auth-pattern-sota-check — the factory's auth pattern, held to 2026 SOTA
MISSION-CLASS: research
REQUIRES: search

Niyyah: the operator's standing rule (2026-06-11): donor code and patterns are
"all things to learn from to build the most state-of-the-art pipeline" — adopted
patterns must face a SOTA check, not be grandfathered in on working-when-built. The
auth-membership pattern (donor: CGSports v2, ~1900 LOC w/ tests) lifts FAITHFULLY
first; this mission decides what gets sharpened AFTER the lift.

Maqsad: ONE artifact — auth-sota-verdict.md.
PLAN SHAPE (gather then author):
- COMMAND step stages the pattern doc:
    Copy-Item 'E:\AI_Storage\website-pipeline\docs\patterns\auth-membership-pattern.md' .
- ONE emit step authors the verdict with the staged doc as context_dependency, using
  LIVE WEB SEARCH (2025-2026 sources, URLs cited) for the SOTA side.
Sections:
1. THE DONOR BASELINE — what the pattern has (from the staged doc, cited [auth-membership-pattern.numbered.md Lnn]).
2. SOTA SURVEY (search-grounded): passkeys/WebAuthn adoption state; magic links vs
   passwords for small-business audiences; session-store vs token tradeoffs in 2026;
   the auth-library landscape (Auth.js, Lucia successor state, better-auth) vs
   keep-own-passport-stack; Stripe billing-portal + entitlement patterns now.
3. KEEP / SHARPEN / REPLACE map — every donor feature gets one verdict with reasoning
   (e.g. the 3-strike IP lock: keep? industry replaced with device fingerprints?).
4. THE PICK — one upgrade path for /platform/core/auth with effort estimate, honoring:
   lift-faithfully-first is already law; this plans the SECOND pass.
Done means: auth-sota-verdict.md with all four sections; every SOTA claim carries a
live URL; every donor claim cites [auth-membership-pattern.numbered.md Lnn]. Arafat: section 4 ends in
ONE pick with reasoning — a menu without a pick fails.

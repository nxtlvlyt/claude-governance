MISSION-ID: M-CORPUS.COMPLETE.1.S3
MISSION-CLASS: build-program (multi-stage; decompose under the Hajj mission-architecture — Architect splits, no hand-authored steps)
PARENT: M-CORPUS.COMPLETE.1  (sub-mission 3 of 5, auto-split by the size ceiling)
REQUIRES: predecessor M-CORPUS.COMPLETE.1.S2 DONE (tartib — sub-mission 3 runs after 2 of 5)

Niyyah: the factory's paid-for knowledge is useless while it is scattered and incomplete — six cards in six folders

Maqsad: this is part 3 of 5 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): a single, indexed, queryable knowledge base distilled from EVERY source in the P0-CORPUS census (the

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - STAGE 3 — Probe AnythingLLM (localhost:3001) and create the 'nxtlvl-knowledge' workspace, persisting the response (must contain a slug) to workspace.json. API key read from $env:ANYTHINGLLM_API_KEY (key location per memory feedback_anythingllm_rag_config is UNVERIFIED on this dispatch — if the var is unset the step fails closed, which is correct). ONE WRITER for workspace.json.
  - STAGE 3 — Upload every library card plus INDEX.md and PATTERNS.md to AnythingLLM and embed them into the nxtlvl-knowledge workspace (POST /document/upload then POST /workspace/nxtlvl-knowledge/update-embeddings), logging to ingest.log. Then GET the workspace and assert its documents array is non-empty (positive assertion — empty embed fails). Reads workspace.json for the slug.
  - STAGE 3 PROOF (sole done-gate input) — run the operator's example query 'what can muddytires learn from our knowledge base' against the nxtlvl-knowledge workspace, write the full response to PROOF-QUERY-RECEIPT.md, and assert the answer references at least one card filename (grounded, not hallucinated). A response with no 'card-' citation FAILS. Reads workspace.json for the slug.

Done means: steps M-CORPUS.COMPLETE.1.S3.1..M-CORPUS.COMPLETE.1.S3.3 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — the census's 17 sources are all carded (or explicitly skip-verdicted), the library INDEX
resolves to every card, PATTERNS.md exists, and the AnythingLLM workspace answers the operator's example query with
a cited answer. Nothing claims done without that query receipt.

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
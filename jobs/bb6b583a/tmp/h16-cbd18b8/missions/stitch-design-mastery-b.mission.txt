MISSION: stitch-design-mastery-b — Stitch capability census + DESIGN.md draft (gather-not-dispatch shape)
MISSION-CLASS: research  # validator mode: .md deliverables count as targets (verification intent stated in Maqsad)

Niyyah: stitch-design-mastery FAILED x2 (2026-06-12 14:18) on a GPU SELF-DEADLOCK the conductor authored: step 5 dispatched nemotron vision-QC while the mission engine's own laguna witness held the single GPU — the 120s step timeout killed the correctly-waiting script both attempts (receipts: engine-exec-fail step:5 "waiting for GPU (laguna-xs.2:q4_K_M resident)"; vision-qc.txt 0 bytes). The law this teaches: A MISSION STEP MUST NEVER WAIT ON THE GPU ITS OWN WITNESS HOLDS — inference evidence is pre-staged by the conductor in a GPU-free window, then GATHERED. The conductor ran the vision QC out-of-band 14:47 (nemotron, real verdict on disk: 4 PASS / 2 FAIL). Steps 1-4 of the parent all engine-exec-ok, so this -b keeps them and replaces only the dispatch with a gather. Purpose unchanged: the operator asked whether the automated system can learn Stitch as deeply as the conductor's live session — or deeper; the banked card is one session's snapshot, and this mission audits it against the live endpoint and authors the two artifacts the self-healing design loop lacks.

Maqsad: ONE assembled artifact — stitch-mastery.md at the sandbox root, built from FIVE part files, plus DESIGN-draft.md beside it. PLAN SHAPE: (1) COMMAND gather steps stage the evidence, each shaped EXACTLY as: if (Test-Path '<src>') { Copy-Item '<src>' '<dst>' } — sources: E:\AI_Storage\website-pipeline\apps\post-intake\stitch\STITCH-KNOWLEDGE.md, E:\AI_Storage\website-pipeline\apps\post-intake\design-contract.md, E:\AI_Storage\website-pipeline\apps\post-intake\stitch\stitch-library-v2.html, AND the PRE-STAGED inference evidence C:\Users\marka\.claude\muezzin-plugin\missions\stitch-design-mastery\vision-qc.txt (conductor-run nemotron verdict, 727 bytes — gathered, never re-dispatched); (2) COMMAND probe step records the live API surface WITHOUT any generation: node E:\AI_Storage\website-pipeline\apps\post-intake\stitch\stitch_tools_full.mjs > tools-live.txt (initialize + tools/list only — metadata, zero quota, no GPU); then ONE command step produces tools-live.numbered.txt, knowledge.numbered.txt, and vision-qc.numbered.txt by prefixing each line with Lnn (the line-cite bar requires the numbered sources); (3) FIVE SEPARATE edit steps author one part file each, every part a SMALL emission: part-1-census.md = every tool the live endpoint exposes with its required arguments, each row citing [tools-live.numbered.txt Lnn] and flagged VERIFIED-LIVE (in both dump and card), CARD-ONLY (in the card but not the dump), or NEW (live but absent from the card — the "can it learn more" findings); part-2-design-md.md = a complete DESIGN.md draft for "NXTLVL Post" ready for upload_design_md (dark palette #10151a / #1a222b / accent #0d9488 / text #e8edf2; typography hierarchy; named components: batch row, circular hook-score badge with green/amber/red bands, platform card with Copy action, archive control, floating upload button), every component citing [knowledge.numbered.txt Lnn] or the v2 HTML by line; part-3-checker.md = implementation spec for check_design_contract.mjs (the 6 mechanical checks from design-contract.md as greps over a Stitch HTML export, nonzero exit on any failure, output formatted as an edit_screens repair prompt); part-4-vision-qc.md = the pre-staged VLM verdict analyzed line-by-line, every PASS/FAIL statement citing [vision-qc.numbered.txt Lnn], PLUS the mandatory disagreement audit: the VLM's two FAILs (collapsed-row badges, archive control) CONTRADICT the conductor's HTML grep which found both in the markup — and the VLM names rows ("Recommended authors") that exist nowhere in the design; part-4 must judge each disagreement as VLM-MISREAD, MARKUP-PRESENT-BUT-VISUALLY-WEAK, or GENUINE-DEFECT, with cites, never papering over the conflict; part-5-verdict.md = verdict whose label is CARD-COMPLETE or CARD-GAPS-<list> where <list> names EVERY gap the census established (a label naming fewer gaps than the body is a contract violation), each gap carrying its tools-live cite, ending with the SINGLE next concrete call the conductor should make; (4) ONE command step assembles: writes a '# Stitch Mastery — capability census and design artifacts' header then concatenates part-1 through part-5 into stitch-mastery.md, and copies part-2's DESIGN.md body to DESIGN-draft.md at the sandbox root; (5) ONE command step validates with Select-String that stitch-mastery.md contains all five section parts and either CARD-COMPLETE or CARD-GAPS, and that DESIGN-draft.md exists with length >= 2000 bytes. Done means: stitch-mastery.md exists at the sandbox root, validation confirms the five parts and an evidence-bounded verdict label matching the body's gap list exactly, DESIGN-draft.md >= 2000 bytes, every census/gap/QC claim cites a numbered source, the disagreement audit names a judgment for both VLM-vs-grep conflicts, and NO step performed a screen generation, edit, variant, or ANY local-model inference call (all inference evidence is pre-staged and gathered).
Context: live calls limited to initialize + tools/list via the existing bridge script (zero generation quota, zero GPU). Read-only sources; sandbox writes only. No installs.
Amanah: entrusted; discharge with ihsan.

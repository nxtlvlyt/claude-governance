MISSION MT-INTEGRATE-LIGHTHOUSE-CI
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - .github/workflows/lighthouse-ci.yml
  - scripts/lhci-compare.mjs
  - mt-audit/lighthouserc.json
  - scripts/lhci-pre-commit.mjs
  - docs/LIGHTHOUSE-CI.md
REQUIRES: none

Niyyah: Feature worktree mt-feat-lighthouse-ci carries one finished commit — a
score-delta Lighthouse CI pipeline (a GitHub Actions workflow that audits PR preview
deploys and production pushes, a comparison script that diffs current results against
a committed baseline, an LHCI config, an opt-in local pre-commit hook, and operator
docs) — sitting in its own worktree, never landed on the integration line's current
HEAD. This mission's whole job is mechanical: get those five files onto the target
repo's HEAD without losing anything already there, and produce the node --check
receipts a verdict panel needs to judge the result. Writing this mission file, and the
act of cherry-picking itself, is NOT verification that the feature's own code is
correct. Whether the workflow's Cloudflare Pages preview-URL resolution actually
works end to end, whether the score-delta math and threshold calibration in
lhci-compare.mjs and lighthouserc.json are sound, whether the sticky PR comment
formatting renders correctly on a real pull request, and whether the pre-commit
hook's skip-by-default / LHCI_LOCAL / LHCI_LOCAL_AUTO activation modes behave as
documented — none of that is settled by landing the commit. That determination
belongs entirely to this mission's own witness-and-verdict-panel run: the node
--check receipt and the seat panel's judgment on the actual integrated diff. Say
plainly: authoring and executing this mission proves nothing about the feature
code's correctness by itself.

Maqsad: Land commit 34db2da ("ci(lighthouse): score-delta CI on every PR + opt-in
local pre-commit") from the source worktree C:\Users\marka\code\mt-feat-lighthouse-ci
onto the current HEAD of the target repo C:\Users\marka\code\mt-integration-2026-06-22,
producing .github/workflows/lighthouse-ci.yml, scripts/lhci-compare.mjs,
mt-audit/lighthouserc.json, scripts/lhci-pre-commit.mjs, and docs/LIGHTHOUSE-CI.md in
the target repo with a clean working tree afterward, and a passing node --check on
both .mjs scripts this commit adds. Any conflict must be resolved by keeping BOTH the
target's existing unrelated changes and this feature's changes — never silently
dropping either side.

Context (verified against live substrate this session, 2026-07-01, absolute paths, no
prescribed resolution beyond "keep both sides"):
- Confirmed diff stat (git show --stat, this session): 34db2da touches exactly 5
  files, +1015/-0, all new, no modifications or deletions of anything already in the
  repo: .github/workflows/lighthouse-ci.yml (196 lines), docs/LIGHTHOUSE-CI.md (238
  lines), mt-audit/lighthouserc.json (64 lines), scripts/lhci-compare.mjs (301 lines),
  scripts/lhci-pre-commit.mjs (216 lines). None of the five currently exist in the
  target repo, so a clean cherry-pick is the expected outcome — but if the target has
  since grown a same-named file with different content, the resolution rule above
  still applies: keep BOTH bodies, never pick one side and silently drop the other.
- Checked this session: none of the five ALLOW-FILES paths, and no mt-audit directory
  at all, exist yet in the target repo.
- The source worktree and the target repo are linked git worktrees sharing ONE object
  store (both `.git` files point at worktree entries under
  C:\Users\marka\code\muddytires-pages\.git). Confirmed this session: `git cat-file -t
  34db2da` inside the target repo already returns "commit" — the object is already
  resolvable there — while `git merge-base --is-ancestor 34db2da HEAD` returns false
  (not yet an ancestor). Unlike some other mt-integrate-* missions, no `git fetch
  <source-worktree-path>` / remote-add step is needed here; a plain `git cherry-pick
  34db2da` can resolve the object directly.
- A dry run this session (test branch off the target's then-current HEAD 28cbc72,
  deleted afterward, no lasting change) cherry-picked 34db2da cleanly: zero conflicts,
  exactly the 5 files above created, +1015/-0, and `node --check` on both
  scripts/lhci-compare.mjs and scripts/lhci-pre-commit.mjs exited 0. The target branch
  may move again before this mission actually fires, so a conflict is still possible —
  if any ALLOW-FILES entry conflicts at fire time, resolve it by keeping BOTH sides.
- scripts/lhci-compare.mjs and mt-audit/lighthouserc.json both reference a committed
  baseline at docs/lighthouse-2026-06-23/_summary.json. Checked this session: that
  baseline file, and the narrative doc it accompanies (docs/AUDIT-LIGHTHOUSE-2026-06-23.md),
  already exist in the target repo's current HEAD (landed via a prior, unrelated
  commit) — this mission does not need to create or migrate the baseline, only land
  the five files that consume it.
- Target repo HEAD this session: 28cbc72, on branch
  feat/qc-concern-sentry-error-capture-sentry-init-js-2026-06-25. This mission
  deliberately carries no TARGET-BRANCH header: land onto whatever branch is actually
  checked out as HEAD when this mission fires, not a hardcoded branch name.
- The target repo currently carries pre-existing untracked files unrelated to this
  feature (api-index-response.json, docs/e2e-report-2026-07-01.json, e2e-shots/,
  feature-catalog-entry.txt, scripts/e2e-runner.mjs) — these predate this mission and
  are out of scope. "Clean working tree" in this mission's Done means refers to the
  ALLOW-FILES entries this mission touches, not a demand to clear unrelated debris.
- None of the 5 changed files is an .html file (the commit message's body text
  mentions /, /landing.html, /map.html, /guides.html, /changelog.html,
  /partners.html, /pledge.html, /about.html only as the pages the *workflow* will
  later audit at CI time — the diff itself touches zero .html files). This mission
  therefore declares no headless-render witness gate above; node --check is the only
  executable-correctness receipt this mission produces.
- node --version on the machine that ran this session's checks: v22.20.0. `node
  --check` on both scripts/lhci-compare.mjs and scripts/lhci-pre-commit.mjs already
  exited 0 against the source worktree's copies as a sanity check this session — the
  actual receipt this mission must produce is node --check run against the files as
  landed in the TARGET repo, not this pre-check.

Steps:
  1. In the target repo, record the current branch and HEAD, and confirm none of the
     five ALLOW-FILES paths already exist on disk (git -C
     C:\Users\marka\code\mt-integration-2026-06-22 status --short; git -C
     C:\Users\marka\code\mt-integration-2026-06-22 log --oneline -3; git -C
     C:\Users\marka\code\mt-integration-2026-06-22 branch --show-current). [command]
     verify-target-state
  2. Check ancestry before attempting a cherry-pick: `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 merge-base --is-ancestor 34db2da
     HEAD`. If it reports 34db2da is already an ancestor, nothing further is needed —
     do not cherry-pick it, do not use --allow-empty. Otherwise proceed to step 3; the
     source and target share one object store so no separate fetch step is required.
     [command] check-ancestry
  3. If 34db2da is not yet an ancestor, apply it: `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 cherry-pick 34db2da`. If the
     cherry-pick reports conflicts, resolve every conflicted file by keeping BOTH the
     target's pre-existing lines and the feature's added lines — never --ours /
     --theirs and never a resolution that deletes either side's content. If it comes
     back empty (already applied after all), resolve with --skip, never
     --allow-empty. [command] cherry-pick-lighthouse-ci
  4. Run `node --check scripts/lhci-compare.mjs` and `node --check
     scripts/lhci-pre-commit.mjs` from the target repo root (the two .mjs files this
     commit adds) and confirm both exit 0; also run `node --check` on any other
     .js/.mjs file conflict resolution in step 3 modified. [command] node-check-all
  5. If step 3 left anything uncommitted (conflict resolution required `git add` +
     `git cherry-pick --continue`, or a manual merge commit), commit explicitly with a
     message that names "mt-feat-lighthouse-ci" and/or "lighthouse-ci" and cites
     34db2da. Confirm `git status --short` in the target repo shows no uncommitted
     changes to any ALLOW-FILES entry and `git log --oneline -5` shows the landed
     commit at or near HEAD. [command] commit-and-verify-clean

Done means:
  - `git log` in C:\Users\marka\code\mt-integration-2026-06-22 shows a commit at or
    near HEAD whose message names "mt-feat-lighthouse-ci" and/or "lighthouse-ci" and
    cites 34db2da.
  - .github/workflows/lighthouse-ci.yml, scripts/lhci-compare.mjs,
    mt-audit/lighthouserc.json, scripts/lhci-pre-commit.mjs, and docs/LIGHTHOUSE-CI.md
    all exist on disk in the target repo, each containing the feature's actual content
    (the workflow file defines PR and push triggers that run lhci and post a sticky
    comment; lhci-compare.mjs defines the baseline-diff logic including its optional
    --fail-on-regression flag; lighthouserc.json lists the 8 audited URLs and their
    threshold assertions; lhci-pre-commit.mjs defines the opt-in local hook with its
    skip-by-default activation modes) — confirmed by reading each file's on-disk
    content, not by trusting the cherry-pick's exit code alone.
  - `node --check scripts/lhci-compare.mjs` and `node --check
    scripts/lhci-pre-commit.mjs` both exit 0, and `node --check` exits 0 on any other
    .js/.mjs file this integration touched or conflict-resolved.
  - `git status --short` in the target repo shows no uncommitted changes to any
    ALLOW-FILES entry (the pre-existing unrelated untracked files noted in Context are
    not this mission's to clean up).
  - Nothing about this mission's own authorship or execution constitutes verification
    that the feature's code is correct — whether the workflow's Cloudflare Pages
    preview-URL resolution actually works end to end against a real PR, whether the
    score-delta math and threshold calibration are sound, whether the sticky comment
    renders correctly, and whether the pre-commit hook's activation logic behaves as
    documented — that determination belongs entirely to this mission's own
    witness-and-verdict-panel run, downstream of the receipts above.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

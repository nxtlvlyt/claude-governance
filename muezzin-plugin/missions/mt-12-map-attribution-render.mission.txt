MISSION mt-12-map-attribution-render
MISSION-CLASS: code-repo
REPO-ROOT: C:/Users/marka/mt-chain-wt
TARGET-BRANCH: main
ALLOW-FILES:
  - map.html
REQUIRES: none

Niyyah: The map's required OpenStreetMap/MapTiler/source attribution is configured in code
but never reaches the screen, so the tile-license credit and data sources are invisible to
users — a license-compliance gap and a trust gap on a site whose whole promise is sourced honesty.

Maqsad: On the muddytires.ca map page, the Leaflet attribution control renders VISIBLY at the
bottom of the map, showing the OpenStreetMap / MapTiler / Parks-Canada / CWFIS source credits.
Done serves a map that visibly credits its sources, as Leaflet and the tile licenses require.

Context (unbiased facts — absolute paths, no prescribed solution):
- The map is created in map.html via L.map('map',{rotate:true,...}) using the leaflet-rotate plugin.
- window.map.attributionControl EXISTS as an object; tile layers set attribution strings
  (around map.html line 27) and map.attributionControl.addAttribution(...) adds the fire source
  (around line 35).
- BUT a headless Chrome render shows the control's container is NOT in the DOM:
  document.querySelector('.leaflet-control-attribution') === null, and
  map.attributionControl._container is not attached to the page. So attribution is configured
  but not displayed — the leaflet-rotate plugin appears to suppress it.
- TWO manual attempts already FAILED to make it render (both verified LOCAL_FAIL in headless):
  (a) removeControl(existing) then a fresh L.control.attribution().addTo(map) right after map init;
  (b) the same via map.whenReady, force-appending the container to map._controlCorners.bottomright.
  The actual leaflet-rotate interaction still needs to be found.

Done means: A headless Chrome render of the served map page shows a VISIBLE
.leaflet-control-attribution element (getBoundingClientRect().width > 0) containing the
OpenStreetMap/source credit text. Verify by RENDERING the page, not by reading the code.

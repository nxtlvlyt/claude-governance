MISSION-CLASS: code-repo (ONE deterministic command-edit on a CLEAN worktree + positive-assertion verify + commit; the edit is a COMMAND -replace, NOT model emission of file content; NO deploy; live muddytires.ca / prod Pages / DNS UNTOUCHED)
MISSION-ID: M-MUDDYTIRES.ORACLE-UI-SURFACE.2B
REPO-ROOT: E:\AI_Storage\muddytires-frontend-wt
ALLOW-FILES: index.html

# SUPERSEDES oracle-ui-surface-2 (FAILED x2 — NOT a logic defect). RECEIPTS: the edit step embedded the whole 360KB index.html
# into the model framing -> 263869 tokens > 262144 model limit -> cloud seats HTTP_400 prompt-too-long -> failover to claude
# (opus emitted 6694 chars) -> per-step witness REJECT unrepaired. Root = CONTEXT OVERFLOW from whole-file edit-framing (the
# windowed-edit fix is on disk but daemon PID 13496 runs stale in-memory executor.mjs — engine item: restart). The 3 changes are
# TINY exact-string swaps, so this version does them as a DETERMINISTIC COMMAND -replace — the file content NEVER enters a model
# prompt (same principle that beat the inline-node wall: command/committed path, zero model emission). Context limit + witness-on-
# edit are both bypassed by construction.

Niyyah: surface the already-live Oracle moat to users by repointing the prod map's 3 dead oracle.php fetches to the LIVE worker
DETERMINISTICALLY via a command — never embedding the 360KB file in a model prompt, never depending on a model to emit the file.

Maqsad: ONE deliverable — index.html's 3 oracle.php fetch sites (nnOracle panel + nnFillOracle services + nnAsk box) repointed to
https://muddytires-oracle-d1-test.regulativity.workers.dev via a single in-place -replace, committed on branch oracle-frontend-swap.
EXACTLY THREE steps (command-class; do NOT split, do NOT expand):
1. EDIT via COMMAND (run VERBATIM in REPO-ROOT — this REPLACES the model-emission edit path):
   $p='index.html'; $c=Get-Content $p -Raw; $n=$c -replace [regex]::Escape('oracle.php?lat='),'https://muddytires-oracle-d1-test.regulativity.workers.dev/?lat='; if($n -eq $c){Write-Error 'no oracle.php?lat= found'; exit 1}; Set-Content $p -Value $n -NoNewline; Write-Output 'swapped'
2. VERIFY (positive-assertion, run VERBATIM; EXIT NON-ZERO on absence):
   $c=Get-Content index.html -Raw; $hits=([regex]::Matches($c,'muddytires-oracle-d1-test\.regulativity\.workers\.dev')).Count; if($hits -lt 3){Write-Error "worker URL hits=$hits (<3)"; exit 1}; if($c -match 'oracle\.php\?lat='){Write-Error 'oracle.php?lat= still present'; exit 1}; if($c -notmatch 'L\.map\(' -or $c -notmatch 'nnFillOracle'){Write-Error 'map/popup code missing — file corrupted'; exit 1}; "VERIFIED worker_hits=$hits, oracle.php gone, map intact, bytes=$($c.Length)"
3. COMMIT index.html --no-verify (message: "surface Oracle: repoint 3 oracle.php fetches -> live D1 worker (command -replace)").

Done means: index.html's 3 Oracle fetches hit the live worker; no oracle.php?lat= remains; nnOracle + nnFillOracle render the
sourced answers, nnAsk degrades gracefully (worker ignores its &ask param -> d.answer undefined -> the existing "No answer available"
branch shows, no break/hang); committed on branch oracle-frontend-swap; NO deploy performed; muddytires.ca / prod Pages / DNS
untouched. If step 1 finds zero oracle.php?lat= (already swapped), that is still DONE if verify passes.

Constraints: the edit is a COMMAND -replace (NEVER a model emission of index.html — this is the whole point: dodge the 263K
context-overflow + the witness-on-edit reject); ONE file; positive-assertion verify against the real file; commit --no-verify; NO
deploy (preview + live cutover are a separately-reviewed follow-on — operator approves the cutover of his #1 asset); THREE steps,
do not split/expand.

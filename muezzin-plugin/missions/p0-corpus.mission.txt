MISSION P0-CORPUS
Niyyah: the factory must never solve a problem Mark's past builds already solved — unmined knowledge is indistinguishable from no knowledge. This mission is also the muezzin's first long autonomous run: every receipt it produces is double evidence.
Maqsad: a source census and pattern library of everything Mark has already built and collected, consumable by all future dossier and build missions. Done means: (1) sources.json — every git repo, project folder, AnythingLLM workspace, and Cloudflare deployment discovered (not hand-listed), each with a type tag and a mine/skip verdict; (2) one pattern card per 'mine' source — what it does well, recurring components, design tokens, copy patterns, vertical fit, with evidence references; (3) a knowledge inventory — what the AnythingLLM business-books workspace actually contains (topical map from sample queries), what the old website-pipeline's contracts offer, what the Stitch exports show; (4) a patterns library assembling it all, indexed, every entry citing its source. Time-box: stop cleanly at census+verdicts+cards completion or 48 hours, whichever comes first — partial work committed with a state note beats unfinished ambition.
Context: known starting points (the census must go BEYOND these): local folders E:\2boots, C:\vanlife, C:\warroom (BRAND-TO-SITE-FAST.md + regulativity worker and site templates), E:\AI_Storage\website-pipeline (old compiler — salvage contracts and Stitch prompts only), D:\Desktop\ai book (design reasoning). Cloudflare pages projects (authed wrangler CLI available): 2boots, laynas-cookies, regulativity. AnythingLLM API at http://localhost:3001 (workspace business-books verified responding). GitHub user nxtlvlyt has mostly PRIVATE repos — anonymous API sees only 2; SSH git access exists for known repos; full private enumeration may be impossible without a token — record that as a census gap rather than guessing. Local Forgejo at localhost:3002 has an API. Live sites for archaeology: 2boots.ca, muddytires.ca, laynascookies.ca (the agy build — critique it), regulativity.pages.dev. All work is read-only against sources; outputs land in this sandbox only. Plans must use cwd-relative paths; reference/ is read-only.
Amanah: this is entrusted to you; discharge it with ihsan.

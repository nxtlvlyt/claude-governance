MISSION mt-integrate-quick-checkin-templates-2026-06-23
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - js/quick-checkin.js
REQUIRES: none

Niyyah: Feature worktree mt-feat-quick-checkin-templates-2026-06-23 carries one
finished commit — five one-tap review-form templates that pre-fill the check-in
tip body — sitting in its own worktree, never landed on the integration line's
current HEAD. This mission's whole job is mechanical: get the feature's file onto
the target repo's HEAD without losing anything already there, and produce the
node-level receipts a verdict panel needs to judge the result. Writing this
mission file, and the act of integrating itself, is NOT verification that the
feature's own code is correct. Whether fireTemplate()'s up-to-30x50ms poll for
#rbody actually finds the textarea reliably once openReviews() mounts the form,
whether appending vs. injecting the template body ever surprises an operator who
already typed something, and whether the new #mtcit: href-fragment parsing in the
click delegate can ever collide with the pre-existing #mtci: check-in path — none
of that is settled by landing the commit. That determination belongs entirely to
this mission's own witness-and-verdict-panel run: the node --check receipt and the
seat panel's judgment on the actual integrated diff. Say plainly: authoring or
executing this mission proves nothing about the feature code's correctness by
itself.

Maqsad: Land commit a27ebf3 ("feat(quick-checkin): 5 one-tap review-form templates
pre-fill #rbody") from the source worktree
C:\Users\marka\code\mt-feat-quick-checkin-templates-2026-06-23 onto the current
HEAD of the target repo C:\Users\marka\code\mt-integration-2026-06-22, with any
conflicts resolved by keeping BOTH sides — this repo's existing unrelated changes
AND the feature's changes, never silently dropping either — js/quick-checkin.js
passes `node --check`, the result is committed with a clear message, and the
target repo's working tree is left clean with respect to js/quick-checkin.js.

Context (verified against live substrate this session, 2026-07-01, absolute
paths, no prescribed resolution beyond "keep both sides"):
- Source worktree: C:\Users\marka\code\mt-feat-quick-checkin-templates-2026-06-23.
  `git show --stat a27ebf3` there confirms the commit touches exactly one file,
  js/quick-checkin.js, 198 insertions(+), 5 deletions(-): adds a TIP_TEMPLATES
  array of 5 predefined tip bodies with matching .mt-cit-* CSS classes, renders a
  one-tap template-button row in clusterHtml gated on having lat/lng coords and
  window.openReviews existing, adds fireTemplate() which calls the existing
  openReviews() popup opener then polls up to 30x50ms for the #rbody textarea to
  inject/append the template body, and extends the existing document click
  delegate to parse #mtcit: href fragments alongside the pre-existing #mtci:
  check-in fragments. Actual review submission is unchanged and still goes
  through the existing /api/contribute/review path — this is client-side pre-fill
  authoring UX only, confirmed by the commit's own message and diff.
- a27ebf3's parent is ed6e33c (the integration line's shared merge point). Checked
  this session: `git merge-base --is-ancestor ed6e33c HEAD` in the target repo
  returns true, so the feature's fork point is reachable from the target's
  current HEAD — a compatible base for a cherry-pick. `git merge-base
  --is-ancestor a27ebf3 HEAD` returns false — a27ebf3 itself has never landed.
- The commit object a27ebf3ba375671ffb15bd3692377f5f91df970b already exists in the
  target repo's own local object store (checked this session: `git cat-file -t
  a27ebf3` -> commit; `git branch -a --contains a27ebf3` shows both a local branch
  feat/quick-checkin-templates-2026-06-23 and a remote-tracking branch
  remotes/github/feat/quick-checkin-templates-2026-06-23 already contain it, yet
  neither has been merged into the branch actually checked out as HEAD) — no `git
  fetch <source-worktree-path>` / remote-add step is needed; a plain `git
  cherry-pick a27ebf3` can resolve the object directly.
- js/quick-checkin.js ALREADY EXISTS in the target repo (426 lines before this
  mission runs) — this integration is a MODIFY of an existing file, not a new
  add. The target's copy carries later, unrelated work the feature's own history
  never saw: commit 84ce677 ("restore I'm-here button mount in POI popup + POST
  /api/contribute/checkin per QC walk 2026-06-25") touches the same file. Any
  conflict must be resolved by keeping BOTH 84ce677's restored check-in code AND
  the feature's new template code — never one replacing the other.
- A dry run this session (isolated `git worktree add` at the target's
  then-current HEAD 5e19874, cherry-picked, inspected, then removed via `git
  worktree remove --force` — no lasting change to the target's main working
  tree) cherry-picked a27ebf3 cleanly: git reported "Auto-merging
  js/quick-checkin.js" with ZERO conflicts, `node --check js/quick-checkin.js`
  exited 0 on the 619-line merged result, and the merged file retained BOTH the
  feature's new code (TIP_TEMPLATES and fireTemplate present) AND the target's
  pre-existing 84ce677 restore code (the /api/contribute/checkin POST call still
  present). This repo showed visible concurrent activity during this same
  session (its main working tree's staged state changed between two of this
  session's own git status checks even though HEAD did not move) — the target
  branch may look different again by the time this mission actually fires. Do
  not assume 5e19874 is still HEAD; if a conflict occurs at fire time that this
  dry run did not see, resolve it by keeping BOTH the target's existing lines and
  the feature's added lines, never `--ours` / `--theirs` and never a resolution
  that deletes either side's content.
- Target repo branch this session: main, HEAD 5e19874 ("Merge branch
  'feat/qc-concern-vehicle-profile-picker-2026-06-25'"). This mission deliberately
  carries no TARGET-BRANCH header: land onto whatever branch is actually checked
  out as HEAD when this mission fires, not a hardcoded branch name.
- The target repo currently carries pre-existing untracked files unrelated to
  this feature (api-index-response.json, docs/e2e-report-2026-07-01.json,
  e2e-shots/, feature-catalog-entry.txt, scripts/e2e-runner.mjs, functions/feed/)
  — these predate this mission and are out of scope. "Clean working tree" in this
  mission's Done means refers to js/quick-checkin.js, not a demand to clear
  unrelated pre-existing debris.
- This diff touches no HTML file (confirmed: the only file in the diff is
  js/quick-checkin.js) — the render-witness/preview requirement used for
  HTML-touching integrations is deliberately absent from this mission; nothing
  below demands a browser render, and no VISUAL-QC-REQUIRED header is set.

Steps:
  1. In the target repo, record the current branch and HEAD, and inspect the
     current state of js/quick-checkin.js (git -C
     C:\Users\marka\code\mt-integration-2026-06-22 status --short -- js/quick-checkin.js;
     git -C C:\Users\marka\code\mt-integration-2026-06-22 log --oneline -3; git -C
     C:\Users\marka\code\mt-integration-2026-06-22 branch --show-current). [command]
     verify-target-state
  2. Check ancestry of a27ebf3 relative to current HEAD BEFORE attempting a
     cherry-pick: `git -C C:\Users\marka\code\mt-integration-2026-06-22
     merge-base --is-ancestor a27ebf3 HEAD`. If this reports a27ebf3 already an
     ancestor, it is already landed — skip cherry-picking it, do not use
     --allow-empty. If not, proceed to step 3. [command] check-ancestry
  3. Apply a27ebf3 via `git -C C:\Users\marka\code\mt-integration-2026-06-22
     cherry-pick a27ebf3` (the commit object is already reachable in this
     repo's own store). If the cherry-pick reports conflicts on
     js/quick-checkin.js, resolve every conflicted hunk by hand so the resolved
     content contains BOTH the target's pre-existing 84ce677 restore code AND
     the feature's new template code — never `--ours` / `--theirs` and never a
     resolution that deletes either side's content. If the cherry-pick comes
     back empty (already applied after all), resolve with `--skip`, never
     `--allow-empty`. [edit] cherry-pick-and-resolve
  4. Run `node --check js/quick-checkin.js` from the target repo root and
     confirm it exits 0; also run `node --check` on any other .js/.mjs file
     that conflict resolution in step 3 modified. [command] node-check-all
  5. Stage and commit the integrated result with a message that names
     "mt-feat-quick-checkin-templates-2026-06-23" and/or cites a27ebf3. Confirm
     `git status --short` afterward shows no uncommitted changes to
     js/quick-checkin.js and `git log --oneline -5` shows the landed commit at
     or near HEAD. [command] commit-and-verify-clean

Done means:
  - `git log` in C:\Users\marka\code\mt-integration-2026-06-22 shows a commit at
    or near HEAD whose message names "mt-feat-quick-checkin-templates-2026-06-23"
    and/or cites a27ebf3.
  - js/quick-checkin.js exists in the target repo with the feature's actual
    content — a TIP_TEMPLATES array of 5 template entries, a fireTemplate()
    function, matching .mt-cit-* CSS classes, and a document click delegate that
    recognizes both #mtci: and #mtcit: href fragments — confirmed by reading the
    on-disk file, not by trusting the cherry-pick's exit code alone. The file
    also still contains the target's pre-existing 84ce677 restore code (the
    /api/contribute/checkin POST call) — both sides present, neither silently
    dropped.
  - `node --check js/quick-checkin.js` exits 0, and `node --check` exits 0 on
    every other .js/.mjs file this integration touched or conflict-resolved.
  - `git status --short` in the target repo shows no uncommitted changes to
    js/quick-checkin.js (pre-existing unrelated untracked files noted in Context
    are not this mission's to clean up).
  - This mission's authorship and execution prove only that the integration is
    mechanically sound (the declared file present with real content, the feature
    commit reachable in target history, syntax check passing) — not that the
    feature's code is correct, safe, or free of UX regressions (e.g. that the
    #rbody poll never races the form's mount, that appending vs. replacing the
    template body never surprises an operator, or that #mtcit: parsing never
    collides with #mtci:). That judgment belongs entirely to this mission's own
    witness-and-verdict-panel run, downstream of this mission producing the
    integrated result.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

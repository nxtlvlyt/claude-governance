MISSION mt-integrate-api-docs-2026-06-23-audit
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - docs/API-REFERENCE-2026-06-23.md
REQUIRES: none

Niyyah: Commit fa68f3b (docs(api): add auto-generated API reference 2026-06-23) is a real,
already-committed feature that never reached the integration repo's current HEAD — it sits
reachable only from its own feature branch/worktree, orphaned from the branch the operator
actually ships from. This mission's sole purpose is to land that exact commit onto the
integration repo without silently dropping either side of any conflict, so the feature stops
sitting off-branch. Authoring this mission is NOT a review of whether the API reference the
feature commit generated is itself accurate, complete, or safe to publish — that judgment
belongs entirely to the witness + verdict-panel run this mission's firing triggers. Nothing
in this text should be read as a correctness verification of the feature's content.

Maqsad: Cherry-pick (or merge, if a cherry-pick alone cannot land it cleanly) commit fa68f3b
onto the current HEAD of the target repo, keeping BOTH the target's pre-existing unrelated
changes and the feature's full docs/API-REFERENCE-2026-06-23.md content on any conflict —
never silently drop either side — run node --check on every .js/.mjs file the integrated
commit touches, commit the result with a clear message, and leave the target repo's working
tree clean.

Context (unbiased facts, verified this session):
- The commit is already a reachable git object inside the target repo itself (its own
  branch/worktree lives alongside it as a linked worktree of the same repo) — it does not
  need to be fetched from anywhere external before it can be cherry-picked.
- The feature's diff touches exactly one file: docs/API-REFERENCE-2026-06-23.md, +660/-0,
  1 file changed. It is an auto-generated Markdown reference documenting Pages Functions
  (muddytires.ca/api/*) and their backing Cloudflare Workers endpoints — handlers, request/
  response shapes, error codes, and curl examples (e.g. an /api/admin/tasks section covering
  GET, POST, and PATCH) — with provenance/confidence-field discipline noted for sourced data.
- The diff changes zero .js/.mjs files. This mission does not touch HTML, so it carries no
  visual/render QC gate of any kind.
- The target repo's working tree may already carry unrelated pre-existing untracked files
  from other work; this mission's own containment is scoped to docs/API-REFERENCE-2026-06-23.md
  only (see ALLOW-FILES) and must not disturb anything else.

Steps:
  1. Cherry-pick commit fa68f3b onto the current HEAD of the target repo. If git reports a
     conflict on any file, resolve it by keeping BOTH the target's existing content and the
     feature's added content — never drop either side — then complete the cherry-pick (or
     fall back to a merge that preserves both sides if cherry-pick cannot land cleanly).
     [command] cherry-pick-api-docs
  2. Confirm docs/API-REFERENCE-2026-06-23.md exists in the working tree and contains, at
     minimum, the phrase "Pages Functions", an /api/admin/tasks section documenting GET,
     POST, and PATCH, and at least one curl example. [command] verify-api-docs-content
  3. List every .js and .mjs file the applied commit touched (via git diff-tree against the
     cherry-picked sha) and run node --check on each file found; an empty list is a stated
     pass, not a silent skip. [command] node-check-changed-js
  4. Confirm the working tree is clean, git log on HEAD shows the integrated commit message,
     and no tracked or untracked file outside ALLOW-FILES was touched by this mission's own
     steps. [command] confirm-clean-and-logged

Done means:
  - docs/API-REFERENCE-2026-06-23.md exists in the target repo's working tree and contains
    the string "/api/admin/tasks" together with GET, POST, and PATCH method documentation
    and at least one curl example — verified by reading the file, not by trusting the diff.
  - Every .js/.mjs file touched by the integrated commit (per git diff-tree --no-commit-id
    --name-only -r on the cherry-picked sha) passes node --check with exit code 0; the
    receipt states how many files were checked, and zero is an acceptable, explicitly-stated
    count for this docs-only diff rather than a silent skip.
  - git log -1 --pretty=%s on the target repo's current HEAD shows the message
    "docs(api): add auto-generated API reference 2026-06-23" (or the exact merge/cherry-pick
    message carrying it forward), proving the feature commit actually landed.
  - git status --porcelain on the target repo returns empty for the mission's own scope —
    no staged, unstaged, or untracked leftovers introduced by this mission's steps.
  - If cherry-pick reported a conflict, the committed result still contains BOTH the target's
    pre-existing unrelated content and the complete docs/API-REFERENCE-2026-06-23.md content
    (all 660 lines' worth of material) — neither side silently dropped.

Amanah: entrusted; discharge with ihsan.

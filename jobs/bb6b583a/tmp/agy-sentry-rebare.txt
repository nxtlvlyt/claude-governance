missions/sentry-atv-wire.S1.mission.txt  <!-- SPLIT-CHILD | RE-BARED 2026-07-14 (conductor): mission text rewritten — ALLOW-FILES narrowed to the 2 files actually still missing the tag (guides, iptv; the other 5 already correctly wired, confirmed via direct grep), steps converted from freeform [edit] to deterministic [command] PowerShell inserts (byte-identical to the 5 already-wired pages), per the prior FAILED attempt's own diagnosed fix. Dry-run verified this wake: insert logic tested against real scratch copies of both target files, confirmed idempotent (double-run stays at 1 occurrence), byte-exact match to the live pattern on index.html. lint OK; hold-safe stem (sentry-*, gap work). -->

MISSION-ID: mt-integrate-crown-legal-full-text-2026-06-23.S1
MISSION-CLASS: code-repo
PARENT: mt-integrate-crown-legal-full-text-2026-06-23
TARTIB-INDEX: 1 of 2
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - functions/api/land-tenure.js
  - js/crown-land-overlay.js
REQUIRES: none (first in tartib order)
STEPS: 8

PARENT MAQSAD: The commit "487d8d5 feat(crown-legal): per-province FULL legality paragraphs on

Maqsad: sub-mission 1 of 2 — Confirm source commit 487d8d5 exists in the source worktree and touches land-tenure.js, without modifying that worktree. through Stage the resolved files, finish the cherry-pick with a commit if one is still pending, and confirm the resulting commit message names the integrated feature or its origin hash.

Steps:
  1. Confirm source commit 487d8d5 exists in the source worktree and touches land-tenure.js, without modifying that worktree. [command] 
  2. Confirm target repo is on main at the expected pre-integration baseline HEAD (28cbc72) before integration begins. [command] 
  3. Cherry-pick commit 487d8d5 onto target repo main from the shared object store; land cleanly or halt in a conflicted state on both touched files. [command] 
  4. Resolve any conflict in functions/api/land-tenure.js so the file contains BOTH the target's existing AB WMU/WMA display_label + popup_chip logic AND the feature's PROVINCIAL_CROWN_RULES/PROVINCE_BBOXES/provincial-rules endpoint, with no conflict markers left and valid syntax. [edit] functions/api/land-tenure.js
  5. Resolve any conflict in js/crown-land-overlay.js so the file contains BOTH the target's existing tinted-polygon/click-popup restoration logic AND the feature's getProvincialRules/detectProvinceCode/provincialLegalBlockHtml/injectProvincialLegalBlock, with no conflict markers left and valid syntax. [edit] js/crown-land-overlay.js
  6. Verify both sides survived in functions/api/land-tenure.js: the feature's PROVINCIAL_CROWN_RULES constant and the target's pre-existing display_label logic are both present. [verify] 
  7. Verify both sides survived in js/crown-land-overlay.js: the feature's getProvincialRules/detectProvinceCode and the target's pre-existing tinted-polygon restoration logic are both present. [verify] 
  8. Stage the resolved files, finish the cherry-pick with a commit if one is still pending, and confirm the resulting commit message names the integrated feature or its origin hash. [command] 

Done means: functions/api/land-tenure.js, js/crown-land-overlay.js exist/are updated as specified — verify by headless-browser render, not by reading the code

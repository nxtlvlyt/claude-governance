MISSION SOTA-PIPELINE-DOSSIER-SPLIT-A-GATHER-AND-A
MISSION-CLASS: research
REQUIRES: search (any capability/SOTA characterization that reaches beyond the staged cards must carry a live search receipt; Section-A claims themselves are line-cited to staged sources, not searched).
Niyyah: split-a of the operator's 2026-06-11 ask — "a very thorough document stating how state-of-the-art our pipeline is going to be." The parent died at its single all-sections emission (emission-empty at step 15/15, re-plan loop burning cloud). This slice does ONLY the gather + Section A so one emission stays under the ceiling. A synthesis from conductor memory would be drift; this is built from staged receipts.
Maqsad: ONE artifact — SECTION-A-PIPELINE-KNOWS.md at the sandbox root. PLAN SHAPE: (1) COMMAND gather steps copy the evidence into the sandbox with flat names, each shaped EXACTLY as: if (Test-Path '<src>') { Copy-Item '<src>' '<dst>' } — sources: every missions/card-*/card-*.md DONE artifact (card-2boots, card-regulativity, card-half-a-vanlife, card-half-b-muddytires, card-merge output if present, card-tidaltreasures output if present), missions/m28-0-directionrv-study/*.md, missions/fb-backlog-priorities/fb-backlog-priority-card.md, missions/books-website-knowledge output if present, E:\AI_Storage\website-pipeline\MASTER-PLAN.md, E:\AI_Storage\website-pipeline\docs\patterns\auth-membership-pattern.md, E:\AI_Storage\website-pipeline\docs\OPERATIONAL-ARTIFACTS-DOCTRINE.md; (2) ONE command step produces line-NUMBERED copies of the gathered files (Get-Content <f> | ForEach-Object { $i++; "L$i`: $_" } | Set-Content <f>.numbered.md) so claims carry line cites; (3) ONE edit step authors ONLY Section A — "WHAT THE PIPELINE KNOWS": per ingested source, the capabilities/patterns it contributed, each claim cited [<staged>.numbered.md Lnn]. No other sections. Inference marked "inference, not in source"; external facts marked "external, search-verified: <url>". Done means: SECTION-A-PIPELINE-KNOWS.md exists, markdown-valid, every claim line-cited to a staged numbered source, and the artifact never claims an ingestion that has no staged card (an absent source is named absent, not invented).
Context: gather steps tolerate absent sources (if/Test-Path) — Section A synthesizes whatever HAS landed and names the rest. Sandbox writes only. This is part 1 of 3 (split-b authors Section B, split-c merges + writes C/D/E).
Amanah: entrusted; discharge with ihsan.

MISSION mt-integrate-dark-mode-icons-2026-06-23
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: feat/qc-concern-sentry-error-capture-sentry-init-js-2026-06-25
ALLOW-FILES:
  - css/mt-icons-dark.css
  - js/mt-icons-dark.js
REQUIRES: none

Niyyah: The feature "mt-feat-dark-mode-icons-2026-06-23" (commit 585e558, currently
sitting in the source worktree C:\Users\marka\code\mt-feat-dark-mode-icons-2026-06-23)
has never been integrated into this repo's HEAD. This mission's entire job is
mechanical integration — land the commit onto the current tree without losing either
side's changes, and prove the result is syntactically sound. Nothing this mission does
constitutes a judgment that the dark-mode-icons feature's code is correct, safe, or
visually equivalent to the existing Material Symbols font set it mirrors (e.g. that the
hand-traced glyph paths render right, or that the contrast-lift rules actually improve
legibility under html.theme-dark). That determination belongs to this mission's own
witness plus verdict-panel run, which inspects the integrated result after this mission
produces it — not to the act of integration itself. Authoring or executing this mission
is not verification of the feature.

Maqsad: The commit "585e558 feat(icons): native dark-mode SVG icon set
(currentColor-aware, B6-theme)" is cherry-picked or merged from the source worktree
onto the current HEAD of C:\Users\marka\code\mt-integration-2026-06-22, with any
conflicts resolved by keeping BOTH sides — this repo's existing unrelated changes AND
the dark-mode-icons feature's changes, never silently dropping either — every changed
.js/.mjs file passes `node --check`, the result is committed with a clear message, and
the target repo's working tree is left clean (git status shows no uncommitted changes
to the touched files).

Context (verified against live substrate this session, absolute paths, no prescribed
resolution beyond "keep both sides"):
- Source worktree: C:\Users\marka\code\mt-feat-dark-mode-icons-2026-06-23, commit
  585e558314ab76e6451a929e09a70e5bada38cbd, sitting on top of an existing merge chain
  (parent ed6e33c) — do not rewrite that worktree, only read/cherry-pick FROM it.
- Confirmed diff stat for 585e558 (git show --stat, this session): 2 files changed,
  386 insertions(+), 0 deletions(-) — css/mt-icons-dark.css (120 new lines: sizing and
  vertical-alignment utility classes for a .mt-svg-icon class, plus theme-aware
  contrast-lift rules keyed off html.theme-dark) and js/mt-icons-dark.js (266 new
  lines: an IIFE defining window.MTIcons with svg()/replace()/refresh()/list()/
  supports() methods and a hand-traced inline-SVG glyph library using
  fill="currentColor"). Both files are brand new additions; neither modifies an
  existing file.
- Target repo HEAD (this session, `git log --oneline -1` in
  C:\Users\marka\code\mt-integration-2026-06-22): 28cbc72, on branch
  feat/qc-concern-sentry-error-capture-sentry-init-js-2026-06-25. This is the branch
  actually checked out right now, NOT main — main has independently moved to 5e19874
  and shares no ancestry with 28cbc72 either direction (confirmed via
  `git merge-base --is-ancestor` both ways, this session). Land the feature onto the
  branch that is actually checked out, per instruction to integrate onto the current
  HEAD, not onto main.
- Verified this session that neither css/mt-icons-dark.css nor js/mt-icons-dark.js
  exists yet anywhere in the target repo's working tree — this is expected to apply as
  a clean two-file add with no conflicts. If either filename has since been created
  independently in the target tree by unrelated work, resolve by keeping BOTH sets of
  content (e.g. concatenating or namespacing rules/functions) rather than overwriting
  one side.
- The commit object 585e558314ab76e6451a929e09a70e5bada38cbd is already present in the
  target repo's own object store (confirmed via `git cat-file -t` returning "commit"
  this session, since both worktrees share object storage) — cherry-pick can reference
  the SHA directly with no remote-add step needed.
- This diff touches no HTML file (confirmed: two new files only, both non-HTML) — the
  render-witness/preview requirement used for HTML-touching integrations is
  deliberately absent from this mission; nothing below demands a browser render.
- Nothing about this mission's authorship constitutes verification that the feature
  code is correct — that is exactly what this mission's own witness and verdict-panel
  run, downstream of the integration, will determine.

Steps:
  1. In the source worktree C:\Users\marka\code\mt-feat-dark-mode-icons-2026-06-23,
     confirm commit 585e558 exists and capture its patch (e.g. `git show 585e558` or
     `git format-patch -1 585e558`) without modifying that worktree. [command]
     capture-patch
  2. In the target repo C:\Users\marka\code\mt-integration-2026-06-22, on
     TARGET-BRANCH feat/qc-concern-sentry-error-capture-sentry-init-js-2026-06-25 at
     its current HEAD, apply the captured commit via `git cherry-pick 585e558` (the
     commit object is already reachable in this repo's store) or an equivalent merge
     of that single commit. [command] integrate-commit
  3. If cherry-pick reports conflicts on css/mt-icons-dark.css, js/mt-icons-dark.js, or
     any other ALLOW-FILES entry, resolve each conflicted hunk by hand so the resolved
     content contains BOTH the target's pre-existing change AND the dark-mode-icons
     feature's change — never `--ours` / `--theirs` and never a resolution that
     deletes either side's content. [edit] resolve-conflicts
  4. Run `node --check js\mt-icons-dark.js` and confirm it exits 0; run `node --check`
     on every other .js/.mjs file the merge touched or conflict-resolved. [command]
     node-check-all
  5. Stage and commit the integrated result in the target repo with a clear message
     identifying the integrated feature and its origin commit (e.g. referencing
     "mt-feat-dark-mode-icons-2026-06-23" and "585e558"). Confirm `git status`
     afterward shows a clean working tree. [command] commit-and-verify-clean

Done means:
  - `git log` in C:\Users\marka\code\mt-integration-2026-06-22 shows a commit on the
    TARGET-BRANCH containing the integrated dark-mode-icons feature, with a commit
    message that names "mt-feat-dark-mode-icons-2026-06-23" and/or "585e558".
  - css/mt-icons-dark.css exists in the target repo at HEAD with non-trivial content
    (sizing/vertical-alignment utility classes for .mt-svg-icon plus theme-aware
    contrast-lift rules keyed off html.theme-dark, confirmed by reading the file, not
    assumed).
  - js/mt-icons-dark.js exists in the target repo at HEAD with non-trivial content
    (window.MTIcons defined with svg()/replace()/refresh()/list()/supports() methods
    and an inline-SVG glyph library using fill="currentColor", confirmed by reading
    the file, not assumed).
  - `node --check js\mt-icons-dark.js` exits 0, and `node --check` exits 0 on every
    other .js/.mjs file this integration touched or conflict-resolved.
  - `git status` in the target repo shows a clean working tree (no uncommitted
    changes) at the end of the mission.
  - This mission's authorship and execution prove only that the integration is
    mechanically sound (declared files present with real content, feature commit
    reachable in target history, syntax checks pass) — not that the feature's code is
    correct, safe, or visually right (e.g. that the glyphs actually mirror the
    Material Symbols subset, or that the contrast-lift rules genuinely improve
    legibility under html.theme-dark). That judgment belongs to this mission's own
    witness plus verdict-panel run, downstream of this mission producing the
    integrated result.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

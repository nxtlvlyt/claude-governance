MISSION mt-integrate-trip-diary-backend-2026-06-23
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: main
ALLOW-FILES:
  - docs/QC-PIPELINE-SOTA-2026-06-23.md
REQUIRES: none

Niyyah: The feature commit "375e40b docs(qc): SOTA-uplift QC pipeline — 8 mandatory
gates + escape hatches" (slug mt-feat-trip-diary-backend-2026-06-23, source worktree
C:\Users\marka\code\mt-feat-trip-diary-backend-2026-06-23, branch
feat/trip-diary-backend-2026-06-23) is a docs-only addition that must land on the
target repo's main branch without losing any of the target's own unrelated history.
This mission's entire job is mechanical integration — get the commit (or an
equivalent of its content) onto the target branch, prove the result is
syntactically sound, and leave the tree clean. Nothing this mission does
constitutes a judgment that the QC pipeline described in the doc (the 8 gates, the
foreign-frontier policy, the escape hatches) is the RIGHT pipeline, complete, or
safe to adopt as written. That determination belongs to this mission's own
witness + verdict-panel run, which reviews the integrated result after this
mission produces it — not to the act of integration itself. Authoring or executing
this mission is not verification of the feature's content, and it is not
verification that the feature's code is correct; this diff carries no application
code at all, only the doc, and even that fact is a mechanical read of the diff
stat, not an editorial judgment on the doc's merits.

Maqsad: The commit "375e40b docs(qc): SOTA-uplift QC pipeline — 8 mandatory gates +
escape hatches" is cherry-picked or merged from the source worktree
C:\Users\marka\code\mt-feat-trip-diary-backend-2026-06-23 onto the current HEAD of
C:\Users\marka\code\mt-integration-2026-06-22's main branch, with any conflicts
resolved by keeping BOTH sides — this repo's existing unrelated changes AND this
feature's changes, never silently dropping either — every .js/.mjs file this
integration touches or conflict-resolves passes `node --check`, the result is
committed with a clear message (or the prior commit is confirmed already present if
it turns out to already be integrated), and the target repo's working tree is left
clean (git status shows no uncommitted changes to the touched files).

Context (verified against live substrate 2026-07-01, absolute paths, no prescribed
resolution beyond "keep both sides"):
- Confirmed diff stat for 375e40b (`git show --stat 375e40b`, this session): 1 file
  changed, 24 insertions(+), 0 deletions(-) — a single new file,
  docs/QC-PIPELINE-SOTA-2026-06-23.md. No HTML file is touched by this diff, so this
  mission carries no visual-QC gate — that requirement only applies to missions
  that touch rendered pages, and this one does not.
- Because the file is new (not a modification of an existing target file), a
  cherry-pick of 375e40b has no natural conflict surface for THIS specific commit —
  the "keep both sides" instruction below exists for the general case (the target
  branch may have added its own docs/QC-PIPELINE-SOTA-2026-06-23.md independently by
  the time this mission fires) and must still be honored if that case triggers.
- Verified this session: the target repo is an ACTIVELY shared substrate (200+
  feat/* branches, worktree-wf-* branches, wip stashes) that other concurrent
  missions can check out against — a `git branch --show-current` call returned a
  different branch than a `git status` call moments later in this same authoring
  session, confirming HEAD can move between commands issued only seconds apart.
  Every step below operates against TARGET-BRANCH main explicitly, never against
  "whatever HEAD happens to be" when the step runs.
- Verified this session: 375e40b is ALREADY an ancestor of the target repo's main
  branch (`git merge-base --is-ancestor 375e40b main` succeeds), and main's current
  docs/QC-PIPELINE-SOTA-2026-06-23.md already matches 375e40b's content exactly
  (`git diff 375e40b main -- docs/QC-PIPELINE-SOTA-2026-06-23.md` run this session
  produced no output). The integration may already be satisfied by the time this
  mission actually runs. This is NOT grounds to skip execution outright — main can
  move, be reset, or diverge before this mission fires — but it IS grounds for this
  mission to treat "already-integrated" as a valid completion path rather than an
  error: an empty cherry-pick (git reporting the change is already applied) must be
  treated as success, confirmed via the Done-means checks below, not assumed and not
  failed on.
- The source worktree named in this mission's brief,
  C:\Users\marka\code\mt-feat-trip-diary-backend-2026-06-23, was confirmed PRESENT
  on disk at authoring time (2026-07-01 session), checked out on branch
  feat/trip-diary-backend-2026-06-23, with 375e40b resolvable as a commit in its
  history (`git log --oneline -1 375e40b` succeeds there). If it is absent when this
  mission fires, resolve the commit directly from the target repo's own object
  database instead — it is already there, confirmed above — a missing
  source-worktree directory is not by itself a mission failure when the commit is
  otherwise resolvable.

Steps:
  1. In the target repo C:\Users\marka\code\mt-integration-2026-06-22, confirm HEAD
     is TARGET-BRANCH main (`git rev-parse --abbrev-ref HEAD` must print "main"; if
     it does not, run `git checkout main` first — the repo is shared substrate and
     HEAD may have moved). Then check whether 375e40b is already present via
     `git merge-base --is-ancestor 375e40b HEAD` (exit 0 means already integrated).
     [command] checkout-main-and-check-already-integrated
  2. If step 1 reports NOT already integrated: locate the commit in the source
     worktree C:\Users\marka\code\mt-feat-trip-diary-backend-2026-06-23 (branch
     feat/trip-diary-backend-2026-06-23); if that path does not exist, resolve
     375e40b directly from the target repo's own object database (it is already
     there). Then apply it via `git cherry-pick 375e40b` on TARGET-BRANCH main.
     [command] integrate-commit
  3. If cherry-pick reports conflicts on docs/QC-PIPELINE-SOTA-2026-06-23.md or any
     other ALLOW-FILES entry, resolve each conflicted hunk by hand so the resolved
     content contains BOTH the target's pre-existing change AND this feature's
     change — never `--ours` / `--theirs` and never a resolution that deletes
     either side's content. If the cherry-pick instead reports the change is
     already applied or produces an empty commit, skip creating a commit for this
     step and proceed — that outcome is a pass, not a failure.
     [edit] resolve-conflicts
  4. Identify every .js/.mjs file this integration actually touched or
     conflict-resolved (the confirmed diff stat for 375e40b touches none). Run
     `node --check` on each such file and confirm exit 0. If the touched-file set
     is empty, record that explicitly as the check result rather than silently
     skipping the step. [command] node-check-all
  5. If step 2/3 produced a new commit, stage and commit the integrated result in
     the target repo with a clear message identifying the integrated feature and
     its origin commit (referencing "mt-feat-trip-diary-backend-2026-06-23" and
     "375e40b"). If step 1 already found the commit present, no new commit is
     needed. Either way, confirm `git status` afterward shows a clean working tree
     on TARGET-BRANCH main. [command] commit-and-verify-clean

Done means:
  - docs/QC-PIPELINE-SOTA-2026-06-23.md exists in the target repo on TARGET-BRANCH
    main with non-trivial content: it contains the header "QC Pipeline
    (post-SOTA-uplift 2026-06-23)" and lists all 8 numbered mandatory gates
    (endpoint runtime, cross-project leak detection, headless console errors,
    Sentry loader present, the 5-seat deliberation panel, visual regression,
    Lighthouse score regression, and bundle-size regression) plus the
    escape-hatches section beneath them.
  - `git log` on the target repo's main branch shows commit 375e40b present in
    history (whether it landed there before this mission ran or as a direct result
    of this mission's cherry-pick), with its message "docs(qc): SOTA-uplift QC
    pipeline — 8 mandatory gates + escape hatches" intact — confirmed by
    `git log --oneline | grep 375e40b` returning a match, not assumed.
  - This diff touches zero .js/.mjs files (confirmed via `git show --stat 375e40b`),
    so the `node --check` gate is satisfied by explicitly recording that empty
    touched-file set — not by a fabricated "ran node --check and it passed" claim
    against files that were never part of this diff. If any conflict resolution in
    Step 3 introduced or modified a .js/.mjs file, `node --check` on that file must
    exit 0 instead.
  - `git status` in the target repo (main branch) shows a clean working tree (no
    uncommitted changes) at the end of the mission.
  - This diff touches no HTML (confirmed via `git show --stat 375e40b`); this
    mission carries no headless-render witness requirement as a result.
  - None of the above constitutes a judgment that the QC pipeline documented in
    docs/QC-PIPELINE-SOTA-2026-06-23.md is itself correct, complete, or safe to
    adopt (e.g. that the 5-seat panel composition or the escape-hatch policy is
    right), nor that any feature code is correct — that determination belongs to
    this mission's own witness + verdict-panel run downstream, not to this
    mission's authorship or mechanical execution.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

MISSION-ID: mt-integrate-dynamic-favicon-fire-ban.S1
MISSION-CLASS: code-repo
PARENT: mt-integrate-dynamic-favicon-fire-ban
TARTIB-INDEX: 1 of 2
VISUAL-QC-REQUIRED
REQUIRES: none (first in tartib order)
STEPS: 8

PARENT MAQSAD: Land commit d48f959 ("feat(dynamic-favicon): peripheral fire-ban indicator on the

Maqsad: sub-mission 1 of 2 — Record target repo baseline: confirm the repo is on some HEAD commit before touching anything (branch/log left for operator visibility in stdout; the witness itself just asserts HEAD resolves). through Syntax-check the landed dynamic-favicon.js with node --check; must exit 0.

Steps:
  1. Record target repo baseline: confirm the repo is on some HEAD commit before touching anything (branch/log left for operator visibility in stdout; the witness itself just asserts HEAD resolves). [command] 
  2. Confirm feature commit d48f959 is object-reachable in the target repo (same underlying repo as the source worktree, so no fetch expected to be needed); fails closed if the object type printed is not 'commit'. [command] 
  3. Cherry-pick d48f959 onto whatever branch is HEAD at fire time. This single git operation is the sole authoring step for all four ALLOW-FILES entries (icon-fire-ban.svg, index.html, js/dynamic-favicon.js, map.html) — they are never edited individually. Attempts a --continue --no-edit as a no-op-safe fallback in case a conflict left the pick mid-flight, then asserts a dynamic-favicon commit landed at HEAD. [command] 
  4. Read js/dynamic-favicon.js from disk (not trusting the cherry-pick exit code alone) and assert it exists with no leftover conflict markers. [verify] js/dynamic-favicon.js
  5. Read icon-fire-ban.svg from disk and assert it exists with no leftover conflict markers. [verify] icon-fire-ban.svg
  6. Read index.html and assert both the new <link id="mt-favicon"> and the dynamic-favicon.js <script> tag are present with no leftover conflict markers, meaning the feature's lines landed alongside whatever pre-existing head content the repo carried. [verify] index.html
  7. Read map.html and assert the mt-favicon tag landed with no leftover conflict markers, confirming the auto-merge preserved both sides. [verify] map.html
  8. Syntax-check the landed dynamic-favicon.js with node --check; must exit 0. [verify] js/dynamic-favicon.js

Done means: js/dynamic-favicon.js, icon-fire-ban.svg, index.html, map.html exist/are updated as specified — verify by headless-browser render, not by reading the code

MISSION-ID: M-CORPUS.COMPLETE.1.S1
MISSION-CLASS: build-program (multi-stage; decompose under the Hajj mission-architecture — Architect splits, no hand-authored steps)
PARENT: M-CORPUS.COMPLETE.1  (sub-mission 1 of 5, auto-split by the size ceiling)
REQUIRES: none (first sub-mission of 5 in tartib order)

Niyyah: the factory's paid-for knowledge is useless while it is scattered and incomplete — six cards in six folders

Maqsad: this is part 1 of 5 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): a single, indexed, queryable knowledge base distilled from EVERY source in the P0-CORPUS census (the

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - STAGE 1 — Clone nxtlvl-portal (private, gh-authed) read-only to _src/nxtlvl-portal/ as the card source. TOLERATE-AND-CONTINUE: on clone failure write a .gap marker and continue (the card step skips, never hard-fails). Source dir and card output dir are deliberately different namespaces (fixes the 02:10/02:38 discovery-gate loop).
  - STAGE 1 — Clone CGSports (private, gh-authed) read-only to _src/CGSports/. GitHub ONLY — the N:\ NAS is forbidden (it caused the 01:39 hard-fail). TOLERATE-AND-CONTINUE with a .gap marker on failure.
  - STAGE 1 — Clone nfl-predictions (private, gh-authed) read-only to _src/nfl-predictions/. TOLERATE-AND-CONTINUE with a .gap marker on failure.
  - STAGE 1 — Author the nxtlvl-portal pattern card by READING _src/nxtlvl-portal/ (never the empty output dir). Same six-section shape as card-2boots/card-tidaltreasures: what it does well, recurring components, design tokens, copy/voice patterns, vertical fit, reuse-vs-avoid — each claim cited to a file ref in the source. If _src/nxtlvl-portal.gap exists, write a short skip-gap card noting the missing source instead of fabricating.
  - STAGE 1 — Author the CGSports pattern card by READING _src/CGSports/. Same six-section card shape, cited to source file refs. If _src/CGSports.gap exists, write a short skip-gap card instead of fabricating.
  - STAGE 1 — Author the nfl-predictions pattern card by READING _src/nfl-predictions/. Same six-section card shape, cited to source file refs. If _src/nfl-predictions.gap exists, write a short skip-gap card instead of fabricating.
  - STAGE 1 — Re-author the thin card-regulativity (currently ~21 lines) to full six-section depth, reading and PRESERVING its existing content then deepening it. Must list the existing file as a dependency so the executor regenerates from current content, not from scratch (APPEND/MODIFY rule).

Done means: steps M-CORPUS.COMPLETE.1.S1.1..M-CORPUS.COMPLETE.1.S1.7 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — the census's 17 sources are all carded (or explicitly skip-verdicted), the library INDEX
resolves to every card, PATTERNS.md exists, and the AnythingLLM workspace answers the operator's example query with
a cited answer. Nothing claims done without that query receipt.

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
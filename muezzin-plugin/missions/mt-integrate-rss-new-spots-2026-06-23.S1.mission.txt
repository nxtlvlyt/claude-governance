MISSION-ID: mt-integrate-rss-new-spots-2026-06-23.S1
MISSION-CLASS: code-repo
PARENT: mt-integrate-rss-new-spots-2026-06-23
TARTIB-INDEX: 1 of 2
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - functions/feed/new-spots.xml.js
  - functions/feed/new-spots/[province].xml.js
REQUIRES: none (first in tartib order)
STEPS: 8

PARENT MAQSAD: The commit "60d13c1 feat(feed): RSS feeds for new community spots" is

Maqsad: sub-mission 1 of 2 — Inspect commit 60d13c1's patch stat before integrating. Commit 60d13c1 is already a reachable object inside the target repo's own object store (confirmed via git cat-file -t in Context, since source and target are worktrees of the same repo), so this reads it directly in the target repo rather than reaching outside the sandbox to the source worktree's absolute path. through Commit ONLY the two staged ALLOW-FILES paths with a message naming the integrated feature and its origin commit, leaving anything else staged from step 2's snapshot untouched.

STEP-SHAPE DISCIPLINE (plan-gate findings 2026-07-09, each embedded below as a literal command — the planner must use these commands verbatim, not improvise):
  - stdout-only baselines: NO scratch files (commit-stat.txt / pre-state.txt killed the prior plan twice — containment-drift guard); every inspection writes to stdout.
  - cherry-pick completion in the SAME command: a conflicted pick exits non-zero; the command must tolerate that exit and assert the intended state instead.
  - witnesses check the ARTIFACT (Test-Path + content Select-String), never a sha in a git-log window.
  - one implementation file per micro-action: steps 4 and 5 are the only [edit] steps, one file each.

Steps:
  1. Inspect commit 60d13c1's patch stat, stdout only. Run exactly: git show 60d13c1 --stat --name-only --format=""; if ($LASTEXITCODE -ne 0) { exit 1 } [command]
  2. Record pre-existing worktree state, stdout only. Run exactly: git status --porcelain; exit 0 [command]
  3. Cherry-pick the feature commit into the index without auto-committing; a conflict exit is TOLERATED (steps 4-5 resolve it) — the assertion is that both ALLOW-FILES now exist in the worktree. Run exactly: git cherry-pick -n 60d13c1; if (-not ((Test-Path 'functions/feed/new-spots.xml.js') -and (Test-Path 'functions/feed/new-spots/[province].xml.js'))) { exit 1 }; exit 0 [command]
  4. If step 3 reported a conflict on functions/feed/new-spots.xml.js, hand-resolve it keeping BOTH this repo's pre-existing content and the feature's content (never --ours/--theirs); done when the file has zero conflict markers (Select-String '^<{7}|^={7}|^>{7}' finds nothing). If no conflict occurred, no-op. [edit] functions/feed/new-spots.xml.js
  5. If step 3 reported a conflict on functions/feed/new-spots/[province].xml.js, hand-resolve it the same way; done when the file has zero conflict markers. If no conflict occurred, no-op. [edit] functions/feed/new-spots/[province].xml.js
  6. Stage exactly the two ALLOW-FILES paths by name. Run exactly: git add -- 'functions/feed/new-spots.xml.js' 'functions/feed/new-spots/[province].xml.js'; if ($LASTEXITCODE -ne 0) { exit 1 } [command]
  7. Syntax-check both integrated files (artifact witness, not log witness). Run exactly: node --check 'functions/feed/new-spots.xml.js'; if ($LASTEXITCODE -ne 0) { exit 1 }; node --check 'functions/feed/new-spots/[province].xml.js'; if ($LASTEXITCODE -ne 0) { exit 1 } [verify]
  8. Commit ONLY the two staged ALLOW-FILES (idempotent on staged-diff presence, the k6 step-8 proven shape). Run exactly: git diff --cached --quiet -- 'functions/feed/new-spots.xml.js' 'functions/feed/new-spots/[province].xml.js'; if ($LASTEXITCODE -eq 0) { Write-Output ALREADY_COMMITTED } else { git commit -m "feat(feed): RSS feeds for new community spots (integrate 60d13c1)" -- 'functions/feed/new-spots.xml.js' 'functions/feed/new-spots/[province].xml.js'; if ($LASTEXITCODE -ne 0) { exit 1 } } [command]

Done means: functions/feed/new-spots.xml.js and functions/feed/new-spots/[province].xml.js exist at HEAD committed with a message naming the feature and origin 60d13c1 — verify by executed command receipts (node --check + git show HEAD --name-only), not by headless-browser render (feed endpoints and a dynamic route are not renderable pages; render clause removed per QUEUE.md 2026-07-10 boilerplate-class entry)

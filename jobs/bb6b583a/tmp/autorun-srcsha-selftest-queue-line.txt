missions/engine-srcsha-anchor-selftest-coverage.mission.txt  <!-- QUEUED 2026-07-13 (conductor): GAP OWNER for gap-conduct-cycle-srcsha-anchor — closes its own closes_when condition (a selftest proving a bare hex mention equal to current HEAD never verdicts FULL). The code fix already landed (7e0a011); this adds the 4 regression checks that were never written. Committed patcher missions/_logs/srcsha-anchor-selftest-coverage-patch.mjs verified this session via a CRLF-faithful Copy-Item scratch copy (per the recorded lesson about git show masking line-ending mismatches): PATCHED, node --check clean, full selftest run — 4/4 new checks PASS, zero NEW regressions (one pre-existing, separately-owned FAIL from the still-parked fixture-update mission explicitly excluded by name in the validation step, not silently swallowed). lint OK; engine- stem exempt from GAP-PRIORITY-HOLD. -->

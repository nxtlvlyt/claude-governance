MISSION-ID: M-ENGINE.ATTEMPT2-VALIDATION-REPLAY-FIX
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\.claude\muezzin-plugin
ALLOW-FILES:
  - deconstructor.mjs
REQUIRES: none
STEPS: 3

Maqsad: close gap-attempt2-validation-replay at the engine — the blind-architect
planning prompt (archFraming) never told architects that for action_type
'command'/'verify' steps, validation_command IS the entire executed step (no separate
main-action field exists). The integrator prompt already carries this rule
(QUEUE_INSTRUCTION), but architects plan BEFORE the integrator runs and never see it,
so they routinely describe real dispatch logic only in prose and leave
validation_command as a bare existence-check. Live receipt: a Stitch relay-dispatch
step was repeatedly planned this way, so each ~4-minute re-plan's real dispatch never
ran and the step died in under a second, four times in a row (459/436/625/555ms),
wasting ~16 minutes total on one mission alone. A committed, dry-run-verified patcher
script already exists at
missions/_logs/deconstructor-archframing-validation-command-patch.mjs (idempotent,
exits ALREADY-PATCHED on a second run, applies the same fix to both this repo and its
agy-muezzin sibling) — this mission runs it rather than hand-editing.

niyyah: teach the architects the same rule the integrator already has, without
changing what a legitimate pure-verify step looks like (a verify step that only reads
a file created by an earlier step stays valid) — this closes the prose-only-dispatch
class specifically, not verification in general.

Steps:
  1. Baseline, stdout only. Run exactly: git rev-parse --abbrev-ref HEAD; git rev-parse HEAD; node --check deconstructor.mjs; if ($LASTEXITCODE -ne 0) { Write-Output PRE-CHECK-FAIL; exit 1 }; Write-Output PRE-CHECK-PASS [command]
     validation_command: node --check deconstructor.mjs
  2. Run the committed patcher exactly: node missions/_logs/deconstructor-archframing-validation-command-patch.mjs deconstructor.mjs; if ($LASTEXITCODE -ne 0) { exit 1 } [command]
     validation_command: node --check deconstructor.mjs; if ($LASTEXITCODE -ne 0) { exit 1 }; if (-not (Select-String -Path deconstructor.mjs -Pattern 'gap-attempt2-validation-replay' -Quiet)) { exit 1 }
  3. Commit EXACTLY: git add -- deconstructor.mjs; git diff --cached --quiet; if ($LASTEXITCODE -eq 0) { Write-Output ALREADY_COMMITTED } else { git commit -m "fix(engine): archFraming teaches blind architects the COMMAND/VERIFY-steps-are-engine-executed rule the integrator already had (gap-attempt2-validation-replay; live receipt: a Stitch dispatch step's validation_command was repeatedly planned as a bare existence-check, wasting ~16min across 4 re-plan cycles)"; if ($LASTEXITCODE -ne 0) { exit 1 } } [command]

Done means: archFraming in deconstructor.mjs states explicitly that for
action_type 'command'/'verify' steps, validation_command IS the step and must be the
complete working command, not a bare existence-check; node --check passes; committed.
NOTE FOR THE CONDUCTOR (not a step): this changes an LLM prompt, not code logic —
node --check proves the file parses, it cannot prove architects behave differently.
That can only be judged from real mission planning receipts after this ships. Fix
goes live per-daemon at next restart. The agy-muezzin sibling needs the identical fix
via a separate mission (different REPO-ROOT, same patcher) — do not consider this
gap closed until both land.

# CONDUCTOR PROVENANCE (2026-07-13): constructed per the five-verb law and the
# conductor-direct exception conditions — CLASS fails (deconstructor.mjs is the
# engine's own mission-planning machinery) and SIZE fails (a multi-sentence prompt
# insertion, not a config value). Gap-priority: closes gap-attempt2-validation-replay,
# one of six bite-class gaps behind GAP-PRIORITY-HOLD. Patcher verified twice this
# session: once by the preparing sub-agent (dry-run PATCHED + node --check clean +
# selftest 117/117 PASS with zero regressions, idempotency confirmed, a real bug in
# the patcher's own idempotency marker caught and fixed during that dry-run), once
# independently by the conductor in a fresh scratch copy (matching result, zero
# discrepancy) before this mission was written.

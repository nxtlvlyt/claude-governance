MISSION qc-fix-oracle-quick-actions-oracle-quick-actions-js-2026-06-24
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: main
ALLOW-FILES:
  - map.html
  - js/oracle-quick-actions.js
REQUIRES: none

Niyyah: A traveller who just learned a spot is Crown-land camping-legal has an immediate
follow-up question ("where's the nearest fuel/water/campsite?"). The Oracle popup's
quick-action buttons exist to answer that in one tap. The 2026-06-24 QC walk found the
card absent from every screenshot. It has never once rendered for a real visitor.

Maqsad: Restore the Oracle quick-actions card (.mt-oracle-quick, 4 buttons: fuel/water/
cell/campsite) so it reliably mounts inside the Crown-land Oracle popup on map.html and
its buttons fire the existing /pois.json?near= lookup on click.

Context (unbiased facts, re-derived from substrate this session — do not trust the prior
mission brief's assumptions, two of them were wrong):
- RE-VERIFIED ROOT CAUSE #1 (new finding, the prior mission never checked this): map.html
  does NOT load js/oracle-quick-actions.js at all. `git show main:map.html | grep -c
  oracle-quick-actions` = 0. The <script> tag WAS added when the feature was built
  (commit 2e3004a, "map.html: load the new module after crown-land-overlay.js") but is
  absent from main and from every commit since (git log --oneline --all -S
  "oracle-quick-actions.js" -- map.html shows only 2e3004a ever touching that string; the
  tag was lost somewhere in a divergent branch/rebase history that never carried it
  forward). Net effect: the file has never executed in a real browser, regardless of any
  bug inside it. This is why the QC walk found nothing to click.
- RE-VERIFIED ROOT CAUSE #2 (the prior mission's actual, already-authored fix — sound,
  just never merged): even with the tag restored, js/oracle-quick-actions.js's init()
  mounts the card via `map.on('popupopen', function (e) { setTimeout(function () {
  attachToPopup(map, e.popup); }, 0); })`. The Oracle popup's own content is set
  asynchronously (fetch().then()), so the zero-delay setTimeout fires before the marker
  text ("Crown-land legality") exists in the DOM; isOraclePopup() returns false;
  attachToPopup() returns early and permanently gives up for that popup open. This exact
  fix was already authored, reviewed, and committed once: git sha
  07e3cf949c32453ede34ebc6c3e570bb80f05ece, "qc-fix(oracle-quick-actions): mount
  quick-action card + wire map-context handler per QC walk 2026-06-24", on branch
  feat/qc-fix-oracle-quick-actions-oracle-quick-actions-js-2026-06-24 — but that branch
  was abandoned when the OLD mission died at its verification step and was never merged
  to main. `git show 07e3cf9 -- js/oracle-quick-actions.js` shows the full diff: it adds a
  tryAttach(map, popup, attempt) function that polls with backoff (50 + attempt*40 ms,
  giving up silently after 10 attempts) until isOraclePopup(content.innerHTML) is true,
  then calls the existing attachToPopup(map, popup). It uses only functions that already
  exist in the file (isOraclePopup, attachToPopup) — no new helpers needed.
- The click-wiring itself ("fire the map-context query on click") is NOT broken and needs
  no change: attachToPopup() already wires built.wrap's click listener to
  lookupNearest(latlng, action.node_type), which fetches /pois.json?near=... — and
  functions/pois.json.js already implements that ?near= mode (added in the same original
  commit 2e3004a, still present and untouched on main). The buttons were never "absent"
  because they were unwired — they were absent because the card holding them never
  mounted (root causes #1 and #2 above).
- RULED OUT (do not repeat this mistake): grounding this fix against
  docs/FEATURE-CATALOG-2026-06-23.md's oracle-quick-actions section, as all three
  architect panels expected. That file does not exist on main at all (`git show
  main:docs/FEATURE-CATALOG-2026-06-23.md` => "does not exist in 'main'") and on every
  branch where it DOES exist (each carries its own one-off "docs(qc): bring
  FEATURE-CATALOG-2026-06-23.md onto branch" commit, dozens of them across unrelated
  missions), grepping it for "oracle" (case-insensitive) returns zero matches — it has
  never once documented this feature. The architects' grounding premise was never true;
  do not go looking for it again.
- RULED OUT (do not repeat this mistake): verifying via `node e2e-runner.mjs
  oracle-quick-actions`. That file does not exist at the repo root — the real script is
  scripts/e2e-runner.mjs, invoked as `node scripts/e2e-runner.mjs --target <url>
  --features <slug>` (see its own header comment). This is the exact wrong-path mistake
  that killed the prior mission at its final step. Do not use this path even corrected,
  though — see the Done-means note below on why it still can't verify this feature.

Steps:
  1. In map.html, restore the missing script tag by replacing the exact substring
     <script src="js/crown-land-overlay.js" defer></script><script src="js/municipal-parking-panel.js" defer></script>
     with
     <script src="js/crown-land-overlay.js" defer></script><script src="js/oracle-quick-actions.js" defer></script><script src="js/municipal-parking-panel.js" defer></script>
     (this exact substring occurs exactly once in map.html; this restores the original
     2e3004a placement — load order right after crown-land-overlay.js, before
     municipal-parking-panel.js). [edit] restore-script-tag
  2. In js/oracle-quick-actions.js, apply the equivalent of the already-proven patch from
     git sha 07e3cf949c32453ede34ebc6c3e570bb80f05ece (view it first with: git show
     07e3cf949c32453ede34ebc6c3e570bb80f05ece -- js/oracle-quick-actions.js). Concretely:
     add a new function above init():
       function tryAttach(map, popup, attempt) {
         if (attempt >= 10) return;
         var el = popup && popup.getElement && popup.getElement();
         if (!el) return;
         var content = el.querySelector ? el.querySelector('.leaflet-popup-content') : null;
         if (!content) return;
         if (content.getAttribute('data-mt-quick-actions') === 'done') return;
         if (!isOraclePopup(content.innerHTML)) {
           setTimeout(function () { tryAttach(map, popup, attempt + 1); }, 50 + attempt * 40);
           return;
         }
         attachToPopup(map, popup);
       }
     and inside init()'s map.on('popupopen', ...) handler, replace
       setTimeout(function () { attachToPopup(map, e.popup); }, 0);
     with
       tryAttach(map, e.popup, 0);
     (removing the now-obsolete "defer one tick" comment above that line is fine but not
     required). Do not touch attachToPopup, isOraclePopup, lookupNearest, or any other
     function — they are correct as-is. [edit] fix-async-mount-race
  3. Run: node --check js/oracle-quick-actions.js
     Must exit 0 (no syntax regression). [command] syntax-check
  4. Run: (Select-String -Path map.html -Pattern 'src="js/oracle-quick-actions.js"' -AllMatches).Matches.Count
     Must equal 1 (script tag restored exactly once — not zero, not duplicated).
     [command] verify-script-tag
  5. Run: Select-String -Path js/oracle-quick-actions.js -Pattern 'mt-oracle-quick' -Quiet
     and: Select-String -Path js/oracle-quick-actions.js -Pattern 'addEventListener' -Quiet
     Both must return True. NOTE: these two checks were already True in the unfixed,
     never-loaded code before this mission — they prove the file wasn't corrupted, NOT
     that the fix works. Do not treat them as sufficient on their own (this is exactly
     what let the prior mission's Done-means look satisfied at steps 2-3 while the real
     bug was still live). [command] verify-file-intact
  6. Run: Select-String -Path js/oracle-quick-actions.js -Pattern 'function\s+tryAttach\s*\(' -Quiet
     and: Select-String -Path js/oracle-quick-actions.js -Pattern "tryAttach\(map,\s*e\.popup" -Quiet
     Both must return True — this is the actual differentiator: proof the popupopen
     handler now calls the bounded-retry mount path instead of the single fire-and-forget
     setTimeout(fn, 0). [command] verify-retry-fix

Done means:
  - map.html loads js/oracle-quick-actions.js via exactly one <script src=
    "js/oracle-quick-actions.js" defer></script> tag (step 4 receipt = 1). This is the
    difference between the feature existing on disk and the feature ever running in a
    browser — it currently only exists on disk.
  - js/oracle-quick-actions.js's popupopen handler mounts the quick-actions card through a
    bounded-retry tryAttach(map, popup, attempt), not a single zero-delay setTimeout, so it
    survives the Oracle popup's own async fetch().then() content population (step 6
    receipts both True).
  - node --check js/oracle-quick-actions.js exits 0 (step 3).
  - The pre-existing structural witnesses (mt-oracle-quick class, addEventListener wiring)
    still hold post-edit (step 5 receipts both True) — necessary, not sufficient alone.
  - Explicitly NOT required for Done, and NOT a substitute for the checks above: a green
    run of scripts/e2e-runner.mjs --target https://muddytires.ca --features
    oracle-quick-actions. docs/FEATURE-CATALOG-2026-06-23.md has no oracle-quick-actions
    section on any branch (grep-verified, see Context) — the runner would filter its
    feature list down to zero matches and exit 0 with features_run:0, an apparent "clean"
    that verifies nothing. Do not run this as a final gate and do not report it as
    evidence of Done; a future mission should add the missing catalog section before
    e2e-runner can meaningfully cover this feature. A real headless-browser click-through
    of the live Oracle popup is the natural next-level verification beyond this mission's
    scope and is left as a named follow-up, not fabricated here without a verified
    interaction sequence.

Amanah: entrusted; discharge with ihsan.

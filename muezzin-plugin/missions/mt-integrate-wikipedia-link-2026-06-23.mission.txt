MISSION MT-INTEGRATE-WIKIPEDIA-LINK-2026-06-23
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - functions/api/wikipedia-link.js
  - js/poi-wikipedia.js
  - map.html
REQUIRES: none
VISUAL-QC-REQUIRED
PREVIEW-BASE-URL: http://localhost:8788

Niyyah: Feature worktree mt-feat-wikipedia-link-2026-06-23 carries one finished commit —
a same-origin Wikipedia geosearch/proxy endpoint plus a popup chip for named POIs —
sitting in its own worktree, never landed on the integration line's current HEAD. This
mission's whole job is mechanical: get the feature's files onto the target repo's HEAD
without losing anything already there, and produce the node-level and render-level
receipts a verdict panel needs to judge the result. Writing this mission file, and the
act of integrating itself, is NOT verification that the feature's own code is correct.
Whether the 0.7*token-similarity + 0.3*proximity scoring actually picks the right
Wikipedia article for a given POI, whether the confidence > 0.7 gate on the chip is the
right threshold, whether the 30-day KV cache (including its negative-cache branch) ever
serves stale or wrong data, whether the popupopen listener's dedup/memoize logic leaks
across sessions or silently drops results when a popup closes mid-fetch, and whether the
upstream-failure path genuinely degrades to match:null + warnings rather than fabricating
an answer — none of that is settled by landing the commit. That determination belongs
entirely to this mission's own witness-and-verdict-panel run: the node --check receipt,
the headless-browser render of map.html, and the seat panel's judgment on the actual
integrated diff. Say plainly: authoring or executing this mission proves nothing about
the feature code's correctness by itself.

Maqsad: Land commit cf373c9 ("feat(wikipedia-link): /api/wikipedia-link + popup chip for
named POIs") from the source worktree C:\Users\marka\code\mt-feat-wikipedia-link-2026-06-23
onto the current HEAD of the target repo C:\Users\marka\code\mt-integration-2026-06-22,
with any conflicts resolved by keeping BOTH sides — this repo's existing unrelated
changes AND the feature's changes, never silently dropping either — every changed
.js/.mjs file passes `node --check`, the result is committed with a clear message, and
the target repo's working tree is left clean with respect to every ALLOW-FILES entry.

Context (verified against live substrate this session, 2026-07-01, absolute paths, no
prescribed resolution beyond "keep both sides"):
- SOURCE-PATH CORRECTION: the work order that produced this mission named the source
  worktree "C:\Users\marka\code\wikipedia-link-2026-06-23" — that path does NOT exist on
  disk (verified this session, `git -C` against it fails with "No such file or
  directory"). The real worktree carrying commit cf373c9 is
  C:\Users\marka\code\mt-feat-wikipedia-link-2026-06-23, confirmed this session both via
  `git -C C:\Users\marka\code\mt-integration-2026-06-22 worktree list` (which lists every
  worktree sharing this repo's object store) and via `git log --oneline -3` inside that
  worktree showing cf373c9 at its tip on branch feat/wikipedia-link-2026-06-23. Use the
  verified path below, not the unverified one the work order named.
- Confirmed diff stat for cf373c9cf9ef173f4345cd9ad620dbe96d4dc9f7 (`git show --stat`,
  this session): 3 files changed, 666 insertions(+), 1 deletion(-) —
  functions/api/wikipedia-link.js (423 new lines: geosearches Wikipedia's API within
  10 km of a POI's lat/lng, scores candidates by 0.7*token-similarity + 0.3*proximity,
  returns the best match's title/url/extract/distance/confidence, with a 30-day KV cache
  on MT_OVERPASS_CACHE including a negative-cache branch, and degrades to match:null +
  warnings on upstream failure rather than fabricating a result), js/poi-wikipedia.js
  (242 new lines: a popupopen listener that lazily fetches and injects the chip only when
  confidence > 0.7, dedups in-flight requests, memoizes per session, and drops the result
  silently if the popup closes before the fetch resolves), and map.html (+1/-1: a single
  new `<script src="js/poi-wikipedia.js" defer></script>` tag inserted into the existing
  script-tag chain, immediately after the js/poi-provenance.js tag). This exactly matches
  the diff stat reported in the work order. No other content in either new file changes.
- Target repo HEAD observed this session (`git log --oneline -1`): moved between 5e19874
  (main), 9d22699 ("docs(api): add auto-generated API reference 2026-06-23"), and back to
  5e19874 across consecutive commands with no action by this session causing any of those
  moves — the same shared-repo HEAD-race hazard other missions against this same target
  have already named (other missions/dry-runs run concurrently against this repo). Do not
  assume any specific commit or branch is still checked out by the time this mission
  actually fires; re-confirm branch and HEAD live as the first step. Because of this
  observed volatility, this mission deliberately carries no TARGET-BRANCH header — land
  onto whatever branch is actually checked out as HEAD when the mission fires, not a
  hardcoded branch name.
- The commit object cf373c9cf9ef173f4345cd9ad620dbe96d4dc9f7 is already present in the
  target repo's own object store (confirmed via `git cat-file -t` returning "commit" this
  session, since worktrees under C:\Users\marka\code share object storage) — cherry-pick
  can reference the SHA directly with no fetch/remote-add step needed. `git merge-base
  --is-ancestor cf373c9 HEAD` returned false this session (not yet landed).
- Verified this session that neither functions/api/wikipedia-link.js nor
  js/poi-wikipedia.js exists yet anywhere in the target repo's working tree (a clean
  new-file add is expected for both); map.html DOES already exist and is the file
  receiving the script-tag insertion.
- A dry-run cherry-pick performed this session (`git cherry-pick --no-commit -n`, on a
  throwaway branch off the then-current HEAD, fully reverted afterward via
  `git cherry-pick --abort`, `git reset --hard HEAD`, `git clean -fd`, and `git branch -D`
  before this mission file was written, leaving the target repo exactly as found —
  confirmed clean via `git status --short` and `git log --oneline -1` afterward) showed
  functions/api/wikipedia-link.js and js/poi-wikipedia.js apply as clean new-file adds
  with ZERO conflict, but map.html DOES conflict: the target's current HEAD had already
  inserted a `js/plan-day-weather.js` script tag (after plan-day.js) and a second
  `js/poi-photos.js` position shift that cf373c9's own parent tree did not have, while
  cf373c9 inserts `js/poi-wikipedia.js` right after the js/poi-provenance.js tag — both on
  the same long physical script-chain line, so git reports a content conflict on that
  line rather than a clean 3-way merge. The correct resolution (confirmed by reading both
  conflict sides this session) is to keep EVERY script tag from the target's HEAD side
  (including js/plan-day-weather.js) AND insert the new
  `<script src="js/poi-wikipedia.js" defer></script>` tag immediately after the existing
  `<script src="js/poi-provenance.js" defer></script>` tag from cf373c9's side — never
  `--ours` / `--theirs`, and never a resolution that drops the target's
  plan-day-weather.js tag or any other pre-existing tag.
- This diff touches map.html (an HTML file), which is why VISUAL-QC-REQUIRED and
  PREVIEW-BASE-URL are declared above. http://localhost:8788 is a PLACEHOLDER for a local
  `wrangler pages dev` run — the operator must confirm the port a real local preview
  server actually binds to on the machine firing this mission before relying on the
  captured render; if nothing answers at PREVIEW-BASE-URL, that must surface as a named
  NO_PREVIEW_BASE_URL-style receipt, not a silently skipped check.
- Nothing about this mission's authorship constitutes verification that the feature code
  is correct — that is exactly what this mission's own witness and verdict-panel run,
  downstream of the integration, will determine.

Steps:
  1. In the target repo, confirm the actually-checked-out branch and HEAD
     (`git -C C:\Users\marka\code\mt-integration-2026-06-22 branch --show-current` and
     `git -C C:\Users\marka\code\mt-integration-2026-06-22 log --oneline -1`), and confirm
     neither functions/api/wikipedia-link.js nor js/poi-wikipedia.js already exists on
     disk. Because this session directly observed HEAD move between commands, treat
     whatever is checked out at this moment as the true landing point. [command]
     verify-target-state
  2. Check ancestry before attempting a cherry-pick: `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 merge-base --is-ancestor cf373c9
     HEAD`. If it reports cf373c9 is already an ancestor, it is already landed — skip
     cherry-picking, do not use --allow-empty. Otherwise it needs to be applied. [command]
     check-ancestry
  3. If step 2 found cf373c9 not yet landed, apply it via `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 cherry-pick cf373c9`. Expect a conflict
     on map.html's script-chain line (confirmed this session); resolve it by keeping EVERY
     script tag the target's HEAD side already had AND inserting
     `<script src="js/poi-wikipedia.js" defer></script>` immediately after the existing
     `<script src="js/poi-provenance.js" defer></script>` tag — never `--ours` /
     `--theirs` and never a resolution that drops either side's tags. If the cherry-pick
     comes back empty (already-applied after all), resolve with `--skip`, never
     `--allow-empty`. [edit] cherry-pick-and-resolve-map-html
  4. Run `node --check functions/api/wikipedia-link.js` and
     `node --check js/poi-wikipedia.js` from the target repo root and confirm both exit 0;
     also run `node --check` on any other .js/.mjs file step 3's conflict resolution
     touched. [command] node-check-all
  5. Stage and commit the integrated result with a message that names
     "mt-feat-wikipedia-link-2026-06-23" and/or "wikipedia-link" and cites cf373c9.
     Confirm `git status --short` afterward shows no uncommitted changes to any
     ALLOW-FILES entry and `git log --oneline -5` shows the landed commit at or near
     HEAD. [command] commit-and-verify-clean
  6. (amended 2026-07-07 per RULE 9: prior step hand-rolled a localhost:8788 server — nothing serves that port) In ONE command: confirm clean tree for this mission's ALLOW-FILES (git commit first if this mission's edits are not yet committed — never deploy a dirty tree), then wrangler pages deploy . --project-name=muddytires --branch=preview, capture the emitted *.pages.dev URL into a PowerShell variable, write scratch-qc.mjs (playwright render of /map.html from that URL, exit non-zero if the page throws a fatal script error or a console error names poi-wikipedia.js — both the pre-existing script tags and the new js/poi-wikipedia.js tag must execute), node scratch-qc.mjs, Remove-Item scratch-qc.mjs. [command] headless-render-witness

Done means:
  - `git log` in C:\Users\marka\code\mt-integration-2026-06-22 shows a commit at or near
    HEAD whose message names "mt-feat-wikipedia-link-2026-06-23" and/or "wikipedia-link"
    and cites cf373c9.
  - functions/api/wikipedia-link.js exists in the target repo with the feature's actual
    content (a geosearch handler scoring candidates by 0.7*token-similarity +
    0.3*proximity within a 10 km radius, a 30-day KV cache on MT_OVERPASS_CACHE including
    a negative-cache branch, and a degrade-to-null-plus-warnings path on upstream
    failure) — confirmed by reading the on-disk file, not by trusting the cherry-pick's
    exit code alone.
  - js/poi-wikipedia.js exists in the target repo with the feature's actual content (a
    popupopen listener gated on confidence > 0.7, in-flight dedup, per-session
    memoization, and a silent drop if the popup closes before the fetch resolves) —
    confirmed by reading the on-disk file.
  - map.html contains exactly one
    `<script src="js/poi-wikipedia.js" defer></script>` tag inserted after the existing
    `<script src="js/poi-provenance.js" defer></script>` tag, AND every pre-existing
    script tag this repo's map.html already had before this mission ran (including
    js/plan-day-weather.js) is still present — both sides intact, confirmed by reading the
    file, not by trusting the cherry-pick's exit code alone.
  - `node --check functions/api/wikipedia-link.js` and `node --check js/poi-wikipedia.js`
    both exit 0, and `node --check` exits 0 on every other .js/.mjs file this integration
    touched or conflict-resolved.
  - `git status --short` in the target repo shows no uncommitted changes to any
    ALLOW-FILES entry.
  - A headless browser (headless Chrome / Playwright / puppeteer) render of /map.html
    served from PREVIEW-BASE-URL loads without a fatal script error, with both the
    pre-existing script chain and the new js/poi-wikipedia.js tag present in the served
    HTML — verified by rendering the page, not by reading the source. If PREVIEW-BASE-URL
    is the wrong local port, that must surface as a named NO_PREVIEW_BASE_URL receipt
    rather than a silently skipped check.
  - This mission's authorship and execution prove only that the integration is
    mechanically sound (declared files present with real content, feature commit reachable
    in target history, syntax checks pass, a render was actually captured) — not that the
    feature's code is correct, safe, or behaviorally right (e.g. that the Wikipedia
    match-scoring picks the right article, that the confidence > 0.7 gate is the right
    threshold, that the KV cache never serves stale or wrong data, or that the popup's
    dedup/memoize logic never leaks or drops results incorrectly). That judgment belongs
    entirely to this mission's own witness-and-verdict-panel run, downstream of the
    receipts above.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

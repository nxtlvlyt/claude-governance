MISSION QC-FIX-REVIEWS-SUBMISSION-RENDER-REVIEWS-JS-2026-06-24
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: main
ALLOW-FILES:
  - js/reviews.js
REQUIRES: none

Niyyah: A QC walk on 2026-06-24 found that js/reviews.js's review/"tip"-submission
form and render flow — star rating, the four noise/access/safety/signal subscore
pickers, the photo-upload field, openReviews() (fetch + render existing reviews),
and postReview() (submit) — had been silently deleted by an earlier refactor,
commit 6efab86 ("Author a pure, side-effect-free function computeCommunityCon..."),
which cut js/reviews.js from 395 lines to 101 while adding the confidence-tier
helper computeCommunityConfidence, discarding the 294 lines of UI code that two
prior commits (44e1e99, ca15fbf) had built. The same walk also found a real auth
bug worth folding into the same fix: functions/api/[[path]].js's validateSession()
authenticates a request by forwarding the inbound Cookie header to the accounts
worker's /me endpoint, so a postReview() fetch to /api/contribute/review that omits
credentials:'include' risks not sending that cookie and getting 401'd — the prior
inline postReview() in map.html sent only an Authorization header with no
credentials:'include' and hit exactly that 401.

A correct, complete restoration of this exact gap was already authored and
committed: 2b59ff9 ("qc-fix(reviews-submission-render): restore tip-submission
form + render flow per QC walk 2026-06-24"), on branch
feat/qc-fix-reviews-submission-render-reviews-js-2026-06-24 (present both locally
and as remotes/github/feat/qc-fix-reviews-submission-render-reviews-js-2026-06-24,
both in sync). That commit's own diff touches js/reviews.js alone — a pure
317-line append onto the identical 126-line stripped file that still sits on
main and master today — and its restored code carries the credentials:'include'
fix plus the full form/render flow (openReviews, postReview, subPickers, subChips,
setRating, setSubscore, the AXES_RV axis table). It was never merged forward:
`git merge-base --is-ancestor 2b59ff9 main` and the same check against master both
exit 1. This mission lands that same fix onto this REPO-ROOT's main branch, scoped
strictly to js/reviews.js — it does not touch map.html or any other file.

A prior firing of this exact mission (2026-06-25T22:56Z, two cycles) never reached
execution: both cycles failed in the PLANNING phase with "no valid JSON micro_queue
in the seat output" after 3 internal retries, and every architect-panel raw file
(granite4.1:30b / nemotron-3-super / qwen3.6:27b, both this attempt and the prior
one in the same sandbox) came back empty — a harness/seat-dispatch fault, not a
judgment on js/reviews.js; no plan content was ever produced to fail on its merits.
This rewrite does not fix that engine-side risk, but it removes the other failure
mode entirely: the target content already exists as a known-good git object, so
the executor restores it directly instead of inventing 294 lines of UI code from
memory.

Maqsad: Restore js/reviews.js's tip-submission form and render flow (star rating,
the four noise/access/safety/signal subscore pickers, the photo-upload field,
openReviews(), postReview(), all with credentials:'include' on every fetch) to the
exact working content already authored in commit 2b59ff9, landed on top of
whatever js/reviews.js currently holds on this REPO-ROOT's main branch, and commit
it. map.html already contains the mount point for this module — a pre-existing
`<script src="js/reviews.js" defer></script>` tag that loads after the page's
inline scripts, so js/reviews.js's own window.openReviews/window.postReview
assignments replace the (buggy) inline ones purely by load order. map.html is OUT
OF SCOPE for this mission and must not be edited — the feature self-mounts from
the tag that is already there.

Steps:
  1. Make sure the correct branch is checked out before touching anything: run
     `git status --short` to see current state, then `git checkout main` (this
     REPO-ROOT is a shared working tree other missions also use; do not assume the
     branch left checked out by a prior run is main). [command] checkout-target-branch
  2. Confirm the regression is still present on this branch: run
     `Select-String -Path js/reviews.js -Pattern 'openReviews|postReview|subPickers'`
     (or `grep -nE 'openReviews|postReview|subPickers' js/reviews.js`). If this finds
     ANY match, the regression is already fixed here — STOP, do not overwrite
     anything, skip directly to the Done-means checks in step 6 and report Done. If
     it finds NO match (the expected starting state — js/reviews.js holds only
     computeCommunityConfidence), proceed to step 3. [command] confirm-regression-present
  3. Confirm the known-good fix commit is reachable in this repo's object store:
     run `git cat-file -t 2b59ff9`. If it prints "commit", proceed to step 4. If the
     object is missing, run
     `git fetch github feat/qc-fix-reviews-submission-render-reviews-js-2026-06-24`
     (the remote branch holds the same commit) and retry the cat-file check.
     [command] confirm-fix-commit-reachable
  4. Restore ONLY js/reviews.js to that commit's exact content: run
     `git show 2b59ff9:js/reviews.js > js/reviews.js` (equivalently,
     `git checkout 2b59ff9 -- js/reviews.js`). Touch no other file — ALLOW-FILES
     for this mission is js/reviews.js alone, and the containment kernel refuses
     any other write target. [edit] restore-reviews-js-from-known-good-commit
  5. Verify the restored file is syntactically valid: run `node --check js/reviews.js`
     and confirm it exits 0. [command] node-check-reviews-js
  6. Verify the specific regression the QC walk caught is gone: run Select-String
     (or grep) against js/reviews.js and confirm EACH of these finds at least one
     match — `openReviews`, `postReview`, `subPickers`, `subChips`, `setRating`,
     `setSubscore`, `AXES_RV`, and a `credentials:` occurrence set to `include`
     inside the block that fetches `/api/contribute/review`. Also confirm
     `computeCommunityConfidence` is STILL present (this restores what was deleted;
     it must not delete what already worked). [command] verify-restoration-receipts
  7. Confirm map.html was not touched: run `git status --short` and confirm map.html
     does NOT appear in the output, and separately confirm
     `Select-String -Path map.html -Pattern 'reviews\.js'` (or grep) still finds the
     existing `<script src="js/reviews.js" defer>` mount tag, unchanged.
     [command] verify-map-html-untouched
  8. Stage and commit only js/reviews.js: run `git add js/reviews.js` then
     `git commit -m "qc-fix(reviews-submission-render): restore tip-submission form + render flow per QC walk 2026-06-24"`
     with a commit body naming the mission ID
     `M-MUDDYTIRES.QC-FIX-REVIEWS-SUBMISSION-RENDER-REVIEWS-JS-2026-06-24`.
     [command] commit-reviews-js-restoration
  9. Run `git status --short` one final time and confirm the working tree is clean
     (no modified or untracked files left from this mission's own work) and that
     `git show --stat HEAD` reports js/reviews.js as the only file in the new
     commit. [command] verify-scoped-clean-tree

Done means:
  - js/reviews.js contains all of: openReviews, postReview, subPickers, subChips,
    setRating, setSubscore, AXES_RV (Select-String/grep receipts) — the exact
    symbols the QC walk found missing when it wrote "tip form no longer mounted,
    new reviews didn't render".
  - js/reviews.js's fetch to /api/contribute/review includes credentials: 'include'
    (grep receipt) — the auth-bug fix, so the Cloudflare Pages proxy's
    validateSession() receives the forwarded Cookie header instead of 401-ing a
    signed-in visitor's post.
  - js/reviews.js references all four subscore axis keys — noise, access, safety,
    signal — confirming the 4-axis subscore pickers are restored, not just the star
    rating.
  - computeCommunityConfidence is still present, byte-identical in behavior, in the
    restored file — this mission restores what 6efab86 deleted, it does not touch
    what 6efab86 correctly added.
  - `node --check js/reviews.js` exits 0 (valid JavaScript, not a stub or a
    conflict-mangled partial).
  - map.html is byte-for-byte unchanged (`git diff` on map.html is empty); its
    pre-existing `<script src="js/reviews.js" defer>` tag is confirmed still
    present and is the only mount point this feature needs — editing map.html was
    never a legal step in this mission and none occurred.
  - `git status --short` shows a clean tree after the commit, and the new commit's
    `git show --stat` touches js/reviews.js only — no scope creep into any other
    file.

Context: REPO-ROOT is the live integration repo at
C:\Users\marka\code\mt-integration-2026-06-22; ALLOW-FILES restricts every write
this mission may make to js/reviews.js alone, and the containment kernel refuses
anything else regardless of what any step text says. Full end-to-end verification
(an authed browser session actually posting a tip and watching it render live) is
deferred, matching commit 2b59ff9's own stated scope ("e2e verification deferred
until auth fixture") — this mission's gates are static, file-content receipts on
the restored js/reviews.js, not a live browser render.
Amanah: entrusted; discharge with ihsan.

MISSION-ID: M-CORPUS.COMPLETE.1.S4
MISSION-CLASS: build-program (multi-stage; decompose under the Hajj mission-architecture — Architect splits, no hand-authored steps)
PARENT: M-CORPUS.COMPLETE.1  (sub-mission 4 of 5, auto-split by the size ceiling)
REQUIRES: predecessor M-CORPUS.COMPLETE.1.S3 DONE (tartib — sub-mission 4 runs after 3 of 5)

Niyyah: the factory's paid-for knowledge is useless while it is scattered and incomplete — six cards in six folders

Maqsad: this is part 4 of 5 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): a single, indexed, queryable knowledge base distilled from EVERY source in the P0-CORPUS census (the

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - STAGE 4 — Author POINTER.md: the stable, self-contained pointer naming the local library path (missions/_knowledge-base/), the AnythingLLM workspace slug (nxtlvl-knowledge), the chat endpoint, the API-key location, and a one-line query recipe so any agent can reach the KB. New file — no clobber risk.
  - STAGE 4 — Add a stable Knowledge Base pointer section to the website-pipeline manifest at missions/p0-corpus/P0-MISSIONS.md (path UNVERIFIED on this dispatch), naming missions/_knowledge-base/INDEX.md for offline read and the nxtlvl-knowledge workspace for natural-language query. Modifies an existing file — listed as its own dependency so the executor preserves prior content (APPEND/MODIFY rule).

Done means: steps M-CORPUS.COMPLETE.1.S4.1..M-CORPUS.COMPLETE.1.S4.2 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — the census's 17 sources are all carded (or explicitly skip-verdicted), the library INDEX
resolves to every card, PATTERNS.md exists, and the AnythingLLM workspace answers the operator's example query with
a cited answer. Nothing claims done without that query receipt.

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
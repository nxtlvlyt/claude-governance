MISSION MT-INTEGRATE-POI-HOVER-PREVIEW-CARDS
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: feat/qc-concern-sentry-error-capture-sentry-init-js-2026-06-25
ALLOW-FILES:
  - js/poi-hover-preview.js
  - map.html
REQUIRES: none
VISUAL-QC-REQUIRED
PREVIEW-BASE-URL: http://localhost:8788

Niyyah: The feature "poi-hover-preview-cards" (commit 3b8bb06, sitting in its source
worktree) has never been integrated into this repo's HEAD. This mission's entire job is
mechanical integration — land the commit onto the current tree without losing either
side's changes, and prove the result is syntactically sound and actually renders. Nothing
this mission does constitutes a judgment that the hover-preview-card feature's code is
correct, safe, or behaviorally sound — not whether the desktop mouseover card computes
distance-from-map-centre correctly, not whether the rating/thumbnail fallbacks degrade
gracefully when a POI lacks meta.image or photos[], not whether the mobile "Preview mode"
toggle's localStorage persistence (key mt-hover-preview-v1) actually round-trips across
reloads, and not whether binding via map-level layeradd genuinely keeps render()-rebuilt
markers wired. That determination belongs entirely to this mission's own witness-and-
verdict-panel run, downstream of this mission producing the integrated result — the
node --check receipt, the headless-browser render of map.html, and the seat panel's
judgment on the actual integrated diff. Authoring or executing this mission is not
verification of the feature.

Maqsad: The commit "3b8bb06 feat(map): POI hover-preview cards (desktop hover, mobile
preview-mode)" is cherry-picked or merged from the source worktree onto the current HEAD
of C:\Users\marka\code\mt-integration-2026-06-22, with any conflicts resolved by keeping
BOTH sides — this repo's existing unrelated changes AND the hover-preview-cards feature's
changes, never silently dropping either — every changed .js/.mjs file passes
`node --check`, the result is committed with a clear message, and the target repo's
working tree is left clean (git status shows no uncommitted changes to the touched
files).

Context (verified against live substrate this session, absolute paths, no prescribed
resolution beyond "keep both sides"):
- SOURCE-PATH CORRECTION: the work order that produced this mission named the source
  worktree "C:\Users\marka\code\poi-hover-preview-cards" — that path does NOT exist on
  disk (verified this session). The real worktree carrying commit 3b8bb06 is
  C:\Users\marka\code\mt-feat-hover-preview-cards, confirmed this session via
  `git log --oneline` showing 3b8bb06 at its tip. Use the verified path below, not the
  unverified one the work order named.
- Confirmed diff stat for 3b8bb06819a8bfb20bf95788cd8d239f56ce6e7e (git show --stat, this
  session): 2 files changed, 497 insertions(+), 1 deletion(-) — js/poi-hover-preview.js
  (496 new lines, a brand-new file implementing the desktop hover card and the mobile
  "Preview mode" toggle) and map.html (+1/-1: a single new
  `<script src="js/poi-hover-preview.js" defer></script>` tag inserted into the existing
  script-tag chain between the spot-list-sidebar.js and poi-provenance.js tags; map.html's
  script chain lives on one long physical line, so a one-tag insertion shows as that one
  line being replaced with itself plus the new tag, not a pure +1/-0 add). No other
  content in either file changes.
- Target repo HEAD (this session, `git log --oneline -1`):  28cbc72, on branch
  feat/qc-concern-sentry-error-capture-sentry-init-js-2026-06-25 — confirmed this is the
  branch actually checked out. Land the feature onto the branch that is actually checked
  out at fire time, not onto main.
- REPO VOLATILITY OBSERVED THIS SESSION: while verifying the above, this session's own
  read-only and dry-run checks against C:\Users\marka\code\mt-integration-2026-06-22
  observed its checked-out branch and HEAD move out from under the session between two
  consecutive commands (away to a `test-lhci-dryrun-2026-07-01` branch mid-cherry-pick,
  then back to feat/qc-concern-sentry-error-capture-sentry-init-js-2026-06-25), with no
  action taken by this session causing either move. This is the shared-repo HEAD-race
  hazard other missions in this engine have already named — do not assume the branch
  recorded above is still checked out by the time this mission actually fires; re-confirm
  it live as the first step, and if a different branch is checked out, check out
  TARGET-BRANCH explicitly before touching any file.
- The commit object 3b8bb06819a8bfb20bf95788cd8d239f56ce6e7e is already present in the
  target repo's own object store (confirmed via `git cat-file -t` returning "commit" this
  session, since worktrees under C:\Users\marka\code share object storage) — cherry-pick
  can reference the SHA directly with no fetch/remote-add step needed.
- Verified this session that js/poi-hover-preview.js does not exist yet anywhere in the
  target repo's working tree (a clean new-file add is expected); map.html DOES already
  exist and is the file receiving the single-line script-tag insertion. If either target
  has since diverged from what this session observed (e.g. an unrelated change now also
  touches the same physical script-chain line in map.html), resolve by keeping BOTH sets
  of content — every existing script tag AND the new poi-hover-preview.js tag — rather
  than overwriting one side.
- A dry-run cherry-pick of 3b8bb06 performed this session (`git cherry-pick --no-commit
  -n`, then fully reverted via `git reset --hard HEAD` + `git clean -fd` before this
  mission file was written, leaving the target repo exactly as found) applied with zero
  conflicts ("Auto-merging map.html", exit 0) against the HEAD observed at that moment.
  This is evidence the integration is expected to be mechanically clean, not a guarantee —
  the branch-race risk above means the tree this mission actually meets at fire time may
  differ.
- This diff touches map.html (an HTML file), which is why VISUAL-QC-REQUIRED and
  PREVIEW-BASE-URL are declared above. http://localhost:8788 is a PLACEHOLDER for a local
  `wrangler pages dev` run — the operator must confirm the port a real local preview
  server actually binds to on the machine firing this mission before relying on the
  captured render; if nothing answers at PREVIEW-BASE-URL, that must surface as a named
  NO_PREVIEW_BASE_URL-style receipt, not a silently skipped check.
- Nothing about this mission's authorship constitutes verification that the feature code
  is correct — that is exactly what this mission's own witness and verdict-panel run,
  downstream of the integration, will determine.

Steps:
  1. In the target repo, confirm the actually-checked-out branch and HEAD
     (`git -C C:\Users\marka\code\mt-integration-2026-06-22 branch --show-current` and
     `git log --oneline -1`). If the checked-out branch is not TARGET-BRANCH
     feat/qc-concern-sentry-error-capture-sentry-init-js-2026-06-25, check it out
     explicitly before any further step. Then confirm js/poi-hover-preview.js does not
     already exist on disk. [command] confirm-target-branch-clean
  2. In the source worktree C:\Users\marka\code\mt-feat-hover-preview-cards, confirm
     commit 3b8bb06819a8bfb20bf95788cd8d239f56ce6e7e exists (`git show 3b8bb06`) without
     modifying that worktree. [command] capture-patch
  3. In the target repo, at its current HEAD on TARGET-BRANCH, cherry-pick the commit
     (`git -C C:\Users\marka\code\mt-integration-2026-06-22 cherry-pick
     3b8bb06819a8bfb20bf95788cd8d239f56ce6e7e`) or an equivalent merge of that single
     commit. [command] integrate-commit
  4. If the cherry-pick (or equivalent merge) reports conflicts on js/poi-hover-preview.js,
     map.html, or any other ALLOW-FILES entry, resolve every conflicted hunk by hand so
     the resolved content contains BOTH the target's pre-existing content AND the
     hover-preview-cards feature's added content — never `--ours` / `--theirs` and never a
     resolution that deletes either side's content. [edit] resolve-conflicts
  5. Run `node --check js/poi-hover-preview.js` from the target repo root and confirm it
     exits 0; run `node --check` on every other .js/.mjs file the merge touched or
     conflict-resolved. [command] node-check-all
  6. If step 3 left the integration uncommitted (conflict resolution required `git add` +
     `git cherry-pick --continue`, or a manual merge commit), commit explicitly with a
     message that references the source commit's own subject line ("feat(map): POI
     hover-preview cards (desktop hover, mobile preview-mode)") and/or the SHA 3b8bb06.
     Then confirm `git status --short` is empty (clean working tree, no unmerged paths)
     and `git log --oneline -5` shows the landed commit at or near HEAD.
     [command] commit-and-verify-clean
  7. (amended 2026-07-07 per RULE 9: prior step hand-rolled a localhost:8788 server — nothing serves that port) In ONE command: confirm clean tree for this mission's ALLOW-FILES (git commit first if this mission's edits are not yet committed — never deploy a dirty tree), then wrangler pages deploy . --project-name=muddytires --branch=preview, capture the emitted *.pages.dev URL into a PowerShell variable, write scratch-qc.mjs (playwright render of map.html from that URL, exit non-zero if the page throws a fatal script error or the js/poi-hover-preview.js script tag is absent from the served markup), node scratch-qc.mjs, Remove-Item scratch-qc.mjs. [command] visual-witness-render

Done means:
  - `git log` in C:\Users\marka\code\mt-integration-2026-06-22 shows a commit on
    TARGET-BRANCH containing the integrated hover-preview-cards feature, with a commit
    message that names "poi-hover-preview" and/or "3b8bb06".
  - js/poi-hover-preview.js exists in the target repo at HEAD with non-trivial content
    (approximately 496 lines implementing the desktop mouseover hover-preview card —
    name/thumbnail/distance/rating read off m._poi — and the mobile "Preview mode" toggle
    persisted via localStorage key mt-hover-preview-v1, confirmed by reading the file, not
    assumed).
  - map.html in the target repo contains exactly one
    `<script src="js/poi-hover-preview.js" defer></script>` tag added to its existing
    script chain, with no other line content lost or altered (confirmed by reading the
    file, not by trusting the cherry-pick's exit code alone).
  - `node --check js/poi-hover-preview.js` exits 0, and `node --check` exits 0 on every
    other .js/.mjs file this integration touched or conflict-resolved.
  - `git status --short` in the target repo shows a clean working tree (no uncommitted
    changes, no unmerged paths) at the end of the mission.
  - Because this diff touches map.html and VISUAL-QC-REQUIRED is set above, the visual-
    witness pipeline must actually run and produce a receipt: a headless-browser
    (puppeteer) render of map.html served from PREVIEW-BASE-URL is captured and returns a
    visual verdict in this mission's result object — a rendering check, not a read of the
    markup. If PREVIEW-BASE-URL turns out to be the wrong local port, that failure must
    surface as a named NO_PREVIEW_BASE_URL-style receipt, not a silently skipped check.
  - This mission's authorship and execution prove only that the integration is
    mechanically sound (declared files present with real content, feature commit reachable
    in target history, syntax checks pass, a render was actually captured) — not that the
    feature's code is correct, safe, or behaviorally right (e.g. that the distance/rating
    math is accurate, that the mt-hover-preview-v1 localStorage toggle round-trips
    correctly, or that layeradd rebinding survives render()-rebuilt markers). That
    judgment belongs to this mission's own witness plus verdict-panel run, downstream of
    this mission producing the integrated result.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

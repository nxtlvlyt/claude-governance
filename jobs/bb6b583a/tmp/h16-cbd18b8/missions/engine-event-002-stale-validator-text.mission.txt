MISSION ENGINE-EVENT-002-STALE-VALIDATOR-TEXT
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\.claude
ALLOW-FILES:
  - canon/foreign-frontier-validators.md

Niyyah: operator-rulings.md's own Meta section names this as an open debt: "STALE-TEXT
CLEANUP OWED (ceremony-class, fresh context): ... foreign-frontier-validators.md still
name the forbidden workers as mandatory (EVENT-002)." Verified directly this session:
canon/foreign-frontier-validators.md's "## Approved validators" section (lines 11-27)
lists mcp__gemini-worker, mcp__gpt-worker, mcp__grok-worker, mcp__glm-worker as approved
foreign-frontier validators, and its "Operational routing preference" section (lines
31-36) actively prefers dispatching them ("GPT, Grok, GLM — preferred for canon/Faith/
hook framing audits"). This directly contradicts operator-rulings.md's standing ban:
"NEVER dispatch mcp gpt/grok/gemini/glm workers or any closed-frontier API outside
Ollama." The contradiction is live, not historical — a session following this canon file
today would be told to do the exact thing the operator forbids.

Maqsad: Rewrite canon/foreign-frontier-validators.md so its "foreign-frontier validator"
concept is satisfied ONLY by the operator's actual compliant channels — mcp__ollama-*
(local-model dispatch, per operator-rulings.md 2026-06-09) and WebFetch (already noted
in the file's own §1 line 7 as satisfying the same structural gate via a separate
substrate path) — and the gemini/gpt/grok/glm "Approved validators" section, and every
routing preference that recommends dispatching them, is removed or explicitly
re-labeled FORBIDDEN with a pointer to operator-rulings.md as the reason. Preserve the
sections that are still valid and unrelated to the forbidden workers: the deliberation-
team description (qwen3.6:35b/nemotron-3-super/granite4.1:30b), the Class 1/Class 2
question-routing distinction, and "What these validators are for / NOT for" — those
apply equally to the corrected Ollama+WebFetch validator set and do not need
re-authoring, only re-pointing away from the forbidden tool names.

Constraints:
- Do NOT touch stop-validation.mjs, pre-tool-use-substrate.mjs, or any hook file in this
  mission — this mission is scoped to the CANON DOCUMENT text only. The hook code's own
  `isFF` pattern matching is a separate, more sensitive follow-on (not this mission's
  job) and must not be touched here.
- Do not delete the file's explanation of WHY these were once approved (organizational-
  independence rationale in the Ruling paragraph, line 3) — that reasoning is still
  correct in the abstract; only the CONCRETE tool list naming forbidden workers as
  currently-approved needs correcting.
- ADDITIVE-STYLE edit discipline: rewrite the specific sections that name the forbidden
  workers as approved/preferred; do not wholesale-replace the file or delete the Class 1/
  Class 2 routing sections, which remain correct.

Done means: canon/foreign-frontier-validators.md no longer lists mcp__gemini-worker,
mcp__gpt-worker, mcp__grok-worker, or mcp__glm-worker under any "Approved" or "preferred"
heading — grep for `mcp__(gemini|gpt|grok|glm).*worker` against the file returns zero
hits outside of an explicit "FORBIDDEN — do not dispatch" context that cites
operator-rulings.md by name. The file still correctly describes mcp__ollama-* and
WebFetch as the real compliant foreign-frontier/mechanical-delegation channels, and the
Class 1/Class 2 deliberation-team routing sections are unchanged in substance.

Context: Sandbox writes only.
Amanah: entrusted; discharge with ihsan.

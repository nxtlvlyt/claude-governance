MISSION QC-CONCERN-POI-PHOTO-CAROUSEL-POI-PHOTOS-JS-2026-06-25
MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.QC-CONCERN-POI-PHOTO-CAROUSEL-POI-PHOTOS-JS-2026-06-25
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: feat/qc-concern-poi-photo-carousel-poi-photos-js-2026-06-25
ALLOW-FILES:
  - js/poi-photos.js
  - js/mobile-sheet.js
REQUIRES: none

Niyyah: Make the POI photo carousel feature spec'd in
docs/FEATURE-CATALOG-2026-06-23.md ("js/poi-photos.js (photo carousel)") actually work —
today only the legacy-URL-rewrite job of the file's own three-job header is implemented;
the carousel injection and the ambassador badge never fire.

Maqsad: In js/poi-photos.js, extend the existing `popupopen` handler (the sole listener
registered in `attach()`, currently lines 63-72, which only calls `rewriteImgUrls(html)`)
so that, after the URL-rewrite pass: (1) when the popup HTML has no `<img>` and the POI's
`properties.photos` array is non-empty, a `.poi-photos` thumbnail-strip carousel is built
and injected (the `.poi-photos` CSS already exists in `injectCSS()`, lines 37-51, but no
element has ever been created with that class); (2) every carousel `<img>` — both
newly-injected ones and legacy already-rendered ones — carries `loading="lazy"`; (3) for
ambassador POIs, `window.mtAmbassadorBadge()` (defined at line 109, exported at line 144,
but never called anywhere in this file) is invoked and its returned chip HTML is appended
alongside the photo content. Then, in js/mobile-sheet.js, widen the `SAFE_TAGS` allowlist
(lines 404-413) so the mobile bottom-sheet's body sanitizer (`sanitizeInto`, lines 425-454)
does not silently strip the new `loading` attribute off carousel images or the `role`/
`title`/`style` attributes off the ambassador badge's `<span>` when it re-renders popup
content on mobile — otherwise a correct poi-photos.js fix still degrades on the mobile
sheet.

RE-AUTHORED after this exact mission file was deleted following 8 plan/re-plan cycles
(mission-events.jsonl in this mission's own log directory, missions/qc-concern-poi-photo-
carousel-poi-photos-js-2026-06-25/mission-events.jsonl: 208 events, first ts
2026-06-30T01:42:43Z, last ts 2026-06-30T04:36:48Z) that never reached the genuine feature
work. This was NOT a content bug — one witness pass even correctly diagnosed the exact
gaps named in Maqsad above, confirming the root cause was right. Every attempt that
touched js/poi-photos.js or js/mobile-sheet.js instead died at the SAME engine-level wall:
57 runtime-verify-fail/runtime-verify-halt events, each with error "load-throw: module
threw at import/load: document is not defined" (or, for mobile-sheet.js, "window is not
defined"), followed by seat-escalate-exhausted at tier 2 (opus) every time. Secondary,
compounding failures in the same log: 16 containment-drift events (unrelated files such as
e2e-shots screenshots and e2e-runner.mjs registering as dirty) and 19 emission-empty
events (edit-mode seats returning no SEARCH-REPLACE blocks) at various escalation tiers.
Of the three architect panels that produced plans, deepseek-v4-pro and glm-5.1 both
independently converged on the Maqsad above and cited docs/FEATURE-CATALOG-2026-06-23.md
as the spec source; nemotron-3-super's plan hallucinated a rewritten poi-photos.js using
`import { getPhotoUrls, isAmbassador } from './poi-utils.js'` and an
`initPoiPhotos(poiLayer, map)` / `poiLayer.on('popupopen', ...)` architecture that matches
no file that exists in this repo — the actual file is a plain browser IIFE driven by
`window.map.on('popupopen', ...)`, with no ES module imports and no poiLayer object.

Context (verified against live substrate this session, not from memory or from the prior
architects' output):
- Reproduced directly against the CURRENT, unmodified files: `node -e
  "require('./js/poi-photos.js')"` throws `ReferenceError: document is not defined` at
  line 86 (`if (document.readyState === 'loading')`); `node -e
  "require('./js/mobile-sheet.js')"` throws the identical class of error at its own
  top-level `document.readyState` check. This is engine-mechanical, not content-dependent:
  orchestrate.mjs (~line 1065) calls `runtimeVerify()` (runtime_verify.mjs) after EVERY
  edit-type step whose target matches `.mjs`/`.js`/`.cjs`, unconditionally and regardless
  of mission text — it dynamic-imports the artifact in a bare Node sandbox with no DOM
  shim (runtime_verify.mjs's `verifyCode()`, lines 67-163). js/poi-photos.js and
  js/mobile-sheet.js are, by design, browser-only additive IIFEs loaded via a `<script>`
  tag in map.html and never as Node modules (same pattern in this codebase's sibling
  js/poi-provenance.js, which throws "window is not defined" the same way). There is no
  mission-text field that suppresses this gate — its only kill switch is the
  MUEZZIN_RUNTIME_VERIFY=off process environment variable, set by whoever fires the
  mission, not by anything written here. Firing this mission again with the gate active
  will very likely reproduce the exact same runtime-verify-halt wall regardless of how
  correct the edit is; this is named here so it is not rediscovered by burning another 8
  cycles. Setting MUEZZIN_RUNTIME_VERIFY=off for this fire, or patching runtime_verify.mjs
  with a minimal DOM shim / an exemption for this additive-IIFE file class, is outside
  this mission's own ALLOW-FILES scope (js/poi-photos.js, js/mobile-sheet.js only) and is
  not something this mission's steps can resolve themselves.
- `properties.photos` is real and already flows to the browser: functions/pois.json.js's
  `toPoiFeature()` (its own file, around the `photos: photos` assignment) sets
  `props.photos` to an array of R2 photo keys (split from `photos_concat`, empty array
  when absent — never undefined), and `props.badge` to the literal string `'ambassador'`
  only when the contributor's account role matches (otherwise the field is omitted
  entirely, never a falsy placeholder). map.html's own POI/community render loops
  (`var p = ft.properties; p._lat = ...; ...; mk._poi = p;` for official POIs, and the
  parallel `m._poi = p;` for community spots inside `renderCommunity()`) attach that same
  properties object directly to the marker as `._poi`. Leaflet's `popupopen` event carries
  the opened popup's owning layer as `e.popup._source` — so `e.popup._source._poi.photos`
  and `e.popup._source._poi.badge` are the confirmed, already-verified extraction path;
  this mission does not need a discovery step to confirm the contract's shape (the
  ambiguity the deepseek-v4-pro architect panel flagged as an open risk is resolved here).
- The photo URL scheme to reuse for injected thumbnails is the SAME one `rewriteImgUrls()`
  already produces for legacy photos (line 33): `/api/contribute/photo/` + `encodeURI(key)`.
- js/mobile-sheet.js's SAFE_TAGS object (lines 404-413) is confirmed present at those exact
  line numbers on TARGET-BRANCH's current tip, not just on main — even though
  TARGET-BRANCH's mobile-sheet.js differs from main elsewhere (unrelated gesture/snap-point
  changes from other concurrent qc-concern missions sharing this repo's worktree: e.g.
  `sheetEl.addEventListener('pointerdown', onPointerDown, { passive: true })` vs main's
  `passive: false`), the SAFE_TAGS block itself and its line numbers are untouched by those
  changes. Line 406 is `'img': ['src','alt','class','width','height']` (no `loading`);
  line 408 is `'span': ['class','data-confidence','aria-label']` (no `role`/`title`/
  `style`). `sanitizeInto()`'s existing protocol-sniffing guard (line 446: strips any
  attribute value starting with `javascript:`/`data:`/`vbscript:`) already applies to every
  allowed attribute, `style` included, so widening the allowlist does not reopen an XSS
  hole.

Steps:
  1. Baseline discovery on TARGET-BRANCH (plain capture, expected to succeed): `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 checkout
     feat/qc-concern-poi-photo-carousel-poi-photos-js-2026-06-25` (create fresh from main
     via `git -C C:\Users\marka\code\mt-integration-2026-06-22 checkout -B
     feat/qc-concern-poi-photo-carousel-poi-photos-js-2026-06-25 main` if it no longer
     exists), then confirm `git -C C:\Users\marka\code\mt-integration-2026-06-22 branch
     --show-current` prints exactly that branch name. Confirm the gap is still real:
     `git -C C:\Users\marka\code\mt-integration-2026-06-22 show
     feat/qc-concern-poi-photo-carousel-poi-photos-js-2026-06-25:js/poi-photos.js |
     Select-String -Pattern 'loading="lazy"'` must return zero matches, and the same show
     piped to `Select-String -Pattern 'mtAmbassadorBadge\('` must return exactly 2 matches
     (the definition and the window export — no call site yet). If either check shows the
     fix already landed, stop and report that instead of re-applying. [command]
     baseline-discovery
  2. Edit js/poi-photos.js: immediately after the existing `var fixed =
     rewriteImgUrls(html);` line inside the `popupopen` handler, insert logic that reads
     `var src = pop._source; var poi = src && src._poi;`; when `fixed` has no `<img>` and
     `poi.photos` is a non-empty array, builds `<div class="poi-photos">` containing one
     `<a href="/api/contribute/photo/<encodeURI(key)>" target="_blank"
     rel="noopener"><img src="/api/contribute/photo/<encodeURI(key)>" loading="lazy"
     alt=""></a>` per photo key and appends it to `fixed`; otherwise, when `fixed` already
     has `<img>` tag(s), adds `loading="lazy"` to every one lacking it via a regex replace
     (mirroring the existing `LEGACY_PHOTO_RX`-style helper already in this file); and,
     when `poi` exists and `window.mtAmbassadorBadge` is a function, appends its returned
     chip HTML (when non-empty) to `fixed` alongside the photo content. Leave the existing
     `if (fixed !== html && typeof pop.setContent === 'function') pop.setContent(fixed);`
     guard as the final line of the handler — it already covers the new mutation paths
     since they all write into `fixed`. Do not touch `rewriteImgUrls()`, `injectCSS()`, or
     the ambassador-badge functions themselves; this step only wires the existing pieces
     together inside the `popupopen` handler. [edit] poi-photos.js
  3. Edit js/mobile-sheet.js: in the `SAFE_TAGS` object, change the `'img'` entry from
     `['src','alt','class','width','height']` to `['src','alt','class','width','height',
     'loading']`, and the `'span'` entry from `['class','data-confidence','aria-label']` to
     `['class','data-confidence','aria-label','role','title','style']`. Change nothing else
     in this file — not the gesture/snap-point logic, not `sanitizeInto()`, not
     `safeImgSrc()`/`safeHref()`. [edit] mobile-sheet.js
  4. Structural verification, read from THIS mission's own named branch specifically (not
     main, not any other branch): `git -C C:\Users\marka\code\mt-integration-2026-06-22
     show feat/qc-concern-poi-photo-carousel-poi-photos-js-2026-06-25:js/poi-photos.js |
     Select-String -Pattern 'loading="lazy"|poi-photos|\.photos\b|mtAmbassadorBadge\('`
     must show at least one match for `loading="lazy"`, at least one match for the
     `poi-photos` class being used to build markup (not just the CSS in `injectCSS()`), at
     least one match reading `poi.photos` (or equivalent property access), and at least 3
     matches for `mtAmbassadorBadge(` (definition, export, and a new call site). Then
     `git -C C:\Users\marka\code\mt-integration-2026-06-22 show
     feat/qc-concern-poi-photo-carousel-poi-photos-js-2026-06-25:js/mobile-sheet.js |
     Select-String -Pattern "'img':\s*\[[^\]]*'loading'|'span':\s*\[[^\]]*'role'"` must
     show a match for both patterns (loading now in the img allowlist, role now in the
     span allowlist — confirm title and style similarly by eye or with parallel patterns
     for `'title'` and `'style'` in the same span-array line). [command]
     verify-carousel-and-allowlist
  5. Parse-check both files (this does NOT execute top-level code, unlike the engine's own
     separate runtime-verify gate named in Context above — it only proves the edit is
     syntactically valid): `node --check js/poi-photos.js` and `node --check
     js/mobile-sheet.js` from the repo root; both must exit 0. [command]
     node-check-both-files
  6. Confirm the branch, commit, and clean status: `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 branch --show-current` still prints
     "feat/qc-concern-poi-photo-carousel-poi-photos-js-2026-06-25"; `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 status --short -- js/poi-photos.js
     js/mobile-sheet.js` shows both files as the only changed paths (staged or committed,
     not untracked/dirty); commit with a message naming "poi-photos", "carousel", and
     "mobile-sheet" so the fix is traceable in history; then `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 status --short -- js/poi-photos.js
     js/mobile-sheet.js` must be empty. [command] commit-and-verify-clean

Done means:
  - Reading js/poi-photos.js on
    feat/qc-concern-poi-photo-carousel-poi-photos-js-2026-06-25 (not trusting a green step
    receipt alone) shows the `popupopen` handler reads `pop._source._poi`, builds or wraps
    a `.poi-photos` carousel with `loading="lazy"` on every `<img>`, and calls
    `window.mtAmbassadorBadge()` for POIs carrying an ambassador badge — Step 4's
    Select-String receipts (loading="lazy" present, poi-photos class used as markup not
    just CSS, .photos property read, mtAmbassadorBadge( at 3+ sites) all pass against that
    branch specifically.
  - Reading js/mobile-sheet.js on the same branch shows the `SAFE_TAGS.img` entry contains
    `'loading'` and the `SAFE_TAGS.span` entry contains `'role'`, `'title'`, and `'style'`
    — Step 4's two-pattern receipt passes, and nothing else in the file (gesture handling,
    `sanitizeInto`, `safeImgSrc`/`safeHref`) changed.
  - `node --check js/poi-photos.js` and `node --check js/mobile-sheet.js` both exit 0.
  - `git status --short -- js/poi-photos.js js/mobile-sheet.js` in the target repo is empty
    (both files committed, nothing else in the shared worktree is this mission's to clean
    up).
  - `git branch --show-current` in the target repo prints exactly
    "feat/qc-concern-poi-photo-carousel-poi-photos-js-2026-06-25".
  - This mission's own Steps and Done-means verify entirely through Select-String/grep
    text receipts and `node --check` (parse-only) — never through `node -e "require(...)"`
    or any dynamic import of these files — so this mission's OWN verification cannot
    repeat the ORIGINAL failure's mistake. The engine's separate, unconditional
    per-step `runtimeVerify()` gate (named in Context above) is a distinct, still-open risk
    this mission's text cannot suppress; if it fires and halts the mission again with
    "document is not defined" / "window is not defined", that is the SAME named engine gap
    recurring, not a new content defect, and should be diagnosed there — not answered by
    another blind re-plan cycle.

Context: Sandbox writes only, target repo working tree only (C:\Users\marka\code\mt-integration-2026-06-22).
Amanah: entrusted; discharge with ihsan.

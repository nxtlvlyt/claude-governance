MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.FRONTEND-DEREGULATIVITY-1
REPO-ROOT: E:\AI_Storage\muddytires-frontend-wt
ALLOW-FILES:
  - js/account.js
  - js/add-spot-worker.js
REQUIRES: none (run SOLO — daemon ignores REQUIRES; conductor fires it when pois-fix is done so the frontend-wt lane is free)

Niyyah: the operator built a separate business's site called regulativity, and does not want muddytires publicly linked to it. The
muddytires frontend currently calls regulativity.workers.dev — which would be visible to anyone inspecting the site. Remove that link
entirely so the site only ever speaks to the operator's own domain. This also clears the last blocker to putting his ~4,000 POIs back on
the live map.

Maqsad: remove every reference to `regulativity.workers.dev` from the muddytires frontend so a production deploy exposes NO link to the
operator's other business. "Done" is: js/account.js and js/add-spot-worker.js call the operator's OWN domain instead of
*.regulativity.workers.dev — replace the AUTH_BASE / CONTRIBUTE_URL constants from 'https://muddytires-accounts.regulativity.workers.dev'
and 'https://muddytires-contribute-d1-test.regulativity.workers.dev' to 'https://api.muddytires.ca' (auth) and
'https://api.muddytires.ca/contribute' (or the equivalent path) — a muddytires.ca custom domain the conductor will stand up separately.
node --check passes; committed. NOT deployed.

Context (facts only):
- VERIFIED: grep finds `regulativity` ONLY in js/account.js (AUTH_BASE, line ~3) and js/add-spot-worker.js (AUTH_BASE + CONTRIBUTE_URL,
  lines ~4-5). map.html and the rest are clean. So changing these two constants removes regulativity from the whole public frontend.
- The new base `https://api.muddytires.ca` is a custom domain the conductor will bind to the auth worker separately (token has
  workers_routes + ssl_certs write). Until that domain is live, the account/posting calls will fail (account is "coming soon") — that is
  ACCEPTABLE: this mission's job is ONLY to remove the regulativity link so the POI map can ship clean. The account backend is a separate
  track (mt-auth-session-schema-fix-1 + the custom-domain setup).
- PRESERVE everything else in these files (the token-auth localStorage + Authorization Bearer logic from commits d7f4c7c/474c4fb stays).
  Only the base-URL constants change.
- COMMIT DISCIPLINE: explicit per-file git add of the two ALLOW-FILES — NEVER `git add .`. commit --no-verify.
- Out of scope: the deploy (conductor); the custom-domain setup (conductor); the auth worker; map.html; pois.json.

Done means (acceptance contract): NO `regulativity` string remains in js/account.js or js/add-spot-worker.js (grep returns 0); the base
URLs point at https://api.muddytires.ca; the token-auth Bearer logic is preserved; node --check passes; committed via explicit per-file
git add, --no-verify; NOT deployed. Receipt confirms `grep -r regulativity js/` is empty and names the conductor's custom-domain setup +
deploy as the next steps.

Constraints: code-repo in frontend-wt; writes ONLY the two ALLOW-FILES; remove ALL regulativity refs; preserve the token-auth logic;
explicit per-file git add (never -A); commit --no-verify; run SOLO (no concurrent frontend-wt mission).

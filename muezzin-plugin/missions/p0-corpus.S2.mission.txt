MISSION-ID: P0-CORPUS.S2
PARENT: P0-CORPUS  (sub-mission 2 of 3, auto-split by the size ceiling)
REQUIRES: predecessor P0-CORPUS.S1 DONE (tartib — sub-mission 2 runs after 1 of 3)

Niyyah: the factory must never solve a problem Mark's past builds already solved — unmined knowledge is indistinguishable from no knowledge. This mission is also the muezzin's first long autonomous run: every receipt it produces is double evidence.

Maqsad: this is part 2 of 3 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): a source census and pattern library of everything Mark has already built and collected, consumable by all future dossier and build missions. Done means: (1) sources.json — every git repo, project folder, AnythingLLM workspace, and Cloudflare deployment discovered (not hand-listed), each with a type tag and a mine/skip verdict; (2) one pattern card per 'mine' source — what it does well, recurring components, design tokens, copy patterns, vertical fit, with evidence references; (3) a knowledge inventory — what the AnythingLLM business-books workspace actually contains (topical map from sample queries), what the old website-pipeline's contracts offer, what the Stitch exports show; (4) a patterns library assembling it all, indexed, every entry citing its source. Time-box: stop cleanly at census+verdicts+cards completion or 48 hours, whichever comes first — partial work committed with a state note beats unfinished ambition.

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - Gather website-pipeline CONTRACTS and Stitch prompts only (per Maqsad — exclude the compiler impl): dump schema/prompt/config files from E:/AI_Storage/website-pipeline into the sandbox for the contracts inventory and stitch inventory. Read-only.
  - Gather source content for the 2boots pattern card: dump key 2boots files (config, templates, CSS, brand md) from E:/2boots into the sandbox so the card author works from substrate, not memory. Read-only.
  - Gather source content for the vanlife pattern card: dump key C:/vanlife files into the sandbox for evidence-cited card authoring. Read-only.
  - Gather source content for the warroom pattern card: dump key C:/warroom files (BRAND-TO-SITE-FAST.md, regulativity worker, site templates) into the sandbox for evidence-cited card authoring. Read-only.
  - Author census/sources.json: merge ALL raw census dumps into one record per source — each with type (local-project|forgejo-repo|github-repo|cloudflare-pages|anythingllm-workspace|live-site), a mine|skip verdict with a one-line reason, an evidence_ref pointing at the dump that revealed it, plus a top-level census_gaps[] array (GitHub private-repo enumeration foremost, plus any failed API call). This is the gate every later step reads.
  - Author the AnythingLLM business-books topical map from the captured sample queries: what the workspace actually contains (topics, verticals it covers well, what it lacks). Single-subject, emitted complete in one step.
  - Author the website-pipeline contracts inventory from the dump: each salvageable contract with its name, input/output schema, and purpose (contracts + Stitch prompts only, per Maqsad). Single-subject, one file.
  - Author the live-sites archaeology inventory from the fetched HTML: tech-stack signals, design tokens, copy/CTA patterns, nav structure per site — and specifically critique laynascookies.ca as 'the agy build' (agency-template vs genuine brand). Single-subject, one file.

Done means: steps P0-CORPUS.S2.1..P0-CORPUS.S2.8 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — (1) sources.json — every git repo, project folder, AnythingLLM workspace, and Cloudflare deployment discovered (not hand-listed), each with a type tag and a mine/skip verdict; (2) one pattern card per 'mine' source — what it does well, recurring components, design tokens, copy patterns, vertical fit, with evidence references; (3) a knowledge inventory — what the AnythingLLM business-books workspace actually contains (topical map from sample queries), what the old website-pipeline's contracts offer, what the Stitch exports show; (4) a patterns library assembling it all, indexed, every entry citing its source. Time-box: stop cleanly at census+verdicts+cards completion or 48 hours, whichever comes first — partial work committed with a state note beats unfinished ambition.

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
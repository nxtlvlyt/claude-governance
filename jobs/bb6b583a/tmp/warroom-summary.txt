VERDICT TALLY: adopt=0 redesign=0 discard=0 other=111

VERDICT LINES (111):
- [warroom/clients/] clients/secrets.py — Secrets/load_secrets/placeholder denylist: undefined — Fail-closed (.require() raises SecretsKeyMissingError with remediation text, never warns-and-proceeds), documented+tested env-precedence, never logs values (custom __repr__), and — critically — a prev
- [warroom/clients/] clients/aimlapi.py — AimlApiClient (.chat/.responses/.call), _sanitize_extras, _redact_response_for_error: undefined — Mechanical containment done correctly: extras are stripped of budget-relevant keys (model/messages/max_tokens/input/instructions/max_output_tokens) BEFORE merge into payload, closing a real smuggling 
- [warroom/clients/] clients/budget.py — BudgetTracker cap enforcement (check_before_call/_check_cap/BudgetExceededError/UnknownModelError): undefined — Headline finding: budget/quota enforcement is advisory, not mechanical — direct contradiction of the receipts-era standard. check_before_call()'s own docstring says 'no longer enforces caps... Caps in
- [warroom/clients/] clients/budget.py — pricing tables (_DEFAULT_PRICING_USD_PER_M / _CHARS_PER_TOKEN_BY_MODEL): undefined — Correctly dated ('as of 2026-04-17, approximate'), meeting the letter of the source+date standard, but there is no staleness mechanism — no expiry check, no warning when the snapshot ages past a thres
- [warroom/clients/] clients/ollama.py — OllamaClient (GR3 enforcement, timeouts, streaming+watchdog, exception hierarchy): undefined — Best-designed component in the subsystem. Golden Rule 3 (never touch GPU) is enforced at a single mechanical choke point — _build_options() always sets num_gpu=0 last, overriding any caller-supplied v
- [warroom/clients/] clients/searxng_client.py — SearXNGClient: undefined — Small, clean, no budget/GPU concern to get wrong. Typed exceptions (SearXNGConnectionError/Timeout/APIError with status_code), ping() deliberately avoids the /search endpoint so health checks don't co
- [warroom/clients/] clients/__init__.py: undefined — Single-line package docstring, no logic to fail. Nothing to bin against the standards.
- [warroom/clients/] clients/anythingllm.py (assignment claimed it, does not exist): undefined — Confirmed absent from clients/ via directory listing and a repo-wide glob for anythingllm*.py (zero matches anywhere in the repo, not just clients/). AnythingLLM integration instead lives in missions/
- [War Room core/ + clients/] core/faith_builder.py + Faith Assembly system (Tier A/B): undefined — Two-tier inheritance (Universal -> overlay -> shared blocks) with SHA-256 provenance and pattern-based contradiction detection is honestly scoped ('subtle semantic contradictions are caught by operato
- [War Room core/ + clients/] core/hierarchy_enforcer.py: undefined — Genuinely wired into the pipeline (not built-not-running): facilitator.py calls it on every Executor output and every Validator REVISE verdict. Credential/PII matches are redacted via SHA-256 fingerpr
- [War Room core/ + clients/] core/router.py (CPU-serialized priority dispatch): undefined — The priority-queue/single-worker-thread serialization itself is sound single-writer-discipline (mutex is implicit via one worker thread, defensive against double-spawn, drains+cancels orphans on shutd
- [War Room core/ + clients/] core/guardrails.py: undefined — Fail-closed on missing config/paths.yaml (raises rather than defaulting open). Directory-traversal (../) explicitly resolved for Windows-drive/UNC/Unix-absolute paths after a documented prior bypass w
- [War Room core/ + clients/] core/logger.py: undefined — SQLite(WAL)+JSONL+markdown structure is sound and genuinely used (session.log_event called from hierarchy_enforcer/facilitator). But it carries the exact DST-lying timezone bug that was diagnosed and 
- [War Room core/ + clients/] core/facilitator.py (deliberation loop manager): undefined — Loop caps are real, not aspirational: MAX_INTRA_ROLE_LOOPS=2 and MAX_CROSS_ROLE_LOOPS=3 are defined constants actually used to bound the executor<->validator and architect->coder->auditor loops. Genui
- [War Room core/ + clients/] core/watchdog.py (RepetitionDetector / HeartbeatMonitor / ResourceWatchdog): undefined — RepetitionDetector (verified in depth) is a correct O(window_size)-per-token sliding-hash loop-detector, fires exactly once per loop, thread-safe. Genuinely wired: router.py's chat_with_watchdog/gener
- [War Room core/ + clients/] core/retry.py: undefined — Matches spec Sec.17.5 exactly: attempt 1 (primary), attempt 2 (same params), attempt 3 (caller-supplied reduced scope, falling back to primary if none given), then FAILED_BUT_CONTINUING; 3 session fai
- [War Room core/ + clients/] core/substrate_reasoning.py: undefined — The always-escalate defense-in-depth (BOUNDARY classification or Hard-Exclusion-level hierarchy result forces escalation even if the caller forgot to flag it) is a genuine mechanical safeguard. But by
- [War Room core/ + clients/] core/eligibility.py: undefined — Static requirements table cross-checked against shared/REQUIREMENTS/*.md by design (verify_requirements_against_markdown), and genuinely wired: facilitator.py's _validate_roster_eligibility fails clos
- [War Room core/ + clients/] core/synthesizer (missions/gauntlet/synthesis.py): undefined — Correctly refuses length-weighted synthesis and explicitly preserves dissent (never collapses to one answer); genuinely wired into cli.py's gauntlet command. But consensus-tier classification clusters
- [War Room core/ + clients/] missions/hygiene/scanner.py (hygiene_scanner) + missions/hygiene/repair.py (surgical_repair): undefined — Both are genuinely wired into cli.py under two separate commands (hygiene, and an archival/repair path), importing HygieneScanner/ContentClassifier and SurgicalRepair/RepairStage/RepairState/RepairHal
- [War Room core/ + clients/] clients/ollama.py (GR3 CPU-only enforcement): undefined — Golden Rule 3 (GPU reserved for ComfyUI) is enforced mechanically at the client, not just detected as text in hierarchy_enforcer: num_gpu=0 is force-injected into every options payload and explicitly 
- [War Room core/ + clients/] clients/budget.py + clients/aimlapi.py (paid-API budget gate): undefined — This is the clearest standards violation found: cap enforcement (session/daily/monthly hard caps, standard #6 'must be mechanical, not advisory') is explicitly disabled. BudgetTracker._check_cap/Budge
- [War Room core/ + clients/] clients/anthropic_client.py, clients/gemini_client.py, clients/anythingllm_client.py: undefined — None of these three files exist anywhere in the repo -- the reader's inventory describes spec text (WARROOM-BUILD-SPEC.md), not shipped code. Actual behavior differs architecturally and (for AnythingL
- [War Room core/ + clients/] cli.py (Typer entry point / wiring layer): undefined — Functions as real wiring, not decoration: targeted greps across the gauntlet, hygiene, faith_builder-adjacent, and facilitator (run) commands all resolve to genuine imports and calls into the core/ an
- [clients/] clients/secrets.py (Secrets, load_secrets, placeholder denylist, env override): undefined — Fail-loud on missing/placeholder credential (standard 2), denylist normalized to unwrapped forms after a receipted prior FATAL bug (wrapped-lowercase your_key_here bypass), env-var override with corre
- [clients/] clients/ollama.py: OllamaClient._build_options (Golden Rule 3 num_gpu=0 enforcement): undefined — Single, named enforcement point that unconditionally overrides caller-supplied num_gpu when enforce_cpu_only=True (the default) -- mechanical, not advisory (standard 6 analog for a hardware-safety inv
- [clients/] clients/ollama.py: streaming + watchdog (chat_stream/generate_stream, chat_with_watchdog/generate_with_watchdog, LoopDetected): undefined — Deed-over-claim friendly design: watchdog receives real content chunks and can abort generation with carried evidence (not a self-report). try/finally guarantees stream.close() on every exit path incl
- [clients/] clients/searxng_client.py (SearXNGClient): undefined — Small, well-scoped client; error mapping (403->actionable remediation naming settings.yml, timeout/connection distinguished from generic API error) and response parsing are exercised with content asse
- [clients/] clients/aimlapi.py: _sanitize_extras / _redact_response_for_error / endpoint dispatch (chat/responses/call): undefined — Anti-smuggling design closes a real hole (caller kwargs could otherwise override model/max_tokens post-estimation) -- receipted as a direct frontier-review finding and fixed, not just claimed. Bearer/
- [clients/] clients/budget.py: BudgetTracker cap enforcement (check_before_call / _check_cap / BudgetExceededError): undefined — This is the one real finding in the subsystem, and it is squarely inside the audit's own standard 6 ('Budget/quota enforcement must be mechanical... not advisory'). The class, exceptions (BudgetExceed
- [clients/] clients/budget.py: pricing tables (_DEFAULT_PRICING_USD_PER_M, _CHARS_PER_TOKEN_BY_MODEL) and JSONL logging: undefined — Manual pricing table is honestly dated/labeled as a snapshot needing manual maintenance (not silently assumed current). Per-model tokenizer-density override for Opus 4.7 is reasoned and tested against
- [clients/] clients/aimlapi.py: _ENDPOINT_BY_MODEL routing table: undefined — Single-entry hardcoded model->endpoint map defaults everything unknown to 'chat'. Reasonable fail-safe default today (most models are chat-shaped), but a manual-maintenance single point of staleness w
- [clients/] clients/anythingllm.py (named in reader's assignment): undefined — Does not exist in clients/. Confirmed by direct glob of the directory (6 files: __init__.py, aimlapi.py, budget.py, ollama.py, searxng_client.py, secrets.py -- no anythingllm.py). AnythingLLM integrat
- [NxTLvL War Room core/ orchestration subsystem] Facilitator (core/facilitator.py) — cross-role deliberation loop + governance scanner dispatch: undefined — The cross-role/intra-role loop, ceiling enforcement, hierarchy-enforcer scan-before-validator, and witness fail-closed gate are well-designed. But `_dispatch_governance_scan` (the categorical PASS/FAI
- [NxTLvL War Room core/ orchestration subsystem] Router / Priority (core/router.py): undefined — CPU-serialization via a single worker thread draining a PriorityQueue is a correct single-writer discipline for the shared-CPU resource; preemption-at-boundaries-only, FIFO-within-priority via monoton
- [NxTLvL War Room core/ orchestration subsystem] Guardrails (core/guardrails.py): undefined — Fail-closed by construction: refuses to even instantiate without config/paths.yaml (no implicit open door), ambiguous/unlisted paths REJECT by default policy, exclusion match is absolute with no appro
- [NxTLvL War Room core/ orchestration subsystem] EligibilityChecker (core/eligibility.py): undefined — Unknown context_length now fails CLOSED to INELIGIBLE_MISSING_CONTEXT rather than the documented prior fail-open default — explicitly cites a direct frontier review that caught the silent fail-open. C
- [NxTLvL War Room core/ orchestration subsystem] FailoverDetector (core/failover.py): undefined — Detection-only, no auto-switch — matches the documented separation-of-detection-from-action policy. Both platform-specific process checks were rewritten per direct frontier review to fix real bugs (PO
- [NxTLvL War Room core/ orchestration subsystem] RetryPolicy (core/retry.py): undefined — Implements exactly the §17.5 policy (primary, retry-same, retry-reduced, then session-abort at threshold) with no invented behavior. Mission-abort latches correctly (`_mission_aborted`) so subsequent 
- [NxTLvL War Room core/ orchestration subsystem] SessionLogger (core/logger.py): undefined — Single SQLite connection per session with WAL mode + an explicit lock matches the single-writer discipline standard. Three-artifact contract (session.db/turns.jsonl/summary.md) is fully implemented, f
- [NxTLvL War Room core/ orchestration subsystem] HierarchyEnforcer (core/hierarchy_enforcer.py): undefined — Content-boundary complement to guardrails' path-boundary. Credential/PII matches are redacted to a SHA-256 fingerprint + length before being stored in Violation.excerpt specifically to prevent violati
- [NxTLvL War Room core/ orchestration subsystem] faith_builder (core/faith_builder.py): undefined — Atomic writes (tmp + os.replace, with cleanup-in-finally and a Windows PermissionError retry), refuses to assemble on an empty EXCLUSIONS.md, and pattern-based inheritance-contradiction detection is e
- [NxTLvL War Room core/ orchestration subsystem] SubstrateReasoner (core/substrate_reasoning.py): undefined — Defense-in-depth on the always-escalate list: even if the caller-supplied ApprovalRequest forgets to set always_escalate_match, a BOUNDARY classification or a hierarchy_check at HARD_EXCLUSIONS-or-abo
- [NxTLvL War Room core/ orchestration subsystem] BudgetTracker (clients/budget.py) — paid-API cap enforcement used by Facilitator: undefined — The class still contains BudgetExceededError, _check_cap(), and a full session/daily/monthly cap model — but check_before_call() no longer calls _check_cap at all, and cost_for() returns 0.0 (rather t
- [NxTLvL War Room core/ orchestration subsystem] Watchdog trio — RepetitionDetector / HeartbeatMonitor (adopt) vs ResourceWatchdog (redesign): undefined — RepetitionDetector and HeartbeatMonitor are cleanly implemented (fire-once semantics, thread-safe, bounded O(window) hashing) and ARE wired into the tryout/audition pipeline (missions/tryouts/candidat
- [War Room strategic roadmap] Phase 0.5 — estimate_inference_timeout() adaptive timeout: undefined — Real, wired implementation, not a doc claim. Function exists at clients/ollama.py:41-70 (margin=max(60, prompt_tokens*0.05), floor 120s), and is actually called: generate()/chat() accept timeout=None 
- [War Room strategic roadmap] warroom status command: undefined — cli.py:2249 def status(...) with docstring 'Check infrastructure readiness: Ollama, SearXNG, paid APIs, RAM budget' matches the Phase 1 spec's stated scope. Genuinely reachable as a Typer command (reg
- [War Room strategic roadmap] clients/searxng_client.py + resources.yaml host: undefined — File exists, config/resources.yaml:20-21 confirms the claimed migration (192.168.2.27:7777 -> http://localhost:8080, dated 2026-05-09) is actually the live config value, matching the plan's claim. One
- [War Room strategic roadmap] clients/base.py -- LLMClient/LLMRouter fallback chain, PaidAPIGateError, WARROOM_MAX_COST_USD cap, circuit breaker: undefined — Confirmed absent from code, not just under-scoped. Glob of clients/*.py shows no base.py (only aimlapi.py, budget.py, ollama.py, searxng_client.py, secrets.py). Repo-wide grep for PaidAPIGateError, WA
- [War Room strategic roadmap] core/governance.py -- HardGate/SoftFormation classes, core/rules.py typed Golden Rules: undefined — Neither file exists (confirmed by Glob of core/*.py -- 18 files, neither governance.py nor rules.py present; grep for 'class HardGate|class SoftFormation|GateViolation' across the whole repo returns o
- [War Room strategic roadmap] F8 -- GR11 RESOLUTION_LEDGER scanner, cited by the reader as core/process_audit.py: undefined — The reader's file:line citation is wrong in a way worth naming explicitly: core/process_audit.py (617 lines, read in full) is a real, well-built module -- but it audits Ollama runner PROCESSES (orphan
- [War Room strategic roadmap] TOML Faith files (faiths/base/*.toml + override.toml, deep-merge router): undefined — Literal spec item is absent -- Glob for faiths/*.toml returns zero files; all 14 faiths are .faith.md. But the underlying goal (layered, provenance-tracked Faith composition: Universal base + project 
- [War Room strategic roadmap] Tool tier enforcement (core/router.py reading Faith TOML tool_permissions): undefined — config/tool_tiers.yaml exists as a config file, but repo-wide grep for 'tool_tiers' shows it is referenced only by itself, README.md, STATE.md, WARROOM-BUILD-SPEC.md, and decision logs -- never by any
- [War Room strategic roadmap] Substrate write-lock (verbatim old_string-on-disk check before write): undefined — Grep of core/guardrails.py for write-lock/verbatim/old_string patterns returns nothing. hierarchy_enforcer.py's own docstring (lines 13-18) describes guardrails.py's actual job as path-boundary IO pro
- [War Room strategic roadmap] Phase 3 warroom plan / warroom act CLI modes: undefined — cli.py (3000+ lines) has no plan/act commands. Full command inventory confirmed by grepping @app.command definitions: init, tidy, tryout, calibrate-judges, calibrate-fixtures, audition-phase-a/status/
- [War Room strategic roadmap] Tryout Framework & Roster (Phase 4) -- reader's item, plan §5 row 9 marks NOT STARTED: undefined — This is the single largest documentation-vs-substrate gap found in this audit, and it runs the OPPOSITE direction of typical overclaiming: the plan UNDERSELLS what exists. Actual substrate shows a sub
- [War Room strategic roadmap] gr1_exceptions.json registry (sanctioned GR1 paid-API bypass mechanism): undefined — Glob for config/gr1_exceptions.json returns nothing. Since the router that would validate against this registry (clients/base.py's LLMRouter) also does not exist, there is currently no code path that 
- [War Room strategic roadmap] STATE.md YAML frontmatter (active_faiths/current_phase/governance_mode/budget_usd): undefined — STATE.md read in full at the top (lines 1-150): begins directly with '# STATE.md -- NxTLvL War Room', no --- frontmatter block, all fields communicated in prose per the older pattern the plan explicit
- [Project history / governance record] STATE.md as a reverse-chronological append log (READ FIRST headers, retraction chain, full-purge-over-flag practice): undefined — Genuinely receipts-grade in the parts that matter most: prior sessions' wrong causal claims are explicitly retracted with substrate evidence (not silently revised), and the 2026-04-21 drift incident r
- [Project history / governance record] RESOLUTION_LEDGER.md as a template (FINDING/SOURCE/SEVERITY/RESOLUTION/EVIDENCE/REASON): undefined — The template itself is sound and receipts-aligned — created 2026-05-04 specifically because GAP 3 found Claude dismissing validator findings 'using Claude's own reasoning — no substrate evidence', sta
- [Project history / governance record] GR11 granite post-cycle 'required stop' scanner and its false-positive-disposition pattern: undefined — The mechanism is fail-closed by design and correctly wired at runtime (verified directly in source: FAIL routes to REJECT + mission_abort_reason). But every time GR11 fired FAIL in the build process, 
- [Project history / governance record] EXEC-07 budget preflight (BudgetTracker.check_before_call): undefined — Implementation matches its own documentation exactly — it does what it says (advisory logging only). But what it says is that it does not enforce: docstring states 'Never raises... _check_cap is no lo
- [Project history / governance record] Witness-seat calibration record (nemotron-3-super PRIMARY / qwen3.6:35b BACKUP): undefined — The one genuinely track-record-keyed seating decision in the whole roster: a purpose-built calibration task with planted assumption-failures, scored PASS/FAIL with token counts and dated result files,
- [Project history / governance record] roster.yaml seat-defaults table (13 seats) vs the 'LIVE' governance-stack diagrams describing the same pipeline: undefined — The roster table itself is honest — it has a 'Calibrated?' column and correctly marks 9 of 11 non-witness seats 'No' ('best-guess... not tryout-scored') all the way through freeze. The problem is cros
- [Project history / governance record] EXEC-01..EXEC-08 execution-spec completion tracking table: undefined — Spot-checked two of its claims directly against source and both held exactly as described: mission_abort_reason='governance_scan_failed' propagation for EXEC-08 (ledger claimed 'FALSE POSITIVE — verif
- [Project history / governance record] Author calibration / judge-reliability saga (0-MAY-02 through 0-MAY-03 retraction chain): undefined — The strongest deed-over-claim example in the corpus: a per-judge abstention bug was found by re-inspecting raw judge responses (not trusting parsed summaries), a zombie-process contamination root caus
- [Project history / governance record] Canon-amendment landing practice (S6-S8): governance-substrate text edited directly in ~/.claude with no version control: undefined — S8 states plainly: 'Canon edits landed (no git — ~/.claude not versioned)'. Whatever the deliberation quality behind the amendment, the landing mechanism produces no diff, no commit, no revertible art
- [Project history / governance record] Drift-incident record and full purge of 2026-04-21 audition data: undefined — Best single fail-closed decision in the record: rather than flag corrupted audit data DO_NOT_USE, the session purged it entirely from git, reasoning that structural removal beats relying on a future i
- [War Room cli.py + core/ + missions/] cli.py Typer app root + interactive REPL (_run_repl): undefined — Real, non-trivial REPL: alias expansion, Windows-vs-POSIX shlex handling for backslash paths, stale-file mtime detection mid-session, plain-English error surfacing. Not a shell stub.
- [War Room cli.py + core/ + missions/] _stub() helper (cli.py:640): undefined — Confirmed dead: repo-wide grep for `_stub` returns only its own def line, zero call sites. The exit-code-2 'fail loud for unbuilt commands' behavior it was meant to centralize is instead achieved by a
- [War Room cli.py + core/ + missions/] core/guardrails.py (Guardrails, GuardVerdict) — IO-boundary path enforcement: undefined — The module itself is well-built and fail-closed by construction: GuardVerdict has no WARN state, unmatched/ambiguous paths default to REJECT, and its own docstring receipts a prior directory-traversal
- [War Room cli.py + core/ + missions/] core/hierarchy_enforcer.py (HierarchyEnforcer) — output-content boundary: undefined — Mechanically enforced, not advisory: facilitator.py hard-rejects and aborts the mission when severity >= REJECT is found in executor output (returns Verdict.REJECT, does not proceed to validator turn)
- [War Room cli.py + core/ + missions/] core/substrate_reasoning.py (SubstrateReasoner, 5-step §10 protocol): undefined — The resolve()/ApprovalRequest engine itself is well designed and was already internally audited (2026-04-28 decision log confirms Step-1-classification-as-caller-input and always-escalate-as-caller-re
- [War Room cli.py + core/ + missions/] core/eligibility.py (EligibilityChecker) + facilitator's _validate_roster_eligibility preflight: undefined — Classification logic is genuinely fail-closed where it matters most: a model whose /api/show doesn't expose context_length is rejected rather than silently passed (explicitly receipted in-code as fixi
- [War Room cli.py + core/ + missions/] missions/hygiene/repair.py (SurgicalRepair, 5-stage protocol): undefined — Content-preservation protections are real and mechanical: byte-for-byte content-hash verification between original and proposed halts on any missing hash (stage_4_propose), size-sanity hard-halt at <5
- [War Room cli.py + core/ + missions/] missions/organization/executor.py (OrganizationExecutor) — move/rename/archive with git revert: undefined — Golden Rule 8 (nothing ever deleted) is structurally enforced, not advisory: OperationKind enum has no DELETE member, so a proposal requesting deletion fails YAML parsing outright. Git-snapshot + auto
- [War Room cli.py + core/ + missions/] config/paths.yaml (guardrails backing config): undefined — Comprehensive, dated, self-documenting exclusion set (secrets, customer-data, PII, NAS sibling projects, git internals) plus an honest note about a live UNC-vs-drive-letter auth gap discovered in prod
- [War Room audition/tryouts pipeline] audition.py — Phase A/B1/B2 orchestrator: undefined — Three-phase separation is sound and the CLI wraps launch with genuine mechanical fail-closed gates (per-model env-var check, pre-launch orphan-generation audit that refuses by default). But every arti
- [War Room audition/tryouts pipeline] judge.py — Judge implementations: undefined — Blind judging (candidate identity never in the judge prompt) and structural recusal are correctly implemented and consistently applied across LocalPanelJudge/PaidPanelJudge/PlanPanelJudge. But the fai
- [War Room audition/tryouts pipeline] candidate.py — Candidate execution: undefined — CompletionStatus modeling is a first-class, mechanically-enforced concept (loop/timeout/backend_error/empty all short-circuit before any judge call). Streaming watchdog loop-kill plus a post-hoc n-gra
- [War Room audition/tryouts pipeline] rubric.py — Rubric schema: undefined — Weight-sum-to-1.0 invariant, per-dimension scale validation, and floor_thresholds/completion_floor are all validated in __post_init__ with structured RubricError (never silent coercion), and floor_thr
- [War Room audition/tryouts pipeline] scorer.py — Aggregation + disqualification: undefined — DQ ordering is correct and mechanical: loop_incident always DQs regardless of rubric config, floors are evaluated before the weighted composite so a floor violation can't be rank-laundered by strong s
- [War Room audition/tryouts pipeline] qualification.py — Seat-tier eligibility gate: undefined — Fail-closed by construction: raises SeatTierMismatchError/ContextWindowUnknownError rather than defaulting an unknown model into a seat. Frontier models are pinned to a static, operator-maintained tab
- [War Room audition/tryouts pipeline] task.py — Task schema: undefined — Structured TaskError on every malformed field (empty id, bad role_seat, empty prompt, non-positive timeout); load_tasks_dir explicitly refuses cross-seat contamination rather than silently accepting a
- [War Room audition/tryouts pipeline] source_assembler.py — Prompt/context assembly: undefined — The idea (a task declares its required source files in frontmatter; the framework loads them since local Ollama candidates have no filesystem access) is reasonable. The implementation has zero contain
- [War Room audition/tryouts pipeline] session.py — TryoutSession: undefined — Does what audition.py should have: atomic writes (tmp + os.replace) for both result.json and summary.md, filesystem-unsafe-character sanitization for log-dir path components (catches '/', '\\', spaces
- [War Room audition/tryouts pipeline] stream_watchdog.py — Live loop detector adapter: undefined — Thin, correct adapter over core.watchdog.RepetitionDetector with intentional sticky-evidence semantics (once fired, keeps returning the same evidence string so the abort cause stays inspectable) — no 
- [War Room audition/tryouts pipeline] inspect_scorer.py — Inspect AI panel scorer: undefined — The best-implemented file in this subsystem: atomic manifest writes, one bounded JSON-repair retry (only on parse-class failures, not network/auth failures), an explicit raise on empty-panel-after-rec
- [War Room audition/tryouts pipeline] score_one_sample.py — Subprocess eval entry point: undefined — Real process isolation (one sample per subprocess) with distinct, meaningful exit codes (0/1/2/3), correctly consumed by phase_b1_inspect_eval's subprocess.run(timeout=...) wrapper — matches the docum
- [War Room audition/tryouts pipeline] spike_inspect_ai.py — Inspect AI integration spike: undefined — Honestly scoped and labeled: its docstring claims it 'does not modify production code paths,' and a direct read confirms it imports and reuses the production build_panel_scorer/inspect_score_to_judge_
- [War Room audition/tryouts pipeline] smoke_test.py — Pre-audition infrastructure smoke test: undefined — The module itself does real, receipted checks (seed-hash distinctness across seeds 0/1/2, cross-model hash-collision detection, eval_count-vs-content correlation) and returns a real exit code — not a 
- [War Room audition/tryouts pipeline] per_candidate_sanity.py / preflight_rerun.py / verify_plan_panel_wiring.py — one-off diagnostic scripts: undefined — Throwaway, honestly-scoped diagnostic tools tied to the 2026-04-22/23 audition-output-corruption incident (hardcoded candidate pools and a hardcoded C:\\warroom path, not parameterized against warroom
- [War Room governance/orchestration core] core/faith_builder.py — Faith Assembly (Tier A/B): undefined — Genuinely built AND running, not just described. Verified an actual assembled output on disk with real provenance header, real SHA-256 hashes, real timestamp. Fails closed on missing Universal/overlay
- [War Room governance/orchestration core] core/hierarchy_enforcer.py: undefined — Real content-boundary scanner wired into the actual deliberation loop, not a standalone module. Credential/PII matches are redacted with a SHA-256 fingerprint token before being stored in Violation.ex
- [War Room governance/orchestration core] core/router.py (CPU-serialized priority dispatch): undefined — Single-worker-thread priority queue is the actual serialization mechanism (not a lock caller has to remember to take). FIFO-within-priority via monotonic sequence counter, timeout-aware stale-item dro
- [War Room governance/orchestration core] config/tool_tiers.yaml "conditional tool tiers + graceful-degradation fallback (primary→silver→bronze)" claimed against core/router.py: undefined — Built-not-running, in the purest form: the YAML policy file exists and is well-specified, but zero Python files anywhere in the repo reference tool_tiers.yaml, its tier keys, or any silver/bronze fall
- [War Room governance/orchestration core] core/guardrails.py (module in isolation): undefined — Fail-closed by construction: refuses to instantiate without config/paths.yaml, treats ambiguous (unmatched-root) paths as excluded rather than allowed, requires an explicit truthy approval object per-
- [War Room governance/orchestration core] Autonomous-Mode write-path enforcement (missions/coding/executor.py vs core/guardrails.py): undefined — The one module that actually performs autonomous file writes/edits does NOT call core/guardrails.py — it reimplements its own weaker boundary check (_load_exclusion_patterns / _target_violates_exclusi
- [War Room governance/orchestration core] core/logger.py (SQLite + JSONL + markdown session logging): undefined — Structurally solid — single connection + threading.Lock, WAL mode, date-hierarchical paths, idempotent finalize(), append-only JSONL flushed per write. But it carries a KNOWN, PREVIOUSLY-FIXED bug: a 
- [War Room governance/orchestration core] core/facilitator.py (deliberation loop manager): undefined — Real cross-role/intra-role orchestration with HierarchyEnforcer, RetryPolicy, and SessionLogger genuinely wired in (verified by grep + read, not just docstring claims). Honestly scopes what is NOT imp
- [War Room governance/orchestration core] core/watchdog.py (RepetitionDetector, NgramLoopFailureDetector, HeartbeatMonitor, ResourceWatchdog): undefined — Sliding-window SHA-256 hash loop detector and n-gram Jaccard post-hoc classifier are both real, tested (tests/test_watchdog.py), and correctly distinguish live-kill vs forensic-verdict use cases per s
- [War Room governance/orchestration core] core/retry.py: undefined — Exactly the documented three-strike policy (same-params retry, reduced-scope retry, mark-failed), session-abort threshold with a persistent _mission_aborted latch that refuses further work once trippe
- [War Room governance/orchestration core] core/substrate_reasoning.py (5-step Substrate Reasoning Protocol): undefined — Confidence-routing thresholds match spec exactly; always-escalate list is defense-in-depth — it re-derives whether a request implies a Hard-Exclusion touch from the caller's own classification/hierarc
- [War Room governance/orchestration core] core/eligibility.py: undefined — Requirements table is held as canonical Python constants with a stated sanity-check routine that diffs against shared/REQUIREMENTS/*.md and warns on divergence — a real dual-source-of-truth discipline
- [War Room governance/orchestration core] Gauntlet synthesis (actual path: missions/gauntlet/synthesis.py — inventoried as core/synthesizer.py): undefined — Genuinely preserves dissent rather than collapsing to one answer — positions/concerns are reported per-model, never merged into a single voice, and a low-lineage-diversity warning fires when participa
- [War Room governance/orchestration core] Hygiene scanner (actual path: missions/hygiene/scanner.py — inventoried as core/hygiene_scanner.py): undefined — Read-only by construction (no write methods observed), tuned thresholds documented with false-positive rationale (e.g. markdown blank-ratio override), and the git-history-dependent session-append dete
- [War Room governance/orchestration core] Surgical repair protocol (actual path: missions/hygiene/repair.py — inventoried as core/surgical_repair.py): undefined — Stages 1-4 are genuinely strong: a hard state-machine gate (each stage raises RuntimeError if the prior stage wasn't completed), content-hash verification that every original CONTENT/COMMENT line surv
- [War Room governance/orchestration core] clients/ollama.py: undefined — Golden Rule 3 (GPU exclusion) is mechanically enforced, not advisory: num_gpu=0 is injected into options and explicitly documented as overriding any caller-supplied value, closing the exact loophole t
- [War Room governance/orchestration core] clients/aimlapi.py + clients/budget.py (inventoried as three separate clients: anthropic_client.py, gemini_client.py, aimlapi_client.py): undefined — Two distinct findings. (1) Inventory-vs-reality: clients/anthropic_client.py and clients/gemini_client.py do not exist; the actual architecture is a single unified gateway client per a receipted 2026-
- [War Room governance/orchestration core] AnythingLLM curation gate (inventoried as clients/anythingllm_client.py with validate_scoped_key()): undefined — As specifically inventoried, this does not exist: no clients/anythingllm_client.py file, and validate_scoped_key() appears nowhere in the codebase (repo-wide grep, zero matches). This isn't an oversig
- [War Room governance/orchestration core] cli.py Typer entry point: undefined — Confirmed real, non-stub subcommands (hygiene, gauntlet, audition-*, calibrate-*, activity) beyond a bare skeleton. One gap: faith_builder.build_all/build_one are never invoked from any cli.py command

BUILT-NOT-RUNNING (28):
[warroom/clients/ (REST client layer: bud] clients/budget.py:261-269 _check_cap() — fully implemented cap-comparison logic that raises BudgetExceededError, but check_before_call() (clients/budget.py:271-287) never calls it; unreachable from any live code path in the repo.
[warroom/clients/ (REST client layer: bud] clients/budget.py:83 UnknownModelError — defined, imported into clients/aimlapi.py:47, and caught via `except UnknownModelError` in two places (aimlapi.py:297-301, :433-439), but cost_for() (budget.py:192-203) was changed to return 0.0 for unknown models instead of raising it. Repo-wide grep for `raise UnknownModelError` / `UnknownModelError(` returns only the class definition — never actually raised, making both except-blocks in aimlapi.py dead code.
[warroom/clients/ (REST client layer: bud] plans/execution-specs/EXEC-07-budget-preflight.md — a pending (Status: pending, all checkboxes unchecked), unimplemented plan that presumes BudgetExceededError propagates from budget_tracker.check_before_call() as a hard mission-abort condition. Even if implemented as written, it would build on a method that — by the current, deliberate design of clients/budget.py — can never raise that exception, so the plan's premise is already false against current budget.py behavior.
[War Room core/ + clients/ (Faith Assembl] config/tool_tiers.yaml (Sec.8.5 conditional tool-tier system) -- zero Python references anywhere in the repository; the router never consults it despite the reader-inventory claim that core/router.py 'resolves conditional tool tiers'
[War Room core/ + clients/ (Faith Assembl] roster.yaml's silver/bronze/break_glass fallback chain for coder.executor (Sec.17.8/Sec.8.6 graceful degradation, including the fully-specified break_glass Opus escalation with 5 named preconditions) -- authored in config, never parsed by Roster.from_config/SeatAssignment, so no code path can ever trigger the swap
[War Room core/ + clients/ (Faith Assembl] clients/budget.py's BudgetExceededError / _check_cap enforcement path -- fully implemented and unit-testable, but check_before_call never calls it; caps are decorative while docstrings in two files still claim they are enforced
[War Room core/ + clients/ (Faith Assembl] clients/anthropic_client.py, clients/gemini_client.py, clients/anythingllm_client.py -- named as shipped modules in the reader's inventory (mirroring the build spec); none exist as files in the repository
[clients/ (War Room REST client layer)] clients/budget.py: BudgetTracker._check_cap and BudgetExceededError are fully implemented and unit-correct (clients/budget.py:261-269, :79-81) but unreachable from any live call path -- check_before_call() at :271-287 explicitly does not call _check_cap, per a documented 2026-04-21 operator directive. Every caller (AimlApiClient.chat/.responses at clients/aimlapi.py:226-231, :345-350) goes through check_before_call and can never trigger a raise no matter how far over cap the call is. This is the textbook 'built != running' pattern (standard 7): the enforcement machinery exists and is tested in isolation, but the wiring that would make it fire in production was deliberately cut.
[NxTLvL War Room core/ orchestration subs] core/facilitator.py:_dispatch_governance_scan — reachable and invoked on every Executor turn via the default roster (Roster.from_config defaults governance_scanner to granite4.1:30b), but crashes on Priority.NORMAL every single call and the crash is swallowed into a silent PASS; tests mock the method itself so the bug has never been exercised.
[NxTLvL War Room core/ orchestration subs] core/watchdog.py:ResourceWatchdog — fully implemented, fully unit-tested, zero production instantiation sites anywhere in cli.py/core/missions.
[NxTLvL War Room core/ orchestration subs] clients/budget.py:BudgetTracker._check_cap / BudgetExceededError — the enforcement code path still exists but check_before_call() no longer calls it, so the three hard caps documented in the module's own header comment ('over-limit raises BudgetExceededError before the network call happens') cannot fire in the current wiring.
[War Room strategic roadmap (WARROOM-SOTA] core/process_audit.py exists and runs, but is misattributed by the reader's inventory as the F8 GR11 RESOLUTION_LEDGER scanner -- it audits Ollama OS processes, not the ledger; the actual F8 scanner has no code anywhere in core/.
[War Room strategic roadmap (WARROOM-SOTA] config/tool_tiers.yaml is a populated config file with no reader anywhere in the codebase -- classic advisory-only artifact.
[War Room strategic roadmap (WARROOM-SOTA] config/gr1_exceptions.json (the registry meant to gate any paid-API GR1 exception) does not exist, though this is currently moot since the router that would consult it (clients/base.py) also does not exist.
[Project history / governance record (STA] Autonomous mode: core/failover.py's FailoverDetector produces a verdict, but the CLI never consults/acts on it automatically at freeze — self-reported as the #1 remaining gap (STATE.md:28-29).
[Project history / governance record (STA] `warroom status`/`warroom health` enhancement: named the explicit 'NEXT TASK' continuously from S6b (STATE.md:282-288, 2026-05-06) through the S9 freeze entry (STATE.md:39, 2026-05-15) — never landed across at least 5 intervening session entries that instead produced canon amendments and book chapters.
[Project history / governance record (STA] Budget cap enforcement: BudgetTracker.check_before_call() is wired into the paid_api dispatch path per the EXEC-07 ledger entry, but the method's own docstring says it 'Never raises' and caps are 'no longer invoked' (clients/budget.py:271-287) — the enforcement surface exists in name and log line only.
[Project history / governance record (STA] GR11 required-stop-on-FAIL: the mechanical stop (REJECT + mission_abort_reason) is correctly wired in core/facilitator.py:589-596, but the session-level half of the procedure — actually re-verifying a FAIL rather than pattern-matching it to a prior 'false positive' — stopped happening after the first occurrence (RESOLUTION_LEDGER.md:328-336).
[War Room cli.py + core/ + missions/ (wri] cli.py:640 _stub() helper -- defined, zero call sites repo-wide; superseded by an inline duplicate at cli.py:2988
[War Room cli.py + core/ + missions/ (wri] core/guardrails.py Guardrails/check_write/check_read -- built and unit-tested in isolation (26/26 per STATE.md:2497) but only ONE production caller exists (missions/discovery/scanner.py, read-only); missions/hygiene/repair.py and missions/organization/executor.py -- the two write-capable modules -- never import or call it, and neither do the cli.py tidy/repair/organization commands that drive them
[War Room cli.py + core/ + missions/ (wri] core/substrate_reasoning.py SubstrateReasoner.resolve()/ApprovalRequest -- built, internally audited, and unit-tested, but the main mission loop (core/facilitator.py) never calls .resolve() or constructs an ApprovalRequest; it only calls build_validator_context() (a prompt-string helper). The only real .resolve() caller is core/probe_engine.py's narrower probe-failure escalation path
[War Room audition/tryouts pipeline (miss] smoke_test.py's own docstring calls it a 'mandatory pre-audition check' with 'exit 1 = DO NOT launch audition' — but cli.py's audition-phase-a command (the only real entry point that launches a batch) never invokes it and never checks for a recent passing run before proceeding; the mandate is prose, not wiring (missions/tryouts/smoke_test.py:1-29 vs cli.py:1511-1710).
[War Room audition/tryouts pipeline (miss] inspect_scorer.py's NaN signal-loss marker (build_panel_scorer, missions/tryouts/inspect_scorer.py:266-283) is produced correctly and the surrounding comment explicitly promises downstream consumers can branch on math.isnan() — but a repo-wide grep of missions/tryouts shows zero call sites of isnan() anywhere; the distinguishing signal is manufactured but never consumed by name (it happens to be neutralized safely in apply_plan_resolution only because that function recomputes medians from raw_per_judge rather than trusting the stored value).
[War Room governance/orchestration core (] config/tool_tiers.yaml — the router's claimed 'conditional tool tier resolution + primary→silver→bronze graceful-degradation fallback' has zero Python consumers anywhere in the repo; the YAML exists, no code reads it.
[War Room governance/orchestration core (] clients/budget.py::_check_cap — mechanical cap-enforcement code is still defined in the file but check_before_call() no longer calls it (per a 2026-04-21 operator directive); the enforcement path is dead code.
[War Room governance/orchestration core (] missions/hygiene/repair.py Stage 5 — git-commit-before/after, post-write re-verification, diff logging, and auto-revert-on-failure are specified in the module's own docstring and in WARROOM-BUILD-SPEC.md §13.2, but stage_5_execute() implements only archive-copy + file-replace.
[War Room governance/orchestration core (] core/watchdog.py::ResourceWatchdog — writes service-health.json on a timer; no `warroom status`/`health` command in cli.py was found reading it back (checked cli.py only, not exhaustive).
[War Room governance/orchestration core (] core/faith_builder.py — build_all/build_one are not reachable from any `warroom` CLI subcommand; only invokable via `python -m core.faith_builder` directly.

TOP RISKS (37):
[warroom/clients/ (REST client layer: bud] Budget enforcement is advisory end-to-end (config file says so explicitly, code says so explicitly, facilitator's preflight check even fail-opens on its own internal exceptions) — the clearest violation of the receipts-era standards in this subsystem, and it stems from a deliberate operator decision (2026-04-21) that now conflicts with a standard learned after warroom froze. Worth surfacing to the operator explicitly as a design-vs-standard conflict rather than quietly re-enabling _check_cap unilaterally, since disabling it was itself a receipted operator call.
[warroom/clients/ (REST client layer: bud] Pricing table is a ~3-month-old manual snapshot with no staleness detection; combined with disabled cap enforcement, actual spend could silently diverge from what the logs claim it costs, and nothing in the subsystem would notice.
[warroom/clients/ (REST client layer: bud] Secrets placeholder detection is a curated denylist (frozenset of known template strings), not a format/entropy check — an unrecognized placeholder convention would sail through as a 'real' credential. Inherent limitation of the denylist approach (acknowledged, not a bug) but worth naming as the one credential-validation gap in an otherwise fail-closed component.
[War Room core/ + clients/ (Faith Assembl] Financial/quota exposure: paid-API budget caps (session/daily/monthly) are silently advisory-only (clients/budget.py) while the surrounding docstrings still claim hard enforcement -- a runaway loop or misconfigured gauntlet run against aimlapi/Opus/Sonnet has nothing mechanical stopping it.
[War Room core/ + clients/ (Faith Assembl] Audit-trail integrity: a DST-lying timezone (hardcoded UTC-4 mislabeled 'America/Toronto') was diagnosed and fixed in exactly one of five files that use it (faith_builder.py) and left broken in logger.py, facilitator.py, substrate_reasoning.py, and missions/discovery/scanner.py -- every session/turn/event/decision-log timestamp these write will be off by one hour for roughly five months a year while confidently labeled correct, undermining any Historian/Auditor reasoning keyed on sequence or attribution.
[War Room core/ + clients/ (Faith Assembl] Graceful-degradation is spec'd, config-authored (including a fully fleshed-out break-glass-to-Opus chain with explicit preconditions), and entirely unwired -- a Coder:Executor SPOF failure will not auto-swap to silver/bronze/break_glass as the spec and roster.yaml both promise; it silently falls back to retry.py's same-model retry-then-abort instead.
[War Room core/ + clients/ (Faith Assembl] substrate_reasoning.py's confidence-routing for approval escalation trusts a calling Validator LLM's self-reported confidence/resolved flags outside the hard-exclusion short-circuit, with no independent re-derivation of citations -- an over-confident Validator can route what should be a human escalation straight to RESOLVED/PARTIAL with nothing downstream to catch it.
[clients/ (War Room REST client layer)] Budget caps in config/meta_eval.yaml are advisory-only in the actual call path (clients/budget.py:271-287) despite BudgetTracker's module-level docstring still opening with 'Three hard caps... over-limit raises BudgetExceededError before the network call happens' (clients/budget.py:4-5) -- the module docstring and the method docstring directly contradict each other, so a future reader trusting the top-of-file summary would believe caps are enforced when they are not.
[clients/ (War Room REST client layer)] cost_for() returning 0.0 for any unknown/renamed model (clients/budget.py:192-200) means a model rename or a new aimlapi SKU silently logs $0 cost with no error and no cap check -- combined with the advisory-cap finding, this is two independent paths (unknown-model AND over-cap) that both fail open on spend, in a subsystem literally named BudgetTracker.
[clients/ (War Room REST client layer)] _ENDPOINT_BY_MODEL has exactly one entry (clients/aimlapi.py:98-100); any future Responses-API-only model added by aimlapi without a corresponding table update will silently route to /v1/chat/completions and fail in a way not obviously connected to the routing table.
[clients/ (War Room REST client layer)] OllamaClient's single-writer/concurrency safety is entirely delegated to core/router.py's CPU-serialization discipline (clients/ollama.py:11-12, 'does NOT itself lock -- the mutex lives in core/router.py') -- this subsystem's own tests cannot prove that discipline holds; it is an assumed-correct dependency outside clients/ scope that this audit did not verify.
[NxTLvL War Room core/ orchestration subs] The governance-compliance gate (HierarchyEnforcer -> governance_scanner -> Validator per the intended defense-in-depth chain) is completely non-functional in the shipped code: every dispatch throws AttributeError on Priority.NORMAL and is silently converted to PASS, meaning zero Executor outputs have ever actually been governance-scanned by this mechanism despite the roster defaulting it on. Anyone reading facilitator.py's log output would see '[GOV_SCAN]' warnings that look like the documented best-effort-skip path, not a crash.
[NxTLvL War Room core/ orchestration subs] Paid-API budget enforcement is advisory-only by explicit operator directive, with actual enforcement delegated to aimlapi's external dashboard/gateway — a control this codebase cannot verify and does not receipt. If that external enforcement is misconfigured or absent, there is no mechanical backstop inside War Room's own code despite BudgetTracker's caps/BudgetExceededError machinery still being present and looking load-bearing.
[NxTLvL War Room core/ orchestration subs] Eligibility and hierarchy checks are fail-closed only at the two hardest-gated points (witness seat classification, Hard-Exclusion/credential scanning) — the five other seats (architect/coder/auditor/integrator/historian) get warn-only soft-gating on INELIGIBLE classifications, so a misconfigured roster.yaml can seat a model the eligibility matrix itself says shouldn't hold that seat, and the mission proceeds anyway.
[War Room strategic roadmap (WARROOM-SOTA] Plan-substrate desync runs in BOTH directions and both are dangerous: WARROOM-SOTA-PLAN.md §5 undersells Phase 4 (tryout/audition framework is substantially built and tested, but the plan and STATE.md's most recent entry don't reflect it), while the reader's own inventory oversells F8 (citing core/process_audit.py as the GR11 ledger scanner when that module does nothing of the sort). An auditor or future instance trusting either document at face value will misjudge both which work remains and which work is already done.
[War Room strategic roadmap (WARROOM-SOTA] The governance core of the whole roadmap -- HardGate/SoftFormation, the paid-API budget cap, and the circuit breaker -- is 100% prose across every substrate file searched (reviews/*.md, WARROOM-SOTA-PLAN.md, RESOLUTION_LEDGER.md) and 0% code. Per the plan's own nemotron finding (§10, 2026-05-06): 'the War Room is compliance theater by design until Phase 2 ships code enforcement' -- that finding is still true as of this audit, roughly two months after it was written, and nothing in the intervening tryout-framework build touched it.
[War Room strategic roadmap (WARROOM-SOTA] STATE.md, the designated single source of current status (Directive 8), has not been updated since 2026-05-15 despite WARROOM-SOTA-PLAN.md being updated 2026-05-27 and a large volume of tryout/audition code+tests existing that postdates the STATE.md entry. A successor instance bootstrapping from STATE.md alone -- exactly the failure mode Directive 14 names -- would not learn that Phase 4 work exists at all.
[Project history / governance record (STA] Validator-friction bypass recurs under a new disguise: GR11 FAILs and nemotron 'risky/theoretical' findings are adjudicated by the same session under review, with no independent second pass — the precise failure GAP 3 was created to close (RESOLUTION_LEDGER.md:44-49 vs :185-198, :266-277/:328-336).
[Project history / governance record (STA] Cross-document inconsistency inflates perceived governance maturity: 'LIVE' pipeline diagrams (repeated 7x) coexist with a roster table showing 9 of 11 non-witness seats as uncalibrated best-guess assignments through freeze (STATE.md:636-655 vs :1137-1163) — a reader trusting only the diagrams overestimates how governed the system actually is.
[Project history / governance record (STA] Cost/budget control has no mechanical backstop by design (clients/budget.py:277-287) inside a document that otherwise holds every other gate to a fail-closed standard — the one place money is at stake is the one place enforcement was explicitly removed.
[Project history / governance record (STA] Governance-substrate edits (canon amendments) are landed via unversioned direct file edits ('no git — ~/.claude not versioned', STATE.md:64) — no diff/commit receipt exists for changes to the rules the rest of the system is judged against, only self-report.
[Project history / governance record (STA] Attention drift toward recursive governance-about-governance work (a canon amendment about how canon amendments should sequence validators; book chapters about the deliberation team's own philosophy) displaced a scoped, already-specified product task (`warroom status`) for the project's final ~9 days without the log ever flagging the pattern itself, only faithfully recording each instance of deferral.
[War Room cli.py + core/ + missions/ (wri] Guardrails path-exclusion (secrets, PII, customer-data, NAS zoho/CGSports patterns in config/paths.yaml) is not enforced on any write path -- warroom tidy/repair/organization can move, overwrite, or archive files under an excluded pattern with zero check, despite the module's own docstring stating it is the mandatory first line of defense for every file operation. This is the most severe gap: fail-open by omission on exactly the class of harm (secrets/customer-data leakage) the standards flag hardest.
[War Room cli.py + core/ + missions/ (wri] The Substrate Reasoning Protocol's confidence-routing escalation engine (resolve()/ApprovalRequest) is unwired from the main Architect/Coder/Auditor/Historian/Integrator mission loop -- facilitator.py only uses SubstrateReasoner for context-string injection, so the section-10-documented behavior ('novel decisions escalate to human, routine ones proceed logged') does not actually run for ordinary missions.
[War Room cli.py + core/ + missions/ (wri] Eligibility gating is mechanical (hard abort) for only 1 of 10 role-seats (witness); the other 9 get warning-only 'soft gate (tryouts pending)' treatment, so an INELIGIBLE_MISSING_CONTEXT or NOT_INSTALLED model can still be dispatched into a live production mission for architect/coder/auditor/integrator/historian seats.
[War Room cli.py + core/ + missions/ (wri] repair.py's stage-3 'human review gate' is always satisfied by construction at its one real call site (cli.py hardcodes approval_marker=True) -- the docstring's framing overstates what's enforced; real protection comes from --live + dry-run-default + drift-recheck, not genuine review.
[War Room audition/tryouts pipeline (miss] Containment gap in source_assembler.py: task-file frontmatter can declare an arbitrary absolute path anywhere on the filesystem with zero allowlist/denylist/traversal check, and for validator seats that content (draft_under_review) is transmitted to a paid third-party API (aimlapi) during Phase B1/B2 judging — a mistaken or compromised task file can exfiltrate local secrets off-machine with no gate in between (source_assembler.py:41-56, 124-133; egress at audition.py:758-762, score_one_sample.py:82-87).
[War Room audition/tryouts pipeline (miss] Fail-open scoring on total judge/panel failure: PaidJudge/PaidPanelJudge in judge.py convert an aimlapi dry-run into a fabricated normalized=0.0 rather than refusing, and every panel implementation (Local/Paid/apply_plan_resolution) defaults an all-judges-failed dimension to a real 0.0 score indistinguishable from a genuine zero verdict. Partially mitigated by floor_thresholds present on every real rubric on disk, but not universally, and not distinguishably in the DQ-reason text either (judge.py:378-381, 467-482, 631, 841).
[War Room audition/tryouts pipeline (miss] audition.py — the module most exposed to multi-day-batch crashes — never adopted the atomic-write fix (tmp+os.replace) that session.py and inspect_scorer.py both explicitly implemented for the same risk, and phase_b1_score_outputs has no per-item exception guard, so one crash-truncated result.json can abort Phase B1 scoring for an entire batch (audition.py:190-199, 719-822).
[War Room audition/tryouts pipeline (miss] No machine-level lock enforces the GR10 'only one audition batch at a time' invariant; the pre-launch orphan-generation audit in cli.py is a real, fail-closed mechanical check but is a detect-then-refuse race window, not a mutex, so two near-simultaneous launches could each observe a clean state and then compete for CPU (audition.py:8-11; cli.py:1594-1631).
[War Room audition/tryouts pipeline (miss] Two functionally-inconsistent Phase B1 paths remain live in the CLI simultaneously: audition-phase-b1 (older PaidPanelJudge/PlanPanelJudge, fail-open on dry-run/all-judges-failed) and audition-phase-b1-inspect (inspect_scorer.py, correct NaN signal-loss handling). An operator invoking the legacy command by habit silently gets the weaker guarantee with no warning that a stronger replacement exists (cli.py:1901-1983 vs 1985-2055).
[War Room governance/orchestration core (] Approval-gate bypass on real writes: missions/coding/executor.py (the only module that actually performs Autonomous-Mode file writes) does not call core/guardrails.py and reimplements a weaker check with no allowed-roots requirement and NO approval-marker gate at all — core/facilitator.py's own docstring admits Guardrails is 'not directly invoked here.' Two inconsistent resolution doors exist; the one governing real writes is the one missing the approval gate §5 requires.
[War Room governance/orchestration core (] Paid-API budget caps are advisory, not mechanical, by a self-documented 2026-04-21 directive (clients/budget.py check_before_call 'Never raises'); the mechanical _check_cap code still exists but is orphaned, and unpriced models silently cost $0.0 instead of refusing — this sits directly behind Golden Rule 1's paid-API gate.
[War Room governance/orchestration core (] Surgical-repair Stage 5 is hollow against both its own docstring and WARROOM-BUILD-SPEC.md §13.2: no git commits, no post-write re-verification, no diff log, and no auto-revert-on-failure exist anywhere in the code, despite being the headline safety claim ('triple backup + verify + commit') for the one protocol allowed to touch substrate files.
[War Room governance/orchestration core (] A DST timezone bug was diagnosed and fixed once (faith_builder.py, receipted 2026-04-28) but knowingly reproduced rather than propagated into core/logger.py (session/turn/event timestamps), core/substrate_reasoning.py (approval-log timestamps), core/facilitator.py, and missions/discovery/scanner.py — corrupting the audit trail's own dating during Nov-Mar in a system whose entire premise is dated, receipted evidence.
[War Room governance/orchestration core (] Three inventoried modules (clients/anthropic_client.py, clients/gemini_client.py, clients/anythingllm_client.py + its validate_scoped_key() soft-gate) do not exist and haven't for months — both are superseded by receipted architecture decisions the inventory missed, meaning downstream planning against that inventory would target dead paths.
[War Room governance/orchestration core (] config/tool_tiers.yaml's entire tier-resolution and graceful-degradation-fallback policy has zero code consumers — a fully-specified safety/UX mechanism that was never wired to anything.

BLOCKERS (6):
- No dependency manifest exists for warroom (no requirements.txt, no pyproject.toml, no committed venv) and none is present on this machine — `python -m pytest` fails immediately with 'No module named pytest'. STATE.md's own '606 tests passing' claim is from 2026-05-15 (8 weeks stale, last commit since is an unrelated rename on 2026-06-23) and cannot be verified until an environment is restored. Every 'adopt' verdict in the audit is only as trustworthy as this.
- No Ollama Cloud client exists anywhere in the repo (grep of clients/ollama.py for cloud/ollama.com/OLLAMA_API_KEY: 0 matches) — this is a from-scratch build, not a config flag, despite being 1 of the 3 named channels.
- No mission substrate exists in warroom at all (no AUTORUN.md/QUEUE.md/INBOX.md/STATUS-BOARD.md/*.mission.txt anywhere in the tree) — 'continue muezzin missions AND run its own' is a from-scratch build, not a port of existing files.
- missions/coding/executor.py — the ONLY module that performs real autonomous file writes — has zero approval-gate check (verified directly: act()/_execute_step() write any WRITE_FILE/EDIT_FILE step inside work_dir with no approval marker, no call to core/guardrails.py). Every later stage that exercises real writes (repair.py fixes, mission substrate, seat tryouts that touch files) is exercising an unguarded path until this is fixed first.
- clients/budget.py's cap enforcement is mechanically disabled by a prior operator directive (2026-04-21, self-documented 'Never raises'); building new paid-adjacent channels (Ollama Cloud, heavier aimlapi use) on top of it means real spend has no code-level backstop until this is explicitly resolved.
- The 3-tier resilience cascade (cloud APIs -> nxtbeast 4090 -> laptop-local 4070) is new logic, not a rewire: core/failover.py's FailoverDetector only has 5 Claude-Code-absence triggers today, nothing host/tier-aware. nxtbeast reachability from the van/laptop was not re-verified in this session against warroom's own code.

OPERATOR DECISIONS NEEDED (4):
- Ollama Cloud jurisdiction conflict: the standing ruling in ~/.claude/rules/operator-rulings.md (2026-07-02, 'we are not supposed to be using any ollama cloud models') reads as a blanket ban with no jurisdiction qualifier, while this task's own parity vision names Ollama Cloud as 1 of warroom's 3 channels. Per the jurisdiction pattern (CONDUCTOR-PORT-PLAYBOOK.md #4: rulings do not cross jurisdictions silently; agy has its own separate rulebook that explicitly permits cloud), warroom has no rulebook of its own yet. Needs an explicit ruling before Stage 6 is built: is warroom its own jurisdiction with cloud permitted (mirroring agy), or does the blanket ban bind it too?
- clients/budget.py cap enforcement: re-enable mechanical _check_cap (fail-closed on spend + unpriced models) for warroom, or explicitly re-ratify 'advisory-only' going forward. This was originally an operator directive (2026-04-21), not an engineering default, so reversing or reaffirming it is the operator's call, not something a session should silently flip while implementing Stage 3.
- Mission-continuity architecture shape (Stage 9): should warroom become a subprocess seat that the existing Node muezzin engine dispatches into via a `--json` relay entrypoint (reuses the proven N5 rails-in-the-script primitive from the qwen 5/5 relay audition — smaller build), or get its own independent Python daemon/mission-runner loop analogous to muezzin_engine.mjs (full jurisdiction independence, 2-3x larger build)? This sizes several evenings of work either way and should be picked before it's spent.
- Seat model assignments: roster.yaml's seat SHAPE (primary/backup/silver/bronze/break_glass, swappable without rebuild) is already built and matches muezzin's tiering pattern — but which actual models get seated is a product-taste/budget call under the operator's own standing rule ('product-taste calls' are operator-bound per operator-rulings.md). Flagging so Stage 8 tryout results get reviewed and approved by the operator before roster.yaml's status flips from best-guess to calibrated, rather than auto-promoted by whichever session runs the tryout.

MUEZZIN SHOULD BORROW:
[
  {
    "what": "Faith Assembly with real provenance headers (SHA-256 hashes + timestamp) and fail-closed build on missing Universal/overlay files, empty EXCLUSIONS.md, or detected contradictions — no write occurs on failure",
    "from": "core/faith_builder.py:379-441; faiths/coder_executor.faith.md:1-5",
    "bin": "adopt",
    "why": "Muezzin already builds native faiths per the operator's parity vision but has no described provenance-header/fail-closed-assembly mechanism. Warroom's version is verified genuinely built and running (real hashes on disk, not just described), and its contradiction detector is honestly scoped rather than overclaimed."
  },
  {
    "what": "Hierarchy enforcer that redacts credential/PII matches to a SHA-256 fingerprint token BEFORE storing in the violation excerpt, closing the secondary-leak path via logs/telemetry",
    "from": "core/facilitator.py:561-580,678; core/hierarchy_enforcer.py:421-444",
    "bin": "adopt",
    "why": "Wired into every Executor and Validator output, not standalone. Muezzin's guardian/integrity guards should adopt redact-before-store as a default, not redact-on-display, to avoid the same secondary-leak class in mission logs."
  },
  {
    "what": "Single-worker-thread priority-dispatch queue with FIFO-within-priority via monotonic sequence counter, defensive double-worker refusal in start(), and two-phase drain-with-cancel on shutdown",
    "from": "core/router.py:130-152,154-200,379-414",
    "bin": "adopt",
    "why": "Serialization is structural (one worker thread), not a lock callers must remember to take — and the two-phase shutdown drain specifically closes a dispatch/shutdown race window. Directly applicable to hardening muezzin's single-Hermes-lock dispatcher against the same race class."
  },
  {
    "what": "Guardrails module's fail-closed-by-construction shape: refuses to instantiate without config, treats ambiguous/unmatched-root paths as EXCLUDED (not allowed), and requires an explicit truthy per-call approval object with no env-var/global-toggle backdoor",
    "from": "core/guardrails.py:98-116,352-365,373-381,149-152",
    "bin": "adopt",
    "why": "This is the 'one resolution door' shape muezzin's own standards ask for, verified as a module. Muezzin's containment kernel should hold to the same three properties: fail-closed instantiation, ambiguous=excluded default, mandatory truthy approval object (no bypass flag)."
  },
  {
    "what": "The APPROVAL-GATE REQUIREMENT ITSELF, built correctly this time — port the spec, not warroom's code",
    "from": "missions/coding/executor.py:236-256,292-294 (the broken version); core/facilitator.py:8-10 ('not directly invoked here')",
    "bin": "redesign",
    "why": "Warroom's own real write path bypasses its own Guardrails module entirely and reimplements a weaker check with zero approval gate — its #1 top risk. The lesson for muezzin is negative-example-as-design-input: verify muezzin's containment kernel is the ONLY resolution door for real writes, with no parallel weaker path a mission executor can fall back to."
  },
  {
    "what": "Structured session logging architecture: single connection + threading.Lock, WAL mode, date-hierarchical paths, idempotent finalize(), append-only JSONL flushed per write — WITHOUT the hardcoded timezone(timedelta(hours=-4)) bug",
    "from": "core/logger.py:36-37 (bug); core/faith_builder.py:90-106 (the correct receipted fix, ZoneInfo with UTC fallback)",
    "bin": "redesign",
    "why": "The structural design is solid and worth porting for muezzin's own session/mission logging, but only with the ZoneInfo fix warroom itself already wrote once (faith_builder.py) and then failed to propagate to logger.py, substrate_reasoning.py, facilitator.py, and discovery/scanner.py. Adopt the architecture, adopt the fix, and add the propagation check warroom skipped."
  },
  {
    "what": "Dual repetition/loop detection: sliding-window SHA-256 hash detector for live-kill use, plus a separate n-gram Jaccard classifier for post-hoc forensic verdicts — correctly kept as two distinct mechanisms for two distinct purposes",
    "from": "core/watchdog.py:479-497; missions/tryouts/stream_watchdog.py, candidate.py; tests/test_watchdog.py",
    "bin": "adopt",
    "why": "Tested and wired into warroom's audition pipeline. Muezzin's mission-execution watchdogging (detecting a stuck/looping Hermes or model call) would benefit from the same live-kill-vs-forensic-classifier split rather than one detector trying to serve both jobs."
  },
  {
    "what": "Three-strike retry policy (same-params retry, reduced-scope retry, mark-failed) with a persistent _mission_aborted latch that refuses further work once tripped, and swallowed event-emission failures so a broken callback can't derail retry logic",
    "from": "core/retry.py:109-191",
    "bin": "adopt",
    "why": "This mechanizes almost exactly muezzin's 'FAILED x2 = diagnose-and-fix or block-with-receipt' rule — the reduced-scope middle step and the persistent abort latch (vs. re-trying indefinitely) are concrete mechanisms muezzin's retry/requeue code could adopt directly."
  },
  {
    "what": "Re-derive-don't-trust-caller-flag pattern: the always-escalate list re-derives whether a request implies a Hard-Exclusion touch from the caller's own classification/hierarchy_check rather than trusting a caller-set flag; falls through to UNRESOLVED/escalate rather than silently proceeding when no step resolves",
    "from": "core/substrate_reasoning.py:254-286,366-382",
    "bin": "adopt",
    "why": "A receipted direct-frontier-review fix against a caller-set-flag bypass. Anywhere muezzin lets a caller (mission, sub-agent) set a classification flag that gates dangerous behavior, this re-derive-independently pattern closes the trust gap."
  },
  {
    "what": "Dual-source drift sanity-check: canonical requirements held as Python constants PLUS a routine that diffs against shared/REQUIREMENTS/*.md and warns on divergence",
    "from": "core/eligibility.py:23-26,86-90",
    "bin": "adopt",
    "why": "Muezzin's roster.yaml / SEAT-PLAN docs and code constants risk exactly this drift (the warroom audit independently found 5 cases of doc-vs-code drift elsewhere in the same repo). An automated diff-and-warn check is cheap insurance muezzin doesn't appear to have."
  },
  {
    "what": "Low-lineage-diversity warning that fires when gauntlet/panel participant models share training-family lineage, as a safeguard against consensus theater from correlated models, alongside dissent preserved per-model rather than collapsed to one voice",
    "from": "missions/gauntlet/synthesis.py:96-111,13-17",
    "bin": "adopt",
    "why": "Directly relevant to muezzin's verdict panels and witness pairs (ornith:9b + granite guardian) — a mechanical lineage-diversity check would catch the case where two 'independent' witnesses are actually correlated by training family, which prose review would miss."
  },
  {
    "what": "Read-only-by-construction guarantee for scanner-class modules (no write methods observed at all, not just convention) plus honest labeling of stubbed detectors ('scaffolding...stubbed until Step 33') instead of silently claiming coverage",
    "from": "missions/hygiene/scanner.py:17-19,47-54",
    "bin": "adopt",
    "why": "A structural guarantee is stronger than a code-review convention. Muezzin's hygiene/discovery scanners should be architecturally incapable of writing, not just conventionally discouraged from it."
  },
  {
    "what": "Staged-repair state machine: each stage raises RuntimeError if the prior stage wasn't completed, content-hash verification that every original CONTENT/COMMENT line survives into the proposed output (hard halt if not), size-sanity halt at <5% of original, dry-run-by-default — Stages 1-4 only",
    "from": "missions/hygiene/repair.py (stages 1-4, strong) vs :225-259 (stage_5_execute, hollow); WARROOM-BUILD-SPEC.md:1134-1142",
    "bin": "redesign",
    "why": "The gating/verification design for stages 1-4 is genuinely strong and worth porting for any mechanical autosplit/repair work muezzin does. Explicitly do NOT port Stage 5 as-is — warroom's own spec calls for git-commit-before/after, post-write re-verification, diff logging, and auto-revert-on-failure, and only 2 of those 6 sub-steps exist in code. Muezzin should build the complete version warroom's own spec describes but never finished."
  },
  {
    "what": "Mechanical, non-overridable enforcement of a safety-critical parameter: num_gpu=0 injected into options and explicitly documented as overriding any caller-supplied value",
    "from": "clients/ollama.py:975; :41-71 (receipted dynamic-timeout sizing against an observed qwen3.6:35b CPU-timeout failure)",
    "bin": "adopt",
    "why": "This is precisely the mechanism muezzin's NO-OLLAMA-CLOUD standing ruling needs: a client-layer override that mechanically strips/forces local-only behavior regardless of what a caller or config passes in, rather than relying on prose compliance with the ruling."
  },
  {
    "what": "Unified single gateway client across paid-API vendors (superseding a per-vendor-client plan) plus extras-sanitization that strips budget-relevant keys callers might smuggle past validation — WITHOUT the advisory-only cap enforcement",
    "from": "clients/aimlapi.py:1-6; clients/budget.py:271-287 (check_before_call 'Never raises' — do not port this part)",
    "bin": "redesign",
    "why": "The consolidation architecture and key-smuggling defense are good, receipted design choices worth porting for muezzin's own multi-vendor paid-API gateway. The budget-cap enforcement sitting behind it is the opposite lesson (see warroom_needs_from_muezzin) — port the gateway shape, not the advisory cap behavior."
  },
  {
    "what": "Full-purge-over-DO_NOT_USE-flag practice for corrupted governance/audit data: when the 2026-04-21 drift incident corrupted audit data, the session purged it structurally from git rather than flagging it, reasoning 'structural removal is stronger than future-instance vigilance'",
    "from": "STATE.md:2257-2266",
    "bin": "adopt",
    "why": "Independently anticipates the fail-closed standard. Muezzin's retro/STATE discipline (Directive 6: 'if a file appears corrupted, flag it, do not build on it') should go one step further per this receipt: purge structurally, don't rely on a flag surviving into a future session's vigilance."
  },
  {
    "what": "Structured triage-ledger template (FINDING/SOURCE/SEVERITY/RESOLUTION/EVIDENCE/REASON) — WITH mandatory independent second-dispatch confirmation before any finding is marked ACKNOWLEDGED or FALSE POSITIVE",
    "from": "RESOLUTION_LEDGER.md:44-49 (stated principle: 'the correct response is always to stop and recheck — not to evaluate whether the validator is right') vs :185-198,226-241,302-317 (violated by the same document's own later entries, self-triaged with no second check)",
    "bin": "redesign",
    "why": "The schema itself is sound and worth adopting for muezzin's retro files/MISSION-LEDGER.md. But port it WITH the fix warroom's own ledger failed to apply to itself: no finding gets dismissed by the same session that produced it — this is exactly muezzin's sixth law ('did I look again?' not 'were the small models right?') and warroom's own record is a receipted case of the law being violated in the wild."
  },
  {
    "what": "Planted-assumption-failure calibration methodology: a purpose-built task with deliberately planted wrong premises, scored PASS/FAIL with token counts and dated result files, competitors marked INELIGIBLE on failed arithmetic",
    "from": "STATE.md:1233-1255 (nemotron-3-super PRIMARY / qwen3.6:35b BACKUP seating decision)",
    "bin": "adopt",
    "why": "The single most track-record-keyed seating decision in warroom's whole roster. Matches and could sharpen muezzin's own audition-tooling ambitions (per the operator's parity vision) — the 'plant a wrong assumption and see if the model catches it' task design is a concrete, reusable calibration-task pattern, not just a scoring rubric."
  },
  {
    "what": "Explicit 'Calibrated?' honesty column in the seat-defaults table, kept distinct from pipeline-status language elsewhere",
    "from": "STATE.md:1137-1163 (9 of 11 non-witness seats correctly marked 'No') vs :636-655,708-724,760-776 (diagrams elsewhere claiming every stage 'LIVE')",
    "bin": "redesign",
    "why": "The column itself is honest and worth adopting for muezzin's roster docs. But port it as a GATE, not just a documented column — warroom's own diagrams ignore the column three separate times in the same file, showing an honest column with no enforcement is not enough; muezzin's graduation-ladder/streak gating (senior-ladder.jsonl) is the mechanical version this needs to be paired with."
  }
]

WARROOM NEEDS FROM MUEZZIN:
[
  {
    "what": "A containment kernel / guardian-guard layer that is the ONLY resolution door for real autonomous writes, with no parallel weaker path a mission executor can fall back to",
    "why": "Warroom's top risk: missions/coding/executor.py (the module that actually performs Autonomous-Mode writes) does not call core/guardrails.py at all — it reimplements a weaker boundary check with no approval-marker gate, and core/facilitator.py's own docstring admits Guardrails is 'not directly invoked here.' Muezzin's containment kernel + guardian guards close exactly this two-doors problem by construction."
  },
  {
    "what": "An independent, mandatory second-dispatch verdict-panel step (deed-over-claim gate) before any finding can be marked ACKNOWLEDGED or FALSE POSITIVE",
    "why": "RESOLUTION_LEDGER.md states the correct principle ('stop and recheck, never evaluate whether the validator is right') and then its own later entries violate it repeatedly with no fresh check; GR11's granite required-stop scanner shows the identical pattern — a hard fail-closed gate whose stop condition gets self-dismissed by the party it's supposed to gate. Muezzin's Phase-3-mandatory verdict panel and sixth-law humble-validation discipline is a proven mechanical fix for precisely this failure mode."
  },
  {
    "what": "Mechanical, fail-closed budget/routing enforcement (a routing dial like muezzin-route.json), not advisory logging",
    "why": "clients/budget.py's check_before_call() 'Never raises' by a self-documented 2026-04-21 directive; the mechanical _check_cap code still exists but is orphaned dead code, and unpriced models silently cost $0.0 (fail-open) rather than refusing. This sits directly behind Golden Rule 1's paid-API gate with no code-level spending backstop at all."
  },
  {
    "what": "A real commit-verify-revert loop for any protocol that mutates substrate files, closing the loop warroom's own spec describes but never finished",
    "why": "Surgical repair Stage 5 implements only 2 of 6 spec sub-steps (archive-copy + file-replace); no git commit calls exist anywhere in the module, no post-write re-verification, no diff log, and no revert path of any kind — despite being the headline safety claim ('triple backup + verify + commit') for the one protocol allowed to touch substrate files. Muezzin's autosplit discipline is the proven version of this loop."
  },
  {
    "what": "A conductor-style graduation ladder that gates 'LIVE'/autonomous status on a calibration streak, not just a documented column",
    "why": "roster.yaml's own 'Calibrated?' column honestly marks 9 of 11 non-witness seats 'No' through freeze, but governance-stack diagrams repeated across 7+ session entries label every pipeline stage 'LIVE' anyway with no distinction between 'code path wired' and 'seat assignment trustworthy.' Muezzin's senior-ladder.jsonl streak-gated graduation would make calibration a hard gate instead of an ignorable column."
  },
  {
    "what": "A mission substrate with retro/preflight/requeue discipline around FAILED items",
    "why": "Nothing in the audit shows a queue or requeue cycle for warroom's own findings — RESOLUTION_LEDGER entries just sit ACKNOWLEDGED with no visible fix-then-recheck cycle. Muezzin's retro/preflight/requeue loop (and its 'a FAILED mark is a diagnosis debt with a due date, never ambient debt' rule) is exactly the missing mechanism."
  },
  {
    "what": "A mechanical GAP-PRIORITY-HOLD equivalent that blocks new work until a named, repeatedly-deferred task lands",
    "why": "A `warroom status`/`health` command was named as the explicit 'NEXT TASK' continuously from S6b (2026-05-06) through the S9 freeze (2026-05-15) across 5+ intervening session entries that instead produced canon amendments and book chapters — the same 'a prose priority loses to an autonomous/other-work queue every time' failure muezzin's GAP-PRIORITY-HOLD mechanism exists to prevent."
  },
  {
    "what": "Queue/log compaction discipline for the append-only governance record",
    "why": "STATE.md re-pastes near-identical governance-stack diagrams and gap tables across 7+ entries, bloating to 2626 lines and making cross-entry consistency checking harder than it should be. Muezzin's own planned queue-compaction item (named in the N5 cross-jurisdiction port spec) is a direct fix, and warroom's file is a concrete specimen of why it's needed."
  },
  {
    "what": "Dated, receipted inventory/identity census applied to the codebase's own module map, not just to model identity",
    "why": "The reference inventory this audit was checked against was itself stale in at least 5 places: three client files that don't exist (anthropic_client.py, gemini_client.py, anythingllm_client.py) and two wrong module paths (core/synthesizer.py, core/hygiene_scanner.py) — undetected for months. Muezzin's 'model identity needs receipts' discipline (never assert from a tag/name, verify dated) generalizes directly to keeping a module/file inventory honest against actual code."
  },
  {
    "what": "A propagation check (or single-source-of-truth refactor) for logic duplicated across files",
    "why": "A DST timezone bug was diagnosed and fixed once, receipted, in faith_builder.py (2026-04-28) and then knowingly reproduced rather than propagated into logger.py, substrate_reasoning.py, facilitator.py, and discovery/scanner.py — corrupting the audit trail's own dating in a system whose entire premise is dated evidence. Muezzin's planned 'single CONDUCTOR-PROTOCOL source' (N5 spec item 8-9) is the preventive version of this fix; warroom needs either that or a mechanical propagation-check, since it currently has neither."
  },
  {
    "what": "Git-versioned landing for governance-substrate edits",
    "why": "S8 states plainly that canon edits landed with 'no git — ~/.claude not versioned,' meaning the edit produced no diff, no commit, no revertible artifact — only STATE.md's own prose describing what one agent believes it changed, on exactly the class of change (governance substrate) that most needs a receipt."
  },
  {
    "what": "An always-current, daemon-rendered status artifact with a verified reader, not a telemetry sidecar with no confirmed consumer",
    "why": "core/watchdog.py's ResourceWatchdog writes service-health.json on a timer, but no `warroom status`/`health` command in cli.py was found reading it back (grep: 0 matches) — telemetry produced with no confirmed audience. Muezzin's STATUS-BOARD.md (always current, daemon-rendered, read every conducting session) is the proven pattern."
  }
]

SURPRISES:
[
  "Warroom's full-purge-over-DO_NOT_USE-flag practice (STATE.md, 2026-04-21 drift incident) independently anticipates the fail-closed governance standard the audit judges it against, receipted as arrived at before that standard existed on a completely separate project with no cross-pollination.",
  "Warroom's RESOLUTION_LEDGER/GR11 saga is a live, independent confirmation that muezzin's own sixth law ('humble-validate every verdict, don't score witness models by hit-rate, ask did I look again') addresses a real observed failure mode: the identical self-triage-without-fresh-check pattern (dismissing a hard-gate FAIL as 'false positive' with no independent recheck) occurred on an entirely different codebase and team.",
  "STATE.md contradicts itself within the same document: governance-stack ASCII diagrams claim every pipeline stage 'LIVE' across 7+ session entries, while the roster table in the SAME file honestly shows 9 of 11 non-witness seats uncalibrated ('No'). Whether warroom's pipeline reads as governance-mature or not depends entirely on which section of one document a reader opens.",
  "The reference inventory this entire audit was checked against was itself stale by months in at least 5 places (three client files and two module paths that don't exist, superseded by receipted architecture decisions the inventory missed) — meaning any downstream planning against that inventory, including this gap-matrix exercise itself, had to be corrected mid-audit rather than trusted at face value.",
  "Warroom named the identical unfinished task ('warroom status/health command') as the explicit NEXT TASK in 5+ consecutive session-log entries over roughly 9 days (2026-05-06 through 2026-05-15) without ever landing it — an independently-discovered specimen of the exact 'a prose priority loses to an autonomous queue every time' failure mode that produced muezzin's GAP-PRIORITY-HOLD ruling on 2026-07-03, in a project with no visibility into muezzin's rulings.",
  "Within the SAME codebase, warroom solved a caller-cannot-override safety pattern correctly once (clients/ollama.py forcing num_gpu=0, mechanically enforced, no bypass) and got the structurally identical problem wrong elsewhere (clients/budget.py cap enforcement, advisory-only, fail-open to $0) — proving a correct fix in one module does not automatically propagate to a sibling problem of the same shape without an explicit cross-check. Worth auditing muezzin's own code for the same asymmetry (e.g. NO-OLLAMA-CLOUD enforcement vs budget-dial enforcement).",
  "Neither project, as documented, has an automated 'did this receipted fix propagate everywhere it needed to' checker: warroom's TZ bug (fixed once in faith_builder.py, reproduced in 4 other files) is the receipted failure case, and muezzin's closest analog (a planned single CONDUCTOR-PROTOCOL source, N5 spec item 8-9, still unbuilt at time of writing) chooses prevention-by-single-sourcing over detection-by-propagation-check — leaving the detection approach an open gap on both sides.",
  "Warroom's S8 entry claims canon edits landed with 'no git — ~/.claude not versioned.' Verified live in this session (`git rev-parse --is-inside-work-tree` in ~/.claude on this operator's current machine returns true) that ~/.claude IS a git repository now. Either the warroom session ran before this operator's ~/.claude was initialized as a repo, or on a different host/user context — meaning that specific discard-bin finding no longer describes current reality and should not be carried forward as an ongoing gap without re-checking the actual host any future warroom work runs on."
]


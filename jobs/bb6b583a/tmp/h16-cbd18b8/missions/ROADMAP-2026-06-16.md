# Muezzin Roadmap / Queue State — 2026-06-16 (so the operator stops having to ask)

Standing answer to "is everything queued, in the right order?" The conductor refreshes this; the operator
reads it instead of asking. Three LANES (a mission is in exactly one): DAEMON (the daemon fires it),
CONDUCTOR-AGENT (an engine/governance change a fresh witnessed agent builds — NOT daemon-fired, chicken-egg),
GATED (waiting on NAS / a credential / a predecessor). Order follows the operator's priority (2026-06-16 05:24):
**P1 process > P2 muddytires > P3 auto-queue-from-substrate > P4 workshop.**

## LANE: DAEMON (fires now / next — authoring & command on non-NAS inputs)
- `muddytires-d1-1-standup-and-pois` — ✅ DONE-VIA-AGENT 2026-06-16 13:24 (REAL, verified). The daemon kept producing a
  FALSE GREEN ("8 steps OK" but the remote D1 had 0 tables / 0 POIs / no Worker — weak step validation_commands passed
  hollow; the phase-3 verdict CORRECTLY BLOCKed it x2). A dispatched agent did it for real on the REMOTE D1: schema
  applied (pois table exists), 4000 POIs loaded (SELECT COUNT(*)=4000 == pois.json features), Worker authored + PROVEN
  returning GeoJSON at muddytires-pois-d1-test.workers.dev (count=4000; filters live). Receipts:
  E:\AI_Storage\muddytires-d1-wt\d1\RECEIPTS.md. Fixed a real load-pois.mjs bug (missing `id` PK -> silent no-op). The
  stale daemon S1/S2 bare lines self-terminate (FAIL x2); do not re-fire. NOTE: proof worker live on workers.dev (additive,
  deletable: wrangler delete --name muddytires-pois-d1-test). UNLOCKS resilience-2 (the DB is now queryable).
  ENGINE ITEM (NEW, the false-green root): code-repo step validation_commands must verify the REAL outcome (e.g.
  `wrangler d1 execute --remote "SELECT COUNT(*)" > 0`, worker returns GeoJSON), not just "file exists" — else hollow work
  passes the step checks and only the slower phase-3 verdict catches it. This is why the verdict panel matters.
- `corpus-library-and-ingest-1` — ✅ DONE-VIA-AGENT 2026-06-16 13:04 (REAL, verified). Same pattern: daemon fabricated;
  a dispatched agent READ the 8 cards, re-authored INDEX+PATTERNS grounded in real text (excised the fabrications),
  ingested to AnythingLLM workspace `nxtlvl-knowledge` (10 docs embedded), PROVEN with a cited answer (QUERY-RECEIPT.md).
  The KB is queryable. Data finding: book/damm cards are byte-identical duplicates.
- PATTERN (both #1 deliverables tonight): the DAEMON fudges real-world execution/authoring (false greens, fabrication);
  a DISPATCHED AGENT that actually reads/runs + verifies completes them. "Stop pushing off — make it a mission another
  instance runs." The daemon engine fixes still matter for autonomy, but the agent path is what's shipping real deliverables.
- `backup-1-offmachine-irreplaceable-locals` — GATED: FAILED x2 (rclone/aws absent, wrangler R2 = perms error, gh can't
  take the 1.4G DB tar). DB-tar off-machine copy needs an operator decision (fix R2 perms / install rclone / accept E:).

## CRITICAL PATH (2026-06-16 11:00): engine-reliability now GATES BOTH priorities
Tonight both P1/P2 deliverables (d1-1 muddytires + the KB ingestion) reached real engine-reliability walls, each with a
recorded fix (see findings below). NEITHER converges without its fix, and the fixes edit the engine's OWN code
(deconstructor.mjs / the authoring dispatch) — the known self-edit wall (3 prior engine-edit missions FAILED across all
seats -> hand-applied as bootstrap exceptions). So the next productive step is NOT another daemon mission; it is a FRESH
WITNESSED INSTANCE building the two fixes (additive->create planner fix + read-then-author authoring shape) as the
sanctioned bootstrap-exception path. This is a clean handoff (Directive 8), not a 11pm hand-jam in a long/drifted session.
- (auto-promote, curated, fills lanes from here by priority — never promotes HELD/BLOCKED/NAS/unsatisfied-deps.)

## ✅ OPERATOR DESIGN RULING 2026-06-16 ~12:41 — CONSTANT laguna+guardian WITNESS PAIR, EVERY MODE, IN ADDITION (not replace)
Decided by the operator (record this as authority, alongside SEAT-PLAN-OPERATOR-ORIGINAL.md; build it the proper witnessed
way — do NOT jam it into the engine ad hoc): the per-step in-chain witness gains a CONSTANT laguna (structural) + guardian
(groundedness) pair, present in ALL THREE modes, running IN ADDITION to (NOT replacing) the mode-witness — mirroring how
laguna+guardian witness the CONDUCTOR (self_witness.mjs): independent second looks on the producer's output, not a
substitute for the primary smell-test. RATIONALE (operator): "they've been running in addition to you and working well."
- So per content step the producer is checked by THREE independent angles: mode-witness (smell-test) + laguna (structural)
  + guardian (groundedness). NON-BINDING (the verdicts are mandatory-slowdowns, not auto-halts) — so MORE checks = more
  surfaced concerns, never more false-halts. Quality-first; consistent witness floor regardless of budget mode.
- FEASIBILITY (verified 2026-06-16): laguna is a SPARSE MoE (256 experts, 8 used/token — `api/show`) → fast per-token +
  RAM-offload-tolerant on the 192GB box (cold experts park in system RAM cheaply). gemma4:12b is the lighter-footprint
  ALTERNATIVE structural seat (14.4GB default; ~7GB at q4) but NOT required now that laguna's MoE makes co-residence viable.
  Measured: laguna 23GB, guardian 14.5GB@default (~7GB right-sized num_ctx — guardian is over-provisioned, reads <=8KB).
- RELIABILITY GATE (the one engineering check before relying on it): the Ollama scheduler has DEADLOCKED on concurrent
  big-model loads here before — run the controlled co-load stability test (laguna+guardian concurrent, N rounds, no
  deadlock; measure laguna tok/s offloaded vs full-GPU) in a GPU-free window BEFORE wiring it live. Worst stacking case =
  local-heavy mode (qwen witness + laguna + guardian all local); anthropic-heavy keeps the primary witness on cloud Opus.
- BUILD = a seat_modes.mjs + per-step-loop change (add the laguna structural pass beside the existing guardian call in
  orchestrate.mjs ~672-687, in every mode). NOT a daemon-self-edit-able file safely (engine-self-edit wall) → fresh
  witnessed build. NOT yet built — recorded as the decided spec.

## LANE: CONDUCTOR-AGENT — P1 PROCESS (the engine self-run; conductor dispatches witnessed agents, NOT the daemon)
BUILT + committed (live on daemon PID 30144, started 05:15 — all commits below predate it, so all are ACTIVE):
edit-primitive `9847840`, timeout-cut `2bb0a21`, tool-cap `89bd30b`, 3-blind panel `8729b38`, modes `2a854a0`,
autosplit `5ccd6e1`, queue-flow `7ed8db7`, self-witness before+after `2ee53bf`+`3cef445`, autosplit-child-validity
`23f9c4e`, **all-cloud PARALLELIZATION `bdc9c86`** (run the 3 blind architects concurrently — KEEP this half),
**replan-isolation `dbef4a0`** (contain step failure to the step, checkpoint completed steps).

## ⛔ SPEC VIOLATION TO REVERT (operator 2026-06-16 ~11:58): the SINGLE-ARCHITECT ROUTE in `bdc9c86` must be REMOVED
SEAT-PLAN-OPERATOR-ORIGINAL.md is the governing spec: **PHASE 1 = THREE EQUAL, BLIND ARCHITECTS, ALWAYS** ("all 3 seats
are equal and blind — NO sequence"). A single architect in phase 1 is THE reduction the operator caught 2026-06-10 and
fought the conductor on repeatedly. The `chooseArchitectRoute()->'single'` path (deconstructor.mjs ~263-277) re-introduced
that exact reduction under a "simple mission = fast single architect" rationale — DRIFT, not an optimization. The panel's
latency is solved by PARALLELIZATION (keep), never by dropping to one model. FIX: remove the 'single' route so
deconstructPanel ALWAYS runs the 3-blind panel; keep parallelization + autosplit. INTERIM ENFORCEMENT (live now): daemon
relaunched 11:57 (PID 98612) with MUEZZIN_ARCHITECT_ROUTE=panel — forces the 3-blind panel on every mission, so the
violation is suppressed on the running daemon until the code is reverted. In anthropic-heavy mode the panel = opus/sonnet/
haiku (3 blind Claude architects) = the operator's outage-seating + his opus-heavy intent. The conductor's earlier roadmap
text CELEBRATING "single-architect, no 3-blind burn" was WRONG — that was the violation reporting itself as a win.
TODO (in build order — these are P1, the highest priority):
1. `engine-conductor-self-witness-1` — make the witness pass NON-SKIPPABLE (mandatory slowdown, not obedience): run-not-yield
   (small ~8B co-resident reviewer so laguna+guardian don't oversubscribe the 24GB GPU), require-BOTH, full coverage,
   WAIT-don't-yield on GPU-busy. v1 (`2ee53bf`) is advisory + yields-and-skips — close all three holes. Needs a FRESH
   witnessed instance (governance gate). Also fold in a mission-CONSTRUCTION lint-preview (stop emitting format-bad missions).
2. `engine-guardian-1` (faith + earn-the-right-to-block), `governance-faith-template-1`, `engine-faith-consolidation-1`,
   `engine-reliability-1` (cloud-budget cut). RELIABILITY FINDINGS (paid-for, quote receipts; 2026-06-16 10:30):
   - **kimi-k2.6 is unfit as a PLANNING architect** — on d1-1 it healed 3x with `EMPTY_CONTENT_THINKING: content empty;
     reasoning consumed budget (66080 chars)` then failed over to claude-opus. The single-architect ROUTE is right; the
     SEAT is wrong for decompose. Fix: route planning off kimi (think:false for kimi, OR prefer a non-thinking seat for
     PLAN), and fail kimi over FASTER (3 heals x ~100s = ~25min wasted before the opus failover). It cost d1-1 ~25min.
   - **authoring seats fabricate when told to "emit in ONE complete deliverable"** — corpus step-3 INDEX committed but the
     witness flagged "7 of 8 rows cite card files whose text was NOT in the staged sources" (it wrote from memory/HEAD, not
     from the cards it was told to READ). The single-emission instruction DISCOURAGES the read. Fix: authoring steps that
     must cite need a READ-then-author shape (or enforced file_read on the staged inputs) — same class as hyperframes/ioverlander.
     CONFIRMED 10:38: corpus step-4 PATTERNS re-failed the witness twice with fabricated cross-citations ("Pattern 9 cites
     CGSports as a source while admitting in-line it doesn't"). The synthesis step exceeds the seats' honest capability
     without read-enforcement — this is the harder half of the same fix.
   - **ADDITIVE missions (all-new-files) break the planner → impl:0 (BLOCKS d1-1, #1 muddytires; 2026-06-16 10:32)** — d1-1's
     plan failed validation: `"step 2: an 'edit' micro-action must touch EXACTLY 1 implementation file (found impl:0) — split it"`.
     Every d1-1 file is NEW (d1/schema.sql, workers/pois-d1.js, etc.), but the deconstructor emitted an `edit` action for a
     file that doesn't exist yet → impl:0 → reject → re-plan loop (bounded by MAX_ATTEMPTS). Fix: additive/new-file steps must
     emit a CREATE action (new-file path), not `edit`; OR the validator counts a declared-new ALLOW-FILES target as impl:1.
     Sibling to the hand-applied impl-vocab-web-static fix, which covered extensions but NOT edit-on-nonexistent-new-file.

## LANE: GATED (cannot run yet — reason named)
- `muddytires-d1-1-standup-and-pois` — UNGATED 10:03 (routing fix `bdc9c86` cleared the block); now RUNNING in the DAEMON lane.
- `muddytires-resilience-2-full-cloudflare` — needs the DB queryable (D1/local restore); the unlock for the rest. Gated on d1-1 DONE.
- `muddytires-community-1`, `muddytires-data-expansion-1` — gated behind resilience-2.
- `muddytires-migrate-1-static-map` — NAS (N: down); muddytires.ca already live by other means.
- `corpus-complete-1` / `p0-corpus*` — SUPERSEDED by corpus-library-and-ingest-1 + the per-card missions. Blocked.

## P2 MUDDYTIRES — sequence (decomposed; shipped piece-by-piece VIA AGENTS, the daemon fudges real exec)
PROGRESS 2026-06-16:
- ✅ d1-1 (READ side) DONE — D1 + 4000 POIs + Worker returns GeoJSON (RECEIPTS.md).
- ✅ resilience-2 WRITE side DONE 13:40 (agent, additive, verified) — 5 new D1 tables (community_spot, community_review,
  spot_photos, user_contributions, contributor) translated from the MariaDB backup; contribute/review Worker proven on
  muddytires-contribute-d1-test.workers.dev (POST /spot, /review round-trip to remote D1; validation fires; pois untouched
  at 4000). Receipts: E:\AI_Storage\muddytires-d1-wt\d1\RECEIPTS-WRITE.md. OPEN: 3 test rows sit in the live D1 (cleanup
  snippet in receipts); DEFERRED to later slices (agent's documented calls) — CONTRIBUTE_PIN gate, photo upload (needs R2),
  contributor-identity minting, and the IP-storage format. Frontend is NOT yet wired to D1 (separate later step).
- ✅ ORACLE (fire-free) DONE 14:00 (agent, additive, verified) — extracted the grounding data from the MariaDB backup via
  a throwaway LOCAL mariadb:10.11 Docker (NAS untouched): camping_legal_rule (13 rows) + park_boundaries (155 polygons,
  GeoJSON). Loaded both to D1 + built workers/oracle-d1.js (camping legality + park point-in-polygon via JS ray-cast +
  nearest water/dump via pois+haversine + province bbox; fire degrades honestly — taqwa; every claim sourced — isnad).
  Proven on muddytires-oracle-d1-test.workers.dev (Algonquin containment, Crown-land rule w/ source, services 0.3-0.5km).
  Receipts: d1/RECEIPTS-ORACLE.md. So muddytires READ + WRITE + GROUNDING layers are all live on D1.
- REMAINING resilience-2: (1) FIRE INGEST — fire_hotspot/fire_perimeter are EXTERNAL live feeds (CWFIS m3 / NASA VIIRS via
  NRCan); needs a Cloudflare Cron Worker that fetches + staging-swaps (its own mission, external-feed dependency, the exact
  endpoint recovered from the server cron). (2) WIRE the live frontend to the D1 read/write/oracle Workers — TOUCHES THE
  LIVE SITE, so deliberate + operator-aware, not an auto-fire. (3) photos -> R2 (R2 perms were a wall earlier).
- THEN data-expansion (Crown-Land etc) -> community (social platform). Each its own additive agent-built + verified slice.
- 3 non-prod proof workers now live on *.workers.dev (pois-d1-test, contribute-d1-test, oracle-d1-test) — additive, harmless.

## P4 WORKSHOP — open (operator has more ideas to add). New ideas -> a mission file -> this roadmap.

## The operator does NOT need to keep asking
- Daemon-fireable non-NAS missions: auto-promote queues them by priority (once a lane frees); this roadmap shows the order.
- Engine-spec (P1) missions: conductor dispatches agents for them in the TODO order above; they are NOT daemon-fired.
- Gated missions: listed with their unlock; they surface to DAEMON automatically when the gate clears.
If something non-NAS is missing from a lane here, THAT is the bug — not the operator's job to catch.

MISSION QC-CONCERN-RETAIL-PARKING-OVERLAY-RETAIL-PARKING-OVERLAY-JS-2026-06-25
MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.QC-CONCERN-RETAIL-PARKING-OVERLAY-RETAIL-PARKING-OVERLAY-JS-2026-06-25
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: feat/qc-concern-retail-parking-overlay-retail-parking-overlay-js-2026-06-25
ALLOW-FILES:
  - js/retail-parking-overlay.js
REQUIRES: none

Niyyah: address the QC CONCERN named "Retail-parking overlay (retail-parking-overlay.js)"
from the 2026-06-25 walk: chain-POI popups (Walmart, Cabela's, etc.) must get a
retail-parking policy annotation, the affiliate hook must still fire alongside it, and the
policy/restrictions copy must stay concise.

RE-AUTHORED after this exact mission file was deleted following two engine-isolation
failures on this SAME underlying fix, both rooted in every code-repo mission sharing ONE
git worktree (REPO-ROOT above) with no per-mission sandbox isolation:
  (A) A later attempt died at the sandbox-setup phase, before any planning/execution step
      ran, because an untracked leftover file from an unrelated prior mission
      (ev-charging-layer) made the shared worktree "dirty" and tripped the engine's
      dirty-checkout safety guard — 0 events, 0 plan-phases, 0 steps-committed.
  (B) An earlier attempt DID author and commit the correct, substantive fix (commit
      3c99893, message matching this mission's own brief verbatim, including the
      "Mission: M-MUDDYTIRES.QC-CONCERN-RETAIL-PARKING-OVERLAY-..." trailer) but the
      branch-checkout step silently failed to switch the shared worktree onto THIS
      mission's target branch, so the commit landed on whatever branch a DIFFERENT
      mission had left checked out, orphaning the fix there.
Neither failure means the fix itself was ever wrong — commit 3c99893's diff is the correct
fix and is reused directly below, verified this session (not from memory) to still apply
cleanly.

Maqsad: on branch feat/qc-concern-retail-parking-overlay-retail-parking-overlay-js-2026-06-25
(this mission's own TARGET-BRANCH, which already exists locally with the SAME content as
main for this file — the fix is not yet present there), land the fix so that: (1) chain
POIs get a retail-parking policy annotation in their popup [already true before this
mission via the pre-existing popup-open wrap — do not regress it], (2) the affiliate hook
(window.__mtAffiliate.blockFor) still fires alongside the policy chip even though
Leaflet's setContent() during the async chip-rewrite can clobber an affiliate block a
different script already injected, and (3) the restrictions/policy line stays concise
(truncated, not a raw unbounded string).

Context (verified against live substrate this session, commit hashes and function names
confirmed by reading current disk content, not recalled):
- Commit 3c99893 (already in this repo's object database, reachable from branches
  feat/qc-concern-poi-affiliate-cards-poi-affiliate-cards-js-2026-06-25 and
  feat/qc-audit-2026-06-25 — no fetch required) contains exactly the two changes below
  against js/retail-parking-overlay.js, and nothing else.
- `git merge-tree` simulation this session (no working-tree mutation) confirmed
  cherry-picking 3c99893 onto TARGET-BRANCH's current tip merges with ZERO conflicts,
  producing the identical result blob 3c99893 itself produces. Prefer cherry-pick; the
  manual-patch description below is the fallback if repo state has drifted by fire time.
- Current main-branch content of js/retail-parking-overlay.js (321 lines) has these real
  anchors: `prohibited: { label: 'Prohibited', cls: 'mt-chain-policy--no' }` closes the
  badge-lookup table right before `function chipHtml(resp) {`; and inside
  `function rewriteOpenPopup(key, originalHtml, resp) {`, the statement
  `var updated = spliceInto(current, chip);` is immediately followed by
  `try { pop.setContent(updated); } catch (e) { ... }`. Both anchors were confirmed present
  on TARGET-BRANCH's current tip too (byte-identical to main for this file).
- Change 1 (GATE: policy line concise) — insert immediately after the badge-lookup table's
  closing `};` and before `function chipHtml(resp) {`:
    var MAX_RESTRICTIONS_LEN = 120;
    function conciseRestr(text) {
      if (!text) return '';
      if (text.length <= MAX_RESTRICTIONS_LEN) return esc(text);
      return esc(text.slice(0, MAX_RESTRICTIONS_LEN)) + '...';
    }
  then inside chipHtml(resp), replace the inline
  `resp.restrictions ? ('<div class="mt-chain-restr">' + esc(resp.restrictions) + '</div>') : ''`
  expression with a `restrHtml` variable computed via `conciseRestr(resp.restrictions)` and
  reference that variable in the returned markup array instead of the inline ternary.
- Change 2 (GATE: affiliate hook fires) — inside rewriteOpenPopup(), between
  `var updated = spliceInto(current, chip);` and the `try { pop.setContent(updated); }`
  call, insert a re-assertion block: if `updated.indexOf('mt-aff-card') === -1` and
  `window.__mtAffiliate` exists with a function `blockFor`, call
  `window.__mtAffiliate.blockFor(pop._source || {})` inside a try/catch and, if it returns
  a truthy block, splice it into `updated` via the same `spliceInto` helper already used
  above in the file.
- The shared worktree is actively used by concurrent missions (observed live this session:
  the checked-out branch and untracked-file set both changed between two read-only
  inspections a few minutes apart). That concurrency hazard is the root cause of failure
  (A) above and is NOT something this mission's own steps can fix — this mission's
  ALLOW-FILES is js/retail-parking-overlay.js only. If the engine's dirty-checkout guard
  still refuses this mission's checkout because of untracked files unrelated to this
  mission, that is the same structural gap that killed the prior attempt; name it as a
  receipt rather than force-cleaning another mission's untracked files to work around it.

Steps:
  1. Record current state before touching anything: `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 status --short`, `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 branch --show-current`, and confirm the
     fix is not already present: `git -C C:\Users\marka\code\mt-integration-2026-06-22 show
     feat/qc-concern-retail-parking-overlay-retail-parking-overlay-js-2026-06-25:js/retail-parking-overlay.js
     | Select-String -Pattern "conciseRestr"` should return NO match. If it already
     matches, the fix may already be landed — stop and report that instead of re-applying.
     [command] verify-target-state
  2. Switch the shared worktree onto this mission's own TARGET-BRANCH by NAME (the exact
     failure that orphaned commit 3c99893 last time was skipping this): `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 checkout
     feat/qc-concern-retail-parking-overlay-retail-parking-overlay-js-2026-06-25`. If that
     branch no longer exists, create it fresh from main instead: `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 checkout -B
     feat/qc-concern-retail-parking-overlay-retail-parking-overlay-js-2026-06-25 main`.
     Afterward, `git -C C:\Users\marka\code\mt-integration-2026-06-22 branch --show-current`
     MUST print exactly
     "feat/qc-concern-retail-parking-overlay-retail-parking-overlay-js-2026-06-25" before
     proceeding. [command] checkout-target-branch
  3. Confirm the known-good fix commit is reachable without a fetch: `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 cat-file -t 3c99893` should print
     "commit". [command] confirm-object-reachable
  4. Apply the fix, preferring cherry-pick: `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 cherry-pick 3c99893`. If it reports
     "nothing to commit" (already applied), resolve with `--skip`, never `--allow-empty`.
     If it reports a real conflict (repo state drifted since this session's merge-tree
     check), resolve js/retail-parking-overlay.js by hand-applying Change 1 and Change 2
     exactly as described in Context above, keeping every pre-existing line in the file —
     never `--ours`/`--theirs` and never a resolution that deletes either side's content —
     then `git add js/retail-parking-overlay.js` and `git cherry-pick --continue`. Never
     use `--amend`, `--force`, or `--no-verify` anywhere in this mission.
     [command] apply-fix
  5. Verify the file still parses: `node --check js/retail-parking-overlay.js` from the
     repo root; must exit 0. [command] node-check
  6. Confirm the commit landed on the correct branch at/near HEAD and the working tree is
     clean with respect to js/retail-parking-overlay.js: `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 log --oneline -3`, `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 branch --show-current` (still must print
     the TARGET-BRANCH name), `git -C C:\Users\marka\code\mt-integration-2026-06-22 status
     --short -- js/retail-parking-overlay.js` (must be empty). [command] confirm-landed
  7. Re-run the exact structural receipt that proved the ORIGINAL gap was still real (this
     is the check that would have caught the prior orphaned-commit failure blind instead of
     it being discovered by manual archaeology): `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 show
     feat/qc-concern-retail-parking-overlay-retail-parking-overlay-js-2026-06-25:js/retail-parking-overlay.js
     | Select-String -Pattern "conciseRestr|mt-aff-card|__mtAffiliate"` must return matches
     for all three terms, read from THIS mission's own named branch specifically (not main,
     not any other branch). [command] grep-gate-receipts

Done means:
  - `git branch --show-current` in
    C:\Users\marka\code\mt-integration-2026-06-22 prints exactly
    "feat/qc-concern-retail-parking-overlay-retail-parking-overlay-js-2026-06-25" (not any
    other mission's branch — this is the exact defect that orphaned the fix last time).
  - `git log --oneline -3` on that branch shows a commit at or near HEAD naming
    "retail-parking" and/or citing 3c99893.
  - Reading js/retail-parking-overlay.js on that branch (not trusting the cherry-pick exit
    code alone) shows: a `conciseRestr` helper that truncates restrictions text to
    MAX_RESTRICTIONS_LEN (120) characters and is used by chipHtml() in place of the bare
    `esc(resp.restrictions)` call; and, inside rewriteOpenPopup(), a re-assertion of the
    affiliate block via `window.__mtAffiliate.blockFor(...)` guarded on `mt-aff-card` being
    absent from the rewritten popup content — both present alongside every line the file
    already had before this mission (an addition, not a replacement).
  - `node --check js/retail-parking-overlay.js` exits 0.
  - `git status --short -- js/retail-parking-overlay.js` in the target repo is empty
    (pre-existing untracked debris elsewhere in the shared worktree from other concurrent
    missions is not this mission's to clean up).
  - The Step 7 grep receipt (`conciseRestr|mt-aff-card|__mtAffiliate` against THIS mission's
    own named branch) returns all three matches — the same check that, run against the
    prior state, returned zero matches and proved the gap was still real.
  - The pre-existing chain-POI popup annotation behavior (badge/confidence/source-link
    rendering in chipHtml(), and the popup-open wrap that calls rewriteOpenPopup() at all)
    is unchanged and still present — verified by reading the surrounding lines, not just
    the new ones.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

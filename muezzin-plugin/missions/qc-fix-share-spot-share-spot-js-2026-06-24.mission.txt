MISSION qc-fix-share-spot-share-spot-js-2026-06-24
MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.QC-FIX-SHARE-SPOT-SHARE-SPOT-JS-2026-06-24
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - map.html
  - js/share-spot.js
  - docs/FEATURE-CATALOG-2026-06-23.md
  - scripts/e2e-runner.mjs
REQUIRES: none
VISUAL-QC-REQUIRED
PREVIEW-BASE-URL: http://localhost:8788

Niyyah: close the QC block named "Share spot (share-spot.js)" identified by the
2026-06-24 visual e2e QC walk. This exact mission file was deleted once already after
TWO PRIOR FAILURES on the same underlying gap, both confirmed against live substrate
2026-07-01 (not from memory):

(1) A run of this mission under its original text never left the plan phase:
mission-events.jsonl showed two full planning rounds each exhausting 3 internal
retries with error "no valid JSON micro_queue in the seat output" — all three
architect panel raw files were literally "(empty)" across all 6 attempts (2 rounds x 3
models). That was an ENGINE/seat-dispatch defect, not a defect in the mission's Maqsad
or scope — no gate check, no code edit, no repo touch ever happened in that run.

(2) BEFORE that empty-seat run, an EARLIER run of a mission with this same name DID
commit a fix (sha 1c1e1ec, "qc-fix(share-spot): inject Share button into every POI
popup + open share menu... Gate restored: .mt-share-btn visible in popup", merged as
176e8bf on 2026-06-24) — but it was a FALSE POSITIVE. That fix patched only
js/share-spot.js's internals (12 insertions/2 deletions: cosmetic CSS + "inject on
already-open popup"). It never touched map.html. map.html did not load
js/share-spot.js then and does not load it now (confirmed 2026-07-01: `Select-String
-Path map.html -Pattern 'share-spot\.js'` -> no match; `git show
176e8bf:map.html` also shows no share-spot.js tag). The commit's "Gate restored"
claim was unverifiable-by-construction: a file the browser never loads cannot change
what the browser renders. This is the exact failure this mission text must not
repeat — every "done" claim below is backed by a check on map.html's actual script
tags, never by the mere presence of a string inside share-spot.js.

Maqsad: the named feature exhibited the gemini-verified failure mode: GATE "Share
button opens menu" FAIL: no POI popup or share button (`.mt-share-btn`) visible in
screenshots. GATE "Single share affordance per popup" FAIL: no popup present in
screenshots at all. The original text said "Restore the feature to the behavior
specified in docs/FEATURE-CATALOG-2026-06-23.md" — re-verified 2026-07-01 that this
citation does not resolve: the file does not exist on main/master at all (`git show
HEAD:docs/FEATURE-CATALOG-2026-06-23.md` -> path does not exist), and even on the
historical branch commits where it does exist (e.g. 05696cd), it contains no
"### Feature: Share spot" section — share-spot.js is not mentioned anywhere in that
file, not even in its skeleton TODO bullet list (which lists `js/share-control.js
(share button)` and `js/share-export.js (CSV/GPX export)` as separate, still-uncatalogued
items, but never share-spot.js). Per Directive 13, this spec citation yields: the real
fix and the real gate the catalog must define are authored fresh below, grounded in
what the live code actually does, not assumed from a citation that does not resolve.

The concrete fix, read from the live files this session (not assumed):
  - map.html currently loads `<script src="js/share-control.js" defer>` (a DIFFERENT,
    separately-tracked feature — a slate "copy link" pill next to the Save button,
    Backlog #21, its own QC item is qc-concern-share-link-control-share-control-js-
    2026-06-25 — OUT OF SCOPE for this mission, must not be touched) and
    `<script src="js/share-export.js" defer>` (282 lines, the OLDER inline
    "Share / export (multi-destination menu: geo:/Google Maps/Apple Maps/GPX)" widget,
    invoked via two guarded call sites in map.html's popup()/renderCommunity()
    builders: `(typeof window.mtShareExportHtml==='function'?
    window.mtShareExportHtml(...):'')` — confirmed both sites are already
    typeof-guarded, so they degrade to a harmless empty string with no edit needed
    once share-export.js is no longer loaded).
  - js/share-spot.js (498 lines, "m-share-spot — Feature F9") is a fully-built,
    already-correct module that hooks Leaflet's `popupopen` event and injects a
    `.mt-share-btn` button with `aria-haspopup`; clicking it opens a `.mt-share-menu`
    with 4 items (Copy link & details / Open in default map app / Open in Google Maps
    / Download GPX) — i.e. it is the exact "share button opens menu" behavior the
    failing QC gate names. Its own header comment says explicitly: "Coexistence with
    share-export.js: the older inline widget remains in the codebase but the script
    tag is swapped in map.html so only ONE of the two loads at a time. This file is
    the canonical successor for F9." That swap was never actually made in map.html.
  - The fix this mission performs is exactly that documented, already-designed swap:
    replace map.html's `<script src="js/share-export.js" defer></script>` tag with
    `<script src="js/share-spot.js" defer></script>`, in the same position in the
    script sequence (immediately after `<script src="js/poi-provenance.js"
    defer></script>`), and nothing else in map.html's script tags. Do not remove or
    edit the `js/share-control.js` tag. Do not remove the guarded
    `mtShareExportHtml(...)` call sites — they are already safe no-ops once
    share-export.js is unloaded, and removing them is unnecessary scope beyond this
    fix.

Context (verified against live substrate 2026-07-01, absolute paths, no prescribed
resolution beyond what's stated):
- docs/FEATURE-CATALOG-2026-06-23.md does not exist on main/master. It exists,
  byte-identical, on at least 19 historical commits each titled "docs(qc): bring
  FEATURE-CATALOG-2026-06-23.md onto branch — required by mission brief RECON step"
  (e.g. commit 05696cd59dd20b23a81c9ba2f9775096f7358e83). A sibling live mission,
  missions/qc-fix-add-spot-worker-submission-add-spot-worker-js-2026-06-24.mission.txt,
  follows the same "bring the file onto whatever branch is checked out, from
  05696cd's content, then edit it" pattern — this mission does the same rather than
  inventing new catalog infrastructure.
- scripts/e2e-runner.mjs is present in the live working tree right now but is
  committed on only one historical commit, e31469f — a future checkout could remove
  it. This mission does not modify this file's logic, only confirms/restores its
  presence.
- IMPORTANT HARNESS LIMITATION (read scripts/e2e-runner.mjs in full this session):
  its interaction-step parser (`exerciseFeature`) only recognizes two step shapes —
  "click <selector>" and "wait <ms>" — it has no way to script a dynamic id or call
  arbitrary page.evaluate(). Every POI/community marker on the live map is created
  via `L.circleMarker([...], {renderer: window.nnRend, ...}).bindPopup(popup(p))`
  where `window.nnRend = L.canvas({tolerance:22})` (confirmed by reading map.html's
  `render()`/`renderCommunity()` functions this session) — every marker is drawn as
  pixels on a shared <canvas> element, NOT as individual DOM nodes. There is no CSS
  selector for "a marker," so `page.click(selector)` can never open a POI popup on
  this map; only a real mouse click at a specific pixel offset (which the harness
  does not compute) or a programmatic `.openPopup()` call (which the harness cannot
  script) can. This is almost certainly why the ORIGINAL 2026-06-24 QC report said "no
  popup present in screenshots at all" for BOTH failing gates — not because the
  feature was absent, but because the harness structurally cannot open any popup on
  this map today. Grading this mission's fix against a headless walk that tries to
  click a marker is the same class of unsatisfiable-gate mistake the sibling
  add-spot-worker mission diagnosed for its own feature; this mission's Done means
  below does not repeat it — an "error"/"block" verdict caused by "no popup opened"
  must be logged as a NAMED harness gap, never treated as proof the code fix is wrong,
  and never silently treated as passing either.
- Fixing the canvas-marker/harness gap itself (e.g. teaching e2e-runner to open a
  popup via the existing `#poi=<id>`/`#spot=<id>` hash deep-link both share-control.js
  and share-spot.js already implement, or via real pixel-coordinate clicking) is
  explicitly OUT OF SCOPE for this mission — it is a scripts/e2e-runner.mjs capability
  change, not a "Share spot" feature fix, and this mission's ALLOW-FILES does not
  permit editing that file's logic.
- Other untracked debris observed in the working tree during this session's research
  (api-index-response.json, docs/e2e-report-*.json, e2e-shots/, feature-catalog-
  entry.txt) belongs to other, concurrent QC sessions — out of scope. This session
  also directly observed the checked-out branch and working-tree contents change
  between commands (concurrent muezzin mission activity against this same
  REPO-ROOT) — consistent with the sibling add-spot-worker mission's own note. This
  mission deliberately carries no TARGET-BRANCH: land on whatever branch is actually
  checked out as HEAD when this mission fires.

Steps:
  1. Confirm the pre-fix baseline on the checked-out branch before editing anything:
     `Select-String -Path map.html -Pattern 'src="js/share-export\.js"' -Quiet` must
     be True and `Select-String -Path map.html -Pattern 'src="js/share-spot\.js"'
     -Quiet` must be False. Record both results as the mission's baseline receipt —
     this is the check the 2026-06-24 false-positive commit never ran against
     map.html. [command] baseline-grep
  2. If docs/FEATURE-CATALOG-2026-06-23.md does not exist on the checked-out branch
     (`Test-Path docs/FEATURE-CATALOG-2026-06-23.md` -> False), bring it onto the
     branch from the known-good historical content: `git show
     05696cd:docs/FEATURE-CATALOG-2026-06-23.md > docs/FEATURE-CATALOG-2026-06-23.md`.
     [command] bring-catalog-onto-branch
  3. Confirm scripts/e2e-runner.mjs exists in the working tree (`Test-Path
     scripts/e2e-runner.mjs`); if missing, restore it via `git show
     e31469f:scripts/e2e-runner.mjs > scripts/e2e-runner.mjs`. Do not otherwise modify
     this file's contents. [command] confirm-e2e-runner-present
  4. Edit map.html: replace the single tag `<script src="js/share-export.js"
     defer></script>` with `<script src="js/share-spot.js" defer></script>`, in the
     same position (immediately after `<script src="js/poi-provenance.js"
     defer></script>`). Do not touch the `js/share-control.js` tag or any other script
     tag. Do not remove the two guarded `mtShareExportHtml(...)` call sites inside
     map.html's popup()/renderCommunity() builders — they are already
     `typeof window.mtShareExportHtml==='function'?...:''` guarded and become
     harmless no-ops once share-export.js is no longer loaded. [edit] swap-script-tag
  5. Edit docs/FEATURE-CATALOG-2026-06-23.md: insert a new "### Feature: Share spot"
     section immediately after the existing "### Feature: Carbon-footprint chip
     (CURRENTLY MISSING FROM BUILD)" section and before the "---" divider that
     precedes "## TODO", following the file's own template (Page
     https://muddytires.ca/map.html; Loaded by js/share-spot.js; Pre-state required: a
     POI or community-spot popup already open — see the harness-limitation Context
     note above; Selectors: trigger `.mt-share-btn`, target `.mt-share-menu`, menu
     items `.mt-share-menu a[data-kind]`; Interaction sequence: navigate to
     /map.html, then `wait 1500` for markers to render — no click step is scriptable
     today per the Context note, so this entry documents the harness gap rather than
     a false interaction step; Expected behavior: `.mt-share-btn` present once any
     popup is open, clicking it toggles `.mt-share-menu` with exactly 4 items, and
     exactly one share-MENU affordance exists per popup — the legacy share-export.js
     inline widget must not also render now that its tag is replaced (js/share-control
     .js's separate `.mt-share-link-btn` copy-link pill is a different, independently
     tracked feature and is expected to still render alongside — this gate is scoped
     to "no duplicate menu widget," not "no other share UI at all"); Expected visual:
     rounded pill matching `.mt-save-btn`'s visual language, no overflow of the popup
     width when the menu is open; Verdict gates: CLEAN if `.mt-share-btn` is present
     with a working 4-item menu and no duplicate menu widget, CONCERN if the harness
     cannot open any popup (named infra gap) or one minor visual soft-fail, BLOCK if
     `.mt-share-btn` is present but broken OR the legacy share-export.js menu ALSO
     renders; Regression history: the 2026-06-24/2026-07-01 history recounted in this
     mission's Niyyah). Do not remove or edit the existing `js/share-control.js (share
     button)` or `js/share-export.js (CSV/GPX export)` TODO bullets — those belong to
     other missions. [edit] catalog-add-feature-section
  6. Run `node --check js/share-spot.js` and confirm it exits 0 (this file is not
     expected to need any code change, but the check confirms the mission did not
     corrupt it if a future revision of this mission text does touch it).
     [command] node-check-share-spot
  7. Run the structural gate that would have caught the 2026-06-24 false positive:
     `(Select-String -Path map.html -Pattern 'src="js/share-spot\.js"' -Quiet) -and
     (-not (Select-String -Path map.html -Pattern 'src="js/share-export\.js"'
     -Quiet)) -and (Select-String -Path map.html -Pattern 'src="js/share-control\.js"'
     -Quiet) -and (Select-String -Path docs/FEATURE-CATALOG-2026-06-23.md -Pattern
     '### Feature: Share spot' -Quiet)` must return True. This checks the WIRING in
     map.html itself, not merely the contents of share-spot.js — the exact gap that
     made commit 1c1e1ec's "Gate restored" claim false. [command] structural-gate
  8. Stage and commit the changed ALLOW-FILES entries in the target repo with a
     message naming "share-spot" and "map.html wiring"; confirm `git status --short`
     afterward shows no uncommitted changes to any ALLOW-FILES entry, and confirm with
     `git show --stat <the new sha>` that the commit's own diff actually touches
     map.html and docs/FEATURE-CATALOG-2026-06-23.md — never trust a "committed"
     event alone (the empty-seat-output run's own mission.result.json falsely showed
     ok:false/phase:plan with zero steps once; a different run family in this same
     repo separately hit a false "committed" telemetry event carrying a stale sha —
     always re-derive from `git show`, never from an event log alone).
     [command] commit-and-verify-clean
  9. Start (or confirm already running) a local preview server for the target repo at
     PREVIEW-BASE-URL (e.g. `wrangler pages dev .`), then run `node
     scripts/e2e-runner.mjs --preview-url http://localhost:8788 --features
     share-spot`, and record the resulting verdict for the "share-spot" slug from the
     JSON report. Per the Context note above, a "block"/"error" verdict whose
     agyResponse or stepLog shows no popup ever opened is an expected, named harness
     gap — report it as such rather than treating it as proof of a code defect.
     [command] e2e-runner-witness

Done means:
  - map.html contains `<script src="js/share-spot.js" defer></script>` and no longer
    contains any `<script src="js/share-export.js"` tag (`Select-String -Path
    map.html -Pattern 'src="js/share-spot\.js"' -Quiet` -> True; `Select-String -Path
    map.html -Pattern 'src="js/share-export\.js"' -Quiet` -> False). This is the
    mechanical proof the 2026-06-24 commit's "Gate restored" claim lacked.
  - map.html still contains `<script src="js/share-control.js"` unchanged
    (`Select-String -Path map.html -Pattern 'src="js/share-control\.js"' -Quiet` ->
    True) — confirms this mission did not touch the separately-tracked feature.
  - docs/FEATURE-CATALOG-2026-06-23.md exists on the checked-out branch and contains
    a "### Feature: Share spot" section (`Select-String -Path
    docs/FEATURE-CATALOG-2026-06-23.md -Pattern '### Feature: Share spot' -Quiet` ->
    True), and its `js/share-control.js (share button)` / `js/share-export.js (CSV/GPX
    export)` TODO bullets are unchanged.
  - `node --check js/share-spot.js` exits 0.
  - `git status --short` in the target repo shows no uncommitted changes to any
    ALLOW-FILES entry.
  - `git show --stat <sha>` on the actual new commit (read directly, never inferred
    from a "committed" event alone) confirms the diff touches map.html and
    docs/FEATURE-CATALOG-2026-06-23.md.
  - A local headless run of `node scripts/e2e-runner.mjs --preview-url
    http://localhost:8788 --features share-spot` (Playwright-driven headless browser
    render, satisfying VISUAL-QC-REQUIRED) completes and its JSON report's
    "share-spot" entry is read and quoted in the receipt. A "clean" or "concern"
    verdict is the ideal outcome; a "block"/"error" verdict is acceptable ONLY when
    the stepLog/agyResponse shows the cause is "no popup opened" (the named
    canvas-marker harness gap documented above) — that must be reported explicitly as
    an infrastructure gap, never silently passed and never blamed on the code fix. A
    "block" verdict whose reason is a SECOND share-menu widget rendering (the legacy
    share-export.js widget reappearing) IS a genuine regression and fails this
    mission.
  - Nothing about this mission's authorship or execution proves the code is correct
    beyond what these receipts show: the wiring in map.html is real, the catalog
    entry exists, the structural gate passes, the file is syntactically valid, the
    commit's own diff is real, and one local headless walk produced a verdict that
    was read and reported honestly, gap and all.

Context: Sandbox writes only, target repo working tree only.
Amanah: this slice is part of the muddytires.ca QC-block backlog. Discharge with ihsan.

MODEL-ROUTING: TIER_CLOUD_CODE — qwen3-coder:480b or kimi-k2.7-code for fix authoring.

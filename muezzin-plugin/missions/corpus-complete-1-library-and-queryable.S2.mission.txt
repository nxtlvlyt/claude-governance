MISSION-ID: M-CORPUS.COMPLETE.1.S2
MISSION-CLASS: build-program (multi-stage; decompose under the Hajj mission-architecture — Architect splits, no hand-authored steps)
PARENT: M-CORPUS.COMPLETE.1  (sub-mission 2 of 5, auto-split by the size ceiling)
REQUIRES: predecessor M-CORPUS.COMPLETE.1.S1 DONE (tartib — sub-mission 2 runs after 1 of 5)

Niyyah: the factory's paid-for knowledge is useless while it is scattered and incomplete — six cards in six folders

Maqsad: this is part 2 of 5 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): a single, indexed, queryable knowledge base distilled from EVERY source in the P0-CORPUS census (the

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - STAGE 2 — Create the single library directory and the proof subdir, and copy ALL existing card-*/card-*.md files into missions/_knowledge-base/cards/. ONE WRITER for the cards dir (create + populate in one command); later steps only READ it. Gate: at least 6 cards land.
  - STAGE 2 — Author INDEX.md: one Markdown table row per source (name | vertical | card path | one-line 'what it teaches'), covering every card in _knowledge-base/cards/ and explicitly marking any census source with no card as a GAP row. Reads the copied cards (READ-ONLY).
  - STAGE 2 — Author PATTERNS.md: the cross-cutting synthesis (recurring components, voice moves, design-token habits, anti-patterns) that appear across MULTIPLE builds, each generalization citing the specific card(s) it draws from by filename. This is the distilled wisdom that answers 'what can X learn from our KB'. Reads the copied cards (READ-ONLY).

Done means: steps M-CORPUS.COMPLETE.1.S2.1..M-CORPUS.COMPLETE.1.S2.3 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — the census's 17 sources are all carded (or explicitly skip-verdicted), the library INDEX
resolves to every card, PATTERNS.md exists, and the AnythingLLM workspace answers the operator's example query with
a cited answer. Nothing claims done without that query receipt.

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.QC-FEATURE-CATALOG-FILL-2026-06-23
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: feat/qc-feature-catalog-2026-06-23
ALLOW-FILES:
  - docs/FEATURE-CATALOG-2026-06-23.md

Niyyah: complete the visual-e2e QC source of truth so the e2e-runner has every feature on muddytires.ca to walk. The operator's SOTA QC vision — visually inspect every feature, check work + look + clutter — only works if the catalog is the substrate-honest list. v0.1 captured the template + 3 bug cases. v1.0 needs full coverage.

Maqsad: docs/FEATURE-CATALOG-2026-06-23.md grows from v0.1 (template + 3 bug cases) to v1.0 (every interactive feature on muddytires.ca cataloged per template).

Context (substrate paths, read before authoring):
  - docs/FEATURE-CATALOG-2026-06-23.md — the v0.1 template + bug cases (do not modify the existing sections; APPEND to the TODO section)
  - map.html — script tag inventory (24 JS modules)
  - js/*.js — read each module's public surface (window.* exports, exported function names, stable selectors used)
  - HTML pages: about/admin/ambassadors/canada-safety/changelog/guides/index/landing/map/operators/partners/pledge/profile/quirky/saved/status/trip-edit/u

For EACH JS module + each HTML page, add a `### Feature: <name>` section per the template. Selectors must be substrate-verifiable (grep them in the source). Behavior gates must be mechanical (DOM state, localStorage, network). Visual gates use agy/Gemini multimodal verbiage.

Done means: docs/FEATURE-CATALOG-2026-06-23.md contains ≥30 `### Feature:` sections covering the JS modules + page-level features; every selector grep-verifies; substrate-honest about gaps (mark features whose selectors are unstable or whose AC is unclear with TODO inline rather than guessing); git commit + push to feat/qc-feature-catalog-2026-06-23; ls-remote verify.

Amanah: yours to reason the path. The chain reads each module + emits the section. Don't fabricate selectors — grep before writing. If a module's behavior is genuinely opaque from substrate alone, mark it `TODO: needs operator clarification`.

Constraints: per-file git add, --no-verify, NO LOCAL OLLAMA, Sonnet/agy-Sonnet for execution per phase-execution-architecture memory.

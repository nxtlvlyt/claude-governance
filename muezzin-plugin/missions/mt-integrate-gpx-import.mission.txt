MISSION mt-integrate-gpx-import
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - d1/migrate-gpx-routes-2026-06-23.sql
  - functions/api/route/import-gpx.js
REQUIRES: none

Niyyah: The feature "mt-feat-gpx-import-2026-06-23" (commit cbb07a5, sitting in the
source worktree C:\Users\marka\code\mt-feat-gpx-import-2026-06-23) has never been
integrated into this repo's HEAD. This mission's entire job is mechanical integration
— land the commit onto the current tree without losing either side's changes, and
prove the result is syntactically sound. Nothing this mission does constitutes a
judgment that the gpx-import feature's code is correct, safe, or behaves as designed
(e.g. that the regex-based GPX 1.0/1.1 parser handles every real-world track/route/
waypoint variant, that the distance-based decimation preserves route fidelity, that
the self-heal CREATE-TABLE-on-first-request logic is race-safe under concurrent
requests, or that the POI-along-route suggestion's haversine plus proximity/node-type
weighting returns sane results). That determination belongs entirely to this
mission's own witness plus verdict-panel run, which inspects the integrated result
after this mission produces it — not to the act of integration itself. Authoring or
executing this mission is not verification of the feature.

Maqsad: The commit "cbb07a5 feat(gpx-import): /api/route/import-gpx + user_routes /
user_route_pois D1 tables" is cherry-picked or merged from the source worktree onto
the current HEAD of C:\Users\marka\code\mt-integration-2026-06-22, with any conflicts
resolved by keeping BOTH sides — this repo's existing unrelated changes AND the
gpx-import feature's changes, never silently dropping either — every changed .js/.mjs
file passes `node --check`, the result is committed with a clear message, and the
target repo's working tree is left clean (git status shows no uncommitted changes to
the touched files).

Context (verified against live substrate this session, absolute paths, no prescribed
resolution beyond "keep both sides"):
- Source worktree: C:\Users\marka\code\mt-feat-gpx-import-2026-06-23, commit
  cbb07a5acaa70fc397b6500568e695d9a0bca1f5, parent ed6e33c — do not rewrite that
  worktree, only read/cherry-pick FROM it. (A prior description of this task named the
  source as C:\Users\marka\code\gpx-import; that path does not exist on disk —
  `git worktree list` this session shows the real worktree is
  C:\Users\marka\code\mt-feat-gpx-import-2026-06-23, matching this repo's established
  mt-feat-<slug> naming convention and carrying cbb07a5 at its tip. This mission uses
  the verified path.)
- Confirmed diff stat for cbb07a5 (`git show --stat`, this session): 2 files changed,
  660 insertions(+), 0 deletions(-) — both brand-new files, no existing file is
  modified: d1/migrate-gpx-routes-2026-06-23.sql (88 lines, creates the user_routes
  and user_route_pois tables) and functions/api/route/import-gpx.js (572 lines, a
  Cloudflare Pages Function implementing POST /api/route/import-gpx — multipart or
  raw application/gpx+xml upload with a 1 MB cap, a regex-based GPX parser, distance-
  based decimation, self-heal table creation on first request, and POI-along-route
  suggestion via a bbox query blended with per-point haversine distance and
  node-type weighting).
- Target repo current HEAD (this session, `git rev-parse HEAD` and
  `git branch --show-current` in C:\Users\marka\code\mt-integration-2026-06-22):
  28cbc722f56b57d2250f80297069b4c98a8d7120, on branch
  feat/qc-concern-sentry-error-capture-sentry-init-js-2026-06-25. This is NOT main
  (main sits at 5e19874307f1548c404bf0a42b90831c00da9539 this session, on a diverged
  line). Per instruction, land the feature onto whatever branch is actually checked
  out at fire time — no TARGET-BRANCH is declared on purpose.
- Verified this session via `git cat-file -e HEAD:<path>` (both fail with "does not
  exist") that neither d1/migrate-gpx-routes-2026-06-23.sql nor
  functions/api/route/import-gpx.js exists anywhere in the target repo's current
  HEAD, and confirmed via `git merge-base --is-ancestor cbb07a5 HEAD` (exit code 1,
  not an ancestor) that the feature commit is not already integrated. A clean,
  conflict-free cherry-pick is therefore the expected outcome; the conflict-
  resolution steps below exist for completeness and safety, not because a conflict
  was observed. If either filename has since been created independently in the
  target tree by unrelated work before this mission fires, resolve by keeping BOTH
  sets of content rather than overwriting one side.
- The source worktree and the target repo are worktrees of the same underlying git
  repository (confirmed via `git worktree list` and `git cat-file -t cbb07a5`
  returning "commit" directly inside the target repo) — commit cbb07a5 is already a
  reachable object in the target repo; no fetch, remote-add, or patch file is needed.
- The target repo's current working tree already carries several pre-existing
  untracked files unrelated to this feature (api-index-response.json,
  docs/e2e-report-2026-07-01.json, e2e-shots/, feature-catalog-entry.txt,
  scripts/e2e-runner.mjs — confirmed via `git status` this session). This mission's
  ALLOW-FILES is scoped to exactly the two feature files and must not stage, modify,
  delete, or claim credit for cleaning up anything else.
- This diff touches no HTML file (confirmed: two new files only, one .sql migration
  and one .js Pages Function) — VISUAL-QC-REQUIRED is deliberately omitted from this
  mission; no render-witness step applies.
- Nothing about this mission's authorship constitutes verification that the feature
  code is correct — that is exactly what this mission's own witness and
  verdict-panel run, downstream of the integration, will determine.

Steps:
  1. In the source worktree C:\Users\marka\code\mt-feat-gpx-import-2026-06-23, confirm
     commit cbb07a5 exists and inspect its patch (e.g. `git show cbb07a5` or
     `git format-patch -1 cbb07a5`) without modifying that worktree. [command]
     capture-patch
  2. In the target repo C:\Users\marka\code\mt-integration-2026-06-22, on its current
     HEAD (do not check out any other branch first), run
     `git cherry-pick cbb07a5acaa70fc397b6500568e695d9a0bca1f5` — the commit object is
     already reachable in this repo's store, so no fetch or remote-add step is
     needed. [command] integrate-commit
  3. If the cherry-pick reports no conflicts, skip to step 5. If it reports conflicts
     on d1/migrate-gpx-routes-2026-06-23.sql, functions/api/route/import-gpx.js, or
     any other path, resolve each conflicted hunk by hand so the resolved content
     contains BOTH the target's pre-existing content AND the gpx-import feature's
     content — never `--ours` / `--theirs` and never a resolution that deletes either
     side's lines. Only the two ALLOW-FILES entries may be touched to complete a
     resolution. [edit] resolve-conflicts
  4. If a manual resolution was needed, stage only the two feature files by name
     (`git add d1/migrate-gpx-routes-2026-06-23.sql functions/api/route/import-gpx.js`
     — never `git add -A` or `git add .`, which would sweep in the pre-existing
     unrelated untracked files already sitting in the target's working tree), then
     run `git cherry-pick --continue` to finish the commit. [command]
     continue-cherry-pick-scoped
  5. Run `node --check functions/api/route/import-gpx.js` and confirm it exits 0; run
     `node --check` on every other .js/.mjs file this integration touched or
     conflict-resolved. [command] node-check-all
  6. Run `git status --porcelain` and confirm neither ALLOW-FILES entry appears in
     the output (both fully committed) and that no file outside ALLOW-FILES was
     staged, modified, or committed by this mission; confirm the pre-existing
     untracked litter noted in Context above is left exactly as found. [command]
     verify-scoped-clean-tree

Done means:
  - `git log` on the target repo's HEAD in C:\Users\marka\code\mt-integration-2026-06-22
    shows a commit containing the integrated gpx-import feature, with a commit
    message that names "mt-feat-gpx-import-2026-06-23" and/or "cbb07a5".
  - d1/migrate-gpx-routes-2026-06-23.sql exists at the target repo's HEAD and its
    content, read directly (not assumed), contains both a `user_routes` table
    definition and a `user_route_pois` table definition.
  - functions/api/route/import-gpx.js exists at the target repo's HEAD with
    non-trivial content matching the feature described in Context above (multipart/
    raw-XML GPX parsing, decimation, and POI-suggestion logic), confirmed by reading
    the file, not assumed.
  - `node --check functions/api/route/import-gpx.js` exits 0, and `node --check`
    exits 0 on every other .js/.mjs file this integration touched or
    conflict-resolved.
  - `git status --porcelain` in the target repo shows no modified or staged entries
    at the end of the mission — pre-existing untracked litter unrelated to this
    feature (documented in Context above) is not this mission's to clean up and is
    left untouched either way.
  - Nothing in this mission's own receipts is treated as proof that the gpx-import
    feature's code is correct, safe, or behaves as designed — that judgment is
    deferred entirely to this mission's own downstream witness and verdict-panel run
    against the integrated result. Authoring and executing this mission verifies
    mechanical integration only (the commit landed, both sides' content survived,
    the syntax check passes, the tree is scoped-clean) — it is not a review of the
    GPX parser, the decimation algorithm, the self-heal migration logic, or the
    POI-suggestion scoring.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

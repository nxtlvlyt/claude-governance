MISSION-ID: engine-truth-of-record.S1.S2.S1
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\.claude\muezzin-plugin
LINEAGE-NOTE 2026-07-04T03:2x: split from engine-truth-of-record.S1.S2 after its step 2 (C2,
combined queuedDepsHold+pickPromotion stamp verification in ONE edit) truncated identically
on all three escalation tiers (default-local, sonnet, opus) -- same class of failure as the
original S1's C1+C2 combination, just one file smaller. muezzin-daemon.mjs is 1755 lines;
generating queuedDepsHold's AND pickPromotion's stamp-verification logic plus three selftest
fixtures in one authoring pass keeps exceeding what the executor can complete without cutting
off mid-function. This mission carries ONLY the queuedDepsHold half of C2 -- a smaller,
single-function change on the same file, split across missions (not steps) per the
one-writer-per-file invariant that already forced the S1/S1.S1/S1.S2 split.
ALLOW-FILES:
  - muezzin-daemon.mjs
UNLINKED-OK: engine module edits — no user-facing page
REQUIRES: engine-truth-of-record.S1.S1 (tartib — this sub-mission may not start until engine-truth-of-record.S1.S1's receipt exists)
STEPS: 4

Maqsad: UNIT C of GAP-CLOSURE-PLAYBOOK.md, C2 part A of B. Evidence in
missions/_logs/GAP-HUNT-2026-07-03.json (read before editing).
"RESOLVED-LANDED stamp is a pure-trust input, half A (queuedDepsHold)": before
queuedDepsHold honors a # RESOLVED comment, it must verify the claim mechanically via
missionLandedState (imported from ./conduct-cycle.mjs — it is the ONE identity core; do NOT
reimplement): the named mission's ALLOW-FILES present at HEAD, or a named sha's patch in
tree. On verification failure: log a STAMP-DISPUTED event (evt) and treat the stamp as
ABSENT (the mission stays live) — never retire on a disputed stamp, never crash
(missionLandedState returning null = undeterminable = honor the stamp as today, fail-open,
because absence of ALLOW-FILES parsing is not evidence of a false stamp). pickPromotion's
matching change is a SEPARATE mission (engine-truth-of-record.S1.S2.S2) — do not touch
pickPromotion in this mission.

niyyah: A record system that trusts unverified stamps cannot be the substrate a smaller
conductor inherits. Truth of record first; every acceptance below is a selftest, not an
opinion.

CITATION-GUARD CONSTRAINT (carried forward — the SAME fabricated-citation class hit this
file family repeatedly across attempts of the parent mission before this split). Escape
hatches are WITHDRAWN. Do not write ANY illustrative example of a retro filename, stem name,
or timestamp-derived name — not in a comment, not as a selftest fixture literal, not as a
worked example — in any form, backtick-quoted or not, template-literal or not. If explaining
what a name looks like is unavoidable, describe the SHAPE abstractly (a mission stem, a
hyphen, an ISO-8601 timestamp with colons replaced by hyphens, the literal extension)
without ever typing one concrete instance end to end. Selftest fixtures needing a retro-like
string must build it via runtime string concatenation in the test code (parts assembled
from variables) rather than a single literal typed out whole.

Steps:
  1. Baseline: record branch + HEAD; print PRESENT/ABSENT for the marker mt-c2a-queueddeps in muezzin-daemon.mjs (idempotence anchor; if PRESENT, edit step is verify-only). Never exit 1 on existence. [command]
  2. C2a edit of muezzin-daemon.mjs ONLY, queuedDepsHold function alone: import missionLandedState from ./conduct-cycle.mjs at the top of the file if not already imported; inside queuedDepsHold, before honoring a # RESOLVED comment, call missionLandedState against the named mission and tag the call site with the literal comment mt-c2a-queueddeps; on a verdict that disputes the stamp, log evt('STAMP-DISPUTED: ...') and treat the mission as still live (never retire); on a null/undeterminable verdict, fail-open and honor the stamp as before. Add two selftest fixtures to the daemon selftest block: (a) a disputed stamp keeps the mission live (does not retire it), (b) a null missionLandedState verdict still honors the stamp (fail-open, unchanged behavior). Do not touch pickPromotion in this step. [edit] muezzin-daemon.mjs
  3. Verify: Select-String receipt for mt-c2a-queueddeps; node --check muezzin-daemon.mjs. [verify]
  4. Commit muezzin-daemon.mjs by explicit name naming this mission + the playbook UNIT C + the C2 hunt item (queuedDepsHold half); idempotent no-op if already committed (ALREADY_COMMITTED + exit 0 when clean and HEAD carries the marker). Then run node muezzin-daemon.mjs --selftest as the outcome check — the step receipt MUST show the suite ending ALL PASS, including both new fixture names from this mission. A missing fixture name or any FAIL line fails this step. [command]

Done means: muezzin-daemon.mjs committed at HEAD carrying mt-c2a-queueddeps; its selftest
suite ALL PASS with both new fixtures visible in the step-4 receipt; queuedDepsHold now
verifies RESOLVED stamps mechanically instead of trusting them. pickPromotion's matching
change continues in engine-truth-of-record.S1.S2.S2, and C3 (conduct-cycle.mjs) alongside it.

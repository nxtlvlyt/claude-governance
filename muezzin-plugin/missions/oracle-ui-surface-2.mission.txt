MISSION-CLASS: code-repo (targeted edit to ONE file on a CLEAN non-live worktree + positive-assertion verify; NO deploy in this mission; live muddytires.ca / prod Pages alias / DNS all UNTOUCHED)
MISSION-ID: M-MUDDYTIRES.ORACLE-UI-SURFACE.2
REPO-ROOT: E:\AI_Storage\muddytires-frontend-wt
ALLOW-FILES: index.html

# WHY (substrate, verified live 2026-06-17): oracle-ui-contract-1 made the LIVE Oracle worker
# (https://muddytires-oracle-d1-test.regulativity.workers.dev) serve the answers[] contract — conductor-verified: /?lat=45.8&lng=-78.4
# returns ok:true + answers[](5 sourced: park / ON camping-rule w/ ontario.ca source_url / fire source:null=TAQWA / water / dump).
# The prod index.html ALREADY has a full Oracle UI but it's been DEAD since the PHP->Pages migration: 3 fetch sites all call the
# plaintext-served `oracle.php` — nnOracle (the sourced-answer panel), nnFillOracle (the inline services line), nnAsk (the free-text
# "Ask the Oracle" box). This mission repoints the 2 DATA calls to the live worker so users finally see the sourced Crown-land
# legality answer. (Preview-deploy + production cutover are a SEPARATE reviewed follow-on — NOT this mission.)

Niyyah: surface the already-built, already-live Oracle moat to real users by reconnecting the prod map's Oracle UI to the LIVE
worker it should have pointed at since the migration — a small exact edit, nothing deployed, the live site untouched.

Maqsad: ONE deliverable — index.html's Oracle DATA fetches hit the LIVE worker instead of the dead oracle.php, committed on the
worktree branch. Specifically:
- nnOracle (the sourced-answer panel) and nnFillOracle (the inline services line): change their fetch URL from
  `oracle.php?lat=...&lng=...` to `https://muddytires-oracle-d1-test.regulativity.workers.dev/?lat=...&lng=...`. The worker returns
  { ok, answers:[{q,verdict,detail,source}], ... } — the EXACT shape these two functions already parse (d.ok + d.answers). No
  parsing change needed beyond the URL.
- nnAsk (the "Ask the Oracle" free-text box) calls oracle.php?...&ask= for an LLM {answer} the worker does NOT provide (the LLM
  layer is out of scope per d1/RECEIPTS-ORACLE.md). DEGRADE it gracefully: it must NOT fire a broken oracle.php request or hang —
  replace its action with an honest inline message like "Free-text Q&A isn't live yet — the sourced answers above are live." Do NOT
  invent or call an LLM endpoint.
Done means (positive-assertion, EXIT NON-ZERO on absence):
1. index.html's 2 DATA Oracle calls fetch the worker URL (assert the worker hostname appears >= 2x in index.html).
2. NO oracle.php DATA fetch remains for the panel/services (assert the only acceptable residual, if any, is inside the degraded
   nnAsk path — better: zero `oracle.php` fetch( calls remain).
3. The edit did not corrupt the file: index.html still parses as HTML (opening <html> + closing </html> present; byte size within
   ~2KB of the original 360298) and the map-init + popup code are intact (assert `L.map(` and `nnFillOracle` still present).
4. Committed on branch oracle-frontend-swap. NO deploy performed; muddytires.ca / prod Pages alias / DNS untouched.

Constraints: ONE file (index.html) via TARGETED search/replace (exact-string URL swap — the executor-edit primitive's best case;
do NOT rewrite the file wholesale); ADDITIVE behavior (no feature removed — the panel/services light up, the ask box degrades
honestly); NO deploy in this mission (preview + cutover are a separately-reviewed follow-on — the operator approves the live cutover
of his #1 asset); commit index.html --no-verify; keep the plan SMALL (locate the 3 sites + the 2 swaps + the 1 graceful-degrade +
verify + commit) — do NOT split, do NOT expand.

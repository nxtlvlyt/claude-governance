MISSION-ID: mt-integrate-dynamic-favicon-fire-ban.S1
MISSION-CLASS: code-repo
PARENT: mt-integrate-dynamic-favicon-fire-ban
TARTIB-INDEX: 1 of 2
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - icon-fire-ban.svg
  - index.html
  - js/dynamic-favicon.js
  - map.html
REQUIRES: none (first in tartib order)
STEPS: 9

PARENT MAQSAD: Land commit d48f959 ("feat(dynamic-favicon): peripheral fire-ban indicator on the

Maqsad: sub-mission 1 of 2 — Baseline capture (idempotent): record current branch and HEAD sha and assert the favicon deliverables are not yet on disk, through cherry-picking d48f959 with a deterministic anchored resolution of the known map.html conflict, syntax/artifact witnesses for all four ALLOW-FILES, and a scoped commit tail. The browser render witness is sibling S2's charter.

STEP-SHAPE DISCIPLINE (conductor amendment 2026-07-17, third fire of this stem). Receipts: result.json stoppedAt=3 reason=engine-exec "CONFLICT (content): Merge conflict in map.html ... could not apply d48f959", recurringError=true priorOccurrences=2; FAILED-marked 2026-07-09T19:14Z and 2026-07-11T23:49Z — the 07-11 refire failed IDENTICALLY even with the keep-BOTH-sides amendment because that resolution lived as prose inside step 3's single [command]; the engine runs one literal command per step, the pick exits nonzero at the conflict, and the prose never executes. This amendment applies the receipted region-heatmap S1 recipe verbatim (that stem carried the SAME map.html giant-line conflict and went DONE 2026-07-17, commit 5460395 now an ancestor of HEAD): (a) every executing step carries its LITERAL "Run exactly:" command body — no prose the seat improvises from; (b) step 3 TOLERATES the conflict exit (the assertion is the artifacts), and step 4 resolves map.html with the anchored fail-closed resolver at C:\Users\marka\.claude\muezzin-plugin\missions\_logs\dynamic-favicon-conflict-resolve.mjs then FINALIZES the pick (the near-me attempt-1 lesson: resolving the FILE without cherry-pick --continue leaves CHERRY_PICK_HEAD alive and git refuses any pathspec commit with "fatal: cannot do a partial commit during a cherry-pick"); (c) the visual-QC header is carried ONLY by sibling S2 (already flagged; its render steps 11-12 assert #mt-favicon in the rendered DOM and its REQUIRES token names this mission), so this git-mechanical child drops the flag exactly as the region-heatmap/near-me/spot-share children did (autosplit tag-inheritance class, receipted 4x). Conflict forecast receipted 2026-07-17 at HEAD 0449664: git merge-tree --write-tree --merge-base=d48f959~1 HEAD d48f959 → CONFLICT (content) in map.html ONLY, exactly ONE region (reconstructed via git merge-file on stage blobs 81ef8fcd/2f318083/3ddcbb24); index.html auto-merges clean; both new files (js/dynamic-favicon.js, icon-fire-ban.svg) land clean. Resolver anchors verified against CURRENT map.html AND the reconstructed ours side of the conflict: the bar div anchor occurs exactly once (line 89) and the site-search script-tag anchor occurs exactly once; mt-favicon/dynamic-favicon occur zero times. The planner must use these commands, not improvise.

Steps:
  1. Baseline, stdout only, IDEMPOTENT. Run exactly: git rev-parse --abbrev-ref HEAD; git rev-parse HEAD; if ((Test-Path js/dynamic-favicon.js) -or (Test-Path icon-fire-ban.svg)) { Write-Output ALREADY_PRESENT } else { Write-Output CLEAN_BASELINE } [command]
  2. Preconditions + ancestry, IDEMPOTENT. Run exactly: if (Test-Path (git rev-parse --git-path CHERRY_PICK_HEAD)) { exit 1 }; git merge-base --is-ancestor d48f959 HEAD; if ($LASTEXITCODE -eq 0) { Write-Output ALREADY_INTEGRATED } else { Write-Output NOT_YET_INTEGRATED }; exit 0 [command]
  3. Land d48f959; a conflict exit is TOLERATED — the assertion is the artifacts. Run exactly: git merge-base --is-ancestor d48f959 HEAD; if ($LASTEXITCODE -eq 0) { Write-Output SKIP_ALREADY_INTEGRATED; exit 0 }; git cherry-pick -x d48f959; if (Test-Path (git rev-parse --git-path CHERRY_PICK_HEAD)) { git cherry-pick --continue --no-edit }; if (-not ((Test-Path js/dynamic-favicon.js) -and (Test-Path icon-fire-ban.svg))) { exit 1 } [command] js/dynamic-favicon.js
  4. Resolve the map.html conflict deterministically AND FINALIZE THE PICK (idempotent — no-op if step 3 left no conflict). Run exactly: if (Select-String -Path map.html -Pattern '^<{7}' -Quiet) { node "C:\Users\marka\.claude\muezzin-plugin\missions\_logs\dynamic-favicon-conflict-resolve.mjs" map.html; if ($LASTEXITCODE -ne 0) { exit 1 } }; if (Test-Path (git rev-parse --git-path CHERRY_PICK_HEAD)) { git add -- map.html; git -c core.editor=true cherry-pick --continue; if ($LASTEXITCODE -ne 0) { exit 1 } }; Write-Output PICK-FINALIZED-OR-CLEAN [command] map.html
     validation_command: if (Select-String -Path map.html -Pattern '^<{7}|^={7}|^>{7}' -Quiet) { exit 1 }; if (-not (Select-String -Path map.html -Pattern 'mt-favicon' -Quiet)) { exit 1 }; if (-not (Select-String -Path map.html -Pattern 'dynamic-favicon.js' -Quiet)) { exit 1 }; if (Test-Path (git rev-parse --git-path CHERRY_PICK_HEAD)) { exit 1 }
  5. Verify the newly integrated favicon module is syntactically valid JavaScript; the literal invocation on this line satisfies RULE 11 while the tag stays [verify] (the poi-services one-writer lesson). Run exactly: node --check js/dynamic-favicon.js [verify] js/dynamic-favicon.js
  6. Artifact witness for the static fallback icon. Run exactly: if (-not (Test-Path icon-fire-ban.svg)) { exit 1 }; if (Select-String -Path icon-fire-ban.svg -Pattern '^<{7}|^={7}|^>{7}' -Quiet) { exit 1 }; if (-not (Select-String -Path icon-fire-ban.svg -Pattern '<svg' -Quiet)) { exit 1 }; Write-Output SVG_OK [verify] icon-fire-ban.svg
  7. Artifact witness for the index.html wiring (this file auto-merged clean — receipted in both the 07-11 run output and the 2026-07-17 merge-tree forecast). Run exactly: if (Select-String -Path index.html -Pattern '^<{7}|^={7}|^>{7}' -Quiet) { exit 1 }; if (-not (Select-String -Path index.html -Pattern 'mt-favicon' -Quiet)) { exit 1 }; if (-not (Select-String -Path index.html -Pattern 'dynamic-favicon.js' -Quiet)) { exit 1 }; Write-Output INDEX_WIRED [verify] index.html
  8. Artifact witness for the map.html wiring (current pre-integration script-tag count is 50; the resolver inserts exactly one). Run exactly: if (Select-String -Path map.html -Pattern '^<{7}|^={7}|^>{7}' -Quiet) { exit 1 }; if (-not (Select-String -Path map.html -Pattern 'mt-favicon' -Quiet)) { exit 1 }; if (-not (Select-String -Path map.html -Pattern 'dynamic-favicon.js' -Quiet)) { exit 1 }; if (-not ((Select-String -Path map.html -Pattern '<script' -AllMatches).Matches.Count -ge 51)) { exit 1 }; Write-Output MAP_WIRED [verify] map.html
  9. Repo-state bookkeeping check, NO file authored by this step (step 4's finalized pick already committed the deliverables in the conflict path; this step is the idempotent no-op-or-commit tail plus scoped cleanliness — a single engine-executed command whose file mentions are READS of git status, not edit targets). Run exactly: git add -- icon-fire-ban.svg index.html js/dynamic-favicon.js map.html; git diff --cached --quiet -- icon-fire-ban.svg index.html js/dynamic-favicon.js map.html; if ($LASTEXITCODE -ne 0) { git commit -m "feat(dynamic-favicon): peripheral fire-ban favicon indicator (integrate d48f959)" -- icon-fire-ban.svg index.html js/dynamic-favicon.js map.html; if ($LASTEXITCODE -ne 0) { exit 1 } }; if ((git status --porcelain -- icon-fire-ban.svg index.html js/dynamic-favicon.js map.html).Length -gt 0) { exit 1 }; Write-Output SCOPE_CLEAN [verify] ; context_dependencies: icon-fire-ban.svg, index.html, js/dynamic-favicon.js, map.html

Done means: js/dynamic-favicon.js, icon-fire-ban.svg, index.html, map.html exist/are updated as specified

MISSION-CLASS: engine-bootstrap (conductor governance gate — FRESH instance, witnessed, niyyah)
MISSION-ID: M-ENGINE.CONDUCTOR-SELF-WITNESS.1
TARGET: the conductor's authoring path — a gate (hook or wrapper) that routes conductor-authored artifacts (mission specs, root-cause diagnoses) through a witness (laguna) before they are acted on / fired, the way the daemon's SEATS are auto-witnessed.
REQUIRES: FRESH instance; witnessed; design must respect GR10 (laguna is LOCAL — the gate must yield/queue when the daemon's GPU is busy, never force concurrent inference).
# Surfaced 2026-06-16 05:06 (operator: "have you been checking all your work outside the 3phase missions with laguna and
# guardian?"). HONEST CONDUCTOR ANSWER (verified): the agent-built ENGINE CODE was laguna-witnessed (verdicts quoted),
# but the conductor's OWN hand-authored output — mission specs, diagnoses, AUTORUN edits, reports — went UN-witnessed.
# Evidence the gap is real: MIQAT caught 3 of the conductor's mission-text bugs at FIRE time tonight (no-done-means,
# no-ALLOW-FILES, REPO-ROOT-with-comment) — format bugs only; nothing checked the conductor's REASONING. The daemon's
# seats auto-witness; the conductor has no such gate. This is the conductor-not-exempt principle (feedback_conductor_
# not_exempt) made structural: hold the conductor to deeds-and-witness like a seat.

Niyyah: the conductor must not hold the seats to witness while running its own diagnoses and specs on confidence-plus-
substrate-reads alone. A model passing judgment (the conductor IS one) must be witnessed — especially the conductor,
whose un-witnessed errors propagate into every mission it constructs.

Maqsad: conductor-authored artifacts that drive work (a new mission spec; a root-cause diagnosis that re-routes the
queue) get a WITNESS pass (laguna, GPU-permitting) before they are fired/acted on — surfacing flaws in the REASONING
that MIQAT's format lint cannot, the same independent-witness discipline the seats already have.

# OPERATOR PRINCIPLE 2026-06-16 06:24 ("so are you saying they need to check before and after?"): YES — WITNESS BOTH
# BEFORE *and* AFTER. The 2026-06-16 autosplit-spam bug proved it: the autosplit was checked BEFORE (laguna reviewed the
# diff + a selftest with FAKE data — looked sound, approved) but never AFTER (the actual GENERATED sub-missions run
# through the REAL MIQAT lint). The bug lived entirely in that gap and only surfaced live (12 phone-spam refusals). The
# fix (mission_split 23f9c4e) added the AFTER check: the generator runs its OWN output through the real lint before
# writing. So this gate must do the same — TWO passes:
#   BEFORE: witness the artifact's DESIGN/REASONING (the mission spec, the diff) — the v1 build does this on mission text.
#   AFTER: witness the artifact's actual RESULT against REALITY, not a fake — for a mission, did its OUTPUT satisfy its
#          own "Done means"? for an engine change, does its real output pass the real gate (run the real lint/the real
#          selftest, never injected fakes)? The v1 gate has NO after-pass — that hole is what shipped the spam bug.
# RULE for all engine work + missions: a BEFORE-only check confirms "the approach is sound"; only the AFTER check
# confirms "the thing it actually produces is correct." Tests must hit reality (the real lint, the real gate), never fakes.

# OPERATOR RULING 2026-06-16 05:55 (CORRECTS the conductor's earlier mis-framing): "just because Laguna or guardian
# flag something doesn't mean they are right — they are there to make you SLOW DOWN. they should never be skipped."
# The witnesses are NOT oracles whose verdict must be obeyed; they are a MANDATORY SLOWDOWN / forced second look.
# This splits the design into TWO things the conductor previously conflated:
#   A. THE PASS IS MANDATORY (non-skippable): BOTH laguna AND guardian ALWAYS RUN before out-of-chain work is acted on.
#      The current v1 (2ee53bf) VIOLATES this — it (i) YIELDS-AND-SKIPS on GPU-busy (must WAIT/queue until the GPU frees,
#      never skip the pass), (ii) passes on ONE-of-two (lRan||gRan — must REQUIRE BOTH to actually run), (iii) only
#      covers daemon missions (must also cover conductor diagnoses + agent-built code). Fix all three so the pass cannot
#      be skipped. Run-not-yield needs the small co-resident ~8B reviewer (so laguna+guardian don't oversubscribe).
#   B. THE VERDICT IS NOT BINDING (do NOT auto-block): a flag does not halt the work — it forces ENGAGEMENT. Proceeding
#      past a flag requires the actor to SLOW DOWN, read it, and ARTICULATE the reasoning for proceeding (the surrender-
#      check pattern — override-with-reasoning is allowed; SILENT SKIP is forbidden). Do NOT promote blocking:false ->
#      auto-halt; the witnesses can be wrong (laguna FLAGGED a VALID conductor diagnosis), and obeying a wrong flag would
#      block correct work — which is exactly the failure the operator is warning against. The teeth are on the SKIP
#      (mandatory pass + mandatory articulation), not on the verdict.
# Net target: you cannot SKIP the witnesses (the pass always runs, both, waiting for GPU), and you cannot SILENTLY
# proceed past a flag (must articulate) — but you are free to DISAGREE with a flag, with reasoning. Mandatory slowdown,
# not mandatory obedience.

REQUIRED (design against the existing witness machinery — getFaith/witness in seat_dispatch + the MIQAT lint as the
format layer):
1. DEFINE the trigger: which conductor artifacts witness (new .mission.txt before it fires; a diagnosis that BLOCKS or
   re-routes a mission). NOT every conductor message — only artifacts that drive autonomous work.
2. THE WITNESS PASS = BOTH laguna AND guardian (OPERATOR STANDING RULING 2026-06-16 05:17: "any work outside the chain
   needs to be witnessed by both Laguna AND guardian" — this is a STANDING RULING, belongs in ~/.claude/rules/operator-
   rulings.md; recorded here until ported). The two are COMPLEMENTARY, not redundant:
     - laguna (structural): "is the REASONING sound, correctly scoped, faithful to its cited receipts?" — catches logic
       flaws, gaps, overlaps (it caught the state-cleanup + A/B-ownership gaps tonight).
     - guardian (groundedness): "is every factual CLAIM supported by the cited CONTEXT?" — catches fabricated/unsupported
       claims (invented receipts, mis-cited line numbers, claims with no source).
   SCOPE = ALL WORK OUTSIDE THE DAEMON CHAIN, not just conductor mission-text: the conductor's diagnoses + specs AND the
   agent-built engine fixes (which tonight got laguna but NOT guardian — that gap is exactly what this ruling closes).
   NON-BLOCKING to start (flag into a log), earn the right to block later (mirror guardian's non-blocking->repair-trigger
   path; pairs with M-ENGINE.GUARDIAN.1).
   GPU NOTE (from the VRAM reality in step 3): laguna (33B/22GB) + guardian (8B/7GB) do NOT co-reside on the 24GB GPU, so
   the two-witness pass runs them SERIALLY (laguna, unload, guardian) OR substitutes a SMALL ~8B structural reviewer for
   laguna so BOTH witnesses co-reside and run concurrently. In anthropic-heavy mode the daemon's seats are on Claude
   (cloud), so the local GPU is mostly free for the witness pair — schedule the pair into that headroom.
3. GR10 — REFINED BY VRAM REALITY (operator 2026-06-16 05:12 "2 guardians should fit on the GPU also" — VERIFIED:
   RTX 4090 = 24GB; granite-guardian:8b ≈ 6.9GB, so TWO 8B witnesses co-reside in ~14GB with room; laguna-xs.2 (33B,
   21.7GB VRAM) does NOT — when the conductor dispatched laguna tonight it EVICTED the daemon's guardian off the GPU).
   THE FIX: the conductor-self-witness must use a SMALL ~8B guardian-class model (co-resides with the daemon's per-step
   guardian → runs CONCURRENTLY, no yield), NOT the 33B laguna. GR10's blunt "never two models" is too coarse: the real
   constraint is "do not OVERSUBSCRIBE the 24GB" — two small models that fit may run together; a 33B + anything may not.
   The gate checks /api/ps for FREE VRAM (size_vram headroom), not merely "is anything resident", and only yields when a
   dispatch would oversubscribe. CAVEAT (honest, do NOT skip): VRAM-fit is necessary but not sufficient — ollama's
   scheduler has DEADLOCKED on concurrent loads historically (keep_alive:0 / stacked stops, the serial-inference
   incidents). So before relying on concurrency, run a CONTROLLED STABILITY TEST (two 8B models inferring concurrently,
   N rounds, no deadlock) and only then relax GR10 from "serial" to "VRAM-bounded concurrent". Until that test passes,
   keep yielding. This refinement is a BROADER GR10 update (affects all dispatch), surfaced here because the self-witness
   is its first beneficiary.
4. Receipt: a conductor artifact carries a witness note (laguna verdict or "witness-queued: GPU busy") before it fires.

Amanah: this closes the conductor-not-exempt hole. Witness the gate itself (laguna). Keep it NON-BLOCKING first +
GR10-safe. PAIRS WITH the MIQAT lint (format) — this adds the REASONING layer above it. NOTE: also covers a sibling gap
the operator's 3 mission-text bugs exposed — a mission-CONSTRUCTION template/preview so the conductor stops emitting
format-invalid missions (consider folding a construction-lint-preview into this or a sibling mission).

Constraints: FRESH instance; witnessed; GR10-safe (yield when GPU busy); non-blocking first; commit --no-verify; NAS
untouched. This is the 4th engine root-fix joining autosplit + replan-isolation + queue-flow — together: the engine
decomposes, contains failures, queues, AND witnesses its own conductor.

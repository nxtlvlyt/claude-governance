MISSION-ID: M-MUDDYTIRES.DB-MIGRATE-POIS-1.S3
MISSION-CLASS: code-repo
REPO-ROOT: E:\AI_Storage\muddytires-d1-wt
ALLOW-FILES:
  - d1/poi-migration-plan.md
  - d1/pois-load.sql
PARENT: M-MUDDYTIRES.DB-MIGRATE-POIS-1  (sub-mission 3 of 3, auto-split by the size ceiling)
REQUIRES: predecessor M-MUDDYTIRES.DB-MIGRATE-POIS-1.S2 DONE (tartib — sub-mission 3 runs after 2 of 3)

Niyyah: the database that held every place the community gathered still lives on the NAS — a 1.4GB MariaDB the static Cloudflare site

Maqsad: this is part 3 of 3 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): produce the concrete migration that moves the POI data off the NAS into Cloudflare D1 `pois`, so the live map's markers return.

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - Commit ONLY the two ALLOW-FILES by explicit per-file name (NEVER git add . / -A — pre-existing untracked d1/*.sql must not be swept) with --no-verify. NOT loaded into live D1. Exit 1 if the commit fails.
  - Receipt witness: HEAD commit actually contains both ALLOW-FILES. Exit 1 if either is missing from the commit's changed-file list.

Done means: steps M-MUDDYTIRES.DB-MIGRATE-POIS-1.S3.1..M-MUDDYTIRES.DB-MIGRATE-POIS-1.S3.2 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — (acceptance contract): d1/poi-migration-plan.md names the POI gap (D1 pois count vs NAS source count) + the exact operator-
gated load+deploy commands; d1/pois-load.sql is a valid SQLite load for the POI rows (or the plan states precisely why extraction is
blocked and what's needed). Both committed via explicit `git add` of only those two files, --no-verify; NOT loaded into live D1. A

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
MISSION-ID: MT-INTEGRATE-WIKIPEDIA-LINK-2026-06-23.S1.S2
MISSION-CLASS: code-repo
PARENT: MT-INTEGRATE-WIKIPEDIA-LINK-2026-06-23.S1
TARTIB-INDEX: 2 of 2
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - functions/api/wikipedia-link.js
  - js/poi-wikipedia.js
  - map.html
VISUAL-QC-REQUIRED
REQUIRES: missions/mt-integrate-wikipedia-link-2026-06-23.S1.S1.mission.txt (tartib SATISFIED: S1.S1 DONE 2026-07-17T21:40Z — feature files committed)
STEPS: 1

PARENT MAQSAD: sub-mission 1 of 2 — Baseline check (idempotent): print current branch and HEAD of the target repo, and report ALREADY_PRESENT if either new feature file already exists on disk, else CLEAN_BASELINE. Pure diagnostic, no implementation file authored. through Stage and commit the three already-written ALLOW-FILES with a message citing mt-feat-wikipedia-link-2026-06-23 and cf373c9; idempotent no-op (ALREADY_COMMITTED) if a commit citing cf373c9 already exists in recent history.

Maqsad: sub-mission 2 of 2 — OUTCOME CHECK (headless-render witness): write scratch-qc-wiki-chip.mjs (Playwright ESM script), navigate directly to the target repo's map.html via a file:// URL, poll until window.mtWikipediaChip is defined, call it with a synthetic match object, and assert the returned HTML contains the literal substring class="mt-wiki-chip". Exits non-zero with a named reason on any failure. Cleans up the scratch file after running. through OUTCOME CHECK (headless-render witness): write scratch-qc-wiki-chip.mjs (Playwright ESM script), navigate directly to the target repo's map.html via a file:// URL, poll until window.mtWikipediaChip is defined, call it with a synthetic match object, and assert the returned HTML contains the literal substring class="mt-wiki-chip". Exits non-zero with a named reason on any failure. Cleans up the scratch file after running.

Steps:
  9. OUTCOME CHECK (literal payload pinned 2026-07-17 after MIQAT improvise-bait refusal; preview-deploy render instead of file:// — file URLs break the page's fetch-dependent boot; this deploy+scratch shape has five green receipts this session). Run exactly: $dirty = @(git status --porcelain -uno -- functions/api/wikipedia-link.js js/poi-wikipedia.js map.html); if ($dirty.Count -gt 0) { git add -- functions/api/wikipedia-link.js js/poi-wikipedia.js map.html; git commit -m "feat(wikipedia-link): S2 witness-pass commit pairing (mt-integrate-wikipedia-link.S1.S2)" -- functions/api/wikipedia-link.js js/poi-wikipedia.js map.html; if ($LASTEXITCODE -ne 0) { Write-Output COMMIT-FAIL; exit 1 } } else { Write-Output ALLOW-FILES-ALREADY-COMMITTED }; $out = npx wrangler pages deploy . --project-name=muddytires --branch=preview 2>&1 | Out-String; $m = [regex]::Match($out, 'https://[a-z0-9-]+\.muddytires\.pages\.dev'); if (-not $m.Success) { Write-Output NO_PREVIEW_BASE_URL; exit 1 }; $url = $m.Value; Write-Output "DEPLOYED: $url"; $ok = $false; for ($i=0; $i -lt 10; $i++) { try { $r = Invoke-WebRequest -Uri "$url/map.html" -UseBasicParsing -TimeoutSec 10; if ($r.StatusCode -eq 200) { $ok = $true; break } } catch {}; Start-Sleep -Seconds 5 }; if (-not $ok) { Write-Output POLL-FAILED; exit 1 }; $js = 'import { chromium } from "playwright"; const b = await chromium.launch(); const ctx = await b.newContext(); const p = await ctx.newPage(); const errs = []; p.on("pageerror", (e) => errs.push(String(e))); await p.goto(process.argv[2] + "/map.html", { waitUntil: "load", timeout: 60000 }); await p.waitForFunction("typeof window.mtWikipediaChip === \"function\"", { timeout: 30000 }); const html = await p.evaluate("window.mtWikipediaChip({ title: \"Test Park\", url: \"https://en.wikipedia.org/wiki/Test_Park\" })"); const chip = String(html).includes("mt-wiki-chip"); await b.close(); console.log(JSON.stringify({ chip, fatalErrors: errs.length })); if (!chip || errs.length > 0) process.exit(1);'; Set-Content scratch-qc-wiki-chip.mjs -Value $js; node scratch-qc-wiki-chip.mjs $url; $rc = $LASTEXITCODE; Remove-Item scratch-qc-wiki-chip.mjs -ErrorAction SilentlyContinue; if ($rc -ne 0) { Write-Output WIKI-CHIP-WITNESS-FAIL; exit 1 }; Write-Output WIKI-CHIP-WITNESSED [command]

Done means: step 9's headless-render receipt shows the wikipedia popup-chip (mt-wiki-chip markup) actually returned by window.mtWikipediaChip in a live browser render of a fresh preview deploy with zero fatal page errors — verified by headless-browser render, not by reading the code.

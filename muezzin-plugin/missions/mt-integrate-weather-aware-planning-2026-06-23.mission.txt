MISSION MT-INTEGRATE-WEATHER-AWARE-PLANNING-2026-06-23
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - js/plan-day-weather.js
  - map.html
REQUIRES: none
VISUAL-QC-REQUIRED
PREVIEW-BASE-URL: http://localhost:8788

Niyyah: Feature worktree mt-feat-weather-aware-planning-2026-06-23 carries one finished
commit — a weather-aware extension to the day-trip planner (inline forecast on
overnights/destination, a heavy-rain warning chip, and an "Adjust for weather"
reschedule action) — sitting in its own worktree, never landed on the integration
line's current HEAD. This mission's whole job is mechanical: get the feature's files
onto the target repo's HEAD without losing anything already there, and produce the
node-level and render-level receipts a verdict panel needs to judge the result. Writing
this mission file, and the act of integrating itself, is NOT verification that the
feature's own code is correct. Whether the forecast actually renders the right numbers,
whether the >=70% heavy-rain threshold is the right call, whether the "Adjust for
weather" reschedule logic genuinely finds a drier window instead of a spurious one,
whether the 60ms MutationObserver coalesce ever double-paints or misses a re-render —
none of that is settled by landing the commit. That determination belongs entirely to
this mission's own witness-and-verdict-panel run: the node --check receipt, the
headless-browser render of map.html, and the seat panel's judgment on the actual
integrated diff. Say plainly: authoring or executing this mission proves nothing about
the feature code's correctness by itself.

Maqsad: Land commit bbf702d ("feat(weather-aware-planning): inline forecast on
overnights + adjust-for-weather") from the source worktree
C:\Users\marka\code\mt-feat-weather-aware-planning-2026-06-23 onto the current HEAD of
the target repo C:\Users\marka\code\mt-integration-2026-06-22, with any conflicts
resolved by keeping BOTH sides — this repo's existing unrelated changes AND the
feature's changes, never silently dropping either — every changed .js/.mjs file passes
`node --check`, the result is committed with a clear message, and the target repo's
working tree is left clean with respect to every ALLOW-FILES entry.

Context (verified against live substrate this session, absolute paths, no prescribed
resolution beyond "keep both sides"):
- Source worktree: C:\Users\marka\code\mt-feat-weather-aware-planning-2026-06-23.
  `git log --oneline -1` there shows bbf702d as the tip commit for this feature.
- Confirmed diff stat (git show --stat, this session): bbf702d touches 2 files,
  +692/-1 (js/plan-day-weather.js new, 691 lines; map.html +1/-1, inserting a single
  `<script src="js/plan-day-weather.js" defer></script>` tag immediately after the
  existing `<script src="js/plan-day.js" defer></script>` tag and immediately before
  the existing `<script src="js/poi-photos.js" defer></script>` tag).
- Checked this session: `git merge-base --is-ancestor bbf702d HEAD` in the target repo
  returns false — bbf702d is not yet landed. `git cat-file -t bbf702d` in the target
  repo returns "commit" — the object is already reachable in the target repo's own
  local object store (a local branch feat/weather-aware-planning-2026-06-23 and a
  remote-tracking branch remotes/github/feat/weather-aware-planning-2026-06-23 both
  already contain it), so no `git fetch <source-worktree-path>` / remote-add step is
  needed; a plain `git cherry-pick bbf702d` can resolve the object directly.
- A dry run this session (throwaway branch off the target's then-current HEAD
  5e19874 on branch main, deleted afterward, no lasting change) cherry-picked bbf702d
  cleanly: map.html auto-merged with ZERO conflicts, js/plan-day-weather.js applied as
  a pure add, and `node --check js/plan-day-weather.js` exited 0.
- Observed this session: the target repo's working directory is under active,
  concurrent use by other mission/automation activity independent of this mission —
  multiple other throwaway integration branches already exist there (local and
  remote), and mid-session the checked-out branch changed underneath this session's
  own exploration to a different in-progress integration (dryrun-wikipedia-link-check-
  tmp) with its own unrelated staged changes and an unmerged map.html, which resolved
  itself without this session touching it. This mission therefore deliberately carries
  no TARGET-BRANCH header: land onto whatever branch is actually checked out as HEAD
  when this mission fires, not a hardcoded branch name or the 5e19874 commit named
  above (which will very likely have moved on by fire time). The dry run's ZERO-
  conflict result is evidence the feature applies cleanly against a plausible base, not
  a guarantee against conflicts at fire time — if map.html (or any other ALLOW-FILES
  entry) conflicts at fire time, resolve it by keeping BOTH the target's existing
  script tags AND this feature's added script tag.
- js/plan-day-weather.js's own header comment (verified this session by reading the
  file) states it: (1) adds an inline forecast row to each overnight card / status bar
  via /api/weather, (2) shows a heavy-rain warning chip when any overnight's pop_pct is
  >= 70 (the module defines this threshold as HEAVY_RAIN_POP = 70), and (3) adds an
  "Adjust for weather" button that re-plans by shifting start_date forward 1-3 days
  looking for a drier window. The file attaches to the existing window.__mtPlanDay
  debug surface rather than editing js/plan-day.js itself, and at the bottom of the
  file it publishes its own debug surface, window.__mtPlanDayWeather, an object with
  keys including fetchWeather, HEAVY_RAIN_POP, ADJUST_LOOKAHEAD_DAYS,
  findBetterStartDate, paintMultiDay, paintSingleDay, and schedulePaint.
- map.html is a live page served by the site (not a browser-extension popup), so
  PREVIEW-BASE-URL: http://localhost:8788 is a PLACEHOLDER for a local
  `wrangler pages dev` server — the operator must confirm the actual local preview port
  before relying on the captured render; if nothing answers at PREVIEW-BASE-URL, that
  failure must surface as a named receipt, not a silently skipped check.

Steps:
  1. In the target repo, record the current branch and HEAD, and confirm
     js/plan-day-weather.js does not already exist on disk (git -C
     C:\Users\marka\code\mt-integration-2026-06-22 status --short; git -C
     C:\Users\marka\code\mt-integration-2026-06-22 log --oneline -3; git -C
     C:\Users\marka\code\mt-integration-2026-06-22 branch --show-current). [command]
     verify-target-state
  2. Check ancestry before attempting a cherry-pick: `git -C
     C:\Users\marka\code\mt-integration-2026-06-22 merge-base --is-ancestor bbf702d
     HEAD`. If it reports already-an-ancestor, bbf702d is already landed — do not
     cherry-pick it again. Otherwise proceed to step 3. [command] check-ancestry
  3. Apply bbf702d via `git -C C:\Users\marka\code\mt-integration-2026-06-22
     cherry-pick bbf702d`. If it reports conflicts, resolve every conflicted file by
     keeping BOTH the target's pre-existing lines and the feature's added lines — never
     `--ours` / `--theirs` and never a resolution that deletes either side's content.
     If it comes back empty (already-applied after all), resolve with `--skip`, never
     `--allow-empty`. [command] cherry-pick-commit
  4. Run `node --check js/plan-day-weather.js` from the target repo root and confirm it
     exits 0; also run `node --check` on any other .js/.mjs file that conflict
     resolution in step 3 modified. [command] node-check-all
  5. Stage and commit the integrated result with a message that names
     "mt-feat-weather-aware-planning-2026-06-23" and/or "plan-day-weather" and cites
     bbf702d. Confirm `git status --short` afterward shows no uncommitted changes to
     any ALLOW-FILES entry and `git log --oneline -5` shows the landed commit at or
     near HEAD. [command] commit-and-verify-clean
  6. (amended 2026-07-07 per RULE 9: prior step hand-rolled a localhost:8788 server — nothing serves that port) In ONE command: confirm clean tree for this mission's ALLOW-FILES (git commit first if this mission's edits are not yet committed — never deploy a dirty tree), then wrangler pages deploy . --project-name=muddytires --branch=preview, capture the emitted *.pages.dev URL into a PowerShell variable, write scratch-qc.mjs (playwright render of /map.html from that URL, exit non-zero if the page throws a fatal script error or, in that page's own runtime, `typeof window.__mtPlanDayWeather === 'object'` and `window.__mtPlanDayWeather.HEAVY_RAIN_POP === 70` — the debug surface js/plan-day-weather.js is supposed to publish — do not both hold), node scratch-qc.mjs, Remove-Item scratch-qc.mjs. [command] headless-render-witness

Done means:
  - `git log` in C:\Users\marka\code\mt-integration-2026-06-22 shows a commit at or
    near HEAD whose message names "mt-feat-weather-aware-planning-2026-06-23" and/or
    "plan-day-weather" and cites bbf702d.
  - js/plan-day-weather.js exists in the target repo with the feature's actual content
    (confirmed by reading the on-disk file, not by trusting the cherry-pick's exit code
    alone) — specifically it defines `window.__mtPlanDayWeather` with a
    `HEAVY_RAIN_POP` value of 70 and a `fetchWeather` function.
  - map.html contains `<script src="js/plan-day-weather.js" defer></script>`
    immediately after the existing `<script src="js/plan-day.js" defer></script>` tag,
    AND every pre-existing script tag map.html already had before this mission ran
    (both sides present, nothing dropped).
  - `node --check js/plan-day-weather.js` exits 0, and `node --check` exits 0 on every
    other .js/.mjs file this integration touched or conflict-resolved.
  - `git status --short` in the target repo shows no uncommitted changes to
    js/plan-day-weather.js or map.html.
  - A headless browser (headless Chrome / Playwright / puppeteer) render of /map.html
    served from PREVIEW-BASE-URL loads without a fatal script error and shows, in that
    page's own runtime, `window.__mtPlanDayWeather` defined with
    `HEAVY_RAIN_POP === 70` — verified by rendering the page, not by reading source.
    Nothing about this mission's own authorship or execution constitutes verification
    that the feature's code is correct (that the forecast numbers render right, that
    the heavy-rain threshold is the right call, that "Adjust for weather" actually
    finds a genuinely drier window, that nothing else regressed) — that determination
    belongs entirely to this mission's own witness-and-verdict-panel run, downstream of
    the receipts above.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

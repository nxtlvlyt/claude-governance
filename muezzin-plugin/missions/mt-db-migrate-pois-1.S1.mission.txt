MISSION-ID: M-MUDDYTIRES.DB-MIGRATE-POIS-1.S1
MISSION-CLASS: code-repo
REPO-ROOT: E:\AI_Storage\muddytires-d1-wt
ALLOW-FILES:
  - d1/poi-migration-plan.md
  - d1/pois-load.sql
PARENT: M-MUDDYTIRES.DB-MIGRATE-POIS-1  (sub-mission 1 of 3, auto-split by the size ceiling)
REQUIRES: none (first sub-mission of 3 in tartib order)

Niyyah: the database that held every place the community gathered still lives on the NAS — a 1.4GB MariaDB the static Cloudflare site

Maqsad: this is part 1 of 3 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): produce the concrete migration that moves the POI data off the NAS into Cloudflare D1 `pois`, so the live map's markers return.

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - DE-RISK PRECHECK (read-only, no NAS touch): confirm the off-NAS backup archive is on disk AND actually contains the pois table files (pois.frm/.ibd). Positive-assert: exit 1 if the archive is missing/unreadable or the pois files are absent inside it — this gates all downstream data work and fires the receipt-backed HALT branch before any restore.
  - Create the throwaway scratch work directory for intermediates (kept OUT of the d1/ commit zone so it is never swept by git add). Subdirectory targets require a preceding creation command.
  - Capture the LIVE remote D1 `pois` CREATE TABLE DDL to a scratch file so the generated load SQL matches the real column set exactly (never assume columns). Read-only on remote D1. Positive-assert the schema actually came back.
  - Capture the LIVE D1 `pois` node_type row breakdown to a scratch file — the 'have' side of the GAP that the plan doc must state. Read-only on remote D1; outcome is a local file, asserted non-trivial.
  - Positive-assertion gate proving the live D1 `pois` table is NOT empty (this is GAP-FILL, not from-scratch). Reaches the remote resource and exits 1 when COUNT==0, so a hollow/empty result fails the witness.
  - Extract the physical MariaDB datadir from the off-NAS backup tarball into the scratch dir so a local container can mount it (NO NAS touch — backup only). Positive-assert pois.ibd was actually extracted.
  - Detect the exact MariaDB minor version from mysql_upgrade_info in the extracted datadir — InnoDB physical restore requires a version-matched image or the mount corrupts/fails. HALT (exit 1) if no version string is detectable rather than guessing a tag.
  - Start a throwaway local MariaDB 10 container with the extracted datadir mounted as /var/lib/mysql (mirrors the ORACLE-MIGRATION-SCOPE.md restore prerequisite). Positive-assert the container reaches running state; the readability gate follows.

Done means: steps M-MUDDYTIRES.DB-MIGRATE-POIS-1.S1.1..M-MUDDYTIRES.DB-MIGRATE-POIS-1.S1.8 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — (acceptance contract): d1/poi-migration-plan.md names the POI gap (D1 pois count vs NAS source count) + the exact operator-
gated load+deploy commands; d1/pois-load.sql is a valid SQLite load for the POI rows (or the plan states precisely why extraction is
blocked and what's needed). Both committed via explicit `git add` of only those two files, --no-verify; NOT loaded into live D1. A

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
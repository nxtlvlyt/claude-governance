MISSION QUIRKY-POI-CURATION
MISSION-ID: 20
MISSION-CLASS: research
REQUIRES: live web search (Claude-tier WebSearch live; cloud SearXNG loop standard).
Niyyah: the Roadside Oddity dataset (PRODUCT-PLAN.md line ~179, staged here) was specced — 50 curated Ontario-corridor items with quirky_rating/photo_stop/walk_out/detour_minutes — but never produced; the operator noticed the missing map filter. Half the filter gap is plumbing (#9, deferred code); this mission is the other half: the data itself.
Maqsad: ONE artifact — quirky-pois-ontario.json at the sandbox root, plus quirky-pois-NOTES.md and search-receipts.md. The JSON: an array of Ontario-corridor roadside-oddity POIs — AS MANY AS GENUINELY VERIFY; the ONLY count criterion is the hard floor of 15 (RECEIPT 2026-06-11 22:50: a soft "target 20+" was enforced by the verdict as a hard bar and killed a 19-verified-entry run whose executor had CORRECTLY refused to pad — aspirational counts are forbidden in this text; 15+ verified entries = the count requirement is MET, full stop. Earlier receipt: the "exactly 50" bar drove FABRICATION — wrong-town attractions, fake coordinates, invented source_urls; every padded entry is an arkan violation). PRIOR VERIFIED RESEARCH IS REUSABLE: missions/quirky-poi-curation/_prior-attempt/ holds earlier drafts including a 10.4KB entries JSON — entries from it MAY be reused ONLY by RE-VERIFYING each (fresh search receipt per reused entry); never blind-copy. Each entry: {name, lat, lon, type:"roadside_oddity", quirky_rating (1-5 INT, editorial judgment per the plan's Atlas-Obscura benchmark), photo_stop (bool), walk_out (bool), detour_minutes (smallint, from Trans-Canada/401/17/69 corridors), one_line (caption in road-trip voice), source_url}. Use live web search to find and VERIFY each (exists now, still standing); search-receipts.md logs ONE line per ACCEPTED entry — the query used + the URL that confirmed it — an entry with no receipt line does not go in the JSON. Seed examples from the staged plan: World's Largest Coin/Big Nickel (Sudbury), Wawa Goose (verify current height before stating it), Big Apple (Colborne), Husky the Muskie (Kenora), giant loonie (Echo Bay) — find the rest along Ontario's RV corridors. Canadian-first ONLY. The NOTES.md: curation rationale, items considered-and-rejected (with why), and OPEN QUESTIONS for the operator (e.g. rating calibration for the >=4 headline gate). Done = JSON parses, >=15 entries, every entry has source_url AND a matching line in search-receipts.md, NOTES exists.
Context: PRODUCT-PLAN.md staged read-only (the spec lives at the Roadside Oddity row). The JSON + NOTES are the only write targets, cwd-relative.
Amanah: entrusted; discharge with ihsan.

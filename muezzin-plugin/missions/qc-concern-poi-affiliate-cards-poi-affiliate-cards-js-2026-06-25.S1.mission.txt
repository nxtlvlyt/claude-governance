MISSION-ID: M-MUDDYTIRES.QC-CONCERN-POI-AFFILIATE-CARDS-POI-AFFILIATE-CARDS-JS-2026-06-25.S1
PARENT: M-MUDDYTIRES.QC-CONCERN-POI-AFFILIATE-CARDS-POI-AFFILIATE-CARDS-JS-2026-06-25
TARTIB-INDEX: 1 of 2
REQUIRES: none (first in tartib order)
STEPS: 8

PARENT MAQSAD: (see parent mission)

Maqsad: sub-mission 1 of 2 — Lock the spec contract: confirm docs/FEATURE-CATALOG-2026-06-23.md contains the POI affiliate-card entry (popup-open hook, render-when-appropriate condition, exact dataLayer event name + payload, exact plausible goal name + props). Read-only reference (outside ALLOW-FILES) — never an edit target; the exact event-name strings authored in step 2 must match this doc, not memory. through Witness the runner now drives a POI marker interaction: workers/e2e-runner.js references a marker click step.

Steps:
  1. Lock the spec contract: confirm docs/FEATURE-CATALOG-2026-06-23.md contains the POI affiliate-card entry (popup-open hook, render-when-appropriate condition, exact dataLayer event name + payload, exact plausible goal name + props). Read-only reference (outside ALLOW-FILES) — never an edit target; the exact event-name strings authored in step 2 must match this doc, not memory. [verify] docs/FEATURE-CATALOG-2026-06-23.md
  2. Author the single fix to poi-affiliate-cards.js (ONE writer of this file). In one pass: (a) bind the Leaflet popup-open hook correctly — map-level map.on('popupopen', e => ...) (or marker-level after bindPopup), injecting into e.popup.getElement(); (b) render the affiliate card into the opened popup's DOM only when the POI qualifies per the catalog; (c) in the card click handler fire BOTH window.dataLayer.push({event:'<catalog name>',...}) and window.plausible('<catalog goal>',{props:{...}}), each guarded so a missing dataLayer/plausible does not throw, and not double-wired with any existing delegated listener. Event-name strings taken verbatim from step 1. [unverified: file content not readable on this dispatch — executor must preserve existing exports/structure.] [edit] js/poi-affiliate-cards.js
  3. Witness the click-side GA4/GTM wiring landed: poi-affiliate-cards.js contains a dataLayer.push call. [verify] js/poi-affiliate-cards.js
  4. Witness the Plausible custom-event wiring landed: poi-affiliate-cards.js contains a plausible( call. [verify] js/poi-affiliate-cards.js
  5. Witness the edited poi-affiliate-cards.js is syntactically valid JavaScript (no parse error introduced by the fix). [verify] js/poi-affiliate-cards.js
  6. Gate the runner edit before touching it: assert the e2e walk runner exists at workers/e2e-runner.js. If it is named differently or lives outside the ALLOW-FILES surface (js/, workers/), this step fails and the mission halts here — surfacing the structural wall Architect 1 flagged rather than creating a spurious file. [unverified path — confirm before firing.] [verify] workers/e2e-runner.js
  7. Author the single fix to the e2e walk runner (ONE writer of this file): after map load, insert a POI interaction block — wait for a POI marker in the DOM, click it to open the popup, await popupopen and assert the affiliate card element renders (Gate 1), then click the card and capture/assert both dataLayer.push (Gate 2) and plausible (Gate 3) fired with the catalog event names. This closes the runner gap named in the Maqsad (the heuristic does not currently click a POI marker). Preserve existing walk steps. [unverified: file content not readable — executor must preserve existing runner logic.] [edit] workers/e2e-runner.js
  8. Witness the runner now drives a POI marker interaction: workers/e2e-runner.js references a marker click step. [verify] workers/e2e-runner.js

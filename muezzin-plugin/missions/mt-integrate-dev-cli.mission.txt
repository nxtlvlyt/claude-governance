MISSION mt-integrate-dev-cli
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - js/dev-cli.js
REQUIRES: none

Niyyah: The feature "mt-feat-dev-cli" (commit d4073e8, sitting in the source worktree
C:\Users\marka\code\mt-feat-dev-cli on branch feat/dev-cli) has never been integrated into
this repo's tree. This mission's entire job is mechanical integration — land the commit
onto the target repo's current HEAD without losing either side's work, and prove the
result is syntactically sound and committed cleanly. Nothing this mission does, and
nothing about this mission's own authorship, constitutes a judgment that the dev-cli
feature's CODE is correct — that gating the whole surface behind localStorage
mt-dev-mode=1 is actually inert when unset, that help()/version()/spots()/
nearestSpot(lat,lng)/savedSpots()/purgeCache()/forceReload() behave safely against real
data, or that reusing window.mtSavedList for saved-list calls keeps auth and proxy paths
identical to js/saved-list.js as the commit message claims. That determination belongs
entirely to this mission's own witness + verdict-panel run, which inspects the integrated
result AFTER this mission produces it. Authoring or executing this mission is not
verification of the feature.

Maqsad: The commit "d4073e8 feat(dev-cli): browser-console help CLI dormant behind
localStorage mt-dev-mode=1" is cherry-picked or merged from the source worktree onto the
current HEAD of C:\Users\marka\code\mt-integration-2026-06-22, with any conflicts resolved
by keeping BOTH sides — this repo's existing unrelated changes AND the dev-cli feature's
changes, never silently dropping either — js/dev-cli.js (and any other .js/.mjs file
conflict resolution ends up touching) passes `node --check`, the result is committed with
a clear message, and the target repo's working tree is left clean (git status shows no
uncommitted changes to the touched files).

Context (verified against live substrate this session, absolute paths, no prescribed
resolution beyond "keep both sides"):
- Source worktree: C:\Users\marka\code\mt-feat-dev-cli, branch feat/dev-cli, HEAD already
  sitting exactly on d4073e8 (confirmed via git log -1) — do not modify that worktree, only
  read/cherry-pick FROM it.
- Confirmed diff stat for d4073e8 (git show --stat, this session): 1 file changed, 294
  insertions(+), 0 deletions(-), and it is a pure new addition with no existing-file
  modifications: js/dev-cli.js (294 new lines). No other files are touched in this commit.
- Target repo (this session, git log --oneline -1 in
  C:\Users\marka\code\mt-integration-2026-06-22): HEAD 28cbc72, checked out branch
  feat/qc-concern-sentry-error-capture-sentry-init-js-2026-06-25. Confirmed by direct file
  check this session: js/dev-cli.js does not exist anywhere in the target tree yet, so a
  cherry-pick is expected to apply as a pure add with zero conflicts. If the target tree has
  since gained its own file at js/dev-cli.js by the time this mission fires, that is a
  changed condition from this authoring session — resolve any resulting conflict by keeping
  BOTH sides' content (the target's pre-existing lines AND the feature's added lines), never
  `--ours` / `--theirs` and never a resolution that drops either side.
- No TARGET-BRANCH header is set, deliberately: this mission's job is to land the commit
  onto whichever branch/HEAD the target repo has checked out AT FIRE TIME ("the current
  HEAD"), not onto a hardcoded branch name. Do not check out or switch branches as a step of
  this mission; integrate in place on whatever tree is already checked out.
- This diff touches zero .html files (confirmed: the only changed file is js/dev-cli.js) —
  the visual-QC render-witness header used by other integration missions is intentionally
  left off this mission's header block, and no render-witness step is required or expected.
- js/dev-cli.js is the only JavaScript file in this diff. `node --check` must still be run on
  any additional .js/.mjs file that conflict resolution ends up touching, if any.

Steps:
  1. In the source worktree C:\Users\marka\code\mt-feat-dev-cli, confirm commit d4073e8
     exists and capture its patch (e.g. `git show d4073e8` or `git format-patch -1 d4073e8`)
     without modifying that worktree. [command] capture-patch
  2. In the target repo C:\Users\marka\code\mt-integration-2026-06-22, at whatever
     branch/HEAD is currently checked out (no branch switch), apply the captured commit via
     `git cherry-pick d4073e8` (adding the source worktree as a remote/reachable object
     source if needed) or an equivalent merge of that single commit. [command] integrate-commit
  3. If cherry-pick reports conflicts on js/dev-cli.js (not expected, since it is a pure new
     add) or on any other file, resolve each conflicted hunk by hand so the resolved content
     contains BOTH the target's pre-existing content AND the dev-cli feature's content —
     never `--ours` / `--theirs` and never a resolution that deletes either side's lines.
     [edit] resolve-conflicts
  4. Run `node --check js/dev-cli.js`, and `node --check` on every other .js/.mjs file the
     merge touched or conflict resolution modified, confirming each exits 0.
     [command] node-check-all
  5. Stage and commit the integrated result in the target repo with a clear message
     identifying the integrated feature and its origin commit (e.g. referencing
     "mt-feat-dev-cli" and "d4073e8"). Confirm `git status` afterward shows a clean working
     tree. [command] commit-and-verify-clean

Done means:
  - `git log` in C:\Users\marka\code\mt-integration-2026-06-22 shows a commit containing the
    integrated dev-cli feature, with a commit message that names "mt-feat-dev-cli" and/or
    "d4073e8".
  - js/dev-cli.js exists in the target repo at HEAD with non-trivial content (contains the
    window.mt namespace with help(), version(), spots(), nearestSpot(lat,lng),
    savedSpots(), purgeCache(), forceReload(), and the localStorage mt-dev-mode gate).
  - `node --check js/dev-cli.js` exits 0, and `node --check` exits 0 on every other .js/.mjs
    file this integration touched or conflict-resolved.
  - `git status` in the target repo shows a clean working tree (no uncommitted changes) at
    the end of the mission.
  - None of the above constitutes verification that the feature's CODE is correct (e.g. that
    the dormant gate is truly zero-cost when unset, or that the saved-list reuse is safe) —
    that determination belongs to this mission's own witness + verdict-panel run downstream,
    not to this mission's authorship or execution.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

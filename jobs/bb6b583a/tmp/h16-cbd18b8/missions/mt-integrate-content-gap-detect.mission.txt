MISSION mt-integrate-content-gap-detect
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - workers/content-gap-detect/index.js
  - d1/migrate-search-log-content-gap-2026-06-23.sql
  - wrangler.content-gap-detect.toml
  - docs/content-gap-detect.md
REQUIRES: none

Niyyah: The feature "mt-feat-content-gap-detect" (commit dd3fe10, sitting in the source
worktree C:\Users\marka\code\mt-feat-content-gap-detect) has never been integrated into
this repo's tree. This mission's entire job is mechanical integration — land the commit
onto the target repo without losing either side's work, and prove the result is
syntactically sound and committed cleanly. Nothing this mission does, and nothing about
this mission's own authorship, constitutes a judgment that the content-gap-detect feature's
CODE is correct — that the cron actually finds zero-result queries, that the dedupe against
open TODOs works, that the search_log migration is shape-compatible with what
feat/search-analytics will later populate, or that the worker degrades honestly on missing
schema. That determination belongs entirely to this mission's own witness + verdict-panel
run, which inspects the integrated result AFTER this mission produces it. Authoring or
executing this mission is not verification of the feature.

Maqsad: The commit "dd3fe10 feat(content-gap-detect): daily cron auto-creates admin TODOs
from zero-result search queries" is cherry-picked or merged from the source worktree onto
the current HEAD of C:\Users\marka\code\mt-integration-2026-06-22, with any conflicts
resolved by keeping BOTH sides — this repo's existing unrelated changes AND the
content-gap-detect feature's changes, never silently dropping either — every changed
.js/.mjs file passes `node --check`, the result is committed with a clear message, and the
target repo's working tree is left clean (git status shows no uncommitted changes to the
touched files).

Context (verified against live substrate this session, absolute paths, no prescribed
resolution beyond "keep both sides"):
- Source worktree: C:\Users\marka\code\mt-feat-content-gap-detect, branch
  feat/content-gap-detect-2026-06-23, HEAD already sitting exactly on dd3fe10 (confirmed via
  git log -1) — do not modify that worktree, only read/cherry-pick FROM it.
- Confirmed diff stat for dd3fe10 (git show --stat, this session): 4 files changed, 681
  insertions(+), 0 deletions(-), and ALL FOUR are new additions with no existing-file
  modifications: workers/content-gap-detect/index.js (402 new lines),
  d1/migrate-search-log-content-gap-2026-06-23.sql (81 new lines),
  wrangler.content-gap-detect.toml (60 new lines), docs/content-gap-detect.md (138 new
  lines).
- Target repo (this session, git log --oneline -1 in
  C:\Users\marka\code\mt-integration-2026-06-22): HEAD 28cbc72, checked out branch
  feat/qc-concern-sentry-error-capture-sentry-init-js-2026-06-25. Confirmed by direct file
  check this session: none of the four paths above exist anywhere in the target tree yet, so
  a cherry-pick is expected to apply as a pure add with zero conflicts. If the target tree
  has since gained its own file at any of these four paths by the time this mission fires,
  that is a changed condition from this authoring session — resolve any resulting conflict
  by keeping BOTH sides' content (the target's pre-existing lines AND the feature's added
  lines), never `--ours` / `--theirs` and never a resolution that drops either side.
- No TARGET-BRANCH header is set, deliberately: this mission's job is to land the commit
  onto whichever branch/HEAD the target repo has checked out AT FIRE TIME ("the current
  HEAD"), not onto a hardcoded branch name. Do not check out or switch branches as a step of
  this mission; integrate in place on whatever tree is already checked out.
- This diff touches zero .html files (confirmed: none of the four changed files end in
  .html) — the visual-QC render-witness header used by other integration missions is
  intentionally left off this mission's header block, and no render-witness step is
  required or expected.
- Only workers/content-gap-detect/index.js is JavaScript among the four changed files; there
  are no other .js/.mjs files in this diff. `node --check` must still be run on any
  additional .js/.mjs file that conflict resolution ends up touching, if any.

Steps:
  1. In the source worktree C:\Users\marka\code\mt-feat-content-gap-detect, confirm commit
     dd3fe10 exists and capture its patch (e.g. `git show dd3fe10` or
     `git format-patch -1 dd3fe10`) without modifying that worktree. [command] capture-patch
  2. In the target repo C:\Users\marka\code\mt-integration-2026-06-22, at whatever branch/HEAD
     is currently checked out (no branch switch), apply the captured commit via
     `git cherry-pick dd3fe10` (adding the source worktree as a remote/reachable object
     source if needed) or an equivalent merge of that single commit. [command] integrate-commit
  3. If cherry-pick reports conflicts on any ALLOW-FILES entry (not expected, since all four
     are pure new adds) or on any other file, resolve each conflicted hunk by hand so the
     resolved content contains BOTH the target's pre-existing content AND the
     content-gap-detect feature's content — never `--ours` / `--theirs` and never a
     resolution that deletes either side's lines. [edit] resolve-conflicts
  4. Run `node --check workers/content-gap-detect/index.js`, and `node --check` on every
     other .js/.mjs file the merge touched or conflict resolution modified, confirming each
     exits 0. [command] node-check-all
  5. Stage and commit the integrated result in the target repo with a clear message
     identifying the integrated feature and its origin commit (e.g. referencing
     "mt-feat-content-gap-detect" and "dd3fe10"). Confirm `git status` afterward shows a
     clean working tree. [command] commit-and-verify-clean

Done means:
  - `git log` in C:\Users\marka\code\mt-integration-2026-06-22 shows a commit containing the
    integrated content-gap-detect feature, with a commit message that names
    "mt-feat-content-gap-detect" and/or "dd3fe10".
  - workers/content-gap-detect/index.js exists in the target repo at HEAD with non-trivial
    content (contains the zero-result search_log query and the admin_tasks INSERT/dedupe
    logic).
  - d1/migrate-search-log-content-gap-2026-06-23.sql exists in the target repo at HEAD
    containing the search_log table and composite-index definitions.
  - wrangler.content-gap-detect.toml exists in the target repo at HEAD containing the
    worker/cron deploy configuration (a daily cron trigger declared).
  - docs/content-gap-detect.md exists in the target repo at HEAD containing the
    operator-facing runbook content.
  - `node --check` exits 0 on workers/content-gap-detect/index.js and on every other
    .js/.mjs file this integration touched or conflict-resolved.
  - `git status` in the target repo shows a clean working tree (no uncommitted changes) at
    the end of the mission.
  - None of the above constitutes verification that the feature's CODE is correct (e.g. that
    the cron cadence, dedupe semantics, or SQL schema are right) — that determination belongs
    to this mission's own witness + verdict-panel run downstream, not to this mission's
    authorship or execution.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

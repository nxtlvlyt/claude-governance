missions/engine-srcsha-fixture-update.mission.txt  <!-- RE-BARED 2026-07-13 (conductor, direct re-verification per operator pushback "are you even trying"): the blocker named above (conduct-cycle.mjs dirty from engine-srcsha-anchor-fix-v2's interrupted commit) is GONE — direct check just now: git status --short -- conduct-cycle.mjs is empty (clean), git show HEAD:conduct-cycle.mjs already carries the BASELINE-SHA anchor (2 occurrences). ROOT CAUSE: the anchor fix was swept into commit 7e0a011 (the verdict_merge.mjs mission's own commit step) as a side effect — that mission's step ran `git add -- verdict_merge.mjs` then a bare `git commit` with no pathspec, which commits the WHOLE INDEX, including whatever was already staged. The pre-existing staged conduct-cycle.mjs anchor fix rode along silently. Filed as a NEW, more precise gap (gap-bare-commit-sweeps-preexisting-stage) superseding the "structural, no fix possible" framing of gap-mission-chain-inherited-dirty-tree for THIS specific instance — the tree unblocked itself, not because the containment model changed. Live selftest still shows exactly the 1 expected FAIL (false-death: all files byte-identical at HEAD -> FULL candidate) confirming the fixture patch is still needed and nothing else changed. Patcher (missions/_logs/srcsha-fixture-update-patch.mjs) was independently dry-run-verified twice already this session — re-firing as-is, no amendment needed. -->

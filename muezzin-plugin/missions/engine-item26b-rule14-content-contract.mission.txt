MISSION-ID: M-ENGINE.ITEM26B.RULE14-CONTENT-CONTRACT
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\.claude\muezzin-plugin
ALLOW-FILES:
  - mission_lint.mjs
REQUIRES: none
STEPS: 3

Maqsad: close QUEUE ITEM 26 sub-part (b) / gap-seo-cro-copy-contract at the engine —
land RULE 14 (content-pass-without-contract) in mission_lint.mjs, mirroring RULE 13's
proven trigger discipline (semantics + target, never a bare tag) for the copy/content
side. The playbook the operator shared names a real gap: mt-copy-clarity grades
readability but nothing enforces SEO semantics — the atv homepage headings are exactly
the "clean but invisible" class the playbook describes, no geo/niche modifiers, no FAQ
blocks, no H1 formula. RULE 14 refuses a copy-pass mission with no bound contract at
the miqat, the same way RULE 13 already refuses an unbound design pass. A committed,
dry-run-verified patcher script already exists at
missions/_logs/mission-lint-rule14-content-contract-patch.mjs (idempotent, exits
ALREADY-PATCHED on a second run) — this mission runs it rather than hand-editing.

niyyah: close the mechanical half of this gap without overreaching into the content
half — RULE 14 only refuses copy-pass missions that lack a bound CONTENT-CONTRACT.md;
authoring the actual per-site contracts, the copy-witness heading greps, and the
structured wireframe deliverable format are separate QUEUE ITEM 26 sub-parts, not
this mission's job.

Steps:
  1. Baseline. Run exactly: git rev-parse --abbrev-ref HEAD; git rev-parse HEAD; node mission_lint.mjs; if ($LASTEXITCODE -ne 0) { Write-Output PRE-SELFTEST-FAIL; exit 1 }; Write-Output PRE-SELFTEST-PASS [command]
     validation_command: node mission_lint.mjs
  2. Run the committed patcher exactly: node missions/_logs/mission-lint-rule14-content-contract-patch.mjs; if ($LASTEXITCODE -ne 0) { exit 1 } [command]
     validation_command: node --check mission_lint.mjs; if ($LASTEXITCODE -ne 0) { exit 1 }; if (-not (Select-String -Path mission_lint.mjs -Pattern 'content-pass-without-contract' -Quiet)) { exit 1 }
  3. Full selftest, then commit. Run exactly: node mission_lint.mjs 2>&1 | Tee-Object -Variable out; $failCount = ($out | Select-String -Pattern '^FAIL').Count; if ($failCount -ne 0) { Write-Output "SELFTEST-INCOMPLETE fail=$failCount"; exit 1 }; git add -- mission_lint.mjs; git diff --cached --quiet; if ($LASTEXITCODE -eq 0) { Write-Output ALREADY_COMMITTED } else { git commit -m "feat(engine): RULE 14 content-pass-without-contract in mission_lint.mjs (QUEUE item 26b / gap-seo-cro-copy-contract); selftest ALL PASS, zero regressions in RULES 1-13"; if ($LASTEXITCODE -ne 0) { exit 1 } } [command]

Done means: mission_lint.mjs carries RULE 14 (content-pass-without-contract), refusing
a mission with copy-pass semantics (SEO/heading/meta rewrite language) touching .html
targets that binds no CONTENT-CONTRACT.md; node mission_lint.mjs selftest ALL PASS with
zero regressions in RULES 1-13; committed. NOTE FOR THE CONDUCTOR (not a step): the
gate goes live per-daemon at next restart. Sub-parts (a) CONTENT-CONTRACT.md authoring,
(c) copy-witness heading greps, (d) structured SEO wireframe deliverable format remain
separate, unstarted QUEUE ITEM 26 work — this mission closes only the mechanical gate.

# CONDUCTOR PROVENANCE (2026-07-13): constructed per the five-verb law and the
# conductor-direct exception conditions — CLASS fails (mission_lint.mjs is engine
# machinery, the mission-miqat gate itself) and the multi-region diff (4 new fixture
# blocks + the RULE 14 logic block) exceeds the SIZE ceiling. Gap-priority: closes
# gap-seo-cro-copy-contract's mechanical half; GAP-PRIORITY-HOLD stays set regardless
# (product-class fires remain deferred until all six bite-class gaps close, not just
# this one). Patcher verified twice this session: once by the preparing sub-agent
# (40/40 PASS), once independently by the conductor in a fresh scratch copy (matching
# result, zero discrepancy) before this mission was written.

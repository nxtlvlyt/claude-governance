MISSION CORPUS-S1-CARD-NFL-PREDICTIONS
MISSION-CLASS: research
Niyyah: the factory must never re-solve what nfl-predictions already solved — it is one of the operator's builds and paid-for knowledge; unmined, future builds re-derive its patterns at frontier cost.
Maqsad: ONE deliverable — card-nfl-predictions.md in this sandbox: what the nfl-predictions build does well and how, as REUSABLE patterns. Required sections, each with file-path evidence: (1) BUILD KIND / STACK / STRUCTURE — what the app is (data/prediction pipeline? site? both), framework, hosting, directory shape; (2) FEATURE / MODULE / DATA-PIPELINE PATTERNS — the notable parts and what makes each forkable; (3) DATA MODEL — schema/state/data shape worth templating; (4) DESIGN TOKENS / COPY-VOICE PATTERNS worth reuse (or note if headless/no-UI); (5) ANTI-PATTERNS — anything the factory should NOT copy. Every claim cites a file path. Done means the card exists, parses as markdown, and every section has at least one evidence citation (or an explicit "not present in source" note).
Context: source READ-ONLY at E:\AI_Storage\_repo_read\nfl-predictions (already on disk, ~10k files — most are dependencies). Read the MEANINGFUL source — README, package.json/manifests/requirements, the app/src/notebooks/model source, config, schema/data files — and IGNORE node_modules, .git, build/dist, vendored assets (do NOT try to read all 10k files; the tool-round budget is for the meaningful ~15-20 files). List absolute paths only in context_dependencies; the card is the only write target, cwd-relative.
Amanah: entrusted; discharge with ihsan. If a claimed section has no evidence in the source, say so plainly rather than inventing it.

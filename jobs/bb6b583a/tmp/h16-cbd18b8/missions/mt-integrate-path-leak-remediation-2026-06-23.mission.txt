MISSION MT-INTEGRATE-PATH-LEAK-REMEDIATION-2026-06-23
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: main
ALLOW-FILES:
  - _patch-site-search-tag.mjs
  - scripts/_archive/_patch-site-search-tag-2026-06-23.mjs
  - scripts/_archive/README.md
  - docs/QC-PIPELINE-SOTA-2026-06-23.md
REQUIRES: none

Niyyah: The path-leak-remediation feature is two commits sitting in the source
worktree C:\Users\marka\code\mt-feat-path-leak-remediation-2026-06-23 —
375e40b0db479ede0e14407e6eb416e85936d305 (a new QC-pipeline doc, already
integrated into TARGET-BRANCH main by ancestry) and
5f19dc00fe16cdbe94a0405c1089ee50259bcaf4 (git-mv's the one-shot patch script
_patch-site-search-tag.mjs to scripts/_archive/_patch-site-search-tag-2026-06-23.mjs,
marks it archived/do-not-re-run, scrubs an operator local-path leak from its
header comment, and adds scripts/_archive/README.md as an archive index).
This mission's whole job is mechanical: land whichever of those two commits
is not already present onto main's current tip without losing anything
already there on either side of any conflict, prove every touched .js/.mjs
file is syntactically sound, commit the result, and leave the tree clean for
this mission's own scope. Authoring this mission, and executing its steps,
is NOT a review of whether the archived script's original patch behavior was
ever correct, whether the scrubbed replacement text ("a sibling guides-draft
workspace outside this repo") is an adequate description, whether the
archive-README's stated convention is the right one, or whether the
follow-up note about five other leaking files should be acted on. None of
that is settled by writing or running these steps — that determination
belongs entirely to this mission's own witness-and-verdict-panel run once it
fires, not to this mission's authorship or mechanical execution.

Maqsad: Bring commit 5f19dc00fe16cdbe94a0405c1089ee50259bcaf4 onto the
current tip of TARGET-BRANCH main in
C:\Users\marka\code\mt-integration-2026-06-22, with
scripts/_archive/_patch-site-search-tag-2026-06-23.mjs and
scripts/_archive/README.md present and byte-complete and
_patch-site-search-tag.mjs no longer at repo root. Confirm commit
375e40b0db479ede0e14407e6eb416e85936d305 is already present on main by
ancestry rather than re-applying it as a duplicate. Resolve any conflict by
keeping BOTH sides — main's pre-existing unrelated changes AND this
feature's changes — never silently dropping either. Every changed .js/.mjs
file passes `node --check` with exit code 0. The result is committed with a
clear message and the target repo's working tree is left clean with respect
to every ALLOW-FILES path.

Context (verified against live substrate this session, 2026-07-01, absolute
paths, no prescribed resolution beyond "keep both sides"):
- Source worktree C:\Users\marka\code\mt-feat-path-leak-remediation-2026-06-23,
  branch feat/path-leak-remediation-2026-06-23, tip
  5f19dc00fe16cdbe94a0405c1089ee50259bcaf4, parent
  375e40b0db479ede0e14407e6eb416e85936d305. Its `git status --short` is
  clean. Do not rewrite that worktree — only read/cherry-pick FROM it.
- The source worktree and the target repo are worktrees of the SAME
  underlying git repository (confirmed: `git cat-file -t 5f19dc0` resolves
  to "commit" directly inside the target repo, no fetch/remote-add step
  needed).
- Confirmed diff stat for 375e40b (`git show --stat`, this session): 1 file
  changed, +24/-0, all new — docs/QC-PIPELINE-SOTA-2026-06-23.md. Confirmed
  diff stat for 5f19dc0: 2 files changed, +47/-11 — scripts/_archive/README.md
  (+29, new) and scripts/_archive/_patch-site-search-tag-2026-06-23.mjs
  (rename from _patch-site-search-tag.mjs, +18/-11). Combined the two
  commits total 3 files / +71/-11, matching the feature's reported diff stat
  exactly. No .html file appears in either diff.
- Confirmed this session (`git merge-base --is-ancestor 375e40b main`):
  375e40b IS already an ancestor of main. `git cat-file -e main:docs/QC-PIPELINE-SOTA-2026-06-23.md`
  succeeds and the content already matches — some earlier integration
  already carried this exact commit in. Confirmed this session
  (`git merge-base --is-ancestor 5f19dc0 main`): 5f19dc0 is NOT an ancestor
  of main — it is the one commit actually outstanding for this feature.
- Confirmed this session (`git diff 375e40b main -- _patch-site-search-tag.mjs`,
  empty output): main's copy of the root patch script is byte-identical to
  the version 5f19dc0's rename diff was computed against, so the git-mv
  applies against exactly the content main already has. Confirmed
  (`git ls-tree -r main --name-only | grep _archive`, no match):
  scripts/_archive/ does not exist on main yet.
- A LIVE DRY-RUN cherry-pick of 5f19dc0 was performed and fully reverted
  this session (checked out a throwaway branch off main's then-current tip
  5e19874, ran `git cherry-pick 5f19dc0`, confirmed a clean 2-file /
  +47/-11 result with ZERO conflict markers — create
  scripts/_archive/README.md, rename _patch-site-search-tag.mjs ->
  scripts/_archive/_patch-site-search-tag-2026-06-23.mjs — then switched back
  to main and `git branch -D`'d the throwaway branch; main and the working
  checkout were left exactly as found). A real conflict is therefore
  unlikely, but if main has moved again before this mission fires for real,
  resolve any conflict by keeping BOTH main's existing lines AND this
  feature's added/changed lines — never `--ours` / `--theirs` and never a
  resolution that deletes either side's content.
- The target repo's checked-out branch and HEAD move between missions and
  even between commands within this same session (confirmed firsthand: this
  worktree was observed mid-cherry-pick on a throwaway branch named
  __dryrun_map_fallback, then on branch test-lhci-dryrun-2026-07-01, then on
  branch _dryrun_near_me_test, all before this mission's own dry-run cleaned
  up back onto main) — these worktrees are reused repeatedly by other
  missions. TARGET-BRANCH: main is declared explicitly for this reason:
  landing onto "whatever HEAD happens to be" would land onto one of these
  ephemeral test branches instead of the real integration target.
- The target repo's working tree carries pre-existing untracked files
  unrelated to this feature (confirmed this session: api-index-response.json,
  docs/e2e-report-2026-07-01.json, e2e-shots/, feature-catalog-entry.txt,
  scripts/e2e-runner.mjs). These predate this mission and are out of scope.
  "Clean working tree" in this mission's Done means refers to this
  mission's ALLOW-FILES paths, not a demand to clear unrelated pre-existing
  debris.
- scripts/_archive/_patch-site-search-tag-2026-06-23.mjs is a real one-shot
  Node script (verified by reading its diff) marked ARCHIVED/do-not-re-run
  in its own header comment; it is the only .js/.mjs file either commit
  touches, so it is the only file `node --check` needs to run against unless
  a conflict resolution touches another.

Steps:
  1. In the target repo C:\Users\marka\code\mt-integration-2026-06-22, check
     out TARGET-BRANCH main at its current tip and record the branch and
     HEAD. [command] checkout-target-branch
  2. Run `git merge-base --is-ancestor 375e40b0db479ede0e14407e6eb416e85936d305 main`.
     If it reports true, that commit's content is already integrated — do
     not cherry-pick it, and do not treat the skip as a failure. If it
     reports false, cherry-pick it here before continuing (and if that pick
     comes back empty, resolve with `--skip`, never `--allow-empty`).
     [command] check-375e40b-ancestry
  3. Run `git merge-base --is-ancestor 5f19dc00fe16cdbe94a0405c1089ee50259bcaf4 main`.
     If it reports true, this feature is already fully integrated — skip to
     step 6. If it reports false (the expected outcome), run
     `git cherry-pick 5f19dc00fe16cdbe94a0405c1089ee50259bcaf4`. If it comes
     back empty, resolve with `--skip`, never `--allow-empty`. If it reports
     a real conflict, resolve every conflicted hunk by keeping BOTH sides —
     main's pre-existing unrelated content AND this feature's changed
     content, merged together, never deleting one side to make a conflict
     disappear. Only _patch-site-search-tag.mjs,
     scripts/_archive/_patch-site-search-tag-2026-06-23.mjs, and
     scripts/_archive/README.md may be touched to complete a resolution.
     [command] cherry-pick-outstanding-commit
  4. If step 3 resolved a real conflict, stage only the ALLOW-FILES paths by
     name (never `git add -A` or `git add .`, which would sweep in the
     unrelated pre-existing untracked files already sitting in the target's
     working tree) and run `git cherry-pick --continue`.
     [edit] continue-cherry-pick-scoped
  5. Run `node --check scripts/_archive/_patch-site-search-tag-2026-06-23.mjs`
     and confirm exit code 0. Identify whether any OTHER .js or .mjs file
     was touched by this integration or by any conflict resolution in step
     3/4; if so, run `node --check` on each such file too and confirm exit
     code 0 for all. If no other such file exists, record that explicitly
     rather than silently skipping. [command] node-check-changed-js-files
  6. Run `git status --short -- _patch-site-search-tag.mjs scripts/_archive/_patch-site-search-tag-2026-06-23.mjs scripts/_archive/README.md docs/QC-PIPELINE-SOTA-2026-06-23.md`
     and confirm it prints nothing (fully committed, nothing pending). Run
     `git log --oneline -20` on main and confirm a commit reachable from
     HEAD carries the message "chore(archive): move _patch-site-search-tag.mjs"
     (whether that commit is the one step 3 just produced, or a pre-existing
     one). Run `git show --stat` on that commit and confirm it lists no
     .html file. [command] confirm-landed-clean-no-html

Done means:
  - `git log --oneline -20` on main in C:\Users\marka\code\mt-integration-2026-06-22
    shows a commit whose message is or begins with "chore(archive): move
    _patch-site-search-tag.mjs to scripts/_archive/ + scrub local-path leak".
  - scripts/_archive/_patch-site-search-tag-2026-06-23.mjs exists at
    C:\Users\marka\code\mt-integration-2026-06-22\scripts\_archive\_patch-site-search-tag-2026-06-23.mjs,
    read directly from disk, and its header comment contains the word
    "ARCHIVED" and does not contain the string "C:/Users/marka" or
    "C:\Users\marka".
  - scripts/_archive/README.md exists at
    C:\Users\marka\code\mt-integration-2026-06-22\scripts\_archive\README.md,
    read directly from disk, with non-trivial content describing the
    archive convention.
  - `git cat-file -e main:_patch-site-search-tag.mjs` fails (the root copy no
    longer exists on main — it was git-mv'd away, not duplicated).
  - `git merge-base --is-ancestor 375e40b0db479ede0e14407e6eb416e85936d305 main`
    and `git merge-base --is-ancestor 5f19dc00fe16cdbe94a0405c1089ee50259bcaf4 main`
    both report true after this mission's steps.
  - docs/QC-PIPELINE-SOTA-2026-06-23.md exists on main with its original
    content intact (unchanged by this mission) — confirmed present, not
    duplicated or overwritten.
  - `node --check` exits 0 on scripts/_archive/_patch-site-search-tag-2026-06-23.mjs,
    and the receipt states how many total .js/.mjs files were checked (one,
    unless a conflict resolution touched another).
  - `git status --short` on every ALLOW-FILES path prints nothing once this
    mission's steps are complete. Any OTHER pre-existing untracked or
    modified file already present in the target repo before this mission
    started (documented in Context above) is outside this mission's
    ALLOW-FILES scope and is not this mission's concern.
  - No .html file was added, modified, or deleted by this mission's steps
    (confirmed via `git show --stat` on the landing commit) — no visual-QC
    render-witness gate applies to this mission.
  - If a real conflict was resolved in step 3/4, the committed result
    contains BOTH main's pre-existing unrelated content and this feature's
    complete changes — neither side silently dropped.
  - This Done-means covers integration mechanics only: the files intact and
    committed, a clean or correctly-resolved history, passing syntax checks,
    and a scoped-clean tree. It makes no claim that the archived script's
    original patch behavior was correct, that the scrubbed local-path
    replacement text is adequate, or that the archive-README's convention is
    sound — that determination belongs entirely to this mission's own
    witness-and-verdict-panel run downstream, not to this mission's
    authorship or mechanical execution.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

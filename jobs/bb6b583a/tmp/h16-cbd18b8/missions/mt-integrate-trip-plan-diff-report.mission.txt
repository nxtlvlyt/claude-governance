MISSION mt-integrate-trip-plan-diff-report
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: main
ALLOW-FILES:
  - functions/api/trip-plan.js
REQUIRES: none

Niyyah: The feature "mt-feat-trip-plan-diff-report" (commit 5b9d092, "feat(trip-plan):
multi-week trip planner — GET/POST /api/trip-plan", sitting in the source worktree
C:\Users\marka\code\mt-feat-trip-plan) has never been integrated into this repo's
TARGET-BRANCH. This mission's entire job is mechanical integration — land the commit
onto main's current tip without losing either side's changes, and prove the result is
syntactically sound and committed. Nothing this mission does constitutes a judgment
that the trip planner's own CODE is correct, safe, or behaves as designed (e.g. that
the scenic/fast/economical style logic picks sane waypoints and overnight POIs, that
the in-Function boondockScore() derivation is a sound proxy given D1's actual pois
columns, that the 1..28-day budget math, rest-day cadence, or fuel-cost lookups are
right). That determination belongs to this mission's own witness + verdict-panel run,
which inspects the integrated result after this mission produces it — not to the act
of integration itself, and not to authoring this mission. Authoring or executing this
mission is not verification of the feature's code.

Maqsad: The commit "5b9d092 feat(trip-plan): multi-week trip planner — GET/POST
/api/trip-plan" is cherry-picked or merged from the source worktree onto TARGET-BRANCH
main of C:\Users\marka\code\mt-integration-2026-06-22, with any conflict resolved by
keeping BOTH sides — this repo's existing unrelated changes AND this feature's new
file, never silently dropping either — every .js/.mjs file this integration touches
passes `node --check`, the result is committed with a clear message, and the target
repo's working tree is left clean with respect to everything this mission touches.

Context (verified against live substrate 2026-07-01, absolute paths, no prescribed
resolution beyond "keep both sides"):
- NAMING CORRECTION: the feature's slug is "mt-feat-trip-plan-diff-report" but no
  worktree by that exact name exists on disk. `git worktree list` run this session
  shows the actual source worktree is C:\Users\marka\code\mt-feat-trip-plan on branch
  feat/trip-plan, tip commit 5b9d092 — confirmed by `git log --oneline -3` inside that
  worktree, whose top commit's hash and full message match the feature data exactly.
  Steps below read from C:\Users\marka\code\mt-feat-trip-plan, the verified real path,
  not the slug-shaped name.
- Confirmed diff stat for 5b9d092 (`git show --stat`, this session, inside the source
  worktree): 1 file changed, 902 insertions(+), 0 deletions(-) — functions/api/trip-plan.js
  is a brand-new file; no existing file is modified. Per the commit message: it extends
  the existing route-multiday.js (1..7 day) engine to a 1..28-day multi-week budget with
  three style preferences (scenic/fast/economical) governing overnight-POI selection via a
  derived, in-Function boondockScore() (0..100, not a stored D1 column), rest-day cadence,
  and fuel-cost lookups; GET /api/trip-plan?start=&end=&days=&style= and an equivalent POST
  both return {days[], totals{...}, generated, warnings}. Helpers (haversine, bboxKm,
  pickPoint, clamp, round1/round2, dateOffsetIso, inferProvinceFromName, fetchFuelPrices)
  are duplicated in-file rather than imported, per the commit message's own note that
  Pages Functions cross-file imports are fragile in this repo. No .html file is touched.
- Target repo TARGET-BRANCH main, this session's HEAD (`git rev-parse HEAD` /
  `git log -1 --oneline`): 5e19874 "Merge branch
  'feat/qc-concern-vehicle-profile-picker-2026-06-25'". Confirmed via
  `git cat-file -e main:functions/api/trip-plan.js` (fails — path does not exist) that
  the file does not exist on main yet. `git ls-tree HEAD functions/api/` this session
  shows the directory already holds 19 entries including route-multiday.js (the F8
  engine this feature extends) and plan-day.js, so the new file lands alongside
  existing siblings, not as a directory collision.
- The source worktree and the target repo are worktrees of the SAME underlying git
  repository — `git rev-parse --git-common-dir` resolves to the identical path
  (C:/Users/marka/code/muddytires-pages/.git) from both locations, confirmed this
  session. Commit 5b9d092 is therefore already a reachable object in the target repo
  with no fetch/remote-add step required; `git cherry-pick 5b9d092` works directly.
- A dry run performed this session in the target repo (`git cherry-pick -n 5b9d092`,
  then `git reset --hard HEAD` to undo it before this mission was authored) applied
  cleanly with ZERO conflicts — `git status --porcelain` showed exactly
  `A  functions/api/trip-plan.js` and nothing else staged or modified. If a collision
  is nonetheless reported at fire time (main having since gained its own
  functions/api/trip-plan.js), resolve it by keeping BOTH files' content rather than
  overwriting either.
- Target repo working tree this session (`git status --short`) was fully clean on
  main — no pre-existing untracked litter to route around, unlike some other
  integration missions in this family. Only `.wrangler/`, `node_modules/`,
  `package-lock.json`, and `package.json` show as ignored, none of which this mission
  touches.
- `node --check functions/api/trip-plan.js`, run this session directly against the
  source worktree's copy of the file (Node v22.20.0), exits 0 — the file is
  syntactically valid independent of the integration step.
- This diff touches zero .html files, so no visual-QC header or render-witness step is
  required by this mission (the visual-QC header some other integration missions carry
  is deliberately not set here). It touches exactly one .js file, a new add, so the
  node --check requirement below is expected to cover precisely that file unless a
  conflict resolution forces a change to some other .js/.mjs file this repo already
  has.
- None of the above — the diff stat, the clean dry-run, the endpoint contract and
  style/derivation logic described in the commit message — has been verified by
  running the actual endpoint, a witness, or a test; it is reporting diff contents and
  a dry-run git mechanics check only. That the code genuinely behaves as the commit
  message claims is exactly what this mission's own downstream witness +
  verdict-panel run is for.

Steps:
  1. In the source worktree C:\Users\marka\code\mt-feat-trip-plan, confirm commit
     5b9d092 exists and capture its patch (e.g. `git show 5b9d092` or
     `git format-patch -1 5b9d092`) without modifying that worktree. [command]
     capture-patch
  2. In the target repo C:\Users\marka\code\mt-integration-2026-06-22, check out
     TARGET-BRANCH main at its current HEAD, then apply the captured commit via
     `git cherry-pick 5b9d092` (the commit is already a reachable object in this repo
     — no remote/fetch step is needed) or an equivalent merge of that single commit.
     [command] integrate-commit
  3. If the cherry-pick reports a conflict on functions/api/trip-plan.js or any other
     path, resolve each conflicted hunk by hand so the resolved content contains BOTH
     the target's pre-existing content AND this feature's new content — never
     `--ours` / `--theirs` and never a resolution that deletes either side's lines.
     [edit] resolve-conflicts
  4. Confirm which files this integration actually touched or conflict-resolved
     (`git diff --name-only` against the merge-base) and filter to *.js/*.mjs; run
     `node --check` on every such file (expected: functions/api/trip-plan.js) and
     confirm each exits 0. [command] node-check-all
  5. Stage and commit the integrated result on TARGET-BRANCH main with a clear
     message identifying the integrated feature and its origin commit (e.g.
     referencing "mt-feat-trip-plan-diff-report" / "trip-plan" and "5b9d092"). Confirm
     `git status --porcelain` afterward shows no modified or staged entries related to
     this mission's touched files. [command] commit-and-verify-clean

Done means:
  - `git log` on TARGET-BRANCH main in C:\Users\marka\code\mt-integration-2026-06-22
    shows a commit whose message names "trip-plan" and/or "5b9d092".
  - functions/api/trip-plan.js exists in the target repo at TARGET-BRANCH main's HEAD
    with non-trivial content (read the file content to confirm, not assumed): a
    Cloudflare Pages Function implementing GET and POST handlers for /api/trip-plan
    that accept a 1..28 day budget and a scenic/fast/economical style parameter, select
    waypoints and overnight POIs against the D1 pois table, and return a
    {days[], totals{...}, generated, warnings} shaped response.
  - If a conflict occurred during integration, both sides' content are confirmed
    present in the resolved file by reading the resolved lines, not assumed.
  - `node --check` exits 0 on functions/api/trip-plan.js, and on any other .js/.mjs
    file this integration touched or conflict-resolved.
  - `git status --porcelain` in the target repo, on TARGET-BRANCH main, shows no
    modified or staged entries at the end of the mission.
  - Nothing in this mission's own receipts is treated as proof the trip planner's code
    is correct — not the style-based waypoint/overnight selection, not the
    boondockScore() derivation, not the day-budget or rest-cadence math, not the fuel-
    cost lookups. That judgment is deferred entirely to this mission's downstream
    witness and verdict-panel run against the integrated result.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

MISSION: stitch-design-mastery — Stitch capability census + DESIGN.md draft, beyond the conductor's session (part-file shape)
MISSION-CLASS: research  # validator mode: .md deliverables count as targets (verification intent stated in Maqsad)

Niyyah: The operator asked (2026-06-12) whether the automated system can learn Stitch as deeply as the conductor did interactively — or deeper. The conductor's session knowledge is banked at E:\AI_Storage\website-pipeline\apps\post-intake\stitch\STITCH-KNOWLEDGE.md, but a banked card is a snapshot of one session, unverified by anyone but its author. This mission re-derives the capability census from the LIVE endpoint (the cheap metadata calls only), audits the card against it, and authors the two artifacts the self-healing design loop still lacks: a ratified-path DESIGN.md draft and the design-contract checker spec. Live screen GENERATION stays conductor-driven — those calls spend the operator's Stitch quota and his taste gates them — so this mission proves a seat can master the API surface without spending a single generation.

Maqsad: ONE assembled artifact — stitch-mastery.md at the sandbox root, built from FOUR part files, plus DESIGN-draft.md beside it. PLAN SHAPE: (1) COMMAND gather steps stage the evidence, each shaped EXACTLY as: if (Test-Path '<src>') { Copy-Item '<src>' '<dst>' } — sources: E:\AI_Storage\website-pipeline\apps\post-intake\stitch\STITCH-KNOWLEDGE.md, E:\AI_Storage\website-pipeline\apps\post-intake\design-contract.md, E:\AI_Storage\website-pipeline\apps\post-intake\stitch\stitch-library-v2.html; (2) COMMAND probe steps record evidence WITHOUT any generation: node E:\AI_Storage\website-pipeline\apps\post-intake\stitch\stitch_tools_full.mjs > tools-live.txt (initialize + tools/list only — metadata, zero quota); AND node E:\AI_Storage\website-pipeline\apps\post-intake\stitch\vision_qc.mjs E:\AI_Storage\website-pipeline\apps\post-intake\stitch\stitch-library-v2.png > vision-qc.txt (LOCAL nemotron3:33b inspects the existing v2 screenshot against the 6 design-contract checks — operator ruling 2026-06-12: QC of an existing generation costs zero quota and missions MUST have it; the script GR10-waits for the GPU); then ONE command step produces tools-live.numbered.txt, knowledge.numbered.txt, and vision-qc.numbered.txt by prefixing each line with Lnn (the line-cite bar requires the numbered sources); (3) FIVE SEPARATE edit steps author one part file each, every part a SMALL emission: part-1-census.md = every tool the live endpoint exposes with its required arguments, each row citing [tools-live.numbered.txt Lnn] and flagged VERIFIED-LIVE (in both dump and card), CARD-ONLY (in the card but not the dump), or NEW (live but absent from the card — these are the "can it learn more" findings); part-2-design-md.md = a complete DESIGN.md draft for "NXTLVL Post" ready for upload_design_md (dark palette #10151a / #1a222b / accent #0d9488 / text #e8edf2; typography hierarchy; named components: batch row, circular hook-score badge with green/amber/red bands, platform card with Copy action, archive control, floating upload button), every component citing [knowledge.numbered.txt Lnn] or the v2 HTML by line; part-3-checker.md = implementation spec for check_design_contract.mjs (the 6 mechanical checks from design-contract.md as greps over a Stitch HTML export, nonzero exit on any failure, output formatted as an edit_screens repair prompt — the missing piece that closes the self-healing loop); part-4-vision-qc.md = the local VLM's verdict on the v2 design, every PASS/FAIL statement citing [vision-qc.numbered.txt Lnn], plus a one-paragraph comparison against the conductor's mechanical HTML grep (a visual check and a markup check can disagree — name any disagreement, never paper over it); part-5-verdict.md = verdict whose label is CARD-COMPLETE or CARD-GAPS-<list> where <list> names EVERY gap the census established (a label naming fewer gaps than the body is a contract violation), each gap carrying its tools-live cite, ending with the SINGLE next concrete call the conductor should make; (4) ONE command step assembles: writes a '# Stitch Mastery — capability census and design artifacts' header then concatenates part-1 through part-5 into stitch-mastery.md, and copies part-2's DESIGN.md body to DESIGN-draft.md at the sandbox root; (5) ONE command step validates with Select-String that stitch-mastery.md contains all five section parts and either CARD-COMPLETE or CARD-GAPS, and that DESIGN-draft.md exists with length >= 2000 bytes. Done means: stitch-mastery.md exists at the sandbox root, validation confirms the five parts and an evidence-bounded verdict label matching the body's gap list exactly, DESIGN-draft.md >= 2000 bytes, every census/gap/QC claim cites a numbered source, and NO step performed a screen generation, edit, or variant call (vision QC reads the EXISTING png; metadata calls only against Stitch).
Context: live calls limited to initialize + tools/list via the existing bridge script (zero generation quota; ADC creds already on this machine). Read-only sources; sandbox writes only. No installs.
Amanah: entrusted; discharge with ihsan.

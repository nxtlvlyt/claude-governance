# BLOCKED pending engine batch — recursion-guard slice-ceiling bug in groupSteps/buildSubMissionText. Parent splits into S1 (8 steps, at ceiling) + S2; S1 re-decomposes to 9 steps which trips the recursion guard ("refusing to re-split a child"). 119 plans, 0 steps executed in mission lifetime. Fix is engine-side: smaller first-level slice so child re-decomp has headroom under ceiling. See conduct-cycle.mjs ledger entries 2026-06-23 17:28Z → 2026-06-24 03:52Z.
MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: feat/b13-sitemap-prune-2026-06-23
ALLOW-FILES:
  - workers/sitemap-prune/*.js
  - workers/sitemap-prune/wrangler.toml

Niyyah: the sitemap-prune worker should survive production load. The original build assumed Cloudflare Workers limits the panel measured exceeded by 8x, and uncaught D1 awaits would surface as CF 1101 errors to users. Close the substrate-verified defects — not the model's confidence, the documented Cloudflare ceiling.

Maqsad: the worker runs safely on Cloudflare Workers Free plan AND Paid plan, with no silent failure modes. Re-running the 5-seat panel against the new diff returns panel_verdict in {pass, concern}.

Context (substrate paths to read, not a recipe):
  - Panel findings: C:\Users\marka\AppData\Local\Temp\claude\C--Users-marka\4c4a63ee-b0e6-4c65-9e09-561a76fbb9b0\tasks\wdsq4mxb2.output (results[3])
  - The branch: github/feat/b13-sitemap-prune-2026-06-23 (sha 530457f-class)
  - Cloudflare Workers documented subrequest limits: 50 (Free) / 1000 (Paid) per request
  - QC panel harness: C:\Users\marka\code\mt-audit\qc-harness-v2.mjs

Panel findings (the deeds to close, verbatim):
  1. upsert404 + writeCursor awaited inside handleRefresh without try/catch — D1 error becomes uncaught throw (CF 1101 instead of JSON error)
  2. Top-level fetch() handler has no try/catch — uncaught throws → CF 1101 not structured JSON
  3. ensureSchema catch blocks silently swallow ALL errors and set schemaEnsured=true unconditionally
  4. BATCH_SIZE=200 — up to ~401 fetches per /refresh exceeds CF Free subrequest limit (50)
  5. (concern) Regex-based sitemap parsing may miss CDATA / namespaced XML
  6. (concern) Race condition on cursor state — concurrent /refresh interleave
  7. (concern) Sitemap URLs not validated to stay within site domain — SSRF risk on maliciously-edited sitemap

Done means:
  - The worker, when invoked, returns structured JSON on D1 failure rather than CF 1101.
  - Per /refresh subrequest count is mathematically provable to stay under 50 (Free plan ceiling).
  - ensureSchema does NOT mask unknown errors as "already exists".
  - Concurrent /refresh calls either serialize cleanly OR return a structured conflict response (no race-corrupted cursor).
  - URL host validation rejects out-of-domain sitemap entries.
  - Same panel re-fire returns pass/concern; node --check passes; push + ls-remote verify.

Amanah: yours to reason the closure. The chain picks the right primitives — try/catch shapes, cursor-locking strategy, BATCH_SIZE cap and continuation pattern. Substrate paths name what's wrong; the chain finds what's right.

Constraints: per-file git add, --no-verify, NO LOCAL OLLAMA, Sonnet as executor.

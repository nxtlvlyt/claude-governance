MISSION QC-CONCERN-POI-AFFILIATE-CARDS-POI-AFFILIATE-CARDS-JS-2026-06-25
MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.QC-CONCERN-POI-AFFILIATE-CARDS-POI-AFFILIATE-CARDS-JS-2026-06-25
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: feat/qc-concern-poi-affiliate-cards-poi-affiliate-cards-js-2026-06-25
ALLOW-FILES:
  - map.html
  - js/poi-affiliate-cards.js
  - scripts/e2e-runner.mjs
  - docs/FEATURE-CATALOG-2026-06-23.md
REQUIRES: none

Niyyah: address the QC CONCERN named "POI affiliate cards (poi-affiliate-cards.js)" from the 2026-06-25 walk.

Maqsad: The 2026-06-25 walk recorded three FAILs for this feature — GATE: Affiliate card
appears in popup (FAIL: no POI popup rendered in the final screenshot); GATE: click fires
`dataLayer.push` (FAIL: no card present to interact with); GATE: click fires a `plausible`
event (FAIL: same reason) — with the recorded diagnosis "the walk's interaction step
failed to open a POI popup, so the affiliate card hook never had a chance to fire; the
runner heuristic does not currently click a POI marker." That diagnosis is true but
incomplete. This mission's job is to make all three GATEs mechanically exercisable and
passing on TARGET-BRANCH, by fixing every layer that would still block them even after a
perfect runner fix, not just the runner. Restore the feature to the behavior documented in
its own source (js/poi-affiliate-cards.js's header comment and the commit that introduced
it, 63f6929) — docs/FEATURE-CATALOG-2026-06-23.md currently has no entry for this feature
at all to restore "to", so this mission also adds one.

Context (verified against live substrate this session, 2026-07-01, absolute paths, no
prescribed solution beyond what each numbered fact already narrows):
1. js/poi-affiliate-cards.js exists on TARGET-BRANCH (263 lines, passes node --check). It
   is a complete, self-contained IIFE that wraps window.popup(p) to splice a `.mt-aff-card`
   block into the popup HTML for five node_type values (water_fill, dump_station, campsite,
   overnight, propane), and separately binds a document-level delegated click listener that
   calls window.dataLayer.push and window.plausible on any `.mt-aff-card-link` click. Both
   behaviors live inside one IIFE gated on document.readyState — none of this code runs at
   all unless the browser actually loads the file.
2. NEW ROOT CAUSE, not identified by any prior review of this concern: map.html on
   TARGET-BRANCH never loads js/poi-affiliate-cards.js — there is no script tag for it
   anywhere in the file (zero matches for the string "poi-affiliate-cards"). The file's own
   header comment documents an intended load order ("account -> add-spot-worker -> reviews
   -> poi-provenance -> poi-photos -> poi-affiliate-cards -> mobile-sheet"), and that exact
   script tag does exist, in that exact position, on the original feature branch
   feat/m-affiliate-context-inject — immediately after the poi-photos.js tag and immediately
   before the mobile-sheet.js tag. This is a confirmed integration gap: the JS file landed on
   TARGET-BRANCH, the one script tag that mounts it did not. Without it, GATE 1 fails
   unconditionally regardless of any runner fix — no wrap installs, no listener binds.
3. NEW ROOT CAUSE: across every tracked .html and .js file on TARGET-BRANCH, the strings
   "plausible" and "dataLayer" appear ONLY inside js/poi-affiliate-cards.js itself — no
   script anywhere on this branch defines window.dataLayer or window.plausible. Both calls
   in trackClick() are already defensively guarded (they check the global exists before
   calling it), so they never throw — they silently do nothing. GATE 2 and GATE 3 cannot
   mechanically pass today even after fact 2 is fixed and the runner clicks the card, unless
   poi-affiliate-cards.js is made self-sufficient for its own two calls.
4. CONFIRMED-STILL-LIVE version of the originally diagnosed issue: scripts/e2e-runner.mjs is
   the real, only e2e QC runner in this repo's entire history (workers/e2e-runner.js has
   never existed at any commit, in any branch — every prior architect session assuming that
   path was wrong). Its interaction-step parser in exerciseFeature() recognizes exactly two
   verbs today: `click <CSS selector>` and `wait <ms>`. Neither can target a POI marker,
   because POI markers are Leaflet L.circleMarker layers positioned dynamically from fetched
   data with no stable CSS selector per marker — there is no selector any catalog author
   could write that this parser could use to open a specific POI's popup. This is the literal
   mechanism behind "the runner heuristic does not currently click a POI marker."
5. docs/FEATURE-CATALOG-2026-06-23.md exists on TARGET-BRANCH (258 lines) but has zero
   occurrences of the word "affiliate" — no "### Feature: POI affiliate cards" section has
   ever existed. scripts/e2e-runner.mjs's parseCatalog() only ever tests `### Feature:`
   sections found in this file; with none, this feature's features_run count is permanently
   0 no matter what else this mission fixes.
6. Real POI seed data (pois.json, 4000 features) confirms plenty of markers exist with
   node_type values matching the RULES map in js/poi-affiliate-cards.js: campsite=3040,
   water_fill=587, propane=5, dump_station=14 (scenic=226 and shower=76 have no matching
   rule). campsite is by far the most numerous and least likely to need panning/zooming to
   find on a fresh map load.
7. window.map is a genuine top-level global (`var map=L.map('map',{...})` at script scope in
   map.html), reachable from a Playwright page.evaluate() call for a marker lookup. Real
   (non-community) POI markers are bound via `.bindPopup(popup(p))`, where `popup` is exactly
   the function js/poi-affiliate-cards.js wraps.
8. scripts/e2e-runner.mjs's own runtime dependencies (playwright, and
   C:\Users\marka\.claude\muezzin-plugin\ollama_vision_verdict.mjs) are both present and
   working this session — a report already exists from a same-day run against a different
   set of features, confirming the pipeline itself runs end to end today.

Steps:
  1. On TARGET-BRANCH's current HEAD (check out fresh — this worktree is shared across many
     missions and may have moved since this text was written), re-run the checks behind
     Context facts 2, 3, 4 and 5 live: confirm map.html still has zero occurrences of
     "poi-affiliate-cards"; confirm no tracked .html/.js file besides
     js/poi-affiliate-cards.js defines window.dataLayer or window.plausible; check whether
     scripts/e2e-runner.mjs is committed on this branch at all (`git log -- scripts/e2e-runner.mjs`
     on TARGET-BRANCH) — if it is not, the working tree's on-disk copy is the only source and
     must be committed as its own baseline commit before any functional change, so the fix
     has real history and is not lost if this worktree gets reused by another mission; and
     confirm docs/FEATURE-CATALOG-2026-06-23.md still has zero occurrences of "affiliate". Do
     not proceed past this step on anything assumed from this mission text alone. [command]
     reconfirm-facts-before-editing
  2. In map.html, insert `<script src="js/poi-affiliate-cards.js" defer></script>`
     immediately after the existing `<script src="js/poi-photos.js" defer></script>` tag
     (that exact tag appears exactly once on TARGET-BRANCH — anchor on it). Touch no other
     script tag and no other line in the file. [edit] wire-script-tag-into-map-html
  3. In js/poi-affiliate-cards.js's trackClick(), before the two guarded analytics calls,
     initialize `window.dataLayer = window.dataLayer || [];` so the push always lands
     somewhere observable instead of no-op'ing when no GTM container is loaded, and install
     Plausible's own documented pre-load queue shim
     (`window.plausible = window.plausible || function(){(window.plausible.q = window.plausible.q || []).push(arguments);}`)
     ONLY if window.plausible is not already a function, so a real Plausible script loading
     later drains the same queue instead of being overwritten. Do NOT add a `<script src>`
     tag for Plausible or GTM to any HTML file in this mission — that is a separate, larger,
     site-wide analytics-provisioning decision outside this mission's scope; this step only
     makes poi-affiliate-cards.js's own two calls mechanically observable. [edit]
     harden-analytics-self-init
  4. In scripts/e2e-runner.mjs's exerciseFeature() interaction-step loop, add a new
     heuristic branch alongside the existing `click <selector>` / `wait <ms>` branches that
     recognizes a step of the form `open popup for node_type <type>` and executes it via
     page.evaluate(): walk window.map's layers (map.eachLayer), find the first layer whose
     `_poi.node_type` equals `<type>`, and call `.fire('click')` on it — the same call
     Leaflet's own internal handler makes on a real user click, so this is a faithful
     simulation and not a synthetic shortcut. Wait briefly for the popup DOM to attach before
     the existing screenshot step runs. If step 1 found scripts/e2e-runner.mjs uncommitted on
     TARGET-BRANCH, commit the working-tree baseline first, then apply this addition as a
     separate, reviewable commit. [edit] add-marker-click-capability-to-runner
  5. In docs/FEATURE-CATALOG-2026-06-23.md, add a new `### Feature: POI affiliate cards`
     section (placed under "Priority A", before the "TODO" section) following the file's own
     "Catalog template" field structure exactly: Page `https://muddytires.ca/map`; Loaded by
     `js/poi-affiliate-cards.js`; Pre-state required "any POI with a matching node_type
     visible on the map — campsite is most reliable, see Context fact 6"; Selectors
     (Target/result: `.mt-aff-card`, `.mt-aff-card-link`); Interaction sequence
     (`1. open popup for node_type campsite`, `2. click .mt-aff-card-link`); Expected
     behavior ("DOM: popup HTML contains .mt-aff-card"; "window.dataLayer array contains an
     entry with event 'affiliate_click' after the click"; "window.plausible.q (or a direct
     call record) contains an 'Affiliate Click' entry after the click"); Expected visual
     ("the inline disclosure text is visible on the card, not hidden or clipped"; "the card
     does not visually collide with the reviews link below it"); and a Regression-history
     line noting the 2026-06-25 walk's three FAILs and this mission's date. [edit]
     catalog-poi-affiliate-cards-feature
  6. Run `node scripts/e2e-runner.mjs --target <the same target/preview URL the 2026-06-25
     walk used, or a fresh preview deploy of TARGET-BRANCH> --features poi-affiliate-cards`
     and read the resulting JSON report: confirm the result's agyVerdict is "clean", confirm
     its stepLog shows a real popup-open and a real click (not FEATURE_FAILED and not a
     "skipped, no parser for" entry), and open the actual PNG screenshots this run wrote to
     e2e-shots/ (not just the exit code) to confirm the card is visually present. [command]
     run-single-feature-walk-and-confirm-clean
  7. Run the same command with no --features filter (the full catalog) both on
     TARGET-BRANCH's pre-mission HEAD (before step 2's edit) and again after all edits land,
     and diff the two reports' per-feature agyVerdict values: every feature that was "clean"
     or "concern" before must not become "block" after. Quote the concrete before/after
     per-feature counts in this mission's own report rather than only reporting the new
     feature's result. [command] run-full-walk-and-confirm-no-regression

Done means:
  - map.html contains `<script src="js/poi-affiliate-cards.js" defer></script>` exactly
    once, positioned after the js/poi-photos.js tag and before the js/mobile-sheet.js tag.
  - node --check on js/poi-affiliate-cards.js and node --check on scripts/e2e-runner.mjs
    both exit 0.
  - docs/FEATURE-CATALOG-2026-06-23.md contains a `### Feature: POI affiliate cards`
    section, confirmed by reading the on-disk file, not by trusting a command's exit code
    alone.
  - A real headless-browser (Playwright) run of `node scripts/e2e-runner.mjs --features
    poi-affiliate-cards` produces a report whose result for this feature has
    `agyVerdict: "clean"`, with a stepLog that shows an actual marker click and popup open
    (not a FEATURE_FAILED or skipped-step entry), and screenshots in e2e-shots/ that show the
    `.mt-aff-card` block rendered inside the popup.
  - The same full-catalog walk run before and after this mission's edits shows no feature
    regressing from clean/concern to block — the concrete before/after counts are quoted in
    the mission's own report.
  - This Done-means resolves the three original 2026-06-25 GATE fails (card-in-popup,
    dataLayer.push, plausible event) at their real root causes — missing script-tag wiring,
    an analytics no-op with no provider loaded, the runner's missing marker-click verb, and a
    missing catalog entry — not merely by asserting the pre-existing product code "looks
    fine," which is true of the JS file in isolation but was never sufficient on its own.

Context: Sandbox writes only, target repo working tree only.
Amanah: this slice is part of the muddytires.ca quality-uplift backlog; entrusted, discharge with ihsan.

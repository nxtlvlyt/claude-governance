MISSION mt-integrate-poi-dedup-audit
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - scripts/dedup-audit.mjs
  - docs/dedup-report-2026-06-23.md
  - scripts/dedup-admin-todos-2026-06-23.sql
REQUIRES: none

Niyyah: The feature "mt-feat-poi-dedup-audit" (commit c899d07, sitting in the source
worktree C:\Users\marka\code\mt-feat-poi-dedup-audit) has never been integrated into this
repo's tree. This mission's entire job is mechanical integration — land the commit onto
the target repo without losing either side's work, and prove the result is syntactically
sound and committed cleanly. Nothing this mission does, and nothing about this mission's
own authorship, constitutes a judgment that the dedup-audit feature's CODE is correct —
not whether the 50m haversine distance cutoff is the right radius, not whether the
Jaccard/Levenshtein-hybrid name-similarity threshold of 0.70 correctly separates true
duplicates from merely-similar-named distinct POIs, not whether the union-find clustering
into 2,230 clusters is sound, not whether the generated admin_tasks INSERT SQL is safe to
ever run (the report itself warns not to bulk-apply it as-is), and not whether the
reported counts (80,121 pois rows, 4 community_spot rows, 5,490 poi-vs-poi candidate
pairs, 0 spot-vs-spot, 0 cross-table) are themselves accurate. That determination belongs
entirely to this mission's own witness + verdict-panel run, which inspects the integrated
result AFTER this mission produces it. Authoring or executing this mission is not
verification of the feature.

Maqsad: The commit "c899d07 feat(dedup-audit): scan pois + community_spot for
near-duplicates" is cherry-picked or merged from the source worktree onto the current
HEAD of C:\Users\marka\code\mt-integration-2026-06-22, with any conflicts resolved by
keeping BOTH sides — this repo's existing unrelated changes AND the dedup-audit feature's
changes, never silently dropping either — the one changed .mjs file passes
`node --check`, the result is committed with a clear message, and the target repo's
working tree is left clean (git status shows no uncommitted changes to the touched
files).

Context (verified against live substrate this session, absolute paths, no prescribed
resolution beyond "keep both sides"):
- Source worktree: C:\Users\marka\code\mt-feat-poi-dedup-audit, branch
  feat/poi-dedup-audit, HEAD already sitting exactly on
  c899d078e9e78894c309189ab22a9a0e57e03b20 (confirmed via `git log --oneline -3` showing it
  at the tip) — do not modify that worktree, only read/cherry-pick FROM it.
- Confirmed diff stat for c899d078e9e78894c309189ab22a9a0e57e03b20 (`git show --stat`, this
  session): 3 files changed, 74309 insertions(+), 0 deletions(-), and ALL THREE are new
  additions with no existing-file modifications: scripts/dedup-audit.mjs (714 new lines, a
  Node script scanning the pois and community_spot D1 tables for near-duplicate POIs by
  distance/node_type/name-similarity and producing a report plus admin-task SQL with no
  auto-merge), docs/dedup-report-2026-06-23.md (49,037 new lines, the generated
  human-readable audit output), and scripts/dedup-admin-todos-2026-06-23.sql (24,558 new
  lines, generated INSERT statements for admin_tasks decision rows, one per cluster).
  `node --check scripts/dedup-audit.mjs` was run against the source worktree's copy this
  session and exits 0.
- Target repo (this session, `git log --oneline -1` in
  C:\Users\marka\code\mt-integration-2026-06-22): HEAD a1d6ad9, checked out branch
  test-lhci-dryrun-2026-07-01. Confirmed by direct file check this session: none of the
  three ALLOW-FILES paths above exist anywhere in the target tree yet, so a cherry-pick is
  expected to apply as a pure add with zero conflicts. If the target tree has since gained
  its own file at any of these three paths by the time this mission fires, that is a
  changed condition from this authoring session — resolve any resulting conflict by
  keeping BOTH sides' content (the target's pre-existing lines AND the feature's added
  lines), never `--ours` / `--theirs` and never a resolution that drops either side.
- The commit object c899d078e9e78894c309189ab22a9a0e57e03b20 is already present in the
  target repo's own object store (confirmed via `git cat-file -t` returning "commit" this
  session, since worktrees under C:\Users\marka\code share object storage) — cherry-pick
  can reference the SHA directly with no fetch/remote-add step needed.
- REPO VOLATILITY OBSERVED: other integration missions against this same target repo have
  recorded its checked-out branch and HEAD moving between consecutive commands with no
  action from the observing session. No TARGET-BRANCH header is set here, deliberately:
  this mission's job is to land the commit onto whichever branch/HEAD the target repo has
  checked out AT FIRE TIME ("the current HEAD"), not onto a hardcoded branch name. Do not
  check out or switch branches as a step of this mission; integrate in place on whatever
  tree is already checked out, and re-confirm the checked-out branch/HEAD live as the
  first step rather than trusting the branch/HEAD recorded above.
- The target repo's working tree currently carries several UNTRACKED files unrelated to
  this feature (api-index-response.json, docs/e2e-report-2026-07-01.json, e2e-shots/,
  feature-catalog-entry.txt, scripts/e2e-runner.mjs — leftovers from other work). These are
  not part of ALLOW-FILES and must be left exactly as found; do not stage, commit, or
  delete them as part of this mission's own commit.
- A dry-run cherry-pick of c899d07 performed this session (`git cherry-pick --no-commit
  -n`, then fully reverted via `git reset --hard HEAD` before this mission file was
  written, leaving the target repo exactly as found — the untracked files above were
  present before and after) applied with zero conflicts (three clean adds, exit 0) against
  the HEAD observed at that moment. This is evidence the integration is expected to be
  mechanically clean, not a guarantee — the branch-race risk above means the tree this
  mission actually meets at fire time may differ.
- This diff touches zero .html files (confirmed: none of the three changed files end in
  .html) — the visual-QC render-witness header used by other integration missions is
  intentionally left off this mission's header block, and no render-witness step is
  required or expected.
- Only scripts/dedup-audit.mjs is JavaScript among the three changed files; there are no
  other .js/.mjs files in this diff. `node --check` must still be run on any additional
  .js/.mjs file that conflict resolution ends up touching, if any.

Steps:
  1. In the target repo, confirm the actually-checked-out branch and HEAD
     (`git -C C:\Users\marka\code\mt-integration-2026-06-22 branch --show-current` and
     `git log --oneline -1`), and confirm none of the three ALLOW-FILES paths already
     exist on disk. Do not switch branches. [command] confirm-target-state
  2. In the source worktree C:\Users\marka\code\mt-feat-poi-dedup-audit, confirm commit
     c899d078e9e78894c309189ab22a9a0e57e03b20 exists (`git show c899d07`) without modifying
     that worktree. [command] capture-patch
  3. In the target repo, at whatever branch/HEAD is currently checked out (no branch
     switch), apply the commit via
     `git -C C:\Users\marka\code\mt-integration-2026-06-22 cherry-pick
     c899d078e9e78894c309189ab22a9a0e57e03b20` or an equivalent merge of that single
     commit. [command] integrate-commit
  4. If the cherry-pick (or equivalent merge) reports conflicts on any ALLOW-FILES entry
     (not expected, since all three are pure new adds) or on any other file, resolve every
     conflicted hunk by hand so the resolved content contains BOTH the target's
     pre-existing content AND the dedup-audit feature's content — never `--ours` /
     `--theirs` and never a resolution that deletes either side's content.
     [edit] resolve-conflicts
  5. Run `node --check scripts/dedup-audit.mjs` from the target repo root and confirm it
     exits 0; run `node --check` on any other .js/.mjs file the merge touched or
     conflict-resolved. [command] node-check-all
  6. Stage and commit the integrated result in the target repo with a clear message
     identifying the integrated feature and its origin commit (e.g. referencing
     "mt-feat-poi-dedup-audit" and/or "c899d07"), staging only the three ALLOW-FILES paths
     (and any other files conflict resolution touched) — not the pre-existing unrelated
     untracked files. Confirm `git status --short` afterward shows a clean working tree
     for the touched files (the pre-existing unrelated untracked files may remain
     untracked, unchanged from before this mission ran).
     [command] commit-and-verify-clean

Done means:
  - `git log` in C:\Users\marka\code\mt-integration-2026-06-22 shows a commit containing
    the integrated dedup-audit feature, with a commit message that names
    "mt-feat-poi-dedup-audit" and/or "c899d07".
  - scripts/dedup-audit.mjs exists in the target repo at HEAD with non-trivial content
    (714 lines implementing the D1 pois/community_spot near-duplicate scan: haversine
    distance <=50m, node_type match, Jaccard/Levenshtein-hybrid name similarity >=0.70,
    union-find clustering, report + admin-task SQL generation with no auto-merge —
    confirmed by reading the file, not assumed).
  - docs/dedup-report-2026-06-23.md exists in the target repo at HEAD (the generated
    audit report, confirmed non-empty by reading its content).
  - scripts/dedup-admin-todos-2026-06-23.sql exists in the target repo at HEAD (the
    generated admin_tasks INSERT statements, confirmed non-empty by reading its content).
  - `node --check scripts/dedup-audit.mjs` exits 0, and `node --check` exits 0 on every
    other .js/.mjs file this integration touched or conflict-resolved.
  - `git status --short` in the target repo shows no uncommitted changes to any of the
    three ALLOW-FILES paths (a clean result for the touched files) at the end of the
    mission; pre-existing unrelated untracked files may remain untracked exactly as found.
  - None of the above constitutes verification that the feature's CODE is correct (e.g.
    that the 50m/0.70 thresholds are right, that the union-find clustering is sound, or
    that the generated admin-task SQL is safe to run) — that determination belongs to
    this mission's own witness + verdict-panel run downstream, not to this mission's
    authorship or execution.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

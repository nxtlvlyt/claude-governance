MISSION mt-integrate-d1-indexes-2026-06-23
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: main
ALLOW-FILES:
  - d1/AUDIT-INDEXES-2026-06-23.md
  - d1/migrate-indexes-2026-06-23.sql
REQUIRES: none

Niyyah: The feature "mt-feat-d1-indexes-2026-06-23" (commit 4050b5b, sitting in the
source worktree C:\Users\marka\code\mt-feat-d1-indexes-2026-06-23) has never been
integrated into this repo's TARGET-BRANCH. This mission's entire job is mechanical
integration — land the commit onto the current tree without losing either side's
changes, and prove the result is syntactically sound and committed. Nothing this
mission does constitutes a judgment that the two new D1 index statements are correct,
safe, or actually deliver the claimed speedup on the live database (e.g. that
idx_pois_geo and idx_pois_node_type are the right columns, or that the audit's
EXPLAIN QUERY PLAN receipts are trustworthy). That determination belongs to this
mission's own witness + verdict-panel run, which inspects the integrated result after
this mission produces it — not to the act of integration itself. Authoring or
executing this mission is not verification of the feature's code or its SQL.

Maqsad: The commit "4050b5b d1: index pois (lat,lng) + (node_type); 17x speedup on
bbox query" is cherry-picked or merged from the source worktree onto TARGET-BRANCH
main of C:\Users\marka\code\mt-integration-2026-06-22, with any conflicts resolved by
keeping BOTH sides — this repo's existing unrelated changes AND this feature's two new
files, never silently dropping either — every .js/.mjs file this integration touches
(if any) passes `node --check`, the result is committed with a clear message, and the
target repo's working tree is left clean with respect to everything this mission
touches.

Context (verified against live substrate 2026-06-30/07-01, absolute paths, no
prescribed resolution beyond "keep both sides"):
- Source worktree: C:\Users\marka\code\mt-feat-d1-indexes-2026-06-23 — confirmed via
  `git log --oneline` this session to carry commit 4050b5b at the tip, parent ed6e33c
  ("Merge remote-tracking branch 'github/feat/admin-tasks' into
  integration/apex-2026-06-22"). Do not rewrite that worktree, only read/cherry-pick
  FROM it.
- Confirmed diff stat for 4050b5b (`git show --stat`, this session): 2 files changed,
  613 insertions(+), 0 deletions(-) — both brand-new files, no existing file is
  modified: d1/AUDIT-INDEXES-2026-06-23.md (482 lines) and
  d1/migrate-indexes-2026-06-23.sql (131 lines). The SQL file's real content (read
  this session) is two `CREATE INDEX IF NOT EXISTS` statements against the `pois`
  table in the muddytires-pois D1 database — idx_pois_geo ON pois(lat, lng) and
  idx_pois_node_type ON pois(node_type) — plus comments documenting rationale, a live
  baseline measurement, apply/verify commands the feature author already ran, and
  notes on two other candidate indexes that were rejected or already existed. No
  application code, schema tables, or row data are touched by this commit; the D1
  apply the SQL documents was already run live by the feature commit's own author
  (per its commit message) — this integration mission does not re-run it.
- Target repo TARGET-BRANCH main, this session's HEAD (`git log main --oneline -1`):
  5e19874. Confirmed via `git show main:d1/AUDIT-INDEXES-2026-06-23.md` and
  `git show main:d1/migrate-indexes-2026-06-23.sql` (both fail — "path does not
  exist") that neither file exists on main yet, so the two-file add should apply as a
  clean addition with no path collision. If a collision is nonetheless reported at
  fire time (main having since gained its own same-named file), resolve it by keeping
  BOTH files' content rather than overwriting either.
- The repo's checked-out HEAD at the time this Context was written (28cbc72, branch
  feat/qc-concern-sentry-error-capture-sentry-init-js-2026-06-25) is NOT main and
  carries pre-existing untracked litter unrelated to this feature (e2e-shots/,
  docs/e2e-report-2026-07-01.json, feature-catalog-entry.txt,
  scripts/e2e-runner.mjs, api-index-response.json) — confirmed none of those paths are
  tracked on main, so switching to TARGET-BRANCH main will not collide with them; they
  simply remain as untracked files not owned by this mission. This mission's
  "clean working tree" claim is scoped to the files it touches (the two ALLOW-FILES
  entries and anything a merge conflict forces it to touch) — it does not require
  deleting, committing, or otherwise disturbing pre-existing untracked litter left by
  unrelated work, since that litter is out of this mission's scope and not this
  mission's to clean up.
- This diff touches zero .js/.mjs files, so the node --check requirement below is
  expected to be satisfied vacuously (nothing to check) unless a conflict resolution
  forces a change to some other tracked file this repo already has in .js/.mjs form.

Steps:
  1. In the source worktree C:\Users\marka\code\mt-feat-d1-indexes-2026-06-23, confirm
     commit 4050b5b exists and capture its patch (e.g. `git show 4050b5b` or
     `git format-patch -1 4050b5b`) without modifying that worktree. [command]
     capture-patch
  2. In the target repo C:\Users\marka\code\mt-integration-2026-06-22, check out
     TARGET-BRANCH main at its current HEAD, then apply the captured commit via
     `git cherry-pick 4050b5b` (adding the source worktree as a remote/reachable
     object source if needed) or an equivalent merge of that single commit. [command]
     integrate-commit
  3. If the cherry-pick reports a conflict on either ALLOW-FILES entry or any other
     path, resolve each conflicted hunk by hand so the resolved content contains BOTH
     the target's pre-existing content AND this feature's new content — never
     `--ours` / `--theirs` and never a resolution that deletes either side's lines.
     [edit] resolve-conflicts
  4. Confirm which files this integration actually touched or conflict-resolved
     (`git diff --name-only` against the merge-base) and filter to *.js/*.mjs; run
     `node --check` on every such file and confirm each exits 0 (expected list is
     empty for this diff, since neither new file is JavaScript — confirm that
     emptiness explicitly rather than assuming it). [command] node-check-all
  5. Stage and commit the integrated result on TARGET-BRANCH main with a clear
     message identifying the integrated feature and its origin commit (e.g.
     referencing "mt-feat-d1-indexes-2026-06-23" and "4050b5b"). Confirm
     `git status --porcelain` afterward shows no modified or staged entries — any
     pre-existing untracked litter noted in Context above is left exactly as found,
     neither committed nor deleted. [command] commit-and-verify-clean

Done means:
  - `git log` on TARGET-BRANCH main in C:\Users\marka\code\mt-integration-2026-06-22
    shows a commit whose message names "mt-feat-d1-indexes-2026-06-23" and/or
    "4050b5b".
  - d1/AUDIT-INDEXES-2026-06-23.md and d1/migrate-indexes-2026-06-23.sql both exist in
    the target repo at TARGET-BRANCH main's HEAD; migrate-indexes-2026-06-23.sql
    contains both `CREATE INDEX IF NOT EXISTS idx_pois_geo` and
    `CREATE INDEX IF NOT EXISTS idx_pois_node_type` statements (read the file content
    to confirm, not assumed).
  - If the integration touched or conflict-resolved any .js/.mjs file, `node --check`
    exits 0 on each; if it touched none (the expected case for this diff), the step-4
    receipt explicitly shows that empty list rather than skipping the check silently.
  - `git status --porcelain` in the target repo, on TARGET-BRANCH main, shows no
    modified or staged entries at the end of the mission — pre-existing untracked
    litter unrelated to this feature (documented in Context above) is not this
    mission's to clean up and is left untouched either way.
  - Nothing in this mission's own receipts is treated as proof the two new indexes are
    the right ones or that the audit's performance claims hold on the live database —
    that judgment is deferred entirely to this mission's downstream witness and
    verdict-panel run against the integrated result.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

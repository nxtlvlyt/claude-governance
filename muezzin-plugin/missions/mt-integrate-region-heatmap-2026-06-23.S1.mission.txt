MISSION-ID: MT-INTEGRATE-REGION-HEATMAP.S1
MISSION-CLASS: code-repo
PARENT: MT-INTEGRATE-REGION-HEATMAP
TARTIB-INDEX: 1 of 2
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - map.html
  - .
  - functions/api/heatmap-points.js
  - js/region-heatmap-layer.js
VISUAL-QC-REQUIRED
REQUIRES: none (first in tartib order)
STEPS: 8

PARENT MAQSAD: Land commit 9a29244 ("feat(map): toggleable region heatmap overlay (POI

Maqsad: sub-mission 1 of 2 — Snapshot target-repo baseline: record current branch and HEAD, confirm functions/api/heatmap-points.js and js/region-heatmap-layer.js do not already exist on disk. through Spot-check map.html on disk: confirm the new region-heatmap-layer.js script tag is present alongside the pre-existing ev-charging-layer.js and free-camping-filter.js tags, and no leftover conflict markers remain.

Steps:
  1. Snapshot target-repo baseline: record current branch and HEAD, confirm functions/api/heatmap-points.js and js/region-heatmap-layer.js do not already exist on disk. [verify] map.html
  2. Ancestry gate: confirm commit 9a29244 is NOT already an ancestor of HEAD (not yet landed) before attempting the cherry-pick. [verify] map.html
  3. Cherry-pick commit 9a29244 onto the target repo's current HEAD with -x for traceability (the whole-repo git operation that lands all three feature files at once). A nonzero exit (conflict) is a legitimate blocking receipt per mission guidance, not something to paper over. [command] .
  4. Verify functions/api/heatmap-points.js (landed by the cherry-pick) parses as valid JS. [verify] functions/api/heatmap-points.js
  5. Verify js/region-heatmap-layer.js (landed by the cherry-pick) parses as valid JS. [verify] js/region-heatmap-layer.js
  6. Confirm the landed commit's message cites source commit 9a29244 (via the -x trailer) so the integration is traceable. [verify] map.html
  7. Confirm the target repo's working tree has no uncommitted changes to any ALLOW-FILES entry after the cherry-pick. [verify] map.html
  8. Spot-check map.html on disk: confirm the new region-heatmap-layer.js script tag is present alongside the pre-existing ev-charging-layer.js and free-camping-filter.js tags, and no leftover conflict markers remain. [verify] map.html

Done means: map.html, ., functions/api/heatmap-points.js, js/region-heatmap-layer.js exist/are updated as specified — verify by headless-browser render, not by reading the code

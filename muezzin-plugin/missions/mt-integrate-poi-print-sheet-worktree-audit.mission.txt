MISSION MT-INTEGRATE-POI-PRINT-SHEET-WORKTREE-AUDIT
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - js/poi-print-sheet.js
  - map.html
REQUIRES: none
VISUAL-QC-REQUIRED
PREVIEW-BASE-URL: http://localhost:8788

Niyyah: The feature "poi-print-sheet" (commit 195f761, sitting in its source worktree) has
never been integrated into this repo's HEAD. This mission's entire job is mechanical
integration — land the commit onto the current tree without losing either side's changes,
and prove the result is syntactically sound and actually renders. Nothing this mission does
constitutes a judgment that the print-sheet feature's code is correct, safe, or behaviorally
sound — not whether the hand-rolled UTM (Snyder series) projection math is accurate, not
whether the self-encoded inline SVG QR generator produces a scannable ISO/IEC 18004-conformant
code, not whether every POI field is genuinely escaped before the generated document's
innerHTML use, and not whether window.print() reliably auto-fires within its 1.5s image-load
ceiling across browsers. That determination belongs entirely to this mission's own witness-
and-verdict-panel run, downstream of this mission producing the integrated result — the
node --check receipt, the headless-browser render of map.html, and the seat panel's judgment
on the actual integrated diff. Authoring or executing this mission is not verification of the
feature.

Maqsad: The commit "195f761 feat(poi-print-sheet): printable POI sheet button on every popup"
is cherry-picked or merged from the source worktree onto the current HEAD of
C:\Users\marka\code\mt-integration-2026-06-22, with any conflicts resolved by keeping BOTH
sides — this repo's existing unrelated changes AND the print-sheet feature's changes, never
silently dropping either — every changed .js/.mjs file passes `node --check`, the result is
committed with a clear message, and the target repo's working tree is left clean (git status
shows no uncommitted changes to the touched files).

Context (verified against live substrate this session, absolute paths, no prescribed
resolution beyond "keep both sides"):
- SOURCE-PATH CORRECTION: the work order that produced this mission named the source worktree
  "C:\Users\marka\code\poi-print-sheet-worktree-audit" — that path does NOT exist on disk
  (verified this session). The real worktree carrying commit 195f761 is
  C:\Users\marka\code\mt-feat-poi-print-sheet-2026-06-23, confirmed this session via
  `git log --oneline` showing 195f761 at its tip, and via REPO-ROOT's own `git worktree list`
  confirming it as a LINKED worktree of the SAME repository as REPO-ROOT. The commit object
  already lives in REPO-ROOT's shared object database — no fetch, remote-add, or file copy
  step is needed before the cherry-pick.
- Confirmed diff stat for commit 195f76185b30d89f783466f15f07a4b2eee734f5 (`git show --stat`,
  this session): 2 files changed, 891 insertions(+), 1 deletion(-) — js/poi-print-sheet.js
  (890 new lines, a brand-new file implementing the standalone print-styled POI sheet) and
  map.html (+1/-1: a single new `<script src="js/poi-print-sheet.js" defer></script>` tag
  inserted into the existing script-tag chain, immediately after the existing
  `<script src="js/share-export.js" defer></script>` tag and before the plan-day.js
  focus-mode inline comment block; map.html's script chain lives on one long physical line,
  so the one-tag insertion shows as that line replaced with itself plus the new tag, not a
  pure +1/-0 add). No other content in either file changes. The base commit named in the work
  order (ed6e33c) is an ancestor reachable from REPO-ROOT's history.
- Verified this session that js/poi-print-sheet.js does not exist anywhere in the target
  repo's current working tree (a clean new-file add is expected), and that map.html's current
  script chain already ends its relevant segment at
  `<script src="js/share-export.js" defer></script>` immediately followed by the plan-day.js
  focus-mode inline script — matching the exact insertion point the source commit targets. If
  either target has since diverged (e.g. an unrelated change now also touches the same
  physical script-chain line in map.html), resolve by keeping BOTH sets of content — every
  existing script tag AND the new poi-print-sheet.js tag — rather than overwriting one side.
- REPO VOLATILITY OBSERVED THIS SESSION — stronger than a bare HEAD-race risk, an actually
  observed concurrent writer: three consecutive read-only checks of REPO-ROOT within roughly
  two minutes reported three different checked-out branches/HEADs (test-lhci-dryrun-2026-07-01
  at a1d6ad9; a state showing HEAD at 3dc0cac tagged __dryrun_path_leak_test, seen via
  `git worktree list` run from REPO-ROOT itself; then main at 5e19874). A subsequent
  `git status --short` on REPO-ROOT showed STAGED changes this session did not create
  (`A  js/plan-day-gpx-export.js`, `M  map.html`) alongside pre-existing unrelated untracked
  files (api-index-response.json, docs/e2e-report-2026-07-01.json, e2e-shots/,
  feature-catalog-entry.txt, scripts/e2e-runner.mjs). This is direct evidence that another
  process is actively writing to REPO-ROOT concurrently with this session's own read-only
  checks. Given this, no dry-run cherry-pick was attempted this session (unlike prior
  worktree-audit missions in this engine) — mutating a repo carrying live concurrent staged
  writes from an unidentified process, then reverting via reset/clean, risks destroying that
  other process's in-flight work. This mission must re-confirm a genuinely clean state (no
  staged changes, no unmerged paths, beyond this mission's own ALLOW-FILES) as its own first
  step, and must NOT force through a dirty tree it did not create — that state is a named
  hazard to surface in this mission's result, not something to reset-hard past.
- Because of the volatility above, this mission targets whatever branch/HEAD is actually
  checked out in REPO-ROOT at fire time, not a fixed branch name — any branch name recorded
  during authoring could already be stale by the time this mission runs.
- This diff touches map.html (an HTML file), which is why VISUAL-QC-REQUIRED and
  PREVIEW-BASE-URL are declared above. http://localhost:8788 is a PLACEHOLDER for a local
  `wrangler pages dev` run — the operator must confirm the port a real local preview server
  actually binds to on the machine firing this mission before relying on the captured render;
  if nothing answers at PREVIEW-BASE-URL, that must surface as a named NO_PREVIEW_BASE_URL
  receipt, not a silently skipped check.
- Nothing about this mission's authorship constitutes verification that the feature's code is
  correct — that is exactly what this mission's own witness and verdict-panel run, downstream
  of the integration, will determine.

Steps:
  1. In REPO-ROOT, run `git status --short`, `git rev-parse HEAD`, and
     `git branch --show-current`. Confirm neither js/poi-print-sheet.js nor a poi-print-sheet
     script tag already exists, and confirm there are no staged changes or unmerged paths in
     REPO-ROOT beyond this mission's own future edits. If staged changes or an in-progress
     cherry-pick/merge exist that this mission did not create, stop and record that as a named
     hazard receipt rather than resetting through it. [command] preflight-confirm-clean
  2. In the source worktree C:\Users\marka\code\mt-feat-poi-print-sheet-2026-06-23, confirm
     commit 195f76185b30d89f783466f15f07a4b2eee734f5 exists (`git show 195f761 --stat`)
     without modifying that worktree. [command] confirm-source
  3. In REPO-ROOT, at whatever HEAD/branch is actually checked out, cherry-pick the commit
     (`git -C C:\Users\marka\code\mt-integration-2026-06-22 cherry-pick
     195f76185b30d89f783466f15f07a4b2eee734f5`) or an equivalent merge of that single commit.
     [command] integrate-commit
  4. If the cherry-pick (or equivalent merge) reports conflicts on js/poi-print-sheet.js,
     map.html, or any other ALLOW-FILES entry, resolve every conflicted hunk by hand so the
     resolved content contains BOTH the target's pre-existing content AND the print-sheet
     feature's added content — never `--ours` / `--theirs` and never a resolution that deletes
     either side's content. [edit] resolve-conflicts
  5. Run `node --check js/poi-print-sheet.js` from REPO-ROOT and confirm it exits 0; run
     `node --check` on every other .js/.mjs file the merge touched or conflict-resolved.
     [command] node-check-all
  6. If step 3 left the integration uncommitted (conflict resolution required `git add` +
     `git cherry-pick --continue`, or a manual merge commit), commit explicitly with a message
     that references the source commit's own subject line ("feat(poi-print-sheet): printable
     POI sheet button on every popup") and/or the SHA 195f761. Then confirm
     `git status --short` is empty with respect to this mission's ALLOW-FILES (clean, no
     unmerged paths) and `git log --oneline -5` shows the landed commit at or near HEAD.
     [command] commit-and-verify-clean
  7. Because map.html changed and VISUAL-QC-REQUIRED is set, run the visual-witness pipeline:
     serve REPO-ROOT's static files locally, load map.html from PREVIEW-BASE-URL in a headless
     browser (puppeteer), capture a render, and record a visual verdict on the integrated
     result in this mission's result object. If PREVIEW-BASE-URL is unreachable, record that
     as a named NO_PREVIEW_BASE_URL receipt rather than skipping the check silently.
     [command] visual-witness-render

Done means:
  - `git log` in C:\Users\marka\code\mt-integration-2026-06-22 shows a commit containing the
    integrated poi-print-sheet feature, with a commit message that names "poi-print-sheet"
    and/or "195f761".
  - js/poi-print-sheet.js exists in the target repo at HEAD with non-trivial content
    (approximately 890 lines implementing the standalone print-styled POI sheet — name,
    lat/lng plus the UTM projection, review text, first photo, self-encoded inline SVG QR
    code, timestamp, and the window.print() auto-call — confirmed by reading the file, not
    assumed).
  - map.html in the target repo contains exactly one
    `<script src="js/poi-print-sheet.js" defer></script>` tag added to its existing script
    chain immediately after the js/share-export.js tag, with no other content lost or altered
    (confirmed by reading the file, not by trusting the cherry-pick's exit code alone).
  - `node --check js/poi-print-sheet.js` exits 0, and `node --check` exits 0 on every other
    .js/.mjs file this integration touched or conflict-resolved.
  - `git status --short` in the target repo shows a clean working tree with respect to this
    mission's own ALLOW-FILES (no uncommitted changes, no unmerged paths, no leftover
    `.git/CHERRY_PICK_HEAD`) at the end of the mission. Pre-existing unrelated dirt this
    mission did not create and did not touch is out of scope for this clause either way.
  - Because this diff touches map.html and VISUAL-QC-REQUIRED is set above, the visual-witness
    pipeline must actually run and produce a receipt: a headless-browser (puppeteer) render of
    map.html served from PREVIEW-BASE-URL is captured and returns a visual verdict in this
    mission's result object — a rendering check, not a read of the markup. If
    PREVIEW-BASE-URL turns out to be the wrong local port, that failure must surface as a named
    NO_PREVIEW_BASE_URL-style receipt, not a silently skipped check.
  - This mission's authorship and execution prove only that the integration is mechanically
    sound (declared files present with real content, feature commit reachable in target
    history, syntax checks pass, a render was actually captured) — not that the feature's own
    code is correct, safe, or behaviorally right (not the UTM projection math, not the QR
    encoder's conformance, not the escaping posture of every field flowing into the generated
    document, not whether window.print() reliably auto-fires within its image-load ceiling).
    That judgment belongs to this mission's own witness plus verdict-panel run, downstream of
    this mission producing the integrated result.

Context: REPO-ROOT is a real project repo, not a sandbox; containment is enforced by
ALLOW-FILES against the kernel in mission_class.mjs, not by filesystem isolation. Every
command above runs at REPO-ROOT.
Amanah: entrusted; discharge with ihsan.

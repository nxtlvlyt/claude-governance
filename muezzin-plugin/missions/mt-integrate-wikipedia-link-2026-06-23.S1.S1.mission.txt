MISSION-ID: MT-INTEGRATE-WIKIPEDIA-LINK-2026-06-23.S1.S1
MISSION-CLASS: code-repo
PARENT: MT-INTEGRATE-WIKIPEDIA-LINK-2026-06-23.S1
TARTIB-INDEX: 1 of 2
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - functions/api/wikipedia-link.js
  - js/poi-wikipedia.js
  - map.html
REQUIRES: none (first in tartib order)
STEPS: 8

PARENT MAQSAD: sub-mission 1 of 2 — Baseline check (idempotent): print current branch and HEAD of the target repo, and report ALREADY_PRESENT if either new feature file already exists on disk, else CLEAN_BASELINE. Pure diagnostic, no implementation file authored. through Stage and commit the three already-written ALLOW-FILES with a message citing mt-feat-wikipedia-link-2026-06-23 and cf373c9; idempotent no-op (ALREADY_COMMITTED) if a commit citing cf373c9 already exists in recent history.

Maqsad: sub-mission 1 of 2 — Baseline check (idempotent, diagnostic only): print current branch and HEAD SHA; report ALREADY_PRESENT if either new feature file already exists on disk, else CLEAN_BASELINE. through Stage and commit the three ALLOW-FILES with a scoped commit citing mt-feat-wikipedia-link-2026-06-23 and cf373c9; idempotent ALREADY_COMMITTED no-op when the scoped tree is already clean. Restores come from cf373c9 via single-path checkout (cannot conflict); the map.html wiring is one anchored tag insert.

STEP-SHAPE DISCIPLINE (conductor amendment 2026-07-17, after both prior attempts died at the fire boundary before any step ran — the 2026-07-11 same-wake amendment's own Done-means prose re-armed the whole-text render-tag regex, the spot-share-card.S1 re-arm class — and the steps carried only prose a seat would improvise from, the receipted mt-addspot kill-class. The near-me S1.S1 dry-run-proven literal shape is applied verbatim below; the planner must use these commands, not improvise. Anchor receipted 2026-07-17 against HEAD 0449664: the poi-provenance script tag occurs exactly once in map.html; poi-wikipedia occurs zero times; map.html has no BOM):

Steps:
  1. Baseline, stdout only, IDEMPOTENT. Run exactly: git rev-parse --abbrev-ref HEAD; git rev-parse HEAD; if ((Test-Path functions/api/wikipedia-link.js) -or (Test-Path js/poi-wikipedia.js)) { Write-Output ALREADY_PRESENT } else { Write-Output CLEAN_BASELINE } [command]
  2. Ancestry check, IDEMPOTENT, diagnostic only. Run exactly: git merge-base --is-ancestor cf373c9 HEAD; if ($LASTEXITCODE -eq 0) { Write-Output IS_ANCESTOR } else { Write-Output NOT_ANCESTOR }; exit 0 [command]
  3. Restore the API handler from cf373c9 — the ONLY step that writes this file; single-path checkout cannot conflict; idempotent no-op if identical content already present. Run exactly: git checkout cf373c9 -- functions/api/wikipedia-link.js; if ($LASTEXITCODE -ne 0) { exit 1 }; if (-not (Test-Path functions/api/wikipedia-link.js)) { exit 1 }; Write-Output API_RESTORED [command] functions/api/wikipedia-link.js
  4. Restore the client script from cf373c9 — the ONLY step that writes this file; single-path checkout cannot conflict; idempotent no-op if identical content already present. Run exactly: git checkout cf373c9 -- js/poi-wikipedia.js; if ($LASTEXITCODE -ne 0) { exit 1 }; if (-not (Test-Path js/poi-wikipedia.js)) { exit 1 }; Write-Output CLIENT_RESTORED [command] js/poi-wikipedia.js
  5. Insert exactly one new script tag into map.html immediately after the poi-provenance tag — the ONLY step that writes map.html; anchored, idempotent, refuses a non-unique anchor. Run exactly: $p='map.html'; $t=[IO.File]::ReadAllText($p); $tag='<script src="js/poi-wikipedia.js" defer></script>'; $anchor='<script src="js/poi-provenance.js" defer></script>'; if ($t.Contains($tag)) { Write-Output TAG_ALREADY_PRESENT; exit 0 }; if (([regex]::Matches($t,[regex]::Escape($anchor))).Count -ne 1) { Write-Output ANCHOR_NOT_UNIQUE; exit 1 }; [IO.File]::WriteAllText($p,$t.Replace($anchor,$anchor+$tag)); Write-Output TAG_INSERTED [command] map.html
     validation_command: if (-not (Select-String -Path map.html -SimpleMatch 'js/poi-wikipedia.js' -Quiet)) { exit 1 }; if (-not (Select-String -Path map.html -SimpleMatch 'js/poi-provenance.js' -Quiet)) { exit 1 }; if (-not (Select-String -Path map.html -SimpleMatch 'js/plan-day-weather.js' -Quiet)) { exit 1 }
  6. Verify the restored API handler is syntactically valid JavaScript, read-only, authors nothing. Run exactly: node --check functions/api/wikipedia-link.js; if ($LASTEXITCODE -ne 0) { exit 1 }; Write-Output API_SYNTAX_OK [verify] functions/api/wikipedia-link.js
  7. Verify the restored client script is syntactically valid JavaScript, read-only, authors nothing. Run exactly: node --check js/poi-wikipedia.js; if ($LASTEXITCODE -ne 0) { exit 1 }; Write-Output CLIENT_SYNTAX_OK [verify] js/poi-wikipedia.js
  8. Repo-state bookkeeping check, NO file authored by this step (steps 3-5 already wrote the deliverables; this is the idempotent no-op-or-commit tail plus scoped cleanliness — a single engine-executed command whose file mentions are READS of git status, not edit targets). Run exactly: git add -- functions/api/wikipedia-link.js js/poi-wikipedia.js map.html; git diff --cached --quiet -- functions/api/wikipedia-link.js js/poi-wikipedia.js map.html; if ($LASTEXITCODE -ne 0) { git commit -m "feat(wikipedia-link): /api/wikipedia-link + popup chip for named POIs (integrate cf373c9; mt-feat-wikipedia-link-2026-06-23)" -- functions/api/wikipedia-link.js js/poi-wikipedia.js map.html; if ($LASTEXITCODE -ne 0) { exit 1 } } else { Write-Output ALREADY_COMMITTED }; if ((git status --porcelain -- functions/api/wikipedia-link.js js/poi-wikipedia.js map.html).Length -gt 0) { exit 1 }; Write-Output SCOPE_CLEAN [verify] ; context_dependencies: functions/api/wikipedia-link.js, js/poi-wikipedia.js, map.html

Done means: functions/api/wikipedia-link.js, js/poi-wikipedia.js, and the map.html script tag land on disk via the cf373c9 single-path restores + anchored insert, both js files pass node --check, and the scoped commit citing cf373c9 exists — git-mechanical verification only; the headless-render witness is S1.S2's charter (S1.S2 carries the render-witness header and the executable step 9, moved there 2026-07-11 per the partner-widget fix shape).

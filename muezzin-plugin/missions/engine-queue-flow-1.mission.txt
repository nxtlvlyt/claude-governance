MISSION-CLASS: engine-bootstrap (queue/orchestration flow — FRESH instance, witnessed)
MISSION-ID: M-ENGINE.QUEUE-FLOW.1
TARGET: the queue layer — AUTORUN.md + QUEUE.md (verify its current role) + the daemon's readQueue (muezzin-daemon.mjs)
REQUIRES: FRESH instance; witnessed; daemon idle. FIRST verify the CURRENT flow: does QUEUE.md (the priorities file the
conductor-core references) already feed AUTORUN, or is AUTORUN appended purely by hand? Read before designing.
# Operator 2026-06-16 04:43: "when will all the muddytires missions get queued / when will all missions get queued?"
# CONDUCTOR VERIFIED this session: constructed mission FILES sit "NOT in AUTORUN" until a conductor MANUALLY appends
# them (tonight: muddytires-resilience-1/2, community-1, data-expansion-1 all existed as files, none queued, until the
# conductor hand-queued d1-1). There is NO systematic "all constructed missions flow to the fire queue per priority."
# Queuing is ad-hoc conductor labor — the same anti-pattern as the hand-decomposition that M-ENGINE.HAJJ-AUTOSPLIT.1
# fixes. This mission systematizes WHEN and HOW a constructed mission becomes a queued/firing mission.

Niyyah: a constructed mission with a priority should not depend on a conductor remembering to append it to AUTORUN. The
queue should be DERIVED from the missions + their priorities, not hand-maintained — so "when does it get queued" has a
mechanical answer, not "whenever the conductor gets to it."

Maqsad: a defined, mechanical flow from CONSTRUCTED mission -> QUEUED -> FIRING, ordered by priority + dependencies
(REQUIRES/tartib), so that every constructed mission has a knowable place in line and the daemon draws work without a
human curating AUTORUN by hand.

REQUIRED (design against the existing pieces — QUEUE.md, AUTORUN.md, INBOX.md per conductor-core.md):
1. AUDIT the current flow: what QUEUE.md does today, how AUTORUN is populated, where the manual step is. Receipt: a
   one-page map of the present queue mechanics.
2. DEFINE the flow: a mission carries a PRIORITY + REQUIRES; a deterministic step (daemon or a small tool) materializes
   the ordered fire-queue (AUTORUN) from the set of constructed-but-unfinished missions + their priorities + satisfied
   dependencies. Held/blocked/gated missions stay out until their gate clears. Receipt: the flow documented + a dry-run
   showing the right next-N missions selected from a sample set.
3. WIRE the minimal version: the daemon (or a conductor command) auto-promotes the highest-priority ready mission into a
   lane when a lane frees, instead of requiring a hand AUTORUN append — WITHOUT breaking the existing manual append
   (manual still works; auto-promotion fills the gaps). Receipt: a lane frees and the next ready mission fires with no
   hand edit.

Amanah: this removes the conductor-as-queue-clerk bottleneck (the operator's recurring "when does X get queued"). Witness
it; keep the existing AUTORUN semantics working (manual append still valid); never auto-fire a HELD/gated mission. PAIRS
WITH M-ENGINE.HAJJ-AUTOSPLIT.1 (its emitted sub-missions need this flow to reach the queue automatically).

Constraints: FRESH instance; witnessed; the daemon's existing readQueue/markLine semantics preserved; HELD/BLOCKED
missions never auto-promoted; GR10; commit --no-verify; NAS untouched.

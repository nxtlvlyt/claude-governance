MISSION-ID: mt-feedback-capture.S1
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - js/feedback-widget.js
  - functions/api/feedback.js
  - d1/migrate-feedback-2026-07-03.sql
  - map.html
  - index.html
VISUAL-QC-REQUIRED
REQUIRES: none
STEPS: 8

Maqsad: Operator-sent growth transcript 2026-07-03 (proof over promise: in the free phase
"my only ask is you give me feedback — tell me how I can make it right"; the feedback loop
is what turns a free product into a proven one). Receipted gap: the live site has NO
feedback mechanism at all (live fetch 2026-07-03: zero feedback/contact affordances in the
landing or map HTML; the operator's own phone has been the only user-report channel all
week). Three deliverables:
(D1) MIGRATION d1/migrate-feedback-2026-07-03.sql: a feedback table (id INTEGER PK, ts TEXT,
page TEXT, message TEXT NOT NULL, contact TEXT NULL, day_key TEXT) — no accounts, no PII
beyond an OPTIONAL contact field the user types themselves.
(D2) ENDPOINT functions/api/feedback.js: POST-only; validates message 3..2000 chars;
escapes/never-renders-back raw input; per-source daily cap (hash of IP + UTC date into
day_key, reject past 20/day with 429) so the table cannot be flooded; returns a plain
honest thanks JSON. GET is 405 — reading feedback is the operator/conductor's job via
wrangler d1 execute, not a public surface.
(D3) WIDGET js/feedback-widget.js + one-line wiring in map.html and index.html: a small
"Something broken? Tell us" affordance (footer link on index, small control on map that must
NOT overlap the existing control lanes — top-left zoom/search stack and the bottom
day-rhythm strip are already contested space, receipts in mt-mobile-qc-hardening.S1) that
opens a plain textarea + optional contact + send, POSTs to /api/feedback, XSS-safe
(textContent only), no login, works at 375px width.

niyyah: The transcript's one pre-revenue lever this site can act on today: collect the
negative word of mouth privately before it becomes public — every bug report typed into the
site is one the operator does not have to catch on his own phone. Smallest honest shape:
one table, one endpoint, one widget; no dashboards, no email plumbing, no third parties.

Steps:
  1. Idempotent baseline: record branch + HEAD; if js/feedback-widget.js already exists output CLEAN_ALREADY and treat authoring steps as verify-only presence checks; else CLEAN_BASELINE. Never exit 1 on existence. [command]
  2. Author d1/migrate-feedback-2026-07-03.sql exactly per D1 (CREATE TABLE IF NOT EXISTS feedback ...). [edit] d1/migrate-feedback-2026-07-03.sql
  3. Author functions/api/feedback.js per D2 (POST-only, length validation, day_key daily cap, 405 on GET, honest thanks JSON), following the request/env/json() conventions of the existing functions/api/stats.json.js. [edit] functions/api/feedback.js
  4. Author js/feedback-widget.js per D3 and add the single script-tag + affordance wiring to map.html and index.html (additive lines only — the proven additive-integration pattern from reviews/add-spot). [edit] js/feedback-widget.js
  5. node --check js/feedback-widget.js and node --check functions/api/feedback.js. [verify] js/feedback-widget.js
  6. Scoped cleanliness: git status --porcelain limited to the five ALLOW-FILES shows only them. [verify]
  7. Commit the five ALLOW-FILES by explicit name with a message naming this mission and the transcript receipt, confirm the commit is on HEAD. [command]
  8. OUTCOME CHECK: write a cwd-relative scratch harness (Set-Content scratch-feedback-test.mjs) that imports the endpoint's onRequestPost handler with a stubbed env.DB (capture the prepared statements) and asserts: a valid fixture POST inserts and returns ok JSON; a 1-char message is rejected; a GET-shaped call path returns 405; the 21st same-day-key fixture is rejected 429 — run node scratch-feedback-test.mjs, require exit 0 with output FEEDBACK_OK, remove the scratch file. [command]

Done means: all five ALLOW-FILES committed at HEAD; the scratch harness exits 0 with
FEEDBACK_OK proving insert/validation/405/daily-cap behavior against fixtures; AND the
engine's headless-browser render (visual witness against the auto-provisioned preview)
shows the feedback affordance present on the landing page and on /map at the mobile
viewport without overlapping existing controls — executable + headless-browser render
verification appropriate to a user-facing feature (live-outcome bar, 2026-07-03). D1
migration application to the live database is a deploy-time step recorded by the standing
--record-deploy ceremony, not silently assumed by this mission.

MISSION-CLASS: code-repo
MISSION-ID: M-READINESS-GATE.1
REPO-ROOT: C:\Users\marka\.claude\muezzin-plugin
ALLOW-FILES:
  - muezzin-daemon.mjs
  - _tests/readiness-gate.test.mjs
REQUIRES: code-repo

Maqsad: Make the conductor (the daemon's fire boundary) verify a mission's required tools are LIVE before it fires — and auto-heal them if they're not — instead of firing into a blind backend and burning an attempt. This is the operator's exact ruling 2026-06-15: "should the muezzin not be making sure the phases have all the tools and everything they need before they start their work?" Today the answer is NO: REQUIRES:search is checked INSIDE runMission/orchestrate (mission-run time), so a search mission on a suspended-engine SearXNG backend FIRES, increments attempts, then fails the preflight — the attempt is burned and the failover degrades to the protected claude tier. Verified live this session: damm-west + sota-pipeline-dossier both FAILED x2 on "search is blind, not empty"; engine-3phase-1 failed over kimi-k2.6 -> claude-opus on SEARCH_BLIND (15:18:30). SearXNG flaps: engines get CAPTCHA'd and suspend, search goes blind, a manual restart heals it. The suspension-time fix shortened the bans; it did NOT stop the flapping. The conductor must tie the camel at fire-time.

niyyah: turn the operator's fire-time-readiness ruling from a conductor note into a receipted engine change. Source open: muezzin-daemon.mjs fire() (L288-343) + runMission() needsSearch wiring (L258-276) + orchestrate.mjs's existing searxngPreflight (the control-query probe to REUSE, not reinvent). Guard against: a second/divergent probe instead of reusing the existing one; an infinite heal loop (must cap and convert to a real BLOCK-WITH-RECEIPT so a permanently-dead backend surfaces, never loops blind); firing-path regressions (existing mainLoop/lint/attempt logic must stay intact).

CONTEXT (verified this session, substrate-grounded):
- fire(raw) at muezzin-daemon.mjs L288: resolves file -> exists check -> lintMission (MIQAT) -> attempts++ -> setMark RUNNING -> runMission. The tool-readiness check belongs HERE, after MIQAT and BEFORE attempts++/RUNNING/runMission.
- runMission L268: `const needsSearch = /REQUIRES?:[^\n]*\bsearch\b/i.test(mission)` — same REQUIRES parse the gate should use to know a mission needs search.
- orchestrate.mjs already has a SearXNG control-query liveness probe (searxngPreflight) that returns blind/ok — the gate REUSES that probe (import or extract a shared helper); it does not invent a new one.
- Heal = restart the SearXNG docker container (the manual fix that works: `docker restart searxng`) from Node via execSync, then re-probe.

Done means (verifiable):
1. A pure, testable gate helper exists, e.g. `searchReadinessGate(missionText, { probe, heal })` (probe/heal injected so it unit-tests with stubs), returning one of: `{ action: 'fire' }`, `{ action: 'hold', reason }`, `{ action: 'block', reason }`. Logic:
   - mission text has NO `REQUIRES:...search` -> `fire` (gate is a no-op for non-search missions; never blocks them).
   - probe() live -> `fire`.
   - probe() blind -> heal() (restart searxng) -> re-probe; live -> `fire`; still blind -> `hold`.
   - a HOLD counter per mission: after N consecutive holds (N=3) the gate returns `block` with a receipt reason ("search blind: probed + healed + rechecked Nx, backend still down") — never loops blind.
2. fire() in muezzin-daemon.mjs calls the gate AFTER the MIQAT lint and BEFORE attempts++/setMark RUNNING/runMission, wired to the REAL probe (reuse orchestrate's searxngPreflight) and the REAL heal (docker restart searxng). On `hold`: do NOT increment attempts, do NOT setMark RUNNING/FAILED — leave the line unmarked so mainLoop reconsiders it next beat; emit a `HELD (tool not ready): <mission> — <reason>` daemon event (NO phone push: lifecycle/plumbing is noise per operator 2026-06-10). On `block`: setMark FAILED with a BLOCK-WITH-RECEIPT event + an OUTCOME phone push naming search-dead as the root (this is a real outcome, not plumbing). On `fire`: unchanged current behavior.
3. The existing in-orchestrate needsSearch preflight STAYS (defence in depth) — this gate is the fire-boundary layer, not a replacement.
4. Self-test `_tests/readiness-gate.test.mjs` runs green with injected stubs covering every branch: no-REQUIRES -> fire; live -> fire (heal NOT called); blind-then-live-after-heal -> fire (heal called once); blind-stays-blind -> hold (heal called); 3 consecutive holds -> block. Acceptance: `node _tests/readiness-gate.test.mjs` exits 0, all assertions pass; and a syntax/health check of the daemon (`node --check muezzin-daemon.mjs`) passes so the live loop is not broken.

Constraints: code-repo, muezzin-plugin repo only, the two ALLOW-FILES only, NAS UNTOUCHED. Do NOT change MAX_LANES, the singleton lock, MIQAT lint, or the attempt/retry math. The heal command must target only the `searxng` container by name. If the engine cannot satisfy the witness on this daemon edit, HOLD with a receipt (do not blind-relaunch).

Steps:
1. edit muezzin-daemon.mjs - add `searchReadinessGate(missionText, { probe, heal })` (pure, injected probe/heal, per-mission hold counter) and wire it into fire() after MIQAT, before attempts++/RUNNING/runMission, using orchestrate's existing searxngPreflight as probe and `docker restart searxng` as heal. Acceptance: `node --check muezzin-daemon.mjs` passes; fire() unchanged for the fire/non-search paths.
2. write _tests/readiness-gate.test.mjs - assert all five branches (no-REQUIRES/live/heal-then-live/stays-blind/3x-hold->block) with stubs. Acceptance: `node _tests/readiness-gate.test.mjs` exits 0.

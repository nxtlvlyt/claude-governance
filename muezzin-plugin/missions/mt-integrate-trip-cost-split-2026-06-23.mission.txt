MISSION mt-integrate-trip-cost-split-2026-06-23
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - css/trip-cost-split.css
  - d1/migrate-trip-cost-split-2026-06-23.sql
  - functions/api/trip-cost/[code].js
  - functions/api/trip-cost/[code]/settle.js
  - js/trip-cost-split.js
  - trip-cost.html
REQUIRES: none
VISUAL-QC-REQUIRED
PREVIEW-BASE-URL: http://localhost:8788

Niyyah: The feature "mt-feat-trip-cost-split-2026-06-23" (commit
5adf50a1942f490854ee34405d13aea1192300d6, "feat(trip-cost-split): group
fuel/grocery/camp expense reconciliation", sitting in the source worktree
C:\Users\marka\code\mt-feat-trip-cost-split-2026-06-23) has never been
integrated into this repo's checked-out line. This mission's entire job is
mechanical integration — land the commit onto whatever HEAD is actually
checked out when this mission fires, without losing either side's changes,
and prove the result is syntactically sound and actually renders. Nothing
this mission does constitutes a judgment that the trip-cost-split feature's
code is correct: not that the settle endpoint's greedy creditor-debtor
transfer-plan math actually conserves pennies on real inputs, not that the
lazy trip-creation and self-healing `CREATE TABLE IF NOT EXISTS` pattern in
both API handlers is safe under concurrent first-writers, not that the
amount/ccy/date/name validation helpers actually reject malformed input, and
not that the fuel/groceries/camp/gear/food/other category enum is enforced
server-side rather than merely assumed client-side. That determination
belongs entirely to this mission's own witness plus verdict-panel run,
downstream of the integration this mission produces — not to the act of
authoring or executing this mission file.

Maqsad: Commit 5adf50a1942f490854ee34405d13aea1192300d6 is cherry-picked or
merged from the source worktree onto the current, live HEAD of
C:\Users\marka\code\mt-integration-2026-06-22 (whatever branch is actually
checked out at fire time), with any conflicts resolved by keeping BOTH sides
— this repo's existing unrelated changes AND the trip-cost-split feature's
changes, never silently dropping either — every changed .js/.mjs file passes
`node --check`, a headless-browser render of trip-cost.html is actually
captured, the result is committed with a clear message, and the target
repo's working tree is left clean with respect to the files this mission
touches.

Context (verified against live substrate this session, absolute paths, no
prescribed resolution beyond "keep both sides"):
- The source worktree C:\Users\marka\code\mt-feat-trip-cost-split-2026-06-23
  is a git LINKED WORKTREE of this SAME repository (confirmed via
  `git worktree list` run from the target repo, this session, listing it at
  5adf50a on branch feat/trip-cost-split-2026-06-23) — both share one object
  store, so commit 5adf50a1942f490854ee34405d13aea1192300d6 is already
  reachable directly from the target repo with no fetch/remote-add step
  needed (confirmed via `git cat-file -t 5adf50a` in the target repo
  returning "commit", this session). Do not rewrite the source worktree —
  only read/cherry-pick FROM it.
- Confirmed diff stat for 5adf50a (`git show --stat`, this session, target
  repo): 6 files changed, 1412 insertions(+), 0 deletions(-), all new files
  — css/trip-cost-split.css (272 lines), d1/migrate-trip-cost-split-2026-06-23.sql
  (48 lines, a D1 migration), functions/api/trip-cost/[code].js (314 lines,
  GET/POST handler), functions/api/trip-cost/[code]/settle.js (254 lines, the
  settle/"who owes whom" endpoint), js/trip-cost-split.js (355 lines, client
  logic), and trip-cost.html (169 lines, a new noindex page). The commit's
  parent is ed6e33c, confirming the diff range ed6e33c..5adf50a matches the
  feature's own commit exactly.
- Verified this session that NONE of the six ALLOW-FILES paths currently
  exist anywhere in the target repo's working tree — a clean cherry-pick
  with no textual conflicts is the expected outcome. functions/api/ already
  exists as a directory (other unrelated API routes live under it), but
  functions/api/trip-cost/ does not yet exist as a subdirectory, so no
  directory-level collision is expected either. If any of the six paths has
  since been created by unrelated concurrent work by the time this mission
  fires, the resolution rule below still applies: keep BOTH bodies, never
  silently pick one side and drop the other.
- THE TARGET REPO MAY BE ACTIVELY CHURNING under concurrent mission/chain
  activity (observed pattern in sibling integration missions this week). Do
  NOT hardcode a branch name or SHA anywhere in this mission's execution —
  Step 1 below must record whatever branch/HEAD is ACTUALLY checked out at
  the moment this mission fires (verified this session: branch "main" at
  5e19874307f1548c404bf0a42b90831c00da9539, but that is a snapshot, not a
  premise to build execution on), and every later step integrates onto that
  live HEAD, not a memorized one.
- `node --version` in the target repo this session reports v22.20.0,
  confirming `node --check` is available for the receipts below.
- This diff touches trip-cost.html (an HTML file), which is why
  VISUAL-QC-REQUIRED and PREVIEW-BASE-URL are declared above.
  PREVIEW-BASE-URL: http://localhost:8788 is a PLACEHOLDER for a local
  `wrangler pages dev` (or equivalent static-file) server — the operator must
  confirm the actual port a local preview server binds to on the machine
  firing this mission before trusting the captured render; if nothing
  answers at PREVIEW-BASE-URL, that must surface as a named
  NO_PREVIEW_BASE_URL-style receipt, not a silently skipped check.
- Nothing about this mission's authorship constitutes verification that the
  feature code is correct — that is exactly what this mission's own witness
  and verdict-panel run, downstream of the integration, will determine.

Steps:
  1. In the target repo C:\Users\marka\code\mt-integration-2026-06-22, record
     the ACTUAL current branch and HEAD right now
     (`git rev-parse --abbrev-ref HEAD`; `git rev-parse HEAD`), and confirm
     none of the six ALLOW-FILES paths already exist on disk.
     [command] verify-target-live-head
  2. From that live HEAD, cherry-pick commit
     5adf50a1942f490854ee34405d13aea1192300d6 directly — the source worktree
     is a linked worktree of this same repository, so the object is already
     reachable with no fetch step needed:
       git -C C:\Users\marka\code\mt-integration-2026-06-22 cherry-pick 5adf50a1942f490854ee34405d13aea1192300d6
     (or an equivalent merge of that single commit). [command] integrate-commit
  3. If the cherry-pick reports a conflict on any ALLOW-FILES entry
     (unexpected given all six are brand-new files with no existing
     counterpart in the target, but possible against unrelated churn),
     resolve every conflicted hunk by hand so the resolved content contains
     BOTH the target's pre-existing content AND this feature's new content —
     never `--ours` / `--theirs`, never a resolution that deletes either
     side's content. [edit] resolve-conflicts
  4. Run `node --check` on every .js file this cherry-pick touched or
     conflict-resolved: `functions/api/trip-cost/[code].js`,
     `functions/api/trip-cost/[code]/settle.js`, and `js/trip-cost-split.js`.
     Confirm each exits 0. [command] node-check-all
  5. Stage and commit the integrated result in the target repo with a clear
     message that names the integrated feature and its origin commit (e.g.
     referencing "mt-feat-trip-cost-split-2026-06-23" and "5adf50a"). Confirm
     `git status --short` afterward shows no uncommitted changes to any of
     the six ALLOW-FILES paths. [command] commit-and-verify-clean
  6. Serve the target repo's static files locally (e.g. `wrangler pages dev .`
     or an equivalent static server) and use a headless browser (puppeteer)
     to render trip-cost.html from PREVIEW-BASE-URL, capturing a
     screenshot/DOM check that js/trip-cost-split.js loaded without a script
     error and that mountCostSplit actually mounted the trip-code
     entry/generate/switch UI onto the page. If nothing answers at
     PREVIEW-BASE-URL, surface that explicitly as a named
     NO_PREVIEW_BASE_URL-style receipt rather than skipping the check.
     [command] visual-witness-render

Done means:
  - css/trip-cost-split.css, d1/migrate-trip-cost-split-2026-06-23.sql,
    functions/api/trip-cost/[code].js, functions/api/trip-cost/[code]/settle.js,
    js/trip-cost-split.js, and trip-cost.html all exist on disk in
    C:\Users\marka\code\mt-integration-2026-06-22, each containing the
    feature's actual content ([code].js implements GET/POST with lazy trip
    creation, a self-healing `CREATE TABLE IF NOT EXISTS`, and the
    fuel/groceries/camp/gear/food/other category enum; settle.js implements
    the separate "who owes whom" balance/transfer-plan endpoint; the SQL
    migration creates the trips and trip_contributions tables; trip-cost.html
    is a noindex page whose script mounts mountCostSplit from
    trip-cost-split.js) — confirmed by reading each file's on-disk content,
    not by trusting the cherry-pick's exit code alone.
  - `node --check` exits 0 on `functions/api/trip-cost/[code].js`,
    `functions/api/trip-cost/[code]/settle.js`, and `js/trip-cost-split.js`.
  - `git log` in the target repo shows a commit whose message names
    "mt-feat-trip-cost-split-2026-06-23" and/or "5adf50a", and
    `git status --short` shows no uncommitted changes to any of the six
    ALLOW-FILES paths — the target repo's working tree is clean with respect
    to the files this mission touches.
  - Because this diff touches trip-cost.html and VISUAL-QC-REQUIRED is set
    above, the visual-witness pipeline must actually run and produce a
    receipt: a headless-browser (puppeteer) render of trip-cost.html served
    from PREVIEW-BASE-URL is captured and returns a visual verdict in this
    mission's result object — a rendering check, not just a read of the
    markup. If PREVIEW-BASE-URL turns out to be the wrong local port, that
    failure must surface as a named NO_PREVIEW_BASE_URL-style receipt, not a
    silently skipped check.
  - This mission's authorship and execution prove only that the integration
    is mechanically sound (declared files present with real content, feature
    commit reachable in target history, syntax checks pass, a render was
    actually captured) — not that the trip-cost-split feature's code is
    correct, safe, or behaves right on the live page (e.g. that the settle
    endpoint's transfer-plan math is right, that the self-healing table
    creation is safe under concurrent writers, that the validation helpers
    actually reject bad amount/ccy/date/name input, or that the category
    enum is enforced server-side). That judgment belongs to this mission's
    own witness plus verdict-panel run, downstream of this mission producing
    the integrated result.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

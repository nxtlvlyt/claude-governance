MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.B13-CDN-IMAGE-ROBUST
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: feat/b13-cdn-image-2026-06-23
ALLOW-FILES:
  - js/poi-photos.js
  - map.html

Niyyah: the CDN-image variant feature should surface its own failures, not absorb them. The panel found that the original build degrades silently when Cloudflare Image Resizing isn't enabled, brittle string-matching drops variants on whitespace changes, and catch(_){} contradicts the executor's own "surfaces regressions loudly" comment. Honest failure modes > silent partial success.

Maqsad: when CF Image Resizing is unavailable OR a code change breaks the variant emission, the failure surfaces in the browser console + the QC Console gate sees it. Same panel re-run returns pass/concern.

Context (substrate paths):
  - Panel findings: C:\Users\marka\AppData\Local\Temp\claude\C--Users-marka\4c4a63ee-b0e6-4c65-9e09-561a76fbb9b0\tasks\wdsq4mxb2.output (results[7])
  - Branch: github/feat/b13-cdn-image-2026-06-23
  - Cloudflare Image Resizing docs: https://developers.cloudflare.com/images/transform-images/
  - QC panel harness: C:\Users\marka\code\mt-audit\qc-harness-v2.mjs

Panel findings (deeds to close, verbatim):
  1. No runtime guard if CF Image Resizing not enabled — broken images render silently
  2. Brittle inline-style fingerprint in applyVariants() — substring match drops variants on minification
  3. selfCheck IIFE catch(_){} contradicts its own "surfaces regressions loudly" comment
  4. popupopen rewrite swallows errors with catch(_){} — CDN-variant rewrite failures silent
  5. cdnVariant(key,width) calls encodeURI(key) with no null-guard — encodeURI(null)='null'

Done means:
  - A first-load runtime probe verifies CF Image Resizing is actually responding; if not, the feature degrades to direct R2 URLs with a single console warn (not silent).
  - Variant selection uses a stable selector (data-attribute, class, or marker) — not brittle string substring matching.
  - Catches log to console (warn at minimum) so the QC Console gate sees them. The "surfaces regressions loudly" comment becomes true.
  - cdnVariant null-input contract documented + enforced.
  - Same panel re-fire returns pass/concern; node --check passes; push + ls-remote verify.

Amanah: yours to reason the closure. The chain picks the selector strategy, the probe shape, the logging level.

Constraints: per-file git add, --no-verify, NO LOCAL OLLAMA, Sonnet executor.

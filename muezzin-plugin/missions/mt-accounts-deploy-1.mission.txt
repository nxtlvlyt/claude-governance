MISSION-CLASS: ops-deploy (PRODUCTION INFRA — creates a new D1 DB + deploys the auth Worker in the operator's Cloudflare account; persistent; operator-authorized 2026-06-18)
MISSION-ID: M-MUDDYTIRES.MT-ACCOUNTS-DEPLOY-1
REPO-ROOT: E:\AI_Storage\muddytires-frontend-wt
ALLOW-FILES: workers/auth-accounts.js (read), d1/schema-users.sql (read), workers/wrangler.auth.toml (create if absent)
REQUIRES: none — mt-cutover-1 is DONE (live-verified, deploy ea13769f) and they are functionally independent. Clear to fire.
CSRF-NOTE: workers/auth-accounts.js now ENFORCES a CSRF gate (security review wm6roic7c fix, applied 2026-06-18): state-changing POSTs require an allowed Origin header. Default allowlist = https://muddytires.ca / www.muddytires.ca / muddytires.pages.dev (env-overridable via ALLOWED_ORIGINS). The round-trip in step 6 MUST send an allowed Origin or it will (correctly) 403.

# WHY: Operator authorized 2026-06-18 (decision: "Deploy accounts now"). The account system (free email/password, PBKDF2 100k,
# server-side revocable sessions, httpOnly+Secure cookie) is built + security-verified + committed (3ff7d9b) but NOT deployed.
# It is the gate under add-a-spot, reviews, admin moderation, and the community/social platform the operator wants to focus on.
# This deploys it. The earlier "blocked" was the approval gate (now cleared), not a permissions wall — the token has d1(write) +
# workers_scripts(write). Command-class: deterministic wrangler steps + a verified signup/login round-trip.

Niyyah: stand up the account system in the operator's Cloudflare account so the community platform can unblock — create the auth
D1, apply the users schema, deploy the auth Worker with a real SESSION_KEY secret, and PROVE it with a signup -> login -> session
round-trip against the deployed Worker URL. Never declare done on a deploy that hasn't round-tripped. Production frontend is NOT
wired to this yet (that is a later, separately-gated step) — this only stands the backend up on its own worker URL.

GROUND IN (read before acting — REQUIRED):
- READ workers/auth-accounts.js (the endpoints: signup / login / logout / verifySession / me; the env bindings it expects — the D1
  binding name + SESSION_KEY secret name). Build the wrangler config to MATCH what the code reads. Do NOT guess binding names.
- READ d1/schema-users.sql (the tables to apply).
- `wrangler whoami` must show markabass@gmail.com with d1(write) + workers(write). Else HALT WITH RECEIPT.

Maqsad: ONE deliverable — a deployed, working auth Worker backed by its own D1, proven by a live round-trip. Steps:
1. D1 ALREADY EXISTS (created 2026-06-18 by a prior attempt): database_name=muddytires-accounts,
   database_id=702a6b94-f505-4eb3-a186-ad6e47818cf8. USE IT — do NOT run `wrangler d1 create` again (it errors on a duplicate name).
   Just record this id in the receipt and proceed.
2. APPLY SCHEMA (ABSOLUTE --file path — the executor CWD is NOT the repo root, so a relative `d1/...` path fails; this was
   attempt-1's REAL failure: "Unable to read SQL text file d1/schema-users.sql"):
   `wrangler d1 execute muddytires-accounts --remote --file "E:\AI_Storage\muddytires-frontend-wt\d1\schema-users.sql"`
   -> then ASSERT tables exist: `wrangler d1 execute muddytires-accounts --remote --command "SELECT name FROM sqlite_master WHERE type='table'"` shows users + user_sessions + login_attempts.
3. CONFIG: workers/wrangler.auth.toml is PRE-STAGED (conductor 2026-06-18 — auth-config authoring is the engine's model-emit
   weak class). VERIFY it exists and binds D1 `DB` -> muddytires-accounts (id 702a6b94-f505-4eb3-a186-ad6e47818cf8); do NOT
   re-author it. (binding `DB` matches auth-accounts.js env.DB; worker name muddytires-accounts; workers_dev=true.)
4. DEPLOY (ABSOLUTE config; creates the worker; wrangler resolves main="auth-accounts.js" relative to the toml dir = workers/):
   `wrangler deploy -c "E:\AI_Storage\muddytires-frontend-wt\workers\wrangler.auth.toml"` -> capture the deployed *.workers.dev URL.
5. SECRET (AFTER deploy — the worker must exist first; non-interactive + secret-safe; ABSOLUTE config): generate a 64-hex
   SESSION_KEY and pipe it WITHOUT echoing the value:
   `$k = -join ((1..64) | ForEach-Object { '{0:x}' -f (Get-Random -Maximum 16) }); $k | wrangler secret put SESSION_KEY -c "E:\AI_Storage\muddytires-frontend-wt\workers\wrangler.auth.toml"`
   Record ONLY "SESSION_KEY set (len 64)" in the receipt — NEVER the value. (Secrets go live immediately; no re-deploy needed.)
6. VERIFY ROUND-TRIP (positive-assertion, EXIT NON-ZERO ON ABSENCE) — the worker enforces the CSRF gate, so the POSTs MUST carry
   an allowed Origin: POST /signup with `-H "Origin: https://muddytires.ca"` (a test account) -> 200/created; POST /login same
   creds + same Origin header -> 200 + a Set-Cookie session; GET /me with that cookie -> 200 + the account. ALSO assert one
   POST /login WITHOUT an Origin header -> 403 (proves the CSRF gate is live, not bypassed). Then DELETE the test rows (leave the
   DB clean). Record all response codes (incl. the expected 403) in the receipt.

Done means: D1 `muddytires-accounts` exists with the users schema applied; the auth Worker is deployed on its workers.dev URL with
SESSION_KEY set; a signup->login->session round-trip returned success and the test rows were cleaned. database_id + worker URL +
the three round-trip codes are in the receipt. The production frontend is NOT wired to it (later gate). SESSION_KEY value never logged.

Constraints: operator-authorized infra (this DOES create a real D1 + deploy a worker — that is the point); TEST worker name only
(no production-domain route in this mission); SECRET HYGIENE (SESSION_KEY value never written to any log/receipt/board); config
MATCHES the code's bindings (read the worker, don't guess); round-trip-or-fail (no success without the live round-trip); clean up
test rows; deterministic command-class. HALT WITH RECEIPT if auth-accounts.js expects bindings the mission can't satisfy.

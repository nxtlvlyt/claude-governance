MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.CORS-FIX-AUTH-1
REPO-ROOT: E:\AI_Storage\muddytires-frontend-wt
ALLOW-FILES:
  - workers/auth-accounts.js
REQUIRES: none

Niyyah: the signup and login backend genuinely works — but a real browser silently refuses to talk to it, because the worker answers
credentialed requests with a wildcard CORS origin the browser is required to reject. A traveller leaving tomorrow will fill in the form
and watch it fail for no visible reason. Make the worker answer honestly: name the site it trusts, and allow the credential through.

Maqsad: fix the CORS bug in the auth worker so a logged-in (credentialed) request from https://muddytires.ca succeeds in a real
browser. "Done" is: every response the worker returns (including the OPTIONS preflight) sets Access-Control-Allow-Origin to the
REQUEST's Origin when that Origin is allowed (echo it, never `*`), sets Access-Control-Allow-Credentials: true, and sets Vary: Origin.
node --check passes; committed. NOT deployed (the conductor deploys with the operator's token).

Context (facts only — verified this session):
- THE BUG: workers/auth-accounts.js lines ~4-6 define a STATIC cors object with `'Access-Control-Allow-Origin': '*'` and no
  Allow-Credentials header. The frontend (account.js) fetches with credentials:'include'. The CORS spec forbids credentialed requests
  against a wildcard ACAO, so browsers BLOCK login/session. (curl passed because curl ignores CORS — do not be fooled by curl.)
- THE FIX SHAPE: the worker ALREADY has `allowedOrigins(env)` (~line 33) and `originAllowed` (~lines 40-42) for its CSRF gate — REUSE
  that allow-list. Build the CORS headers PER-REQUEST from the request's Origin: if `allowedOrigins(env).includes(origin)`, set
  Access-Control-Allow-Origin: <that origin>, Access-Control-Allow-Credentials: 'true', Vary: 'Origin', and keep the existing
  Allow-Methods/Allow-Headers. Apply these headers to EVERY response path — the OPTIONS preflight (~line 267) AND every GET/POST/error
  response — so none still emit the static `*`. https://muddytires.ca must be in the allow-list (confirm allowedOrigins includes it; if
  it only lists localhost/workers.dev, ADD the production origin).
- COMMIT DISCIPLINE: explicit `git add workers/auth-accounts.js` (and `workers/wrangler.auth.toml` if it is this worker's deploy config
  and currently untracked) — NEVER `git add .` (the repo has untracked .wrangler/, _maptwist-locate.txt, a modified about.html that are
  NOT this mission's). commit --no-verify.
- Out of scope: any deploy; the write worker (separate mission/repo); about.html; the frontend JS.

Done means (acceptance contract): workers/auth-accounts.js returns, on EVERY response incl. OPTIONS, ACAO=echoed-allowed-Origin (never
`*`) + Allow-Credentials: true + Vary: Origin, with https://muddytires.ca allowed; node --check passes; committed via explicit per-file
git add, --no-verify; NOT deployed. A bad/foreign Origin still gets rejected (the CSRF gate stays intact) — that is correct.

Constraints: code-repo, writes ONLY workers/auth-accounts.js (+ its wrangler.auth.toml if untracked); reuse the existing allow-list, do
not weaken the CSRF gate; explicit per-file git add (never -A); commit --no-verify; deeds proven by receipts.

MISSION-ID: M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - workers/sitemap-prune/index.js
PARENT: M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS  (sub-mission 1 of 2, auto-split by the size ceiling)
REQUIRES: none (first sub-mission of 2 in tartib order)

Niyyah: fix the 3 SOTA-panel-confirmed defects on the original Sonnet build of sitemap-prune (wdsq4mxb2 — BLOCK at sha 530457f). These are real production-risk issues: uncaught D1 awaits become CF 1101 errors, BATCH_SIZE=200 exceeds Cloudflare's 50 subrequest limit on Free plan, and unvalidated sitemap URLs allow SSRF on a maliciously-edited sitemap. Honest about CF limits — the worker would silently fail in production.

Maqsad: this is part 1 of 2 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): patch the existing branch feat/b13-sitemap-prune-2026-06-23 with the corrective patches below. Re-fire the panel and confirm pass/concern.

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - Sync the existing worktree to the remote tip of the target branch before editing. Engine runs the pull and asserts HEAD is on feat/b13-sitemap-prune-2026-06-23 (fail-closed if the branch name is absent).
  - Single authoring pass on workers/sitemap-prune/index.js (ONE-WRITER: this is the only step that writes this file; the executor emits FULL file contents, so all seven fixes land in this one emission and the existing file MUST be read via context_dependencies to avoid dropping unchanged lines). Apply ALL panel fixes: (1) BLOCK#1 — wrap every `await upsert404(...)` and `await writeCursor(...)` in handleRefresh in individual try/catch; each catch does console.error and returns Response.json({ok:false,error:'db',detail:String(e)},{status:500}). (2) BLOCK#2 — wrap the entire default export fetch() handler body in an outer try/catch returning Response.json({ok:false,error:'internal',detail:String(e)},{status:500}) so no path emits CF 1101. (3) BLOCK#3 — ensureSchema catch matches ONLY /(already exists|duplicate column)/i (set schemaEnsured=true and return); ALL other errors `throw e` (never set schemaEnsured=true on unknown errors). (4) BLOCK#4 — set BATCH_SIZE=40 (operator-ratified value; UNVERIFIED whether it is a const here vs wrangler.toml [vars] — file_read unavailable; panel finding #4 cites it as a code constant so it is treated as living in index.js). Process at most 40 URLs per /refresh from the stored cursor, then return {ok:true,processed,nextCursor,done:false} or {done:true}; caller pages. Add a header comment documenting the CF free-plan 50-subrequest budget (link https://developers.cloudflare.com/workers/platform/limits/) and DISABLE/gate the per-URL GET fallback (1 sitemap + 40 HEAD = 41 ≤ 50; HEAD+GET would reach ~81 and BLOW the 50 ceiling — verified 2026-06-23, free stayed 50 after the 2026-02-11 paid-only raise to 10,000). (5) CONCERN#7 SSRF — define ALLOWED_HOSTS (['muddytires.ca','www.muddytires.ca'], operator-extendable); for each extracted <loc> do new URL(loc) and `continue` with console.warn for any host not in the allowlist. (6) CONCERN#6 race — optimistic lock: SELECT cursor_value WHERE id=1, then UPDATE cursor_value=? WHERE id=1 AND cursor_value=?; if result.meta.changes===0 return Response.json({ok:false,error:'conflict'},{status:409,headers:{'Retry-After':'5'}}). (UNVERIFIED cursors table column names — adapt if schema differs.) (7) CONCERN#5 CDATA — augment the <loc> regex to a CDATA-aware pattern e.g. /<loc>(?:<!\[CDATA\[)?([^<\]]*?)(?:\]\]>)?<\/loc>/g, trim captures, and add a comment that DOMParser is unavailable in the Workers runtime (verified 2026-06-23) and HTMLRewriter is the streaming upgrade path.
  - Witness BLOCK#1: inner D1 try/catch returns a structured db error. Asserts the error:'db' JSON response marker is present (Select-String -Quiet exits non-zero if absent).
  - Witness BLOCK#2: outer fetch() try/catch is present. Asserts the error:'internal' marker exists (fails non-zero if absent).
  - Witness BLOCK#3: ensureSchema only swallows expected errors. Asserts the 'already exists' guard pattern is present (fails non-zero if absent).
  - Witness BLOCK#4: BATCH_SIZE is capped at 40 (≤ free-plan budget). Asserts BATCH_SIZE = 40 (fails non-zero if absent or any other value).
  - Witness CONCERN#7: SSRF host allowlist in place. Asserts the muddytires.ca allowlist host is referenced (fails non-zero if absent).
  - Witness CONCERN#6: optimistic cursor lock in place. Asserts the conditional UPDATE clause 'AND cursor_value' is present (fails non-zero if absent).

Done means: steps M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1.1..M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1.8 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — panel re-check pass/concern; BATCH_SIZE ≤40; outer try/catch in fetch(); ensureSchema catches only expected errors; URL host-validation in place; cursor optimistic-locking in place; all 4 BLOCK findings + 3 CONCERN findings have visible code addressing them.

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
MISSION-ID: mt-tripcost-receipt-ai.S1
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - trip-cost.html
  - js/trip-cost-split.js
  - functions/api/trip-cost/extract.js
UNLINKED-OK: this adds capability to the already-live /trip-cost page; reachability is owned by mt-tripcost-nav-link.S1 (queued ahead)
REQUIRES: none (tartib SATISFIED 2026-07-17: mt-copy-clarity.S1 split into S1.S1+S1.S2, both RESOLVED-LANDED — the plain-language rewrites are ancestors of HEAD (a7b72ec index.html, 136eecd map.html, merge-base receipts), so this feature's UI strings are authored against the POST-rewrite page as the tartib intended)
STEPS: 6

Maqsad: Operator word 2026-07-03 ("it should exist... Gemma 4 helping them"): on the
trip-cost page, a person can SNAP A PHOTO of a real receipt and Gemma-4 reads it into the
form — closing the gap between what the page's old copy implied ("add their receipts") and
what it did (typed fields only). PROVEN FOUNDATION (conductor live probe this hour):
/api/oracle/nl-brief returns real google/gemma-4-31b-it briefings on production — the
AIMLAPI binding is functional. Deliverable, mirroring nl-brief's receipted TAQWA pattern:
(R1) functions/api/trip-cost/extract.js — POST { image_b64, mime } (JPEG/PNG/WebP, hard cap
~2.5MB base64; reject larger with a plain-language 413), sends the image to the AIMLAPI
vision-capable Gemma-4 model with a prompt asking ONLY for { amount, currency, category
(one of the page's list), description, spent_at } from the receipt, and returns
{ ok:true, suggestion:{...}, degraded:false } — or { ok:true, suggestion:null,
degraded:true } on ANY model failure/low-confidence read. NEVER fabricate: an amount the
model cannot read confidently comes back null, never guessed. The photo is processed
TRANSIENTLY — never written to R2/D1/disk (the R2 perms wall stays untouched; state the
transient handling in a code comment AND in the UI copy).
(R2) trip-cost.html + js/trip-cost-split.js — a "Snap your receipt" control (file input,
accept=image/*, capture=environment) that POSTs to extract.js, then PRE-FILLS the existing
form fields with the suggestion for the USER TO CONFIRM OR EDIT — the human always presses
Add contribution; the AI never submits. Entries added from a photo carry a small
"from photo" tag in the contributions list — that list IS the "what got ingested" view the
operator asked for (every AI-read entry visible, editable, deletable like any other).
Grade-6 copy per the copy-clarity bar; no jargon ("We read the photo and fill in the
numbers. Check them, then add.").

niyyah: The operator expected the site's existing AI helpfulness (crown-land briefings) to
meet him here, and the honest version is exactly bounded: the model SUGGESTS, the human
CONFIRMS, failures degrade to the typed form that already works, nothing is stored. Clear,
not clever — the receipt photo becomes typed-in numbers the user can see and fix.

Steps:
  1. Idempotent baseline: record branch + HEAD; if functions/api/trip-cost/extract.js exists output CLEAN_ALREADY and treat edit steps as verify-only presence checks; else CLEAN_BASELINE. Never exit 1 on existence. [command]
  2. Land the endpoint DETERMINISTICALLY (converted 2026-07-17 after two attempts died on empty executor emissions at the old authoring step — the one-step-authors-three-files kill-class; artifacts drafted by prep wf_570d9e67 grounded in nl-brief.js/feedback.js/near.js, staged + node --check green + dry-run receipted). Run exactly: New-Item -ItemType Directory -Force functions/api/trip-cost | Out-Null; Copy-Item 'C:\Users\marka\.claude\muezzin-plugin\missions\_logs\staged-tripcost-2026-07-17\extract.js' functions/api/trip-cost/extract.js -Force; node --check functions/api/trip-cost/extract.js; if ($LASTEXITCODE -ne 0) { Write-Output EXTRACT-SYNTAX-FAIL; exit 1 }; Write-Output ENDPOINT-STAGED [command]
  3. Land the UI graft DETERMINISTICALLY (same conversion; both patchers anchored fail-closed idempotent, dry-run receipts: PATCH_OK 10 sentinels / ALREADY_PATCHED, PATCH_APPLIED 16 graft lines CRLF / ALREADY_APPLIED). Run exactly: node 'C:\Users\marka\.claude\muezzin-plugin\missions\_logs\staged-tripcost-2026-07-17\patch-trip-cost-split.mjs' .; if ($LASTEXITCODE -ne 0) { Write-Output SPLIT-PATCH-FAIL; exit 1 }; node 'C:\Users\marka\.claude\muezzin-plugin\missions\_logs\staged-tripcost-2026-07-17\patch-trip-cost-html.mjs' trip-cost.html; if ($LASTEXITCODE -ne 0) { Write-Output HTML-PATCH-FAIL; exit 1 }; node --check js/trip-cost-split.js; if ($LASTEXITCODE -ne 0) { Write-Output SPLIT-SYNTAX-FAIL; exit 1 }; Write-Output UI-GRAFTED [command]
  4. Verify (literal payload pinned 2026-07-17 after rule-16 refusal). Run exactly: node --check js/trip-cost-split.js; if ($LASTEXITCODE -ne 0) { Write-Output SPLIT-JS-SYNTAX-FAIL; exit 1 }; node --check functions/api/trip-cost/extract.js; if ($LASTEXITCODE -ne 0) { Write-Output EXTRACT-JS-SYNTAX-FAIL; exit 1 }; if (-not (Select-String -Path functions/api/trip-cost/extract.js -Pattern 'MAX_IMAGE_BYTES|size' -Quiet)) { Write-Output SIZE-CAP-MISSING; exit 1 }; if (-not (Select-String -Path functions/api/trip-cost/extract.js -Pattern 'degraded' -Quiet)) { Write-Output DEGRADED-PATH-MISSING; exit 1 }; if (Select-String -Path functions/api/trip-cost/extract.js -Pattern '\.put\(|D1.*INSERT.*image|R2' -Quiet) { Write-Output IMAGE-PERSISTENCE-FOUND-FAIL; exit 1 }; if (Select-String -Path js/trip-cost-split.js -Pattern 'auto.?submit|submit\(\)\s*;?\s*//\s*auto' -Quiet) { Write-Output AUTO-SUBMIT-FOUND-FAIL; exit 1 }; Write-Output STEP4-VERIFIED [verify]
  5. Scoped commit (RULE 15 pathspec form, amended 2026-07-17 after MIQAT bare-commit refusal). Run exactly: git add -- trip-cost.html js/trip-cost-split.js functions/api/trip-cost/extract.js; git diff --cached --quiet -- trip-cost.html js/trip-cost-split.js functions/api/trip-cost/extract.js; if ($LASTEXITCODE -ne 0) { git commit -m "feat(trip-cost): receipt-photo Gemma-4 extract endpoint + snap-your-receipt pre-fill (operator word 2026-07-03; mt-tripcost-receipt-ai.S1)" -- trip-cost.html js/trip-cost-split.js functions/api/trip-cost/extract.js; if ($LASTEXITCODE -ne 0) { exit 1 } } else { Write-Output NOTHING_TO_COMMIT }; git log -1 --format=%H [command]
  6. OUTCOME CHECK (literal payload pinned 2026-07-17; degraded:true from the model is an ACCEPTABLE result — the honest path IS a working path). Run exactly: $clean = @(git status --porcelain -uno -- trip-cost.html js/trip-cost-split.js functions/api/trip-cost/extract.js); if ($clean.Count -gt 0) { Write-Output TREE-DIRTY; exit 1 }; $out = npx wrangler pages deploy . --project-name=muddytires --branch=preview 2>&1 | Out-String; $m = [regex]::Match($out, 'https://[a-z0-9-]+\.muddytires\.pages\.dev'); if (-not $m.Success) { Write-Output NO_PREVIEW_BASE_URL; exit 1 }; $url = $m.Value; Write-Output "DEPLOYED: $url"; $ok = $false; for ($i=0; $i -lt 10; $i++) { try { $r = Invoke-WebRequest -Uri "$url/trip-cost" -UseBasicParsing -TimeoutSec 10; if ($r.StatusCode -eq 200) { $ok = $true; break } } catch {}; Start-Sleep -Seconds 5 }; if (-not $ok) { Write-Output POLL-FAILED; exit 1 }; $png = 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR4nGP4//8/AwAI/AL+p3qZhAAAAABJRU5ErkJggg=='; try { $resp = Invoke-RestMethod -Uri "$url/api/trip-cost/extract" -Method Post -ContentType 'application/json' -Body (@{image=$png}|ConvertTo-Json) -TimeoutSec 60; $shape = if ($resp.suggestion -or $resp.degraded) { 'ok' } else { 'bad' } } catch { $shape = 'bad'; Write-Output "EXTRACT_ERR: $($_.Exception.Message)" }; Write-Output "EXTRACT_SHAPE=$shape"; $js = 'import { chromium } from "playwright"; const b = await chromium.launch(); const ctx = await b.newContext({ viewport: { width: 430, height: 932 }, isMobile: true, hasTouch: true }); const p = await ctx.newPage(); const errs = []; p.on("pageerror", (e) => errs.push(String(e))); await p.goto(process.argv[2] + "/trip-cost", { waitUntil: "load", timeout: 60000 }); const html = await p.content(); const snap = /[Ss]nap/.test(html); await b.close(); console.log(JSON.stringify({ snap, fatalErrors: errs.length })); if (!snap || errs.length > 0) process.exit(1);'; Set-Content scratch-tripcost-witness.mjs -Value $js; node scratch-tripcost-witness.mjs $url; $rc = $LASTEXITCODE; Remove-Item scratch-tripcost-witness.mjs -ErrorAction SilentlyContinue; if ($shape -ne 'ok' -or $rc -ne 0) { Write-Output RECEIPT_AI_QC_FAIL; exit 1 }; Write-Output RECEIPT_AI_QC_PASS [command]

Done means: the three files committed at HEAD; the step-6 evidence lines show the extract
endpoint answering with the honest suggestion/degraded shape and the page carrying the
photo control with the typed flow intact — a person can photograph a receipt and Gemma
fills the form for them to confirm, failures fall back to typing, nothing is stored, and
every AI-read entry is visible in the contributions list with its from-photo tag
(live-outcome bar; operator word receipt 2026-07-03).

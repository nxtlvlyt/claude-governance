MISSION QC-CONCERN-OPERATORS-HTML-BUSINESS-CLAIM-PAGE-2026-06-25
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - operators.html
REQUIRES: none
VISUAL-QC-REQUIRED
PREVIEW-BASE-URL: http://localhost:8788

Niyyah: Finish the QC concern named "operators.html business-claim page" from the
2026-06-25 e2e-runner walk — a prior attempt at this exact mission file was authored,
never reached its architect/execution phase, and was later deleted; this is a corrected
re-authoring, not a first attempt.

Correction against live substrate (verified this session, 2026-07-01): the recovered
prior mission text (still readable at
C:\Users\marka\.claude\muezzin-plugin\_prior-attempt\missions_qc-concern-operators-html-business-claim-page-2026-06-25.mission.txt)
told the fix to "restore behavior to that specified in docs/FEATURE-CATALOG-2026-06-23.md."
That citation does not hold up. Reading docs/FEATURE-CATALOG-2026-06-23.md directly this
session (grep for "### Feature" and "Page:") shows exactly three cataloged features —
Vehicle profile picker, Fuel-cost chip, Carbon-footprint chip — all on /map. operators.html
appears nowhere except one line in a TODO skeleton ("Page-level features (18 HTML pages)"
-> "/operators") carrying only the generic requirement "a render-check (page loads to 200
+ has expected heading), a clutter-check (visual density ratio vs baseline)." There is no
operators-specific layout spec anywhere in that file to "restore." The prior mission's fix
directive overstated its own citation; this mission does not repeat that overstatement.

Second correction: the prior mission's ALLOW-FILES was `js/*.js` and `workers/**/*.js` —
it never included operators.html itself, even though the only file the fix can possibly
touch is operators.html (a static page with its markup, not a .js module). Had that
mission reached the executor, resolveRepoTarget would have rejected the one edit it
needed to make. This mission's ALLOW-FILES is corrected to the one file the fix actually
requires.

Third correction (why this mission carries no TARGET-BRANCH header): the prior mission's
ONLY recorded failure (C:\Users\marka\.claude\muezzin-plugin\missions\qc-concern-operators-html-business-claim-page-2026-06-25.mission.result.json,
phase "sandbox") was the engine refusing to check out this mission's declared
TARGET-BRANCH over a dirty shared worktree left by the immediately-prior mission that
day (an untracked `_disc-layerctl.txt` file) — confirmed by the retro file showing
events:0, plan-phases:0, steps-committed:0, and by the mission's own sandbox directory
being genuinely empty (verified via both `ls -la` and `Get-ChildItem -Force -Recurse`
this session). Nothing about the page content or the fix itself ever ran. This was a
worktree-hygiene/sequencing failure, not a content failure. Verified this session that
this class of failure is real and current: `git status --porcelain` in the target repo
right now shows staged additions and a modified map.html from an unrelated,
concurrently-in-flight mission, plus the same class of pre-existing untracked debris
(api-index-response.json, docs/e2e-report-2026-07-01.json, e2e-shots/,
feature-catalog-entry.txt, scripts/e2e-runner.mjs) noted in other missions' Context
sections this same day. A hardcoded TARGET-BRANCH forces a checkout that can be refused
by exactly this kind of debris regardless of which branch name is chosen. Following the
pattern already in use elsewhere in this missions folder (e.g.
mt-integrate-dynamic-favicon-fire-ban.mission.txt, which deliberately carries no
TARGET-BRANCH for the same reason), this mission lands its commit onto whatever branch
is actually checked out as HEAD when it fires, not a hardcoded branch name.

Maqsad: The 2026-06-25 e2e-runner walk of operators.html (the "claim your listing"
B2B landing page) named two GATE failures verbatim:
  GATE "Page 200, form visible" — FAIL: the page loaded (200) but the claim form is
  not visible in the screenshot captures (likely below the fold or needs interaction).
  GATE "Tier comparison block, claim form, no clutter" — FAIL: neither the tier
  comparison block (#tiers) nor the claim form (#claim-form) are visible in the
  captures.

Re-verified live and current this session (not assumed stale): the target repo's
operators.html is unchanged since 2026-06-25 (confirmed no commits touch it: `git diff
main HEAD -- operators.html` and `git diff main
feat/qc-concern-operators-html-business-claim-page-2026-06-25 -- operators.html` both
return empty). A headless Chromium render (Playwright, already present in this repo's
node_modules — no new dependency needed) of the live file at a 1440x900 viewport (the
size e2e-runner.mjs itself uses for every feature it walks) measures:
  #tiers   boundingBox().top ~= 897  (of a 900px-tall viewport — effectively AT the
           fold, not meaningfully visible on first paint)
  #claim-form boundingBox().top ~= 2122  (over 1200px below the fold)
Root cause, confirmed by reading the file: <main> currently opens with a 4-card "Why
claim" feature-grid section BEFORE <section id="tiers">, and <section id="claim"> lists
its 3-step "how it works" <ol> BEFORE the <form id="claim-form">. Both elements are
present in the DOM (this was never a mount/render bug) — they are simply sequenced too
far down the page for the gate's stated viewport.

Real, achievable Maqsad for this mission: reorder operators.html so
(a) <section id="tiers"> renders inside the initial 1440x900 viewport without scrolling,
and (b) <form id="claim-form"> moves substantially higher on the page — by moving
existing sections/elements, not by deleting, shortening, or rewriting any content. This
mission does NOT claim to make <form id="claim-form"> fully visible within one 900px
screen; the "how it works" 3-step explanation ahead of it is long enough that fully
fitting the form above the fold would require cutting that content, which is a product
decision outside a QC-fix mission's authority. Report the measured before/after numbers
honestly rather than rounding a partial improvement up to "fixed."

Fourth correction (scope of "e2e-runner walk returns {clean}"): the prior mission's
Done-condition was "e2e-runner walk returns {clean}." Verified this session by reading
scripts/e2e-runner.mjs: it parses ONLY `### Feature:` sections out of
docs/FEATURE-CATALOG-2026-06-23.md and walks exactly those; since that file has zero
entries for operators.html, e2e-runner.mjs has no code path to walk this page at all
today. Passing `--features <slug>` for a slug that matches nothing does not error — it
silently reports `features_run: 0` with `counts.clean: 0` and exits 0, which is a
vacuous pass, not a real verdict, if mistaken for one. This mission does not add a
catalog entry to manufacture a literal "clean" verdict from e2e-runner.mjs, because
doing so would require editing docs/FEATURE-CATALOG-2026-06-23.md, which is outside this
mission's ALLOW-FILES (kept tight to operators.html, the one file the actual fix
touches). Closing that catalog gap so e2e-runner.mjs can mechanically cover operators.html
going forward is real, worthwhile follow-on work — it is just not this mission, and this
mission's Done means below does not pretend otherwise.

Context (verified against live substrate this session, 2026-07-01, absolute paths):
- REPO-ROOT C:\Users\marka\code\mt-integration-2026-06-22 currently has an unrelated,
  concurrently-in-flight mission's changes staged (poi-tags additions, modified
  map.html) plus pre-existing untracked debris (api-index-response.json,
  docs/e2e-report-2026-07-01.json, e2e-shots/, feature-catalog-entry.txt,
  scripts/e2e-runner.mjs). None of this touches operators.html. "Clean working tree" in
  this mission's Done means below refers only to this mission's own ALLOW-FILES entry
  (operators.html) — not a demand to clean up unrelated in-flight or pre-existing debris.
- operators.html is a live page served by the site (not a build artifact), so
  VISUAL-QC-REQUIRED and PREVIEW-BASE-URL are set above. PREVIEW-BASE-URL:
  http://localhost:8788 is a PLACEHOLDER for a local `wrangler pages dev` server — probe
  it before trusting any render captured against it; if nothing answers, that is a named
  NO_PREVIEW_BASE_URL-style receipt, not a silently skipped check. The direct
  file:// Playwright render in Steps 2/6 below does not depend on this server and is the
  primary, required evidence; the PREVIEW-BASE-URL capture is secondary/best-effort.
- The two sections being reordered: (1) the "Why claim" 4-card section — identified by
  containing `<div class="mono ...">Why claim</div>` and the heading "Operators on Muddy
  Tires get four things travellers actually use." — currently the FIRST section inside
  <main>, directly above <section id="tiers">. (2) <form id="claim-form"
  ...>...</form> — currently the LAST child of <section id="claim">, after its
  <ol class="space-y-6"> three-step list.

Steps:
  1. (amended 2026-07-07: prior attempt died on ENOENT writing into a nonexistent
     e2e-shots/ dir) FIRST create the evidence dir explicitly: New-Item -ItemType
     Directory -Force e2e-shots. Then, in the target repo, record the current branch
     and HEAD, and confirm operators.html
     is still in its pre-fix state: using Select-String/grep with line numbers, confirm
     the line containing "Why claim" (the mono label div) has a LOWER line number than
     the line containing `<section id="tiers"`, and the line containing
     `<ol class="space-y-6">` has a LOWER line number than the line containing
     `<form id="claim-form"`. If either is already flipped, stop and report — the
     baseline assumption is wrong and the rest of this mission's steps must be
     re-derived, not run blind. [command] verify-pre-fix-baseline
  2. Using the Playwright package already installed in this repo's node_modules (no new
     dependency), launch headless Chromium, open operators.html via a file:// URL at a
     1440x900 viewport, and record getBoundingClientRect().top for #tiers and
     #claim-form BEFORE any edit. Expect numbers close to the documented ~897 and ~2122;
     note plainly if they differ meaningfully rather than assuming the prior evidence is
     current. [command] baseline-viewport-measure
  3. Move the entire "Why claim" <section>...</section> block (identified in Context
     above) so it sits AFTER the closing </section> of <section id="claim"> and BEFORE
     the "Trust signals" section that follows it. Move only — do not delete, shorten,
     reword, or duplicate any of its markup or text. Result: <section id="tiers">
     becomes the first section inside <main>, directly below the hero <header>.
     [edit] move-why-claim-section-down
  4. Within <section id="claim">, move the <form id="claim-form" ...>...</form> block so
     it appears immediately after the section's intro <p> paragraph (the one starting
     "After you submit, a human reviewer...") and BEFORE the <ol class="space-y-6">
     three-step list. Move only — the <ol> content, the form's fields/labels, and its
     `onsubmit="return submitClaim(event)"` hookup must stay byte-identical, just
     relocated. [edit] move-claim-form-above-steps
  5. Re-run the same Select-String/grep line-number check as Step 1 against the edited
     file. Confirm the order is now flipped both ways: "Why claim"'s line number is
     GREATER than <section id="tiers">'s, and `<ol class="space-y-6">`'s line number is
     GREATER than `<form id="claim-form"`'s. Also grep-count that `id="tiers"` and
     `id="claim-form"` each still appear EXACTLY ONCE in the file (the move introduced no
     duplication and no deletion). [command] post-edit-order-check
  6. Re-run the same Playwright measurement as Step 2 against the edited file. Required:
     #tiers boundingBox top < 900 (visible without scrolling in the initial viewport —
     this is the literal fix for the "Tier comparison block ... visible" gate). Record
     #claim-form's new top and report it exactly, even if it is still below 900 — do not
     characterize the claim-form gate as fully fixed unless the measured number actually
     supports it; a substantial reduction from the ~2122 baseline is expected progress,
     not a licence to round up. [command] post-edit-viewport-measure
  7. Capture a full-page (fullPage: true) screenshot of the edited operators.html at
     1440x900 and read it: confirm every pre-existing section (hero, tiers, claim/
     how-it-works+form, "Why claim" cards, trust signals, talk-to-a-human CTA, footer) is
     still present, intact, and not visually overlapping or clipped as a result of the
     reorder. This is the "no clutter, no regression elsewhere on this page" check named
     in the original GATE. [command] full-page-regression-screenshot
  8. (amended 2026-07-07 per RULE 9: prior step hand-rolled a localhost:8788 server — nothing serves that port) In ONE command: confirm clean tree for this mission's ALLOW-FILES (git commit first if this mission's edits are not yet committed — never deploy a dirty tree), then wrangler pages deploy . --project-name=muddytires --branch=preview, capture the emitted *.pages.dev URL into a PowerShell variable, write scratch-qc.mjs (playwright render of /operators.html from that URL, exit non-zero if the page does not return HTTP 200 or throws a fatal script error), node scratch-qc.mjs, Remove-Item scratch-qc.mjs. Steps 2/6/7 already provide the required render evidence directly against the file; this deploy-render is the VISUAL-QC-REQUIRED capture. [command] preview-deploy-and-render
  9. Confirm `git status --porcelain` / `git diff --name-only` in the target repo shows
     operators.html as the only file this mission changed (pre-existing unrelated
     staged/untracked items noted in Context are not this mission's to touch or clean
     up). [command] scope-check

Done means:
  - operators.html: `<section id="tiers"` now appears BEFORE the "Why claim" section's
    heading (line-number order flipped from the documented pre-fix baseline, confirmed
    via Select-String/grep) — the receipt that would have caught the original bug, since
    the original file had this order reversed.
  - operators.html: `<form id="claim-form"` now appears BEFORE
    `<ol class="space-y-6">` within <section id="claim"> (line-number order flipped from
    baseline).
  - operators.html: `id="tiers"` and `id="claim-form"` each still appear EXACTLY ONCE
    (grep count check) — no duplication, no accidental deletion from the move.
  - Playwright headless render of the edited operators.html at 1440x900 measures
    `#tiers` boundingBox top < 900 (visible in the initial viewport without scrolling) —
    the literal, directly-measured fix for GATE "Tier comparison block ... visible."
  - Playwright measurement of `#claim-form` boundingBox top is reported as an exact
    number and is substantially lower than the documented ~2122 baseline — stated
    honestly even if it remains below 900; this mission does not claim full first-paint
    visibility for the form unless the number actually supports it.
  - A full-page screenshot of the edited page shows no broken/overlapping/clipped layout
    and every pre-existing section still present.
  - `git status --porcelain` / `git diff --name-only` show operators.html as the only
    changed file.
  - Named, not silently absorbed: docs/FEATURE-CATALOG-2026-06-23.md still has zero
    `### Feature:` entries for operators.html, so `scripts/e2e-runner.mjs` still cannot
    mechanically walk this page, and "e2e-runner walk returns {clean}" (the prior
    mission's literal Done-condition) is explicitly NOT claimed as satisfied here.
    Closing that catalog gap is separate, out-of-ALLOW-FILES follow-on work, not folded
    into this mission's result.
  - Nothing else on this page or elsewhere in the repo is touched by this mission.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

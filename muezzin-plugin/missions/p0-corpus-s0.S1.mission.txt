MISSION-ID: P0-CORPUS-S0.S1.S1
PARENT: P0-CORPUS-S0.S1  (sub-mission 1 of 2, auto-split by the size ceiling)
REQUIRES: none (first sub-mission of 2 in tartib order)

Niyyah: hand-picked source lists miss things; the factory's knowledge must start from discovery, not memory.

Maqsad: this is part 1 of 2 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): this is part 1 of 2 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - Create the discovery/ scratch subdirectory at sandbox root to hold all raw evidence captures. Prerequisite for every later capture step's target path (subdirectory targets need a preceding dir-creation step). Idempotent.
  - Probe the five mission-seeded local folders for on-disk existence and record only the paths that actually resolve (absent paths are intentionally omitted; synthesis in S2 marks any seeded path missing here as skip/not-found). The command WITNESSES disk reality via Get-Item, so a wrong/guessed candidate simply records nothing — no presence is ever fabricated. Seed literals are the best-corroborated candidates available to this dispatch; if upstream substrate names different paths the engine should substitute them, but the witness design keeps the capture honest regardless.
  - Expand discovery beyond the seed list: enumerate top-level subdirectories of the four parent locations (E:\, C:\, E:\AI_Storage, D:\Desktop) to surface sibling project folders the seed never named. This is the discovery-not-memory backstop and depends on no guessed literals. Inaccessible roots are silently skipped (-ErrorAction SilentlyContinue); reachable roots always yield directories so the file is non-empty.
  - Capture the AnythingLLM workspace census via the local Developer API (GET /api/v1/workspaces; verified versioned-API path) into JSON for llm-workspace sources. try/catch guarantees a file is always written: a non-responding service or an auth-required (403) response records an explicit gap marker rather than inventing workspaces, satisfying the honest-failure niyyah. Port 3001 is the documented default; a wrong port is caught as a gap.
  - Capture the local Forgejo repository census via its Gitea-compatible anonymous endpoint (GET /api/v1/repos/search?limit=50; verified anonymous-capable) into JSON for git-repo sources. try/catch writes a gap marker if unreachable; auth-walled/private repos beyond the anonymous result are a recorded gap at S2 synthesis, never guessed. Port 3000 is the Forgejo/Gitea default; a wrong port is caught as a gap.
  - Capture PUBLIC GitHub repos for user nxtlvlyt via the anonymous REST API (User-Agent header set, which GitHub requires) into JSON for git-repo sources. Anonymous enumeration returns public repos only by design; private repos are unenumerable without a token and become an explicit gap entry at S2 synthesis, never guessed. try/catch records a gap marker on network/rate-limit failure.
  - Enumerate Cloudflare Pages projects under the authed account via the wrangler CLI (wrangler pages project list, authed on PATH per mission), capturing stdout+stderr as text evidence for cloud-deployment sources (expected: 2boots, laynas-cookies, regulativity plus any others). try/catch + 2>&1 capture means an unauthed/missing wrangler is recorded as honest gap text, not a hard step failure.
  - Probe reachability of the four named live sites in one curl invocation, writing each effective URL and HTTP status code (one line per site) as live-site evidence. A non-200/unreachable result yields status 000 and is captured as honest reachability data; the trailing Test-Path keeps the step exit-0 so honest failure is recorded, not retried blindly. Site URLs are the best-corroborated candidates available to this dispatch (aligned with the expected Cloudflare projects); the probe records actual reachability so a wrong URL self-reports rather than fabricating a site.

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.B13-TRIP-DIARY-SECURITY-HARDEN
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: feat/b13-trip-diary-user-2026-06-23
ALLOW-FILES:
  - functions/api/trips/[[path]].js
  - functions/api/trips/[id].js
  - functions/api/trips/[id]/stops.js
  - functions/api/trips/[id]/stops/[stop_id].js
  - functions/api/trips/[id]/stops/reorder.js
  - functions/_lib/trip-helpers.js
  - trips.html
  - trip-edit.html

Niyyah: harden the trip-diary feature against the 5-seat SOTA panel findings on the original Sonnet build (wdsq4mxb2 sha — BLOCK verdict at panel_verdict=block, confidence 0.88, 4 of 5 seats agreeing). Apply standard web-security hygiene: CORS allowlist, CSRF defense, transaction wrap, error handling, XSS encoding, race fix on MAX(ord). Substrate-honest: the original feature WORKS (substrate checks all passed), but the panel correctly flagged 5 real security defects that ship-ready code should not have.

Maqsad: patch the existing branch feat/b13-trip-diary-user-2026-06-23 with the 5 corrective patches below. Re-run all the original verification steps + a panel re-check at end (call deliberationPanelReview from C:/Users/marka/code/mt-audit/qc-harness-v2.mjs against the new diff; aim for panel_verdict=pass or concern). Push the result to the same branch as a follow-up commit.

Context (panel findings, verbatim from wdsq4mxb2):
1. CORS wildcard ACAO:'*' combined with Cookie/Authorization forwarding to upstream auth — no origin allowlist (4/5 seats: qwen, glm, gpt-oss, deepseek)
2. No CSRF protection on state-changing POST/PATCH/DELETE endpoints — no Origin check, no token, SameSite enforcement unverified (4/5 seats)
3. Race on ord assignment: POST /api/trips/:id/stops reads MAX(ord) then inserts non-atomically — concurrent stops can collide on ord (3/5 seats)
4. Reorder endpoint issues N UPDATEs via env.DB.batch() without an enclosing transaction — partial failure leaves inconsistent ords (3/5 seats)
5. XSS risk: user-controlled trip/stop fields (title, notes, name, note) rendered into trips.html / trip-editor.html / profile.html without verified output sanitization (2/5 seats)
6. DB-error swallowing in ownerTrip/ownerStop and MAX(ord) lookup masks real failures as 404 / silently defaults nextOrd=0 (1/5 seats)
7. [id].js DELETE: two sequential DELETEs (trip_stop then trip) not wrapped in batch()/transaction — partial failure leaves orphaned trip-less stops (1/5 seats)

Steps:
1. (worktree) git worktree at ../mt-feat-b13-trip-diary-user (existing) on branch feat/b13-trip-diary-user-2026-06-23. git pull --no-rebase github feat/b13-trip-diary-user-2026-06-23 to ensure synced.
2. (CORS) Replace wildcard ACAO with explicit allowlist: ['https://muddytires.ca', 'http://localhost:8788']. Add Vary: Origin header. Document that Access-Control-Allow-Credentials is true (needed for Cookie forwarding). Reject requests from unlisted origins with 403.
3. (CSRF) Add SameSite=Strict to any session-cookie set; reject state-changing requests (POST/PATCH/DELETE) whose Origin header is missing OR not in the CORS allowlist. Document the chosen defense in a one-line comment per handler.
4. (race) Replace MAX(ord)+INSERT with a single atomic INSERT using \`SELECT COALESCE(MAX(ord),-1)+1\` inline (or use UPSERT pattern). Document in the SQL.
5. (txn) Wrap the reorder N-UPDATEs in env.DB.batch() with a SINGLE .run() call (per Cloudflare D1 docs, batch is the atomicity primitive). If any UPDATE fails, the whole batch rolls back.
6. (error) Add outer try/catch in the top-level fetch() handler returning JSON {ok:false,error:'internal',detail:msg} with status 500 on any uncaught throw. NEVER allow CF 1101.
7. (XSS) Audit every interpolated user-string in trips.html / trip-edit.html / profile.html. Use textContent for user content (never innerHTML) OR pass through a local escapeHtml(&<>\"') helper. Add tests if helpful.
8. (delete-atomic) Wrap the [id].js DELETE (trip_stop then trip) in env.DB.batch() so either both delete or neither does.
9. (verify) node --check on every .js touched. Run a local smoke test against any 3 endpoints via curl (Origin: https://muddytires.ca for valid case, Origin: https://attacker.example for rejected case).
10. (panel re-check) Call deliberationPanelReview against the new diff. If panel_verdict=pass, mission DONE. If panel_verdict=concern, mission DONE with caveat. If panel_verdict=block, mission BLOCKED-WITH-RECEIPT (don't loop — surface for operator).
11. (commit) git add per-file (no -A). git commit --no-verify -m "B13 trip-diary: harden CORS/CSRF/race/txn/XSS per SOTA panel findings". git push --no-verify -u github. VERIFY via git ls-remote.

Done means: panel re-check returns pass or concern; all 7 panel findings have visible code-change addressing them in the diff; CORS allowlist concrete; CSRF defense named in comments; race + txn primitives used; XSS escape function visible; outer try/catch in fetch().

Constraints: TARGET-BRANCH already exists; this is a follow-up commit not a new branch. Per-file git add. --no-verify on commit and push. NO LOCAL OLLAMA per operator-rulings. Ollama Cloud preferred; Claude Sonnet fallback for execution per operator's Phase-2 architecture rule.

MISSION-ID: MT-INTEGRATE-NEAR-ME-DISCOVERY.S1
MISSION-CLASS: code-repo
PARENT: MT-INTEGRATE-NEAR-ME-DISCOVERY
TARTIB-INDEX: 1 of 2
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - map.html
  - functions/api/spots/near.js
  - js/near-me.js
VISUAL-QC-REQUIRED
REQUIRES: none (first in tartib order)
STEPS: 9

PARENT MAQSAD: Land commit ca83ba4 ("feat(near-me): GET /api/spots/near + \"Spots near me\"

Maqsad: sub-mission 1 of 2 — Record the current branch and latest commit, and assert neither functions/api/spots/near.js nor js/near-me.js already exist on disk (fails if either is unexpectedly present). through Start (or confirm running) a local preview server for the target repo at the PREVIEW-BASE-URL host:port so the render witness in the next step has something to reach.

STEP-SHAPE DISCIPLINE (amended 2026-07-10 after MIQAT-REFUSED 2026-07-09T18:52 —
"VISUAL-QC-REQUIRED mission has no step that executes anything" — plus the receipted
kill-classes the original steps carried: bare exit-1-on-existence step 1 = the
d1-backup/gpx false-fail class; "start a local preview server" step 8 = the RULE-9
localhost-phantom class that killed trip-cost twice).

STEP 4 ADDED 2026-07-14 (conductor, diagnosing the 2026-07-11 FAILED attempt): step 3's
cherry-pick conflict on map.html was tolerated but never resolved — steps 4-6 (old
numbering) only checked syntax/presence, never conflict markers, so it silently sailed
through to step 7's commit, which correctly refused ("COMMIT-CONFLICT: unresolved git
conflict markers"). Root cause of the conflict, inspected live via an atomic
cherry-pick/abort probe: HEAD has evolved forward with many new scripts/styles since
ca83ba4 branched off; ca83ba4's ONLY actual contribution to map.html is the near-me.js
script tag. Deterministic resolution (never freeform "hand-resolve both sides" — that
class failed 3 different ways on the weather-aware-planning sibling mission this same
session): keep HEAD's side of the conflict entirely, insert exactly the near-me.js
script tag at the position it held on ca83ba4's side (immediately after the
spot-list-sidebar.js tag, a stable anchor present on both sides). Verified via an atomic
dry-run (cherry-pick -> resolve -> verify zero markers + tag present + script count 46
-> full abort/reset, repo confirmed back to clean baseline, HEAD unchanged) before this
step was added.

Steps:
  1. Baseline, stdout only, IDEMPOTENT. Run exactly: git rev-parse --abbrev-ref HEAD; git rev-parse HEAD; if ((Test-Path functions/api/spots/near.js) -or (Test-Path js/near-me.js)) { Write-Output ALREADY_PRESENT } else { Write-Output CLEAN_BASELINE } [command]
  2. Preconditions + ancestry, IDEMPOTENT. Run exactly: if (Test-Path (git rev-parse --git-path CHERRY_PICK_HEAD)) { exit 1 }; git merge-base --is-ancestor ca83ba4 HEAD; if ($LASTEXITCODE -eq 0) { Write-Output ALREADY_INTEGRATED } else { Write-Output NOT_YET_INTEGRATED }; exit 0 [command]
  3. Land ca83ba4; a conflict exit is TOLERATED — the assertion is the artifacts. Run exactly: git merge-base --is-ancestor ca83ba4 HEAD; if ($LASTEXITCODE -eq 0) { Write-Output SKIP_ALREADY_INTEGRATED; exit 0 }; git cherry-pick -x ca83ba4; if (Test-Path (git rev-parse --git-path CHERRY_PICK_HEAD)) { git cherry-pick --continue --no-edit }; if (-not ((Test-Path functions/api/spots/near.js) -and (Test-Path js/near-me.js))) { exit 1 } [command] map.html
  4. Resolve the map.html conflict deterministically (idempotent — no-op if step 3 left no conflict markers). Run exactly: if (-not (Select-String -Path map.html -Pattern '^<{7}' -Quiet)) { Write-Output NO-CONFLICT-SKIP; exit 0 }; node "C:\Users\marka\.claude\muezzin-plugin\missions\_logs\near-me-conflict-resolve.mjs" map.html; if ($LASTEXITCODE -ne 0) { exit 1 } [command] map.html
     validation_command: if (Select-String -Path map.html -Pattern '^<{7}|^={7}|^>{7}' -Quiet) { exit 1 }; if (-not (Select-String -Path map.html -Pattern 'near-me.js' -Quiet)) { exit 1 }
  5. Verify the newly integrated API handler is syntactically valid JavaScript. [verify] functions/api/spots/near.js
  6. Verify the newly integrated client script is syntactically valid JavaScript. [verify] js/near-me.js
  7. Artifact witness for the wiring. Run exactly: if (-not (Select-String -Path map.html -Pattern 'near-me.js' -Quiet)) { exit 1 }; if (-not ((Select-String -Path map.html -Pattern '<script' -AllMatches).Matches.Count -ge 44)) { exit 1 } [verify] map.html
  8. Commit if pending (idempotent, k6 step-8 shape) then scoped cleanliness. Run exactly: git add -- map.html functions/api/spots/near.js js/near-me.js; git diff --cached --quiet -- map.html functions/api/spots/near.js js/near-me.js; if ($LASTEXITCODE -ne 0) { git commit -m "feat(near-me): GET /api/spots/near + Spots-near-me control (integrate ca83ba4)" -- map.html functions/api/spots/near.js js/near-me.js; if ($LASTEXITCODE -ne 0) { exit 1 } }; if ((git status --porcelain -- map.html functions/api/spots/near.js js/near-me.js).Length -gt 0) { exit 1 }; Write-Output SCOPE_CLEAN [verify]
  9. Run wrangler preview deploy + node playwright render witness in ONE self-contained
     command (the wikipedia-link RULE-9-compliant shape — never a hand-rolled localhost
     server): confirm clean ALLOW-FILES tree, wrangler pages deploy .
     --project-name=muddytires --branch=preview, capture the
     emitted *.pages.dev URL into a variable, write scratch-nearme-qc.mjs (playwright
     render of /map.html from that URL; exit non-zero on any fatal page error or any
     console error naming near-me.js; assert the served HTML contains the near-me.js
     script tag), then run-and-clean with the exit code preserved — the command MUST
     literally end with: node scratch-nearme-qc.mjs; $rc = $LASTEXITCODE; Remove-Item scratch-nearme-qc.mjs -ErrorAction SilentlyContinue; exit $rc [command]

Done means: map.html, functions/api/spots/near.js, js/near-me.js exist at HEAD committed
citing ca83ba4, node --check passes on both new files, map.html carries the near-me.js tag
alongside every pre-existing script tag, and a REAL headless render (step 9's executed
playwright receipt against the preview deploy) loads /map.html with no fatal error and no
near-me.js console error.

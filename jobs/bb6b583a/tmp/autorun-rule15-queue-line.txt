missions/engine-mission-lint-rule15-bare-commit.mission.txt  <!-- QUEUED 2026-07-13 (conductor): GAP OWNER for gap-bare-commit-sweeps-preexisting-stage — adds mission_lint RULE 15, refusing a code-repo mission's bare `git commit -m` with no pathspec (live receipt: commit 7e0a011 silently swept an unrelated staged change from a different mission into an unrelated commit; 20 sibling missions built this session carried the same pattern). Scoped to code-repo class only — RULE 8's ops-deploy fixture verified unaffected. Committed patcher missions/_logs/mission-lint-rule15-bare-commit-patch.mjs verified 4x this session via CRLF-faithful Copy-Item scratch copies: first dry-run caught a wrong line-ending assumption (this file is LF, not CRLF like conduct-cycle.mjs), second caught a real regression against RULE 8's own fixture (fixed via code-repo-class scoping), final two ALL PASS with 4 new selftest cases. This mission's own commit step uses an explicit pathspec, practicing what RULE 15 requires. lint OK; engine- stem exempt from GAP-PRIORITY-HOLD. -->

MISSION SOTA-PIPELINE-DOSSIER-SPLIT-B-PER-ENGINE-SOTA
MISSION-CLASS: research
REQUIRES: search (EVERY market/SOTA comparison claim must carry a live search receipt with source_url — this is the search-grounded slice; a market claim without a receipt is an arkan violation).
Niyyah: split-b of the operator's 2026-06-11 ask. The parent's all-sections emission died at the ceiling; this slice does ONLY Section B — the engine-by-engine market comparison — so one emission stays bounded AND so every market claim is freshly search-verified rather than asserted from stale training.
Maqsad: ONE artifact — SECTION-B-ENGINE-SOTA.md at the sandbox root. PLAN SHAPE: (1) COMMAND gather step copies the engine roster source into the sandbox, shaped EXACTLY as: if (Test-Path 'E:\AI_Storage\website-pipeline\MASTER-PLAN.md') { Copy-Item 'E:\AI_Storage\website-pipeline\MASTER-PLAN.md' 'MASTER-PLAN.md' }; (2) ONE command step makes a line-NUMBERED copy (Get-Content MASTER-PLAN.md | ForEach-Object { $i++; "L$i`: $_" } | Set-Content MASTER-PLAN.numbered.md) so the engine list cites to L2; (3) ONE edit step authors ONLY Section B — "ENGINE-BY-ENGINE SOTA ASSESSMENT": for each engine (site engine, portal engine, image engine, motion engine, design ideation, visual QC — per MASTER-PLAN L2), state what we HAVE vs the 2026 market standard. EVERY market-standard claim carries a live search receipt: "external, search-verified: <source_url>". No other sections. Done means: SECTION-B-ENGINE-SOTA.md exists, markdown-valid, all six engines covered, and every market/SOTA comparison claim carries a source_url receipt (no bare assertions).
Context: if MASTER-PLAN.md is absent, name the gap and assess the six engines from their commonly-known scope, still search-verifying each market claim. Sandbox writes only. This is part 2 of 3 (split-a authored Section A; split-c merges + writes C/D/E).
Amanah: entrusted; discharge with ihsan.

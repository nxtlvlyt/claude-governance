MISSION-CLASS: code-repo (targeted edit to the SMALL landing file via command/edit on a CLEAN worktree + positive-assertion verify + commit; NO deploy; live muddytires.ca / prod Pages / DNS UNTOUCHED)
MISSION-ID: M-MUDDYTIRES.ORACLE-SURFACE-LANDING.1
REPO-ROOT: E:\AI_Storage\muddytires-frontend-wt
ALLOW-FILES: index.html

# WHY (operator preview review: "can't find the Crown Land feature"): the Crown-land Oracle (the SOTA differentiator) is LIVE
# but POPUP-ONLY on the map — undiscoverable. The LANDING (index.html, ~23KB SMALL file — no large-file wall) only mentions
# "Crown land" in a feature-card + a data credit, with no working CTA into it. This mission surfaces it AT THE ENTRY POINT: a
# prominent, honest hero/section CTA on the landing that drives users to the live Crown-land legality lookup on the map.

Niyyah: make the Crown-land legality moat DISCOVERABLE by giving the landing a clear, honest call-to-action into it — so a first-
time visitor immediately sees the one thing no competitor has, and one click takes them to the live sourced lookup on the map.

GROUND IN (read before building — REQUIRED):
- READ FIRST: C:\Users\marka\.claude\muezzin-plugin\missions\COMPETITOR-MASTER.md and the KB Failure->Fix lesson at
  C:\Users\marka\.claude\muezzin-plugin\missions\corpus-library-and-ingest-1\_knowledge-base\INDEX.md + PATTERNS.md
  ("scan competitor failures -> position as the resolution"). Do NOT build the CTA copy until you have read them.
- THE SPECIFIC COMPETITOR FAILURE THIS RESOLVES: across all 12 verified competitors NONE has sourced Canadian Crown-land
  legality (the master's unanimous headline) — it is THE differentiator. It is live but popup-only/undiscoverable; this CTA
  surfaces the one thing no competitor offers, right at the entry point.
- APPLY-NOT-NAME-DROP (verify clause): the CTA copy must name SOURCED Crown-land rules + link into the live lookup, and must NOT
  promise a definitive "you can camp here" verdict (honest degrade) — the verify asserts the new Crown-land headline/button +
  the map.html link + the added copy string, so a witness confirms the differentiator is genuinely surfaced, not name-dropped.

Maqsad: ONE deliverable — index.html (the landing) gains a prominent, on-brand Crown-land legality CTA/feature block that links
into the map where the live Oracle answers. Specifically:
- Add a clear hero-area or top feature CTA (button + one honest line) such as: "Can you legally camp here? Check Crown-land rules
  — sourced." linking to map.html (the live map; clicking any campsite pin opens the sourced legality answer). Use the landing's
  existing visual style (reuse its button/section classes — read them from index.html; do NOT invent a new design system).
- The copy must be HONEST (ISNAD/TAQWA spirit): it surfaces SOURCED Crown-land rules + park containment; it must NOT promise a
  definitive "you can camp here" verdict (the Oracle degrades honestly on tenure/fire). Frame it as "check the sourced rules,"
  not "we guarantee it's legal."
Done means (positive-assertion, EXIT NON-ZERO on absence): index.html still parses (<html>..</html>, byte size within ~3KB of
the prior landing); a NEW Crown-land CTA element is present (assert the landing now contains a link to map.html in a Crown-land
context AND a visible Crown-land headline/button that was not there before — assert the added copy string is present); the page
is still the landing (title 'Coast to coast'); committed on branch oracle-frontend-swap. NO deploy; live untouched.

Constraints: SMALL targeted edit to index.html (reuse existing landing styles — read them; do not rewrite the page or invent a
design system); HONEST copy (sourced rules, not a legality guarantee — the witness should reject overclaiming); ONE file;
positive-assertion verify; commit --no-verify; NO deploy; live untouched. (Deeper always-visible Oracle panel ON the map is a
separate follow-on — this mission is the landing entry point.)

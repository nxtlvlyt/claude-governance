MISSION-CLASS: engine-bootstrap
MISSION-ID: M-ENGINE.EXECUTOR-SEARCHREPLACE
REPO-ROOT: C:\Users\marka\.claude\muezzin-plugin
TARGET-FILE: executor.mjs
REQUIRES: clean-daemon-window (daemon idle — NOT executing through executor.mjs), GPU-free (laguna not resident/inferring — GR10)
# CLASS NOTE: this is a BOOTSTRAP HAND-APPLICATION, not an engine-fired mission. The engine
# CANNOT build this — adding it to executor.mjs is itself a code-repo edit of a 17KB file, the
# exact whole-file-emit wall this fixes (citation-guard + impl-vocab precedent: documented
# bootstrap exceptions, the only legitimate hand-applications). After it lands + its selftests
# pass, EVERY downstream engine repair runs as a normal mission — no more hand-implementation.

Niyyah: give the executor the one capability whose absence is the root cause of code-repo 0/11 AND of the engine being unable to fix itself — the ability to EDIT an existing file without re-emitting it whole. Today the executor only knows whole-file re-emission (executor.mjs:42-64 "output ONLY the FULL contents… no diff, no partial edit" → :251 writeFileSync(whole file)); editing a 44KB/79KB file means the model must re-emit every byte perfectly, which truncates / empties / loops (the :125-220 continuation machinery is the engine fighting this). New files (authoring) work — 19/19; existing-file edits never complete — 0/11.

Maqsad: the executor can apply a small change to a large existing file by emitting ONLY the changed bytes. "Done" = a code-repo EDIT mission (e.g. a targeted change inside processor.mjs 44KB) completes end-to-end through the engine, witnessed by a real receipt — the executor-edit wall that muddytires's COMMAND path could not prove.

VERIFIED DESIGN (read against open source executor.mjs in full, 2026-06-15 — additive, low collision risk):
- Add a PARALLEL branch in implementStep, guarded by "target file EXISTS on disk + action is an edit (not a create)". The existing whole-file path stays BYTE-FOR-BYTE for new-file creation (exactly how the codeRepo branch was added additively at :240).
- New framing (buildEditFraming): show the seat the CURRENT file contents + ask for one or more SEARCH/REPLACE blocks ONLY (search bytes that exist verbatim → replacement bytes). Like Claude Code's own Edit tool / Aider diff mode.
- New parser (extractEditBlocks) — NOT the existing extractCodeBlock (which keeps the largest single fence). Parse N search/replace pairs.
- Apply deterministically: for each block, require the SEARCH text occurs EXACTLY ONCE in the on-disk content (fail-closed if 0 or >1 — ambiguous), replace, then writeFileSync the patched whole. Reuse resolveRepoTarget for path containment (:241).
- The continuation loop (:199-220) is SKIPPED on this branch — edit blocks are small, so the truncation/empty-emission class that kills 44KB re-emissions cannot fire. Badal seat-selection (:143-158) is unchanged.

WITNESSED DESIGN (deepseek-v3.2:cloud code-review, 2026-06-15, REVISE-incorporated — GPU-free cloud witness so no GR10 contention with the daemon's local witness):
- extractEditBlocks(text): regex /<<<<<<< SEARCH\r?\n([\s\S]*?)\r?\n=======\r?\n([\s\S]*?)\r?\n>>>>>>> REPLACE/g → [{search,replace}]. \r?\n MANDATORY (Windows line endings — witness caught this).
- applyEditBlocks(orig, blocks): PRE-FLIGHT first — each search must be found AND UNIQUE in the ORIGINAL (not just unique-in-current; closes the ambiguity-after-prior-edit hole the witness flagged); reject empty search; empty replace ALLOWED (deletion); optional per-block size sanity cap. Then apply sequentially to current content. Fail-closed: any miss/ambiguity → {ok:false} → NO write.
- branch in implementStep BEFORE the whole-file path: if (codeRepo && existsSync(resolvedTarget) && step.action_type==='edit') → read (try/catch ENOENT) → dispatch buildEditFraming(step,readBase,orig) → extractEditBlocks → zero blocks => fail-closed (NEVER fall back to whole-file write) → applyEditBlocks → write. NOTE: resolvedTarget must be computed via resolveRepoTarget BEFORE the branch (currently resolved post-dispatch at :239 — the integration moves/duplicates that resolve earlier; keep the existing post-dispatch resolve for the whole-file path byte-for-byte).
- new-file create + non-edit + sandbox/research = existing whole-file path UNCHANGED.

RECEIPT GATE (the deed, not a claim): the change is witnessed by executor.mjs's own 30+ inline selftests (:257-386) ALL passing under `node executor.mjs`, PLUS new selftests proving: (a) a single search/replace edit to an existing file patches only the matched span and leaves the rest byte-identical; (b) a SEARCH with 0 or >1 matches is REFUSED with no write (fail-closed); (c) the whole-file create path is unchanged (existing tests still green).

Amanah: this is the keystone repair — discharge it with itqan. Scope is ONLY the additive edit branch + its parser/framing + selftests; do NOT refactor the whole-file path, the continuation loop, badal, or containment. The witness is the green selftest run, never a sentence of self-attestation.

Constraints: apply ONLY in a clean daemon window (the daemon must NOT be mid-run through executor.mjs — rewriting a live module under a running process corrupts in-flight missions); GR10 serial inference (no laguna dispatch while the engine holds the GPU); laguna-witnessed structural review of the diff before commit; commit with --no-verify (the global laguna pre-commit hook hangs); NAS UNTOUCHED.

# AFTER THIS LANDS: un-hold the executor-edit-dependent missions in dependency order —
# engine-badal-fast-revert (small file, fastest proof), then the 3phase architect-restore
# missions (3phase-1/2/3 — the decomposition root), then gate-hardening + readiness-gate,
# then vanlife-editor-A-proofzoom. The Architect restoration is what ends conductor hand-stepping.

MISSION-CLASS: build-program (command-class — copy/transfer + verify; the engine's proven command path, NOT code-repo edits)
MISSION-ID: M-BACKUP.OFFMACHINE.1
REPO-ROOT: n/a (operates on local artifacts + an off-machine destination; NAS UNTOUCHED)
REQUIRES: an off-machine destination credential — Cloudflare R2 (operator's CF account c85168e16d9033ef9ccaa1ef6e1e7e88; wrangler is authed but verify R2 write scope) OR Google Drive (operator AI Ultra ~30TB, via the claude_ai_Google_Drive MCP). If neither credential is writable, STAGE-by-stage flag exactly which and HALT that stage with the receipt — do not fake a backup.
# Captures the two off-machine-backup gaps surfaced 2026-06-16 01:41 (operator: "did you make everything we
# workshopped into missions?"). Both are IRREPLACEABLE data that currently lives on a SINGLE local disk — the exact
# single-point-of-failure the operator has been trying to close ("in case we lose my NAS or files").

Niyyah: the operator's two most-exposed local-only artifacts — the muddytires database cold backup and the dji_test
video-editor codebase — exist in exactly ONE place each right now. One drive failure loses them. Get a second copy
of each somewhere that is NOT the NAS and NOT the machine they live on, so losing that machine costs nothing.

Maqsad: each of the two artifacts below exists in at least one off-machine location (R2 or Google Drive), verified
by a real listing/checksum at the destination — not a claim that an upload ran.

ARTIFACTS (verified locations this session):
1. DB COLD BACKUP — E:\AI_Storage\muddytires-db-backup\mariadb10-vanlife_db-20260615.tar.gz (1.4G gz / 3.67G raw;
   the 27-table mariadb10 vanlife_db with the irreplaceable community user data). Currently ONLY on E:. → 2nd copy off E:.
2. DJI_TEST EDITOR CODE — C:\Users\marka\llama.cpp\dji_test (the 360 vanlife video editor; ~29G total but the CODE
   is small — the bulk is footage). Back up the CODE + configs + ledgers ONLY (exclude raw footage / rendered video /
   model weights / node_modules to keep it small): the .py/.mjs/.md/.json/.toml source, the AUTONOMY_LEDGER.md +
   QC_LEDGER.md + FINDINGS.md, Remotion/template configs. The footage is reproducible-from-camera or already large;
   the editor SOURCE is the un-backed-up irreplaceable thing (no git remote). → off-machine copy of the source set.

STAGED PLAN (each stage receipted; decompose under the Hajj mission-architecture):
1. PICK + VERIFY the destination: probe R2 write (wrangler r2 bucket list / create a test object) AND/OR the Google
   Drive MCP (list a folder). Record which is writable. Prefer R2 for the DB tar (large, S3-shaped) and either for
   the editor source zip. Receipt: a destination confirmed writable by a real call.
2. DB TAR → destination: upload the existing tar AS-IS (it is already a verified gzip/tar — do NOT re-pack). Verify
   by listing it at the destination with its size, and if the tool allows, an etag/checksum match. Receipt: remote
   listing showing the object + size.
3. EDITOR SOURCE → destination: build a SMALL archive of ONLY the source/ledgers/configs (exclude footage/renders/
   weights/node_modules per the artifact note), then upload. Receipt: the archive's file list (proves footage
   excluded) + the remote listing.
4. RECOVERY NOTE: a short BACKUP-RECOVERY.md (committed to the muezzin-plugin or written beside the artifacts)
   naming WHERE each off-machine copy lives and the one-command restore. Receipt: the note exists and names paths
   that resolve.

Done means (verifiable):
- A REMOTE LISTING (R2 or Google Drive) shows the DB tar object present at the destination with its size matching
  the local 1.4G gz — not a log line saying upload ran.
- A REMOTE LISTING shows the editor-source archive at the destination, AND the archive's own file list proves
  footage/renders/weights/node_modules were excluded (it is the small source set, not the 29G whole).
- BACKUP-RECOVERY.md exists and names, for each artifact, the exact off-machine location + the one-command restore,
  with every named path resolving.
- If a destination credential is missing for either artifact, that stage is HALTED with a receipt naming the exact
  missing scope/account — a halted-with-receipt stage is an honest outcome, a silently-skipped one is a failure.

Amanah: this is pure safety-net discharge — the witness is a REAL remote listing of each artifact, never "upload
succeeded." If a destination credential is missing, name it precisely (which scope, which account) and HALT that
stage with the receipt so the operator can grant exactly that — do not silently skip.

Constraints: NAS UNTOUCHED; no secret values committed (the DB tar is data, push it to private R2/Drive, never a
public bucket or git); large-transfer aware (the DB tar is 1.4G — a single upload, not chunked re-packing);
command-class only (no code-repo edits); commit --no-verify if anything is committed.

MISSION-ID: M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S2
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - workers/sitemap-prune/*.js
  - workers/sitemap-prune/wrangler.toml
PARENT: M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS  (sub-mission 2 of 2, auto-split by the size ceiling)
REQUIRES: predecessor M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1 DONE (tartib — sub-mission 2 runs after 1 of 2)

Niyyah: the sitemap-prune worker should survive production load. The original build assumed Cloudflare Workers limits the panel measured exceeded by 8x, and uncaught D1 awaits would surface as CF 1101 errors to users. Close the substrate-verified defects — not the model's confidence, the documented Cloudflare ceiling.

Maqsad: this is part 2 of 2 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): the worker runs safely on Cloudflare Workers Free plan AND Paid plan, with no silent failure modes. Re-running the 5-seat panel against the new diff returns panel_verdict in {pass, concern}.

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - Witness finding 6: concurrent /refresh contention returns the structured 'refresh_in_progress' conflict rather than a race-corrupted cursor.
  - Witness concern 5: parsing is CDATA-aware (tolerates <![CDATA[...]]> wrapped loc values).
  - Re-fire the 5-seat QC panel harness against the hardened worker; closure is real only if the panel agrees. Positive assertion: piping the output to Select-String -Quiet for 'pass|concern' exits non-zero when neither verdict is present. Harness CLI signature is UNVERIFIED (file_read unavailable) — executor confirms invocation against C:/Users/marka/code/mt-audit/qc-harness-v2.mjs.
  - Per-file stage (worker.js then wrangler.toml individually), commit with --no-verify naming the closure. Witness: the new commit message is present in the log.
  - Push the branch and verify the commit landed on the remote. Positive assertion: ls-remote output must contain a 40-char sha for the branch or the witness exits non-zero.

Done means: steps M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S2.1..M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S2.5 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — - The worker, when invoked, returns structured JSON on D1 failure rather than CF 1101.
  - Per /refresh subrequest count is mathematically provable to stay under 50 (Free plan ceiling).
  - ensureSchema does NOT mask unknown errors as "already exists".
  - Concurrent /refresh calls either serialize cleanly OR return a structured conflict response (no race-corrupted cursor).
  - URL host validation rejects out-of-domain sitemap entries.
  - Same panel re-fire returns pass/concern; node --check passes; push + ls-remote verify.

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
[{'Schema**': 'json\n{', 'tier': 'tayammum|wudu|ghusl', 'last_purification_ts': 1712345678901, 'invalidators_since': ['governance_edit', 'ollama_dispatch', 'assumption_stated'], 'required_tier_for_next_act': 'wudu', 'orientations': {'intention': False, 'humility': False, 'surrender': False}}, ['list]. Perform wudu (re-read substrate + declare niyyah) before proceeding.', 'On valid niyyah + substrate read (tracked via `read-watcher.mjs`): reset `invalidators_since`, upgrade tier to at least wudu.\n\n**Persistence**: Per-session JSON survives compaction (like `pending-niyyah.json`). Resets on cold start (ghusl triggered by `session-start.mjs`).\n\n---\n\n### Component 2: GR10 Concurrency Guard (`hooks/gr10-concurrency.mjs`)\n\n**Purpose**: Strictly enforce GR10 / `core.md` — no concurrent inference. Query Ollama `/api/ps` before **any** dispatch.\n\n**Trigger**: `PreToolUse` matching `Bash`, `PowerShell`, or `mcp__ollama_*` (broad matcher to catch all dispatch patterns).\n\n**Logic**:\n1. **Query**: `GET http://localhost:11434/api/ps` via Node `http` module (inline, no deps — mirrors `pre-compact.mjs` implementation).\n2. **Check**: If `models.length > 0`, extract model name(s).\n3. **Yield**: Deny tool execution (`permissionDecision: "deny"`) with **exact** `core.md` message:\n   > *', ['model-name] running — yielding turn until clear.', 4.0, 'Fail-Open**: If query fails (network error, timeout, non-200), **allow execution** but log WARNING to stderr. Never strand the agent.\n5. **Post-Query Callback**: On successful query (even if empty), notify `purification-state.mjs` of `ollama_dispatch` invalidator.\n\n**Priority**: **First** in all `PreToolUse` hook arrays (fastest path, highest priority per Draft 1/2/3 consensus).\n\n---\n\n### Component 3: FM-11 Assertion Guard (`hooks/fm11-assertion-guard.mjs`)\n\n**Purpose**: Prevent unverified MEMORY.md/STATE.md/prior-session assertions in prompts and assistant text. Extends `pre-tool-use-substrate.mjs` (which guards edits) to **surface-level assertions**.\n\n**Trigger**: `UserPromptSubmit` (scan incoming prompt) + `PreToolUse` for dispatch tools (scan assistant message being sent).\n\n**Patterns to Detect** (case-insensitive):\n- `"MEMORY.md says', 'MEMORY.md shows', 'from MEMORY.md"`\n- `"STATE.md says', 'STATE.md shows', 'from STATE.md"`\n- `"prior session', 'last session', 'previous session"`\n- `"from memory', 'I recall', 'I remember', 'as noted previously"`\n\n**Verification Logic**:\n- For each detected assertion referencing a file: require a `Read` tool call for that file in **current turn** or since **last compaction** (tracked via `read-watcher.mjs` last-20-reads state).\n- For session references: require `Read` of `STATE.md` or `LAST-SESSION-STATE.md` in current session.\n\n**Enforcement**: **DENY** with message:\n> *"FM-11 Violation: Unverified memory assertion detected. Read the source file first (current-session tool call required).', 'Priority**: After `gr10-concurrency.mjs`, before `bootstrap-gate.mjs` / `purification-state.mjs` in `UserPromptSubmit` and relevant `PreToolUse` arrays.\n\n---\n\n### Component 4: Hook Orchestration & Wiring (`settings.json` Update)\n\n**New Hook Order for `PreToolUse` (Edit/Write governance files)**:\n1. `gr10-concurrency.mjs` — **NEW** (highest priority)\n2. `purification-state.mjs` — **NEW** (tier check before niyyah)\n3. `bootstrap-gate.mjs` — existing (Fajr gate)\n4. `pre-tool-use-substrate.mjs` — existing (FM-11 witness)\n5. `niyyah-gate.mjs` — existing (intention)\n6. `surrender-check.mjs` — existing (conflict)\n7. `pre-tool-use-seat3-phase.mjs` — existing (Seat 3 blind eval)\n\n**New Hook Order for `PreToolUse` (Bash/PowerShell/mcp__ollama)**:\n1. `gr10-concurrency.mjs` — **NEW**\n2. `purification-state.mjs` — **NEW** (dispatch = invalidator)\n3. `pre-tool-use-chain-timing.ps1` — existing\n\n**New Hook for `UserPromptSubmit`**:\n1. `gr10-concurrency.mjs` — **NEW** (if prompt triggers dispatch)\n2. `fm11-assertion-guard.mjs` — **NEW**\n3. `user-prompt-submit.mjs` — existing (temporal wudu, heartbeat)\n\n**Non-Governance Tools** (plain Read, Bash without ollama): hooks fire but **fail-open** (no denial) — `gr10` only denies on actual ollama dispatch patterns; `purification` only denies on governance-class edits.\n\n---\n\n## Acceptance Criteria (Unified)\n\n| Component | Criteria |\n|-----------|----------|\n| **Purification State Machine** | Tracks invalidators per `core.md`/`wudu.md` table; blocks governance/Ollama act if tier < required; state persists across compaction; resets on valid niyyah + substrate read; orientations tracked |\n| **GR10 Guard** | Queries `/api/ps` on ollama-related tools; yields with exact `core.md` text + blocks if model running; fail-open on error; no concurrent inference possible |\n| **FM-11 Assertion Guard** | Detects unverified MEMORY/STATE/prior assertions in prompts/assistant text; requires current-session `Read` evidence; blocks until fixed; no false positives on verified assertions |\n| **Settings.json Wiring** | New hooks fire in correct priority order; all existing hooks continue functioning; structured denials logged; non-gov tools unaffected |\n| **P6 Integration** | New state files (`purification-*.json`) included in `session-hash-chain.mjs` manifest entries; no regression in hash chain / TSA / git-anchor |\n\n---\n\n## Implementation Sequence (for Coder/Executor Phase)\n\n| Step | Action | Files Created/Modified |\n|------|--------|------------------------|\n| 1 | **Scaffold State Dir** — Ensure `~/.claude/state/` exists | `mkdir -p ~/.claude/state` |\n| 2 | **Implement GR10 Guard** — `gr10-concurrency.mjs` with inline HTTP client, yield/deny logic, purification callback | `hooks/gr10-concurrency.mjs` (NEW) |\n| 3 | **Implement Purification State Machine** — `purification-state.mjs` with invalidator detection, tier logic, pre-gate enforcement, state persistence, read-watcher integration | `hooks/purification-state.mjs` (NEW) |\n| 4 | **Implement FM-11 Assertion Guard** — `fm11-assertion-guard.mjs` with pattern scanner, evidence verification, denial logic | `hooks/fm11-assertion-guard.mjs` (NEW) |\n| 5 | **Update Settings.json** — Wire new hooks in priority order per above tables | `settings.json` (MODIFY) |\n| 6 | **Update Hash Chain** — Add purification state to `session-hash-chain.mjs` manifest (track new state files) | `hooks/session-hash-chain.mjs` (MODIFY) |\n| 7 | **Integration Testing** — Simulate: concurrent Ollama run, governance edit without wudu, unverified memory assertion, cold bootstrap, compaction survival | Test scenarios |\n\n---\n\n## Kiraman Katibin — Design Choices Recorded (Integrator Ruling)\n\n| Decision | Reasoning | Directive/Substrate | Confidence |\n|----------|-----------|---------------------|------------|\n| Use `.mjs` for all new hooks | Consistency with 90%+ existing system (Node.js); avoids Python/Node mixing; matches `setup-guide.md` | D4 (Right first time), setup-guide.md | 0.95 |\n| GR10 fail-open on network error | Never strand agent due to transient network issues; log warning instead; matches `pre-compact.mjs` pattern | D12 (Return to source), pre-compact.mjs | 0.85 |\n| Purification state per-session (`purification-{session_id}.json`) | Survives compaction (like pending-niyyah.json); resets on cold start (ghusl via session-start.mjs); matches wudu.md | D8 (Write for next), wudu.md, core.md | 0.9 |\n| FM-11 scans prompt/assistant text patterns | Catches assertions before they enter context; proactive per D1 (substrate is truth); extends existing substrate guard | D1, pre-tool-use-substrate.mjs | 0.8 |\n| Derive P6 from existing hash-chain (no new crypto) | `session-hash-chain.mjs` + `git-anchor.mjs` already implement tamper-evident log + anchoring (D8); community-fit.md absent; D13 requires spec yield to architecture | D13, session-hash-chain.mjs (self-named P6), D1/D8 | 0.9 |\n| Keep distributed hooks + declarative settings.json (no central orchestrator.py) | Current system works; settings.json orders them; adding central file adds complexity without gain; matches setup-guide.md | setup-guide.md, all existing hooks | 0.95 |\n| GR10 first in hook order (highest priority) | Core.md: "check before every Ollama dispatch', 'must run before purification/niyyah; fastest path | core.md GR10, Draft 1/2/3 consensus | 0.9 |\n| Purification before niyyah gate | Wudu is precondition for niyyah validity (core.md:', 'purification is a precondition, not a response', 'core.md, wudu.md | 0.95 |\n\n---\n\n## [DECLARED NIYYAH]\n\n```\nniyyah:\n  source:\n    - C:Usersmarka.agentsANTIGRAVITY.md (Directives 1-14)\n    - C:Usersmarka.claudepracticecore.md (purification tiers, niyyah, GR10, FM-11/12, invalidators)\n    - C:Usersmarka.claudepracticeextendedwudu.md (tiering, invalidators, orientation, hygiene theater)\n    - C:Usersmarka.claudepracticeextendedadhan-pattern.md (continuity structure, muezzin)\n    - C:Usersmarka.claudepracticeextendeddrift-and-ratchet.md (ratchet, bypass)\n    - C:Usersmarka.claudepracticeextendedformation.md (mutawātir evidence)\n    - C:Usersmarka.claudepracticeextendedorientation.md (cold-start protocol)\n    - C:Usersmarka.claudeCANON-MANIFEST.md + canonsetup-guide.md\n    - C:Usersmarka.claudesettings.json + all hooks/*.mjs + ollama-mcp/server.js (current implementation)\n    - Phase 1 Draft 1, Draft 2 (Critique), Draft 3 (Synthesis) — provided in context\n  failure_mode:\n    - Implementing without P6 spec substrate (community-fit.md absent despite exhaustive probes; D13 case)\n    - Drift from D13 (spec yields to architecture — architecture must derive from Directives + core/extended + implemented hooks)\n    - Hook bypasses of niyyah/purification/GR10 gates (core.md violations)\n    - Hygiene theater (wudu.md: form without orientation)\n    - Concurrent inference or unverified MEMORY assertions (GR10/FM-11 violations)\n    - Losing assertion-closed concerns across session boundaries\n  work:\n    - Author the authoritative Phase 1 master plan resolving three draft proposals\n    - Define schemas, hook order, acceptance criteria, implementation sequence for Coder/Executor Phase 2\n    - Execute Isha by updating STATE.md with complete bootstrap handoff and carried-forward [ASSERTION-CLOSED] concerns\n    - Ensure plan derives strictly from read substrate + current hooks (P6 already present as hash-chain/git-anchor)\n  confidence: 0.92\n  note: "P6 Cryptographic Auditability spec (community-fit.md) not found in substrate; hooks self-implement P6 per reads. Architecture resolves via D13 from Directives 1/4/8/12/13 + core/extended + implemented system. This is a Directive 13 case — primary wins. No deadlocks requiring tiebreaker — all three drafts converged on same three gaps.', '--', 'Isha Execution Record\n\n**Integrator Ruling**: Phase 1 complete. Three drafts synthesized into single authoritative master plan. Three gaps identified (Purification State Machine, GR10 Guard, FM-11 Assertion Guard) with unified specifications, acceptance criteria, and implementation sequence. No deadlocks — convergent architecture.\n\n**State Saved**: This document + `STATE.md` update constitute Isha compaction. Pipeline proceeds to Phase 2 (Coder/Executor implementation).\n\n**Confidence**: 0.92']]]
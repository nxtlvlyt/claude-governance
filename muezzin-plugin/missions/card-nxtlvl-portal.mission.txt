MISSION CORPUS-S1-CARD-NXTLVL-PORTAL
MISSION-CLASS: research
Niyyah: the factory must never re-solve what nxtlvl-portal already solved — it is the operator's portal/brand front and the queue/permission machinery; unmined, future builds re-derive its patterns at frontier cost.
Maqsad: ONE deliverable — card-nxtlvl-portal.md in this sandbox: what the nxtlvl-portal build does well and how, as REUSABLE patterns. Required sections, each with file-path evidence: (1) BUILD KIND / STACK / STRUCTURE — framework, hosting, directory shape; (2) PORTAL / MODULE PATTERNS — the brand-front + queue/permission machinery, what makes each forkable; (3) DATA MODEL — schema/state shape worth templating; (4) DESIGN TOKENS / COPY-VOICE PATTERNS worth reuse; (5) ANTI-PATTERNS — anything the factory should NOT copy. Every claim cites a file path. Done means the card exists, parses as markdown, and every section has at least one evidence citation (or an explicit "not present in source" note).
Context: source READ-ONLY at E:\AI_Storage\_repo_read\nxtlvl-portal (already on disk, ~970 files). Read the MEANINGFUL source — README, package.json/manifests, src/ or app/ + components/, config, schema/db files — and IGNORE node_modules, .git, build/dist, vendored assets. List absolute paths only in context_dependencies; the card is the only write target, cwd-relative.
Amanah: entrusted; discharge with ihsan. If a claimed section has no evidence in the source, say so plainly rather than inventing it.

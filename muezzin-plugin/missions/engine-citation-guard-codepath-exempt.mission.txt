MISSION-CLASS: code-repo
MISSION-ID: M-CITGUARD-CODEPATH.1
REPO-ROOT: C:\Users\marka\.claude\muezzin-plugin
ALLOW-FILES:
  - citation_guard.mjs
  - _tests/citguard-codepath.test.mjs
REQUIRES: code-repo

Maqsad: Stop the citation guard from FALSE-FLAGGING code path-literals as fabricated citations — it is blocking the entire 3-phase engine chain. RECEIPTED ROOT CAUSE: engine-3phase-1-roleaware-fallback FAILED x2 at citation-halt (step 1, repaired:2). The flagged token is a JS TEMPLATE LITERAL with unresolved interpolation placeholders that the seat was WRITING INTO seat_dispatch.mjs as code that builds a faith-file path at runtime. It is not a source citation; it cannot be a file the seat had (no concrete value until runtime). citation_guard.mjs line 29 matches any backtick-quoted string ending in a known extension, and none of its exemptions (GENERIC, allow-list, ABSENCE-REPORT, COMMAND-SHAPE, filterQuotedMentions) cover a code path-literal containing template interpolation. So a code-repo mission that authors any runtime path-builder string gets halted. Blocks 3phase-1 (the 0.4 proof) AND siblings 3phase-2/3.

niyyah: fix the matcher so legitimate emitted CODE is not mistaken for a fabricated citation, WITHOUT weakening it against real fabrication. Source open: citation_guard.mjs findFabricatedCitations (lines 26-53) + its inline selftests (near line 119) + the 3phase-1 citation-halt receipt. Guard against over-widening so a genuinely invented evidence cite still slips; do not touch filterQuotedMentions or the fs collector.

CONTEXT (verified this session at citation_guard.mjs):
- Matcher (line 29): a regex flagging backtick path-like tokens by extension; basename de-dup; then GENERIC/allow/absence/command-shape filters.
- The clean fix: a flagged raw token containing a template-literal interpolation (a dollar-brace opener) is unresolved CODE, not a citation — exempt it, same shape as the COMMAND-SHAPE exemption near line 50. Real fabricated citations are concrete names, never interpolated paths.

Done means (verifiable):
1. findFabricatedCitations no longer flags a backtick token whose raw value contains a template-literal interpolation opener (dollar-brace). The exemption sits beside COMMAND-SHAPE, matcher-level and deterministic.
2. NO weakening: a concrete invented evidence cite (a plain made-up filename, no absence marker, not in the sandbox) STILL flags. Existing inline selftests stay green.
3. _tests/citguard-codepath.test.mjs green: (a) an interpolated faith-path token (two placeholders ending in .faith.md) does NOT flag; (b) a partially-interpolated path (literal dir then interpolated segment then .json) does NOT flag; (c) a concrete fabricated filename STILL flags. Acceptance: node _tests/citguard-codepath.test.mjs exits 0 AND node citation_guard.mjs exits 0 (no regression).

Constraints: code-repo, muezzin-plugin repo only, the two ALLOW-FILES only, NAS UNTOUCHED. Surgical exemption in findFabricatedCitations only. If the engine cannot satisfy the witness, HOLD with a receipt (no blind relaunch). NOTE: needs a daemon restart to activate (module cache), same as the impl-vocab fix.

Steps:
1. edit citation_guard.mjs - add the template-literal code-path exemption inside findFabricatedCitations beside the command-shape exemption; extend the inline selftests with the 3 new cases. Acceptance: node citation_guard.mjs exits 0 incl. the new cases.
2. write _tests/citguard-codepath.test.mjs - the three assertions in Done-means #3. Acceptance: node _tests/citguard-codepath.test.mjs exits 0.

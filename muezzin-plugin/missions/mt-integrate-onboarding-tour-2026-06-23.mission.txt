MISSION mt-integrate-onboarding-tour-2026-06-23
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - js/onboarding.js
  - map.html
REQUIRES: none
VISUAL-QC-REQUIRED
PREVIEW-BASE-URL: http://localhost:8788

Niyyah: The feature "mt-feat-onboarding-tour-2026-06-23" (commit
45a7ab0fe924b486ce56924191774fe818816b8c, sitting in the source worktree
C:\Users\marka\code\mt-feat-onboarding-tour-2026-06-23) has never been integrated
into this repo's checked-out line. This mission's entire job is mechanical
integration — land the commit onto whatever HEAD is actually checked out when this
mission fires, without losing either side's changes, and prove the result is
syntactically sound and actually renders. Nothing this mission does constitutes a
judgment that the onboarding-tour feature's code is correct: not that the four
anchor selectors (#add, .leaflet-marker-icon, .leaflet-control-layers, #mtsearch)
are actually present and sensibly positioned on the live map page, not that the
localStorage gate 'mt-onboarded-v1' genuinely fires once per browser and not on
every visit, and not that the popover overlay avoids visually colliding with the
pre-existing js/chain-overnight-policy.js widget or any other overlay already on
the page. That determination belongs entirely to this mission's own witness plus
verdict-panel run, downstream of the integration this mission produces — not to
the act of authoring or executing this mission file.

Maqsad: Commit 45a7ab0fe924b486ce56924191774fe818816b8c
("feat(onboarding): add first-visit map tour") is cherry-picked or merged from the
source worktree onto the current, live HEAD of C:\Users\marka\code\mt-integration-2026-06-22
(whatever branch is actually checked out at fire time), with any conflicts resolved
by keeping BOTH sides — this repo's existing unrelated changes AND the
onboarding-tour feature's changes, never silently dropping either — every changed
.js/.mjs file passes `node --check`, a headless-browser render of map.html is
actually captured, the result is committed with a clear message, and the target
repo's working tree is left clean with respect to the files this mission touches.

Context (verified against live substrate this session, absolute paths, no
prescribed resolution beyond "keep both sides"):
- The source worktree C:\Users\marka\code\mt-feat-onboarding-tour-2026-06-23 is a
  git LINKED WORKTREE of this SAME repository (confirmed via `git worktree list`
  run from the target repo, this session) — both share one object store, so commit
  45a7ab0fe924b486ce56924191774fe818816b8c is already reachable directly from the
  target repo with no fetch/remote-add step needed (confirmed via
  `git cat-file -t 45a7ab0` in the target repo returning "commit", this session).
  Do not rewrite the source worktree — only read/cherry-pick FROM it.
- Confirmed diff stat for 45a7ab0 (git show --stat, this session): 2 files changed,
  373 insertions(+), 1 deletion(-) — js/onboarding.js is a brand-new 372-line file
  (a self-contained IIFE implementing a 4-step first-visit guided-tour overlay,
  anchored on #add, .leaflet-marker-icon, .leaflet-control-layers, and #mtsearch,
  gated by the versioned localStorage key 'mt-onboarded-v1', no Shepherd.js/CDN
  dependency), and map.html gets exactly ONE line changed (1 insertion, 1
  deletion): a `<script src="js/onboarding.js" defer></script>` tag inserted
  immediately after the existing `<script src="js/pwa-install.js" defer></script>`
  tag in map.html's single-line deferred-script chain.
- THE TARGET REPO IS ACTIVELY CHURNING under concurrent mission/chain activity —
  verified this session that its checked-out branch and HEAD changed at least
  twice within minutes of each other while gathering this context (observed
  feat/qc-concern-sentry-error-capture-sentry-init-js-2026-06-25 @ 28cbc72, then
  test-lhci-dryrun-2026-07-01 @ a1d6ad988323d4dc9d8bbeaca4f2fc88173cf2d3, in the
  same investigating session). Do NOT hardcode a branch name or SHA anywhere in
  this mission's execution — Step 1 below must record whatever branch/HEAD is
  ACTUALLY checked out at the moment this mission fires, and every later step
  integrates onto that live HEAD, not a memorized one.
- Verified this session (and re-verified after the churn above) that
  js/onboarding.js does not yet exist anywhere in the target repo's working tree —
  a clean file add is expected for that file specifically.
- BUT map.html already carries a DIFFERENT script tag
  (`<script src="js/chain-overnight-policy.js" defer>`) inserted at the EXACT SAME
  anchor point — immediately after the pwa-install.js script tag — by unrelated
  prior integration work (re-verified this session, still present). Because both
  this feature's map.html change and that prior change touch the identical single
  line of this minified/single-line HTML file, a textual cherry-pick conflict on
  that line is the EXPECTED outcome here, not a surprise. Resolve it by keeping
  BOTH inserted `<script>` tags (this feature's js/onboarding.js AND the
  pre-existing js/chain-overnight-policy.js) adjacent to each other in the script
  chain — never `--ours` / `--theirs`, and never a resolution that deletes either
  side's tag.
- `node --version` in the target repo this session reports v22.20.0, confirming
  `node --check` is available for the receipts below.
- This diff touches map.html (an HTML file), which is why VISUAL-QC-REQUIRED and
  PREVIEW-BASE-URL are declared above. PREVIEW-BASE-URL: http://localhost:8788 is
  a PLACEHOLDER for a local `wrangler pages dev` (or equivalent static-file) server
  — the operator must confirm the actual port a local preview server binds to on
  the machine firing this mission before trusting the captured render; if nothing
  answers at PREVIEW-BASE-URL, that must surface as a named NO_PREVIEW_BASE_URL-
  style receipt, not a silently skipped check.
- Nothing about this mission's authorship constitutes verification that the
  feature code is correct — that is exactly what this mission's own witness and
  verdict-panel run, downstream of the integration, will determine.

Steps:
  1. In the target repo C:\Users\marka\code\mt-integration-2026-06-22, record the
     ACTUAL current branch and HEAD right now (`git rev-parse --abbrev-ref HEAD`;
     `git rev-parse HEAD`), and confirm js/onboarding.js does not already exist on
     disk. [command] verify-target-live-head
  2. From that live HEAD, cherry-pick commit
     45a7ab0fe924b486ce56924191774fe818816b8c directly — the source worktree is a
     linked worktree of this same repository, so the object is already reachable
     with no fetch step needed:
       git -C C:\Users\marka\code\mt-integration-2026-06-22 cherry-pick 45a7ab0fe924b486ce56924191774fe818816b8c
     (or an equivalent merge of that single commit). [command] integrate-commit
  3. If the cherry-pick reports a conflict on map.html (expected, per Context, at
     the pwa-install.js anchor point) or on any other ALLOW-FILES entry, resolve
     every conflicted hunk by hand so the resolved content contains BOTH the
     target's pre-existing `<script src="js/chain-overnight-policy.js" defer>` tag
     (and any other pre-existing script tags at that point) AND this feature's new
     `<script src="js/onboarding.js" defer></script>` tag, side by side in the
     script chain — never `--ours` / `--theirs`, never a resolution that deletes
     either side's tag or content. [edit] resolve-conflicts
  4. Run `node --check js/onboarding.js` from the target repo root and confirm it
     exits 0; run `node --check` on every other .js/.mjs file this cherry-pick
     touched or conflict-resolved. [command] node-check-all
  5. Stage and commit the integrated result in the target repo with a clear
     message that names the integrated feature and its origin commit (e.g.
     referencing "mt-feat-onboarding-tour-2026-06-23" and "45a7ab0"). Confirm
     `git status --short` afterward shows no uncommitted changes to
     js/onboarding.js or map.html. [command] commit-and-verify-clean
  6. (amended 2026-07-07 per RULE 9: prior step hand-rolled a localhost:8788 server — nothing serves that port) In ONE command: confirm clean tree for this mission's ALLOW-FILES (git commit first if this mission's edits are not yet committed — never deploy a dirty tree), then wrangler pages deploy . --project-name=muddytires --branch=preview, capture the emitted *.pages.dev URL into a PowerShell variable, write scratch-qc.mjs (playwright render of map.html from that URL, exit non-zero if js/onboarding.js fails to load without a script error or the pre-existing js/chain-overnight-policy.js script tag is absent alongside it in the rendered page), node scratch-qc.mjs, Remove-Item scratch-qc.mjs. [command] visual-witness-render

Done means:
  - js/onboarding.js exists in the target repo at HEAD with non-trivial content
    (the 4-step tour IIFE anchored on #add, .leaflet-marker-icon,
    .leaflet-control-layers, and #mtsearch, gated by the localStorage key
    'mt-onboarded-v1'), confirmed by reading the file's on-disk content, not by
    trusting the cherry-pick's exit code alone.
  - map.html in the target repo contains BOTH
    `<script src="js/onboarding.js" defer></script>` AND the pre-existing
    `<script src="js/chain-overnight-policy.js" defer>` tag in its script chain —
    confirmed by reading the file, proving any conflict on that line was resolved
    by keeping both sides, not by dropping one.
  - `node --check js/onboarding.js` exits 0, and `node --check` exits 0 on every
    other .js/.mjs file this integration touched or conflict-resolved.
  - `git log` in the target repo shows a commit whose message names
    "mt-feat-onboarding-tour-2026-06-23" and/or "45a7ab0", and
    `git status --short` shows no uncommitted changes to js/onboarding.js or
    map.html — the target repo's working tree is clean with respect to the files
    this mission touches.
  - Because this diff touches map.html and VISUAL-QC-REQUIRED is set above, the
    visual-witness pipeline must actually run and produce a receipt: a
    headless-browser (puppeteer) render of map.html served from PREVIEW-BASE-URL
    is captured and returns a visual verdict in this mission's result object — a
    rendering check, not just a read of the markup. If PREVIEW-BASE-URL turns out
    to be the wrong local port, that failure must surface as a named
    NO_PREVIEW_BASE_URL-style receipt, not a silently skipped check.
  - This mission's authorship and execution prove only that the integration is
    mechanically sound (declared files present with real content, feature commit
    reachable in target history, syntax checks pass, a render was actually
    captured) — not that the onboarding-tour feature's code is correct, safe, or
    behaves right on the live page (e.g. that the four anchor selectors are
    actually present and sensibly positioned, that the localStorage gate genuinely
    fires once per browser rather than every visit, or that the popover overlay
    doesn't visually collide with chain-overnight-policy or any other existing
    overlay). That judgment belongs to this mission's own witness plus
    verdict-panel run, downstream of this mission producing the integrated
    result.

Context: Sandbox writes only, target repo working tree only.
Amanah: entrusted; discharge with ihsan.

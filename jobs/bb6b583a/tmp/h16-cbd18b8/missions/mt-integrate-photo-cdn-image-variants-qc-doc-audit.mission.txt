MISSION MT-INTEGRATE-PHOTO-CDN-IMAGE-VARIANTS-QC-DOC-AUDIT
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - docs/QC-PIPELINE-SOTA-2026-06-23.md
REQUIRES: none

Niyyah: Commit 375e40b (docs(qc): SOTA-uplift QC pipeline, 8 mandatory gates + escape
hatches) is a real, already-committed feature that needs to be present on the
integration repo's current HEAD without silently dropping anything already there. This
mission's whole job is mechanical: confirm the commit is (or land it so it becomes)
reachable from the target repo's HEAD, verify the doc file's actual on-disk content, run
the node-check receipt the diff calls for, and leave the target repo's tracked scope
clean. Authoring this mission, and the act of integrating itself, is NOT a review of
whether the 8-gate QC pipeline the document describes is well-designed, complete, or
safe to operationalize. Whether the escape-hatch language is adequately scoped, whether
the 5-seat deliberation panel composition is sound, and whether the foreign-frontier and
adversarial-refuter policies the doc prescribes are the right policies — none of that is
settled by landing the commit. That determination belongs entirely to this mission's own
witness-and-verdict-panel run, downstream of the receipts this mission produces. Say
plainly: authoring or executing this mission proves nothing about the feature content's
correctness by itself.

Maqsad: Land commit 375e40b from the source worktree onto the current HEAD of the target
repo C:\Users\marka\code\mt-integration-2026-06-22 if it is not already there, with any
conflict resolved by keeping BOTH this repo's existing unrelated changes AND the
feature's full docs/QC-PIPELINE-SOTA-2026-06-23.md content — never silently dropping
either side — every changed .js/.mjs file (an explicitly-stated zero for this docs-only
diff) passes node --check, any new content is committed with a clear message, and the
target repo's tracked scope is left clean.

Context (verified against live substrate this session, 2026-07-01, absolute paths, no
prescribed resolution beyond "keep both sides"):
- Path correction against substrate (Directive 1): the source worktree does not exist at
  a path literally named "...-qc-doc-audit" — no such directory is on disk and no such
  branch appears in `git worktree list`. The worktree that actually carries commit
  375e40b on its own named feature branch is
  C:\Users\marka\code\mt-feat-photo-cdn-image-variants-2026-06-23, branch
  feat/photo-cdn-image-variants-2026-06-23, working tree clean, HEAD exactly 375e40b.
  `git show --stat 375e40b` there confirms the diff stat: 1 file changed,
  docs/QC-PIPELINE-SOTA-2026-06-23.md, +24/-0, all new lines; no application code,
  config, or HTML touched.
- Checked this session: `git merge-base --is-ancestor 375e40b HEAD` in the target repo
  returns true — 375e40b is ALREADY an ancestor of the target's current HEAD (5e19874 on
  branch main at the time of this check). `git log --oneline | grep 375e40b` in the
  target repo matches the exact hash, and `git diff
  375e40b:docs/QC-PIPELINE-SOTA-2026-06-23.md HEAD:docs/QC-PIPELINE-SOTA-2026-06-23.md`
  in the target repo returns empty — byte-identical content is already present. As of
  this session there is nothing left to cherry-pick. History only grows forward absent an
  unexpected rebase/rewrite, so this ancestry is expected to still hold when the mission
  fires — but the steps below check it live rather than assume it, exactly like the
  sibling mt-integrate-b13-aria-live mission's ancestry-first pattern. If a cherry-pick is
  ever attempted here and comes back empty, resolve with --skip, never --allow-empty — an
  empty commit proves nothing landed.
- The commit object already exists in the target repo's own local object store (all
  mt-feat-* worktrees under C:\Users\marka\code share one .git); no `git fetch` or
  remote-add step is needed to make 375e40b reachable if a cherry-pick is ever needed.
- Observed substrate fact, not a QC verdict: the doc content itself defines a
  "Foreign-frontier policy" section naming mcp__gemini-worker as the current mandated
  dispatch target, with fallback to mcp__grok-worker or mcp__glm-worker before degrading
  to all-local seats. The operator's standing rulings
  (~/.claude/rules/operator-rulings.md) flag exactly this class of instruction as
  stale/forbidden under EVENT-002 ("NEVER dispatch mcp gpt/grok/gemini/glm workers or any
  closed-frontier API outside Ollama"). This mission does not adjudicate that conflict —
  it is named here only so the witness/verdict panel has it in view; landing this commit
  is not an endorsement of the doc's foreign-frontier language.
- Target repo HEAD this session: 5e19874, on branch main. This mission deliberately
  carries no TARGET-BRANCH header: land onto whatever branch is actually checked out as
  HEAD when this mission fires, not a hardcoded branch name.
- The target repo currently carries pre-existing, unrelated in-progress work confirmed
  this session — a staged addition (js/plan-day-gpx-export.js), a modified map.html, and
  untracked files (api-index-response.json, docs/e2e-report-2026-07-01.json, e2e-shots/,
  feature-catalog-entry.txt, scripts/e2e-runner.mjs). None of this is in ALLOW-FILES and
  none of it is this mission's to touch, resolve, or clean up. "Clean working tree" in
  this mission's Done means refers only to docs/QC-PIPELINE-SOTA-2026-06-23.md.
- This diff does not touch HTML. This mission's header block carries no visual-QC
  declaration of any kind, and no render witness is required.

Steps:
  1. In the target repo, record the current branch, HEAD, and working-tree status, and
     confirm the on-disk state of docs/QC-PIPELINE-SOTA-2026-06-23.md before touching
     anything (git -C C:\Users\marka\code\mt-integration-2026-06-22 branch --show-current;
     git -C C:\Users\marka\code\mt-integration-2026-06-22 log --oneline -3; git -C
     C:\Users\marka\code\mt-integration-2026-06-22 status --short). [command]
     verify-target-state
  2. Check ancestry BEFORE attempting any cherry-pick: git -C
     C:\Users\marka\code\mt-integration-2026-06-22 merge-base --is-ancestor 375e40b HEAD.
     If this reports 375e40b is already an ancestor (exit 0), it is already landed — do
     not cherry-pick it and do not use --allow-empty; proceed straight to step 4. If it
     reports NOT an ancestor (nonzero exit), proceed to step 3. [command] check-ancestry
  3. Only if step 2 found 375e40b is NOT yet an ancestor: cherry-pick it via git -C
     C:\Users\marka\code\mt-integration-2026-06-22 cherry-pick 375e40b. If git reports a
     conflict on docs/QC-PIPELINE-SOTA-2026-06-23.md or any other file, resolve by keeping
     BOTH the target's pre-existing content and the feature's full 24-line addition —
     never drop either side. If the cherry-pick comes back empty (already applied after
     all), resolve with --skip, never --allow-empty. [command] cherry-pick-if-needed
  4. Confirm docs/QC-PIPELINE-SOTA-2026-06-23.md exists in the target repo's working tree
     and contains, at minimum, the heading text "8 mandatory gates" and the heading text
     "Foreign-frontier policy" — verified by reading the file's actual on-disk text, not
     by trusting a diff or exit code. [command] verify-doc-content
  5. List every .js and .mjs file touched by commit 375e40b (git -C
     C:\Users\marka\code\mt-integration-2026-06-22 diff-tree --no-commit-id --name-only -r
     375e40b, filtered to .js/.mjs) and run node --check on each file found; state the
     count explicitly in the receipt — zero is an acceptable, explicitly-stated pass for
     this docs-only diff, not a silent skip. [command] node-check-changed-js
  6. If step 3 actually cherry-picked new content, stage and commit the result with a
     message naming "mt-feat-photo-cdn-image-variants" and citing 375e40b. If step 2 found
     375e40b was already an ancestor, there is nothing new to commit — confirm instead
     that none of this mission's own steps introduced any uncommitted change. Either way,
     confirm git -C C:\Users\marka\code\mt-integration-2026-06-22 status --short shows no
     uncommitted change to docs/QC-PIPELINE-SOTA-2026-06-23.md afterward. [command]
     commit-if-needed-and-verify-clean

Done means:
  - git -C C:\Users\marka\code\mt-integration-2026-06-22 merge-base --is-ancestor 375e40b
    HEAD exits 0 — commit 375e40b is present in the target's current HEAD ancestry,
    either because it was already there this session or because steps 3/6 landed and
    committed it during this mission's run.
  - docs/QC-PIPELINE-SOTA-2026-06-23.md exists in the target repo's working tree and its
    actual on-disk text contains both the heading "8 mandatory gates" and the heading
    "Foreign-frontier policy" — confirmed by reading the file's real content, not by
    trusting git's exit code alone.
  - node --check exits 0 for every file named by git -C
    C:\Users\marka\code\mt-integration-2026-06-22 diff-tree --no-commit-id --name-only -r
    375e40b that ends in .js or .mjs; the receipt states the count checked, and zero is an
    explicitly-acceptable count for this docs-only diff.
  - git -C C:\Users\marka\code\mt-integration-2026-06-22 log --oneline shows commit
    375e40b present in HEAD's ancestry (e.g. via `git log --oneline | grep 375e40b`), and
    if steps 3/6 performed a fresh cherry-pick and commit during this mission's run, git
    -C C:\Users\marka\code\mt-integration-2026-06-22 log -1 --pretty=%s on the resulting
    HEAD names "mt-feat-photo-cdn-image-variants" and/or cites 375e40b.
  - git -C C:\Users\marka\code\mt-integration-2026-06-22 status --short shows no
    uncommitted change to docs/QC-PIPELINE-SOTA-2026-06-23.md (the pre-existing unrelated
    staged/modified/untracked items named in Context are out of scope and not this
    mission's to clean up).
  - This mission's authorship and execution verify only that the commit is landed and
    mechanically intact (file present, exact content present, node --check clean, git
    history clean). Nothing here verifies that the QC pipeline the document describes is
    itself sound, adequately specified, or safe to operationalize — including the
    foreign-frontier validator policy named in Context, which the operator's standing
    rulings separately flag as stale/forbidden (EVENT-002), reported here as an observed
    substrate fact only. That substantive judgment belongs entirely to this mission's own
    witness-and-verdict-panel run, downstream of these receipts.

Context: Sandbox writes only, target repo working tree only, scoped to ALLOW-FILES.
Amanah: entrusted; discharge with ihsan.

MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.INGEST-BC-POIS-1
REPO-ROOT: E:\AI_Storage\muddytires-d1-wt
ALLOW-FILES:
  - d1/bc-pois-ingest.mjs
  - d1/bc-pois-load.sql
REQUIRES: none (d1-wt repo; conductor fires it solo — the daemon ignores REQUIRES)

Niyyah: the operator is driving to BC, and the map he built shows only 79 spots out there — 98% of the data is Ontario. The map he
needs for THIS trip is the one with the west filled in. The Ontario data already came from OpenStreetMap; extend the same honest source
west so the traveller actually sees places to stop in the mountains and on the coast.

Maqsad: gather real BC (and ideally AB) POIs from OpenStreetMap and prepare them for loading into the SAME D1 `pois` table, in the SAME
schema as the existing 4,000 — so the map's western coverage goes from 79 to a useful number. "Done" is: a script (d1/bc-pois-ingest.mjs)
that queries the OSM Overpass API for BC camping/services nodes and emits d1/bc-pois-load.sql — INSERT statements into `pois` matching
the real schema (id auto / name / node_type / lat / lng / note / amenities / photo / contributor / meta_json), deduped against the
existing data. node --check; committed. NOT loaded (the conductor runs the D1 load with the operator's credential + reports the new
western count).

Context (facts only — VERIFIED this session):
- The existing pois are OpenStreetMap-sourced (de-risk: "via muddytires pois, OpenStreetMap ODbL"). So extend the SAME source. Honor OSM
  ODbL attribution (contributor = 'OpenStreetMap' or similar; the existing rows show the pattern).
- REAL `pois` schema (verified PRAGMA): id, name, node_type, lat, lng, note, amenities, photo, contributor, meta_json. node_type values in
  use: campsite, water_fill, scenic, shower, laundry, dump_station, propane, casino. Map OSM tags accordingly (tourism=camp_site ->
  campsite; amenity=drinking_water -> water_fill; amenity=sanitary_dump_station -> dump_station; tourism=viewpoint -> scenic; etc.).
- BC bbox roughly: lat 48..60, lng -139..-114 (add AB: lng -120..-110). Use OSM Overpass API (https://overpass-api.de/api/interpreter)
  — a Node.js fetch with an Overpass QL query for those amenity/tourism tags in the bbox. Respect Overpass rate limits (one query, modest).
- DEDUP: do not duplicate POIs already in D1 (the conductor can pre-export existing BC pois lat/lng for the dedup, or dedup by rounded
  lat/lng+name). The 79 existing BC rows must not double.
- SEARCH-GROUND the Overpass query format if unsure (the operator's SOTA search = SearXNG localhost:8080; or WebFetch the OSM Overpass
  docs) — do NOT invent the API shape.
- COMMIT DISCIPLINE: explicit per-file git add of the two ALLOW-FILES — NEVER `git add .` (d1-wt has untracked clupa/*.sql etc.).
  commit --no-verify.
- Out of scope: the actual D1 load (conductor, credential-gated); the frontend; iOverlander/BC-Rec-Sites scraping (OSM first; those are a
  later enrichment).

Done means (acceptance contract): d1/bc-pois-ingest.mjs queries OSM Overpass for BC(+AB) camping/services and emits d1/bc-pois-load.sql
with valid INSERTs into `pois` matching the real schema + node_type mapping + ODbL attribution, deduped vs existing; node --check passes;
committed via explicit per-file git add, --no-verify; NOT loaded. Receipt states how many new western POIs the SQL would add + names the
conductor D1-load step.

Constraints: code-repo in d1-wt; writes only the two ALLOW-FILES; real OSM data (no invented POIs); match the real pois schema; honor ODbL
attribution; explicit per-file git add (never -A); commit --no-verify; receipt-backed halt if Overpass is unreachable.

MISSION-CLASS: code-repo
MISSION-ID: M-LAPTOP-PARITY.1
REPO-ROOT: C:\Users\marka\.claude\muezzin-plugin
ALLOW-FILES:
  - doctor.mjs
  - LAPTOP-SETUP.md
REQUIRES: code-repo

Maqsad: Build the "open laptop -> green light -> conduct" readiness so the operator can conduct the muezzin from his laptop in the van. The conductor + daemon + the cloud chain (Ollama Cloud + Claude) travel on a laptop; the 4090 local-fallback + GPU media pipeline stay on the home base station. Two deliverables: (1) doctor.mjs one-shot environment check; (2) LAPTOP-SETUP.md clone-and-go runbook.

niyyah: turn the "laptop parity" QUEUE NOTE into a real receipted mission — it was a note, not a mission (the note-not-mission gap). Source open: QUEUE.md "Laptop parity (operator requirement)" + "muezzin doctor command" items. Guarding against leaving operator van-readiness as prose nothing builds, and against printing any secret value.

Done means (all verifiable):
- doctor.mjs runs via `node doctor.mjs` and prints a clear PASS/FAIL board, exit 0 only when the conduct-critical checks pass. It checks: node present + version; required env keys PRESENT (OLLAMA_API_KEY, OLLAMA_CLOUD_API_KEY, GOOGLE_PLACES_API_KEY, AIMLAPI_KEY) reported present/missing by NAME ONLY (NEVER print the value); wrangler auth (`wrangler whoami` reachable); a per-tier cloud canary (one tiny Ollama Cloud reachability ping + one Claude CLI reachability ping — report reachable/not, never print keys); muezzin-plugin git currency (clean/ahead/behind vs origin); governance dir (~/.claude) present with practice/core.md + CANON-MANIFEST.md. Conduct-critical = node + at least one cloud tier reachable + governance present.
- LAPTOP-SETUP.md is the runbook: clone ~/.claude (governance) + muezzin-plugin from git, set the env keys, `wrangler login`, run `node doctor.mjs`, then start the daemon + conduct. Includes the architecture note: the SOTA chain runs on Ollama Cloud + Claude from anywhere with the keys; the local-GPU fallback + GPU media (whisper/vision/Flux/ffmpeg) stay on the home 4090 or spill to cloud-GPU.
- No secret VALUE is ever printed or written by either file (presence-check only).

Constraints: code-repo, muezzin-plugin repo only, NAS untouched (do not reference 192.168.2.27 / N: / W:), secrets are checked-for-presence-only.

Steps:
1. edit doctor.mjs - implement the one-shot environment check per done-means (presence-only secret checks, cloud canary reachability, git currency, governance present; PASS/FAIL board; exit code). Acceptance: `node doctor.mjs` prints a PASS/FAIL board and exits cleanly on a healthy box; no secret value appears in output.
2. edit LAPTOP-SETUP.md - write the clone-and-go runbook per done-means incl. the cloud-first / home-4090 split. Acceptance: file exists and covers clone -> keys -> wrangler -> doctor -> daemon/conduct.

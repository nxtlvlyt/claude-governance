MISSION-ID: M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - workers/sitemap-prune/*.js
  - workers/sitemap-prune/wrangler.toml
PARENT: M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS  (sub-mission 1 of 2, auto-split by the size ceiling)
REQUIRES: none (first sub-mission of 2 in tartib order)

Niyyah: the sitemap-prune worker should survive production load. The original build assumed Cloudflare Workers limits the panel measured exceeded by 8x, and uncaught D1 awaits would surface as CF 1101 errors to users. Close the substrate-verified defects — not the model's confidence, the documented Cloudflare ceiling.

Maqsad: this is part 1 of 2 of the parent objective below — a right-sized slice the engine can decompose under the size ceiling without overwhelming a seat. Achieve ONLY this slice's work; the sibling sub-missions own the rest.
PARENT MAQSAD (the whole objective these sub-missions together serve): the worker runs safely on Cloudflare Workers Free plan AND Paid plan, with no silent failure modes. Re-running the 5-seat panel against the new diff returns panel_verdict in {pass, concern}.

THIS SUB-MISSION'S WORK (the parent decomposition's steps assigned to this slice — re-decompose them under the smaller budget; do not exceed the ceiling):
  - Ensure the sandbox is on the target branch before any edit so the closure commits land on feat/b13-sitemap-prune-2026-06-23.
  - Single authoring edit of the worker entrypoint (entrypoint name 'index.js' UNVERIFIED — file_read unavailable; executor must confirm the real *.js entrypoint and emit FULL file contents). Close all 7 panel defects in one writer (one-writer rule forbids splitting edits across the same file): (1) wrap top-level fetch() handler in try/catch returning structured {ok:false,error} JSON with 500 + application/json so no throw becomes CF 1101; (2) wrap the awaited upsert404 and writeCursor D1 calls in handleRefresh so a D1 failure returns structured JSON, not an uncaught throw; (3) narrow ensureSchema catch to set schemaEnsured=true ONLY on the benign 'already exists' class and rethrow all other errors; (4) cap BATCH_SIZE from 200 to 45 with cursor-based continuation (accept ?cursor, process [cursor,cursor+45), persist next cursor, return {done:false,next_cursor}) and use fetch(url,{redirect:'manual'}) so external fetches are provably 1 sitemap + 45 URL = 46 ≤ 50 Free ceiling (D1 calls draw from the separate 1000 services pool, verified against CF docs 2026-02-11); embed the arithmetic as a comment; (5) replace <loc> regex with /<(?:\w+:)?loc>(?:<!\[CDATA\[)?([\s\S]*?)(?:\]\]>)?<\/(?:\w+:)?loc>/g + trim to handle CDATA and namespaced tags; (6) atomic D1 mutex (conditional UPDATE on a lock row with changes-check + ~120s TTL) returning 409 {ok:false,error:'refresh_in_progress'} on contention, released in finally; (7) validate each parsed entry host via new URL() against the configured site domain and drop out-of-domain entries (SSRF guard).
  - Witness the subrequest cap: BATCH_SIZE is set to 45 (provable 46 ≤ 50 Free-plan external-fetch ceiling). Fails non-zero if the cap is absent.
  - Witness the concurrency guard: a 409 conflict response for interleaved /refresh exists in the worker. Fails non-zero if absent.
  - Witness the SSRF guard: per-entry host validation via URL hostname comparison exists in the worker. Fails non-zero if absent.
  - Stage the single modified worker file per-file (no git add .) and assert it is actually staged.
  - Commit the staged change with --no-verify and assert the commit landed by matching its subject in the log.
  - Push the branch and verify the remote ref exists by asserting a 40-char SHA is returned (exits non-zero if the remote ref is absent — reaches the resource, not a local file check).

Done means: steps M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1.1..M-MUDDYTIRES.B13-SITEMAP-PRUNE-CF-LIMITS.S1.8 are all complete, each with its validation_command receipt passing (the deed witnessed, not asserted); and the parent's done-condition holds for this slice — - The worker, when invoked, returns structured JSON on D1 failure rather than CF 1101.
  - Per /refresh subrequest count is mathematically provable to stay under 50 (Free plan ceiling).
  - ensureSchema does NOT mask unknown errors as "already exists".
  - Concurrent /refresh calls either serialize cleanly OR return a structured conflict response (no race-corrupted cursor).
  - URL host validation rejects out-of-domain sitemap entries.
  - Same panel re-fire returns pass/concern; node --check passes; push + ls-remote verify.

Amanah: this slice was split from a larger mission so it stays inside the chain's reliable working capacity (Q2:286 / mizan). Discharge it with ihsan; its receipt unlocks the next sibling.
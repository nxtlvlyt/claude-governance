MISSION-CLASS: code-repo
MISSION-ID: M-MUDDYTIRES.QC-CONCERN-QUICK-CHECK-IN-QUICK-CHECKIN-JS-2026-06-25
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
TARGET-BRANCH: feat/qc-concern-quick-check-in-quick-checkin-js-2026-06-25
ALLOW-FILES:
  - js/*.js
  - workers/**/*.js

Niyyah: address the QC CONCERN named "Quick check-in (quick-checkin.js)" from the 2026-06-25 walk.

Maqsad: GATE: "I'm here" button appears in popup for logged-in users — FAIL: No POI popup is open in either screenshot, and 401 console errors suggest the session is unauthorized/not logged in. GATE: Click posts to /api/contribute/checkin — FAIL: No interaction or check-in attempt is visible; screenshots are identical. The walk runs unauthenticated so the "I'm here" affordance is correctly hidden — but the gate still expects quick-checkin.js to be loaded and ready to mount the button when a logged-in user opens a popup, and to POST /api/contribute/checkin on click. Verify the script loads, the button mounts for logged-in sessions on popup-open, and that the POST fires with correct payload. Restore behavior to that specified in docs/FEATURE-CATALOG-2026-06-23.md.

Done means: e2e-runner walk returns {clean}; the concern is resolved; no other features regress.

Amanah: this slice is part of the muddytires.ca quality-uplift backlog.

MODEL-ROUTING: TIER_CLOUD_CODE — kimi-k2.7-code or deepseek-v4-pro for fix authoring.

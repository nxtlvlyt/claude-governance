MISSION-ID: V36-ANSWER-EXTRACTION-GROUNDTRUTH.S2
MISSION-CLASS: research
PARENT: V36-ANSWER-EXTRACTION-GROUNDTRUTH
TARTIB-INDEX: 2 of 3
REQUIRES: V36-ANSWER-EXTRACTION-GROUNDTRUTH.S1 (tartib — this sub-mission may not start until V36-ANSWER-EXTRACTION-GROUNDTRUTH.S1's receipt exists)
STEPS: 8

PARENT MAQSAD: for each of the 10 BFCL board misses below, establish (a) the correct final answer,

Maqsad: sub-mission 2 of 3 — Item web_search_base_49 (male artist whose track reached 1 billion Spotify streams fastest -> his record label): resolve hop chain via SearXNG + WebFetch, write standalone part-file or UNRESOLVED. through Hop-pattern grouping check — confirm at least two distinct named shape groups exist in HOP-PATTERNS.md (a single catch-all group would mean no real analysis happened).

PRE-STEP GUARDS (added 2026-08-07 by conductor after S1 shipped contaminated evidence).
Both failures below voided sub-mission S1 and are INHERITED by this file, because the
autosplit generated it from the parent BEFORE the parent was fixed. ornith:9b flagged this
artifact REVISE for "near-exact copy-paste of its parent spec". Its literal claim was wrong
(this file is 30 lines against the parent's 112, with its own steps 9-16) — but looking again
because it flagged is what surfaced these two real defects. That is what the witness is for.

  G1. LOAD THE REAL QUESTIONS — the item descriptions in the steps below are LOSSY POINTERS,
      not the questions. Before any search, run:
        Get-Content -Raw 'C:/Users/marka/conductor-qwen/phase4/v36-mining/MISS-SHAPES.json'
      and quote each question VERBATIM from its `question` field.
      In S1 this path was RELATIVE, never resolved (the executor reported ENOENT), and it
      reconstructed the questions from pointers exactly like these — silently changing them:
        id 20 REAL:          "the largest university IN THE U.S. BY ACREAGE in 2024"
        id 20 RECONSTRUCTED: "the world's largest university BY ENROLLMENT" -> IGNOU, India
      Different metric, country and entity, so every hop answered a question nobody asked and
      the whole output was quarantined as evidence-grade zero.
      If the file cannot be read, STOP and report that. Do NOT reconstruct a question.

  G2. PROBE SEARXNG — DO NOT ASSUME IT. Before your first search, run this and record the
      output in your part-file:
        powershell -NoProfile -Command "try { (Invoke-WebRequest -UseBasicParsing -Uri 'http://localhost:8080/' -TimeoutSec 8).StatusCode } catch { 'DOWN' }"
      If it prints 200, SearXNG is UP and MUST be used for every query — Anthropic WebSearch
      is then NOT permitted (operator ruling 2026-06-09). Only a DOWN/error output licenses a
      fallback, and that output must be pasted as the receipt for falling back.
      S1 named SearXNG five times in prose and used WebSearch for every single query while
      SearXNG was verified UP (HTTP 200). A rule that can be satisfied by not reading it is
      not a rule.

  NEVER INVENT A RECEIPT (teacher.faith.md). An honest UNRESOLVED, with what you tried and
  why it failed, beats a confident answer — a wrong row becomes a reflex in the student model
  that it cannot attribute or unlearn.

Steps:
  9. Item web_search_base_49 (male artist whose track reached 1 billion Spotify streams fastest -> his record label): resolve hop chain via SearXNG + WebFetch, write standalone part-file or UNRESOLVED. [edit] parts/part-49.md ; context_dependencies: C:/Users/marka/conductor-qwen/phase4/v36-mining/MISS-SHAPES.json
     validation_command: $c = Get-Content parts/part-49.md -Raw; if ($c -match '(?i)hop chain' -and $c -match '(SOURCE URL|UNRESOLVED)' -and $c.Length -gt 100) { Write-Output "PASS: part-49 complete" } else { throw "FAIL: part-49 missing required fields" }
  10. Item web_search_base_66 (winner of the referenced city's 2024 mayoral election -> their current spouse, noting fetch date): resolve hop chain via SearXNG + WebFetch, write standalone part-file or UNRESOLVED. [edit] parts/part-66.md ; context_dependencies: C:/Users/marka/conductor-qwen/phase4/v36-mining/MISS-SHAPES.json
     validation_command: $c = Get-Content parts/part-66.md -Raw; if ($c -match '(?i)hop chain' -and $c -match '(SOURCE URL|UNRESOLVED)' -and $c.Length -gt 100) { Write-Output "PASS: part-66 complete" } else { throw "FAIL: part-66 missing required fields" }
  11. Item web_search_base_84 (badminton-dominant country referenced -> its most populous city -> that city's mayor as of April 2025): resolve hop chain via SearXNG + WebFetch, write standalone part-file or UNRESOLVED. [edit] parts/part-84.md ; context_dependencies: C:/Users/marka/conductor-qwen/phase4/v36-mining/MISS-SHAPES.json
     validation_command: $c = Get-Content parts/part-84.md -Raw; if ($c -match '(?i)hop chain' -and $c -match '(SOURCE URL|UNRESOLVED)' -and $c.Length -gt 100) { Write-Output "PASS: part-84 complete" } else { throw "FAIL: part-84 missing required fields" }
  12. Item web_search_base_90 (losing nominee of the 2024 Palauan general election -> college attended -> college's president -> that president's spouse): resolve hop chain via SearXNG + WebFetch, write standalone part-file or UNRESOLVED. [edit] parts/part-90.md ; context_dependencies: C:/Users/marka/conductor-qwen/phase4/v36-mining/MISS-SHAPES.json
     validation_command: $c = Get-Content parts/part-90.md -Raw; if ($c -match '(?i)hop chain' -and $c -match '(SOURCE URL|UNRESOLVED)' -and $c.Length -gt 100) { Write-Output "PASS: part-90 complete" } else { throw "FAIL: part-90 missing required fields" }
  13. Assemble all 10 completed part-files, in mission order, into the single ANSWER-GROUNDTRUTH.md deliverable (one section per id, each carrying the full question, hop chain, final answer, source URL, search query, and confidence). [edit] ANSWER-GROUNDTRUTH.md ; context_dependencies: parts/part-20.md, parts/part-21.md, parts/part-31.md, parts/part-39.md, parts/part-40.md, parts/part-46.md, parts/part-49.md, parts/part-66.md, parts/part-84.md, parts/part-90.md
     validation_command: $c = Get-Content ANSWER-GROUNDTRUTH.md -Raw; $ids = 20,21,31,39,40,46,49,66,84,90; $missing = $ids | Where-Object { $c -notmatch "web_search_base_$_" }; if ($missing.Count -eq 0) { Write-Output "PASS: all 10 ids present" } else { throw "FAIL: missing ids $($missing -join ',')" }
  14. Anti-fabrication check — re-resolve every URL cited in ANSWER-GROUNDTRUTH.md live to confirm nothing was invented (a dead/unreachable URL fails the mission's zero-invented-sources bar). [verify] ANSWER-GROUNDTRUTH.md
     validation_command: $c = Get-Content ANSWER-GROUNDTRUTH.md -Raw; $urls = [regex]::Matches($c,'https?://[^\s\)\]"]+') | ForEach-Object { $_.Value } | Select-Object -Unique; $fail=@(); foreach ($u in $urls) { try { $r = Invoke-WebRequest -Uri $u -UseBasicParsing -TimeoutSec 15 -Method Head; if ($r.StatusCode -ge 400) { $fail += $u } } catch { try { $r2 = Invoke-WebRequest -Uri $u -UseBasicParsing -TimeoutSec 15 -Method Get; if ($r2.StatusCode -ge 400) { $fail += $u } } catch { $fail += $u } } }; if ($fail.Count -eq 0) { Write-Output "PASS: $($urls.Count) URLs resolve" } else { throw "FAIL: dead/unreachable URLs: $($fail -join ', ')" }
  15. Analyze the completed hop chains across all 10 items and write HOP-PATTERNS.md, grouping them into named recurring hop shapes (e.g. resolve-superlative-then-lookup-person, resolve-place-then-lookup-office) and stating which shape the model kept failing to complete. [edit] HOP-PATTERNS.md ; context_dependencies: ANSWER-GROUNDTRUTH.md
     validation_command: $c = Get-Content HOP-PATTERNS.md -Raw; $ids = 20,21,31,39,40,46,49,66,84,90; $missing = $ids | Where-Object { $c -notmatch "web_search_base_$_" }; if ($missing.Count -eq 0 -and $c -match '(?i)shape') { Write-Output "PASS: all 10 ids grouped into named shapes" } else { throw "FAIL: missing ids $($missing -join ',') or no named shapes" }
  16. Hop-pattern grouping check — confirm at least two distinct named shape groups exist in HOP-PATTERNS.md (a single catch-all group would mean no real analysis happened). [verify] HOP-PATTERNS.md
     validation_command: $c = Get-Content HOP-PATTERNS.md -Raw; $headers = [regex]::Matches($c, '(?m)^##\s+.+'); if ($headers.Count -ge 2) { Write-Output "PASS: $($headers.Count) distinct shape groups" } else { throw "FAIL: only $($headers.Count) shape group(s) found" }

Done means: parts/part-49.md, parts/part-66.md, parts/part-84.md, parts/part-90.md, ANSWER-GROUNDTRUTH.md, HOP-PATTERNS.md exist/are updated as specified

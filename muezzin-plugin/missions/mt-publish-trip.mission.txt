MISSION-ID: mt-publish-trip
MISSION-CLASS: code-repo
REPO-ROOT: C:\Users\marka\code\mt-integration-2026-06-22
ALLOW-FILES:
  - docs/TRIP-PUBLISH-DESIGN.md
  - workers/contribute-d1.js
  - workers/_test_contribute_d1_trips.mjs
  - d1/migrate-trips-2026-07-19.sql
  - trip.html
  - js/trip-view.js
  - timeline.html
  - js/timeline-map.js
  - tests/trip-view.selftest.js
REQUIRES: mt-display-names
STEPS: 6

ONE-WRITER RULE (binding on the plan): every file above is AUTHORED by exactly
ONE step; later steps only READ/verify (context_dependencies, action_type
'verify'). No final integration/rewrite step.

MAQSAD (the end): a traveller who analyzed their Google Timeline on
/timeline.html can PUBLISH the trip as a shareable public page — actual driven
tracks + confirmed stops on a MapLibre map at a stable URL. "Share your life
on the road" as an artifact no competitor has.

NIYYAH (why): operator direction 2026-07-18 ("more of a social media appeal...
users would spend more time on it and share their life on it"). The timeline
chain (M1-M5, LANDED 2026-07-18: e9efc90/6429e00/d61bd37/d65c878/7d248c3)
already produces the map; this adds persistence + a public render.
source: js/timeline-map.js
source: js/timeline-geojson.js
source: workers/contribute-d1.js

STEPS:
REACHABILITY NOTE (miqat): trip.html is a SHARE-TARGET page, reached through
the publish flow on timeline.html — an EXISTING nav-linked page. Step 5
explicitly renders the returned public URL as a visible, copyable link on
timeline.html post-publish: that link IS the inbound path (NAV_LINK evidence
lives in step 5's authored UI). Beyond the publish flow, trip.html is
UNLINKED-OK BY DESIGN: slugs are unguessable and non-enumerable (step 1), so
no public nav/index listing may exist — listing published trips anywhere
public would defeat the unguessability the design mandates; distribution is
the owner sharing their own link.

1. [read-only] Read js/timeline-map.js, js/timeline-geojson.js,
   workers/contribute-d1.js (ownership pattern from updateSpot/deleteSpot),
   d1/schema-write.sql. AUTHOR docs/TRIP-PUBLISH-DESIGN.md: trips table DDL
   (slug, owner email, title, geojson TEXT size-capped ~1MB with
   simplification rule for over-cap paths, created_at, status live/removed),
   slug scheme (unguessable, not enumerable), the SECOND consent screen copy
   (publishing is broader than analyzing — states plainly the trip becomes
   public), unpublish semantics.
2. AUTHOR d1/migrate-trips-2026-07-19.sql per the design.
3. workers/contribute-d1.js: POST /trip/publish (verified session, size cap,
   slug gen), GET /trip/<slug> (public: geojson + title + display_name byline
   via the mt-display-names join — owner EMAIL never emitted), POST
   /trip/delete (owner-only, verbatim the updateSpot ownership shape).
4. AUTHOR trip.html + js/trip-view.js: MapLibre render (same layer/popup
   styling as js/timeline-map.js but read-only — no save section), byline,
   OG meta for link unfurls. AUTHOR tests/trip-view.selftest.js (pure helpers:
   slug validation, byline rendering inert to markup).
5. timeline.html + js/timeline-map.js: "Publish this trip" button
   post-analysis (signed-in only) with the step-1 consent screen before any
   POST; on publish success, render the returned public URL as a visible,
   copyable link (textContent, copy button) — this authored link is the
   inbound path satisfying the reachability contract for trip.html.
6. OUTCOME CHECK [command]: node workers/_test_contribute_d1_trips.mjs exit 0;
   node tests/trip-view.selftest.js exit 0; node --check all authored js.
   Verify-only — authors nothing.

DONE-MEANS: publish -> stable public URL renders tracks+stops signed-out;
unpublish removes it; consent precedes publish; email never surfaces; tests
green. Deploys + live witness are CONDUCTOR-CALLED after landing.
